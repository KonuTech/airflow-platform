---
context: research
phase: none (Phase 7 complete, Phase 8 not started)
task: n/a
total_tasks: n/a
status: in_progress
last_updated: 2026-08-16T19:51:23.587Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

None identified as blocking. See Advisory item below.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming max_active_runs=1 pipeline is "fixed" from one clean E2E pass | The scheduler CPU-leak fix (commit `6ea4129`) genuinely resolved the *total paralysis* bug, but a residual `wait_for_files`/`resolve_window` stuck-in-queued flakiness still occurs intermittently. Because `csv_ingest_customers` has `max_active_runs=1`, a single stuck run blocks ALL new file discovery until cleared — so E2E tests that upload a file and poll will fail with "discovery never registered" whenever this hits, even though the underlying pipeline is healthy. Two consecutive full E2E suite runs (9m46s each) hit this identically. | advisory | Before trusting an E2E test failure as a real regression, first check `DagRun`/`TaskInstance` state via the scheduler pod (see Infrastructure State below) for a stuck `up_for_retry`/`queued` task before assuming the test's own assertion logic is wrong. |

<current_state>
Phase 7 (Observability, Metrics, Tracing & Lineage) is fully complete and pushed to `origin/main` (commit `1d75a92`, plus a post-restart recovery + review-fix commit trail after that). All 9 plans done, including gap-closure 07-09. Phase 8 (Validation, Quarantine & Metadata Control Plane Completion) has not been started — no `discuss-phase`/`plan-phase` run yet.

Mid-session, the user's laptop froze from CPU/RAM exhaustion and required a restart. This broke the kind cluster's DAGs hostPath mount again (`DagModel.is_stale=True`, same failure class as the Phase 5 incident) — already fixed live via `docker restart` on all 3 kind nodes; confirmed working again.

After the restart, per the user's request, we deliberately re-ran the heaviest E2E test load (the full `tests/e2e/observability/` suite) under host-memory monitoring to check whether the freeze was resource-related. **It was not**: memory stayed flat at ~16GB available across two consecutive 9m46s runs, zero swap. The host-freeze root cause remains unknown/unconfirmed (possibly unrelated concurrent host activity, or a Docker Desktop/WSL2 quirk), but this specific test workload does not reproduce it.

However, both full-suite runs hit the SAME 4 test failures (`test_forced_freshness_breach_delivers_a_real_webhook_post`, `test_prometheus_scrapes_dataplat_metrics_via_the_otel_collector`, `test_ingest_pod_traceparent_matches_persisted_trace_id`, `test_ingest_pod_dag_context_matches_persisted_lineage_row`), all with "discovery never registered it" — traced to `wait_for_files`/`resolve_window` getting stuck `up_for_retry` with no pod, exactly the residual risk the `/gsd:debug` session (`.planning/debug/resolved/airflow-scheduler-stuck-tasks.md`) explicitly flagged as NOT fully eliminated (KPO's `startup_timeout_seconds` racing tight CPU). Cleared the stuck task instance for the most recent occurrence (safe, same intervention validated in the debug session) but did not re-run the E2E suite again afterward — ran out of context budget.
</current_state>

<completed_work>

- Phase 7 gap-closure (07-09): dag_id/dag_run_id/task_id threaded end-to-end into `meta.v_customers_lineage`, code-reviewed and fixed (4 findings), live-verified via `test_ingest_pod_dag_context_matches_persisted_lineage_row` passing once.
- Airflow scheduler CPU-exhaustion root cause diagnosed via `/gsd:debug` and fixed (commit `6ea4129`): `kpo.py`'s `on_finish_action` → `delete_pod` (closes xcom-sidecar leak), `ingest`'s `max_active_tis_per_dag` 5→1.
- `meta.v_customers_lineage` SELECT grant to `analytics_owner` added (migration `0013`, commit `1d75a92`) — was blocking the E2E test's own final assertion query.
- All work committed and pushed to `origin/main`.
- Post-restart recovery: kind nodes restarted, DAGs mount reattached, `DagModel.is_stale` confirmed `False`.
- Host-freeze resource investigation: confirmed NOT reproduced by the E2E test load (2× 9m46s runs, memory flat, no swap).
</completed_work>

<remaining_work>

1. **Resolve the residual `wait_for_files`/`resolve_window` stuck-task flakiness** — this is a real, not-yet-fixed gap in the earlier debug session's fix. Options: resume/reopen `.planning/debug/resolved/airflow-scheduler-stuck-tasks.md` (move back out of `resolved/`, it's not fully resolved) as a new debug session; likely needs tuning `startup_timeout_seconds` on the sensor/KPO tasks, or further node-CPU-budget work.
2. Once stable, re-run `tests/e2e/observability/` in full and confirm all 7 tests pass cleanly (currently 3/7 passing reliably: `test_alert_webhook_delivery` other test, plus the two lineage tests passed once each in isolation but not together in the full suite under load).
3. Then start Phase 8: `/gsd:discuss-phase 8`.
</remaining_work>

<decisions_made>

- Chose to accept OBS-07's live E2E gap via a documented `07-VERIFICATION.md` override rather than block Phase 7 completion on a scheduler bug unrelated to the code fix — then closed that override for real once the scheduler fix + a passing live run were both confirmed.
- Chose to deliberately re-run the heaviest E2E load under monitoring (rather than skip verification or blindly re-trigger without safeguards) to answer the host-freeze question safely.
- Cleared the currently-stuck task instance directly via the scheduler pod's ORM session (same pattern validated in the debug session) rather than digging further, given context budget — this is a stopgap, not a fix for the underlying flakiness.
</decisions_made>

<blockers>
- Residual Airflow `wait_for_files`/`resolve_window` stuck-task flakiness (see remaining_work #1) — not blocking Phase 7 (already shipped with an accepted override), but blocks getting a fully-clean E2E suite pass.
</blockers>

## Required Reading (in order)
1. `.planning/debug/resolved/airflow-scheduler-stuck-tasks.md` — the prior debug session's root cause and fix; read its "Residual risk" note before starting new investigation.
2. `.planning/STATE.md`'s Blockers/Concerns section — has the most current summary.
3. `.planning/phases/07-observability-metrics-tracing-lineage/07-VERIFICATION.md` — the OBS-07 override this residual flakiness relates to (now should be fully closeable given the live pass).

## Infrastructure State
- Local kind cluster: healthy, 3 nodes Ready, DAGs mount fresh (not stale).
- `csv_ingest_customers` DAG: unpaused, `max_active_runs=1`. Check current state before assuming anything is broken:
  ```
  kubectl -n airflow exec deploy/airflow-scheduler -c scheduler -- python3 -c "
  from airflow.models import DagRun, TaskInstance
  from airflow.utils.session import create_session
  with create_session() as session:
      runs = session.query(DagRun).filter(DagRun.dag_id=='csv_ingest_customers').order_by(DagRun.start_date.desc()).limit(5).all()
      for r in runs: print(r.run_id, r.state)
  "
  ```
- Registry images: `localhost:5001/csv-processor:4872fd6` (post-review-fix), `localhost:5001/airflow:7403b96` — both current, deployed.
- No orphaned/stale pods as of last check (cleaned up post-restart).

<context>
Phase 7 is genuinely, functionally done — the OBS-07 gap-closure code is correct and has been proven live at least once. The open thread is purely about the *test infrastructure's* reliability under this project's own `max_active_runs=1` design, not about Phase 7's own deliverable. Don't re-litigate Phase 7 content; just finish stabilizing the E2E suite (or accept it as inherently flaky under this DAG's concurrency design and move on), then start Phase 8.
</context>

<next_action>
Start with: check current DagRun/TaskInstance state (see Infrastructure State above) to see if the pipeline has self-stabilized since last checked. If clean, re-run `uv run --frozen --group cluster pytest tests/e2e/observability/ -m cluster -q` once more. If the same stuck-task pattern recurs, open a fresh debug session focused specifically on tuning `startup_timeout_seconds`/sensor retry behavior rather than re-diagnosing from scratch.
</next_action>
