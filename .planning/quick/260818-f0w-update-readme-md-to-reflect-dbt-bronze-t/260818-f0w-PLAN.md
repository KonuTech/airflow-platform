---
phase: quick-260818-update-readme-md-to-reflect-dbt-bronze-silver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - "Reflect the locked dbt-bronze-to-silver architecture decision (.planning/notes/dbt-silver-layer-architecture-decision.md, 2026-08-18) throughout README.md's existing sections, in place -- not as an appended section -- per the user's explicit instruction: 'Do not just append this change in approach. Update the points, plan, schemas already inputted in README.md.' Phase 08.1 (ROADMAP.md, inserted between Phase 8 and Phase 9) already tracks the implementation work this documents; this quick task is documentation-only."

must_haves:
  truths:
    - "README.md's architecture diagram (section 2), Analytical PostgreSQL description (section 4.2) and processing-flow diagram (section 7) show a bronze (staging) -> silver (dbt) -> warehouse (gold) pipeline, not a two-layer staging->warehouse pipeline"
    - "README.md's transformation/staging sections (26, 27, 32, 35, 36, 63) describe dbt (Postgres adapter) as the bronze-to-silver transformation owner and the existing Python MergePublisher as the unchanged, sole gold-publish owner, citing PostgreSQL BUG #18279 as the reason dbt never publishes gold"
    - "README.md's SCD sections (54-61) explicitly state SCD Type 2 remains Python-owned and that dbt snapshot was evaluated and rejected for it"
    - "README.md's Repository Structure (75), Development Roadmap (92) and Definition of Done (94) reflect a dbt/ project directory, a dbt bronze-to-silver roadmap step, and dbt-aware Definition-of-Done items"
    - "No existing top-level '# N.' section in README.md is renumbered, added, or removed -- external cross-references from STACK.md/PROJECT.md/ROADMAP.md (e.g. section 5, 81.9, 94.99) remain valid, and Definition of Done items 1-114 keep their original numbers"
    - "Content describing what Phases 1-8 already built is left factually accurate -- this is a forward-looking architecture update, not a historical rewrite"
  artifacts:
    - path: "README.md"
      provides: "Master specification with dbt bronze-to-silver transformation integrated into existing architecture, staging/publication, deduplication, SCD, repository-structure, roadmap and Definition-of-Done sections"
      contains: "dbt (Postgres adapter)"
  key_links:
    - from: "README.md section 36 (Staging and Atomic Publication)"
      to: "README.md section 35 (Transactional Loading)"
      via: "shared reference to PostgreSQL BUG #18279 and the Python MergePublisher as the sole gold-publish mechanism"
      pattern: "BUG #18279"
    - from: "README.md section 54 (Slowly Changing Dimensions)"
      to: "README.md section 36 (Staging and Atomic Publication)"
      via: "shared dbt-ownership-boundary language (dbt owns silver, never gold/SCD)"
      pattern: "dbt snapshot"
    - from: "README.md section 75 (Repository Structure)"
      to: "README.md section 36 (Staging and Atomic Publication)"
      via: "a dbt/ project directory whose purpose matches the bronze-to-silver stage described in the pipeline diagram"
      pattern: "dbt/"
---

<objective>
Rewrite README.md (the 3,386-line master specification) in place so every passage that currently
describes the raw -> staging -> normalized -> warehouse transformation pipeline as a pure-Python
flow instead reflects the locked architecture decision: dbt (Postgres adapter) owns bronze-to-
silver transformation only (cleaning, deduplication, late-arriving-event resolution), landing in a
new persisted silver schema inside the existing analytical PostgreSQL. Bronze (raw CSV files on
MinIO, immutable, append-only) is unchanged -- dbt never touches raw ingestion. Gold (the
business-ready normalized/warehouse tables, including SCD Type 2) stays owned by the existing
Python MergePublisher (pg_advisory_xact_lock + INSERT ... ON CONFLICT, inside the single META-03
transaction) -- never dbt, because dbt-postgres's merge incremental strategy compiles to a real
PostgreSQL MERGE statement that collides with a documented, verified concurrency bug (BUG #18279)
this project already worked around. dbt snapshot was evaluated and explicitly rejected for SCD
Type 2 for the same transactional reasons.

Purpose: README.md is the master specification this whole platform is built against. Every
planning document already reflects this decision (ROADMAP.md's inserted Phase 08.1, PROJECT.md,
REQUIREMENTS.md) except README.md itself, which the user explicitly does not want silently
appended to -- every place that currently models the old pure-Python two-layer (staging/warehouse)
pipeline must be edited in place so the document stays internally consistent.

Output: README.md updated across its architecture diagrams, transformation/staging/dedup/SCD
numbered sections, repository structure, development roadmap, and Definition of Done -- with zero
top-level section renumbering (preserving every existing "section N" cross-reference used
elsewhere in the repo's planning documents).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/notes/dbt-silver-layer-architecture-decision.md
@README.md

<roadmap_excerpt>
ROADMAP.md's inserted Phase 08.1 (between Phase 8 and Phase 9), for reference only -- already
committed, not in scope to edit in this task:

### Phase 08.1: dbt Silver Transformation Layer (INSERTED)

Goal: A dbt (Postgres adapter) bronze-to-silver transformation stage exists, producing a
persisted, deduplicated silver schema with correct late-arriving-event handling -- while the
existing Python MergePublisher continues to own the atomic silver-to-gold publish inside the
META-03 transaction, unchanged.
Depends on: Phase 8. Plans: 0 (not yet planned -- out of scope for this quick task).
</roadmap_excerpt>

<critical_constraint>
README.md uses 95 top-level numbered sections ("# N. Title", or, for section 1 only,
"## 1. Title") running from 1 to 95, plus a separately-numbered 1-114 Definition of Done list
inside section 94. These numbers are cited extensively and verbatim by other already-committed
planning documents (STACK.md cites, among others, section 5, section 9-21, section 39, section 67,
section 77, section 81.9, section 92, section 94.99; PROJECT.md and ROADMAP.md cite section 36 and
the 54-61 range). Every task in this plan MUST edit prose/diagrams WITHIN existing sections --
never insert a new top-level "# N. Title" header, never renumber an existing one, and never
renumber an existing Definition of Done list item (1-114). New Definition of Done content must be
appended as new items after item 114, still inside section 94's own list, not as a new top-level
section. Verify this constraint holds after every task using the automated checks provided.
</critical_constraint>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update the high-level architecture diagrams and data-flow (sections 2, 4.2, 7)</name>
  <files>README.md</files>
  <action>
    Locate each target section by its header text (grep for the header string below) rather than
    by the original line numbers given here for orientation only -- line numbers will drift once
    you start editing.

    1. Section 2, "Target Architecture" (fenced ASCII diagram). Inside the existing diagram's
       "PostgreSQL / Analytical DB" box, the three inner label lines currently read "staging",
       "warehouse", "analytics". Relabel them "staging (bronze)", "silver (dbt)", "warehouse
       (gold)", keeping the box's existing width/box-drawing style, widening the box only if the
       new labels no longer fit, and keeping it visually consistent with the sibling Airflow/MinIO
       boxes in the same diagram. Immediately after the diagram's closing fence (before the "---"
       separator), add one short prose paragraph (not a diagram) stating that dbt (Postgres
       adapter) performs the bronze-to-silver transformation inside the Analytical PostgreSQL box
       shown above, gold publish remains the existing Python MergePublisher, and pointing to
       section 36 for the full pipeline. Do not touch the Airflow, MinIO, Secrets Manager or CI/CD
       portions of this diagram.

    2. Section "4.2 Analytical PostgreSQL" (ends before section 5, "MinIO / Data Lake"). Replace
       the bullet list currently reading "staging data / normalized data / warehouse data /
       analytical tables / ingestion metadata / data-quality metadata / schema metadata / SCD
       dimensions" with a list naming three schema layers and their owner explicitly:
       staging/bronze (raw, append-only landing tables -- owned by the Python CSV Processor);
       silver (cleaned, deduplicated, late-arriving-event-resolved tables -- owned by dbt, Postgres
       adapter, a new schema introduced for this pipeline); warehouse/gold (normalized,
       business-ready tables including SCD dimensions -- owned exclusively by the Python
       MergePublisher via INSERT ... ON CONFLICT inside the single META-03 transaction, never via
       dbt's merge incremental strategy). Keep analytical tables, ingestion metadata, data-quality
       metadata and schema metadata in the list as Python/meta-schema-owned, unchanged. Then
       replace the small fenced diagram below it (currently "CSV Processor -> Analytical
       PostgreSQL") with a five-stage chain: CSV Processor -> Analytical PostgreSQL
       (staging/bronze) -> dbt (Postgres adapter, bronze-to-silver) -> Analytical PostgreSQL
       (silver) -> Python MergePublisher (silver-to-gold, INSERT ... ON CONFLICT) -> Analytical
       PostgreSQL (warehouse/gold). After the existing closing sentence about separation remaining
       clear even inside one cluster, add one sentence noting that within the Analytical PostgreSQL
       instance the bronze/staging, silver and warehouse/gold layers are separate schemas with
       separate role grants -- dbt's role gets read access to staging and write access only to
       silver; gold writes remain exclusively the Python publisher's -- cross-referencing section
       81.6 (least privilege).

    3. Section 7, "First Workload -- Universal CSV Processing" (fenced processing-flow diagram).
       Keep every stage from "CSV in MinIO" through "Normalization" exactly as-is (unchanged,
       already-built Python stages). Remove the standalone "Deduplication" stage that currently
       sits between Normalization and Transactional Load -- cross-file/cross-batch deduplication is
       no longer a pre-load Python step; it becomes part of the dbt bronze-to-silver stage described
       next. After "Normalization" the chain becomes: Transactional Load (into PostgreSQL
       staging/bronze) -> the existing VALID/INVALID branch -> PostgreSQL (staging) / Quarantine.
       From PostgreSQL (staging), add a new downstream chain: dbt (Postgres adapter): bronze ->
       silver -- cleaning, deduplication, late-arriving-event resolution -> PostgreSQL (silver) ->
       Python MergePublisher: silver -> gold -- atomic publication (INSERT ... ON CONFLICT, single
       transaction with watermark + run-status) -> PostgreSQL (warehouse/gold). Preserve the
       existing plain vertical-arrow visual style used by this diagram (no box-drawing characters
       here, matching the current file).

    Do not edit section 1 ("Project Objective") -- its content is generic practice-area framing and
    introduces no factual inconsistency with this change.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && sed -n '/^#\+ 2\. Target Architecture/,/^#\+ 3\. Mandatory Architectural Decisions/p' README.md | grep -qi "silver" && sed -n '/^#\+ 4\.2 Analytical PostgreSQL/,/^#\+ 5\. MinIO/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 4\.2 Analytical PostgreSQL/,/^#\+ 5\. MinIO/p' README.md | grep -qi "MergePublisher" && sed -n '/^#\+ 7\. First Workload/,/^#\+ 8\. Filename Parsing/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 7\. First Workload/,/^#\+ 8\. Filename Parsing/p' README.md | grep -qi "silver" && echo PASS || (echo FAIL; exit 1)</automated>
  </verify>
  <done>Section 2's diagram relabels the Analytical DB box as bronze/silver/gold and gains a short prose note about dbt's role; section 4.2 names the three schema layers and their owners and shows a five-stage chain diagram; section 7's processing-flow diagram routes staging through a new dbt bronze-to-silver stage into a Python gold publish, with the old pre-load Deduplication stage removed.</done>
</task>
<task type="auto">
  <name>Task 2: Update transformation, deduplication, staging and SCD sections (26, 27, 32, 35, 36, 54, 58, 63)</name>
  <files>README.md</files>
  <action>
    Locate each section by header text; line numbers below are original-file orientation only and
    will have drifted after Task 1's edits.

    1. Section 26, "Deduplication Between CSV Batches". Before the existing "Deduplication must
       therefore work: inside a single CSV / across CSV files / across batches / across ingestion
       runs" list, add a short paragraph stating that cross-file, cross-batch and cross-run
       deduplication (the strategies enumerated below) is implemented as dbt (Postgres adapter)
       models operating on the persisted staging/bronze schema and landing in silver -- not as an
       in-memory Python step before the transactional load (cross-reference section 36).
       Within-single-CSV structural checks may still happen in the Python CSV processor ahead of
       the load. Keep the existing strategy list (exact-row, business-key, business-key+timestamp,
       latest-record-wins, source-priority, batch-aware) unchanged -- these strategy concepts are
       still valid, dbt just implements them at a different point in the pipeline. After the list,
       add one sentence: the strategy remains dataset-specific and is expressed as dbt model
       configuration. Keep the closing "Never assume DISTINCT is sufficient" sentence.

    2. Section 27, "Deduplication Auditability". After the existing tracked-fields list, add a
       paragraph stating that deduplication auditability for the dbt-owned silver stage is tracked
       the same way as for the Python-owned bronze and gold stages -- via the meta.dedup_audit
       table (already designed, section 65) -- so lineage does not stop at dbt's boundary
       (cross-reference section 83, Data Lineage). Keep the existing "Never silently discard
       records" sentence.

    3. Section 32, "Late-Arriving and Out-of-Order Data". After the existing content, add a
       paragraph: bronze ingestion (file discovery, validation, transactional load into staging)
       treats every arriving file the same regardless of its event timestamp -- late/out-of-order
       records are never rejected at this layer. Resolution of late-arriving and out-of-order
       events into correct historical state happens in the dbt bronze-to-silver stage (section 36),
       using the record's event/business timestamp, not ingestion time.

    4. Section 35, "Transactional Loading". After the existing bullet list (transactions / staging
       tables / MERGE-upsert / database constraints / atomic swaps / commit boundaries) and its
       closing sentence, add a paragraph: the atomic silver-to-gold publish (section 36) never uses
       SQL MERGE -- including dbt-postgres's merge incremental strategy -- because of a documented,
       verified PostgreSQL concurrency bug (BUG #18279) where two concurrent MERGE transactions can
       each decide independently that no matching row exists and both attempt an insert. The
       existing Python MergePublisher uses pg_advisory_xact_lock + INSERT ... ON CONFLICT instead,
       inside the single transaction that also advances the watermark and run status (section 28).

    5. Section 36, "Staging and Atomic Publication". Replace the existing fenced diagram (MinIO ->
       staging -> validation -> transformation -> PostgreSQL staging table -> validation -> atomic
       publication -> warehouse/target table) with: MinIO (raw/bronze, immutable) -> CSV Processor:
       discovery, validation, normalization -> PostgreSQL staging table (bronze) -> dbt (Postgres
       adapter): bronze -> silver -- cleaning, deduplication, late-arriving-event resolution ->
       PostgreSQL silver schema (persisted) -> Python MergePublisher: silver -> gold -- atomic
       publication (pg_advisory_xact_lock + INSERT ... ON CONFLICT, single transaction with
       watermark + run-status) -> warehouse/target table (gold). Keep the existing plain
       vertical-arrow style. Keep the closing "Consumers should not see partially loaded datasets"
       sentence; you may append " -- true at every layer boundary: bronze/staging, silver, and
       warehouse/gold." to it.

    6. Section 54, "Slowly Changing Dimensions (SCD)". After the existing Type 2 example (the
       customer_id/country/valid_from/valid_to/is_current table) and before the "---" separator,
       add an "Implementation note:" paragraph stating that SCD Type 2 (and Types 0/1) remain
       entirely Python-owned, even though dbt (Postgres adapter) owns bronze-to-silver
       transformation elsewhere in this pipeline (section 36). dbt snapshot was evaluated for SCD
       Type 2 and explicitly rejected: dbt's own documentation states snapshots are not a
       replacement for CDC or event streaming -- they re-diff a source table's current state rather
       than consuming an ordered CDC event stream with explicit operation/sequence/transaction-ID
       semantics (sections 29, 30), which is what late-arriving SCD corrections (section 58)
       require. SCD2's historized dimension tables also carry the same single-transaction
       requirement as gold publish (sections 35, 36, META-03) that keeps dbt out of the gold layer
       generally.

    7. Section 58, "Late-Arriving SCD Changes". After the existing content, add one sentence: see
       the implementation note in section 54 -- historical corrections here are Python-owned; dbt
       snapshot was evaluated and rejected for this exact scenario.

    8. Section 63, "Immutable Raw Layer". Replace the existing fenced diagram's four labels (RAW /
       STAGING / NORMALIZED / WAREHOUSE) with: RAW (MinIO, immutable) / STAGING (PostgreSQL, bronze
       -- CSV Processor) / SILVER (PostgreSQL, dbt -- cleaning, dedup, late-arriving resolution) /
       WAREHOUSE (PostgreSQL, gold -- Python MergePublisher). Keep the "immutable" annotation on the
       RAW -> STAGING arrow and the rest of the section's text (overwrite prohibition, corrections
       via new files/versions/reprocessing) unchanged.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && sed -n '/^#\+ 26\. Deduplication Between CSV Batches/,/^#\+ 27\. Deduplication Auditability/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 27\. Deduplication Auditability/,/^#\+ 28\. Incremental Processing/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 32\. Late-Arriving and Out-of-Order Data/,/^#\+ 33\. Backfilling/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 35\. Transactional Loading/,/^#\+ 37\. Partial Failure and Recovery/p' README.md | grep -qi "BUG #18279" && sed -n '/^#\+ 35\. Transactional Loading/,/^#\+ 37\. Partial Failure and Recovery/p' README.md | grep -qi "silver" && sed -n '/^#\+ 54\. Slowly Changing Dimensions/,/^#\+ 55\. SCD Change Detection/p' README.md | grep -qi "dbt snapshot" && sed -n '/^#\+ 58\. Late-Arriving SCD Changes/,/^#\+ 59\. SCD + CDC/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 63\. Immutable Raw Layer/,/^#\+ 64\. Data Retention/p' README.md | grep -qi "dbt" && echo PASS || (echo FAIL; exit 1)</automated>
  </verify>
  <done>Sections 26, 27, 32, 35, 36, 54, 58 and 63 all describe dbt as the bronze-to-silver owner, cite BUG #18279 as the reason gold publish never uses SQL MERGE, and section 54 records the dbt-snapshot rejection for SCD Type 2, all without adding or renumbering any top-level section.</done>
</task>
<task type="auto">
  <name>Task 3: Update repository structure, library-architecture note, roadmap and Definition of Done (68, 75, 92, 94)</name>
  <files>README.md</files>
  <action>
    Locate each section by header text; line numbers below are original-file orientation only and
    will have drifted after Tasks 1-2's edits.

    1. Section 68, "Python Library Architecture". After the existing csv_processor/ directory tree
       and before the closing "Keep business logic out of DAG files wherever practical" sentence,
       add one short note: dbt (Postgres adapter) owns bronze-to-silver transformation as a
       separate dbt/ project outside this package (see section 75) -- csv_processor remains
       responsible for bronze ingestion (discovery through normalization and transactional load
       into staging) and never re-implements dbt's transformation logic.

    2. Section 75, "Repository Structure". Inside the existing repository-tree fenced code block
       (the one starting "airflow-etl-platform/"), insert a new top-level dbt/ entry positioned
       after the csv_processor/ block and before schemas/, matching the tree's existing indentation
       and box-drawing characters (those already used for csv_processor/'s subdirectories).
       Contents to include, at minimum: models/staging/ (a short inline comment: bronze source
       definitions, read-only) and models/silver/ (a short inline comment: cleaned, deduplicated,
       late-arriving-resolved models), plus dbt_project.yml and profiles.yml as sibling files under
       dbt/. Do not add a models/warehouse/ or models/gold/ directory -- gold stays outside dbt
       entirely, owned by csv_processor's existing storage/postgres.py path.

    3. Section 92, "Development Roadmap", subsection "## Phase 8 -- Production-Like Data
       Engineering". Add one new bullet to the existing list (idempotency / checksums / retries /
       ... / lineage / observability): dbt bronze-to-silver transformation (cleaning, deduplication,
       late-arriving-event resolution). Then, in subsection "## Phase 9 -- CDC and SCD", after the
       existing bullet list, add one short parenthetical line: (SCD Type 2 remains Python-owned;
       dbt snapshot was evaluated and rejected -- see section 54.)

    4. Section 94, "Definition of Done". Edit exactly two existing numbered items in place,
       keeping their original numbers unchanged:
       - Item 40 ("Dataset-specific deduplication strategies are supported.") -> append: "
         Cross-batch/cross-file deduplication into the silver layer is implemented as dbt models
         (Postgres adapter); a Python-owned secondary safety net may layer on top per Phase 9's
         dedup-strategy decision."
       - Item 52 ("Transactional/atomic loading is supported.") -> append: " The silver-to-gold
         publish stays the existing Python MergePublisher (pg_advisory_xact_lock + INSERT ... ON
         CONFLICT), never dbt's SQL MERGE, per the PostgreSQL MERGE concurrency bug (BUG #18279)."
       Do not renumber items 1-114 or change any other item's text. After item 114 (the last item,
       "Analytical data can be rebuilt from immutable raw data where practical."), still inside
       section 94's numbered list and before the section's closing "---" separator, append four new
       numbered items continuing the sequence (115-118):
       115. Bronze-to-silver transformation (cleaning, deduplication, late-arriving-event
            resolution) is implemented as dbt (Postgres adapter) models, landing in a persisted
            silver schema in the analytical PostgreSQL.
       116. Silver-to-gold publish remains exclusively Python-owned (MergePublisher), using
            INSERT ... ON CONFLICT inside the single META-03 transaction with watermark and
            run-status -- dbt's merge incremental strategy (SQL MERGE) is never used against gold,
            avoiding PostgreSQL BUG #18279.
       117. SCD Type 2 (sections 54-61) remains entirely Python-owned; dbt snapshot was evaluated
            and explicitly rejected as a CDC/event-stream replacement.
       118. The dbt role follows least privilege: read access to staging/bronze schemas, write
            access only to its own silver schema (section 81.6).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && test "$(grep -cE '^#{1,2} [0-9]+\.' README.md)" -eq 95 && grep -q "^114\. Analytical data can be rebuilt from immutable raw data where practical\." README.md && grep -qE "^115\." README.md && grep -qE "^118\." README.md && grep -qE "^40\..*dbt" README.md && grep -qE "^52\..*(MERGE|BUG #18279)" README.md && sed -n '/^#\+ 75\. Repository Structure/,/^#\+ 76\. CI\/CD and DevOps/p' README.md | grep -qi "dbt/" && sed -n '/^#\+ 92\. Development Roadmap/,/^#\+ 93\. Important Engineering Principle/p' README.md | grep -qi "dbt" && sed -n '/^#\+ 68\. Python Library Architecture/,/^#\+ 69\. Python Engineering Standards/p' README.md | grep -qi "dbt" && echo PASS || (echo FAIL; exit 1)</automated>
  </verify>
  <done>Section 68 notes the dbt/csv_processor boundary; section 75's repository tree includes a dbt/ project directory with staging/silver model folders; section 92's Phase 8 bullet list and Phase 9 note mention dbt; section 94's items 40 and 52 are amended in place and four new items (115-118) are appended after item 114, with the total top-level section count still exactly 95.</done>
</task>

</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None introduced | This is a documentation-only change to README.md, the master specification. No code, credentials, dependencies, or running system are touched -- no new trust boundary is created. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-quick-01 | Tampering (informational integrity, not a security control) | README.md's architecture description | accept | The only real risk is the document becoming internally inconsistent (some sections describing pure-Python transformation, others describing dbt-owned silver). Mitigated by this plan's own structure: every task's automated verify checks that the relevant sections mention dbt/silver/BUG #18279 as appropriate, and Task 3's verify additionally confirms the top-level section count stays at exactly 95 (no accidental renumbering that would break existing cross-references from STACK.md/PROJECT.md/ROADMAP.md). |
| T-quick-02 | Tampering (accepted, no code path affected) | README.md is prose/documentation, not executable code or a package-manager install target | accept | No new dependency, no new package install, no new external service integration is introduced by this change -- the npm/pip/cargo package-legitimacy gate does not apply. |
</threat_model>

<verification>
1. Every task's automated `sed`/`grep` check passes, confirming dbt/silver/gold ownership language and the BUG #18279 rationale appear in the correct sections (2, 4.2, 7, 26, 27, 32, 35, 36, 54, 58, 63, 68, 75, 92, 94).
2. `grep -cE '^#{1,2} [0-9]+\.' README.md` still equals 95 after all three tasks -- no top-level section was added, removed, or renumbered.
3. Definition of Done item 114's exact original text is still present verbatim, confirming items 1-114 were not renumbered; items 115-118 exist as new appended content.
4. `git diff --stat README.md` shows only README.md changed; no other planning document (PROJECT.md, ROADMAP.md, REQUIREMENTS.md) is touched by this quick task.
5. Manual skim confirms no passage describing what Phases 1-8 already built was altered -- this is a forward-looking architecture update to still-unimplemented Phase 08.1/9/10 content, not a historical rewrite.
</verification>

<success_criteria>
- README.md's architecture diagram, Analytical PostgreSQL section, and processing-flow diagram all show the bronze (staging) -> silver (dbt) -> warehouse (gold) pipeline in place of the old two-layer staging/warehouse pipeline.
- The transformation/staging/dedup/SCD numbered sections (26, 27, 32, 35, 36, 54, 58, 63) consistently describe dbt as the bronze-to-silver owner, the Python MergePublisher as the unchanged sole gold-publish owner, and cite PostgreSQL BUG #18279 as the reason dbt never touches gold or SCD.
- Repository Structure, Development Roadmap and Definition of Done reflect the new dbt/ project, roadmap step, and dbt-aware DoD items -- with items 1-114 unrenumbered and four new items (115-118) appended.
- Zero top-level `# N.` sections were added, removed, or renumbered anywhere in the 3,386-line document; the total count remains exactly 95.
- No content describing Phases 1-8's already-completed work was altered.
</success_criteria>

<output>
Create `.planning/quick/260818-f0w-update-readme-md-to-reflect-dbt-bronze-t/260818-f0w-SUMMARY.md` when done
</output>
