---
phase: 02-kind-cluster-core-infrastructure
plan: 07
type: execute
wave: 5
depends_on: [02-03, 02-04, 02-06]
files_modified:
  - tools/k8s/install_kubeconform.sh
  - tools/k8s/crd_to_jsonschema.py
  - scripts/vendor-crd-schemas.sh
  - scripts/render-manifests.sh
  - helm/schemas/cnpg/
  - Makefile
  - .gitignore
  - .github/workflows/ci.yml
  - tests/policy/test_manifest_validation_fails_closed.py
  - tests/policy/badmanifests/
autonomous: true
requirements: [CICD-07, INFRA-10]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`helm template` renders all five pinned charts for both values profiles, and `kubeconform -strict` against Kubernetes 1.35.5 validates every rendered document (CICD-07)"
    - "CI fails on an invalid manifest or an invalid chart, and the gate has been observed failing on a deliberately-broken manifest paired with its valid twin (CICD-07)"
    - "The CloudNativePG `Cluster` CR validates against vendored, in-repo JSON schemas rather than an unpinned network schema catalogue, so the gate is hermetic and its schemas move only when the chart pin moves"
    - "The manifest gate is invoked from CI through `make` and nothing else, preserving the standing fact that the Makefile is the only place a gate is defined"
    - "The manifest gate lives in `make ci` rather than `make check`, because it fetches pinned charts over the network — the same reason `gitleaks` lives there"
    - "Both values profiles render for every one of the five components, so INFRA-10's two profiles are proved to exist and to be valid rather than merely present (INFRA-10)"
  artifacts:
    - path: "scripts/render-manifests.sh"
      provides: "Renders both profiles for all five charts into a gitignored build directory"
    - path: "scripts/vendor-crd-schemas.sh"
      provides: "Regenerates helm/schemas/cnpg from the pinned CNPG chart, keeping schema and CRD in lockstep"
    - path: "tools/k8s/install_kubeconform.sh"
      provides: "Pinned, digest-verified kubeconform installer"
    - path: "helm/schemas/cnpg/"
      provides: "Vendored CRD JSON schemas, committed, regenerable"
    - path: "tests/policy/test_manifest_validation_fails_closed.py"
      provides: "Non-vacuity: kubeconform observed rejecting a broken manifest and accepting its valid twin"
    - path: "tests/policy/badmanifests/"
      provides: "The paired broken and valid samples, each declaring the rule it trips and the test that consumes it"
  key_links:
    - from: "Makefile"
      to: "scripts/render-manifests.sh"
      via: "`manifests` target delegates and names no version literal"
      pattern: "render-manifests\\.sh"
    - from: "scripts/render-manifests.sh"
      to: "helm/schemas/cnpg"
      via: "kubeconform -schema-location with the ResourceKind/ResourceAPIVersion template"
      pattern: "helm/schemas/cnpg"
    - from: ".github/workflows/ci.yml"
      to: "Makefile"
      via: "a run step invoking `make manifests` and nothing else"
      pattern: "make manifests"
  prohibitions:
    - "The offline gate must never be made to depend on a live cluster or on network access — `make check` stays runnable on a fresh clone with nothing running."
    - "A gate must never be softened into a warning to make a build green, and the one deliberate advisory in this phase (D-04's rebuild wall-clock) must never be hardened into a blocking gate."
---

<objective>
Make CI fail on an invalid manifest or chart, offline of any cluster: render both values profiles
for all five pinned charts, validate every rendered document with `kubeconform -strict` against
Kubernetes 1.35.5, and prove the gate is live by watching it reject a deliberately-broken manifest.

Purpose: ROADMAP success criterion 5 has two halves. The rendering half is proved here; the sizing
half is plan 02-08. This half is worthless unless the validator can actually discriminate — and
`kubeconform` silently cannot validate a CloudNativePG `Cluster` CR without a CRD schema, so a
naive setup passes everything and certifies nothing.

Output: a pinned kubeconform, vendored CNPG CRD schemas regenerable from the pinned chart, the
render-and-validate pipeline behind `make manifests`, its CI invocation, and the non-vacuity test.
</objective>

<user_story>
**As a** platform engineer opening a pull request that edits a Helm values file,
**I want to** be told by CI that my manifest is invalid before it reaches a cluster,
**so that** a typo in a values key is a red build in seconds rather than a half-applied release I
discover twenty minutes into a bootstrap.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-CONTEXT.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pinned kubeconform and vendored CloudNativePG CRD schemas</name>
  <files>tools/k8s/install_kubeconform.sh, tools/k8s/crd_to_jsonschema.py, scripts/vendor-crd-schemas.sh, helm/schemas/cnpg/</files>
  <read_first>
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Code Examples → "Offline CRD schema vendoring for kubeconform", in full, including the verified before-and-after (`could not find schema for Cluster`, exit 1 → `Valid: 3, Invalid: 0, Errors: 0`, exit 0) and the finding that ingress-nginx 4.15.1 ships no CRDs at all, so only the CNPG half is needed
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Don't Hand-Roll (the CRD-schema row) and § Alternatives Considered (the CRDs-catalog-over-the-network row)
    - `tools/k8s/install_kind.sh` and `tools/k8s/install_helm.sh` from plan 02-01 — the six-stage installer shape these must match
    - `tools/security/install_gitleaks.sh` — the original analog
    - `pyproject.toml` — `[tool.ruff]` `select = ["ALL"]` and `[tool.mypy]` `strict = true` with `TYPECHECK_PATHS` covering `tools`, which is why a vendored third-party script would need a permanent lint exclusion
    - `helm/versions.env` and `scripts/helm-install.sh` from plan 02-01
  </read_first>
  <action>
    `tools/k8s/install_kubeconform.sh` — the same six-stage shape as the kind and helm installers:
    committed per-platform SHA-256 trust anchors, refuse on version or digest mismatch, idempotence
    by digest comparison against a stamp file, verify before extract, advisory cross-check after,
    install into `tools/bin/`. It reads `KUBECONFORM_VERSION` from `helm/versions.env`. Compute the
    digests by downloading the artifact in this session; do not invent them.

    `tools/k8s/crd_to_jsonschema.py` — a small, typed, in-repo converter that reads a multi-document
    YAML stream of `CustomResourceDefinition` objects and writes one JSON Schema file per
    kind-and-version into an output directory, named `{Kind}_{version}.json` to match kubeconform's
    `-schema-location` template. It lifts each version's `schema.openAPIV3Schema`, adds the JSON
    Schema draft declaration kubeconform expects, and strips the Kubernetes-specific vendor
    extension keys that are not JSON Schema. It must pass this repository's own gates — `tools/` is
    inside `TYPECHECK_PATHS` under `mypy --strict`, and `ruff` runs with `select = ["ALL"]`.

    Writing forty lines here rather than vendoring `openapi2jsonschema.py` from the kubeconform
    repository is a deliberate departure from the research's recommendation, and the reason is that
    the lockstep property the research actually cares about comes from regenerating the schemas
    from the *pinned chart*, not from the identity of the converter — while vendoring an unlinted
    third-party script into a `mypy --strict` tree buys a permanent exclusion in `pyproject.toml`.
    The behavioural gate is what decides this: the generated schemas must make `kubeconform` accept
    a valid `Cluster` CR and reject an invalid one, which Task 3 proves. If the in-repo converter
    cannot produce schemas that discriminate, fall back to vendoring the upstream script with a
    committed SHA-256 and the necessary `extend-exclude` entries, and record the reason in the
    script header.

    `scripts/vendor-crd-schemas.sh` — regenerates `helm/schemas/cnpg/` from the pinned CNPG operator
    chart: `helm template` the chart at `CNPG_OPERATOR_CHART_VERSION`, select the
    `CustomResourceDefinition` documents, and run the converter over them. Use `python3` with the
    already-available PyYAML for the document selection rather than adding `yq` as a sixth pinned
    binary. Commit the generated schema files; head the directory with a short README stating that
    they are generated, naming the script that regenerates them, and stating that they must be
    regenerated whenever the CNPG chart pin moves.
  </action>
  <acceptance_criteria>
    - `tools/k8s/install_kubeconform.sh` exits 0 and `tools/bin/kubeconform -v` reports `v0.8.0`
    - Running the installer a second time prints an already-installed line and performs no download
    - `scripts/vendor-crd-schemas.sh` exits 0 and writes at least one schema file for kind `Cluster`
    - `helm/schemas/cnpg/` is committed and contains a `Cluster` schema whose filename matches kubeconform's `{{.ResourceKind}}_{{.ResourceAPIVersion}}` template
    - Re-running `scripts/vendor-crd-schemas.sh` produces byte-identical output, verified by a clean `git status --porcelain` afterwards
    - `uv run --frozen mypy tools` and `uv run --frozen ruff check tools` both exit 0
    - `uv run --frozen pytest tests/policy/test_pinned_tool_versions_agree.py -q` exits 0 with the kubeconform readings now resolving against a real installer
  </acceptance_criteria>
  <verify>
    <automated>tools/k8s/install_kubeconform.sh &amp;&amp; scripts/vendor-crd-schemas.sh &amp;&amp; test -z "$(git status --porcelain helm/schemas)" &amp;&amp; uv run --frozen ruff check tools &amp;&amp; uv run --frozen mypy tools &amp;&amp; uv run --frozen pytest tests/policy/test_pinned_tool_versions_agree.py -q</automated>
  </verify>
  <done>kubeconform installs from a committed digest, and the CloudNativePG CRD schemas are vendored in-repo, regenerable deterministically from the pinned chart, and produced by code that passes this repository's own lint and type gates.</done>
</task>

<task type="auto">
  <name>Task 2: `make manifests` — render both profiles for all five charts and validate them</name>
  <files>scripts/render-manifests.sh, Makefile, .gitignore, .github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Pitfall 9 (`helm template` emits both test hooks and post-install hooks; `--no-hooks` drops the MinIO bucket-bootstrap job too, so filter on the annotation rather than at the render call) and § Anti-Patterns (never pipe `helm template` into `kubectl apply`)
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Code Examples → the verified `kubeconform` invocation with its two `-schema-location` entries
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md` § `Makefile` → "Gate composition and the offline contract" (the exact rule for whether `manifests` may join `check`) and § `.github/workflows/ci.yml`
    - `Makefile` lines 96-102 — the `check`/`ci` composition and its comment explaining why `gitleaks` lives in `ci`
    - `tests/policy/test_ci_calls_make_ci.py` — it parses the Makefile prerequisite graph, so adding a target to the `ci` chain automatically requires a workflow job to invoke it
    - `tests/policy/test_ci_invokes_make_only.py` — the constraint that a workflow step may run `make <target>` and nothing else
    - `.github/workflows/ci.yml` in full — the SHA-pinned actions, the workflow-level read-only permissions, and the existing two-job shape
    - `.gitignore`
    - All ten values files under `helm/values/` produced by plans 02-01, 02-03, 02-04 and 02-06
  </read_first>
  <action>
    `scripts/render-manifests.sh` — for each of the two profiles and each of the five charts, add the
    pinned chart repository, then `helm template` the chart at its pinned version with the matching
    `helm/values/<profile>/<component>.yaml` and the namespace the component deploys into, writing
    one file per chart-and-profile under a gitignored `build/manifests/<profile>/`. Never pipe the
    output into `kubectl apply`: `helm template` emits `helm.sh/hook: test` resources, and doing so
    with the CNPG cluster chart applied a stray ping-test job into a real namespace. Do not pass
    `--no-hooks` either — it also drops MinIO's bucket-creation post-install job, which is a genuine
    part of the deployed system and must be validated. Filtering of test hooks happens in the policy
    tests, on the annotation.

    Then run `kubeconform` over everything rendered: `-strict`, `-summary`,
    `-kubernetes-version` from `KUBERNETES_VERSION`, `-schema-location default`, and a second
    `-schema-location` pointing at `helm/schemas/cnpg/` with kubeconform's ResourceKind and
    ResourceAPIVersion template. Exit non-zero on any invalid document. Add
    `-schema-location` entries only for CNPG: ingress-nginx 4.15.1 ships no CRDs at all, so the
    ROADMAP's expectation of entries for both is half unnecessary.

    Add `build/` to `.gitignore`.

    `Makefile` — add `helm-lint` and `manifests` to `.PHONY`, each with a `## comment`. `helm-lint`
    runs `helm lint` for each chart against each profile's values file. `manifests` depends on
    `helm-lint` and delegates to `scripts/render-manifests.sh`. Neither names a version literal;
    both read `helm/versions.env`. Add `manifests` to the `ci` target's prerequisites and **not** to
    `check`: `check` must never need the network, and rendering requires fetching the pinned charts.
    Extend the comment above `check`/`ci` to record that reasoning next to the existing gitleaks
    explanation, so the placement reads as a decision rather than an oversight.

    `.github/workflows/ci.yml` — add a job that checks out with the SHA-pinned action, sets up uv
    with the SHA-pinned action, runs `make install`, installs the pinned helm and kubeconform
    binaries through their installer scripts, and then runs `make manifests` and nothing else. Keep
    the workflow-level read-only permissions untouched and reference no repository secret. The
    installer invocations follow the precedent already set by the secret-scanning job, which calls
    `tools/security/install_gitleaks.sh` directly before running its make target.
  </action>
  <acceptance_criteria>
    - `make manifests` exits 0 and writes rendered output for both profiles under `build/manifests/`
    - The rendered output contains documents from all five charts for both profiles
    - `git check-ignore -q build` exits 0
    - `make manifests` exits non-zero when a values file is given an unknown key that the chart rejects
    - `kubeconform` output reports zero invalid documents and a non-zero count of valid ones
    - `make check` still exits 0 with no network access available
    - `uv run --frozen pytest tests/policy/test_ci_invokes_make_only.py tests/policy/test_ci_calls_make_ci.py -q` exits 0
    - `make -n ci` shows `manifests` in the prerequisite chain and `make -n check` does not
  </acceptance_criteria>
  <verify>
    <automated>make manifests &amp;&amp; uv run --frozen pytest tests/policy/test_ci_invokes_make_only.py tests/policy/test_ci_calls_make_ci.py -q &amp;&amp; make check</automated>
  </verify>
  <done>Both values profiles render for all five pinned charts and every rendered document validates under `kubeconform -strict` against Kubernetes 1.35.5; the gate is defined only in the Makefile, invoked from CI through one make call, and kept out of the offline gate for a written reason.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the validator discriminates — a broken manifest and its valid twin</name>
  <files>tests/policy/test_manifest_validation_fails_closed.py, tests/policy/badmanifests/</files>
  <read_first>
    - `tests/policy/test_gates_actually_fail.py` in full — the opening claim, the `_run` helper with `check=False`, the negative-plus-positive-control pairing, the `tests/policy/badsamples/` header convention (`# Trips:` / `# Proves:`), and `test_the_main_gate_does_not_lint_the_bad_samples`
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md` § Analog D and the paragraph naming what `test_manifest_validation_fails_closed.py` owes
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Pitfall 3 — the exact rejection message for a `Cluster` whose `spec.postgresql` renders null, which is a real, previously-observed failure and therefore the honest broken sample
    - `pyproject.toml` — the existing `extend-exclude` and `exclude` entries for `tests/policy/badsamples`, which the new sample directory needs an equivalent of if any tool would otherwise read it
    - `scripts/render-manifests.sh` and `Makefile` from Task 2
  </read_first>
  <action>
    `tests/policy/badmanifests/` — a paired corpus, following the `tests/policy/badsamples/`
    convention exactly. One deliberately-invalid manifest and one valid twin, each carrying a header
    comment declaring the rule it trips and naming the test that consumes it. Make the invalid
    sample a real failure rather than a contrived one: a CloudNativePG `Cluster` whose `postgresql`
    key is present with a null value is precisely the failure the phase research hit against a
    correct-looking values file, and it is the failure a future contributor is most likely to
    reintroduce by deleting a parameter that looks decorative.

    `tests/policy/test_manifest_validation_fails_closed.py` — the subprocess model. Open with the
    honest-limit docstring the repository convention requires: this proves `kubeconform` as
    configured rejects a known-invalid document and accepts its valid twin, and does not prove the
    configuration catches every class of invalid manifest. Then:
    - The negative case: run the real `kubeconform` with the real `-schema-location` arguments
      against the invalid sample, require a non-zero exit, and require the output to name the
      expected rule. Do not use `check=True` — the non-zero exit is the signal under test.
    - The positive control: the same tool, the same configuration, the valid twin, exit 0. A gate
      that rejects everything is exactly as broken as one that rejects nothing.
    - A CRD-schema case: run `kubeconform` against the valid `Cluster` sample *without* the vendored
      schema location and require it to fail with a missing-schema error, then with the schema
      location and require it to pass. This is the assertion that keeps someone from deleting
      `helm/schemas/cnpg/` and leaving a gate that validates nothing while reporting success.
    - A guard mirroring `test_the_main_gate_does_not_lint_the_bad_samples`: `make manifests` exits 0
      with the sample directory present, so a broken sample sitting in the tree cannot make the main
      gate permanently red. Require the tool name to appear in the transcript, because a target that
      ran nothing would also exit 0.
  </action>
  <acceptance_criteria>
    - `uv run --frozen pytest tests/policy/test_manifest_validation_fails_closed.py -q` exits 0
    - The invalid sample causes a non-zero `kubeconform` exit whose output names the failing path
    - The valid twin causes a zero exit under the identical configuration
    - The valid `Cluster` sample fails with a missing-schema error when the vendored schema location is omitted, and passes when it is supplied
    - `make manifests` exits 0 with `tests/policy/badmanifests/` present, and its transcript names `kubeconform`
    - `make check` and `make ci` both green
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/policy/test_manifest_validation_fails_closed.py -q &amp;&amp; make manifests</automated>
  </verify>
  <done>The manifest gate has been observed rejecting a real, previously-encountered invalid manifest and accepting its valid twin, and the vendored CRD schemas are proved load-bearing rather than decorative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream chart repositories → CI runner | `helm template` fetches pinned chart tarballs over the network during the gate |
| upstream kubeconform release → developer host and CI | a binary is downloaded and executed |
| pull-request author → the manifest gate | a contributor's values change is what the gate exists to reject |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-29 | Tampering | `tools/k8s/install_kubeconform.sh` | high | mitigate | Committed per-platform SHA-256 trust anchor, verify before extract, refuse rather than fall back, idempotence by digest rather than by executing the binary |
| T-02-30 | Tampering | a gate that validates nothing while reporting success | high | mitigate | `test_manifest_validation_fails_closed.py` proves the missing-CRD-schema case fails and the supplied-schema case passes, so deleting `helm/schemas/cnpg/` breaks the build rather than silently disabling validation |
| T-02-31 | Spoofing | unpinned chart resolution during rendering | high | mitigate | Every `helm template` passes an explicit `--version` read from `helm/versions.env`; no chart is resolved to "latest" |
| T-02-32 | Elevation of Privilege | CI workflow permissions | medium | mitigate | The new job inherits the workflow-level read-only `contents` permission, references no repository secret, pins every action by commit SHA, and runs `make` plus the pinned installers and nothing else |
| T-02-33 | Tampering | `helm template` output applied to a live cluster | medium | mitigate | Rendering writes to a gitignored build directory only; the anti-pattern of piping into `kubectl apply` is called out in the script and would apply a stray test-hook job into a real namespace |
</threat_model>

## Artifacts this phase produces (plan 02-07)

**Makefile targets:** `helm-lint`, `manifests`; `manifests` joins the `ci` chain and not `check`.

**Scripts:** `tools/k8s/install_kubeconform.sh`, `tools/k8s/crd_to_jsonschema.py`, `scripts/vendor-crd-schemas.sh`, `scripts/render-manifests.sh`.

**Generated and committed:** `helm/schemas/cnpg/` (one JSON Schema per CNPG CRD kind and version, including `Cluster`), plus a README stating how to regenerate them.

**Gitignore addition:** `build/`. **Render output path:** `build/manifests/<profile>/`.

**CI:** a new job in `.github/workflows/ci.yml` invoking `make manifests`.

**Tests:** `tests/policy/test_manifest_validation_fails_closed.py`; `tests/policy/badmanifests/` with a paired invalid and valid `Cluster` sample.

**Config keys consumed:** `KUBECONFORM_VERSION`, `KUBERNETES_VERSION`, and every `*_CHART_VERSION` from `helm/versions.env`.

## Planner note — scope fence observed

`kind/cluster-ci.yaml` is **not** written here. The phase boundary states that the ephemeral-kind
E2E job belongs to Phase 11 and that this phase writes only the `values-ci.yaml` files that job will
consume. The research's recommended structure listed a CI kind config; it is deliberately deferred
so the fence holds.

## Flagged planner assumptions (spec-less probe)

- **CICD-07, category `unclassified`** — "Kubernetes manifests and Helm charts are validated in CI".
  Planner assumption: *validated* means schema validation of rendered output plus `helm lint`, both
  offline of any cluster. Server-side `kubectl apply --dry-run=server` — which catches CRD and
  admission-webhook errors no offline tool can — is a CONTEXT.md deferred idea belonging to Phase 11
  with the ephemeral-kind job. If the developer expects admission-webhook validation now, this plan
  does not deliver it.
- **INFRA-10, category `unclassified`** — "two Helm values profiles exist from the first
  infrastructure commit". Planner assumption: *exist* is proved by both profiles rendering and
  validating for every one of the five components, not merely by the files being present. The
  stronger claim — that the CI profile actually runs on a 4 CPU / 16 GB runner — is bounded
  statically by the sizing test in plan 02-08 and is not executed until Phase 11.

## Verification

- `make manifests` green for both profiles across all five charts
- `kubeconform -strict` reports zero invalid documents
- The gate observed rejecting a broken manifest and accepting its twin, and observed failing without the vendored schemas
- `make check` still green with no network

## Success criteria

- The rendering half of ROADMAP success criterion 5 is mechanically true
- The manifest gate is defined only in the Makefile, invoked from CI by one make call, and proved non-vacuous

<output>
Create `.planning/phases/02-kind-cluster-core-infrastructure/02-07-SUMMARY.md` when done
</output>
