---
phase: 02-kind-cluster-core-infrastructure
plan: 08
type: execute
wave: 6
depends_on: [02-07]
files_modified:
  - tests/policy/test_manifest_resources.py
  - tests/policy/test_values_profiles.py
  - tests/policy/test_no_manual_kubectl_surgery.py
  - tests/policy/test_workflow_secrets.py
  - tests/policy/test_supply_chain_guards.py
autonomous: true
requirements: [CICD-07, INFRA-10, INFRA-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The summed container resource requests over the rendered CI profile fit inside a 4 CPU / 16 GB runner, including the two CloudNativePG `Cluster` CRs — so success criterion 5's 'sized for' claim is mechanically true rather than asserted (D-12 test 1, CICD-07)"
    - "Every container in both rendered profiles carries a CPU request, a memory request, a CPU limit and a memory limit; an unrequested container is QoS BestEffort and is evicted first, which is precisely when its data is needed to explain the incident (D-12 test 2)"
    - "The sizing test fails on any unrecognised custom-resource kind rather than silently contributing zero, because a budget test that quietly ignores the two heaviest workloads is worse than no test (D-12 test 1)"
    - "The two values profiles differ on replica counts, resource sizing, monitoring, and the executor — and on nothing else; a fifth divergence axis fails the build (D-06, INFRA-10)"
    - "The executor divergence is allowlisted by name with its written argument, so D-06's 'any fourth axis needs an argument' is enforced rather than waived (D-06)"
    - "No script in the repository performs manual `kubectl` surgery: `create`, `edit` and `patch` are absent, and `apply` appears only against a committed file under `kubernetes/` (INFRA-07)"
    - "No credential literal appears in any file under `helm/`, `kubernetes/`, `kind/` or `scripts/`; every secret is a reference by name (D-14, §81)"
    - "No container image in any values file is selected by a mutable tag; every image is pinned and every pin agrees with `helm/versions.env` (§77, INFRA-08 precedent)"
  artifacts:
    - path: "tests/policy/test_manifest_resources.py"
      provides: "D-12 tests 1 and 2 over the rendered manifests, with the CNPG Cluster special case and a Kubernetes quantity parser"
    - path: "tests/policy/test_values_profiles.py"
      provides: "The D-06 divergence-axis rule with a named, argued allowlist"
    - path: "tests/policy/test_no_manual_kubectl_surgery.py"
      provides: "INFRA-07 stated in the form that is actually decidable"
    - path: "tests/policy/test_workflow_secrets.py"
      provides: "Credential-literal scanning widened from workflows to the whole infrastructure tree"
    - path: "tests/policy/test_supply_chain_guards.py"
      provides: "Image pin agreement between values files and helm/versions.env"
  key_links:
    - from: "tests/policy/test_manifest_resources.py"
      to: "build/manifests/ci/"
      via: "parses the rendered CI profile produced by make manifests"
      pattern: "build/manifests"
    - from: "tests/policy/test_values_profiles.py"
      to: "helm/values/local and helm/values/ci"
      via: "structural diff of the two profiles, keyed by leaf path"
      pattern: "helm/values"
    - from: "tests/policy/test_supply_chain_guards.py"
      to: "helm/versions.env"
      via: "reader function added to the pin-agreement table"
      pattern: "versions\\.env"
  prohibitions:
    - "The offline gate must never be made to depend on a live cluster or on network access — `make check` stays runnable on a fresh clone with nothing running."
    - "A gate must never be softened into a warning to make a build green, and the one deliberate advisory in this phase (D-04's rebuild wall-clock) must never be hardened into a blocking gate."
    - "The S3 claim must never be proven with MinIO's own tooling — a verification that passes only through `mc` would certify a swap-out property the platform does not have."
---

<objective>
Close the sizing half of ROADMAP success criterion 5 and the remaining INFRA-07 and D-14 claims with
five static tests over the rendered manifests and the committed infrastructure tree: the CI profile
fits its runner, every container is sized, the two profiles diverge on named axes only, no script
performs manual `kubectl` surgery, and no credential or mutable image tag has leaked into a values
file.

Purpose: each of these is a claim the phase has been making since its first commit. Until a test
holds them, they are assertions. The sizing test in particular is the one that could most easily be
written to pass while measuring nothing — the research's prototype summed 0.35 cores across four
charts because the two PostgreSQL clusters are custom resources and not Pod templates.

Output: two new policy tests, one new detector, and two widened existing detectors — all static,
all sub-second, all inside the offline gate.
</objective>

<user_story>
**As a** platform engineer,
**I want to** be told at pull-request time that my values change no longer fits the CI runner, left
a container unsized, opened a new divergence between the two profiles, or put a password in a file,
**so that** each of those becomes a red build in seconds rather than an eviction, a leaked
credential, or a CI-only bug discovered nine phases downstream.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-CONTEXT.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-06-SUMMARY.md
@.planning/phases/02-kind-cluster-core-infrastructure/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The two D-12 tests — the CI profile fits its runner, and every container is sized</name>
  <precondition>`make manifests` has been run and `build/manifests/local/` and `build/manifests/ci/` contain rendered output for all five charts.</precondition>
  <files>tests/policy/test_manifest_resources.py</files>
  <read_first>
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Pitfall 6 in full — the CNPG `Cluster` CR is not a Pod template, the prototype summed 0.350 cores with both databases invisible, and the required special case plus the fail-on-unrecognised-kind rule
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Code Examples → "D-12 policy test skeleton (prototyped and run)" — the suffix table, the quantity regex, the `containers()` dispatch over Pod-template kinds, the `is_test_hook()` annotation filter, and the load-bearing `if not doc` guard for documents that parse to None
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Pitfall 5 (the seventeen containers left unsized by a realistic values file, and the two non-obvious key mappings) and § Don't Hand-Roll (why a naive quantity parser silently under-counts)
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-CONTEXT.md` D-12
    - `tests/policy/test_workflow_secrets.py` lines 85-151 — the pure-predicate-returning-messages form and the mutation-based non-vacuity with its applied-guard
    - `scripts/render-manifests.sh` and `Makefile` from plan 02-07
  </read_first>
  <action>
    `tests/policy/test_manifest_resources.py`. Open with the honest-limit docstring the repository
    convention requires: this sums requests over *rendered* manifests for a fixed values profile and
    therefore bounds what the scheduler will be asked for, not what the workloads will actually use;
    and it special-cases the CloudNativePG `Cluster` custom resource, without which the two heaviest
    workloads contribute nothing.

    Write a Kubernetes quantity parser covering the plain form, the milli suffix, the decimal SI
    suffixes, the binary suffixes and exponent notation — all four shapes appear in real chart
    defaults, and a parser that strips a trailing character silently under-counts.

    Write a container walker dispatching on document kind over the Pod-template kinds, reaching both
    `initContainers` and `containers`. Skip documents annotated as Helm test hooks; do not skip
    post-install hooks, because MinIO's bucket-bootstrap job is a genuine part of the deployed
    system. Guard against documents that parse to `None`, which a separator followed by comments
    produces.

    Special-case the CloudNativePG `Cluster` kind: its requests live at `spec.resources.requests`
    and must be multiplied by `spec.instances`. Then make the walker **fail on any unrecognised
    custom-resource kind** rather than returning an empty list — a budget test that quietly ignores a
    workload is worse than no test, and the failure message must name the kind so adding a component
    forces a deliberate decision.

    `test_ci_profile_fits_runner` — sum CPU and memory requests across every container and every
    `Cluster` in the rendered CI profile and assert the totals fit inside a 4 CPU / 16 GB runner with
    a stated headroom fraction. Name the budget constants at module level with a comment citing the
    runner spec. Prove non-vacuity by inflating one container's request on an in-memory copy and
    requiring the predicate to report it, with the guard that the mutation applied.

    `test_every_container_is_sized` — assert every container in **both** rendered profiles carries a
    CPU request, a memory request, a CPU limit and a memory limit, reporting each offender as
    `<file>: <kind>/<name>: <container>: missing <field>`. Prove non-vacuity by stripping the
    resources block from one rendered container on an in-memory copy and requiring it reported, and
    pair it with the false-positive control that the real rendered output produces no messages.

    Both tests read from the gitignored render output, so they need `make manifests` to have run.
    Skip with a clear message naming that target when the render directory is absent, rather than
    failing for a reason unrelated to the claim.
  </action>
  <acceptance_criteria>
    - `uv run --frozen pytest tests/policy/test_manifest_resources.py -q` exits 0 after `make manifests`
    - `pytest tests/policy/test_manifest_resources.py::test_ci_profile_fits_runner -x` passes and its computed CPU and memory totals are both strictly greater than zero
    - The summed CI totals include a non-zero contribution from each of the two CloudNativePG `Cluster` resources
    - Inflating one container request on an in-memory copy past the budget is reported, and the mutation is guarded as having applied
    - Stripping one container's resources on an in-memory copy is reported by `test_every_container_is_sized`
    - Introducing an unrecognised custom-resource kind into an in-memory copy causes a failure naming that kind, rather than a silent zero
    - The quantity parser round-trips the plain, milli, decimal-SI, binary and exponent forms correctly under direct unit assertions
    - With the render directory absent, the module skips with a message naming `make manifests`
  </acceptance_criteria>
  <verify>
    <automated>make manifests &amp;&amp; uv run --frozen pytest tests/policy/test_manifest_resources.py -q</automated>
  </verify>
  <done>The CI profile's request total is computed over every workload including both databases, fits its runner, and every container in both profiles is sized — each claim proved non-vacuous by mutation and paired with a false-positive control.</done>
</task>

<task type="auto">
  <name>Task 2: The divergence-axis rule — three permitted axes plus one argued fourth, and nothing else</name>
  <files>tests/policy/test_values_profiles.py</files>
  <read_first>
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-CONTEXT.md` D-06 in full, including "Every additional divergence axis is a bug class that appears only in CI, nine phases downstream of where it was introduced" and "Any fourth axis needs an argument"
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md` § Analog B — `tests/policy/test_pinned_tool_versions_agree.py`, the reader-per-source table and `test_every_source_is_load_bearing`, and the specific instruction that this test must report an injected fourth axis
    - `helm/values/local/airflow.yaml` and `helm/values/ci/airflow.yaml` from plan 02-06 — the header comment recording the executor as the argued fourth axis
    - All ten values files under `helm/values/`, to see which leaf paths legitimately differ
    - `tests/policy/test_pinned_tool_versions_agree.py`
  </read_first>
  <action>
    `tests/policy/test_values_profiles.py`. Open with the honest-limit docstring: this compares the
    two committed profiles structurally by leaf path and therefore decides which *keys* differ, not
    whether the differing values are correct.

    Write a pure predicate that, for each component, loads both profiles, flattens each to a mapping
    from dotted leaf path to value, and returns the set of leaf paths whose values differ or which
    exist in only one profile. Classify each differing path against a module-level table of
    permitted axes:
    - replica counts,
    - resource sizing,
    - monitoring enablement,
    - and the executor, which is the argued fourth axis.

    Each entry in that table carries a one-line written argument beside it, and the executor entry
    carries the argument from plan 02-06: the CI runner is 4 CPU / 16 GB, the KubernetesExecutor
    costs two pods and two multi-gigabyte image pulls per task, and `values-ci.yaml` exists
    precisely because the full stack does not fit that runner. Any differing leaf path that matches
    no entry is reported. Write the table so that adding an entry without an argument is a visible
    omission rather than a silent widening.

    Required tests:
    - `test_both_profiles_exist_for_every_component` — every component with a local values file has a
      CI one and vice versa, so INFRA-10's "from the first infrastructure commit" cannot drift as
      components are added.
    - `test_profiles_diverge_only_on_permitted_axes` — the predicate returns no unclassified path.
    - `test_a_fifth_axis_is_reported` — inject a difference on an unrelated leaf path into an
      in-memory copy and require it reported, with the guard that the mutation applied. This is the
      non-vacuity proof.
    - `test_a_permitted_axis_is_not_reported` — the false-positive control: a difference on a replica
      count is not reported, because a rule that fires on the permitted divergences would be turned
      off rather than fixed.
    - `test_every_permitted_axis_carries_an_argument` — every entry in the table has a non-empty
      argument string, so D-06's requirement is enforced by the test rather than by convention.
  </action>
  <acceptance_criteria>
    - `uv run --frozen pytest tests/policy/test_values_profiles.py -q` exits 0
    - The permitted-axis table contains exactly four entries, each with a non-empty argument
    - Injecting a difference on an unrelated leaf path into an in-memory copy is reported, and the mutation is guarded as having applied
    - A difference on a replica count is not reported
    - Deleting one CI values file causes `test_both_profiles_exist_for_every_component` to fail
    - `make check` green with the new module collected by `make policy`
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/policy/test_values_profiles.py -q &amp;&amp; make check</automated>
  </verify>
  <done>Every component has both profiles; the profiles differ only on four named axes each carrying a written argument; a fifth axis is reported; and the rule is proved not to fire on the permitted ones.</done>
</task>

<task type="auto">
  <name>Task 3: Infrastructure as code, no credential literals, no mutable image tags</name>
  <files>tests/policy/test_no_manual_kubectl_surgery.py, tests/policy/test_workflow_secrets.py, tests/policy/test_supply_chain_guards.py</files>
  <read_first>
    - `tests/policy/test_ci_invokes_make_only.py` in full — the forbidden-construct regex, the deliberate carve-out documented in its docstring, and the comment-stripping behaviour needed so a script that explains why it delegates is not reported
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-PATTERNS.md` § Analog C and § `tests/policy/test_workflow_secrets.py` (modify — widen scope for D-14), including the instruction not to widen the allowed-secrets set
    - `tests/policy/test_workflow_secrets.py` in full — the current scanned surface, the allowed-secrets docstring obligation, and the pure-predicate form
    - `tests/policy/test_supply_chain_guards.py` in full — the existing shape to extend
    - `.planning/phases/02-kind-cluster-core-infrastructure/02-RESEARCH.md` § Project Constraints (the exact forbidden values keys and their permitted secret-reference alternatives) and § Pattern 3 (why `kubectl wait` must stay permitted)
    - Every script under `scripts/` and `scripts/stages/`, and every file under `helm/`, `kubernetes/` and `kind/`
  </read_first>
  <action>
    `tests/policy/test_no_manual_kubectl_surgery.py` — INFRA-07 stated in the form that is actually
    decidable. Open with the honest-limit docstring: the general claim that no step ever requires
    manual intervention is undecidable, and what this file asserts is the stronger structural claim
    that no script in this repository mutates cluster state except by applying a committed file.
    Scan every script under `scripts/` and `tools/`, stripping comment lines so a script explaining
    why it delegates is not reported. Report any imperative `kubectl` subcommand that mutates state
    outside the permitted set. Permit `kubectl wait`, `kubectl get`, and `kubectl apply` only when
    its argument is a committed path under `kubernetes/`. Document the permitted set and its
    justification in the docstring — the readiness waits in plan 02-01's `scripts/wait-for.sh` are
    structurally necessary, and RESEARCH's measured webhook failure is why. Prove non-vacuity by
    injecting an imperative mutation into an in-memory copy of a script and requiring it reported,
    and pair it with the false-positive control that the real scripts produce no messages.

    Widen `tests/policy/test_workflow_secrets.py` additively for D-14. Keep the existing
    workflow-scoped claim exactly as it is and **do not** widen the allowed-secrets set — its
    docstring makes adding an entry an obligation to re-audit the general claim, and this phase adds
    no CI secret. Add a second scanned surface covering `helm/`, `kubernetes/`, `kind/` and
    `scripts/`, reporting any credential literal: the chart keys that take a literal password, key
    or token where a secret-reference alternative exists, and any inline base64 secret data block.
    Name the permitted reference forms in the docstring so the message tells a contributor what to
    write instead. Prove non-vacuity by injecting a literal into an in-memory copy of a values file
    and requiring it reported, and pair it with the control that the real tree is clean.

    Extend `tests/policy/test_supply_chain_guards.py` with image-pin agreement. Every container image
    referenced by any values file must carry an immutable tag or digest, and every image tag that
    also appears in `helm/versions.env` must agree with it. Add a reader per source so a
    perturbation of either side is reported as drift, following the load-bearing-source model the
    pinned-tool-version test already uses. Prove non-vacuity by replacing one values-file tag with a
    mutable one on an in-memory copy and requiring it reported.
  </action>
  <acceptance_criteria>
    - `uv run --frozen pytest tests/policy/test_no_manual_kubectl_surgery.py -q` exits 0
    - Injecting an imperative state-mutating `kubectl` call into an in-memory copy of a script is reported; the real scripts produce no messages
    - `kubectl wait` and `kubectl apply` against a committed path under `kubernetes/` are not reported
    - `uv run --frozen pytest tests/policy/test_workflow_secrets.py -q` exits 0 and now covers `helm/`, `kubernetes/`, `kind/` and `scripts/`
    - Injecting a literal credential into an in-memory copy of a values file is reported
    - The allowed-secrets set in `test_workflow_secrets.py` is unchanged from its Phase 1 contents
    - `uv run --frozen pytest tests/policy/test_supply_chain_guards.py -q` exits 0 and reports drift when a values-file image tag is perturbed on an in-memory copy
    - `./tools/bin/gitleaks dir --redact --no-banner --exit-code 1 .` exits 0
    - `make check` and `make ci` both green
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/policy -q &amp;&amp; make check</automated>
  </verify>
  <done>INFRA-07 is enforced in its decidable form; the credential scan covers the whole infrastructure tree without weakening the Phase 1 claim; and every container image is pinned and agrees with the single version source — each proved by a paired negative case and false-positive control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull-request author → the committed infrastructure tree | a values or script change is what these detectors exist to reject |
| repository → git history | a credential committed once is world-readable forever after the repository goes public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-34 | Information Disclosure | a committed values file drifting to a literal password | high | mitigate | `test_workflow_secrets.py` widened to `helm/`, `kubernetes/`, `kind/` and `scripts/`, reporting literal credential keys and inline base64 secret data, with the permitted secret-reference forms named in the failure message; gitleaks has scanned full history since Phase 1 |
| T-02-35 | Denial of Service | a BestEffort pod evicted first, destroying the evidence needed to explain the incident | high | mitigate | `test_every_container_is_sized` fails any container in either profile missing a CPU request, a memory request, a CPU limit or a memory limit |
| T-02-36 | Denial of Service | the CI profile outgrowing its runner unnoticed | high | mitigate | `test_ci_profile_fits_runner` sums requests over every workload including both CloudNativePG `Cluster` resources, and fails on any unrecognised custom-resource kind rather than contributing zero |
| T-02-37 | Tampering | an infrastructure change applied by hand and never committed | medium | mitigate | `test_no_manual_kubectl_surgery.py` permits only `wait`, `get` and `apply` against a committed path, so cluster state can only come from the repository |
| T-02-SC | Tampering | an image selected by a mutable tag | high | mitigate | `test_supply_chain_guards.py` extended to require an immutable tag or digest on every values-file image and agreement with `helm/versions.env`, so a repointed upstream tag cannot silently change what runs |
</threat_model>

## Artifacts this phase produces (plan 02-08)

**Tests created:** `tests/policy/test_manifest_resources.py` — `test_ci_profile_fits_runner`, `test_every_container_is_sized`, plus the quantity-parser unit assertions and the mutation-based non-vacuity cases. `tests/policy/test_values_profiles.py` — `test_both_profiles_exist_for_every_component`, `test_profiles_diverge_only_on_permitted_axes`, `test_a_fifth_axis_is_reported`, `test_a_permitted_axis_is_not_reported`, `test_every_permitted_axis_carries_an_argument`. `tests/policy/test_no_manual_kubectl_surgery.py`.

**Tests modified:** `tests/policy/test_workflow_secrets.py` (second scanned surface over `helm/`, `kubernetes/`, `kind/`, `scripts/`; allowed-secrets set unchanged), `tests/policy/test_supply_chain_guards.py` (image-pin agreement against `helm/versions.env`).

**Named constants introduced:** the CI runner budget (4 CPU / 16 GB with a stated headroom fraction) and the four-entry permitted-divergence-axis table with one written argument per entry.

## Flagged planner assumptions (spec-less probe)

Both remaining `unclassified` probe rows for this plan's requirements are surfaced in plan 02-07
(CICD-07 and INFRA-10) and are not restated here. No probe row for this phase remains unaccounted
for: of thirteen surfaced items, seven were authored into `must_haves.truths` across plans 02-01,
02-02 and 02-06, and six were surfaced as flagged assumptions across plans 02-01, 02-03, 02-04 and
02-07.

## Verification

- `make manifests` then `make policy` green
- Every detector observed reporting an injected defect and observed not reporting the real tree
- `make check` and `make ci` green; gitleaks green

## Success criteria

- The sizing half of ROADMAP success criterion 5 is mechanically true
- Every claim this phase has been making since its first commit is now held by a test that has been watched failing

<output>
Create `.planning/phases/02-kind-cluster-core-infrastructure/02-08-SUMMARY.md` when done
</output>
