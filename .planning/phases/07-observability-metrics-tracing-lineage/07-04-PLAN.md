---
phase: 07-observability-metrics-tracing-lineage
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - docker/airflow/Dockerfile
  - Makefile
  - helm/values/local/airflow.yaml
  - helm/values/ci/airflow.yaml
  - airflow/dags/_common/kpo.py
  - airflow/dags/_common/tracing_kpo.py
  - airflow/dags/csv_ingest_customers.py
  - tests/policy/test_dag_thinness.py
autonomous: true
requirements: [OBS-10]
must_haves:
  truths:
    - "docker/airflow/Dockerfile builds an apache/airflow:3.3.0-python3.12-derived image that actually imports opentelemetry (verified: today's stock image does not, per RESEARCH.md's live docker pull/pip list check)"
    - "A TracingKubernetesPodOperator injects a well-formed W3C traceparent env var into a launched pod's spec when an Airflow-managed span is active, and injects nothing when tracing is disabled — proven at the unit level, without a live cluster"
    - "Only the ingest task (D-12) becomes the trace root; discover stays a plain KubernetesPodOperator"
  artifacts:
    - path: "docker/airflow/Dockerfile"
      provides: "the custom Airflow image with apache-airflow[otel] installed under the pinned constraints file"
      contains: "apache-airflow[otel]"
    - path: "airflow/dags/_common/tracing_kpo.py"
      provides: "TracingKubernetesPodOperator, overriding build_pod_request_obj() (not env_vars templating, not common_kpo_kwargs — RESEARCH.md Pitfalls 2/3)"
      contains: "def build_pod_request_obj"
  key_links:
    - from: "airflow/dags/csv_ingest_customers.py"
      to: "airflow/dags/_common/tracing_kpo.py"
      via: "ingest = TracingKubernetesPodOperator.partial(...)"
      pattern: "TracingKubernetesPodOperator\\.partial"
    - from: "helm/values/local/airflow.yaml"
      to: "docker/airflow/Dockerfile"
      via: "defaultAirflowRepository/defaultAirflowTag pointing at the locally-built, git-sha-tagged image"
      pattern: "defaultAirflowTag"
---

<objective>
Build the custom Airflow image this phase discovered is genuinely required (the stock
`apache/airflow:3.3.0-python3.12` image contains zero `opentelemetry` packages — verified live,
not from documentation), wire it into both Helm values profiles, flip Airflow's own metrics to
StatsD in both profiles (D-02) with tracing enabled locally only, and implement the one mechanism
that actually propagates a W3C `traceparent` across the Airflow→pod boundary: a
`KubernetesPodOperator` subclass overriding `build_pod_request_obj()` — not `env_vars` templating
(documented as unreliable, RESEARCH.md Pitfall 3) and not `common_kpo_kwargs()` (evaluated at
DAG-parse time, before any span exists, RESEARCH.md Pitfall 2).

Purpose: after this plan, `ingest`'s launched pod carries a real, correctly-parented `TRACEPARENT`
env var whenever Airflow's own tracing is active — the Airflow-side half of OBS-10. Plan 07-05
completes the pod-side half (extracting it inside `dataplat`); Plan 07-08 proves the whole chain
live.
Output: `docker/airflow/Dockerfile` (was `.gitkeep` since Phase 1), `make image-airflow`,
`TracingKubernetesPodOperator`, `csv_ingest_customers.py`'s `ingest` task using it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-CONTEXT.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-RESEARCH.md
</context>

<interfaces>
<!-- Exact existing conventions this plan's new code must match. -->

From docker/csv-processor/Dockerfile (lines ~113-121 — the OCI-label/non-root convention every
image in this repo follows):
    LABEL org.opencontainers.image.revision="${GIT_SHA}" \
          org.opencontainers.image.source="https://github.com/KonuTech/airflow-platform" \
          org.opencontainers.image.version="${GIT_SHA}"
    USER 1000

From Makefile's image-csv-processor target (the exact target shape to mirror for image-airflow —
GIT_SHA computed inline via `git rev-parse --short HEAD` for BOTH the build-arg and the -t tag,
never a shortcut, because tests/policy/test_no_latest_image_tag.py reads this recipe body directly):
    GIT_SHA := $(shell git rev-parse --short HEAD)
    image-csv-processor:
        docker build --build-arg GIT_SHA=$$(git rev-parse --short HEAD) \
          -t csv-processor:$$(git rev-parse --short HEAD) -f docker/csv-processor/Dockerfile .
        docker tag csv-processor:$(GIT_SHA) localhost:5001/csv-processor:$(GIT_SHA)
        docker push localhost:5001/csv-processor:$(GIT_SHA)

From airflow/dags/_common/kpo.py (full file — the ONLY shared code between the two DAG tasks;
`common_kpo_kwargs()` runs at DAG-PARSE time, so it can only ever hold STATIC values):
    _S3_ENDPOINT_URL = "http://minio.data.svc.cluster.local:9000"
    _VAULT_ADDR = "http://vault.vault.svc.cluster.local:8200"
    _VAULT_K8S_ROLE = "csv-processor"
    def common_kpo_kwargs(*, resources, extra_env_vars=None) -> dict[str, object]:
        return {
            "namespace": "etl", "service_account_name": "csv-processor", ...,
            "env_vars": [
                k8s.V1EnvVar(name="DATAPLAT_DB_DSN", value="vault://etl/analytics-db#dsn"),
                ...,
                *(extra_env_vars or []),
            ],
        }

From airflow/dags/csv_ingest_customers.py (the exact site this plan changes — only the operator
class name, `**common_kpo_kwargs(...)` stays untouched, `discover` at lines ~123-130 stays a plain
KubernetesPodOperator per D-12):
    from airflow.providers.cncf.kubernetes.operators.pod import KubernetesPodOperator
    ...
    ingest = KubernetesPodOperator.partial(
        task_id="ingest", cmds=["dataplat"], retries=3, retry_exponential_backoff=True,
        max_active_tis_per_dag=5,
        **common_kpo_kwargs(resources=_INGEST_RESOURCES, extra_env_vars=_INGEST_EXTRA_ENV_VARS),
    ).expand(arguments=build_ingest_args(discover.output))

From tests/policy/test_dag_thinness.py (the exact by-name exemption mechanism, currently covering
only kpo.py's kubernetes.client.models import — extend this SET, do not weaken the scan itself):
    _EXEMPT_FROM_IMPORT_CHECK = frozenset({"airflow/dags/_common/kpo.py"})
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: docker/airflow/Dockerfile and make image-airflow</name>
  <files>docker/airflow/Dockerfile, Makefile</files>
  <read_first>
    docker/csv-processor/Dockerfile
    Makefile
    docker/airflow/.gitkeep
  </read_first>
  <action>
    Delete `docker/airflow/.gitkeep` (now superseded by a real file in the same directory) and
    create `docker/airflow/Dockerfile`: `ARG GIT_SHA=unknown`, `FROM apache/airflow:3.3.0-python3.12`,
    one `RUN pip install --no-cache-dir "apache-airflow[otel]==3.3.0" --constraint
    "https://raw.githubusercontent.com/apache/airflow/constraints-3.3.0/constraints-3.12.txt"`
    (RESEARCH.md's Standard Stack section already verified this extra's necessity live — the stock
    image has zero `opentelemetry` packages), then the same `LABEL
    org.opencontainers.image.{revision,source,version}` block `docker/csv-processor/Dockerfile`
    already uses, with `${GIT_SHA}`. No `WORKDIR`/`ENTRYPOINT` override — the base image already
    sets both correctly; this file only layers one dependency install plus labels, never a
    multi-stage build (unlike csv-processor's from-scratch `uv` build). Do NOT add
    `_PIP_ADDITIONAL_REQUIREMENTS` anywhere — that is a documented Airflow anti-pattern
    (RESEARCH.md's own Alternatives Considered table), not a substitute for this Dockerfile.

    In the Makefile, add an `image-airflow` target immediately after `image-csv-processor`, mirroring
    its exact shape: build (`--build-arg GIT_SHA=$$(git rev-parse --short HEAD) -t
    airflow:$$(git rev-parse --short HEAD) -f docker/airflow/Dockerfile .`), tag
    (`docker tag airflow:$(GIT_SHA) localhost:5001/airflow:$(GIT_SHA)`), push
    (`docker push localhost:5001/airflow:$(GIT_SHA)`) — the same two inline `git rev-parse --short
    HEAD` calls (build-arg + `-t`), not a shortcut, so `tests/policy/test_no_latest_image_tag.py`
    keeps passing against this new target too. Add `image-airflow` to the `.PHONY` line.
  </action>
  <acceptance_criteria>
    - `test -f docker/airflow/.gitkeep` fails (file removed)
    - `docker build -f docker/airflow/Dockerfile -t airflow:test --build-arg GIT_SHA=test .` exits 0
    - `docker run --rm airflow:test pip list | grep -i opentelemetry` returns at least one match (the exact gap RESEARCH.md verified live is closed)
    - `grep -n "^image-airflow:" Makefile` matches, and its recipe body contains two separate `git rev-parse --short HEAD` invocations
    - `uv run pytest tests/policy/test_no_latest_image_tag.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Helm values wiring — custom image, StatsD both profiles, otel_on local only</name>
  <files>helm/values/local/airflow.yaml, helm/values/ci/airflow.yaml</files>
  <read_first>
    helm/values/local/airflow.yaml
    helm/values/ci/airflow.yaml
  </read_first>
  <action>
    In BOTH `helm/values/local/airflow.yaml` and `helm/values/ci/airflow.yaml`: repoint
    `defaultAirflowRepository`/`defaultAirflowTag` from `apache/airflow`/`"3.3.0"` to the
    locally-built, locally-pushed image (`localhost:5001/airflow`, tag = the git SHA `make
    image-airflow` just pushed — read this file's own existing `defaultAirflowRepository` key
    location first, and repoint it the same way `image-csv-processor`'s consumer already resolves
    its own tag, e.g. via an Airflow Variable or a values-file placeholder this repo's existing
    convention already establishes for the csv-processor image; match whatever mechanism is already
    used there rather than inventing a second one). Flip `statsd.enabled` from `false` to `true` in
    BOTH profiles — this is NOT a local/ci divergence axis; Airflow's own metrics stay on StatsD
    regardless of environment, per CONTEXT.md D-02. Update the existing header comment at this exact
    site (currently reads "Phase 7 owns metrics ... disabled in both profiles for now") to state
    Phase 7 has now enabled it, in both profiles, per D-02, and that `otelCollector.metricsEnabled`
    must stay `false`/unset everywhere (the chart's own StatsD-XOR-OTel mutual exclusion,
    `.claude/CLAUDE.md` §H) — only tracing uses OTel here, never Airflow's own metrics.

    In `helm/values/local/airflow.yaml` ONLY, add `config.traces.otel_on: "True"` and
    `config.traces.otel_exporter_otlp_endpoint` (or the equivalent `env`/`extraEnv` entry setting
    `OTEL_EXPORTER_OTLP_ENDPOINT`, per CLAUDE.md's note that the modern mechanism is the standard
    `OTEL_EXPORTER_OTLP_*` env vars, not the deprecated `otel_host`/`otel_port` keys) pointing at
    `http://otel-collector.monitoring.svc.cluster.local:4318` — the OTel Collector Service Plan
    07-03 deployed (verify the exact Service name via `kubectl -n monitoring get svc` once that
    plan's chart is live, since the exact name depends on the chart's own service-naming convention;
    do not guess without checking). Do NOT set `otel_on`/the OTLP endpoint in
    `helm/values/ci/airflow.yaml` — CI has no OTel Collector deployed (D-16), so tracing stays off
    there; this IS a legitimate local/ci divergence and falls under the already-permitted "monitoring
    enablement" axis.
  </action>
  <acceptance_criteria>
    - `grep -n "statsd:" -A2 helm/values/local/airflow.yaml helm/values/ci/airflow.yaml` shows `enabled: true` in both
    - `grep -n "otel_on" helm/values/local/airflow.yaml` matches; `grep -n "otel_on" helm/values/ci/airflow.yaml` returns no match
    - `grep -n "localhost:5001/airflow" helm/values/local/airflow.yaml helm/values/ci/airflow.yaml` matches both
    - `make manifests` still exits 0 (both profiles still render and pass kubeconform after this edit)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TracingKubernetesPodOperator — the build_pod_request_obj() override</name>
  <files>airflow/dags/_common/kpo.py, airflow/dags/_common/tracing_kpo.py, airflow/dags/csv_ingest_customers.py, tests/policy/test_dag_thinness.py</files>
  <read_first>
    airflow/dags/_common/kpo.py
    airflow/dags/csv_ingest_customers.py
    tests/policy/test_dag_thinness.py
  </read_first>
  <behavior>
    - No active OTel span, `otel_on` disabled: `TracingKubernetesPodOperator.build_pod_request_obj()`
      returns a pod spec with NO `TRACEPARENT` env var appended (genuinely no-op-safe, matching
      `opentelemetry.propagate.inject()`'s own documented no-op-when-no-context behavior).
    - An active OTel span exists (simulate via `opentelemetry.sdk.trace.TracerProvider` +
      `start_as_current_span` in the test, matching Plan 07-02's own real backend): the override
      appends exactly one `k8s.V1EnvVar(name="TRACEPARENT", value=...)` whose value matches the W3C
      `traceparent` header format (`00-<32 hex>-<16 hex>-<2 hex>`) and correctly encodes the active
      span's trace/span IDs.
    - Calling the override twice with two DIFFERENT active spans produces two DIFFERENT
      `TRACEPARENT` values — proves the value is genuinely per-execution, not a DAG-parse-time
      constant (the exact defect RESEARCH.md Pitfall 2 warns against).
  </behavior>
  <action>
    In `airflow/dags/_common/kpo.py`, add one new static env var to `common_kpo_kwargs()`'s
    `env_vars` list (this one is genuinely static — the collector's endpoint never varies
    per-execution, unlike a trace ID, so it belongs here and not in the subclass):
    `k8s.V1EnvVar(name="OTEL_EXPORTER_OTLP_ENDPOINT", value="http://otel-collector.monitoring.svc.cluster.local:4318")`
    — added for BOTH `discover` and `ingest` (both pods get real `dataplat` tracing/metrics
    available; only `ingest` additionally receives a per-execution `TRACEPARENT`, via the subclass
    below). Update this module's docstring/constant block to name the new endpoint constant
    alongside the existing `_S3_ENDPOINT_URL`/`_VAULT_ADDR` constants.

    Create `airflow/dags/_common/tracing_kpo.py`: `class TracingKubernetesPodOperator
    (KubernetesPodOperator)` overriding `build_pod_request_obj(self, context=None)` — call
    `super().build_pod_request_obj(context)` to get the already-built `V1Pod`, then
    `carrier: dict[str, str] = {}`, `opentelemetry.propagate.inject(carrier)`, and if
    `"traceparent" in carrier`, append `k8s.V1EnvVar(name="TRACEPARENT", value=carrier["traceparent"])`
    to `pod.spec.containers[0].env` (append, never replace — this pod already has the six env vars
    `common_kpo_kwargs()` set), then return `pod`. Give this file the same module-docstring
    convention `kpo.py` itself uses (states its own scope up front, references
    `test_dag_thinness.py`'s by-name exemption by name) — this file legitimately imports
    `opentelemetry.propagate` and `kubernetes.client.models`, neither of which is business logic, but
    both trip the same import-based policy scan `kpo.py` is already exempted from.

    In `airflow/dags/csv_ingest_customers.py`, add
    `from _common.tracing_kpo import TracingKubernetesPodOperator` alongside the existing
    `KubernetesPodOperator` import, and change ONLY `ingest`'s operator class from
    `KubernetesPodOperator.partial(...)` to `TracingKubernetesPodOperator.partial(...)` — every
    other argument at that call site (`task_id`, `cmds`, `retries`, `retry_exponential_backoff`,
    `max_active_tis_per_dag`, `**common_kpo_kwargs(...)`) stays byte-for-byte unchanged. `discover`
    (D-12) stays a plain `KubernetesPodOperator`, untouched.

    In `tests/policy/test_dag_thinness.py`, add `"airflow/dags/_common/tracing_kpo.py"` to the
    existing `_EXEMPT_FROM_IMPORT_CHECK` frozenset (same by-name mechanism `kpo.py` already uses) —
    do not add a broader exemption pattern, and do not exempt this file from the SQL-string check
    (it should never contain one, matching `kpo.py`'s own deliberate narrowness).

    Write the `<behavior>` cases above as a new unit test (place it under `tests/unit/` in whichever
    location this repo's existing DAG-adjacent unit tests live — check for a precedent directory
    such as `tests/unit/dags/` before creating a new one) using `apache-airflow-providers-cncf-kubernetes`
    already available via the root `pyproject.toml`'s `dev` dependency group (confirmed present:
    `apache-airflow-providers-cncf-kubernetes==10.19.0`) and `opentelemetry-sdk` (already added to
    `packages/dataplat/pyproject.toml` by plan 07-02, and importable in the same `dev`-group venv
    since `dataplat` is a workspace member) — no live cluster or live pod needed for this proof.
  </action>
  <verify>
    <automated>uv run pytest tests/unit -k tracing_kpo -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "OTEL_EXPORTER_OTLP_ENDPOINT" airflow/dags/_common/kpo.py` matches
    - `grep -n "class TracingKubernetesPodOperator" airflow/dags/_common/tracing_kpo.py` matches
    - `grep -n "def build_pod_request_obj" airflow/dags/_common/tracing_kpo.py` matches
    - `grep -n "TracingKubernetesPodOperator.partial" airflow/dags/csv_ingest_customers.py` matches; `grep -n "discover = KubernetesPodOperator(" airflow/dags/csv_ingest_customers.py` still matches unchanged (discover stays plain)
    - `grep -n "tracing_kpo.py" tests/policy/test_dag_thinness.py` matches inside `_EXEMPT_FROM_IMPORT_CHECK`
    - `uv run pytest tests/unit -k tracing_kpo -q` exits 0, proving all three `<behavior>` cases
    - `uv run pytest tests/policy/test_dag_thinness.py -q` still exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Airflow scheduler/worker pod → launched KPO pod's spec | Trace context is written into a pod spec the API server then schedules |
| Custom Airflow image build | A new, git-sha-tagged image layered on the pinned upstream base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | `docker/airflow/Dockerfile`'s pinned constraints URL | mitigate | Pinned to the exact `constraints-3.3.0/constraints-3.12.txt` URL (matching STACK.md's own installation recipe) — never an unpinned/latest constraints fetch |
| T-07-12 | Information Disclosure | `TRACEPARENT` env var value | accept | A W3C trace/span ID pair, not a credential — carries no PII, no secret material; visible in `kubectl describe pod` the same way every other non-secret env var already is |
| T-07-SC | Tampering (supply chain) | `apache-airflow[otel]` extra's transitive package set | accept | Resolves under Airflow's own pinned constraints file (HIGH confidence per RESEARCH.md); not a bare npm/pip/cargo install this phase introduces independently — no new unpinned package source |
| T-07-13 | Elevation of Privilege | New static `OTEL_EXPORTER_OTLP_ENDPOINT` env var on every KPO pod | accept | A plain, non-secret configuration value (a cluster-internal DNS name) — carries no credential, matches `_S3_ENDPOINT_URL`'s own existing precedent in the same file |
</threat_model>

<verification>
- `docker run --rm airflow:test pip list | grep -i opentelemetry` — the custom image genuinely contains OTel packages
- `uv run pytest tests/unit -k tracing_kpo -q` — injection proven correct and per-execution, offline
- `uv run pytest tests/policy/test_dag_thinness.py -q` — DAG-thinness policy still holds with the new exemption
- `make manifests` — both Airflow values profiles still render and validate after the image/statsd/otel edits
</verification>

<success_criteria>
- The custom Airflow image exists, is git-sha-tagged, and genuinely contains `opentelemetry` packages
- `TracingKubernetesPodOperator` injects a correct, per-execution `TRACEPARENT` only when a span is active, and only into `ingest`'s launched pods
- Airflow's own metrics are on StatsD in both profiles; tracing is enabled locally only
</success_criteria>

<output>
Create `.planning/phases/07-observability-metrics-tracing-lineage/07-04-SUMMARY.md` when done
</output>
