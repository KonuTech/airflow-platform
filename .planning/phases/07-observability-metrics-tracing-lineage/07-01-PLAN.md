---
phase: 07-observability-metrics-tracing-lineage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0010_meta_datasets_freshness.py
  - migrations/versions/0011_grafana_reader_role.py
  - migrations/versions/0012_meta_v_customers_lineage.py
  - packages/dataplat/src/dataplat/config/model.py
  - packages/dataplat/src/dataplat/config/registry.py
  - configs/datasets/customers.yaml
  - tests/integration/test_lineage_view.py
  - tests/integration/test_freshness_query.py
  - tests/integration/test_config_registry.py
  - tests/integration/test_migrations.py
autonomous: true
requirements: [OBS-01, OBS-07, OBS-09]
must_haves:
  truths:
    - "A platform operator can run one SQL query (SELECT * FROM meta.v_customers_lineage WHERE customer_id = ...) and get that row's source file, object path, checksum, batch, ingestion timestamp, DAG/run/task ID, processor version, schema version and config version"
    - "A dataset with no freshness: block configured (expected_frequency IS NULL) never appears in the freshness-breach SQL condition's result set, structurally, regardless of file/run history"
    - "A dataset with a configured expected_frequency whose most recent file/run is older than expected_frequency (+ grace) appears in the freshness-breach result set, including on a dataset that has never received a single file (cold start, via meta.datasets.created_at)"
  artifacts:
    - path: "migrations/versions/0010_meta_datasets_freshness.py"
      provides: "meta.datasets.expected_frequency / freshness_warn_after / freshness_fail_after (nullable interval columns)"
      contains: "expected_frequency"
    - path: "migrations/versions/0011_grafana_reader_role.py"
      provides: "the grafana_reader PostgreSQL role, SELECT-only, scoped to meta.datasets/meta.files/meta.ingestion_runs"
      contains: "CREATE ROLE grafana_reader"
    - path: "migrations/versions/0012_meta_v_customers_lineage.py"
      provides: "meta.v_customers_lineage, the OBS-07 lineage view"
      contains: "CREATE VIEW meta.v_customers_lineage"
    - path: "packages/dataplat/src/dataplat/config/model.py"
      provides: "FreshnessConfig, an optional nested block on DatasetConfig"
      contains: "class FreshnessConfig"
    - path: "tests/integration/test_lineage_view.py"
      provides: "proof that meta.v_customers_lineage returns every OBS-07-named column for a genuinely published row"
    - path: "tests/integration/test_freshness_query.py"
      provides: "proof of the NULL-vs-configured structural distinction (OBS-09) and cold-start behavior"
  key_links:
    - from: "packages/dataplat/src/dataplat/config/registry.py"
      to: "meta.datasets.expected_frequency"
      via: "ConfigRegistry._resolve_dataset_id's INSERT ... ON CONFLICT DO UPDATE"
      pattern: "expected_frequency"
    - from: "migrations/versions/0012_meta_v_customers_lineage.py"
      to: "migrations/versions/0011_grafana_reader_role.py"
      via: "down_revision chain + GRANT SELECT ON meta.v_customers_lineage TO grafana_reader"
      pattern: "down_revision\\s*=\\s*.0011."
---

<objective>
Deliver OBS-07 completely and stand up the data foundation OBS-01/OBS-09 need: three small,
sequential migrations (freshness columns on `meta.datasets`, the new least-privilege
`grafana_reader` role, and the `meta.v_customers_lineage` view those two enable), a new
`FreshnessConfig` block threaded from `configs/datasets/customers.yaml` through
`ConfigRegistry.sync()` into the database, and the integration tests that prove both capabilities
— including the sync-to-database write path itself, not just the SQL condition it feeds — against
a real (testcontainers) PostgreSQL; no Helm chart, no live cluster, no Grafana required for any of
this to be true.

Purpose: after this plan, a platform operator can already answer "where did this row come from"
by SQL alone (OBS-07, in full), and the platform already knows, per dataset, whether staleness is
even a concept that applies to it (OBS-09's "never configured" vs "configured and stale"
distinction) — Plan 07-07 later wires a Grafana panel/alert on top of exactly this SQL, unchanged.
Output: `alembic upgrade head` creates `meta.v_customers_lineage` and the three new
`meta.datasets` columns; `meta.datasets` upserts via `ConfigRegistry.sync()` now carry
`customers.yaml`'s `freshness:` block, provably (a real `sync()` call against a real database);
new passing integration tests covering lineage, the freshness structural distinction, and the
config-sync write path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-CONTEXT.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-RESEARCH.md
</context>

<interfaces>
<!-- Real column names this plan's SQL joins against — extracted directly from the
     already-committed migrations, so no exploration is needed. -->

From migrations/versions/0004_meta_ingestion_runs.py (meta.ingestion_runs, all nullable unless noted):
run_id (PK), idempotency_key (unique, not null), dataset_id (FK, not null), file_id (FK), batch_id (FK),
config_version_id (FK, not null), schema_version_id (FK, nullable — LEFT JOIN target),
processor_version (not null), processor_image_digest (not null), dag_id, dag_run_id, task_id,
map_index, try_number, k8s_namespace, k8s_pod_name, k8s_node_name, trace_id, span_id, status (not null),
started_at, finished_at, duration_ms, rows_read, rows_parsed, rows_valid, rows_invalid,
rows_deduplicated, rows_loaded, error_detail (JSONB — excluded from the lineage view, see Security).

From migrations/versions/0001_meta_datasets_config_versions.py (meta.datasets):
dataset_id (PK), dataset_name (unique, not null), source_system, description, is_active, created_at
(not null, server_default now()), updated_at (not null). meta.config_versions: config_version_id (PK),
dataset_id (FK), version, config_hash, hash_version, config_document (JSONB), config_schema_version,
valid_from, valid_to.

From meta.files (migration 0002) and meta.batches (migration 0003) — read the files directly for the
full column list before writing the migration/view SQL; the columns this view needs are: file_id,
object_uri, content_sha256, hash_version, filename, business_date (meta.files) and batch_id, batch_key
(meta.batches).

From migrations/versions/0005_normalized_customers.py (normalized.customers) — read directly for the
full column list; this view needs: id, customer_id, _source_row_number, _record_hash,
_record_hash_version, _ingested_at, _run_id, _file_id, _batch_id.

From migrations/versions/0009_meta_schema_versions.py (meta.schema_versions): schema_version_id (PK),
version, schema_hash.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Three migrations — freshness columns, grafana_reader role, the lineage view</name>
  <files>migrations/versions/0010_meta_datasets_freshness.py, migrations/versions/0011_grafana_reader_role.py, migrations/versions/0012_meta_v_customers_lineage.py</files>
  <read_first>
    migrations/versions/0009_meta_schema_versions.py
    migrations/versions/0008_grant_schema_usage_to_etl_app.py
    migrations/versions/0001_meta_datasets_config_versions.py
    migrations/versions/0002_meta_files.py
    migrations/versions/0003_meta_batches_batch_files.py
    migrations/versions/0004_meta_ingestion_runs.py
    migrations/versions/0005_normalized_customers.py
    tests/integration/test_migrations.py
  </read_first>
  <action>
    First confirm `0009` is still the current migration head (no file under `migrations/versions/`
    has `down_revision = "0009"` yet — it does not, per this session's own directory listing, but
    re-verify before writing revision numbers, matching plan 06-01's own precedent for this exact
    check).

    Create `migrations/versions/0010_meta_datasets_freshness.py` (`revision = "0010"`,
    `down_revision = "0009"`), matching migration 0001's docstring/header convention (module
    docstring naming what it adds and why, `Revision ID:`/`Revises:`/`Create Date:` footer).
    `upgrade()` adds three nullable columns to `meta.datasets` via `op.add_column(..., schema="meta")`:
    `expected_frequency` (`sa.Interval()`), `freshness_warn_after` (`sa.Interval()`),
    `freshness_fail_after` (`sa.Interval()`) — all nullable, no server default (absence is a real,
    load-bearing state per CONTEXT.md D-08: NULL `expected_frequency` means "freshness not tracked
    for this dataset" structurally, not "unset for now"). `downgrade()` drops the same three columns
    in reverse order via `op.drop_column`.

    Create `migrations/versions/0011_grafana_reader_role.py` (`revision = "0011"`,
    `down_revision = "0010"`). This is the first `CREATE ROLE` migration in this codebase (no
    `postInitApplicationSQL` path exists for it — the analytical CNPG cluster is already running,
    matching migration 0008's own `op.execute()`-only shape for non-`op.*`-helper DDL). `upgrade()`
    issues, via `op.execute()`: `CREATE ROLE grafana_reader LOGIN;` (no password — the migration
    never embeds a credential literal per §81; the password is set later, out-of-band, by
    `_ensure_grafana_secrets` in plan 07-06's `scripts/vault-bootstrap.py` change, via
    `ALTER ROLE grafana_reader WITH PASSWORD ...`), then `GRANT USAGE ON SCHEMA meta TO grafana_reader;`
    and `GRANT USAGE ON SCHEMA normalized TO grafana_reader;` (the exact two-schema USAGE-grant shape
    migration 0008 already established for `etl_app`), then
    `GRANT SELECT ON meta.datasets, meta.files, meta.ingestion_runs TO grafana_reader;` — exactly the
    three tables Grafana's dashboard queries and the freshness alert condition read directly (per
    RESEARCH.md's Architecture Patterns); do not grant broader access — `grafana_reader` never gets
    `INSERT`/`UPDATE`/`DELETE` on anything, and never gets table-level access to `normalized.customers`
    (the lineage view in migration 0012 grants exactly what it needs to see that table's data, without
    a direct table grant — Postgres view semantics run under the view owner's privileges, not the
    querying role's). `downgrade()` reverses in the opposite order: `REVOKE SELECT ...`,
    `REVOKE USAGE ON SCHEMA normalized ...`, `REVOKE USAGE ON SCHEMA meta ...`, `DROP ROLE grafana_reader;`.

    Create `migrations/versions/0012_meta_v_customers_lineage.py` (`revision = "0012"`,
    `down_revision = "0011"` — it must chain after 0011 because it grants to `grafana_reader`, which
    0011 creates). `upgrade()` issues one `op.execute()` creating `meta.v_customers_lineage` — use
    RESEARCH.md's Code Examples SQL verbatim (already verified against the real column names listed
    in this plan's `<interfaces>` block above): select every OBS-07-named column
    (`customer_row_id`, `customer_id`, `_source_row_number`, `_record_hash`, `_record_hash_version`,
    `_ingested_at` from `normalized.customers` aliased `c`; `file_id`, `object_uri`, `content_sha256`,
    `hash_version AS file_hash_version`, `filename`, `business_date` from `meta.files` aliased `f`;
    `batch_id`, `batch_key` from `meta.batches` aliased `b`; `run_id`, `idempotency_key`, `dag_id`,
    `dag_run_id`, `task_id`, `map_index`, `try_number`, `k8s_namespace`, `k8s_pod_name`, `trace_id`,
    `span_id`, `processor_version`, `processor_image_digest`, `started_at AS run_started_at`,
    `finished_at AS run_finished_at` from `meta.ingestion_runs` aliased `r`; `version AS config_version`,
    `config_hash` from `meta.config_versions` aliased `cv`; `version AS schema_version`, `schema_hash`
    from `meta.schema_versions` aliased `sv`), joining `c` to `r`/`f`/`b`/`cv` via plain `JOIN` (all
    four FKs are `NOT NULL` on `normalized.customers`/`meta.ingestion_runs`) and to `sv` via
    `LEFT JOIN` (`meta.ingestion_runs.schema_version_id` is nullable). Deliberately EXCLUDE
    `error_detail` from the SELECT list (Security Domain finding: raw exception JSONB must never
    reach a dashboard/view without the same redaction discipline `observability/logging.py`'s
    `_redact()` already applies at the logging layer). Follow with two more `op.execute()` calls:
    `GRANT SELECT ON meta.v_customers_lineage TO etl_app;` and
    `GRANT SELECT ON meta.v_customers_lineage TO grafana_reader;`. `downgrade()` is
    `DROP VIEW meta.v_customers_lineage;`.
  </action>
  <acceptance_criteria>
    - `alembic -c migrations/alembic.ini upgrade head` against a throwaway PostgreSQL exits 0
    - `psql` against that database shows `meta.datasets` has columns `expected_frequency`, `freshness_warn_after`, `freshness_fail_after`, all `interval`, all nullable
    - `psql -c "\du grafana_reader"` shows the role exists with `Login` attribute and no superuser/createrole attributes
    - `psql -c "\dp meta.v_customers_lineage"` shows `grafana_reader` and `etl_app` both holding `SELECT`
    - `grep -n "error_detail" migrations/versions/0012_meta_v_customers_lineage.py` returns no match inside the `CREATE VIEW` SELECT list
    - `alembic -c migrations/alembic.ini downgrade -1` three times in a row (0012→0011→0010→0009) exits 0 each time with no error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FreshnessConfig — the opt-in config block, threaded through sync() into meta.datasets</name>
  <files>packages/dataplat/src/dataplat/config/model.py, packages/dataplat/src/dataplat/config/registry.py, configs/datasets/customers.yaml</files>
  <read_first>
    packages/dataplat/src/dataplat/config/model.py
    packages/dataplat/src/dataplat/config/registry.py
    configs/datasets/customers.yaml
  </read_first>
  <action>
    In `config/model.py`, add `class FreshnessConfig(BaseModel)` following `BatchingConfig`'s exact
    template (`model_config = ConfigDict(extra="forbid", frozen=True)`). Fields: `expected_frequency:
    str` (required when the block is present — a PostgreSQL interval literal, e.g. `"1 day"`; parsing
    stays server-side via a `::interval` cast at the SQL layer, never a Python interval parser),
    `warn_after: str | None = None`, `fail_after: str | None = None` (same interval-literal shape,
    both optional grace periods added on top of `expected_frequency` before a warn/fail threshold
    fires). Add a `@model_validator(mode="after")` (mirroring `DatasetConfig`'s own
    `_check_delimiter_does_not_collide_with_decimal_separator` template) that rejects the combination
    `warn_after is not None and fail_after is not None` when a naive string-lexical comparison is
    insufficient to prove ordering — since these are opaque Postgres interval literals Python cannot
    safely compare without a parser, do NOT attempt numeric validation here; instead document in the
    docstring that ordering (`warn_after <= fail_after`) is enforced by Postgres at query time in the
    freshness condition, not by this model, and skip adding an unenforceable validator. Add
    `freshness: FreshnessConfig | None = None` to `DatasetConfig` (same optional-nested-block shape as
    the existing `filename: FilenameMaskConfig | None = None`), with a docstring line matching that
    field's style (opt-in per dataset, `customers.yaml` will declare one).

    In `config/registry.py`, widen `ConfigRegistry._resolve_dataset_id`'s signature to
    `_resolve_dataset_id(cur, dataset_name, *, expected_frequency, warn_after, fail_after)` (all three
    new params typed `str | None`) and widen its `INSERT INTO meta.datasets (...)` statement to also
    insert/upsert `expected_frequency`, `freshness_warn_after`, `freshness_fail_after` — bind each new
    param through a `%s::interval` placeholder (so Postgres parses the interval literal; binding
    `None` still resolves to `NULL` correctly through the same placeholder) and add all three to the
    `ON CONFLICT (dataset_name) DO UPDATE SET ...` clause (`expected_frequency = EXCLUDED.expected_frequency`,
    etc.) exactly like `dataset_name` is today — so a dataset whose `freshness:` block is later removed
    from its YAML correctly nulls these columns back out on the next `sync()`, never leaving stale
    freshness state behind. Update `sync()`'s call site to extract `config.freshness.expected_frequency`
    /`.warn_after`/`.fail_after` (or `None`/`None`/`None` when `config.freshness is None`) and pass them
    into the widened `_resolve_dataset_id` call.

    In `configs/datasets/customers.yaml`, add a top-level `freshness:` block sitting alongside
    `batching:` (same top-level-key convention this file's own header comment documents):
    `expected_frequency: "1 day"`, `warn_after: "2 hours"`, `fail_after: "6 hours"` — a reasonable
    illustrative value for a daily customer feed, consistent with this file being the first real,
    non-illustrative dataset config in the repository.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from dataplat.config.model import FreshnessConfig, DatasetConfig; print(DatasetConfig.model_fields['freshness'])"` exits 0 and shows an `Optional`/`| None` annotation
    - `grep -n "class FreshnessConfig" packages/dataplat/src/dataplat/config/model.py` matches
    - `grep -n "expected_frequency" packages/dataplat/src/dataplat/config/registry.py` matches inside both the `INSERT` and the `ON CONFLICT ... DO UPDATE` clauses
    - `uv run python -c "from dataplat.config.loader import load_config; c = load_config('customers'); print(c.freshness)"` (or the equivalent already-established loader call site) exits 0 and prints a non-`None` `FreshnessConfig`
    - `uv run pytest tests/unit -k config -q` exits 0 (no existing config test broken by the new optional field)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integration tests — the lineage view, the freshness structural distinction, and the config-sync write path</name>
  <files>tests/integration/test_lineage_view.py, tests/integration/test_freshness_query.py, tests/integration/test_config_registry.py, tests/integration/test_migrations.py</files>
  <read_first>
    tests/integration/test_run_ingest.py
    tests/integration/test_config_registry.py
    tests/integration/conftest.py
    tests/integration/test_migrations.py
  </read_first>
  <action>
    Create `tests/integration/test_lineage_view.py`. Reuse (import or closely mirror)
    `test_run_ingest.py`'s own `env`/`_seed_pending_run`/`_make_ctx` fixture chain and its real
    `dataplat.pipeline.run.run_ingest()` call — do NOT hand-write raw `INSERT` statements across five
    tables; the existing fixture chain already produces one genuinely published `normalized.customers`
    row with every lineage column (`_run_id`/`_file_id`/`_batch_id`) populated exactly as production
    code populates them, which is a stronger and lower-drift-risk proof than a hand-seeded row. After
    a successful `run_ingest()` call, query
    `SELECT * FROM meta.v_customers_lineage WHERE customer_id = %s` (or `customer_row_id` matching the
    published row) against the same `migrated_dsn`, and assert every OBS-07-named column
    (`object_uri`, `content_sha256`, `batch_id`, `run_started_at`, `dag_id`/`dag_run_id`/`task_id`
    — `NULL` is an acceptable value for these three since this fixture chain does not run inside
    Airflow, `processor_version`, `config_version`, `schema_version`) is present in the row and that
    `file_hash_version`/`content_sha256` match the seeded file's own values byte-for-byte.

    Create `tests/integration/test_freshness_query.py`, SQL-only (no `dataplat` code under test —
    matches its own classification in RESEARCH.md's Test Map). Seed three rows directly into
    `meta.datasets` (`INSERT INTO meta.datasets (dataset_name, expected_frequency, ...)`) against
    `migrated_dsn`: (a) `expected_frequency IS NULL` — freshness never configured; (b)
    `expected_frequency = '1 hour'`, no `meta.files`/`meta.ingestion_runs` rows ever inserted for it,
    `created_at` set (via a direct `UPDATE ... SET created_at = now() - interval '2 hours'` after
    insert, since the column has a `now()` server default) to over an hour in the past — the cold-start
    case; (c) `expected_frequency = '1 day'`, with a `meta.files` row inserted whose `discovered_at`
    is recent (e.g. `now() - interval '1 hour'`). Run RESEARCH.md's Code Examples freshness `HAVING`
    query (Architecture Patterns / Code Examples section) verbatim against `migrated_dsn`, and assert:
    row (a)'s `dataset_id` never appears in the result set (proves the structural `WHERE
    expected_frequency IS NOT NULL` exclusion — OBS-09's "never configured, stays quiet"); row (b)'s
    `dataset_id` DOES appear (proves the `COALESCE(MAX(f.discovered_at), d.created_at)` cold-start
    fallback); row (c)'s `dataset_id` does NOT appear (fresh, not stale). Keep this exact SQL text
    (or a clearly-labeled constant matching it verbatim) somewhere retrievable in this test file's own
    source, since plan 07-07's Grafana alert rule embeds the identical query and must not drift from
    what this test proves correct.

    Extend `tests/integration/test_config_registry.py` — the established, existing test file for
    exactly this mechanism (its own module docstring: "Proves ARCHITECTURE.md §5.1's exact sync
    rule against a real, migrated PostgreSQL") — with a new test,
    `test_sync_persists_freshness_config_to_meta_datasets`, reusing this file's own module-scoped
    `registry`/`customers_config` fixtures (do not add a new fixture). Since `customers_config` now
    carries this plan's own Task 2 `freshness:` block (`expected_frequency: "1 day"`,
    `warn_after: "2 hours"`, `fail_after: "6 hours"`), call `registry.sync(...)` with it under a
    dedicated, isolated dataset name (e.g. `"freshness_sync_round_trip"` — matching
    `test_get_by_id_returns_the_exact_config_that_was_synced`'s own precedent of syncing under a name
    other than `"customers"`, so this new test never disturbs the three existing `test_sync_*`
    tests' shared-row, execution-order-dependent narrative this file's own module docstring
    documents). After `sync()` returns, query `meta.datasets` directly (a fresh
    `psycopg.connect(migrated_dsn)`, matching this file's own `_config_version_rows` helper's own
    connection style) for `expected_frequency`/`freshness_warn_after`/`freshness_fail_after` on that
    dataset's row, and assert all three are present and equal the exact interval values
    `customers.yaml`'s `freshness:` block declares — psycopg's default adapter returns a Postgres
    `interval` as `datetime.timedelta`, so assert against `timedelta(days=1)`/`timedelta(hours=2)`/
    `timedelta(hours=6)` respectively, never a string comparison. This is the one assertion this plan
    was otherwise missing: proof that `ConfigRegistry.sync()` — the actual mechanism OBS-01/OBS-09
    depend on, not the raw-`INSERT`-seeded rows `test_freshness_query.py` and `test_lineage_view.py`'s
    fixture chain both use instead — writes D-08's three freshness columns correctly against a real
    database.

    In `tests/integration/test_migrations.py`, extend whatever structural assertion set already
    covers views/roles (if `_table_set`'s underlying query includes views, add
    `("meta", "v_customers_lineage")` to `EXPECTED_TABLES`; if it is scoped to `information_schema.tables
    WHERE table_type = 'BASE TABLE'`, no change is needed there). Add a new assertion (a small new
    test function, following this file's existing naming convention, e.g.
    `test_grafana_reader_role_exists_and_is_select_only`) that queries `pg_roles`/`information_schema
    .role_table_grants` and asserts `grafana_reader` exists, has no `INSERT`/`UPDATE`/`DELETE` grant on
    any table, and holds `SELECT` on exactly `meta.datasets`, `meta.files`, `meta.ingestion_runs` and
    `meta.v_customers_lineage`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_lineage_view.py -m integration -x -q` exits 0
    - `uv run pytest tests/integration/test_freshness_query.py -m integration -x -q` exits 0
    - `uv run pytest tests/integration/test_config_registry.py -m integration -x -q` exits 0 (all tests, including the new `test_sync_persists_freshness_config_to_meta_datasets`)
    - `uv run pytest tests/integration/test_migrations.py -m integration -x -q` exits 0 (all tests, including the new grafana_reader assertion)
    - the freshness test's assertion list explicitly names all three seeded datasets by their distinguishing property (never a bare row-count check that could pass for the wrong reason)
    - the new `test_config_registry.py` test asserts all three of `meta.datasets`'s `expected_frequency`/`freshness_warn_after`/`freshness_fail_after` columns against real `timedelta` values after a genuine `ConfigRegistry.sync()` call — never a Python-level-only check of `FreshnessConfig`'s fields, and never a raw-`INSERT`-seeded row
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `meta.v_customers_lineage` → Grafana/any SQL client | A read-only projection surface over five tables, queried by a lower-privilege role than `etl_app` |
| Alembic migration execution | DDL run with elevated (schema-owner) privileges against the analytical database |
| `configs/datasets/*.yaml` → `meta.datasets` | Config-not-code input, parsed by Pydantic before ever reaching SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `meta.v_customers_lineage`'s SELECT list | medium | mitigate | `error_detail` (raw exception JSONB, may carry stack/context text) is deliberately excluded from the view's column list — enforced by this plan's own acceptance criteria grep |
| T-07-02 | Elevation of Privilege | `grafana_reader` role (migration 0011) | high | mitigate | Dedicated role, `SELECT`-only, scoped to exactly three tables plus the lineage view — never reuses `etl_app` (which holds `INSERT`/`UPDATE`) or any superuser-adjacent role; proven by `test_grafana_reader_role_exists_and_is_select_only` |
| T-07-03 | Tampering | Migration DDL (`CREATE ROLE`/`CREATE VIEW`) | low | accept | Runs only via `alembic upgrade head` against a database the developer already controls; no untrusted input reaches this DDL, matching every prior migration's own threat posture |
| T-07-04 | Input Validation | `FreshnessConfig` (config/model.py) | low | mitigate | `extra="forbid"`/`frozen=True` (ASVS V5), matching every sibling config model; a typo'd key fails config validation loudly rather than being silently ignored |
</threat_model>

<verification>
- `alembic -c migrations/alembic.ini upgrade head` (against throwaway PostgreSQL) creates `meta.v_customers_lineage`, the three `meta.datasets` freshness columns, and the `grafana_reader` role
- `uv run pytest tests/integration/test_lineage_view.py tests/integration/test_freshness_query.py tests/integration/test_config_registry.py tests/integration/test_migrations.py -m integration -q` — all green
- `uv run pytest tests/unit -k config -q` — no regression in existing config model tests
</verification>

<success_criteria>
- `meta.v_customers_lineage` exists and answers OBS-07's literal wording — one query, every named column, for a genuinely published row
- `meta.datasets` carries `expected_frequency`/`freshness_warn_after`/`freshness_fail_after`, nullable, wired end-to-end from `configs/datasets/customers.yaml` through `ConfigRegistry.sync()` — proven by a real `sync()` call against a real database, not just by inspection of the code path
- The freshness SQL condition structurally distinguishes "never configured" from "configured and stale," including correct cold-start behavior with zero prior file history
- `grafana_reader` exists, is `SELECT`-only, and is scoped to exactly the tables/view it needs
</success_criteria>

<output>
Create `.planning/phases/07-observability-metrics-tracing-lineage/07-01-SUMMARY.md` when done
</output>
