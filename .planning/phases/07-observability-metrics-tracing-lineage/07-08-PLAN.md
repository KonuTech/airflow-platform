---
phase: 07-observability-metrics-tracing-lineage
plan: 08
type: execute
wave: 4
depends_on: ["07-04", "07-05", "07-07"]
files_modified:
  - tests/e2e/observability/__init__.py
  - tests/e2e/observability/conftest.py
  - tests/e2e/observability/test_trace_propagation.py
  - tests/e2e/observability/test_alert_webhook_delivery.py
autonomous: true
requirements: [OBS-10, OBS-01, OBS-09]
must_haves:
  truths:
    - "A real csv_ingest_customers DagRun's ingest pod carries a well-formed TRACEPARENT env var in its live spec, and meta.ingestion_runs.trace_id for that run equals the trace ID encoded in it (D-12/OBS-10, proof over prose)"
    - "A real, forced freshness breach causes Grafana's Alerting engine to actually POST to an in-cluster webhook receiver with the expected payload shape — not merely an assertion that the underlying SQL predicate is true (D-20)"
    - "Both tests restore every piece of live state they mutate (the freshness-breaching row, the webhook Secret's contents) in a finally block, leaving the cluster exactly as they found it"
  artifacts:
    - path: "tests/e2e/observability/test_trace_propagation.py"
      provides: "the OBS-10 live-cluster proof"
    - path: "tests/e2e/observability/test_alert_webhook_delivery.py"
      provides: "the D-20 live-cluster alert-delivery proof"
    - path: "tests/e2e/observability/conftest.py"
      provides: "the in-cluster webhook-receiver Pod+Service fixture — genuinely new to this codebase"
  key_links:
    - from: "tests/e2e/observability/test_trace_propagation.py"
      to: "meta.ingestion_runs.trace_id"
      via: "poll_ingestion_run-style polling against the live analytical database"
      pattern: "trace_id"
    - from: "tests/e2e/observability/test_alert_webhook_delivery.py"
      to: "the in-cluster webhook receiver"
      via: "kubectl logs on the receiver pod, asserting an observed POST"
      pattern: "webhook"
---

<objective>
Deliver the two live-cluster proofs CONTEXT.md D-19/D-20 require and every earlier plan in this
phase deliberately deferred: a real `csv_ingest_customers` DagRun whose `ingest` pod carries a
correctly-propagated `TRACEPARENT`, matched against the `trace_id` the processor itself persisted
(OBS-10, end to end); and a real, forced data-freshness breach that causes Grafana's own Alerting
engine to actually deliver an HTTP POST to a receiver running inside the cluster (D-20) — because
Grafana's Alerting engine runs in-cluster and cannot reach a pytest-process-local `localhost`
listener, this plan's own `conftest.py` deploys a minimal, throwaway receiver Pod+Service for each
test's duration, the single most novel testing mechanism this phase introduces.

Purpose: after this plan, every mechanism the rest of Phase 7 built is proven not just correct in
isolation (testcontainers, unit tests, `helm template`) but genuinely working together, live,
matching the exact "real, not simulated" bar Phase 4 (`kubectl delete pod`) and Phase 5 (live
credential rotation) already established for this project.
Output: `tests/e2e/observability/` — a new, permanent, `pytest.mark.cluster`-gated test package.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-CONTEXT.md
@.planning/phases/07-observability-metrics-tracing-lineage/07-RESEARCH.md
</context>

<interfaces>
<!-- Exact existing fixtures/conventions this plan's new package must reuse, not reinvent. -->

From tests/e2e/vault/conftest.py (the kubectl/kubectl_context/_require_cluster shape — this
directory's own established convention is that EACH tests/e2e/*/conftest.py carries its own copy
of these small helpers rather than sharing a library module; mirror this, do not import across
directories):
    @pytest.fixture(scope="session")
    def kubectl_context(cluster_name: str) -> str: return f"kind-{cluster_name}"
    @pytest.fixture(scope="session", autouse=True)
    def _require_cluster(kubectl_context: str) -> None: ...  # pytest.skip with a named reason if unreachable
    @pytest.fixture(scope="session")
    def kubectl(kubectl_context: str) -> Callable[..., subprocess.CompletedProcess[str]]: ...
    @pytest.fixture(scope="session")
    def vault_root_client(...) -> hvac.Client: ...  # authenticated root-token client, for the
                                                       # webhook-Secret override in test 2

From tests/e2e/slice/conftest.py (poll_ingestion_run — line ~619; open_analytics_connection — a
plain function, not fixture-only, exposed specifically so a test can open a second connection
without fighting pytest's fixture caching; and the _unpause_slice_dags autouse fixture that already
keeps csv_ingest_customers unpaused for every test in this repository's e2e suites):
    def poll_ingestion_run(...) -> ...: ...   # polls meta.ingestion_runs by idempotency_key/run_id,
                                                 # never a fixed sleep — this repo's established idiom
    def open_analytics_connection(kubectl_context_value, kubectl_json_fn, kubectl_fn) -> Connection: ...

From tests/e2e/vault/test_rotation.py (the force/observe/restore-in-finally shape D-20 explicitly
extends — full test body, lines ~124-193):
    before = ...read current state...
    try:
        ...force the condition (a live Vault KV write / a live Postgres UPDATE)...
        ...observe the effect (poll, never sleep)...
        assert ...
    finally:
        ...restore the original state, unconditionally...
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The observability e2e package — fixtures, including the in-cluster webhook receiver</name>
  <files>tests/e2e/observability/__init__.py, tests/e2e/observability/conftest.py</files>
  <read_first>
    tests/e2e/vault/conftest.py
    tests/e2e/slice/conftest.py
  </read_first>
  <action>
    Create `tests/e2e/observability/__init__.py` (empty package marker, matching every sibling
    `tests/e2e/*/`'s own trivial shape). Create `tests/e2e/observability/conftest.py`: duplicate
    (do not import) `tests/e2e/vault/conftest.py`'s `repo_root`/`cluster_name`/`kubectl_context`/
    `_require_cluster`/`kubectl`/`kubectl_json` fixtures verbatim, per this directory's own
    established convention (this plan's own `<interfaces>` block). Add a `vault_root_client` fixture
    (same shape as `tests/e2e/vault/conftest.py`'s own — a session-scoped `kubectl port-forward` to
    `svc/vault:8200` plus an `hvac.Client` authenticated from `.secrets/vault-init.json`) — Task 3
    needs it to temporarily override the webhook URL Vault secret.

    Add a function-scoped `webhook_receiver` fixture that: picks a unique name (e.g.
    `webhook-receiver-<short uuid>`) to avoid colliding with a leftover pod from an interrupted
    prior run; deploys a minimal Pod into namespace `monitoring` running a small Python HTTP server
    (use an image already present in this project's own pull set — `python:3.12-slim`, matching
    `docker/csv-processor/Dockerfile`'s own base — with `--command` overriding the entrypoint to run
    an inline `python3 -c "..."` one-file `http.server`-based handler that: listens on `0.0.0.0:8080`,
    logs the HTTP method, path and full request body of every incoming request to stdout in a
    single-line, greppable format (e.g. `WEBHOOK_RECEIVED: <body>`), and responds `200`); a matching
    ClusterIP Service (`port: 8080`) selecting that Pod's label; waits (poll, not sleep) for the Pod
    to reach `Running`; yields the Service's in-cluster DNS URL
    (`http://<service-name>.monitoring.svc.cluster.local:8080/webhook`) AND a small helper callable
    the test can use to `kubectl logs` the pod and check for a `WEBHOOK_RECEIVED:` line; tears down
    both the Pod and the Service unconditionally in a `finally`/fixture-teardown block, even if the
    test itself fails. Document, in this fixture's own docstring, why it exists (Grafana's Alerting
    engine runs in-cluster and cannot reach a pytest-process-local listener — the exact constraint
    RESEARCH.md's own Wave 0 Gaps section names).
  </action>
  <acceptance_criteria>
    - `grep -n "def webhook_receiver" tests/e2e/observability/conftest.py` matches, and its docstring names the in-cluster-reachability constraint
    - running `pytest tests/e2e/observability -m cluster --collect-only -q` against the live cluster exits 0 (fixtures at least import/collect cleanly)
    - manually invoking the `webhook_receiver` fixture (e.g. via a throwaway one-line test) against the live cluster leaves zero `webhook-receiver-*` pods behind after teardown, success or failure
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: test_trace_propagation.py — a real ingest pod, a real trace_id, matched</name>
  <files>tests/e2e/observability/test_trace_propagation.py</files>
  <read_first>
    tests/e2e/slice/test_smoke_and_idempotency.py
    tests/e2e/slice/conftest.py
  </read_first>
  <action>
    Mark the module `pytestmark = pytest.mark.cluster` (matching every sibling e2e module). Reuse
    `tests/e2e/slice/`'s own established file-drop-and-poll mechanism (mirror
    `test_smoke_and_idempotency.py`'s own shape: upload a small, uniquely-named customers CSV to
    `s3://raw/customers/` via the already-established MinIO fixture chain, so the already-unpaused
    `csv_ingest_customers` DAG's `S3KeySensor` → `discover` → `ingest` chain runs naturally — do not
    invent a second triggering mechanism). Poll (never sleep — `poll_ingestion_run`'s own idiom)
    `meta.ingestion_runs` for the resulting run's `trace_id` to become non-`NULL` (bounded by a
    generous timeout matching this suite's own existing timeout conventions). Once observed: (1)
    `kubectl get pod` the launched `ingest` pod (find it by label/name pattern matching this DAG's
    own KubernetesExecutor-assigned pod naming) BEFORE it is deleted (`common_kpo_kwargs()`'s
    `on_finish_action: "delete_succeeded_pod"` means this is a race — either poll fast enough after
    the DAG-run trigger, or assert against the pod's `V1Pod` spec captured via a `kubectl get
    events`/pod-watch mechanism if the pod itself has already been deleted by observation time;
    prefer capturing the spec via `kubectl get pod <name> -o json` in a tight poll loop racing the
    deletion, and treat a genuinely-missed capture as a test infrastructure issue to fix, not a
    silently-skipped assertion) and assert its `spec.containers[0].env` contains a `TRACEPARENT`
    entry matching the W3C format (`00-<32 hex>-<16 hex>-<2 hex>`); (2) assert the `trace_id` hex
    segment inside that `TRACEPARENT` value equals `meta.ingestion_runs.trace_id` for the SAME run,
    read via `open_analytics_connection` (reused from `tests/e2e/slice/conftest.py`, imported the
    same way this repository's own precedent already establishes for cross-directory conftest
    reuse — confirm the exact import mechanism `tests/e2e/slice/conftest.py` itself uses to reuse
    `tests/e2e/cluster/conftest.py`'s fixtures, and mirror it).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/e2e/observability/test_trace_propagation.py -m cluster -x -q` exits 0 against the live cluster
    - the test's assertions explicitly compare the pod-spec `TRACEPARENT`'s trace-ID segment against `meta.ingestion_runs.trace_id`, never asserting only one side
    - the test never uses a fixed-duration `time.sleep()` as its wait mechanism — only bounded polling
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: test_alert_webhook_delivery.py — force a breach, observe a real POST, restore</name>
  <files>tests/e2e/observability/test_alert_webhook_delivery.py</files>
  <read_first>
    tests/e2e/vault/test_rotation.py
  </read_first>
  <action>
    Mark the module `pytestmark = pytest.mark.cluster`. Follow `test_rotation.py`'s own
    force/observe/restore-in-`finally` shape exactly (this plan's own `<interfaces>` block). Before:
    read the CURRENT `grafana/alert-webhook` Vault secret value (via `vault_root_client`) and the
    current state of one chosen test dataset's `meta.datasets` row (`expected_frequency` and
    whichever files/runs exist for it). Force the condition, in this order: (1) point the webhook
    AT the Task 1 `webhook_receiver` fixture's in-cluster URL — write it to
    `grafana/alert-webhook` in Vault via `vault_root_client`, then re-apply the
    `grafana-alert-webhook` Kubernetes Secret's `GRAFANA_ALERT_WEBHOOK_URL` key to match (mirroring
    `scripts/vault-bootstrap.py`'s own `_ensure_grafana_secrets` Secret-materialization shape,
    invoked directly or via a small duplicated helper in this test file — do not require a Grafana
    pod restart if the chart's own `envFromSecret` wiring re-reads the Secret on next evaluation;
    if it does NOT (env vars are typically process-start-only), explicitly `kubectl rollout restart`
    the Grafana deployment and wait for the new pod to become Ready before proceeding — verify which
    is actually true against the live chart and implement whichever is correct); (2) force a
    freshness breach against a dataset dedicated to this test (never `customers` itself, to avoid
    interfering with other suites' own assumptions about its state) by directly `UPDATE`-ing (or
    `INSERT`-ing, if none exists) a `meta.datasets` row with `expected_frequency = interval '1
    second'` AND `freshness_fail_after = interval '1 second'` — Plan 07-07's two-tier design (Blocker
    fix: warn_after-gated and fail_after-gated rules now fire independently, `labels: {severity:
    warning}` / `labels: {severity: critical}`) means setting BOTH thresholds this low breaches BOTH
    rules on the very next evaluation tick, so this one forced breach proves the full two-severity
    chain, not just the warn tier — and no recent `meta.files`/`meta.ingestion_runs` activity —
    guaranteed stale the moment Grafana's next alert-rule evaluation tick runs. Observe: poll
    (bounded, generous timeout covering at least two of the alert rule group's own `interval: 5m`
    evaluation cycles, or temporarily tighten the rule's own interval for this test via the same
    live-Secret/values mechanism if that proves faster and still genuine) the webhook receiver's
    logs (via Task 1's fixture helper) for a `WEBHOOK_RECEIVED:` line carrying `"severity":
    "critical"` (Grafana's alert payload includes its rule's labels — assert the FAIL-tier rule
    specifically fired, not merely that some alert of some severity arrived), and assert the
    captured body contains the test dataset's own name/identifier, proving Grafana's real evaluation
    → real contact point → real HTTP delivery chain fired for this specific breach, not a stale
    unrelated alert. In `finally`:
    restore the original `meta.datasets` row exactly (delete it if this test inserted it, or
    restore its prior column values if it already existed), restore the original
    `grafana/alert-webhook` Vault secret value and the Kubernetes Secret's matching key, and (if a
    Grafana rollout restart was needed to pick up the test URL) restart it again to pick up the
    restored value — with the finally block's own final assertion confirming the restored value
    round-trips correctly (matching `test_rotation.py`'s own "restore, with its own closing
    assertion" convention).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/e2e/observability/test_alert_webhook_delivery.py -m cluster -x -q` exits 0 against the live cluster
    - the test asserts the captured webhook payload's content (dataset name) AND a `"severity": "critical"` marker confirming the FAIL-tier rule specifically fired, not merely that SOME request of SOME severity arrived
    - after the test completes (pass or fail), `grafana/alert-webhook` in Vault and the `grafana-alert-webhook` Kubernetes Secret both hold their pre-test values (verified by a follow-up manual read)
    - the test never uses a fixed-duration `time.sleep()` longer than strictly necessary to wait out one evaluation-interval floor — polling is the primary wait mechanism
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → live cluster | Full read/write access via the developer's own kubeconfig context, same trust model every prior `tests/e2e/*` suite already operates under |
| The throwaway webhook-receiver Pod | An ad-hoc, test-owned workload running inside the cluster for the test's duration only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Tampering | This test suite's own live writes (Vault secret, `meta.datasets` row, Kubernetes Secret) | medium | mitigate | Every mutation is restored in a `finally` block with its own closing assertion, matching `test_rotation.py`'s established precedent; the test dataset is dedicated to this suite, never `customers` |
| T-07-24 | Denial of Service | The webhook-receiver Pod left running after a crashed test process | low | mitigate | Fixture teardown runs in a `finally`/pytest fixture-teardown path regardless of test outcome; the pod's uniquely-suffixed name makes an orphan (if the whole process is killed, e.g. `SIGKILL`, before teardown) easy to identify and manually clean up, and does not collide with a future run's own fixture instance |
| T-07-25 | Information Disclosure | The webhook-receiver Pod logs the full request body | low | accept | Test-owned, ephemeral, in-cluster-only Pod; the payload is Grafana's own alert-notification JSON (dataset names, threshold values) — no credential or PII ever reaches it, by construction of everything upstream in this phase |
</threat_model>

<verification>
- `uv run pytest tests/e2e/observability -m cluster -q` — both tests green against the live cluster
- Manual: no `webhook-receiver-*` pods remain in `monitoring` after a full suite run
- Manual: `grafana/alert-webhook` (Vault) and the `grafana-alert-webhook` Kubernetes Secret both hold their original values after the suite runs
</verification>

<success_criteria>
- A real `ingest` pod's `TRACEPARENT` matches the `trace_id` the processor itself persisted — OBS-10 proven end to end, live
- A real freshness breach causes a real webhook POST to arrive — D-20 proven, "proof over prose"
- Neither test leaves any live-cluster state mutated after it completes
</success_criteria>

<output>
Create `.planning/phases/07-observability-metrics-tracing-lineage/07-08-SUMMARY.md` when done
</output>
