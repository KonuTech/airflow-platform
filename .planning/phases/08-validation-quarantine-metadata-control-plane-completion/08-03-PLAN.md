---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/dataplat/src/dataplat/metadata/postgres.py
  - tests/integration/test_validation_persistence.py
  - tests/integration/test_backfill_resolution.py
autonomous: true
requirements: [VALID-04, VALID-08]
must_haves:
  truths:
    - "After calling record_validation_results/record_rejected_records inside an open transaction, the rows are visible once that transaction commits, and rolled back together if the transaction rolls back"
    - "resolve_rejected_records_for_batch is the ONLY code path anywhere in this codebase that can change resolution_type/resolved_by_run_id, and it only ever operates on a whole batch's PENDING rows, never a single row"
  artifacts:
    - path: "packages/dataplat/src/dataplat/metadata/postgres.py"
      provides: "PostgresMetadataRepository implementations of record_validation_results, record_rejected_records, resolve_rejected_records_for_batch"
      exports: ["record_validation_results", "record_rejected_records", "resolve_rejected_records_for_batch"]
  key_links:
    - from: "packages/dataplat/src/dataplat/metadata/postgres.py"
      to: "meta.rejected_records"
      via: "resolve_rejected_records_for_batch's UPDATE ... WHERE batch_id = %s AND resolution_type = 'PENDING'"
      pattern: "WHERE batch_id = %s AND resolution_type = 'PENDING'"
---

<objective>
Implement the three `MetadataRepository` Protocol methods plan 08-01 stubbed, against real PostgreSQL, and prove them directly — with no Airflow, no pipeline, no barrier stage involved — that persistence and whole-batch-only resolution behave exactly as D-04/D-05/D-11 require.

Purpose: this is the "metadata completion" stream (S9) ROADMAP's Wave E names — the SQL-level proof that later plans' barrier stages (referential integrity, circuit breaker) and the eventual `pipeline/run.py` wiring can build on without re-deriving transaction semantics themselves.
Output: `PostgresMetadataRepository`'s three new method bodies, plus two integration test files proving persistence-inside-a-transaction and whole-batch-only resolution directly against `migrated_dsn`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
</context>

<interfaces>
<!-- The exact Protocol signatures this plan implements, from plan 08-01's
metadata/repository.py stubs -- read that file directly before writing
postgres.py's bodies. -->
See `packages/dataplat/src/dataplat/metadata/repository.py`'s
`record_validation_results`/`record_rejected_records`/
`resolve_rejected_records_for_batch` docstrings (plan 08-01) for the exact
signatures and `conn`-ownership contract to implement against.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostgresMetadataRepository — implement the three new methods</name>
  <files>packages/dataplat/src/dataplat/metadata/postgres.py</files>
  <read_first>packages/dataplat/src/dataplat/metadata/postgres.py, packages/dataplat/src/dataplat/metadata/repository.py, packages/dataplat/src/dataplat/models/report.py, packages/dataplat/src/dataplat/models/record.py</read_first>
  <behavior>
    - `record_validation_results(conn, run_id, results)`: with `results=[]`, executes zero INSERTs and does not raise. With N results, one row lands in `meta.validation_results` per `ValidationResult`, all columns populated verbatim (including `threshold`/`observed` dicts round-tripping through JSONB).
    - `record_rejected_records(conn, run_id, file_id, batch_id, rejected)`: with `rejected=[]`, no-op. With N `RejectedRecord`s, N rows land in `meta.rejected_records`, `resolution_type` defaults to `'PENDING'` (server default, never set explicitly by this method), `run_id`/`file_id`/`batch_id` populated from the method's own arguments (never from the `RejectedRecord` dataclass, which carries none of them).
    - `resolve_rejected_records_for_batch(conn, batch_id, resolved_by_run_id, resolution_type)`: updates only rows matching `batch_id` AND `resolution_type = 'PENDING'`; returns the count of rows updated (`cursor.rowcount`); a second call with the same arguments updates 0 rows (already resolved) and returns `0`, never raises.
  </behavior>
  <action>
Implement `record_validation_results`: never opens its own connection (mirrors `finalize_publication`'s documented exception) — takes `conn: Connection[Any]` directly. Uses `conn.cursor().executemany(...)` or a loop of `conn.execute(...)` calls (match this file's existing style — no `executemany` precedent exists in this file today, so prefer a simple loop of parameterized `conn.execute` calls, one per `ValidationResult`, each binding `run_id, result.rule_id, result.rule_type, result.severity, result.outcome, result.evaluated_count, result.failed_count, Jsonb(result.threshold), Jsonb(result.observed)` — import `Jsonb` from `psycopg.types.json`, matching `config/registry.py`'s existing usage).

Implement `record_rejected_records`: same `conn`-never-opened-here shape, one parameterized `INSERT INTO meta.rejected_records (run_id, file_id, batch_id, source_row_number, source_byte_offset, raw_line, error_type, error_column, error_message) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)` per `RejectedRecord` (no `source_byte_offset` field exists on `RejectedRecord` today — bind `None` for it explicitly; `resolution_type` is never set here, it uses the migration's `'PENDING'` server default).

Implement `resolve_rejected_records_for_batch`: single `UPDATE meta.rejected_records SET resolution_type = %s, resolved_by_run_id = %s WHERE batch_id = %s AND resolution_type = 'PENDING'` via `conn.execute(...)`, binding `(resolution_type, resolved_by_run_id, batch_id)`. Return `cursor.rowcount` (an `int`). Docstring must explicitly state this is the ONLY method in this class that can ever change `resolution_type`/`resolved_by_run_id` — no narrower per-row variant exists anywhere, matching D-04's hard constraint (RESEARCH.md's own Anti-Patterns entry: never build one "for debugging").
  </action>
  <verify>
    <automated>pytest tests/integration/test_validation_persistence.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - All three methods take `conn: Connection[Any]` as their first keyword-only parameter and never call `self._pool.connection()`
    - `record_validation_results`/`record_rejected_records` never call `conn.commit()`/`conn.rollback()`
    - `resolve_rejected_records_for_batch` returns an `int` equal to `cursor.rowcount`
  </acceptance_criteria>
  <done>All three methods take `conn: Connection[Any]` as their first keyword-only parameter and never call `self._pool.connection()`</done>

</task>

<task type="auto">
  <name>Task 2: Integration tests — transactional persistence + whole-batch-only resolution</name>
  <files>tests/integration/test_validation_persistence.py, tests/integration/test_backfill_resolution.py</files>
  <read_first>tests/integration/test_publish_merge.py, tests/integration/conftest.py</read_first>
  <action>
`test_validation_persistence.py`, `@pytest.mark.integration`, depends on `migrated_dsn`: build a real `meta.datasets`/`meta.files`/`meta.batches`/`meta.ingestion_runs` row set via direct SQL helpers (matching `test_publish_merge.py`'s own `_insert_config_version`-style local-helper convention — do not import `StagingLoader`/`run_ingest`). Test A (commit path): open a real `psycopg.Connection`, begin a transaction, call `record_validation_results` and `record_rejected_records` with 2-3 synthetic entries each, commit, then re-query `meta.validation_results`/`meta.rejected_records` on a FRESH connection and assert the rows are visible with correct values (including a `threshold`/`observed` dict round-tripping through JSONB unchanged). Test B (rollback path — Pitfall 2's exact distinguishing case): open a second transaction, call both methods again with new synthetic entries, then explicitly roll back (`conn.rollback()` or let the `with conn.transaction():` block raise), re-query on a fresh connection, and assert NONE of the second batch's rows exist — proving these writes genuinely participate in the caller's transaction rather than auto-committing.

`test_backfill_resolution.py`, `@pytest.mark.integration`, depends on `migrated_dsn`: seed 3 `PENDING` `meta.rejected_records` rows across 2 different `batch_id`s (2 rows in batch A, 1 in batch B) via direct SQL. Call `resolve_rejected_records_for_batch(conn, batch_id=<A>, resolved_by_run_id=<some real run_id>, resolution_type="REDRIVEN")` inside a transaction, commit. Assert: both batch-A rows now have `resolution_type='REDRIVEN'` and `resolved_by_run_id` set; the batch-B row is untouched (`resolution_type='PENDING'`, `resolved_by_run_id IS NULL`) — proving the whole-batch `WHERE` scope never leaks across batches. Call the SAME resolve call a second time and assert it returns `0` (idempotent no-op on already-resolved rows). Assert via direct `information_schema`/`pg_catalog` inspection (or a documented code-review note in the test's own docstring) that no OTHER method on `PostgresMetadataRepository`/`MetadataRepository` can set `resolution_type` — i.e. grep the class source for `resolution_type` and assert exactly one method body references it (a simple `inspect.getsource` + `.count()` assertion is sufficient, matching this codebase's existing policy-test style).
  </action>
  <verify>
    <automated>pytest tests/integration/test_validation_persistence.py tests/integration/test_backfill_resolution.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass against a real testcontainers PostgreSQL 18
    - `test_backfill_resolution.py` proves cross-batch isolation (batch B untouched) and idempotent re-resolution (second call returns 0)
  </acceptance_criteria>
  <done>Both test files pass against a real testcontainers PostgreSQL 18</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataplat pipeline code -> meta.rejected_records/validation_results | Every write in this plan is issued by trusted `dataplat` code, never directly by external input; the untrusted-content path (raw_line) is bound via `%s`, never formatted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Tampering | `record_rejected_records`'s raw_line binding | mitigate | `%s` placeholder binding exclusively for `error_message`/`raw_line` — these carry untrusted CSV content and must never be string-formatted into SQL text |
| T-08-08 | Repudiation | resolve_rejected_records_for_batch's single-writer path | mitigate | The method itself is the only write path (V4 Access Control, enforced structurally here and at the DB-privilege level by plan 08-01's no-DELETE grant) — proven by Task 2's source-inspection assertion |
</threat_model>

<verification>
- `pytest tests/integration/test_validation_persistence.py tests/integration/test_backfill_resolution.py -x -m integration` green
</verification>

<success_criteria>
The metadata-completion stream's persistence layer is proven correct in isolation — later plans (08-08 referential, 08-09 volume anomaly, 08-11 publish-transaction wiring) call these methods with confidence, never re-testing their transactional contract.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-03-SUMMARY.md` when done
</output>
