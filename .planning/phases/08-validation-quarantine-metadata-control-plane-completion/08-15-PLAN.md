---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 15
type: execute
wave: 8
depends_on: ["08-14"]
files_modified:
  - tests/e2e/slice/test_backfill_reentry.py
autonomous: true
gap_closure: true
requirements: [VALID-08]
must_haves:
  truths:
    - "A backfill invocation that loses Airflow's own transient in-flight row-lock race is retried automatically (bounded attempts, short backoff) instead of silently burning the test's full 300s timeout"
    - "When the backfill invocation genuinely cannot proceed (exhausts retries) or the re-executed DagRun never reaches success, the failure message names Airflow's own backfill_dag_run.exception_reason value directly, not just a bare clear_number-never-advanced timeout"
  artifacts:
    - path: "tests/e2e/slice/test_backfill_reentry.py"
      provides: "A retrying, self-diagnosing airflow backfill create invocation for VALID-08's live re-drive proof"
      contains: "_fetch_latest_backfill_exception_reason"
  key_links:
    - from: "tests/e2e/slice/test_backfill_reentry.py::_run_backfill_and_wait_for_reexecution"
      to: "backfill_dag_run.exception_reason (Airflow metadata DB)"
      via: "airflow_metadata_connection SQL query"
      pattern: "exception_reason"
---

<objective>
Close UAT gap 2 (`.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-HUMAN-UAT.md`, test 2): `test_backfill_resolves_previously_rejected_row` makes exactly one `airflow backfill create` CLI invocation with no retry for Airflow 3.3.0's own documented-transient `backfill_dag_run.exception_reason='in flight'` race (a `SELECT ... FOR UPDATE SKIP LOCKED` losing to a concurrent scheduler transaction), so a single lost lock race burns the full 300s timeout and surfaces as an indistinguishable "never re-executed" failure instead of the well-understood transient it actually is.

Root cause is fully diagnosed and confirmed live in `.planning/debug/backfill-does-not-redrive-rejected-row.md` (source-trace of the installed `apache-airflow==3.3.0` `airflow/models/backfill.py`, plus a live re-invocation of the identical CLI command that succeeded immediately on the second try, no code change). This is a test-robustness gap in this repository's own test helper — NOT a `dataplat`/DAG application defect. The separately-flagged batch_key/content_sha256 architecture question (`deferred-items.md`, "From plan 08-14") remains untouched by this plan; it was never reachable by this specific failure and stays a distinct, still-open question.

Purpose: make VALID-08's live re-drive proof resilient to Airflow's own known-transient backfill race, so it produces a fast, unambiguous signal on genuine failure instead of a 300s false-negative timeout.
Output: `tests/e2e/slice/test_backfill_reentry.py` retries `airflow backfill create` on `exception_reason == 'in flight'` and surfaces `exception_reason` in failure diagnostics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/debug/backfill-does-not-redrive-rejected-row.md
</context>

<interfaces>
<!-- Schema/behavior facts the executor needs, extracted from the live-verified debug session
     (.planning/debug/backfill-does-not-redrive-rejected-row.md) — no further live-cluster
     investigation should be needed to implement this fix. -->

Airflow metadata DB tables involved (both live in the `airflow_metadata_connection` fixture's
database, `airflow-db`, confirmed via live SQL in the debug session):

- `backfill` — one row per `airflow backfill create` invocation. Relevant columns: `id`
  (PK, autoincrement — a retried invocation for the SAME dag_id/logical_date gets a NEW `id`,
  confirmed live: `backfill_id=1` then `backfill_id=2` for two consecutive CLI calls),
  `dag_id`.
- `backfill_dag_run` — one row per `(backfill_id, logical_date)` pair. Relevant columns: `id`
  (PK), `backfill_id` (FK -> `backfill.id`), `logical_date`, `dag_run_id` (FK -> `dag_run.id`,
  NULL when `exception_reason` is set — confirmed live), `exception_reason` (nullable text;
  observed live value on a lost lock race: the literal string `"in flight"`; NULL on success).

Existing helper in `tests/e2e/slice/test_backfill_reentry.py` this plan modifies:

- `_run_backfill_and_wait_for_reexecution(kubectl_fn, airflow_conn, *, dag_id, dag_run_id,
  logical_date_iso, pre_backfill_clear_number, timeout=...)` — currently invokes
  `airflow backfill create` exactly once via `kubectl_fn(...)`, asserts `returncode == 0`, then
  polls `dag_run.state`/`dag_run.clear_number` (via `dag_id`+`run_id`, NOT `logical_date`) until
  `clear_number > pre_backfill_clear_number and state == 'success'`, or raises `AssertionError`
  on timeout. The caller (`test_backfill_resolves_previously_rejected_row`) already has both a
  `logical_date` (a live `datetime` value, read from `dag_run.logical_date` via SQL) and a
  `logical_date_iso` (`logical_date.isoformat()`, used only for the CLI's `--from-date`/
  `--to-date` args) in scope before calling this helper — pass the `datetime` value through as a
  new parameter for the SQL-side lookup; do not re-parse it from the ISO string.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retry-on-in-flight + exception_reason diagnostics in test_backfill_reentry.py</name>
  <files>tests/e2e/slice/test_backfill_reentry.py</files>
  <read_first>.planning/debug/backfill-does-not-redrive-rejected-row.md, tests/e2e/slice/test_backfill_reentry.py, tests/e2e/slice/conftest.py</read_first>
  <behavior>
    This is test-infrastructure code exercised only against a live `-m cluster` cluster, so there
    is no offline unit-test target for the new SQL helper's live behavior. Correctness is instead
    proven by: (1) `ruff check` passing, (2) `pytest --collect-only` succeeding (import-time and
    signature correctness with no live cluster), and (3) the full `-m cluster` run against the
    live cluster reaching a clean pass or a fast, self-diagnosing failure — no more silent 300s
    timeouts on the known transient. There is no separate unit-test file to add for this fix.
  </behavior>
  <action>
    Add three module-level constants near the existing `_BACKFILL_DAGRUN_TIMEOUT_SECONDS`/
    `_POLL_INTERVAL_SECONDS` block: `_BACKFILL_CREATE_MAX_ATTEMPTS` (int, `3`),
    `_BACKFILL_CREATE_RETRY_BACKOFF_SECONDS` (float, `5.0` — a short, bounded backoff per the
    UAT gap's own wording), and `_BACKFILL_ROW_SETTLE_TIMEOUT_SECONDS` (float, `15.0` — how long
    to poll for the `backfill_dag_run` row to appear after the CLI returns; per the debug
    session's own source trace this row is written synchronously within the same CLI invocation,
    so this is a small safety margin, not the primary wait mechanism).

    Add a new helper function `_fetch_latest_backfill_exception_reason(conn, *, dag_id,
    logical_date)` (`conn: psycopg.Connection[Any]`, `dag_id: str`, `logical_date:
    datetime.datetime`, returning `str | None`) that runs a single SQL query joining
    `backfill_dag_run` (aliased `bdr`) to `backfill` (aliased `b`) on `b.id = bdr.backfill_id`,
    filtering `b.dag_id = %s AND bdr.logical_date = %s`, ordering `b.id DESC, bdr.id DESC`
    (the most recent invocation's row for this exact dag_id/logical_date pair — a retried
    invocation creates a NEW `backfill.id`, per the Interfaces section above), `LIMIT 1`, and
    returning the single `exception_reason` column value, or `None` if no row exists yet. Give it
    a docstring citing `.planning/debug/backfill-does-not-redrive-rejected-row.md` as the source
    of this schema/behavior, matching this module's own existing docstring convention of citing
    its evidence.

    Extract the existing CLI-invocation block (the `kubectl_fn(...)` call building `airflow
    backfill create ...` plus its `assert backfill.returncode == 0` check, currently inline at
    the top of `_run_backfill_and_wait_for_reexecution`) into its own private helper
    `_invoke_backfill_create_once(kubectl_fn, *, dag_id, logical_date_iso)` returning the
    `CompletedProcess`, with the same argument list shape and the same assertion message,
    unchanged in substance.

    In `_run_backfill_and_wait_for_reexecution`, add a new `logical_date: datetime.datetime`
    keyword parameter (per the Interfaces section — the caller already has this value; update
    the one call site in `test_backfill_resolves_previously_rejected_row` to pass it). Replace
    the single unconditional CLI invocation with a bounded retry loop: for each attempt in
    `range(1, _BACKFILL_CREATE_MAX_ATTEMPTS + 1)`, call `_invoke_backfill_create_once`, then poll
    `_fetch_latest_backfill_exception_reason` for the target `dag_id`/`logical_date` (bounded by
    `_BACKFILL_ROW_SETTLE_TIMEOUT_SECONDS`, sleeping `_POLL_INTERVAL_SECONDS` between polls)
    until a non-`None` value is observed or the settle timeout elapses. If the observed value
    equals the literal string `"in flight"`: sleep `_BACKFILL_CREATE_RETRY_BACKOFF_SECONDS`
    (skip the sleep on the final attempt) and continue to the next attempt. Any other outcome —
    a `None` exception_reason (Airflow's own success case, per the debug session's live proof)
    or any non-`"in flight"` value — breaks out of the retry loop and falls through to the
    existing re-execution wait logic, unchanged. If every attempt is exhausted still observing
    `"in flight"`, raise `AssertionError` immediately, WITHOUT proceeding to the re-execution
    wait loop, with a message naming `dag_id`, `logical_date`, the attempt count, and citing
    `.planning/debug/backfill-does-not-redrive-rejected-row.md` as the known cause — this is the
    fast, unambiguous failure this gap closure requires, replacing the old behavior of silently
    burning the full 300s timeout on this exact transient.

    In the existing re-execution wait loop's own timeout branch (the final `raise
    AssertionError(msg)` in `_run_backfill_and_wait_for_reexecution`, reached only when the
    retry loop above got past `"in flight"` but the DagRun still never reached `clear_number >
    pre_backfill_clear_number and state == 'success'` within `timeout`), call
    `_fetch_latest_backfill_exception_reason` one more time for the same `dag_id`/`logical_date`
    and append its value to the existing failure message (e.g. a trailing
    `latest backfill_dag_run.exception_reason=...` clause), so this failure mode is also
    self-diagnosing instead of a bare clear_number-never-advanced timeout, per the UAT gap's
    second `missing` item.

    Update `_run_backfill_and_wait_for_reexecution`'s docstring, and the module docstring's
    "confirmed live" claim about the backfill mechanism if it does not already account for the
    transient in-flight race, to note the retry behavior and cite
    `.planning/debug/backfill-does-not-redrive-rejected-row.md` — matching this module's own
    established convention of documenting non-obvious live-cluster behavior inline.

    Do NOT modify `dataplat`/`csv_processor`/DAG application code, `resolve_rejected_records_for_batch`,
    or `discovery.py` — the debug session confirmed live that the root cause is entirely inside
    this test's own single-invocation CLI helper, not application code. This is a test-file-only
    fix (scope boundary, per the debug session's own Resolution section and the UAT gap's own
    diagnosis).
  </action>
  <verify>
    <automated>ruff check tests/e2e/slice/test_backfill_reentry.py && pytest tests/e2e/slice/test_backfill_reentry.py --collect-only -q && pytest tests/e2e/slice/test_backfill_reentry.py -x -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check tests/e2e/slice/test_backfill_reentry.py` is clean
    - `pytest tests/e2e/slice/test_backfill_reentry.py --collect-only -q` succeeds with no live cluster (import-time and signature correctness)
    - Against a live cluster, `pytest tests/e2e/slice/test_backfill_reentry.py -x -m cluster` either passes cleanly, or — if it still fails — fails FAST (well under the old 300s) with a message that names `backfill_dag_run.exception_reason` directly, never a bare "clear_number never advanced" timeout on the known `"in flight"` transient
    - No file outside `tests/e2e/slice/test_backfill_reentry.py` is modified
  </acceptance_criteria>
  <done>The helper retries `airflow backfill create` up to `_BACKFILL_CREATE_MAX_ATTEMPTS` times on Airflow's own `exception_reason == "in flight"` transient before declaring failure, and every failure path (retry-exhausted and re-execution-timeout) surfaces `backfill_dag_run.exception_reason` in its message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> live kind cluster (kubectl exec, Airflow metadata DB) | Same trust boundary `test_backfill_reentry.py` already crosses (established in plan 08-14) — no new surface, only a bounded retry and an additional read-only diagnostic SQL query against a table this suite already reads (`dag_run`/`backfill_dag_run` share the same connection/credentials as the existing `airflow_metadata_connection` fixture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-24 | Denial of Service | Bounded retry loop around `airflow backfill create` | accept | Capped at `_BACKFILL_CREATE_MAX_ATTEMPTS=3` with a fixed short backoff — cannot loop unboundedly; each retried invocation is idempotent by the same D-1 idempotency guarantees the original test already relies on (re-running backfill against the same logical_date is a documented-safe operation per this module's own docstring) |
</threat_model>

<verification>
- `ruff check tests/e2e/slice/test_backfill_reentry.py` clean
- `pytest tests/e2e/slice/test_backfill_reentry.py --collect-only -q` succeeds offline
- `pytest tests/e2e/slice/test_backfill_reentry.py -x -m cluster` against the live kind cluster: either a clean pass, or a fast (not 300s) failure whose message names `backfill_dag_run.exception_reason`
</verification>

<success_criteria>
UAT gap 2 is closed: `test_backfill_resolves_previously_rejected_row` no longer treats Airflow's own documented-transient `"in flight"` backfill race as an indistinguishable 300s timeout — it retries automatically and, on any remaining failure, names the exact `exception_reason` observed. Once this passes end-to-end against the live cluster, the separately-flagged batch_key/content_sha256 architecture question (`deferred-items.md`, "From plan 08-14") should be re-verified in a future session — noted here for the SUMMARY, not actioned by this plan.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-15-SUMMARY.md` when done
</output>
