---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 12
type: execute
wave: 6
depends_on: ["08-02", "08-11"]
files_modified:
  - airflow/dags/csv_ingest_customers.py
  - airflow/dags/csv_ingest_orders.py
  - tests/unit/test_dag_structure.py
autonomous: true
requirements: [VALID-07, VALID-08, LOAD-10]
must_haves:
  truths:
    - "csv_ingest_customers gains an integrity_gate task upstream of discover, and its ingest task declares outlets=[customers_asset] -- an Asset a second DAG can schedule against (D-15)"
    - "csv_ingest_orders exists, mirrors csv_ingest_customers's shape exactly (D-14), is schedule=[customers_asset]-triggered rather than cron-triggered, and also gates discover behind integrity_gate"
    - "discover never runs in either DAG when integrity_gate fails for a given file -- proven structurally via DagBag task-dependency inspection, no live cluster needed"
  artifacts:
    - path: "airflow/dags/csv_ingest_orders.py"
      provides: "The second real ingestion DAG (D-14), Asset-triggered off customers' own publish step (D-15)"
      contains: "schedule=[customers_asset]"
  key_links:
    - from: "airflow/dags/csv_ingest_customers.py"
      to: "airflow/dags/csv_ingest_orders.py"
      via: "an Airflow Asset (customers_asset) -- outlets=[...] on customers' ingest task, schedule=[...] on orders' own @dag decorator"
      pattern: "customers_asset"
---

<objective>
Wire `integrity_gate` (plan 08-02) into both DAGs and stand up `csv_ingest_orders`, the second real ingestion DAG (D-14), coupled to `csv_ingest_customers` via an Airflow Asset (D-15) — the orchestration layer that makes every `dataplat`-side capability built in Waves 1-5 reachable through a real, deployable pipeline.

Purpose: this is the last piece before the phase's live proofs (plan 08-13's `dag.test()` tier, plan 08-14's cluster-marked E2E) can run at all — no DAG, no backfill re-entry, no live orphan race.
Output: `csv_ingest_customers.py`'s `integrity_gate`/`outlets` additions, the new `csv_ingest_orders.py` DAG, and a `test_dag_structure.py` extension proving the structural shape of both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: integrity_gate wiring + outlets on csv_ingest_customers; new csv_ingest_orders DAG</name>
  <files>airflow/dags/csv_ingest_customers.py, airflow/dags/csv_ingest_orders.py</files>
  <read_first>airflow/dags/csv_ingest_customers.py, airflow/dags/_common/kpo.py, airflow/dags/_common/integrity_gate.py, airflow/dags/_common/tracing_kpo.py, configs/datasets/orders.yaml</read_first>
  <action>
In `csv_ingest_customers.py`: import `Asset` from `airflow.sdk` and `integrity_gate` from `_common.integrity_gate`. Declare `customers_asset = Asset("s3://normalized/customers")` at module scope, above the `@dag` function (D-15). Insert `gate = integrity_gate.partial(bucket="raw", dataset_name="customers").expand(key=<derived from wait_for_files' own matched keys — since S3KeySensor does not directly hand a KEY LIST to a downstream task by default, resolve this via a small XCom-pull or by re-listing under bucket_key's prefix inside a thin `@task` wrapper if `S3KeySensor` alone cannot supply it; check `S3KeySensor`'s XCom push behavior in the pinned provider version before assuming `.output` yields a key list — if it does not, `integrity_gate` should instead be a `.partial(bucket="raw", key=<a resolved single wildcard match or a small helper task listing matched keys>)`, whichever the provider version actually supports; document the resolved approach in the DAG's own docstring>` between `wait_for_files` and `discover`: `wait_for_files >> gate >> discover` (replacing the existing `wait_for_files >> discover` edge). Add `outlets=[customers_asset]` to the `ingest` task's `TracingKubernetesPodOperator.partial(...)` kwargs (D-15's actual publish-adjacent task — `ingest` is where `run_ingest`'s publish transaction executes, matching the docstring's own framing of "customers' publish step").

Create `airflow/dags/csv_ingest_orders.py`, mirroring `csv_ingest_customers.py`'s full shape (module docstring adapted to name D-14/D-15/D-16 instead of D-01..D-04; `resolve_window`, `build_ingest_args`, `aggregate_receipts` tasks identical in shape, `dataset="orders"` substituted everywhere `"customers"` appeared): `wait_for_files` (`bucket_key="orders/*.csv"`), `gate = integrity_gate...` (same shape as customers', but `integrity_gate.partial(bucket="raw", dataset_name="orders")`), `discover` (`arguments=["discover", "--dataset", "orders"]`), `ingest` (`arguments` built the same way, `cmds=["dataplat"]`). The `@dag` decorator uses `schedule=[customers_asset]` (imported from `csv_ingest_customers` — `from csv_ingest_customers import customers_asset`, matching Airflow's own multi-file-DAG-module convention within one `dags/` folder) instead of a cron string, `catchup=False`, `max_active_runs=1`, `tags=["vertical-slice", "orders"]`. `csv_ingest_orders.py` does NOT declare its own `outlets=` (D-15: orders only ever consumes the customers asset, it produces none this phase).
  </action>
  <verify>
    <automated>pytest tests/unit/test_dag_structure.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `DagBag(dag_folder="airflow/dags", include_examples=False).import_errors == {}` for both DAG files
    - `csv_ingest_customers`'s `ingest` task has `outlets` containing an `Asset` whose URI is `"s3://normalized/customers"`
    - `csv_ingest_orders`'s `schedule` resolves to `[customers_asset]`, not a cron string
    - `wait_for_files >> gate >> discover` ordering holds in both DAGs (gate is never bypassed)
  </acceptance_criteria>
  <done>`DagBag(dag_folder="airflow/dags", include_examples=False).import_errors == {}` for both DAG files</done>

</task>

<task type="auto">
  <name>Task 2: test_dag_structure.py — both DAGs present, gate wiring, Asset coupling</name>
  <files>tests/unit/test_dag_structure.py</files>
  <read_first>tests/unit/test_dag_structure.py, tests/unit/conftest.py</read_first>
  <action>
Extend `_BOTH_DAG_IDS` to `{"smoke_kubernetes_pod", "csv_ingest_customers", "csv_ingest_orders"}`. Add `test_integrity_gate_upstream_of_discover`: for each of `{"csv_ingest_customers", "csv_ingest_orders"}`, walk the DAG's task dependency graph (`dag.task_dict`, `task.downstream_task_ids`) and assert a task whose callable is `integrity_gate` (or whose `task_id` matches the gate's declared id) appears in `discover`'s `upstream_task_ids`, and `wait_for_files`/the sensor task appears in the gate's own `upstream_task_ids` — proving the `wait_for_files >> gate >> discover` chain structurally, without executing anything. Add `test_orders_dag_present_and_asset_scheduled`: `dagbag.dags["csv_ingest_orders"].schedule` (or the Airflow-3-appropriate attribute exposing the resolved schedule) contains an `Asset` (not a cron string), and its URI matches `"s3://normalized/customers"`. Add `test_customers_ingest_declares_outlets`: `dagbag.dags["csv_ingest_customers"].tasks` contains the `ingest` task with a non-empty `outlets` list containing an `Asset` with the same URI. Extend `test_namespace_and_service_account`/`test_kpo_resources`/`test_retries_set`'s existing `_BOTH_DAG_IDS`-driven loops automatically pick up `csv_ingest_orders` once the constant above is updated — verify they still pass without further per-test changes (their loops are already DAG-id-agnostic).
  </action>
  <verify>
    <automated>pytest tests/unit/test_dag_structure.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All existing `test_dag_structure.py` tests still pass with `csv_ingest_orders` now included in `_BOTH_DAG_IDS`
    - The three new tests pass, proving gate ordering and Asset coupling structurally
  </acceptance_criteria>
  <done>All existing `test_dag_structure.py` tests still pass with `csv_ingest_orders` now included in `_BOTH_DAG_IDS`</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Airflow scheduler process -> integrity_gate task | Already covered by plan 08-02's threat model; this plan only wires the existing, already-hardened task into two DAGs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Denial of Service | The Asset coupling (D-15) never producing an orders run if customers never publishes | accept | This is the intended, documented Airflow scheduling behavior — orders' own `wait_for_files` sensor still independently gates each run on real file arrival regardless of the Asset trigger's cadence |
</threat_model>

<verification>
- `pytest tests/unit/test_dag_structure.py -x` green
</verification>

<success_criteria>
Both DAGs exist, structurally correct, gated by `integrity_gate`, coupled via the customers Asset — ready for plan 08-13's `dag.test()` proof and plan 08-14's live cluster proof.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-12-SUMMARY.md` when done
</output>
