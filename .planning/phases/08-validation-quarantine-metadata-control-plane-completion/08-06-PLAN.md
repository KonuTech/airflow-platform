---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 06
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/dataplat/src/dataplat/discovery.py
  - tests/unit/validate/test_batch_complete_marker.py
  - tools/corpus/generators.py
  - tests/fixtures/slice-corpus.yaml
autonomous: true
requirements: [LOAD-11]
must_haves:
  truths:
    - "A dataset configured with source.batch_complete_marker discovers nothing from a batch directory missing that marker object, and discovers normally once the marker is present -- proven against a corpus fixture, even though no live dataset (customers/orders) opts in this phase (D-19)"
  artifacts:
    - path: "packages/dataplat/src/dataplat/discovery.py"
      provides: "discover_files' opt-in _BATCH_COMPLETE gate: when config.source.batch_complete_marker is set, discovery yields nothing until an object with that marker key is listed"
      contains: "batch_complete_marker"
  key_links:
    - from: "packages/dataplat/src/dataplat/discovery.py"
      to: "packages/dataplat/src/dataplat/config/model.py"
      via: "config.source.batch_complete_marker, added in plan 08-01"
      pattern: "config\\.source\\.batch_complete_marker"
---

<objective>
Implement LOAD-11's `_BATCH_COMPLETE` manifest-marker capability inside `discover_files`, as a corpus-tested but unexercised opt-in — matching Phase 6 D-10's exact precedent (filename masks: built, fixture-proven, `customers.yaml` declares none). No live dataset config turns this on this phase (D-19).

Purpose: ROADMAP's Phase 8 goal names "a documented path back into the pipeline" and success criterion #4 explicitly requires "missing its `_BATCH_COMPLETE` marker is refused before any parsing occurs" as a named scenario. This plan delivers that capability as a real, tested code path — even though neither `customers` nor `orders` exercises it in production this phase.
Output: `discover_files`'s new marker-gate check, a corpus fixture proving both the withheld-until-marker-present and marker-present-discovers-normally cases, and a unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: discover_files' opt-in _BATCH_COMPLETE gate + corpus fixture + unit test</name>
  <files>packages/dataplat/src/dataplat/discovery.py, tests/unit/validate/test_batch_complete_marker.py, tools/corpus/generators.py, tests/fixtures/slice-corpus.yaml</files>
  <read_first>packages/dataplat/src/dataplat/discovery.py, packages/dataplat/src/dataplat/config/model.py, tools/corpus/generators.py</read_first>
  <behavior>
    - `discover_files(..., config=<config with source.batch_complete_marker="_BATCH_COMPLETE">)` against a listed object set that does NOT include an object whose key is `f"{config.source.path}_BATCH_COMPLETE"` returns `[]` — no file is hashed, registered, batched or assigned, even though real data files are present in the listing.
    - The identical call against a listed object set that DOES include that marker object returns discovered units exactly as if `batch_complete_marker` were `None` — and the marker object itself is never treated as a data file (never hashed/registered/staged).
    - `discover_files(..., config=<config with source.batch_complete_marker=None>)` (the `customers`/`orders` default) is byte-for-byte unchanged from before this plan.
  </behavior>
  <action>
In `discovery.py`'s `discover_files`, immediately after the existing `listed = sorted(objects.list_objects(...), ...)` line and before the multipart-partition step, add the opt-in gate: if `config.source.batch_complete_marker is not None`, compute `marker_key = config.source.path + config.source.batch_complete_marker` and check whether any `obj.key == marker_key` is present in `listed`. If absent, `log.info("discovery.batch_incomplete", dataset=dataset_name, marker_key=marker_key)` and `return []` immediately — no object is hashed or registered this call (LOAD-11's "refused before any parsing occurs," extended to "before any discovery bookkeeping occurs" for the whole batch). If present, remove the marker object from `listed` (`listed = [obj for obj in listed if obj.key != marker_key]`) before continuing to the existing multipart-partition/per-object loop, so the marker is never mistaken for a data file. Document this addition in `discover_files`'s own docstring, cross-referencing D-19's "opt-in, unexercised by both live datasets" framing explicitly, matching the filename-mask precedent's own docstring language.

Extend `tools/corpus/generators.py` (or `tests/fixtures/slice-corpus.yaml`'s generator manifest, whichever this repo's existing corpus tooling uses for new fixture cases — read `tools/corpus/generators.py` first to confirm the exact mechanism) with one new synthetic case: a small batch directory with 2 data files and no marker (case A), and the same directory with the marker object added (case B) — mirroring how existing corpus cases are parameterized. If the corpus generator only produces individual CSV files rather than directory listings, instead build this fixture directly inside the new unit test via a fake/in-memory `ObjectStore`/`ObjectSummary` list (matching how `discover_files`'s existing tests likely construct `listed` — check `tests/unit/` or `tests/integration/` for an existing `discover_files` test's own object-listing construction pattern before choosing).

`tests/unit/validate/test_batch_complete_marker.py`: three tests per the `<behavior>` block above, using a fake/stub `ObjectStore` (`list_objects` returning a fixed `ObjectSummary` list, `get_object` returning canned bytes for hashing) and a fake `MetadataRepository`/`SchemaRepository` stub sufficient to reach the point where `discover_files` would normally register files — assert the empty-return case never calls `metadata.create_file`/`get_or_create_batch` at all (a `unittest.mock.Mock` call-count assertion), proving "no parsing occurs," not merely "the file count differs."
  </action>
  <verify>
    <automated>pytest tests/unit/validate/test_batch_complete_marker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `discover_files` with `batch_complete_marker` set and no matching marker object returns `[]` and never calls `metadata.create_file`
    - `discover_files` with the marker present discovers the SAME set of `DiscoveredUnit`s as an equivalent call with `batch_complete_marker=None` (minus the marker object itself, which is never a candidate)
    - Existing `discover_files` tests (customers-shaped, `batch_complete_marker=None`) remain green, unmodified
  </acceptance_criteria>
  <done>`discover_files` with `batch_complete_marker` set and no matching marker object returns `[]` and never calls `metadata.create_file`</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Object listing -> marker-presence check | The marker check is a simple key-equality test over already-listed, already-trusted-tier object metadata (no new external input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Denial of Service | A batch directory that never receives its marker | accept | This is the intended, documented behavior (discovery withholds indefinitely until the marker arrives) — an operational/monitoring concern (freshness alerting, Phase 7), not a defect this plan must mitigate |
</threat_model>

<verification>
- `pytest tests/unit/validate/test_batch_complete_marker.py -x` green
- Existing discovery.py tests remain green
</verification>

<success_criteria>
LOAD-11 is a real, corpus-tested capability with zero production exposure this phase — a future dataset opts in by setting one config field, with no code change required.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-06-SUMMARY.md` when done
</output>
