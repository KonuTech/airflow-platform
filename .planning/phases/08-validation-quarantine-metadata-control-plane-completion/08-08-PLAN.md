---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 08
type: execute
wave: 3
depends_on: ["08-04", "08-05"]
files_modified:
  - packages/dataplat/src/dataplat/validate/referential.py
  - packages/dataplat/src/dataplat/validate/registry.py
  - configs/datasets/orders.yaml
  - tests/integration/test_referential_integrity.py
autonomous: true
requirements: [VALID-07]
must_haves:
  truths:
    - "An orders row whose customer_id has no matching normalized.customers row is classified REFERENTIAL_ORPHAN, and the rest of the same file's rows still publish normally (D-16 -- row-level quarantine, never a whole-file failure)"
    - "A race between an orders file and a not-yet-loaded customers batch is handled as a normal, expected orphan case (QUARANTINE_RECORD), not a false-alarm whole-run failure (Pitfall 5)"
  artifacts:
    - path: "packages/dataplat/src/dataplat/validate/referential.py"
      provides: "ReferentialIntegrityBarrier -- the second concrete BarrierStage, anti-joining a run's staged customer_id values against normalized.customers"
      contains: "class ReferentialIntegrityBarrier"
  key_links:
    - from: "packages/dataplat/src/dataplat/validate/referential.py"
      to: "normalized.customers"
      via: "an anti-join query over the run's staged customer_id values, parameterized, never string-formatted"
      pattern: "NOT EXISTS|LEFT JOIN"
---

<objective>
Build `ReferentialIntegrityBarrier`, the second concrete `BarrierStage`, proving VALID-07's real behavior: an `orders` row whose `customer_id` is not found in `normalized.customers` is classified `REFERENTIAL_ORPHAN` and quarantined at the ROW level (D-16), while every other row in the same file still publishes — proven with real staged rows and a real `normalized.customers` table, including the deliberate race scenario Pitfall 5 names (an orphan that is a legitimate, not-yet-arrived customer, not a data error).

Purpose: this is the requirement ROADMAP's success criterion #5 names directly ("an orphan foreign key produces the dataset's configured fail/quarantine/warn outcome") and the one CONTEXT.md is most insistent gets proven against real data, not fixtures alone (D-13's whole reason for choosing a real second dataset).
Output: `referential.py`, `orders.yaml`'s `quality:` block gaining its one `REFERENTIAL` rule entry (D-16), and an integration test proving both the orphan-quarantine and the non-orphan-still-publishes halves of the behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReferentialIntegrityBarrier + orders.yaml quality: REFERENTIAL rule</name>
  <files>packages/dataplat/src/dataplat/validate/referential.py, packages/dataplat/src/dataplat/validate/registry.py, configs/datasets/orders.yaml</files>
  <read_first>packages/dataplat/src/dataplat/pipeline/protocol.py, packages/dataplat/src/dataplat/load/publish/merge_orders.py, configs/datasets/orders.yaml</read_first>
  <behavior>
    - `ReferentialIntegrityBarrier(target_table="normalized.customers", target_column="customer_id", staging_column="customer_id", strategy="QUARANTINE_RECORD", rule_id="r5").apply(ctx)`, given a `staging.orders__r<n>` table containing one row whose `customer_id` matches an existing `normalized.customers` row and one row whose `customer_id` does not, returns a `StageResult` whose `rejected` contains exactly one `RejectedRecord` (`error_type="REFERENTIAL_ORPHAN"`) for the non-matching row, and whose `findings` records the barrier's own PASS/QUARANTINE outcome and counts.
    - This stage never blocks or rejects the MATCHING row — only the orphan row is ever named in `rejected`.
  </behavior>
  <action>
`validate/referential.py`: `ReferentialIntegrityBarrier(BarrierStage)`, `name = "referential_integrity_barrier"`. `__init__(self, *, staging_table: str, target_table: str, target_column: str, staging_column: str, strategy: str, rule_id: str) -> None` — `staging_table` (e.g. `"staging.orders__r8123"`) is passed by the caller (plan 08-11's wiring, which already knows `staging_result.staging_table` at the point this barrier runs, per Pattern 2's own "after publisher.publish() ... staged rows already visible" note). `apply(self, ctx: PipelineContext) -> StageResult`: opens `ctx.db.connection()` (a NEW connection, matching how a barrier reads live target-table state — note in the docstring that this is a deliberate READ against a separate connection from the run's own staging connection; the caller decides whether this runs before or after the publish transaction commits, per the wiring plan's own ordering requirements) and issues one parameterized anti-join query: `SELECT s.customer_id, s._source_row_number, s.order_id FROM {staging_table} s LEFT JOIN {target_table} t ON s.{staging_column}::int = t.{target_column} WHERE t.{target_column} IS NULL` — `staging_table`/`target_table`/`target_column`/`staging_column` are all identifiers built from config/run identity (never CSV content, matching `merge.py`'s own T-04-01 precedent — cite this explicitly in the docstring), and the query itself takes NO bound parameters since nothing here is a value. For each returned row, append a `RejectedRecord(source_row_number=row["_source_row_number"], error_type="REFERENTIAL_ORPHAN", error_message=f"customer_id {row['customer_id']} not found in {target_table}", raw_line=str(row["order_id"]), error_column=staging_column)`. Return `StageResult(chunk=<placeholder empty chunk>, rejected=orphans, findings=[ValidationResult(rule_id=self._rule_id, rule_type="REFERENTIAL", severity="ERROR", outcome="QUARANTINE" if orphans else "PASS", evaluated_count=<total staged row count, a second COUNT(*) query or derived from the anti-join's own total>, failed_count=len(orphans), message=..., threshold={}, observed={"orphan_count": len(orphans)})])`.

Add `"REFERENTIAL": ReferentialIntegrityBarrier` to `VALIDATION_RULE_REGISTRY`.

Extend `configs/datasets/orders.yaml` with a `quality:` block (D-16): `quality: {rules: [{rule_id: "orders_customer_id_referential", rule_type: "REFERENTIAL", strategy: "QUARANTINE_RECORD", column: "customer_id"}]}`.
  </action>
  <verify>
    <automated>pytest tests/integration/test_referential_integrity.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `ReferentialIntegrityBarrier.apply()` uses only identifier-interpolated table/column names (config/run-derived), zero row-content values are ever interpolated into the SQL text
    - `DatasetConfig.model_validate` on the updated `orders.yaml` succeeds
  </acceptance_criteria>
  <done>`ReferentialIntegrityBarrier.apply()` uses only identifier-interpolated table/column names (config/run-derived), zero row-content values are ever interpolated into the SQL text</done>

</task>

<task type="auto">
  <name>Task 2: Integration test -- orphan quarantine + non-orphan still publishes + the race scenario</name>
  <files>tests/integration/test_referential_integrity.py</files>
  <read_first>tests/integration/test_publish_orders.py, tests/integration/conftest.py</read_first>
  <action>
`tests/integration/test_referential_integrity.py`, `@pytest.mark.integration`, depends on `migrated_dsn`: hand-build a `staging.orders__r<n>` table directly via SQL (independent of `StagingLoader`, matching `test_publish_orders.py`'s own convention) containing 3 rows: one with a `customer_id` that has a matching row in a hand-inserted `normalized.customers`, one with a `customer_id` that does not, and one more matching row — so the test proves both "the orphan is caught" AND "the two non-orphan rows are left alone" in the same call. Call `ReferentialIntegrityBarrier(...).apply(ctx)` and assert: `len(result.rejected) == 1`, the rejected row's `error_type == "REFERENTIAL_ORPHAN"`, and the barrier's own `findings[0].outcome == "QUARANTINE"` with `failed_count == 1`.

Second test (Pitfall 5's exact race scenario, RESEARCH.md's own named warning sign to guard against): stage an `orders` row referencing a `customer_id` that does NOT yet exist in `normalized.customers` (simulating a later `customers` batch that legitimately hasn't landed), run the SAME `ReferentialIntegrityBarrier`, and assert the barrier does NOT raise, does NOT report `outcome="FAIL"`, and correctly classifies it as `QUARANTINE` with the orphan row named — proving the default `QUARANTINE_RECORD` strategy is genuinely row-level, never file-level/run-level, exactly matching D-16's text. A third assertion in this same test: publish the staged rows via `OrdersMergePublisher` afterward (excluding the orphan row from what gets published, since the barrier already identified it — the wiring plan, 08-11, owns the actual exclude-then-publish sequencing; THIS test only needs to prove the barrier's OWN classification is correct, not the full pipeline sequencing) and confirm the non-orphan row is unaffected by the orphan's presence.
  </action>
  <verify>
    <automated>pytest tests/integration/test_referential_integrity.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - Orphan row correctly classified `REFERENTIAL_ORPHAN`, non-orphan rows never appear in `rejected`
    - The race scenario (orphan = legitimately-not-yet-arrived customer) produces `QUARANTINE`, never `FAIL`
  </acceptance_criteria>
  <done>Orphan row correctly classified `REFERENTIAL_ORPHAN`, non-orphan rows never appear in `rejected`</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging table -> normalized.customers anti-join | The anti-join's WHERE clause compares two already-staged/already-published columns; no CSV row content is ever interpolated as a SQL value, only pre-validated identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering | `ReferentialIntegrityBarrier`'s dynamic identifier interpolation (`staging_table`/`target_table`/columns) | mitigate | Every interpolated fragment is a config/run-derived identifier (dataset name + numeric run_id, or a fixed config-declared column name) — never CSV row content, matching `merge.py`'s T-04-01 precedent verbatim |
| T-08-16 | Information Disclosure | An orphan's `error_message` naming the unmatched `customer_id` value | accept | `customer_id` is a business identifier already present in the source file the operator owns; no additional sensitivity beyond what `meta.rejected_records` already stores for every other rejection type |
</threat_model>

<verification>
- `pytest tests/integration/test_referential_integrity.py -x -m integration` green
</verification>

<success_criteria>
VALID-07's core detection logic is proven correct against real staged and published data, including the race condition CONTEXT.md explicitly calls out as expected, non-error behavior.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-08-SUMMARY.md` when done
</output>
