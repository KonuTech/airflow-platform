---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 10
type: execute
wave: 4
depends_on: ["08-04", "08-07"]
files_modified:
  - packages/dataplat/src/dataplat/load/staging.py
  - tests/integration/test_staging_quality_rules.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]
must_haves:
  truths:
    - "A dataset whose quality: block declares a completeness/validity-range/pattern/uniqueness rule on a column has that rule actually applied during staging -- a bad row is rejected and captured, not silently loaded, without the dataset needing any code change beyond its YAML"
    - "A dataset with no quality: block behaves byte-for-byte as before this plan (RaggedRowGuard runs unconditionally first, no other rule stages are added) -- this is a strict additive extension, never a behavior change for an unconfigured dataset"
  artifacts:
    - path: "packages/dataplat/src/dataplat/load/staging.py"
      provides: "StagingLoader._build_stages extended to dispatch ctx.config.quality's STREAMING rule_type entries through VALIDATION_RULE_REGISTRY, after the existing normalizer stages"
      contains: "VALIDATION_RULE_REGISTRY"
  key_links:
    - from: "packages/dataplat/src/dataplat/load/staging.py"
      to: "packages/dataplat/src/dataplat/validate/registry.py"
      via: "resolve_validation_rule(rule.rule_type) constructs each configured StreamingStage, appended to _build_stages' existing list"
      pattern: "resolve_validation_rule"
---

<objective>
Wire the four row-scoped `StreamingStage` quality rules (completeness, validity-range, pattern, uniqueness — plans 08-04/08-07) into `StagingLoader._build_stages`, driven entirely by a dataset's `quality:` config block, appended after the existing normalizer stages so every value has already been normalized before a quality rule evaluates it.

Purpose: this is the moment VALID-01/02/03 stop being isolated, unit-tested rule classes and become real, config-addressable pipeline behavior — the last piece before plan 08-11 can prove the full FAIL/QUARANTINE run-level outcome against a real dataset.
Output: `StagingLoader._build_stages`'s extension, and an integration test proving a configured rule genuinely rejects a bad row during a real staged load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
</context>

<interfaces>
<!-- packages/dataplat/src/dataplat/load/staging.py's _build_stages (read the
whole method before editing -- it is dense, order-sensitive code): stages
today are [RaggedRowGuard(), <per-column NullTokenNormalizer + type
normalizer, in ctx.config.columns order>, UnicodeNormalizer()]. This plan
appends a FOURTH section, after UnicodeNormalizer(), never before it or
interleaved with it -- quality rules must evaluate fully-normalized values
(NFC-normalized, type-coerced-to-None-or-bool where applicable), matching
the codebase's own "normalization MUST precede hashing" invariant extended
one step further to "normalization MUST precede quality evaluation" for the
identical reason (a not-yet-NFC-normalized string could fail a pattern rule
that would otherwise match). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _build_stages dispatches ctx.config.quality's streaming rules</name>
  <files>packages/dataplat/src/dataplat/load/staging.py</files>
  <read_first>packages/dataplat/src/dataplat/load/staging.py, packages/dataplat/src/dataplat/validate/registry.py, packages/dataplat/src/dataplat/config/model.py</read_first>
  <behavior>
    - A `DatasetConfig` with `quality=None` (e.g. `customers.yaml` before plan 08-11 adds its own block) produces the SAME `_build_stages(ctx)` output as today — `RaggedRowGuard()` + normalizers + `UnicodeNormalizer()`, nothing appended.
    - A `DatasetConfig` with `quality.rules=[QualityRuleConfig(rule_id="r1", rule_type="QUALITY_COMPLETENESS", strategy="REJECT_RECORD", column="name")]` produces the existing stage list PLUS one `CompletenessRule` instance at the end, constructed with `column_index` resolved by looking up `"name"`'s position in `self._target_columns` (the SAME by-name lookup `_build_stages` already uses for normalizers, never assumed positional).
    - A `quality.rules` entry naming `rule_type="REFERENTIAL"` or `rule_type="CIRCUIT_BREAKER"` or `rule_type="VOLUME"` is SKIPPED by this method entirely (these are `BarrierStage`s, wired into the publish transaction by plan 08-11, never into `_build_stages`'s streaming list) — `_build_stages` only ever constructs `StreamingStage` instances; attempting to construct a `BarrierStage` here would fail its `apply()` signature at the very next call. Detect this via `issubclass(resolve_validation_rule(rule.rule_type), StreamingStage)`... note `StreamingStage`/`BarrierStage` are `Protocol`s, not concrete base classes, so `issubclass` against a `Protocol` requires `@runtime_checkable` — check whether `pipeline/protocol.py`'s `StreamingStage`/`BarrierStage` are already `@runtime_checkable`; if not, do NOT add it there (a cross-cutting change outside this plan's file scope) — instead maintain a small local frozenset of streaming-shaped `rule_type` values in `staging.py` itself (`_STREAMING_RULE_TYPES = frozenset({"STRUCTURAL", "QUALITY_COMPLETENESS", "QUALITY_UNIQUENESS", "QUALITY_VALIDITY_RANGE", "QUALITY_PATTERN"})`) and skip any `rule.rule_type` not in that set, with a code comment explaining why (barrier-shaped rule types are a later plan's wiring point, not an oversight).
  </behavior>
  <action>
In `load/staging.py`'s `_build_stages(ctx)`, after the existing `stages.append(UnicodeNormalizer())` line (the current last line before `return stages`), add: if `ctx.config.quality is not None`, iterate `ctx.config.quality.rules`; for each `rule` whose `rule.rule_type in _STREAMING_RULE_TYPES` (skip `STRUCTURAL` specifically — `RaggedRowGuard()` is ALREADY unconditionally first in this list, per D-08's "no behavior change to already-working code"; a `STRUCTURAL`-typed config entry is a documented no-op here, its presence only matters to whichever future code reads `quality.rules` for reporting/introspection, not to stage construction), resolve `stage_class = resolve_validation_rule(rule.rule_type)`, resolve `column_index = self._target_columns.index(rule.column)` when `rule.column is not None` (reuse the exact `try/except ValueError` pattern the existing per-column normalizer loop already uses, raising the same `ValueError` shape on a config/target-schema mismatch), and construct the stage: `CompletenessRule`/`UniquenessRule` take `(column_index=, column_name=rule.column, strategy=rule.strategy, rule_id=rule.rule_id)`; `ValidityRangeRule` additionally unpacks `rule.params` for `minimum`/`maximum` (`stage_class(column_index=..., column_name=..., strategy=rule.strategy, rule_id=rule.rule_id, minimum=rule.params.get("minimum"), maximum=rule.params.get("maximum"))`); `PatternRule` additionally unpacks `rule.params["pattern"]`. Append each constructed stage to `stages` in `ctx.config.quality.rules`' own declared order (deterministic — no reordering).

Import `VALIDATION_RULE_REGISTRY`/`resolve_validation_rule` and the concrete rule classes from `dataplat.validate.*` at the top of `staging.py`, alongside the existing `dataplat.normalize.*` imports.
  </action>
  <verify>
    <automated>pytest tests/integration/test_staging_quality_rules.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `_build_stages` with `quality=None` produces an identical stage-class list to before this plan (a simple `[type(s).__name__ for s in stages]` comparison test)
    - `_build_stages` with a `QUALITY_COMPLETENESS` rule appends exactly one `CompletenessRule` at the end, correctly constructed with the resolved `column_index`
    - A `REFERENTIAL`/`CIRCUIT_BREAKER`/`VOLUME`-typed `quality.rules` entry is silently skipped by `_build_stages` (never raises, never appears in the returned stage list)
  </acceptance_criteria>
  <done>`_build_stages` with `quality=None` produces an identical stage-class list to before this plan (a simple `[type(s).__name__ for s in stages]` comparison test)</done>

</task>

<task type="auto">
  <name>Task 2: Integration test — a configured rule genuinely rejects a bad row during a real staged load</name>
  <files>tests/integration/test_staging_quality_rules.py</files>
  <read_first>tests/integration/ (find the existing StagingLoader integration test, if any, for its fixture/harness pattern — likely alongside test_publish_merge.py or a dedicated staging test file; read it before writing a new one to reuse its conventions), packages/dataplat/src/dataplat/load/staging.py</read_first>
  <action>
`tests/integration/test_staging_quality_rules.py`, `@pytest.mark.integration`, depends on `migrated_dsn`: build a small in-memory `Source`/`RecordChunk`-yielding stub (or reuse whatever fixture-backed `Source` the existing `StagingLoader` integration coverage already uses) yielding 3 rows for a `customers`-shaped dataset config extended in-test with a `quality:` block declaring one `QUALITY_COMPLETENESS` rule on `name` — one of the 3 rows has an empty `name`. Call `StagingLoader(target_columns=(...)).load(ctx, conn)` against a real `migrated_dsn` connection and assert: `StagingResult.rows_rejected == 1` (mirrors the existing `RaggedRowGuard`-only counting convention — quality-rule rejections increment the SAME counter, since `run_streaming`'s chunk-loop already aggregates `len(result.rejected)` uniformly across every stage, streaming or quality), and a `SELECT COUNT(*) FROM <staging_table>` shows exactly 2 rows staged (the bad row never reached `COPY`). A second assertion: the SAME test with `quality=None` on an otherwise-identical config stages all 3 rows (proving the regression guarantee holds for a real load, not just the unit-level stage-list comparison).
  </action>
  <verify>
    <automated>pytest tests/integration/test_staging_quality_rules.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - A row failing a configured completeness rule never lands in the staging table
    - `StagingResult.rows_rejected` correctly counts quality-rule rejections, not just `RaggedRowGuard`'s
  </acceptance_criteria>
  <done>A row failing a configured completeness rule never lands in the staging table</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ctx.config.quality -> _build_stages' rule construction | `quality:` config is developer-authored YAML, not attacker input; the untrusted boundary (a row's own field values) is handled entirely inside each already-hardened `StreamingStage.apply()`, unchanged by this wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering | A `quality.rules[].column` naming a column absent from `ctx.config.columns`/`self._target_columns` | mitigate | Reuses the exact same `try/except ValueError` guard the existing per-column normalizer loop already has — a config/target-schema mismatch fails loudly at staging-construction time, never silently skips the rule |
</threat_model>

<verification>
- `pytest tests/integration/test_staging_quality_rules.py -x -m integration` green
</verification>

<success_criteria>
Every `StreamingStage` quality rule built in Wave 2/3 is now reachable purely through YAML configuration — no dataset needs a code change to opt into a completeness/validity-range/pattern/uniqueness check.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-10-SUMMARY.md` when done
</output>
