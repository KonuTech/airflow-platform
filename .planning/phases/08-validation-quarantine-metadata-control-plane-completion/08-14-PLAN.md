---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 14
type: execute
wave: 7
depends_on: ["08-12"]
files_modified:
  - tests/e2e/slice/test_referential_orphan.py
  - tests/e2e/slice/test_backfill_reentry.py
autonomous: true
requirements: [VALID-07, VALID-08]
must_haves:
  truths:
    - "Against the real deployed cluster, an orders file referencing a not-yet-loaded customer produces a real REFERENTIAL_ORPHAN row in meta.rejected_records while the rest of that file's rows genuinely land in normalized.orders (ROADMAP success criterion #5's 'real' proof, not fixtures-only)"
    - "A real airflow dags backfill invocation against a corrected file flips the previously-PENDING meta.rejected_records rows to RESOLVED/REDRIVEN, linked to the new run_id -- proven against the actual deployed DAGs, not dag.test() (ROADMAP success criterion #3)"
  artifacts:
    - path: "tests/e2e/slice/test_referential_orphan.py"
      provides: "The live, cluster-marked VALID-07 proof"
      contains: "pytest.mark.cluster"
    - path: "tests/e2e/slice/test_backfill_reentry.py"
      provides: "The live, cluster-marked VALID-08 proof"
      contains: "airflow dags backfill"
---

<objective>
Deliver the phase's final closing proofs — the two `cluster`-marked E2E tests RESEARCH.md's own Validation Architecture names as the genuine, no-shortcuts evidence for ROADMAP's success criteria #3 and #5: a real orphan-order race against the real deployed DAGs, and a real `airflow dags backfill` invocation genuinely resolving previously-rejected records.

Purpose: every earlier plan in this phase proved its own piece in isolation (unit tests) or against a testcontainers/`dag.test()` proxy (integration, dagtest tiers). This plan is the one place the WHOLE chain — real MinIO, real Vault-backed credentials, real Airflow scheduler, real KubernetesPodOperator pods, real PostgreSQL — is exercised together, matching this repository's own `tests/e2e/slice/` charter ("proves the PIPELINE's own ROADMAP success criteria against the real deployed DAGs").
Output: `test_referential_orphan.py` and `test_backfill_reentry.py`, both `@pytest.mark.cluster`, extending `tests/e2e/slice/conftest.py`'s existing fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_referential_orphan.py — a real orphan-order race against the deployed cluster</name>
  <files>tests/e2e/slice/test_referential_orphan.py</files>
  <read_first>tests/e2e/slice/conftest.py, tests/e2e/slice/test_smoke_and_idempotency.py</read_first>
  <action>
`@pytest.mark.cluster`, using `tests/e2e/slice/conftest.py`'s existing `_require_cluster`/`s3_client`/`analytics_connection`(or the `etl_app`-role connection fixture) fixtures. Upload a synthetic `orders` CSV to `s3://raw/orders/` referencing a `customer_id` that does NOT yet exist in `normalized.customers` on the live cluster (query first to pick a genuinely absent id, or use a clearly-synthetic out-of-range id), alongside 1-2 rows referencing customer_ids that DO already exist (from prior E2E fixture data). Wait for `csv_ingest_orders` to process the file (poll `meta.ingestion_runs`/`meta.files` for terminal status, matching the polling convention `tests/e2e/slice/`'s existing tests already use — reuse, do not reinvent). Assert: the run reaches `SUCCEEDED` (orphan quarantine alone does not fail the run), a `meta.rejected_records` row exists for the orphan `order_id` with `error_type='REFERENTIAL_ORPHAN'`, and the non-orphan rows genuinely appear in `normalized.orders` via a direct query.
  </action>
  <verify>
    <automated>pytest tests/e2e/slice/test_referential_orphan.py -x -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - Test skips cleanly (via `_require_cluster`) when no live cluster is reachable
    - Against a live cluster, the orphan row is genuinely quarantined and the non-orphan rows genuinely publish, both observed via direct SQL against the real analytical database
  </acceptance_criteria>
  <done>Test skips cleanly (via `_require_cluster`) when no live cluster is reachable</done>

</task>

<task type="auto">
  <name>Task 2: test_backfill_reentry.py — a real airflow dags backfill resolving a real rejection</name>
  <files>tests/e2e/slice/test_backfill_reentry.py</files>
  <read_first>tests/e2e/slice/conftest.py, tests/e2e/slice/test_smoke_and_idempotency.py</read_first>
  <action>
`@pytest.mark.cluster`. Upload a synthetic `customers` (or `orders`) CSV containing at least one row that will be quarantined by a configured `quality:` rule (e.g. a row with an empty `name`, tripping the `QUALITY_COMPLETENESS` rule `customers.yaml` now declares). Wait for the run to `SUCCEEDED` with the bad row quarantined (`meta.rejected_records` row, `resolution_type='PENDING'`), matching the same polling convention as Task 1. Correct the file (fix the bad row) and re-upload it under a path/batch this run's original `logical_date`/batch can be re-targeted by. Invoke the real Airflow backfill mechanism against the corrected file — via `kubectl exec` into an Airflow component to run `airflow dags backfill csv_ingest_customers -s <logical_date> -e <logical_date> --reset-dagruns -y` (or the CLI-appropriate equivalent for this Airflow 3.3.0 deployment; confirm the exact invocation shape against the live cluster's own `airflow dags backfill --help` output before locking the command, since D-01 requires this to be THE re-entry mechanism, not a close approximation), matching how `tests/e2e/slice/`'s existing tests already shell out to cluster commands (reuse that helper, do not invent a new subprocess pattern). Poll for the new run's completion, then assert via direct SQL that the previously-`PENDING` `meta.rejected_records` row now shows `resolution_type='REDRIVEN'` (or `RESOLVED`, matching whatever exact value plan 08-01/08-11's wiring settled on — confirm against the actual column values written, not assumed) and `resolved_by_run_id` set to the NEW run's `run_id` — and that the corrected row now appears in the target table.
  </action>
  <verify>
    <automated>pytest tests/e2e/slice/test_backfill_reentry.py -x -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - Test skips cleanly when no live cluster is reachable
    - Against a live cluster, a genuine `airflow dags backfill` invocation flips the rejected row's resolution state and links it to the new run, and the corrected data lands in the target table
  </acceptance_criteria>
  <done>Test skips cleanly when no live cluster is reachable</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> live kind cluster (kubectl/Airflow CLI) | Same trust boundary every existing `tests/e2e/slice/*.py`/`tests/e2e/cluster/*.py` test already crosses — no new surface, reuses `_require_cluster`'s existing gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-23 | Denial of Service | An `airflow dags backfill` invocation against the wrong `logical_date`/DAG accidentally re-processing unrelated historical data | accept | This platform's idempotency guarantees (D-1, Phase 4) make any accidental re-run a no-op against already-published rows — a documented, tested property this exact test relies on, not a new risk it introduces |
</threat_model>

<verification>
- `pytest tests/e2e/slice/test_referential_orphan.py tests/e2e/slice/test_backfill_reentry.py -x -m cluster` green against a live cluster; skips cleanly without one
</verification>

<success_criteria>
ROADMAP's Phase 8 success criteria #3 and #5 are proven true against the real, deployed system — the phase's closing evidence.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-14-SUMMARY.md` when done
</output>
