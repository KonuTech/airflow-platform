---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0014_meta_validation_results.py
  - migrations/versions/0015_meta_rejected_records.py
  - migrations/versions/0016_normalized_orders.py
  - packages/dataplat/src/dataplat/errors.py
  - packages/dataplat/src/dataplat/models/report.py
  - packages/dataplat/src/dataplat/metadata/repository.py
  - packages/dataplat/src/dataplat/config/model.py
  - tests/integration/test_migrations.py
  - tests/unit/test_quality_config.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-07, VALID-09, LOAD-11]
must_haves:
  truths:
    - "meta.validation_results and meta.rejected_records exist after alembic upgrade head, granted SELECT/INSERT/UPDATE to etl_app and no DELETE (D-04's no-per-row-edit constraint enforced at the DB-privilege level)"
    - "normalized.orders exists with the same six embedded lineage columns as normalized.customers, so a future orders row answers 'which file/batch/run' by SQL alone exactly like customers does"
    - "A dataset config declaring a quality: block with completeness/uniqueness/validity_range/pattern/referential rules, each with its own strategy, validates successfully against DatasetConfig"
  artifacts:
    - path: "migrations/versions/0014_meta_validation_results.py"
      provides: "meta.validation_results DDL (run_id, rule_id, rule_type, severity, outcome, evaluated_count, failed_count, threshold jsonb, observed jsonb)"
      contains: "create_table(\n        \"validation_results\""
    - path: "migrations/versions/0015_meta_rejected_records.py"
      provides: "meta.rejected_records DDL, including resolution_type/resolved_by_run_id (D-04/D-05) and batch_id (needed by resolve_rejected_records_for_batch's WHERE clause)"
      contains: "resolution_type"
    - path: "migrations/versions/0016_normalized_orders.py"
      provides: "normalized.orders DDL, D-17's 4 business columns + the 6 embedded lineage columns verbatim from migration 0005"
      contains: "_record_hash_version"
    - path: "packages/dataplat/src/dataplat/config/model.py"
      provides: "QualityRuleConfig/QualityConfig (D-07/D-09/D-10) and SourceConfig.batch_complete_marker (LOAD-11/D-19)"
      contains: "class QualityConfig"
  key_links:
    - from: "packages/dataplat/src/dataplat/config/model.py"
      to: "packages/dataplat/src/dataplat/metadata/repository.py"
      via: "ValidationResult/RejectedRecord shapes both the config's rule_type vocabulary and the repository's persistence methods agree on"
      pattern: "rule_type"
---

<objective>
Land the phase's foundational, coordinate-once DDL and typed contracts: the two new `meta` tables (`validation_results`, `rejected_records`), the `normalized.orders` table, the widened `ValidationResult` value object, the `MetadataRepository` Protocol's three new methods (stubs only — implementation is plan 08-03), the new exception classes this phase first raises, and the `quality:`/`batch_complete_marker` config surface every later plan in this phase reads from.

Purpose: ROADMAP's Wave E guidance is explicit — the validation stream and the metadata-completion stream "coordinate only on the `meta.validation_results` DDL landing first." This plan is that landing point, plus every other shared contract (config schema, exception types, repository Protocol) so Wave 2's two parallel streams never redefine the same shape twice.
Output: Three new Alembic migrations, `errors.py`'s two new exception classes, `models/report.py`'s widened `ValidationResult`, `metadata/repository.py`'s three new Protocol method signatures, and `config/model.py`'s `quality:`/`batch_complete_marker` fields — every one of them a pure, additive contract with no behavior wired in yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
</context>

<interfaces>
<!-- The exact target signatures this plan's Task 2 must produce -- refined
from RESEARCH.md's own draft (08-RESEARCH.md lines 448-463) to accept `conn`
explicitly, because Pattern 3/D-11 requires every one of these writes to
execute inside the SAME transaction as Publisher.publish() -- an
open-your-own-connection method (like every OTHER MetadataRepository method)
would break the FAIL-means-rollback guarantee. This refinement supersedes
the simplified draft in 08-RESEARCH.md's Code Examples section. -->

```
def record_validation_results(
    self, *, conn: Connection[Any], run_id: int, results: list[ValidationResult],
) -> None: ...

def record_rejected_records(
    self, *, conn: Connection[Any], run_id: int, file_id: int, batch_id: int,
    rejected: list[RejectedRecord],
) -> None: ...

def resolve_rejected_records_for_batch(
    self, *, conn: Connection[Any], batch_id: int, resolved_by_run_id: int,
    resolution_type: str,
) -> int:
    """Whole-batch side effect only (D-04) -- no per-row variant exists.
    WHERE batch_id = %s AND resolution_type = 'PENDING' -- the ONLY write
    path to resolution_type/resolved_by_run_id anywhere in this codebase."""
```

Widened `ValidationResult` (`packages/dataplat/src/dataplat/models/report.py`,
currently `rule_id`/`outcome`/`message` only -- no live caller constructs one
yet, so widening is safe): add `rule_type: str` (one of `FILE`/`STRUCTURAL`/
`SCHEMA`/`TYPE`/`QUALITY`/`REFERENTIAL`/`VOLUME`), `severity: str`,
`evaluated_count: int`, `failed_count: int`, `threshold: dict[str, object]`
(default `{}`), `observed: dict[str, object]` (default `{}`). Keep
`outcome`/`message` as-is (still plain `str`, per this repo's
text-not-enum convention). `rule_id` stays required, no default.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Three new migrations — validation_results, rejected_records, normalized.orders</name>
  <files>migrations/versions/0014_meta_validation_results.py, migrations/versions/0015_meta_rejected_records.py, migrations/versions/0016_normalized_orders.py, tests/integration/test_migrations.py</files>
  <read_first>migrations/versions/0009_meta_schema_versions.py, migrations/versions/0002_meta_files.py, migrations/versions/0005_normalized_customers.py, migrations/versions/0004_meta_ingestion_runs.py, tests/integration/test_migrations.py</read_first>
  <action>
Create three sequential Alembic revisions, `down_revision` chained off `0013` -> `0014` -> `0015` -> `0016`, `Create Date: 2026-08-17`.

`0014_meta_validation_results.py`: `op.create_table("validation_results", ...)` in schema `meta`, columns: `validation_result_id` (BigInteger, `sa.Identity(always=True)`, primary key), `run_id` (BigInteger, `sa.ForeignKey("meta.ingestion_runs.run_id")`, not null), `rule_id` (Text, not null), `rule_type` (Text, not null — app-validated against `{FILE,STRUCTURAL,SCHEMA,TYPE,QUALITY,REFERENTIAL,VOLUME}`, never a native Postgres ENUM), `severity` (Text, not null), `outcome` (Text, not null — app-validated `{PASS,PASS_WITH_WARNING,FAIL,QUARANTINE}`), `evaluated_count` (BigInteger, not null), `failed_count` (BigInteger, not null), `threshold` (`JSONB`, nullable), `observed` (`JSONB`, nullable), `created_at` (DateTime(timezone=True), not null, `server_default=sa.text("now()")`). Add `op.create_index("ix_validation_results_run_id", "validation_results", ["run_id"], schema="meta")`. `op.execute("GRANT SELECT, INSERT, UPDATE ON meta.validation_results TO etl_app")` — no `DELETE` (D-04's DB-privilege-level enforcement).

`0015_meta_rejected_records.py`: `op.create_table("rejected_records", ...)` in schema `meta`, columns: `rejected_record_id` (BigInteger, Identity, PK), `run_id` (BigInteger, FK `meta.ingestion_runs.run_id`, not null), `file_id` (BigInteger, FK `meta.files.file_id`, not null), `batch_id` (BigInteger, FK `meta.batches.batch_id`, not null — needed by `resolve_rejected_records_for_batch`'s `WHERE batch_id = %s` clause; not in ARCHITECTURE.md's original sketch but required by that method's own signature this plan's Task 2 defines), `source_row_number` (BigInteger, not null), `source_byte_offset` (BigInteger, nullable), `raw_line` (Text, not null), `error_type` (Text, not null), `error_column` (Text, nullable), `error_message` (Text, not null), `rejected_at` (DateTime(timezone=True), not null, `server_default=sa.text("now()")`), `resolution_type` (Text, not null, `server_default=sa.text("'PENDING'")` — D-04's 2-state lifecycle disambiguated by this column: `PENDING`/`REDRIVEN`/`DISCARDED`), `resolved_by_run_id` (BigInteger, FK `meta.ingestion_runs.run_id`, nullable — a direct nullable FK, not deferred, since `meta.ingestion_runs` already exists). Add `op.create_index("ix_rejected_records_batch_resolution", "rejected_records", ["batch_id", "resolution_type"], schema="meta")`. `op.execute("GRANT SELECT, INSERT, UPDATE ON meta.rejected_records TO etl_app")` — no `DELETE`.

`0016_normalized_orders.py`: `op.create_table("orders", ...)` in schema `normalized` (schema already exists from migration 0005 — do not `CREATE SCHEMA` again). Business columns per D-17: `order_id` (Integer, not null), `customer_id` (Integer, not null — a business reference, deliberately NOT a Postgres FK to `normalized.customers`: an orphan order under D-16's default `QUARANTINE_RECORD` strategy must be able to publish rows referencing a not-yet-loaded customer; referential integrity is enforced application-side by `ReferentialIntegrityBarrier`, a later plan, never a DB constraint here), `order_date` (Date, nullable), `amount` (Numeric, nullable). Then the six lineage columns verbatim from migration 0005 lines 52-87: `_run_id`, `_file_id`, `_batch_id`, `_source_row_number`, `_record_hash` (LargeBinary), `_record_hash_version` (SmallInteger, `server_default=sa.text("1")`), `_ingested_at` (DateTime(timezone=True), `server_default=sa.text("now()")`). Add `op.create_index("ix_orders_customer_id", "orders", ["customer_id"], schema="normalized")` (anti-join performance for the referential barrier, not uniqueness). `op.execute("GRANT SELECT, INSERT, UPDATE ON normalized.orders TO etl_app")`.

Extend `tests/integration/test_migrations.py`'s `EXPECTED_TABLES`/`GRANTED_TABLES` sets to add `("meta", "validation_results")`, `("meta", "rejected_records")`, `("normalized", "orders")`, and `HASH_VERSION_COLUMNS` to add `("normalized", "orders", "_record_hash_version")`.
  </action>
  <verify>
    <automated>pytest tests/integration/test_migrations.py -x -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` (via `run_migrations`/`migrated_dsn` fixtures) applies revisions 0014/0015/0016 cleanly against a throwaway PostgreSQL 18, and `alembic downgrade -3` then `upgrade head` again succeeds (idempotent upgrade, matching `test_upgrade_head_is_idempotent`'s existing pattern)
    - `information_schema.table_privileges` for `etl_app` on `meta.validation_results`/`meta.rejected_records`/`normalized.orders` contains exactly `SELECT`, `INSERT`, `UPDATE` — no `DELETE`
    - `meta.rejected_records.resolution_type` has `column_default` containing `'PENDING'`
  </acceptance_criteria>
  <done>`alembic upgrade head` (via `run_migrations`/`migrated_dsn` fixtures) applies revisions 0014/0015/0016 cleanly against a throwaway PostgreSQL 18, and `alembic downgrade -3` then `upgrade head` again succeeds (idempotent upgrade, matching `test_upgrade_head_is_idempotent`'s existing pattern)</done>

</task>

<task type="auto">
  <name>Task 2: Widen contracts — errors.py, report.py, repository.py Protocol, config/model.py quality: block</name>
  <files>packages/dataplat/src/dataplat/errors.py, packages/dataplat/src/dataplat/models/report.py, packages/dataplat/src/dataplat/metadata/repository.py, packages/dataplat/src/dataplat/config/model.py, tests/unit/test_quality_config.py</files>
  <read_first>packages/dataplat/src/dataplat/errors.py, packages/dataplat/src/dataplat/models/report.py, packages/dataplat/src/dataplat/metadata/repository.py, packages/dataplat/src/dataplat/config/model.py, tests/unit/test_dataset_config_columns.py</read_first>
  <action>
`errors.py`: append `QualityThresholdExceeded(DataPlatformError)` and `PublicationError(DataPlatformError)` after `IncompatibleSchemaError` (end of file), with the exact docstrings the module docstring already promises (see `08-RESEARCH.md` lines 470-486) — `QualityThresholdExceeded` documents that `RejectionRateCircuitBreaker` (D-10) raises it and it causes the publish transaction to roll back (D-11); `PublicationError` documents it covers a non-quality publication failure (constraint violation, connection loss), distinct from a deliberate business-rule rollback.

`models/report.py`: widen `ValidationResult` (currently `rule_id`/`outcome`/`message`) to add `rule_type: str`, `severity: str`, `evaluated_count: int`, `failed_count: int`, `threshold: dict[str, object] = field(default_factory=dict)`, `observed: dict[str, object] = field(default_factory=dict)` — keep `@dataclass(frozen=True, slots=True)`, keep `rule_id`/`outcome`/`message` as required plain `str` fields (no enum). Update the module docstring: this is no longer "the minimal D-05 shape" — it is now the real, phase-8 shape backing `meta.validation_results`.

`metadata/repository.py`: add the three `Protocol` method stubs from this plan's `<interfaces>` block above (`record_validation_results`, `record_rejected_records`, `resolve_rejected_records_for_batch`), each with a full docstring in the existing convention (`Maps to` SQL-shape sentence, `Args`/`Returns`/`Raises`, an explicit callout that `conn` is caller-supplied and never committed/rolled back here — mirroring `finalize_publication`'s own callout). Import `ValidationResult`/`RejectedRecord` under `TYPE_CHECKING` from `dataplat.models.report`/`dataplat.models.record`.

`config/model.py`: add `QualityRuleConfig(BaseModel)` (`extra="forbid", frozen=True`): `rule_id: str`, `rule_type: str` (plain str, resolved through `VALIDATION_RULE_REGISTRY` — matches `SourceConfig.type`'s registry convention, NOT `ColumnContract.type`'s closed-Literal convention, since rule types are a real extension point per RESEARCH.md Pattern 1), `strategy: str` (one of `FAIL_FILE`/`REJECT_RECORD`/`QUARANTINE_FILE`/`QUARANTINE_RECORD`/`WARN_AND_CONTINUE`, D-07 — plain str, same reasoning), `column: str | None = None` (the column this rule applies to; `None` for dataset-wide rules like `REFERENTIAL`), `params: dict[str, object] = {}` (rule-specific parameters, e.g. `{"min": 0, "max": 100}` for a validity-range rule, `{"pattern": "^[A-Z]"}` for a pattern rule — interpreted by each rule's own `__init__`, never by this model). Add `QualityConfig(BaseModel)` (`extra="forbid", frozen=True`): `rules: list[QualityRuleConfig] = []`, `rejection_rate_threshold: float | None = None` (D-10's run-level circuit-breaker threshold, e.g. `0.10` for 10%). Add `quality: QualityConfig | None = None` to `DatasetConfig` (opt-in, `FreshnessConfig`'s own precedent — but D-09 means `customers.yaml` populates it for real, a later plan's job, not this one's). Add `batch_complete_marker: str | None = None` to `SourceConfig` (LOAD-11/D-19: the object key suffix, e.g. `"_BATCH_COMPLETE"`, a batch directory must contain before `discover_files` includes it — `None` means the check is skipped entirely, matching D-19's "opt-in, unexercised by both live datasets" precedent).

New `tests/unit/test_quality_config.py`, mirroring `tests/unit/test_dataset_config_columns.py`'s shape: a `_VALID_DOCUMENT` built from `customers.yaml`'s real shape plus a `quality:` block with one rule of each of the 5 `rule_type` values used later in this phase (`STRUCTURAL`, `QUALITY_COMPLETENESS`, `QUALITY_VALIDITY_RANGE`, `QUALITY_PATTERN`, `REFERENTIAL`), asserting successful validation; one test asserting `extra="forbid"` rejects an unknown key inside a `QualityRuleConfig`; one test asserting `SourceConfig.batch_complete_marker` defaults to `None` and round-trips a set value.
  </action>
  <verify>
    <automated>pytest tests/unit/test_quality_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `from dataplat.errors import QualityThresholdExceeded, PublicationError` succeeds and both subclass `DataPlatformError`
    - `dataclasses.fields(ValidationResult)` includes `rule_type`, `severity`, `evaluated_count`, `failed_count`, `threshold`, `observed` in addition to the existing three fields
    - `MetadataRepository` (a `Protocol`) declares `record_validation_results`, `record_rejected_records`, `resolve_rejected_records_for_batch` (visible via `typing.get_type_hints`/`dir()` inspection or a structural `isinstance`-style check in a test)
    - `DatasetConfig.model_validate(_VALID_DOCUMENT)` with a populated `quality:` block succeeds; a document with an unknown key inside a `quality.rules[]` entry raises `pydantic.ValidationError`
  </acceptance_criteria>
  <done>`from dataplat.errors import QualityThresholdExceeded, PublicationError` succeeds and both subclass `DataPlatformError`</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config -> DatasetConfig | `configs/datasets/*.yaml` is developer-authored, not attacker-controlled, but `extra="forbid"` still guards against config-typo drift |
| meta.rejected_records DDL -> future untrusted content | This plan only creates the table; the actual untrusted-content write path (raw_line/error_message) is enforced in plan 08-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | meta.rejected_records/validation_results DB grants | mitigate | GRANT SELECT, INSERT, UPDATE only, no DELETE — enforces D-04's no-per-row-edit invariant at the database-privilege level, not merely application logic |
| T-08-02 | Tampering | normalized.orders.customer_id | accept | Deliberately not a DB-level FK to normalized.customers (D-16 requires orphan orders to publish); referential integrity is an application-level barrier stage (plan 08-08), documented risk, not a gap |
| T-08-03 | Information Disclosure | QualityRuleConfig.params (jsonb-shaped dict) | accept | Developer-authored config, not attacker input; no secret material is ever a valid value here per this repo's existing SecretsResolver convention |
</threat_model>

<verification>
- `pytest tests/integration/test_migrations.py -x -m integration` green
- `pytest tests/unit/test_quality_config.py -x` green
- `alembic upgrade head` against a throwaway PostgreSQL 18 exits 0
</verification>

<success_criteria>
Every downstream plan in this phase can `from dataplat.errors import QualityThresholdExceeded, PublicationError`, `from dataplat.models.report import ValidationResult` (widened shape), `from dataplat.metadata.repository import MetadataRepository` (with the 3 new stubs), and validate a `quality:`-bearing YAML document against `DatasetConfig` — all without touching this plan's files again.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-01-SUMMARY.md` when done
</output>
