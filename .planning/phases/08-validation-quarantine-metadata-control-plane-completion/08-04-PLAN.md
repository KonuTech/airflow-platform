---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/dataplat/src/dataplat/validate/__init__.py
  - packages/dataplat/src/dataplat/validate/registry.py
  - packages/dataplat/src/dataplat/validate/completeness.py
  - packages/dataplat/src/dataplat/validate/validity_range.py
  - packages/dataplat/src/dataplat/validate/pattern.py
  - tests/unit/validate/__init__.py
  - tests/unit/validate/test_structural_rules.py
  - tests/unit/validate/test_quality_rules.py
  - tests/property/test_quality_rules_never_raise.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03]
must_haves:
  truths:
    - "A completeness rule against a required, non-nullable column rejects a row with an empty value, per its configured strategy, and never raises for the row itself (QUAL-03)"
    - "A validity-range rule rejects a value outside its configured min/max, and a pattern rule rejects a value not matching its configured regex, both producing a RejectedRecord naming row/column/error_type"
    - "The STRUCTURAL rule_type resolves through the registry to the existing RaggedRowGuard — no new detection logic, only a config-addressable name (D-08)"
  artifacts:
    - path: "packages/dataplat/src/dataplat/validate/registry.py"
      provides: "VALIDATION_RULE_REGISTRY (rule_type -> Stage class) + resolve_validation_rule(), the config-not-code dispatch point every later rule (circuit_breaker, referential, volume_anomaly, uniqueness) also registers into"
      contains: "VALIDATION_RULE_REGISTRY"
    - path: "packages/dataplat/src/dataplat/validate/completeness.py"
      provides: "CompletenessRule, a StreamingStage rejecting rows with an empty value on a required column"
      contains: "class CompletenessRule"
  key_links:
    - from: "packages/dataplat/src/dataplat/validate/registry.py"
      to: "packages/dataplat/src/dataplat/pipeline/protocol.py"
      via: "every registered class implements StreamingStage or BarrierStage"
      pattern: "class.*\\(StreamingStage\\)|class.*\\(BarrierStage\\)"
---

<objective>
Build the `validate/` package's registry and its first three row-scoped `StreamingStage` rule implementations (completeness, validity-range, pattern) — the "validation" stream (S8) ROADMAP's Wave E names, running in parallel with plan 08-03's "metadata completion" stream.

Purpose: VALID-02's DoD-mandated minimum rule set (completeness, uniqueness, validity ranges, patterns, referential integrity) needs a real, config-addressable home. This plan delivers three of the five rule families as pure, testable `StreamingStage`s with zero wiring into the live pipeline yet (wiring is plan 08-10) — proving each rule's row-level classification logic in complete isolation first.
Output: `validate/registry.py` (the dispatch table every later rule plan also registers into), `completeness.py`, `validity_range.py`, `pattern.py`, and their unit/property tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-CONTEXT.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
</context>

<interfaces>
<!-- packages/dataplat/src/dataplat/pipeline/protocol.py's StreamingStage Protocol
(already exists, unchanged by this plan):
class StreamingStage(Protocol):
    name: str
    def apply(self, ctx: PipelineContext, chunk: RecordChunk) -> StageResult: ...
Every class in this plan implements this Protocol exactly, following
pipeline/engine.py's RaggedRowGuard as the concrete shape to copy: never
raise for a row-level problem (QUAL-03); a violation becomes a RejectedRecord
in StageResult.rejected (REJECT_RECORD/QUARANTINE_RECORD strategies) or a
ValidationResult in StageResult.findings (WARN_AND_CONTINUE); chunk.replace(
rows=tuple(kept)) is the only functional-update mechanism. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VALIDATION_RULE_REGISTRY + CompletenessRule + ValidityRangeRule</name>
  <files>packages/dataplat/src/dataplat/validate/__init__.py, packages/dataplat/src/dataplat/validate/registry.py, packages/dataplat/src/dataplat/validate/completeness.py, packages/dataplat/src/dataplat/validate/validity_range.py, tests/unit/validate/__init__.py, tests/unit/validate/test_structural_rules.py, tests/unit/validate/test_quality_rules.py</files>
  <read_first>packages/dataplat/src/dataplat/pipeline/engine.py, packages/dataplat/src/dataplat/pipeline/protocol.py, packages/dataplat/src/dataplat/load/publish/registry.py, packages/dataplat/src/dataplat/models/record.py, packages/dataplat/src/dataplat/models/report.py, packages/dataplat/src/dataplat/normalize/boolean_null.py, tests/unit/normalize/test_boolean_null.py</read_first>
  <behavior>
    - `resolve_validation_rule("STRUCTURAL")` returns the `RaggedRowGuard` class (not an instance — rules take per-rule config at construction); `resolve_validation_rule("nonsense")` raises `ConfigurationError` naming the unknown key and the known-keys list (mirrors `resolve_publisher`'s exact shape).
    - `CompletenessRule(column_index=0, column_name="name", strategy="REJECT_RECORD", rule_id="r1")`: a row whose field at `column_index` is `None` or `""` is rejected with `error_type="COMPLETENESS_VIOLATION"`, `error_column="name"`; a row with a non-empty value at that index survives unchanged; the stage never raises for either case.
    - `ValidityRangeRule(column_index=0, column_name="amount", strategy="REJECT_RECORD", rule_id="r2", minimum=0, maximum=1000)`: a numeric-string value outside `[minimum, maximum]` is rejected with `error_type="VALIDITY_RANGE_VIOLATION"`; a value inside range, or a non-numeric value the rule cannot parse (treated as a separate, distinct `error_type="VALIDITY_RANGE_UNPARSEABLE"` rejection, never a raised exception), are both handled without raising.
  </behavior>
  <action>
`validate/__init__.py`: empty package init, mirrors `normalize/__init__.py`.

`validate/registry.py`: `VALIDATION_RULE_REGISTRY: dict[str, type[StreamingStage] | type[BarrierStage]]` mapping `"STRUCTURAL"` to `dataplat.pipeline.engine.RaggedRowGuard` (import it), `"QUALITY_COMPLETENESS"` to `CompletenessRule`, `"QUALITY_VALIDITY_RANGE"` to `ValidityRangeRule`. Later plans (08-07, 08-08, 08-09) add `"QUALITY_UNIQUENESS"`, `"QUALITY_PATTERN"` (this plan's own Task 2 adds this one), `"REFERENTIAL"`, `"VOLUME"` — this plan's registry dict is the single, append-only home every one of those PRs a new key into, never a parallel registry. `resolve_validation_rule(rule_type: str) -> type[StreamingStage] | type[BarrierStage]`: `PUBLISHER_REGISTRY`/`resolve_publisher`'s exact shape — `try: return VALIDATION_RULE_REGISTRY[rule_type] except KeyError: raise ConfigurationError(msg, context={"rule_type": rule_type, "known": sorted(VALIDATION_RULE_REGISTRY)}) from None`.

`validate/completeness.py`: `CompletenessRule(StreamingStage)`, `name = "completeness_rule"`. `__init__(self, *, column_index: int, column_name: str, strategy: str, rule_id: str) -> None` stores all four. `apply(self, ctx, chunk) -> StageResult`: iterate `chunk.rows`, for each row check `row[self._column_index] in (None, "")`; if empty, append a `RejectedRecord(source_row_number=chunk.first_ordinal + i, error_type="COMPLETENESS_VIOLATION", error_message=f"required column {self._column_name!r} is empty", raw_line="|".join(...), error_column=self._column_name)` to `rejected`; otherwise keep the row. Emit `metrics.increment("rows_rejected"/"rows_kept", ..., dataset=ctx.config.dataset, stage=self.name, status=...)` matching `RaggedRowGuard`'s exact label-set convention. Return `StageResult(chunk=chunk.replace(rows=tuple(kept)), rejected=rejected, findings=[])`. `self._strategy`/`self._rule_id` are stored but not yet dispatched-on in this plan (that logic — deciding REJECT_RECORD vs WARN_AND_CONTINUE vs QUARANTINE_FILE behavior — is plan 08-10's wiring job; this plan's rules always populate `rejected`, proving the detection logic, and note in the class docstring that strategy-based branching lands in plan 08-10).

`validate/validity_range.py`: `ValidityRangeRule(StreamingStage)`, `name = "validity_range_rule"`. `__init__(self, *, column_index: int, column_name: str, strategy: str, rule_id: str, minimum: float | None = None, maximum: float | None = None) -> None`. `apply()`: for each row, attempt `float(row[column_index])` inside a narrow `try/except ValueError`; on parse failure, reject with `error_type="VALIDITY_RANGE_UNPARSEABLE"`; on parse success, if `minimum is not None and value < minimum` or `maximum is not None and value > maximum`, reject with `error_type="VALIDITY_RANGE_VIOLATION"` and `error_message` naming the configured bound and observed value; otherwise keep the row unchanged (never coerce the field's own string representation — this rule only classifies, `StagingLoader`'s existing normalizers own actual type coercion).

New `tests/unit/validate/__init__.py` (empty, matching `tests/unit/normalize/__init__.py`'s own ruff-INP001-avoidance docstring convention).

`tests/unit/validate/test_structural_rules.py`: reuse `tests/unit/normalize/test_boolean_null.py`'s `_make_context()`/`_chunk()` helper convention; one test asserting `resolve_validation_rule("STRUCTURAL") is RaggedRowGuard`; one test constructing a chunk with a ragged row and asserting the resulting `RejectedRecord.error_type == "RAGGED_ROW"`, `error_column is None`, `source_row_number` correct (VALID-01's row/error_type/diagnostic proof, reusing existing behavior under the new registry-addressable name).

`tests/unit/validate/test_quality_rules.py`: `CompletenessRule`/`ValidityRangeRule` unit tests per the `<behavior>` block above — one test per case (empty value rejected, non-empty kept, unparseable rejected distinctly from out-of-range, both bound edges).
  </action>
  <verify>
    <automated>pytest tests/unit/validate/test_structural_rules.py tests/unit/validate/test_quality_rules.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `resolve_validation_rule("STRUCTURAL")` returns `RaggedRowGuard` (the class, not an instance)
    - `resolve_validation_rule("unknown")` raises `ConfigurationError`
    - `CompletenessRule.apply()`/`ValidityRangeRule.apply()` never raise for any row-shaped input in the test suite — every case resolves to a kept row or a `RejectedRecord`
  </acceptance_criteria>
  <done>`resolve_validation_rule("STRUCTURAL")` returns `RaggedRowGuard` (the class, not an instance)</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: PatternRule + registry entry + property test (never raises)</name>
  <files>packages/dataplat/src/dataplat/validate/pattern.py, packages/dataplat/src/dataplat/validate/registry.py, tests/unit/validate/test_quality_rules.py, tests/property/test_quality_rules_never_raise.py</files>
  <read_first>packages/dataplat/src/dataplat/validate/completeness.py, packages/dataplat/src/dataplat/validate/registry.py, tests/property/ (any existing property test for its hypothesis-strategy convention)</read_first>
  <behavior>
    - `PatternRule(column_index=0, column_name="code", strategy="REJECT_RECORD", rule_id="r3", pattern=r"^[A-Z]{2}$")`: a value matching the compiled regex (`re.fullmatch`) survives; a non-matching value is rejected with `error_type="PATTERN_VIOLATION"`.
    - Property: for ANY chunk of rows (hypothesis-generated arbitrary strings, including `None`, empty string, unicode, very long strings) through `CompletenessRule`/`ValidityRangeRule`/`PatternRule`, `apply()` never raises and always returns a `StageResult` whose `len(chunk.rows) == len(kept) + len(rejected)` (every row is accounted for exactly once).
  </behavior>
  <action>
`validate/pattern.py`: `PatternRule(StreamingStage)`, `name = "pattern_rule"`. `__init__(self, *, column_index: int, column_name: str, strategy: str, rule_id: str, pattern: str) -> None` compiles `self._compiled = re.compile(pattern)` once at construction (never per-row). `apply()`: for each row, `self._compiled.fullmatch(str(row[column_index]) if row[column_index] is not None else "")`; no match -> reject with `error_type="PATTERN_VIOLATION"`, `error_message` naming the pattern and the observed value; match -> keep.

Add `"QUALITY_PATTERN": PatternRule` to `VALIDATION_RULE_REGISTRY` in `validate/registry.py`.

`tests/property/test_quality_rules_never_raise.py`: using `hypothesis`, define a strategy generating rows as `tuple[str | None, ...]` of varying length (including empty strings, `None`, unicode, and strings containing the configured pattern's own special regex characters, to prove the compiled pattern never chokes on adversarial input), feed each into `CompletenessRule`/`ValidityRangeRule`/`PatternRule` instances constructed with fixed, valid configs, and assert (a) no exception propagates, (b) `len(result.chunk.rows) + len(result.rejected) == len(original_rows)` for every generated chunk. Mark the module `pytest.mark.slow`? No — property tests in this repo run in the "quick" gate per 08-VALIDATION.md's `pytest tests/unit tests/property -q` command; do not add a slow marker unless the existing property-test convention (check any `tests/property/*.py` file) uses one.
  </action>
  <verify>
    <automated>pytest tests/unit/validate/test_quality_rules.py tests/property/test_quality_rules_never_raise.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `resolve_validation_rule("QUALITY_PATTERN") is PatternRule`
    - The hypothesis property test runs at least 50 examples (default) and finds zero counterexamples across `CompletenessRule`/`ValidityRangeRule`/`PatternRule`
  </acceptance_criteria>
  <done>`resolve_validation_rule("QUALITY_PATTERN") is PatternRule`</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV row content -> RejectedRecord.raw_line/error_message | Every rejection message embeds a row's own field values; these become untrusted content downstream (persisted via %s binding in plan 08-03/08-11, never here) |
| PatternRule's regex compilation | `pattern` comes from `configs/datasets/*.yaml`, developer-authored, not attacker input — no ReDoS concern from an untrusted regex source in this platform's threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Denial of Service | PatternRule's `re.compile` | accept | Pattern is developer-authored config, compiled once per stage construction (not per-row); no untrusted-regex-source ReDoS vector in this platform |
| T-08-10 | Tampering | RejectedRecord.raw_line construction | accept (deferred) | This plan only constructs the in-memory `RejectedRecord`; the SQL-injection-relevant boundary (persisting it) is mitigated in plan 08-03/08-11 via parameterized binding — tracked there, not duplicated here |
</threat_model>

<verification>
- `pytest tests/unit/validate/ tests/property/test_quality_rules_never_raise.py -x` green
</verification>

<success_criteria>
Three of VALID-02's five rule families exist as isolated, fully-tested `StreamingStage`s, registered by name — ready for plan 08-10 to wire into `StagingLoader` without any further detection-logic changes.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-04-SUMMARY.md` when done
</output>
