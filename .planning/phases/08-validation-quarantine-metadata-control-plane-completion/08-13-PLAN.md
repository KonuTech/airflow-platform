---
phase: 08-validation-quarantine-metadata-control-plane-completion
plan: 13
type: execute
wave: 7
depends_on: ["08-12"]
files_modified:
  - tests/dagtest/__init__.py
  - tests/dagtest/conftest.py
  - tests/dagtest/test_backfill_dagrun.py
  - pyproject.toml
autonomous: true
requirements: [VALID-08]
must_haves:
  truths:
    - "A backfill DagRun against csv_ingest_customers (or csv_ingest_orders) executes with the correct logical_date and task graph, KubernetesPodOperator execution mocked, proven via dag.test() against a real (testcontainers) Airflow metadata database -- this codebase's first dag.test() usage"
    - "tests/dagtest/ is its own CI stage/job, never sharing a pytest invocation with tests/integration/'s own testcontainers Postgres, so the two never run concurrent containers inside the same 4 CPU/16GB CI budget"
  artifacts:
    - path: "tests/dagtest/conftest.py"
      provides: "A session-scoped testcontainers PostgreSQL for the Airflow metadata DB (distinct from tests/integration/'s analytical DB fixture), AIRFLOW__DATABASE__SQL_ALCHEMY_CONN wiring, AIRFLOW__CORE__EXECUTOR=LocalExecutor, and a KubernetesPodOperator.execute mock fixture"
      contains: "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN"
    - path: "tests/dagtest/test_backfill_dagrun.py"
      provides: "The dag.test()-based proof that a backfill DagRun's mechanics are correct"
      contains: "dag.test("
---

<objective>
Stand up `tests/dagtest/`, a genuinely new test tier for this codebase: `dag.test()`-based behavioral DAG tests against a real (testcontainers) Airflow metadata database, with `KubernetesPodOperator.execute` mocked per CLAUDE.md's own documented pattern. Prove `csv_ingest_customers`'/`csv_ingest_orders`'s backfill-DagRun mechanics — correct `logical_date`, correct task graph traversal, correct `run_id`/`try_number` wiring — without ever launching a real pod.

Purpose: RESEARCH.md's own Pitfall 3 is explicit that `dag.test()` can prove DAG-level backfill mechanics but NOT the real resolution-state-transition logic inside a mocked-away pod (that is plan 08-03's job) nor the full genuine live proof (plan 08-14's job) — this plan is the middle tier of that explicit three-tier split.
Output: `tests/dagtest/conftest.py` (a new testcontainers-backed fixture set, deliberately separate from `tests/integration/`'s), `test_backfill_dagrun.py`, and a new `dagtest` pytest marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-RESEARCH.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-PATTERNS.md
@.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/dagtest/ conftest.py — testcontainers Airflow metadata DB + KPO mock fixture</name>
  <files>tests/dagtest/__init__.py, tests/dagtest/conftest.py, pyproject.toml</files>
  <read_first>tests/integration/conftest.py, tests/unit/conftest.py, CLAUDE.md</read_first>
  <action>
Add a `dagtest` entry to `pyproject.toml`'s `[tool.pytest.ini_options]` `markers` list: `"dagtest: needs a local Docker daemon (testcontainers PostgreSQL for Airflow metadata); excluded from the offline gate"`, mirroring the `integration` marker's exact phrasing precedent.

Create `tests/dagtest/__init__.py` (empty, ruff INP001 precedent, matching `tests/unit/normalize/__init__.py`'s own docstring reasoning).

Create `tests/dagtest/conftest.py`: a session-scoped `_require_docker` fixture, copied structurally from `tests/integration/conftest.py`'s own (same skip-with-reason shape, but this tier's own reason text names `tests/dagtest/`). A session-scoped `airflow_metadata_dsn` fixture using `testcontainers.community.postgres.PostgresContainer` — a SEPARATE container from `tests/integration/conftest.py`'s `postgres_dsn` (never share a fixture across the two conftest.py files; this is a distinct Airflow *metadata* DB, not the analytical DB — CLAUDE.md's §4 constraint applies to test infrastructure too, not only production). A fixture `airflow_env` (function- or session-scoped, whichever `dag.test()`'s own DB-initialization cost favors — prefer session-scoped with a per-test DB reset if `dag.test()` proves noisy across tests, else function-scoped for isolation; default to session-scoped and document the tradeoff) that sets `AIRFLOW__DATABASE__SQL_ALCHEMY_CONN` (from `airflow_metadata_dsn`, with the `postgresql://` -> `postgresql+psycopg2://` SQLAlchemy dialect prefix Airflow's metadata DB driver expects — NOT the plain `psycopg` DSN shape `tests/integration/`'s analytical fixtures use) and `AIRFLOW__CORE__EXECUTOR=LocalExecutor` as environment variables for the test process, then runs `airflow db migrate` (or the Airflow-3-appropriate metadata-DB-initialization command) once against the fresh container before yielding. A fixture `mock_kpo_execute` that patches `KubernetesPodOperator.execute` (via `unittest.mock.patch.object`) to return a canned success value without touching any real cluster, matching CLAUDE.md's own documented "Mock the KPO in unit tests: patch `KubernetesPodOperator.execute` and assert on the constructed pod spec" guidance — yields the mock so a test can assert on call args (image tag, SA name, resources, env) if needed.
  </action>
  <verify>
    <automated>pytest tests/dagtest/ --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dagtest/conftest.py`'s fixtures never import or reference `tests/integration/conftest.py`'s `postgres_dsn`/`migrated_dsn` — fully independent container lifecycle
    - `pyproject.toml`'s `markers` list contains the new `dagtest` entry
  </acceptance_criteria>
  <done>`tests/dagtest/conftest.py`'s fixtures never import or reference `tests/integration/conftest.py`'s `postgres_dsn`/`migrated_dsn` — fully independent container lifecycle</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: test_backfill_dagrun.py — dag.test() proves backfill DagRun mechanics</name>
  <files>tests/dagtest/test_backfill_dagrun.py</files>
  <read_first>airflow/dags/csv_ingest_customers.py, airflow/dags/csv_ingest_orders.py, tests/dagtest/conftest.py</read_first>
  <behavior>
    - `dag.test(execution_date=<a fixed historical logical_date>)` against `csv_ingest_customers` (loaded via `DagBag`, `KubernetesPodOperator.execute` mocked) completes without raising, and the resulting DagRun's tasks all reach a terminal state consistent with success (the sensor/gate/discover/ingest chain all "ran," per the mock).
    - The SAME call with a DIFFERENT `execution_date` produces a DIFFERENT `dag_run_id` but resolves the identical task graph shape (same task count, same dependency edges) — proving a backfill run is not a structurally different code path from a normal scheduled run, matching `csv_ingest_customers.py`'s own module docstring claim ("a backfilled run just re-invokes wait_for_files -> discover -> ingest against the CURRENT state").
    - `resolve_window()`'s own `logical_date` output is populated (non-`None`) for a backfill-triggered run specifically (distinct from the existing `ORCH-05` proof, which covers the `logical_date=None` asset-triggered case) — this test's own new angle on that task.
  </behavior>
  <action>
Mark the whole module `@pytest.mark.dagtest`. Load `csv_ingest_customers`'s `DAG` object via `DagBag(dag_folder="airflow/dags", include_examples=False).dags["csv_ingest_customers"]` (reuse `tests/unit/conftest.py`'s existing `sys.path`/env-var bootstrap pattern for `DagBag` construction — import or replicate its logic, since `tests/dagtest/`'s own `conftest.py` is deliberately a separate file from `tests/unit/conftest.py`). Call `dag.test(execution_date=pendulum.datetime(2026, 1, 1, tz="UTC"))` inside the `mock_kpo_execute`/`airflow_env` fixtures' scope. Assert no exception propagates and inspect the returned `DagRun`'s task instance states (via `dag_run.get_task_instances()` or the `dag.test()` return value's own documented shape — confirm the exact Airflow-3.3.0 return contract by checking the installed `airflow` package's own `DAG.test` docstring/signature before writing assertions, since this is a genuinely new API surface for this codebase). Repeat with a second, different `execution_date` and assert the task graph shape (task_id set, dependency edges) is identical between the two runs. Add a parallel, smaller test for `csv_ingest_orders` proving the same DAG-executes-cleanly property (not necessarily every assertion duplicated — one solid proof per DAG is sufficient, matching Pitfall 3's own scope limit: this tier proves DAG-level mechanics, not the real resolution-state-transition logic, which stays in plan 08-03's tier).
  </action>
  <verify>
    <automated>pytest tests/dagtest/test_backfill_dagrun.py -x -m dagtest</automated>
  </verify>
  <acceptance_criteria>
    - `dag.test()` completes without raising for both DAGs, with `KubernetesPodOperator.execute` mocked throughout (no real pod launch attempted)
    - Two different `execution_date`s produce structurally identical task graphs
  </acceptance_criteria>
  <done>`dag.test()` completes without raising for both DAGs, with `KubernetesPodOperator.execute` mocked throughout (no real pod launch attempted)</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-only testcontainers Airflow metadata DB | Ephemeral, throwaway, never touches the real cluster's Airflow metadata DB or the analytical DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-22 | Denial of Service | Three concurrent testcontainers PostgreSQL instances (this tier's + tests/integration/'s two) exceeding the 4 CPU/16GB CI runner budget | mitigate | This tier's own CI job/stage is separate from `tests/integration/`'s (per the orchestrator's resolved guidance) — never invoked in the same pytest process/CI step |
</threat_model>

<verification>
- `pytest tests/dagtest/ -x -m dagtest` green (requires Docker)
- `pytest tests/dagtest/ --collect-only -q` succeeds with zero collection errors even without Docker (proves the module imports cleanly; actual execution still needs `_require_docker`'s skip)
</verification>

<success_criteria>
This codebase's first `dag.test()`-based test tier exists, proving backfill-DagRun mechanics for both DAGs — sized and isolated per the orchestrator's own CI-budget guidance.
</success_criteria>

<output>
Create `.planning/phases/08-validation-quarantine-metadata-control-plane-completion/08-13-SUMMARY.md` when done
</output>
