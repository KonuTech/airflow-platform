# Deferred Items — Phase 09

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Test infra (pre-existing, out of scope) | `pytest tests/dagtest/` (running the WHOLE directory in one session) fails both `test_backfill_dagrun.py` tests with `sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) no such table: task_instance` — `tests/dagtest/test_run_stage_recorder.py` imports `_common.run_stage_recorder` (which imports `airflow.sdk`) at module COLLECTION time (see that file's own top-of-file `sys.path` bootstrap comment), triggering `airflow.settings.initialize()`/`configure_orm()` against the DEFAULT sqlite backend before `test_backfill_dagrun.py`'s session-scoped `airflow_env` fixture ever sets `AIRFLOW__DATABASE__SQL_ALCHEMY_CONN` to the real testcontainers Postgres DSN. Confirmed via `git stash` that this reproduces byte-identically on the UNMODIFIED phase-09-wave-3 baseline (commit `53313e3`), i.e. it predates plan 09-09's DAG wiring entirely. Each file individually (`pytest tests/dagtest/test_backfill_dagrun.py` alone, or `pytest tests/dagtest/test_run_stage_recorder.py` alone) passes cleanly. | Deferred (pre-existing, out of scope per SCOPE BOUNDARY) | 09-09 |
| Lint (pre-existing, out of scope) | `tests/policy/test_gates_actually_fail.py::test_the_main_gate_does_not_lint_the_bad_samples` fails because `make lint` (`ruff check .` over the whole repo) reports `E501 Line too long (101 > 100)` at `tests/integration/test_migrations.py:618-619` (the `dbt_app must never be granted on normalized/meta...` assertion message). Confirmed via `git diff HEAD -- tests/integration/test_migrations.py` that plan 09-09 never touched this file at any commit — pre-existing, unrelated to LOAD-06's DAG wiring. | Deferred (pre-existing, out of scope per SCOPE BOUNDARY) | 09-09 |
| **Core correctness gap (pre-existing, LOAD-06-relevant, out of scope for this plan's file list)** | `stage_ingest` (`packages/dataplat/src/dataplat/pipeline/run.py`, from plan 08.1-10) never writes a `STAGE_LOAD` row to `meta.run_stages` — live-confirmed via direct `psql` queries against the analytics DB: `SELECT stage_name, status, count(*) FROM meta.run_stages GROUP BY stage_name, status` returns ONLY `PUBLISH` rows, zero `STAGE_LOAD`/`DBT_BUILD` ever, across the whole live cluster. `grep -rn "STAGE_LOAD" packages/dataplat/src/` finds exactly one hit — a docstring in `repository.py` — never an actual call site. Root cause: `claim_run_stage`'s own SQL guard (`WHERE EXISTS (SELECT 1 FROM meta.ingestion_runs WHERE run_id = %(run_id)s AND status = 'STAGED')`, `postgres.py` lines ~489-492) can only succeed for a run ALREADY at `status='STAGED'` — a status `stage_ingest` itself only reaches at its own end, so the existing `claim_run_stage` method is structurally the wrong shape to also serve `STAGE_LOAD`'s own claim (it was built for `publish_ingest`'s "claim STAGED runs" pattern, commit `013eb67`, and never generalized). Impact: `list_run_ids_pending_dbt_build` (plan 09-04) requires a `SUCCEEDED` `STAGE_LOAD` row to consider a run DBT_BUILD-eligible — since one never exists, DBT_BUILD tracking never activates live, `meta.v_run_recovery.stage_load_status` is permanently NULL, and this plan's own `test_pod_kill_mid_dbt_build_produces_no_duplicates` (Task 2) cannot pass against the live cluster until fixed. Fix requires a new claim mechanism in `dataplat`'s `MetadataRepository` (`postgres.py`/`repository.py`) plus a `stage_ingest` call site and an image rebuild+redeploy — a genuine architectural decision (Rule 4), not attempted in this plan. | Deferred (Rule 4 — architectural decision, needs its own plan/debug session) | 09-10 |
