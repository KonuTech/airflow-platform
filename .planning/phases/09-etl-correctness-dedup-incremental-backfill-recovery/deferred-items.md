# Deferred Items — Phase 09

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Test infra (pre-existing, out of scope) | `pytest tests/dagtest/` (running the WHOLE directory in one session) fails both `test_backfill_dagrun.py` tests with `sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) no such table: task_instance` — `tests/dagtest/test_run_stage_recorder.py` imports `_common.run_stage_recorder` (which imports `airflow.sdk`) at module COLLECTION time (see that file's own top-of-file `sys.path` bootstrap comment), triggering `airflow.settings.initialize()`/`configure_orm()` against the DEFAULT sqlite backend before `test_backfill_dagrun.py`'s session-scoped `airflow_env` fixture ever sets `AIRFLOW__DATABASE__SQL_ALCHEMY_CONN` to the real testcontainers Postgres DSN. Confirmed via `git stash` that this reproduces byte-identically on the UNMODIFIED phase-09-wave-3 baseline (commit `53313e3`), i.e. it predates plan 09-09's DAG wiring entirely. Each file individually (`pytest tests/dagtest/test_backfill_dagrun.py` alone, or `pytest tests/dagtest/test_run_stage_recorder.py` alone) passes cleanly. | Deferred (pre-existing, out of scope per SCOPE BOUNDARY) | 09-09 |
