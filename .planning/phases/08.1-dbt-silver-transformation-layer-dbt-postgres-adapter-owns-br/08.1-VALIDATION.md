---
phase: 08.1
slug: dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-18
---

# Phase 08.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.1.1 (existing) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` (existing) |
| **Quick run command** | `pytest tests/unit -q` |
| **Full suite command** | `pytest tests/ -q -m "not cluster and not manifests"` (matches this project's existing offline-gate convention) |
| **Estimated runtime** | ~30s (unit-only offline gate; new `dbt`-marked integration tests run against testcontainers, excluded from the fast per-commit loop, same treatment as `integration`/`cluster`) |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/unit -q`
- **After every plan wave:** Run `pytest tests/ -q -m "not cluster and not manifests"`
- **Before `/gsd:verify-work`:** Full suite must be green, including the new `dbt`-marked integration
  tests
- **Max feedback latency:** 60s

---

## Per-Task Verification Map

Bound to the 13 PLAN.md files created for this phase (revision iteration 1 — every task's
`<verify><automated>` command now exists in its PLAN.md; the `Task ID`/`Plan`/`Wave` columns below
are filled in from those files, replacing the pre-planning `TBD` placeholders).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.1-08.T3 | 08.1-08 | 3 | DEDUP-01 | — | Within-file business-key + hash dedup lands correctly in silver | integration | `pytest tests/integration/test_dbt_silver_dedup.py -m dbt` | ❌ W0 | ⬜ pending |
| 08.1-08.T3 | 08.1-08 | 3 | DEDUP-02 | — | Cross-file/batch/run dedup detected via `is_incremental()` cursor across sequential `dbt build` runs | integration | `pytest tests/integration/test_dbt_silver_incremental.py -m dbt` | ❌ W0 | ⬜ pending |
| 08.1-08.T2/T3 | 08.1-08 | 3 | DEDUP-03 | — | Strategy interface (business-key + hash, per-dataset) expressed as dbt model config, no `SELECT DISTINCT` | integration | `pytest tests/integration/test_dbt_silver_dedup.py -m dbt` | ❌ W0 | ⬜ pending |
| 08.1-08.T1/T3 | 08.1-08 | 3 | DEDUP-04 | — | `meta.dedup_audit` populated atomically (same transaction) with the silver write via dbt post-hook | integration | `pytest tests/integration/test_dbt_dedup_audit.py -m dbt` | ❌ W0 | ⬜ pending |
| 08.1-01.T3 / 08.1-08.T3 | 08.1-01 / 08.1-08 | 1 / 3 | D-14 | T-08.1-02 | A hand-crafted duplicate-key bronze fixture row raises a real Postgres constraint violation, never a silent overwrite | integration | `pytest tests/integration/test_silver_constraints.py -m dbt` | ❌ W0 | ⬜ pending |
| 08.1-09.T1 | 08.1-09 | 3 | D-12 (OBS-07 ext.) | — | `meta.v_customers_lineage` resolves a row end-to-end through its dbt/silver hop | integration | `pytest tests/integration/test_lineage_view.py -m integration` | ✅ existing, needs extension | ⬜ pending |
| 08.1-01.T1/T3 | 08.1-01 | 1 | New `dbt_app` role | T-08.1-01 | Migration creates role with correct USAGE-before-table-grant sequencing (this project has been burned by the reverse order once, migration 0008); negative test proves no write access to `normalized`/`meta` | integration | `pytest tests/integration/test_migrations.py -m integration` | ✅ existing, needs extension | ⬜ pending |
| 08.1-02.T1/T2 | 08.1-02 | 1 | Docker image (dbt) | T-08.1-04/T-08.1-05/T-08.1-SC | `docker run <dbt-image> dbt --version` succeeds; non-root; no `apache-airflow`/`dataplat` present | integration | `pytest tests/integration/test_dbt_docker_image.py -m integration` | ❌ W0 (template: existing `test_docker_image.py`) | ⬜ pending |
| 08.1-06.T1 | 08.1-06 | 2 | D-01 (durable bronze) | — | New bronze table is `LOGGED` (permanent), not `UNLOGGED` — survives an unclean shutdown | integration | `pytest tests/integration/test_staging_durability.py -m integration` | ❌ W0 | ⬜ pending |
| 08.1-07.T1 | 08.1-07 | 3 | D-17 (two-phase claim) | — | `stage` claim/completion and `publish` claim/completion are independently observable states; a `publish` that finds no `STAGED` run is a clean no-op, not an error | integration | `pytest tests/integration/test_claim_lease_split.py -m integration` | ❌ W0 | ⬜ pending |
| 08.1-07.T3 | 08.1-07 | 3 | D-18 (idempotency-key extension) | — | Extended idempotency-key formula produces a NEW claimable `run_id` for a historical file already `SUCCEEDED` under the old formula, with `replay_of_run_id` set | integration | `pytest tests/integration/test_backfill_idempotency.py -m integration` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Threat Ref note: corrected against the final `08.1-01`/`08.1-02`-PLAN.md `<threat_model>` STRIDE
registers (re-verification pass, revision iteration 1) — the D-14 and "New `dbt_app` role" rows had
their `T-08.1-01`/`T-08.1-02` values swapped from the pre-planning draft, and the Docker-image row's
placeholder `T-08.1-03` (actually 08.1-01's bronze-tables threat) was replaced with the real
Docker-image threats from `08.1-02-PLAN.md`.*

---

## Wave 0 Requirements

- [x] `tests/integration/test_dbt_silver_dedup.py` — covers DEDUP-01/DEDUP-03 (plan 08.1-08, Task 3)
- [x] `tests/integration/test_dbt_silver_incremental.py` — covers DEDUP-02, D-05/D-06 (plan 08.1-08, Task 3)
- [x] `tests/integration/test_dbt_dedup_audit.py` — covers DEDUP-04, D-09 (plan 08.1-08, Task 3)
- [x] `tests/integration/test_silver_constraints.py` — covers D-14 (plan 08.1-08, Task 3; also
      proven at the migration level by plan 08.1-01, Task 3)
- [x] `tests/integration/test_dbt_docker_image.py` — new file, template from the existing
      `test_docker_image.py` for `csv-processor` (plan 08.1-02, Task 2)
- [x] `tests/integration/test_staging_durability.py` — covers D-01's `LOGGED` bronze table (plan 08.1-06, Task 1)
- [x] `tests/integration/test_claim_lease_split.py` — covers D-17's two-phase claim (plan 08.1-07, Task 1)
- [x] `tests/integration/test_backfill_idempotency.py` — covers D-18's idempotency-key extension (plan 08.1-07, Task 3)
- [x] A `dbt` pytest marker registered in `pyproject.toml`'s `markers = [...]` list (plan 08.1-08, Task 1)
- [x] `tests/integration/test_migrations.py` extension for the new `dbt_app` role/`silver` schema
      (plan 08.1-01, Task 3; further extended by plan 08.1-05, Tasks 1-2 and plan 08.1-09, Task 2)
- [x] `tests/integration/test_lineage_view.py` extension for the dbt hop (D-12) (plan 08.1-09, Task 1)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| D-16 backfill re-processes all historical files through stage→dbt→publish; silver ends up complete and matching gold | D-16, D-18 | Requires the real kind cluster's Airflow scheduler, KPO pod fan-out, and the actual historical files in MinIO — not reproducible in testcontainers | Trigger the backfill per README §33/§34 over the historical window; verify `SELECT count(*) FROM silver.customers` matches the business-key-deduplicated count in `normalized.customers`; spot-check `meta.dedup_audit` rows exist for the backfilled range (implemented as plan 08.1-13, Task 3) |
| Two-phase claim (D-17) survives a `dbt build` slower than the old 5-minute lease window without losing run state | D-17 | Requires real KPO pod scheduling/timing under this project's documented tight CPU budget — not reproducible with mocked timing | Observe (or deliberately throttle) a slow `dbt build` KPO pod on the live cluster; confirm `publish` still resolves the run to `SUCCEEDED` once dbt completes, with no orphaned `RUNNING`/`STAGED` row left behind (implemented as part of plan 08.1-13, Task 2's live proof) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — confirmed: every non-checkpoint
      task across all 13 PLAN.md files has a `<verify><automated>` element (`gsd-sdk query
      verify.plan-structure` run against all 13 files, zero `hasVerify: false` outside the two
      `checkpoint:human-verify` tasks in plan 08.1-13, which use `<what-built>`/`<how-to-verify>`
      instead by design)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — the only tasks
      lacking `<verify>` are plan 08.1-13's two consecutive checkpoint tasks (Task 2, Task 3),
      preceded by Task 1's automated verify; no run of 3
- [x] Wave 0 covers all MISSING references — every Wave 0 Requirements bullet above now cites the
      real plan/task that delivers it
- [x] No watch-mode flags — confirmed, no `<automated>` command across any plan uses `--watch` or
      an equivalent long-running flag
- [x] Feedback latency < 60s — quick-gate command (`pytest tests/unit -q`) stays under 60s; `dbt`-
      and `cluster`-marked commands are explicitly excluded from the per-commit quick gate (same
      treatment as this project's existing `integration`/`cluster` tiers), per the Sampling Rate
      section above
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** Revision iteration 1 addressed the checker's `nyquist_compliance` blocker: all ~30
non-checkpoint tasks across all 13 plans were missing `<verify><automated>` and `<done>` — every
task now has both, using the same command already implied by its `acceptance_criteria` (never a
newly invented check). Verified via `gsd-sdk query verify.plan-structure` against every plan file
(`valid: true`, zero structural errors). No remaining known Nyquist gaps.
