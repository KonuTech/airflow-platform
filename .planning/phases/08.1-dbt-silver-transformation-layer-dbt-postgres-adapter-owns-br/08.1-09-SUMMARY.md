---
phase: 08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br
plan: 09
subsystem: database
tags: [postgresql, alembic, lineage, indexing, dbt, migrations]

# Dependency graph
requires:
  - phase: 08.1 (plan 08.1-05)
    provides: meta.dedup_audit / meta.dedup_decisions (migration 0024), the dbt dedup audit trail this plan's lineage-view extension joins against
  - phase: 08.1 (plan 08.1-05, migration 0023)
    provides: silver.customers / silver.orders tables this plan's lineage-view extension LEFT JOINs against
provides:
  - meta.v_customers_lineage extended with the dbt/silver hop (silver_loaded_at, dbt_invocation_id, dbt_run_at, dbt_invocation_records_deduplicated), migration 0026
  - Five gold physical-modeling indexes on normalized.orders/normalized.customers serving time-range, point-lookup and ad-hoc BI access patterns, migration 0027
  - Closes out D-12 and D-13, the last two 08.1-CONTEXT.md decisions not covered by an earlier plan in this phase
affects: [phase-08.1-verification, any future gold-schema physical-modeling work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "DROP VIEW + CREATE VIEW full-replacement pattern for view schema changes (Postgres has no ALTER VIEW ... ADD COLUMN), matching migration 0012's own established convention"
    - "Range-based JOIN (col BETWEEN min_id AND max_id) to bridge a batching invocation's audit summary to the individual rows it covers, when a single dbt invocation can span multiple bronze runs (D-05 watermark batching)"
    - "Indexing over partitioning when a partition-key-in-unique-constraint requirement would conflict with an existing single-column ON CONFLICT business-key guarantee"

key-files:
  created:
    - migrations/versions/0026_v_customers_lineage_dbt_hop.py
    - migrations/versions/0027_normalized_gold_physical_modeling.py
  modified:
    - tests/integration/test_lineage_view.py
    - tests/integration/test_migrations.py

key-decisions:
  - "meta.v_customers_lineage's new silver.customers/dedup_audit joins are LEFT JOIN, never JOIN -- a gold row published before this phase, or before dbt's first invocation covering it, must still return a row with the four new columns NULL, never dropped or errored"
  - "dbt_app is NOT granted SELECT on meta.v_customers_lineage -- it has no legitimate need to read its own lineage bridge (08.1-PATTERNS.md's own flagged call, resolved as no)"
  - "Gold stays index-only, not partitioned -- range-partitioning normalized.orders by order_date would force the ON CONFLICT (order_id) unique constraint to become (order_id, order_date), silently allowing a date-correcting update to insert a duplicate business-key row. User confirmed this resolution during planning (08.1-CONTEXT.md D-13)."

patterns-established:
  - "When extending a lineage view across a new subsystem boundary (dbt/silver here), use LEFT JOIN plus an explicit downgrade that restores the prior view verbatim -- never assume every future consumer of the view already has the new joined data."

requirements-completed: [DEDUP-04]

# Metrics
duration: 25min
completed: 2026-08-18
---

# Phase 08.1 Plan 09: Lineage Bridge + Gold Physical Modeling Summary

**`meta.v_customers_lineage` now answers "which dbt invocation deduplicated this row, and how many siblings did it drop" in the same query as OBS-07's original chain; `normalized.orders`/`normalized.customers` gained four new indexes for time-range/point-lookup/ad-hoc BI access without touching either table's `ON CONFLICT` business-key uniqueness.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-08-18T17:05:00Z (approx, worktree setup)
- **Completed:** 2026-08-18T17:24:03Z
- **Tasks:** 2 completed
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments
- `meta.v_customers_lineage` (migration 0026) bridges the dbt/silver hop: `LEFT JOIN silver.customers` (business-key match) + `LEFT JOIN meta.dedup_audit` (range join on `min_run_id`/`max_run_id`, honoring D-05's watermark-batching schema shape from migration 0024), surfacing `silver_loaded_at`, `dbt_invocation_id`, `dbt_run_at`, `dbt_invocation_records_deduplicated`
- `normalized.orders`/`normalized.customers` (migration 0027) gained the D-13 physical-modeling indexes, with the module docstring documenting exactly why gold stays index-only rather than partitioned (the `ON CONFLICT` / partition-key-in-unique-constraint conflict, confirmed by the user during planning)
- Both migrations verified end-to-end: `alembic upgrade head` -> `alembic downgrade base` -> `alembic upgrade head` succeeds cleanly across the full 27-migration chain
- 19/19 integration tests pass across `tests/integration/test_lineage_view.py` (3 tests) and `tests/integration/test_migrations.py` (16 tests)

## Task Commits

Each task was committed atomically:

1. **Task 1: Migration 0026 — extend meta.v_customers_lineage across the dbt/silver hop (D-12)** - `2c773ab` (feat)
2. **Task 2: Migration 0027 — gold physical modeling via indexes, not partitioning (D-13)** - `ab6144e` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `migrations/versions/0026_v_customers_lineage_dbt_hop.py` - DROP+CREATE VIEW extending `meta.v_customers_lineage` with the dbt/silver hop; genuine `downgrade()` restoring migration 0012's original view verbatim
- `migrations/versions/0027_normalized_gold_physical_modeling.py` - Four new indexes on `normalized.orders`/`normalized.customers`; module docstring documents the indexing-over-partitioning tradeoff
- `tests/integration/test_lineage_view.py` - Extended the existing published-row test to seed `silver.customers`+`meta.dedup_audit` and assert the four new columns; added a new test proving the `LEFT JOIN` NULL contract when no silver data exists; added the missing `pytestmark = pytest.mark.integration`
- `tests/integration/test_migrations.py` - Added `test_gold_indexes_exist_and_business_key_uniqueness_is_unchanged`: asserts all five named gold indexes exist via `pg_indexes`, and regression-proves a duplicate `order_id` INSERT still raises `psycopg.errors.UniqueViolation`

## Decisions Made
- Confirmed via the plan's own objective note and applied as written: gold stays index-only (D-13), never partitioned, because `MergePublisher`/`OrdersMergePublisher`'s `ON CONFLICT (customer_id)`/`ON CONFLICT (order_id)` publish targets must stay unchanged, and Postgres declarative partitioning requires every unique constraint to include the partition key.
- `dbt_app` is deliberately not granted `SELECT` on the extended `meta.v_customers_lineage` view, matching the plan's explicit instruction and 08.1-PATTERNS.md's own flagged call.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Skipped re-creating `ix_orders_customer_id` in migration 0027**
- **Found during:** Task 2 (writing migration 0027)
- **Issue:** The plan's task action literally instructed `op.create_index("ix_orders_customer_id", "orders", ["customer_id"], schema="normalized")` as one of the five gold indexes. That exact index (same name, same table, same schema) was already created by migration 0016 (`normalized.orders`'s own original DDL, for the referential-integrity barrier's anti-join performance). Re-issuing an identical `CREATE INDEX` would raise `DuplicateTable` ("relation ... already exists") and abort the entire migration chain at `alembic upgrade head` — a genuine blocking bug, not a stylistic choice.
- **Fix:** Migration 0027 creates four NEW indexes instead of five (`ix_orders_order_date`, `ix_orders_order_date_customer_id`, `ix_customers_event_ts`, `ix_customers_country`), and its module docstring explicitly documents that `ix_orders_customer_id` already exists from migration 0016 and is deliberately not duplicated. The completeness test (`test_gold_indexes_exist_and_business_key_uniqueness_is_unchanged`) still asserts all FIVE named indexes exist via `pg_indexes` after `alembic upgrade head` — it doesn't matter which migration created each one, only that the physical-modeling picture is complete.
- **Files modified:** migrations/versions/0027_normalized_gold_physical_modeling.py, tests/integration/test_migrations.py
- **Verification:** `alembic upgrade head` succeeds (verified via a throwaway PostgreSQL 18 testcontainer, full 27-migration chain, upgrade -> downgrade to base -> re-upgrade all succeed cleanly); `test_gold_indexes_exist_and_business_key_uniqueness_is_unchanged` passes, confirming all five index names exist and the `order_id` `UniqueViolation` regression guard holds.
- **Committed in:** `ab6144e` (Task 2 commit)

**2. [Rule 1 - Bug] Added missing `pytestmark = pytest.mark.integration` to test_lineage_view.py**
- **Found during:** Task 1 (running the plan's own `<verify>` command)
- **Issue:** `tests/integration/test_lineage_view.py` had no `pytestmark`, so the plan's own verify command (`pytest tests/integration/test_lineage_view.py -m integration -q`) silently selected zero tests and exited 0 without running anything — the exact same class of bug `test_migrations.py`'s own docstring documents having previously fixed for itself.
- **Fix:** Added `pytestmark = pytest.mark.integration` at module scope, matching every other sibling `tests/integration/*.py` module's convention.
- **Files modified:** tests/integration/test_lineage_view.py
- **Verification:** `pytest tests/integration/test_lineage_view.py -m integration -q` now genuinely collects and runs all 3 tests (previously 0), all passing.
- **Committed in:** `2c773ab` (Task 1 commit)

**3. [Rule 1 - Bug] Fixed key_suffix/idempotency_key mismatch in the new NULL-columns test**
- **Found during:** Task 1 (writing `test_lineage_view_returns_null_dbt_hop_columns_when_no_silver_data_exists`)
- **Issue:** First draft used `key_suffix="lineage-no-silver"` for `_seed_pending_run` but `idempotency_key="run_ingest_lineage_no_silver:1"` (underscore vs. hyphen) and a stale `dataset_id` lookup (`"run_ingest_lineage"` instead of the correct per-test dataset name) — `claim_ingestion_run` refused the claim since no `PENDING` row matched, raising a `DataPlatformError`.
- **Fix:** Derived both the seeding `key_suffix` and the `idempotency_key`/`dataset_id` lookups from one shared local variable, eliminating the mismatch.
- **Files modified:** tests/integration/test_lineage_view.py
- **Verification:** Test passes; `receipt.status == "SUCCEEDED"` and the view correctly returns `NULL` for all four new dbt/silver-hop columns.
- **Committed in:** `2c773ab` (Task 1 commit)

---

**Total deviations:** 3 auto-fixed (all Rule 1 — bug fixes required to make the plan's own stated verification actually run and pass)
**Impact on plan:** All three fixes were necessary for the plan's stated acceptance criteria to be genuinely provable rather than vacuously true. No scope creep — the physical-modeling and lineage-bridging deliverables match the plan's objective exactly; only the literal index-duplication instruction and two test-authoring bugs needed correction.

## Issues Encountered
None beyond the three auto-fixed deviations documented above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- D-12 and D-13 are both closed; every 08.1-CONTEXT.md decision this phase's plans were scoped to cover is now implemented.
- The migration chain (0001 through 0027) is verified idempotent end-to-end (`upgrade head` -> `downgrade base` -> `upgrade head`), ready for phase-level verification.
- No blockers or concerns for phase verification specific to this plan.

---
*Phase: 08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br*
*Completed: 2026-08-18*

## Self-Check: PASSED

All created files verified present on disk (0026/0027 migrations, updated tests, this SUMMARY.md). All three commits (`2c773ab`, `ab6144e`, `e8c9d96`) verified present in `git log`.
