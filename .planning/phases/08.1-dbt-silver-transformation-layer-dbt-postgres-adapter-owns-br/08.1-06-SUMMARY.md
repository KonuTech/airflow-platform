---
phase: 08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br
plan: 06
subsystem: database
tags: [postgresql, staging, publisher, dbt-bronze, deduplication-docs]

# Dependency graph
requires:
  - phase: 08.1-01
    provides: "migration 0022's durable staging.customers/staging.orders (LOGGED, cumulative) tables"
provides:
  - "StagingLoader.promote_to_durable_bronze() -- appends a staged attempt's scratch buffer into durable staging.<dataset>, then drops the scratch buffer (D-01)"
  - "Publisher.publish()'s source_table parameter (renamed from staging_table) across protocol.py/merge.py/merge_orders.py -- proven to read from any table name, not just staging.*"
  - "DeduplicationConfig docstring corrected to point at the real dbt-based dedup mechanism instead of a Python DEDUP_REGISTRY that was never built"
affects: [08.1-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Per-run scratch buffer -> durable cumulative table promotion, collapsed into one connection's transaction scope (never TRUNCATE before append)"
    - "Publisher parameter naming reflects what the caller actually passes, not the historical scratch-table shape"

key-files:
  created:
    - tests/integration/test_staging_durability.py
  modified:
    - packages/dataplat/src/dataplat/load/staging.py
    - packages/dataplat/src/dataplat/load/publish/protocol.py
    - packages/dataplat/src/dataplat/load/publish/merge.py
    - packages/dataplat/src/dataplat/load/publish/merge_orders.py
    - packages/dataplat/src/dataplat/config/model.py
    - tests/integration/test_publish_merge.py

key-decisions:
  - "promote_to_durable_bronze() is a separate method from load(), not folded into it -- load() stays Phase 6/8's proven chunked-COPY path untouched; the new method is called only after any referential-integrity/circuit-breaker filtering already ran against the scratch buffer (plan 08.1-10), so exactly what survives filtering gets promoted"
  - "Publisher.publish()'s _PUBLISH_SQL format-string key inside merge.py/merge_orders.py stays named staging_table -- only the public parameter name changed; renaming the private format-key too was explicitly optional polish per the plan, skipped to keep the diff minimal"

requirements-completed: [DEDUP-02, DEDUP-03, INCR-03]

# Metrics
duration: 30min
completed: 2026-08-18
---

# Phase 08.1 Plan 06: Durable Bronze Promotion & Publisher Contract Rename Summary

**`StagingLoader.promote_to_durable_bronze()` gives bronze staging a durable, cumulative home (migration 0022) and `Publisher.publish()`'s `source_table` rename proves the FROM-clause reads from any table name, not just a scratch buffer.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-08-18T16:55:00Z (approx, after worktree base correction)
- **Completed:** 2026-08-18T17:12:00Z
- **Tasks:** 3
- **Files modified:** 6 (1 new test file, 5 modified)

## Accomplishments
- `StagingLoader.promote_to_durable_bronze()` appends a staging attempt's scratch-buffer rows into the durable `staging.<dataset>` table (migration 0022's LOGGED, cumulative bronze), then drops the scratch buffer -- proven cumulative across two runs and proven the durable table is never `UNLOGGED`.
- `Publisher.publish()`'s `staging_table` parameter renamed to `source_table` across the Protocol and both concrete publishers (`MergePublisher`, `OrdersMergePublisher`) -- a pure rename, proven live by a new test that publishes from a table literally named `test_silver_customers_arbitrary_name`.
- `DeduplicationConfig`'s docstring no longer references a Python `DEDUP_REGISTRY` that was never built and, per D-07, never will be -- it now points a future reader at the real mechanism (the dbt model's own SQL + `incremental_strategy`/`unique_key` config) and names `_check_deduplication_keys_are_business_key_columns` as `keys`'s real Python consumer.

## Task Commits

Each task was committed atomically:

1. **Task 1: StagingLoader.promote_to_durable_bronze()** - `b96d7cd` (feat), lint fix in `e1dce64` (fix)
2. **Task 2: Rename Publisher.publish()'s staging_table to source_table** - `efe6ee7` (refactor)
3. **Task 3: Correct DeduplicationConfig's stale DEDUP_REGISTRY docstring** - `6bbab3a` (docs)

**Plan metadata:** (this commit)

## Files Created/Modified
- `packages/dataplat/src/dataplat/load/staging.py` - Adds `promote_to_durable_bronze()`: `INSERT INTO staging.<dataset> ... SELECT ... FROM <scratch>` then `DROP TABLE IF EXISTS <scratch>`, both on the caller's connection, never committed here
- `tests/integration/test_staging_durability.py` - New: 3 tests proving promotion appends+cleans-up, is cumulative across two runs, and the durable table's `relpersistence` is `'p'` (permanent)
- `packages/dataplat/src/dataplat/load/publish/protocol.py` - `Publisher.publish()`'s `staging_table` parameter renamed `source_table`, docstring updated
- `packages/dataplat/src/dataplat/load/publish/merge.py` - `MergePublisher.publish()`'s parameter renamed to `source_table` (internal `_PUBLISH_SQL.format(staging_table=source_table)` key name kept as-is)
- `packages/dataplat/src/dataplat/load/publish/merge_orders.py` - Same rename for `OrdersMergePublisher.publish()`
- `tests/integration/test_publish_merge.py` - New `test_publish_reads_from_an_arbitrarily_named_source_table`, publishing from a table literally named `test_silver_customers_arbitrary_name`
- `packages/dataplat/src/dataplat/config/model.py` - `DeduplicationConfig`'s class/field docstrings rewritten: no more `DEDUP_REGISTRY` reference, dbt named as the real strategy interface, `_check_deduplication_keys_are_business_key_columns` named as `keys`'s real consumer -- no field/type/validator logic changed

## Decisions Made
- Kept `_PUBLISH_SQL`'s internal `{staging_table}` format-string placeholder name unchanged in `merge.py`/`merge_orders.py` -- the plan explicitly marked renaming it "optional polish, not required" since it's a private implementation detail, not part of the public API.
- `promote_to_durable_bronze()` placed immediately after `load()` in `StagingLoader`'s class body, per the plan's explicit placement instruction, with a docstring explaining why it's a separate method (load() stays the proven Phase 6/8 path; the new method is D-01's promotion step, called post-filtering by plan 08.1-10).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Docstring's own explanatory text initially re-introduced the literal string it was correcting away**
- **Found during:** Task 3 verification (`grep -n "DEDUP_REGISTRY" ... ` acceptance criterion)
- **Issue:** The first draft of `DeduplicationConfig`'s corrected docstring wrote "No `DEDUP_REGISTRY` exists in this codebase..." to explain the correction, which itself matched the exact grep pattern the acceptance criteria uses to confirm the stale reference is gone -- self-defeating.
- **Fix:** Reworded to "No Python dispatch registry for dedup strategies exists in this codebase..." -- same meaning, no longer contains the literal string.
- **Files modified:** `packages/dataplat/src/dataplat/config/model.py`
- **Verification:** `grep -n "DEDUP_REGISTRY" packages/dataplat/src/dataplat/config/model.py` now returns no match; `pytest tests/unit -k dataset_config -q` still 13 passed.
- **Committed in:** `6bbab3a` (part of Task 3 commit)

**2. [Rule 3 - Blocking] ruff S608/E501 failures on the new SQL statement and test line**
- **Found during:** Post-Task-1 lint verification (not in the plan's own `<verify>` block, but required by this project's overall CI gate and CLAUDE.md's lint/type conventions)
- **Issue:** `staging.py`'s new `INSERT INTO ... SELECT ... FROM` statement tripped ruff's S608 (possible SQL injection) rule; `test_staging_durability.py` had one line over the 100-char limit.
- **Fix:** Added a `# noqa: S608` justification identical in shape to `pipeline/run.py`'s existing precedent (config/run-derived identifiers only, never CSV content -- T-08.1-13, this plan's own threat model); wrapped the overlong test line across multiple arguments.
- **Files modified:** `packages/dataplat/src/dataplat/load/staging.py`, `tests/integration/test_staging_durability.py`
- **Verification:** `ruff check` and `ruff format --check` both clean; all 13 integration tests + `mypy packages/dataplat/src/dataplat/load/` still pass.
- **Committed in:** `e1dce64`

---

**Total deviations:** 2 auto-fixed (1 bug in my own docstring draft, 1 blocking lint issue)
**Impact on plan:** Both fixes are cosmetic/lint-level corrections to this plan's own new code; no scope creep, no logic changed beyond what the plan specified.

## Issues Encountered

**Worktree base drift at session start (pre-existing, not introduced by this plan's work).** On startup, this worktree's `HEAD` was found 5 commits ahead of the orchestrator's declared base commit (`09f8e92...`), carrying unrelated `docs`/`chore` commits from a prior `quick` task session (README updates, merge commits) that had nothing to do with plan 08.1-06. Per the mandatory `<worktree_branch_check>` pre-flight step, corrected via `git reset --hard 09f8e92580f82e649035b8ce32b89f91d09a0c5b` before any plan work began. No plan-08.1-06 work was lost (none had started yet); this is noted for visibility only.

**`-m integration` pytest marker filter (repo-wide pre-existing convention, not a deviation).** The plan's `<verify>`/`<verification>` blocks specify `pytest ... -m integration -q`. No test in `tests/integration/` anywhere in this codebase (confirmed by grep across `test_staging_loader.py`, `test_publish_merge.py`, and the new files this plan adds) actually carries `@pytest.mark.integration` -- the real gate, `make test-integration`, runs `pytest tests/integration -q` with no `-m` filter. Running the literal `-m integration` command deselects all tests (exit code 0, "N deselected") rather than running them. This plan's new/modified tests were verified the same way every other file in `tests/integration/` is verified in practice: `pytest tests/integration/test_staging_durability.py tests/integration/test_publish_merge.py -q` (13 passed). No fix applied -- this is a pre-existing, repo-wide documentation/convention gap outside this plan's file scope, not something this plan's tasks caused or are responsible for correcting.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `StagingLoader.promote_to_durable_bronze()` and `Publisher.publish()`'s `source_table` rename are both built and proven in isolation, ready for plan 08.1-10's `pipeline/run.py` split (`stage_ingest`/`publish_ingest`) to wire them together.
- `schemas/dataset-config.schema.json` (a committed, seemingly-manually-exported JSON Schema mirroring `DatasetConfig`'s Pydantic models) still contains the old `DEDUP_REGISTRY` docstring text verbatim -- it was NOT in this plan's `files_modified` scope and no generation script references it anywhere in `scripts/`/`Makefile`, so it was left untouched. Worth a follow-up note if a future phase discovers this schema file is meant to be kept in sync with `model.py`'s docstrings.
- No blockers for 08.1-10.

## Self-Check: PASSED

All 8 claimed files verified present on disk (`ls -la`). All 4 task commit hashes (`b96d7cd`, `efe6ee7`, `6bbab3a`, `e1dce64`) verified present in `git log --oneline --all`.
