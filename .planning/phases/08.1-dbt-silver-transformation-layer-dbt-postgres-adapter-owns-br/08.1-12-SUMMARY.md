---
phase: 08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br
plan: 12
subsystem: infra
tags: [airflow, kubernetesexecutor, kubernetespodoperator, dbt, taskflow, asset, dagbag]

# Dependency graph
requires:
  - phase: 08.1
    provides: "dbt ServiceAccount + Vault role (08.1-03), dbt_image Variable/image build (08.1-02), dataplat stage/publish CLI split (08.1-08/08.1-11)"
provides:
  - "csv_ingest_customers/csv_ingest_orders DAGs restructured to discover -> stage -> dbt_build -> publish"
  - "common_kpo_kwargs() generalized with service_account_name/image_variable/vault_k8s_role/include_dataplat_credentials, backward-compatible"
  - "dbt_build KubernetesPodOperator running under its own least-privilege dbt ServiceAccount/Vault role"
  - "publish (not stage) owns customers_asset's outlets, correcting cross-DAG scheduling to depend on GOLD state"
affects: [08.1-verification, phase-09]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "common_kpo_kwargs() keyword-only override pattern: new params default to today's exact values so existing call sites need zero changes"
    - "dbt_build KPO construction has no cmds/arguments override -- relies entirely on the dbt image's own ENTRYPOINT"

key-files:
  created: []
  modified:
    - airflow/dags/_common/kpo.py
    - airflow/dags/csv_ingest_customers.py
    - airflow/dags/csv_ingest_orders.py
    - tests/unit/test_dag_structure.py
    - tests/policy/test_dag_line_budget.py
    - tests/unit/conftest.py
    - tests/dagtest/conftest.py
    - tests/dagtest/test_backfill_dagrun.py
    - tests/e2e/observability/test_trace_propagation.py

key-decisions:
  - "dbt_build reuses _DISCOVER_RESOURCES (no fourth resource constant) to stay under the 150-line ORCH-06 budget, per plan's own explicit instruction"
  - "publish, not stage, owns outlets=[customers_asset] -- orders must wait for GOLD state, not bronze staging"
  - "dbt_build's construction is byte-identical between csv_ingest_customers.py and csv_ingest_orders.py -- dbt's project is dataset-agnostic, an unscoped dbt build from either DAG is a safe idempotent no-op for whichever model has nothing new"

requirements-completed: [DEDUP-01, DEDUP-02]

duration: 20min
completed: 2026-08-18
---

# Phase 08.1 Plan 12: Wire dbt_build into both ingestion DAGs Summary

**Airflow-side `discover -> stage -> dbt_build -> publish` task graph wired into both DAGs, with `dbt_build` running under its own least-privilege `dbt` ServiceAccount/Vault role via a generalized `common_kpo_kwargs()`.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-18T21:40Z
- **Completed:** 2026-08-18T21:59Z
- **Tasks:** 3
- **Files modified:** 9 (3 planned DAG/library files + 2 planned test files + 4 additional test files fixed as direct consequences of the rename, see Deviations)

## Accomplishments

- `common_kpo_kwargs()` generalized with `service_account_name`/`image_variable`/`vault_k8s_role`/`include_dataplat_credentials`, all defaulted to today's exact values -- `discover`/`stage`/`publish` call sites needed zero changes, proven by a regression check (default call produces byte-identical `env_vars` list/order to before)
- `csv_ingest_customers.py` and `csv_ingest_orders.py` both restructured from a single `ingest` task to `discover -> stage -> dbt_build -> publish`, `dbt_build` running under its own `"dbt"` ServiceAccount/Vault role/`dbt_image` Variable, with no `DATAPLAT_*` credential env vars at all (T-08.1-28)
- `publish` (not `stage`) owns `outlets=[customers_asset]`, correcting cross-DAG scheduling: `csv_ingest_orders` now waits for a real customers publish (GOLD state), not merely bronze staging
- Both DAG files stay under the 150-line ORCH-06 budget (149 lines each)
- `tests/unit/test_dag_structure.py` updated: `test_namespace_and_service_account` branches on `dbt_build`'s `"dbt"` SA; `test_customers_ingest_declares_outlets` renamed to `test_customers_publish_declares_outlets`; new `test_dbt_build_runs_between_stage_and_publish` proves the `stage -> dbt_build -> publish` chain structurally in both DAGs
- `tests/policy/test_dag_line_budget.py` gained `test_csv_ingest_orders_stays_under_150_lines`

## Task Commits

Each task was committed atomically:

1. **Task 1: Generalize common_kpo_kwargs() for the dbt pod's different identity** - `ebd6740` (feat)
2. **Task 2: Restructure csv_ingest_customers.py (stage -> dbt_build -> publish)** - `e8429f5` (feat)
3. **Task 3: Mirror csv_ingest_orders.py; update DagBag structural tests** - `0ee59ff` (feat)

**Plan metadata:** commit pending (this SUMMARY.md + STATE.md/ROADMAP.md, applied by the orchestrator after wave merge)

## Files Created/Modified

- `airflow/dags/_common/kpo.py` - `common_kpo_kwargs()` generalized with 4 new keyword-only params, all backward-compatible defaults
- `airflow/dags/csv_ingest_customers.py` - `discover -> stage -> dbt_build -> publish` task graph, `publish` owns `outlets=[customers_asset]`
- `airflow/dags/csv_ingest_orders.py` - identical restructuring, `dataset="orders"` substituted, no outlets of its own (D-16)
- `tests/unit/test_dag_structure.py` - service-account branching, renamed outlets test, new `stage -> dbt_build -> publish` structural test
- `tests/policy/test_dag_line_budget.py` - added the orders line-budget test
- `tests/unit/conftest.py` - `dagbag` fixture now also sets `AIRFLOW_VAR_DBT_IMAGE` (Rule 3 fix, see Deviations)
- `tests/dagtest/conftest.py` - `airflow_env`/`mock_kpo_execute` fixtures updated for the 4-task graph and the new `dbt_image` Variable (Rule 1/3 fixes, see Deviations)
- `tests/dagtest/test_backfill_dagrun.py` - `mocked_task_ids` assertions updated to `{"discover", "stage", "dbt_build", "publish"}` (Rule 1 fix, see Deviations)
- `tests/e2e/observability/test_trace_propagation.py` - live trace-root task_id literal updated from `"ingest"` to `"stage"` (Rule 1 fix, see Deviations)

## Decisions Made

- Reused `_DISCOVER_RESOURCES` for both `dbt_build` and `publish` rather than adding a third/fourth resource constant, exactly as the plan instructed, to preserve the 150-line budget
- Kept `dbt_build`'s construction byte-identical between the two DAG files (same `service_account_name`/`image_variable`/`vault_k8s_role`/`include_dataplat_credentials`) since dbt's project is dataset-agnostic scaffolding and an unscoped `dbt build` run from either DAG is a safe, idempotent no-op for whichever model has nothing new to process
- Tightened both DAG files' module docstrings (denser prose, dropped RST double-backtick markup in a few paragraphs) specifically to fit the 150-line budget after `ruff format` re-expanded the `dbt_build` `common_kpo_kwargs(...)` call onto one-parameter-per-line

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Scoped `# noqa: PLR0913` on `common_kpo_kwargs()`**
- **Found during:** Task 1 (generalizing `common_kpo_kwargs()`)
- **Issue:** `ruff check` (this project's `select = ["ALL"]` config) flagged `PLR0913` (6 > 5 arguments) on the newly-generalized function signature
- **Fix:** Added a scoped `# noqa: PLR0913` with an inline justification (all 6 are keyword-only, each documented in the function's own `Args:` section)
- **Files modified:** `airflow/dags/_common/kpo.py`
- **Verification:** `ruff check` passes clean
- **Committed in:** `e8429f5` (Task 2 commit, discovered while linting Task 2's changes)

**2. [Rule 3 - Blocking] `AIRFLOW_VAR_DBT_IMAGE` missing from both DagBag-parsing test fixtures**
- **Found during:** Task 3 (running `tests/unit/test_dag_structure.py`)
- **Issue:** `dbt_build`'s construction calls `Variable.get("dbt_image")` at DAG-parse time; neither `tests/unit/conftest.py`'s `dagbag` fixture nor `tests/dagtest/conftest.py`'s `airflow_env` fixture set that env var, so `DagBag`/`dag.test()` parsing raised `AirflowRuntimeError: VARIABLE_NOT_FOUND` for both DAG files
- **Fix:** Set `AIRFLOW_VAR_DBT_IMAGE` alongside the existing `AIRFLOW_VAR_CSV_PROCESSOR_IMAGE` in both fixtures, restored on teardown identically
- **Files modified:** `tests/unit/conftest.py`, `tests/dagtest/conftest.py`
- **Verification:** `pytest tests/unit/test_dag_structure.py tests/policy/test_dag_line_budget.py tests/policy/test_dag_thinness.py -q` passes (18/18)
- **Committed in:** `0ee59ff` (Task 3 commit)

**3. [Rule 1 - Bug] `tests/dagtest/` fixture/test assertions stale after the task-graph rename**
- **Found during:** Post-Task-3 sweep for other files referencing the old `"ingest"` task_id
- **Issue:** `tests/dagtest/conftest.py`'s `mock_kpo_execute` fixture docstring and `tests/dagtest/test_backfill_dagrun.py`'s two tests asserted `mocked_task_ids == {"discover", "ingest"}` -- a direct, mechanical consequence of this task's own rename (the previously-mapped `ingest` task is now `stage`, plus two new plain-KPO tasks `dbt_build`/`publish`)
- **Fix:** Updated both fixture docstrings and the two `mocked_task_ids` assertions to `{"discover", "stage", "dbt_build", "publish"}`
- **Files modified:** `tests/dagtest/conftest.py`, `tests/dagtest/test_backfill_dagrun.py`
- **Verification:** File syntax/lint verified (`ruff check`); this tier needs a local Docker daemon (testcontainers) and was not executed live in this worktree session -- the fix is a direct, mechanical rename with no new logic, verified by inspection against the DAG's actual task_ids
- **Committed in:** `0ee59ff` (Task 3 commit)

**4. [Rule 1 - Bug] `tests/e2e/observability/test_trace_propagation.py`'s live trace-root task_id literal was stale**
- **Found during:** Post-Task-3 sweep for other files referencing the old `"ingest"` task_id
- **Issue:** This live e2e test (requires a real kind cluster, `-m cluster`) asserts `lineage["task_id"] == claimed["task_id"] == "ingest"` -- the tracing-root task is now `stage`, not `ingest`, a direct consequence of this task's rename
- **Fix:** Updated the literal to `"stage"` and the three most load-bearing docstring/prose references to the mapped tracing-root task's name. Left this file's pre-existing, unrelated staleness (references to `dataplat.pipeline.run.run_ingest`, which a prior plan in this phase already split into `stage_ingest`/`publish_ingest`) untouched -- out of this task's scope per the deviation rules' scope boundary
- **Files modified:** `tests/e2e/observability/test_trace_propagation.py`
- **Verification:** File syntax/lint verified; this is a live-cluster-only test, not executable in this worktree session
- **Committed in:** `0ee59ff` (Task 3 commit)

---

**Total deviations:** 4 auto-fixed (2 Rule 3 blocking-issue fixes, 2 Rule 1 bug fixes)
**Impact on plan:** All four were direct, mechanical consequences of this plan's own task-graph rename (`ingest` -> `stage`/`dbt_build`/`publish`) or of generalizing `common_kpo_kwargs()`'s signature. No scope creep -- fixes were scoped narrowly to what the rename broke, and one adjacent pre-existing staleness (`run_ingest` references in the e2e file) was deliberately left untouched as out of scope.

## Issues Encountered

Fitting both restructured DAG files under the 150-line ORCH-06 budget required more iteration than expected: `ruff format` re-expands the `dbt_build` task's multi-keyword `common_kpo_kwargs(...)` call onto one parameter per line (Black-style), which cost 3 lines back after each attempt to condense it manually. Resolved by tightening the module docstrings' prose density (dropping RST double-backtick markup in a few paragraphs, denser wrapping) rather than fighting the formatter -- both files land at exactly 149 lines, verified by both `wc -l` and the actual `splitlines()`-based test assertion.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Both DAGs' Python/dbt/metadata machinery is now wired into a real, schedulable pipeline (`discover -> stage -> dbt_build -> publish`). Live-cluster proof (a real `dbt_build` pod running under the `dbt` ServiceAccount, resolving `etl/dbt-db` and only that credential) is deferred to this phase's verification/live-proof step, consistent with prior phases' pattern of structural-first, live-second validation. The `tests/dagtest/` and `tests/e2e/observability/` fixes in this plan are mechanical and unverified against a live Docker daemon/cluster in this session -- worth a quick live confirmation during phase verification.

---
*Phase: 08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br*
*Plan: 12*
*Completed: 2026-08-18*

## Self-Check: PASSED

All created/modified files verified present on disk; all 4 commits
(`ebd6740`, `e8429f5`, `0ee59ff`, `180f41e`) verified present in `git log`.
