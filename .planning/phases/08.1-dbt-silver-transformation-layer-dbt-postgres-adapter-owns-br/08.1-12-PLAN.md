---
phase: 08.1
plan: 12
type: execute
wave: 6
depends_on: ["08.1-11", "08.1-02", "08.1-08"]
files_modified:
  - airflow/dags/_common/kpo.py
  - airflow/dags/csv_ingest_customers.py
  - airflow/dags/csv_ingest_orders.py
  - tests/unit/test_dag_structure.py
  - tests/policy/test_dag_line_budget.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02]
must_haves:
  truths:
    - "Both DAGs' task graphs are wait_for_files -> matched_keys -> integrity_gate -> discover -> stage (mapped) -> dbt_build (single) -> publish (single), and publish -- not stage -- carries the outlets that make csv_ingest_orders schedulable, since orders depends on customers' GOLD state, not its bronze state."
    - "dbt_build runs as its own ServiceAccount (dbt) and Vault role (dbt), never csv-processor's -- proven structurally by DagBag introspection, no live cluster needed."
    - "Both DAG files stay under ORCH-06's 150-line budget after the restructuring."
  artifacts:
    - path: "airflow/dags/csv_ingest_customers.py"
      provides: "stage -> dbt_build -> publish task chain replacing the single ingest task"
      contains: "dbt_build"
    - path: "airflow/dags/_common/kpo.py"
      provides: "common_kpo_kwargs() generalized with service_account_name/image_variable/vault_k8s_role/include_dataplat_credentials parameters, backward-compatible defaults"
      contains: "service_account_name: str = \"csv-processor\""
  key_links:
    - from: "airflow/dags/csv_ingest_customers.py::dbt_build"
      to: "kubernetes/rbac-etl.yaml"
      via: "service_account_name=\"dbt\" (plan 08.1-03's ServiceAccount)"
      pattern: "service_account_name=\"dbt\""
    - from: "airflow/dags/csv_ingest_customers.py::publish"
      to: "packages/csv-processor/src/csv_processor/cli.py::publish"
      via: "cmds=[\"dataplat\"], arguments=[\"publish\", \"--dataset\", \"customers\"]"
      pattern: "\"publish\", \"--dataset\""
---

<objective>
Wire the phase's Airflow-side finish line: insert a `dbt_build` `KubernetesPodOperator` task
between the existing `discover` and the now-split `stage`/`publish` tasks, in both DAG files
(D-03/D-04), reusing `common_kpo_kwargs()`/existing KPO wiring patterns wherever they already fit
and generalizing `common_kpo_kwargs()` itself where they do not.

**A correction to RESEARCH.md's framing, made concrete here:** RESEARCH.md describes reusing
`common_kpo_kwargs()` "with zero new plumbing." That is true for `stage` (unchanged shape from
today's `ingest`), but a *literal*, unmodified reuse for `dbt_build` would be a real bug: `dbt_build`
needs its OWN `service_account_name="dbt"` (plan 08.1-03), its OWN image Variable (`dbt_image`,
plan 08.1-02), and its OWN Vault role (`dbt`, so `resolve_secrets.py` authenticates as the right
identity) — none of which `common_kpo_kwargs()`'s current hardcoded `"csv-processor"`/
`"csv_processor_image"` values provide. This plan generalizes `common_kpo_kwargs()` with optional,
backward-compatible parameters (existing `discover`/`stage` call sites need zero changes) rather
than writing a second, divergent KPO-kwargs helper.

**publish, not stage, owns `outlets=[customers_asset]`**, correcting today's assignment (`ingest`
held it): the Asset URI `"s3://normalized/customers"` names GOLD's own state. `csv_ingest_orders`
must wait for customers' actual gold publish to commit, not merely for customers' bronze staging
to land — staging alone tells `orders` nothing about whether a customer referenced by a new order
actually exists in `normalized.customers` yet.

Purpose: this is where the whole phase's Python/dbt/metadata machinery becomes a real, scheduled
pipeline.
Output: `kpo.py` generalized, both DAG files restructured (still under 150 lines each), and
updated structural tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-CONTEXT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-RESEARCH.md

<interfaces>
<!-- airflow/dags/_common/kpo.py -- READ THE WHOLE CURRENT FILE FIRST. Full current signature
and body (lines 50-123) to generalize, not replace. -->
def common_kpo_kwargs(*, resources: k8s.V1ResourceRequirements, extra_env_vars=None) -> dict[str, object]:
    return {"namespace": "etl", "service_account_name": "csv-processor",
        "image": Variable.get("csv_processor_image"), "do_xcom_push": True,
        "on_finish_action": "delete_pod", "get_logs": True, "container_resources": resources,
        "env_vars": [DATAPLAT_DB_DSN, DATAPLAT_S3_ACCESS_KEY, DATAPLAT_S3_SECRET_KEY,
            DATAPLAT_S3_ENDPOINT_URL, VAULT_ADDR, VAULT_K8S_ROLE, OTEL_EXPORTER_OTLP_ENDPOINT,
            *(extra_env_vars or [])]}

<!-- airflow/dags/csv_ingest_customers.py -- READ THE WHOLE CURRENT FILE FIRST (149 lines).
Current shape (lines 96-149) this plan restructures: discover (KPO, unchanged) -> ingest
(TracingKPO.partial().expand(), outlets=[customers_asset]) -> aggregate_receipts(ingest.output).
This plan's new shape: discover (unchanged) -> stage (TracingKPO.partial().expand(), the trace
root and per-file granularity ingest used to have) -> dbt_build (plain KPO, single, no
cmds/arguments override -- the image's own ENTRYPOINT already runs `dbt build --project-dir
/app/dbt --profiles-dir /app/dbt`) -> publish (plain KPO, single, outlets=[customers_asset]) ->
aggregate_receipts(stage.output). -->
_DISCOVER_RESOURCES = k8s.V1ResourceRequirements(requests={"cpu": "100m", "memory": "128Mi"}, limits={"cpu": "500m", "memory": "256Mi"})
_INGEST_RESOURCES = k8s.V1ResourceRequirements(requests={"cpu": "500m", "memory": "1Gi"}, limits={"cpu": "2", "memory": "4Gi"})
_INGEST_EXTRA_ENV_VARS = [k8s.V1EnvVar(name="DATAPLAT_HEARTBEAT_INTERVAL_SECONDS", value="2")]
customers_asset = Asset("s3://normalized/customers")

@task
def build_ingest_args(discovered: dict) -> list[list[str]]:
    return [["ingest", "--assignment", unit["assignment_uri"]] for unit in discovered["units"]]

ingest = TracingKubernetesPodOperator.partial(task_id="ingest", cmds=["dataplat"], retries=3,
    retry_exponential_backoff=True, max_active_tis_per_dag=1, outlets=[customers_asset],
    **common_kpo_kwargs(resources=_INGEST_RESOURCES, extra_env_vars=_INGEST_EXTRA_ENV_VARS),
).expand(arguments=build_ingest_args(discover.output))
aggregate_receipts(ingest.output)

<!-- tests/unit/test_dag_structure.py -- READ THE WHOLE FILE FIRST. Two tests this plan MUST
update or they will fail: test_namespace_and_service_account (lines 95-107, currently asserts
EVERY KPO-shaped task uses service_account_name == "csv-processor" -- must exempt/branch for
dbt_build's "dbt") and test_customers_ingest_declares_outlets (lines 164-174, references
dag.task_dict["ingest"] -- rename to "publish"). -->

<!-- tests/policy/test_dag_line_budget.py -- READ THE WHOLE FILE FIRST.
test_csv_ingest_customers_stays_under_150_lines asserts line_count < 150 -- a HARD gate this
plan's restructuring must keep passing. Check whether an equivalent csv_ingest_orders assertion
already exists; add one (mirroring the customers test exactly, dataset-substituted) if not. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize common_kpo_kwargs() for the dbt pod's different identity</name>
  <files>airflow/dags/_common/kpo.py</files>
  <read_first>
    airflow/dags/_common/kpo.py
    tests/unit/test_tracing_kpo.py
  </read_first>
  <action>
    Extend `common_kpo_kwargs`'s signature with four new keyword-only parameters, ALL defaulted
    to today's exact existing values so `discover`/`stage`'s call sites need zero changes:
    `service_account_name: str = "csv-processor"`, `image_variable: str = "csv_processor_image"`,
    `vault_k8s_role: str = "csv-processor"`, `include_dataplat_credentials: bool = True`. Replace
    the function body's hardcoded `"service_account_name": "csv-processor"` with
    `"service_account_name": service_account_name`, `Variable.get("csv_processor_image")` with
    `Variable.get(image_variable)`, and the `VAULT_K8S_ROLE` env var's hardcoded value
    (`_VAULT_K8S_ROLE = "csv-processor"` module constant) with `vault_k8s_role` directly (the
    module constant can stay as the DEFAULT value fed into the new parameter's own default, or be
    removed if no longer referenced elsewhere in the file — check). Wrap the four
    `DATAPLAT_DB_DSN`/`DATAPLAT_S3_ACCESS_KEY`/`DATAPLAT_S3_SECRET_KEY`/`DATAPLAT_S3_ENDPOINT_URL`
    env vars in an `if include_dataplat_credentials:` conditional inclusion (a plain Python `if`
    building a local list before the final `env_vars` list is assembled — `VAULT_ADDR`/the new
    `vault_k8s_role`-driven `VAULT_K8S_ROLE` var, and `OTEL_EXPORTER_OTLP_ENDPOINT` stay
    UNCONDITIONAL, every pod this function serves needs Vault + tracing regardless of which
    credential set it needs). Update the function's own docstring `Args:` section to document
    all four new parameters, explaining `dbt_build`'s own use case as the reason they exist (cite
    plan 08.1-12 by number). Do not change `discover`'s or `stage`'s own call sites in either DAG
    file in this task — that is Task 2/3's job.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('airflow/dags/_common/kpo.py').read())" && grep -q 'service_account_name: str = "csv-processor"' airflow/dags/_common/kpo.py && grep -q "include_dataplat_credentials: bool = True" airflow/dags/_common/kpo.py</automated>
  </verify>
  <acceptance_criteria>
    - `common_kpo_kwargs`'s signature contains all four new parameters with the exact default values specified above
    - Calling `common_kpo_kwargs(resources=X)` with no other arguments returns a dict byte-for-byte identical to today's pre-this-plan output (regression proof — verify by reading the existing call sites in both DAG files after this task, confirming neither needs edits yet)
    - `common_kpo_kwargs(resources=X, include_dataplat_credentials=False)`'s returned `env_vars` list contains no `DATAPLAT_DB_DSN`/`DATAPLAT_S3_*` entries
    - `python -c "import ast; ast.parse(open('airflow/dags/_common/kpo.py').read())"` succeeds
  </acceptance_criteria>
  <done>`common_kpo_kwargs`'s signature carries all four new keyword-only parameters with backward-compatible defaults, and `common_kpo_kwargs(resources=X, include_dataplat_credentials=False)` omits every `DATAPLAT_*` credential env var</done>

</task>

<task type="auto">
  <name>Task 2: Restructure csv_ingest_customers.py (stage -> dbt_build -> publish)</name>
  <files>airflow/dags/csv_ingest_customers.py</files>
  <read_first>
    airflow/dags/csv_ingest_customers.py
    airflow/dags/_common/kpo.py
  </read_first>
  <action>
    Rename `_INGEST_RESOURCES` to `_STAGE_RESOURCES` (same values, unchanged — staging still does
    the heavy chunked-COPY work `ingest` used to). Do NOT add new resource constants for
    `dbt_build`/`publish` — reuse `_DISCOVER_RESOURCES` for both (RESEARCH.md's own Assumption
    A3: start at `discover`'s lighter tier; this also keeps the file's line budget under
    control). Rename `build_ingest_args` to `build_stage_args`, changing its returned argv from
    `["ingest", "--assignment", ...]` to `["stage", "--assignment", ...]` — no other change.
    Replace the `ingest = TracingKubernetesPodOperator.partial(...)` block with THREE task
    constructions: `stage = TracingKubernetesPodOperator.partial(task_id="stage",
    cmds=["dataplat"], retries=3, retry_exponential_backoff=True, max_active_tis_per_dag=1,
    **common_kpo_kwargs(resources=_STAGE_RESOURCES, extra_env_vars=_INGEST_EXTRA_ENV_VARS),
    ).expand(arguments=build_stage_args(discover.output))` (stage keeps `ingest`'s OLD trace-root
    role — this is the mapped, per-file task, one trace per `meta.ingestion_runs` row, `outlets`
    REMOVED from here); `dbt_build = KubernetesPodOperator(task_id="dbt_build", retries=2,
    retry_exponential_backoff=True, max_active_tis_per_dag=1, **common_kpo_kwargs(
    resources=_DISCOVER_RESOURCES, service_account_name="dbt", image_variable="dbt_image",
    vault_k8s_role="dbt", include_dataplat_credentials=False))` — deliberately NO `cmds`/
    `arguments` (the dbt image's own `ENTRYPOINT` already resolves secrets and runs `dbt build
    --project-dir /app/dbt --profiles-dir /app/dbt`; overriding `cmds` here would bypass that
    entrypoint entirely — add a one-line comment saying exactly this, since it is the one KPO
    task in either DAG file that looks incomplete without it); `publish =
    KubernetesPodOperator(task_id="publish", cmds=["dataplat"], arguments=["publish", "--dataset",
    "customers"], retries=3, retry_exponential_backoff=True, outlets=[customers_asset],
    **common_kpo_kwargs(resources=_DISCOVER_RESOURCES))` (plain KPO — `publish` covers possibly
    several runs per invocation, so it is not this DAG's per-file trace root; `outlets` moves
    HERE, per this plan's Objective reasoning). Wire `stage >> dbt_build >> publish` explicitly
    (a single `>>` chain line — `dbt_build` takes no `.expand()`/XCom-derived arguments from
    `stage`, it is triggered purely by task-graph position). Replace
    `aggregate_receipts(ingest.output)` with `aggregate_receipts(stage.output)`. Add ONE comment
    line near `dbt_build`'s construction (or in the module docstring) stating: "`dbt build` must
    only ever run via this KPO task — never invoked manually against a live cluster's `silver`
    schema" (RESEARCH.md Security Domain finding, T-08.1-19-class mitigation — a runbook-level
    note, not enforceable in code). Keep the file under 150 lines: reuse `_DISCOVER_RESOURCES`
    as instructed above, do not add a fourth resource constant, and trim only NON-load-bearing
    comment density if still over budget after the above (never trim the module docstring's own
    D-01..D-18/README-citation content, never trim a comment explaining a genuine correction or
    non-obvious decision — those are exactly what a future reader needs).
  </action>
  <verify>
    <automated>pytest tests/policy/test_dag_line_budget.py -k customers -q</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l airflow/dags/csv_ingest_customers.py` reports fewer than 150 lines
    - File contains `dbt_build = KubernetesPodOperator(` with no `cmds=` or `arguments=` keyword on that same construction
    - File contains `service_account_name="dbt"` and `vault_k8s_role="dbt"` in the `dbt_build` construction's `common_kpo_kwargs(...)` call
    - `publish`'s construction contains `outlets=[customers_asset]`; `stage`'s construction does not
    - File contains the exact chain `stage >> dbt_build >> publish` (or an equivalent multi-line form expressing the same three-task linear dependency)
    - `python -c "import ast; ast.parse(open('airflow/dags/csv_ingest_customers.py').read())"` succeeds
  </acceptance_criteria>
  <done>`airflow/dags/csv_ingest_customers.py` stays under 150 lines, chains `stage >> dbt_build >> publish` with `dbt_build` running under its own `dbt` ServiceAccount/Vault role, and `publish` (not `stage`) owns `outlets=[customers_asset]`</done>

</task>

<task type="auto">
  <name>Task 3: Mirror csv_ingest_orders.py; update DagBag structural tests</name>
  <files>airflow/dags/csv_ingest_orders.py, tests/unit/test_dag_structure.py, tests/policy/test_dag_line_budget.py</files>
  <read_first>
    airflow/dags/csv_ingest_orders.py
    tests/unit/test_dag_structure.py
    tests/policy/test_dag_line_budget.py
  </read_first>
  <action>
    Apply Task 2's identical restructuring to `airflow/dags/csv_ingest_orders.py`, substituting
    `orders` for `customers` throughout (dataset name, `arguments=["publish", "--dataset",
    "orders"]`, etc.) — `dbt_build`'s own construction stays IDENTICAL between the two files
    (`service_account_name="dbt"`, `vault_k8s_role="dbt"`, `image_variable="dbt_image"` — dbt's
    project is dataset-agnostic scaffolding; an unscoped `dbt build` in either DAG's `dbt_build`
    task builds BOTH `silver_customers` and `silver_orders`, and re-running it from the OTHER
    DAG moments later is a safe, idempotent no-op for whichever model already has nothing new to
    process — document this explicitly as the deliberate, simple design choice, not an
    oversight). `orders` has no `customers_asset`-equivalent outlet of its own to declare in this
    phase — leave its `publish` task's `outlets` exactly as today's `ingest` task's were (`orders`
    dataset has no downstream DAG depending on it yet). In `tests/unit/test_dag_structure.py`:
    update `test_namespace_and_service_account` to branch on `task.task_id == "dbt_build"` —
    assert `attrs.get("service_account_name") == "dbt"` for that task, `== "csv-processor"` for
    every other KPO-shaped task — and bump the `checked >= 3` threshold to reflect the real new
    count (`discover`, `stage`, `dbt_build`, `publish` in each DAG, plus the smoke pod). Rename
    `test_customers_ingest_declares_outlets` to `test_customers_publish_declares_outlets`,
    changing `dag.task_dict["ingest"]` to `dag.task_dict["publish"]`. Add a new test,
    `test_dbt_build_runs_between_stage_and_publish`, asserting (for both DAG ids)
    `"stage" in dag.task_dict["dbt_build"].upstream_task_ids` and `"dbt_build" in
    dag.task_dict["publish"].upstream_task_ids`. In `tests/policy/test_dag_line_budget.py`: if no
    `csv_ingest_orders`-scoped line-budget test exists yet, add
    `test_csv_ingest_orders_stays_under_150_lines` mirroring the customers test exactly
    (dataset-substituted); if one already exists, leave it untouched (it already covers this
    plan's changes structurally).
  </action>
  <verify>
    <automated>pytest tests/unit/test_dag_structure.py tests/policy/test_dag_line_budget.py tests/policy/test_dag_thinness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l airflow/dags/csv_ingest_orders.py` reports fewer than 150 lines
    - `airflow/dags/csv_ingest_orders.py` and `csv_ingest_customers.py` differ ONLY in dataset-name-substituted literals and the `orders` DAG's `schedule=[customers_asset]`/lack of its own outlet (confirm via `diff <(sed 's/orders/customers/g; s/Orders/Customers/g' airflow/dags/csv_ingest_orders.py) airflow/dags/csv_ingest_customers.py` showing no unexpected structural divergence)
    - `pytest tests/unit/test_dag_structure.py -q` exits 0
    - `pytest tests/policy/test_dag_line_budget.py -q` exits 0
    - `pytest tests/policy/test_dag_thinness.py -q` exits 0 (regression proof — this plan's DAG changes introduce no new business-logic import or raw SQL string)
  </acceptance_criteria>
  <done>`pytest tests/unit/test_dag_structure.py tests/policy/test_dag_line_budget.py tests/policy/test_dag_thinness.py -q` exits 0, proving both DAGs correctly chain `discover -> stage -> dbt_build -> publish` under 150 lines each</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dbt_build`'s ServiceAccount/Vault role | Must resolve `etl/dbt-db` only, never `etl/analytics-db`/`etl/minio` (the `csv-processor` credential set) |
| DAG-authored task graph -> live pod launch | The only sanctioned way `dbt build` ever runs against this platform's `silver` schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-28 | Elevation of Privilege | `dbt_build`'s `common_kpo_kwargs(..., include_dataplat_credentials=False)` | mitigate | `dbt_build`'s pod never receives `DATAPLAT_DB_DSN`/`DATAPLAT_S3_*` env vars at all — it cannot even attempt to resolve the `csv-processor` credential set, let alone succeed, since its own ServiceAccount (`dbt`) has no Vault policy granting `etl/data/analytics-db`/`etl/data/minio` (plan 08.1-03) |
| T-08.1-29 | Tampering | Manual `dbt build` invocation against the live cluster, outside this DAG | accept | Not enforceable in code (RESEARCH.md's own Known Threat Patterns table calls this out explicitly) — mitigated by an explicit runbook-level comment in the DAG file (Task 2's own acceptance criterion) and by `dbt_app`'s narrow grants (plan 08.1-01) bounding the blast radius of any such invocation regardless |
| T-08.1-30 | Denial of Service | `dbt_build` retried indefinitely if it never succeeds | mitigate | `retries=2` + `retry_exponential_backoff=True`, matching every other KPO task in both DAGs — bounded retry, never infinite |
</threat_model>

<verification>
`pytest tests/unit/test_dag_structure.py tests/policy/test_dag_line_budget.py tests/policy/test_dag_thinness.py -q` passes. `wc -l airflow/dags/csv_ingest_customers.py airflow/dags/csv_ingest_orders.py` both report fewer than 150.
</verification>

<success_criteria>
Both DAGs' task graphs correctly sequence `discover -> stage -> dbt_build -> publish`, `dbt_build`
runs under its own least-privilege identity, `publish` correctly owns the customers Asset outlet,
and every structural test proves this without a live cluster.
</success_criteria>

<output>
Create `.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-12-SUMMARY.md` when done
</output>
