---
phase: 08.1
plan: 11
type: execute
wave: 5
depends_on: ["08.1-10"]
files_modified:
  - packages/csv-processor/src/csv_processor/cli.py
  - tests/unit/test_csv_processor_cli.py
autonomous: true
requirements: [DEDUP-02]
must_haves:
  truths:
    - "`dataplat stage --assignment <uri>` and `dataplat publish --dataset <name>` exist as real CLI subcommands; `dataplat ingest` no longer exists."
    - "Both commands write a Receipt/XCom payload on every exit path, success or failure -- the same catch-once discipline `ingest` already established, never silently swallowing an exception."
  artifacts:
    - path: "packages/csv-processor/src/csv_processor/cli.py"
      provides: "stage and publish click commands replacing ingest"
      contains: "def stage("
  key_links:
    - from: "packages/csv-processor/src/csv_processor/cli.py::stage"
      to: "packages/dataplat/src/dataplat/pipeline/run.py::stage_ingest"
      via: "direct call, same PipelineContext-construction prologue as today's ingest"
      pattern: "stage_ingest\\(ctx"
    - from: "packages/csv-processor/src/csv_processor/cli.py::publish"
      to: "packages/dataplat/src/dataplat/pipeline/run.py::publish_ingest"
      via: "direct call, no assignment document needed -- publish discovers its own work"
      pattern: "publish_ingest\\(ctx\\)"
---

<objective>
Expose `stage_ingest`/`publish_ingest` (plan 08.1-10) as two new `dataplat` CLI subcommands,
replacing today's single `ingest` command (D-04). `stage` keeps `ingest`'s exact prologue
(assignment-document fetch/validate, `CsvSource` construction — this module remains the one
place `dataplat` and `csv_processor` are allowed to meet, per this file's own module docstring).
`publish` is new and lighter: no assignment document, no `CsvSource` — it takes only `--dataset`
and lets `publish_ingest` discover which runs are ready to finalize.

Purpose: this is the CLI surface plan 08.1-12 (DAG wiring) invokes from the two
`KubernetesPodOperator` tasks that replace today's single `ingest` task.
Output: `csv_processor/cli.py` with `stage`/`publish` replacing `ingest`, plus updated/new unit
tests in the existing `tests/unit/test_csv_processor_cli.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-CONTEXT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-RESEARCH.md

<interfaces>
<!-- packages/csv-processor/src/csv_processor/cli.py -- READ THE WHOLE CURRENT FILE FIRST.
`ingest`'s full body (lines 250-370) is the exact template for `stage`'s prologue; `discover`'s
full body (lines 153-217) is the exact template for `publish`'s (no assignment document, no
CsvSource, same _build_common()/pool-close discipline). `_failure_receipt`/`_write_xcom` are
REUSED UNCHANGED by both new commands. -->
@cli.command()
@click.option("--assignment", required=True, help="The s3://bucket/key URI of the assignment JSON.")
def ingest(assignment: str) -> None:
    pool, doc = None, None
    try:
        pool, metadata, objects = _build_common()
        bucket, key = _parse_s3_uri(assignment)
        # ... fetch + validate AssignmentDocument, resolve config, build RunContext + PipelineContext(source=CsvSource(...))
        receipt = run_ingest(ctx, heartbeat_interval_seconds=...)
        _write_xcom(receipt)
    except DataPlatformError:
        _write_xcom(_failure_receipt(doc)); raise
    except Exception:
        _write_xcom(_failure_receipt(doc)); raise
    finally:
        if pool is not None: pool.close()

@cli.command()
@click.option("--dataset", required=True, help="The dataset name, e.g. 'customers'.")
def discover(dataset: str) -> None:
    pool = None
    try:
        pool, metadata, objects = _build_common()
        config = load_config(Path(f"configs/datasets/{dataset}.yaml"), defaults_path=Path("configs/defaults.yaml"))
        registry = ConfigRegistry(pool)
        record = registry.sync(dataset, config)
        # ... discover_files(...), _write_xcom({"status": "SUCCEEDED", ...})
    except DataPlatformError:
        _write_xcom({"status": "FAILED", ...}); raise
    finally:
        if pool is not None: pool.close()

<!-- packages/dataplat/src/dataplat/pipeline/run.py (plan 08.1-10's new signatures) -->
def stage_ingest(ctx: PipelineContext, *, heartbeat_interval_seconds: float = ...) -> Receipt: ...
def publish_ingest(ctx: PipelineContext) -> dict[str, object]: ...

<!-- packages/dataplat/src/dataplat/models/identity.py -- RunContext, unchanged shape -->
RunContext(run_id: int, idempotency_key: str, attempt: int = 1, dag_id=None, dag_run_id=None,
    task_id=None, trace_id=None, span_id=None, file_id=None, batch_id=None, map_index=None,
    k8s_namespace=None)

<!-- tests/unit/test_csv_processor_cli.py -- THE EXISTING TEST FILE this plan extends/renames
tests inside, lines 1-364 READ IN FULL FIRST. Key facts:
  - `dataplat.cli.main(["ingest", "--assignment", uri])` is the invocation pattern -- NOT
    click.testing.CliRunner. `main()` returns an exit code; a raised, non-caught exception (e.g.
    the WR-01 RuntimeError test) still propagates THROUGH main() and must be caught with
    `pytest.raises(...)` around the `main([...])` call itself.
  - Existing fakes to REUSE, never re-invent: `_FakeAssignmentStream`, `_FakeIngestObjectStore`,
    `_FakeIngestMetadata` (only `get_or_create_dataset`), `_FakeIngestPool`, `_FakeConfigRegistry`
    (only `get_by_id` -- extend with a `sync(dataset_name, config) -> ConfigVersionRecord`-shaped
    method for `publish`'s own `ConfigRegistry(pool).sync(...)` call, or add a sibling
    `_FakePublishConfigRegistry`), `_fake_build_common()`.
  - `_make_fake_run_ingest(captured)` builds a monkeypatch target capturing `ctx` and returning a
    `Receipt` -- rename this helper (and every `test_ingest_*` function using it) to reference
    `stage_ingest` instead of `run_ingest`, since `run_ingest` no longer exists after plan 08.1-10.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ingest with stage and publish commands</name>
  <files>packages/csv-processor/src/csv_processor/cli.py</files>
  <read_first>
    packages/csv-processor/src/csv_processor/cli.py
    packages/dataplat/src/dataplat/pipeline/run.py
  </read_first>
  <action>
    Delete the `ingest` command entirely (its function body, decorator, and `_failure_receipt`
    stays — it is reused by both new commands). Add `@cli.command() @click.option("--assignment",
    required=True, ...) def stage(assignment: str) -> None:` — copy `ingest`'s ENTIRE prologue
    verbatim (pool open, assignment fetch/parse/validate, `ConfigRegistry.get_by_id`,
    multipart-parts resolution, `get_or_create_dataset`, `RunContext` construction from
    `AIRFLOW_CTX_*` env vars, `PipelineContext` construction with `source=CsvSource(...)`) up to
    and including the `heartbeat_interval_seconds` resolution; replace the final
    `receipt = run_ingest(ctx, heartbeat_interval_seconds=...)` line with `receipt =
    stage_ingest(ctx, heartbeat_interval_seconds=heartbeat_interval_seconds)`; keep the
    `_write_xcom(receipt)`/`except DataPlatformError`/`except Exception`/`finally` block
    byte-for-byte identical to `ingest`'s own (reusing `_failure_receipt(doc)`). Add
    `@cli.command() @click.option("--dataset", required=True, help="The dataset name, e.g.
    'customers'.") def publish(dataset: str) -> None:` — a NEW, lighter prologue mirroring
    `discover`'s shape (pool open via `_build_common()`, `load_config` +
    `ConfigRegistry(pool).sync(dataset, config)` to obtain a real, current `DatasetConfig` —
    `publish_ingest` reads `ctx.config.dataset`/`ctx.config.load.strategy` only, never a
    `config_version_id`-pinned historical config, since `MergePublisher`/`OrdersMergePublisher`
    hardcode their own target table and do not vary their SQL by config version). Build a
    `RunContext` with `run_id=0` (a sentinel — `publish_ingest` never claims a `meta.ingestion_runs`
    row of its own identity; document this explicitly in a code comment: "0 is never a real
    claimed run_id — this RunContext exists for tracing/log correlation only"),
    `idempotency_key=f"publish:{dataset}"`, `attempt=int(os.environ.get("AIRFLOW_TASK_TRY_NUMBER",
    "1"))`, and the SAME `AIRFLOW_CTX_*`-sourced `dag_id`/`dag_run_id`/`task_id`/`map_index`/
    `k8s_namespace` fields `ingest`/`stage` already populate (read the identical env vars, for
    tracing parity — whichever task plan 08.1-12 wires `TracingKubernetesPodOperator` onto is
    that plan's decision, not this one's). Build `PipelineContext(run=run, config=config,
    metadata=metadata, objects=objects, db=pool, log=get_logger())` — `source` stays its default
    `None` (`publish_ingest` never opens a `CsvSource`). Call `result = publish_ingest(ctx)`;
    `_write_xcom(result)` (a plain dict — `_write_xcom` already accepts `dict[str, object] |
    Receipt`, no change needed there). Wrap in the SAME `except DataPlatformError` / `except
    Exception` / `finally: pool.close()` shape as `discover`, writing
    `{"status": "FAILED", "error_type": ..., "error_message": ...}` on failure (mirror
    `discover`'s own failure-payload dict shape exactly, not `ingest`'s `Receipt`-shaped one,
    since `publish` has no single `run_id` to attribute a `Receipt` to on failure either).
  </action>
  <acceptance_criteria>
    - `grep -n "^def ingest" packages/csv-processor/src/csv_processor/cli.py` returns no match
    - `packages/csv-processor/src/csv_processor/cli.py` defines `def stage(assignment: str)` and `def publish(dataset: str)`
    - `stage`'s body contains `stage_ingest(ctx` and `publish`'s body contains `publish_ingest(ctx)`
    - `publish`'s body contains no `AssignmentDocument` and no `CsvSource(`
    - `python -c "from csv_processor.cli import stage, publish"` succeeds with no import error
    - `mypy packages/csv-processor/` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt existing ingest tests to stage; add publish tests (offline, main()-based)</name>
  <files>tests/unit/test_csv_processor_cli.py</files>
  <behavior>
    - Test (renamed from existing): `test_stage_writes_a_failed_receipt_for_a_non_dataplatformerror_exception` — `main(["stage", "--assignment", uri])` with `_build_common` monkeypatched to raise a plain `RuntimeError` still writes a `status="FAILED"` `Receipt` (`run_id == -1`) before the exception propagates — the exact WR-01 regression this file already proves, now for `stage`.
    - Test (renamed from existing): `test_stage_dataplatformerror_path_is_unaffected_by_the_new_except_clause` — same shape, `ConfigurationError`.
    - Test (renamed from existing, ×2): `test_stage_populates_run_context_dag_fields_from_airflow_ctx_env_vars` / `test_stage_leaves_map_index_none_when_airflow_ctx_map_index_is_unset` — identical bodies, `run_ingest` monkeypatch target renamed to `stage_ingest`.
    - Test (new): `test_publish_writes_the_publish_ingest_result_as_xcom` — `main(["publish", "--dataset", "customers"])` with `_build_common`/`load_config`/`ConfigRegistry` mocked and `publish_ingest` monkeypatched to return a fixed dict (e.g. `{"status": "SUCCEEDED", "runs_finalized": [7, 8], "rows_loaded": 12, "duration_ms": 5}`) — assert the XCom file contains exactly that JSON.
    - Test (new): `test_publish_writes_a_failed_payload_for_a_dataplatformerror` — `publish_ingest` monkeypatched to raise a `ConfigurationError`; assert the XCom payload is `{"status": "FAILED", "error_type": "ConfigurationError", "error_message": ...}` and the exception still propagates through `main()`.
    - Test (new): `test_publish_never_constructs_a_csv_source` — inspect the `ctx` captured by the monkeypatched `publish_ingest` (mirror `_make_fake_run_ingest`'s own `captured` dict pattern) and assert `ctx.source is None`.
  </behavior>
  <read_first>
    tests/unit/test_csv_processor_cli.py
    packages/csv-processor/src/csv_processor/cli.py
  </read_first>
  <action>
    Rename every `test_ingest_*` function in this file to `test_stage_*`, and every
    `main(["ingest", ...])` call to `main(["stage", ...])`. Rename `_make_fake_run_ingest` to
    `_make_fake_stage_ingest` and every `monkeypatch.setattr(csv_processor_cli, "run_ingest",
    ...)` call to `monkeypatch.setattr(csv_processor_cli, "stage_ingest", ...)` — `stage_ingest`
    is the real name now imported into `csv_processor.cli`'s module namespace (plan 08.1-10/
    Task 1 of this plan already wires that import). Add a `_FakePublishConfigRegistry` class
    (sibling to `_FakeConfigRegistry`, constructed the same `__init__(self, pool)` way) with a
    `sync(self, dataset_name: str, config: DatasetConfig) -> ConfigVersionRecord` method
    returning a minimal fake `ConfigVersionRecord` (check `dataplat.config.registry`'s actual
    dataclass shape — `config_version_id`/`config_hash` fields, per `discover`'s own usage).
    Add a `_make_fake_publish_ingest(captured, result)` helper mirroring
    `_make_fake_stage_ingest`'s own capture-and-return shape, but matching `publish_ingest`'s
    real signature (`ctx: PipelineContext) -> dict[str, object]`, no `heartbeat_interval_seconds`
    keyword). Add the five new/renamed test functions described in `<behavior>` above, reusing
    `_fake_build_common`/`_FakeIngestPool`/`_FakeIngestObjectStore`/`_FakeIngestMetadata` where
    they fit `publish`'s own prologue shape (note `publish` never calls `objects.get_object()`,
    so `_FakeIngestObjectStore`'s existing single method is unused but harmless to reuse as-is).
  </action>
  <acceptance_criteria>
    - `grep -c "def test_ingest_" tests/unit/test_csv_processor_cli.py` returns 0
    - `grep -c "def test_stage_" tests/unit/test_csv_processor_cli.py` returns 4 (the four renamed tests)
    - `grep -c "def test_publish_" tests/unit/test_csv_processor_cli.py` returns 3 or more
    - `pytest tests/unit/test_csv_processor_cli.py -q` exits 0, fully offline (no `-m integration`/`-m cluster` marker needed)
    - None of the tests in this file import or construct a real `psycopg`/`boto3`/testcontainers object
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `publish --dataset` CLI argument | Operator/Airflow-supplied dataset name, resolved through the same `load_config(Path(f"configs/datasets/{dataset}.yaml"))` path traversal `discover` already uses — no new trust boundary, same existing risk class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-26 | Tampering | `--dataset` path construction (`configs/datasets/{dataset}.yaml`) | accept | Identical, already-accepted trust class as `discover`'s existing identical construction — this plan introduces no new risk; `dataset` values are DAG-authored literals (`"customers"`/`"orders"`), never end-user input |
| T-08.1-27 | Repudiation | Every CLI exit path writes a receipt/dict, success or failure | mitigate | Both `stage` and `publish` keep the `except DataPlatformError` / `except Exception` / `finally` shape `ingest`/`discover` already established — proven by Task 2's new `test_publish_writes_a_failed_payload_for_a_dataplatformerror` |
</threat_model>

<verification>
`pytest tests/unit/test_csv_processor_cli.py -q` passes. `mypy packages/csv-processor/ -q` exits 0.
</verification>

<success_criteria>
`dataplat stage --assignment <uri>` and `dataplat publish --dataset <name>` both work end-to-end
at the CLI layer, `ingest` is fully removed, and both new commands honor the existing receipt-on-
every-exit-path contract, proven by an updated, fully offline unit test suite.
</success_criteria>

<output>
Create `.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-11-SUMMARY.md` when done
</output>
