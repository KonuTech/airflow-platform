---
phase: 08.1
plan: 13
type: execute
wave: 7
depends_on: ["08.1-12", "08.1-03"]
files_modified:
  - Makefile
  - tests/e2e/vault/test_positive_auth.py
  - tests/e2e/vault/test_negative_auth.py
  - tests/e2e/slice/test_dbt_silver_pipeline.py
autonomous: false
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04, INCR-03, INCR-04, QUAL-10]
user_setup: []
must_haves:
  truths:
    - "The live analytical PostgreSQL carries all 7 new migrations (0021-0027), both the dbt image AND a freshly rebuilt csv-processor image (carrying the new stage/publish CLI commands) are built and pushed, and Vault genuinely serves the dbt ServiceAccount its own credential while denying it to every other ServiceAccount -- proven live, not just in testcontainers."
    - "A real CSV file dropped into MinIO flows, unattended, through sensor -> gate -> discover -> stage -> dbt_build -> publish and lands correctly deduplicated in both silver.customers and normalized.customers, with a real meta.dedup_audit row and a lineage view that resolves the whole chain."
    - "A backfill over the historical window re-processes every already-SUCCEEDED (old-formula) file through the new 3-stage pipeline, and silver.customers' business-key-deduplicated row count matches normalized.customers'."
  artifacts:
    - path: "Makefile"
      provides: "migrate-analytics target -- port-forward + alembic upgrade head against the live analytical cluster"
      contains: "migrate-analytics:"
  key_links:
    - from: "Makefile::migrate-analytics"
      to: "migrations/env.py"
      via: "ALEMBIC_DSN env var pointed at the port-forwarded live analytical cluster"
      pattern: "ALEMBIC_DSN"
---

<objective>
Prove the whole phase works end-to-end on the real kind cluster: apply the 7 new migrations, build
and push the dbt image, run `vault-bootstrap` live to create the `dbt` Vault identity for real,
prove its positive/negative auth, then prove two behaviors testcontainers cannot reach — a fresh
file flowing through the complete `stage -> dbt_build -> publish` chain, and D-16's backfill
re-processing historical files through the new pipeline.

Purpose: this is the phase's own final gate — every earlier plan built or tested one piece in
isolation; this plan is the only place all of them run together, for real.
Output: the live cluster carries the full new schema/image/Vault identity, a new
`make migrate-analytics` target, extended live Vault auth tests, and a new live-cluster E2E test
proving the fresh-file and backfill paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-CONTEXT.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-RESEARCH.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-VALIDATION.md

<interfaces>
<!-- migrations/env.py line 51-73 (ALEMBIC_DSN is the ONE env var the migration tooling reads --
never guess a target, this is a hard-fail-closed design already in place) -->
def _sqlalchemy_url() -> str:
    dsn = os.environ.get("ALEMBIC_DSN")
    if dsn is None: raise RuntimeError("ALEMBIC_DSN is not set...")

<!-- scripts/vault-bootstrap.py's own port-forward pattern (lines ~205-230, _port_forwarded_vault)
-- the template for this plan's own port-forward-to-analytics-db helper, whether written as a
Makefile shell recipe or a small Python contextmanager mirrored from this exact function -->

<!-- tests/e2e/vault/test_positive_auth.py line 58 / test_negative_auth.py lines 40-62 (the
EXACT SEC-12 positive/negative-proof shape to extend for the new dbt identity -- read both
files in full before extending) -->
pytestmark = pytest.mark.cluster
def test_csv_processor_reads_its_own_two_vault_paths(...): ...
def test_default_service_account_is_denied_the_csv_processor_role(...): ...

<!-- Makefile lines 171, 184 (vault-verify / cluster-verify -- the existing "-m cluster" test-
running targets this plan's own live proof mirrors, never re-implements) -->
vault-verify: ## run tests/e2e/vault against the live cluster
cluster-verify: ## run tests/e2e/cluster, tests/e2e/slice and tests/e2e/observability against the live cluster
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply live migrations, build/push dbt image, bootstrap Vault, prove dbt's identity</name>
  <files>Makefile, tests/e2e/vault/test_positive_auth.py, tests/e2e/vault/test_negative_auth.py</files>
  <read_first>
    Makefile
    migrations/env.py
    scripts/vault-bootstrap.py
    tests/e2e/vault/test_positive_auth.py
    tests/e2e/vault/test_negative_auth.py
  </read_first>
  <action>
    Add a `migrate-analytics` target to `Makefile` (and `.PHONY`): discover the CNPG-generated
    superuser (or the already-known `analytics_owner`) credential live via `kubectl get secrets -n
    data` (CNPG auto-generates a `<cluster-name>-superuser` Secret by convention — confirm the
    exact name live, do not assume), port-forward `svc/analytics-db-rw` (namespace `data`) to a
    free local port (mirror `scripts/vault-bootstrap.py`'s own `_free_local_port`/port-forward
    subprocess pattern, or a plain `kubectl port-forward ... &` + `wait`-for-ready shell idiom),
    export `ALEMBIC_DSN=postgresql://<user>:<password>@localhost:<port>/analytics`, run `uv run
    alembic upgrade head`, then tear down the port-forward. Run `make migrate-analytics` for
    real against the live cluster now — confirm via a live `psql`/`kubectl exec` query that
    `silver.customers`, `silver.orders`, `meta.dedup_audit`, `meta.run_stages` and
    `meta.v_customers_lineage`'s new columns all exist. Run `make image-dbt` for real (plan
    08.1-02's target — this registers the `dbt_image` Airflow Variable as a side effect). Run
    `make image-csv-processor` for real too — this is NOT optional: plans 08.1-06/07/10/11 all
    changed `dataplat`/`csv_processor` source (the `stage`/`publish` CLI commands,
    `promote_to_durable_bronze`, the `run_stages` claim methods, the `pipeline_version`
    idempotency-key term), none of which reach the live cluster until this image is rebuilt and
    the `csv_processor_image` Variable is re-registered — without this step, plan 08.1-12's DAG
    changes would launch pods still running the OLD `ingest`-only image and every later task in
    this plan would fail for a confusing, indirect reason. Run `make vault-bootstrap` for real
    (plan 08.1-03's extension — this creates the `dbt` Vault
    policy/role and the `etl/dbt-db` secret). Extend `tests/e2e/vault/test_positive_auth.py` with
    `test_dbt_reads_its_own_vault_path` (mirrors `test_csv_processor_reads_its_own_two_vault_paths`'s
    exact shape, targeting the `dbt` ServiceAccount and `etl/dbt-db`). Extend
    `tests/e2e/vault/test_negative_auth.py` with `test_default_service_account_is_denied_the_dbt_role`
    and `test_csv_processor_is_denied_the_dbt_role` (the `csv-processor` ServiceAccount reading
    `etl/dbt-db`, and vice versa in the sibling test — a real two-way least-privilege proof, not
    only "some other SA is denied"). Run `make vault-verify` and confirm all three new tests pass
    live.
  </action>
  <verify>
    <automated>pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -m cluster -q</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains a `migrate-analytics:` target and `.PHONY` includes it
    - `psql`/`kubectl exec` against the live analytical cluster confirms `silver.customers`, `silver.orders`, `meta.dedup_audit`, `meta.run_stages` all exist (`\dt silver.*` and `\dt meta.dedup_audit` both non-empty)
    - `kubectl get variable -n airflow ...` or `airflow variables get dbt_image` (via `kubectl exec`) returns a real `localhost:5001/dbt:<sha>` value
    - `airflow variables get csv_processor_image` (via `kubectl exec`) returns a tag built from the CURRENT git SHA (matching `git rev-parse --short HEAD` at the time this task ran) -- proving the image genuinely rebuilt, not a stale pre-phase tag
    - `pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -m cluster -q` passes live, including all three new tests
  </acceptance_criteria>
  <done>`pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -m cluster -q` passes live, proving the `dbt` ServiceAccount reads exactly `etl/dbt-db` and every other ServiceAccount is denied it</done>

</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fresh-file end-to-end proof (discover -&gt; stage -&gt; dbt_build -&gt; publish)</name>
  <action>
    Upload a small, real customers CSV to `s3://raw/customers/` on the live cluster (reuse a
    corpus fixture or synthesize one matching `customers.yaml`'s contract); poll
    `meta.ingestion_runs`/`meta.run_stages` (bounded wait, matching this project's existing E2E
    polling convention in `tests/e2e/slice/`) until the corresponding run reaches
    `status='SUCCEEDED'`; run and print the four verification queries described in
    `&lt;what-built&gt;` below.
  </action>
  <what-built>
    Automated: uploaded a small, real customers CSV to `s3://raw/customers/` on the live cluster,
    then polled `meta.ingestion_runs`/`meta.run_stages` until the corresponding run reached
    `status='SUCCEEDED'` (bounded wait, matching this project's existing E2E polling convention
    in `tests/e2e/slice/`). Then ran and recorded the output of four verification queries: (1)
    `SELECT * FROM silver.customers WHERE customer_id = '<the test row's business key>'` — one
    row, correctly typed/deduplicated; (2) `SELECT * FROM normalized.customers WHERE
    customer_id = <the same key>::int` — one row, matching silver's content; (3) `SELECT * FROM
    meta.dedup_audit WHERE model_name = 'silver_customers' ORDER BY dedup_audit_id DESC LIMIT 1`
    — a real row with plausible counts; (4) `SELECT * FROM meta.v_customers_lineage WHERE
    customer_id = <the same key>::int` — resolves the FULL chain including the new
    `silver_loaded_at`/`dbt_invocation_id`/`dbt_run_at`/`dbt_invocation_records_deduplicated`
    columns, none of them `NULL` for this row.
  </what-built>
  <how-to-verify>
    Review the four query outputs recorded above (they will be printed in this session's own
    output — no separate action needed to reproduce them unless you want to). Optionally,
    independently confirm via the Airflow UI: open the `csv_ingest_customers` DAG's most recent
    successful run and confirm its graph view shows `discover -> stage -> dbt_build -> publish`
    as four distinct, all-green task boxes (not the old single `ingest` box).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-16 backfill proof (historical files reprocessed through the new pipeline)</name>
  <action>
    Trigger `airflow dags backfill -d csv_ingest_customers --start-date &lt;earliest historical
    file's known arrival date&gt; --end-date &lt;today&gt; --reset-dagruns -y` via `kubectl exec`
    into the API server pod (mirroring the Makefile's existing `airflow variables set` invocation
    pattern); poll until the backfill's own DagRuns all reach a terminal state; run and print the
    three verification queries described in `&lt;what-built&gt;` below.
  </action>
  <what-built>
    Automated: triggered `airflow dags backfill -d csv_ingest_customers --start-date &lt;earliest
    historical file's known arrival date&gt; --end-date &lt;today&gt; --reset-dagruns -y` (via
    `kubectl exec` into the API server pod, mirroring the Makefile's existing `airflow variables
    set` invocation pattern) — proving D-18's idempotency-key extension genuinely makes every
    historical, already-`SUCCEEDED` (old-formula) file re-claimable, per this phase's own
    Pitfall-1 fix. Polled until the backfill's own DagRuns all reached a terminal state, then ran
    and recorded: (1) `SELECT count(*) FROM silver.customers` vs. `SELECT count(*) FROM
    normalized.customers` — the two counts, and an explanation of any expected difference (silver
    should equal or exceed gold's deduplicated count, never be less); (2)
    `SELECT count(*) FROM meta.dedup_audit WHERE run_at > &lt;the backfill's own start
    time&gt;` — confirms at least one dbt invocation covered the backfilled range; (3)
    `SELECT run_id, status, replay_of_run_id FROM meta.ingestion_runs WHERE replay_of_run_id IS
    NOT NULL ORDER BY run_id` — every historical file's NEW run correctly links back to its
    original `SUCCEEDED` run.
  </what-built>
  <how-to-verify>
    Review the three query outputs recorded above. Confirm the `replay_of_run_id` column
    genuinely links each new run back to a real, pre-existing `SUCCEEDED` run_id (not `NULL`,
    not a self-reference). If any historical file failed to backfill, its specific error will be
    printed — flag it rather than approving silently.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live migration application (`migrate-analytics`) | Runs with superuser/owner privilege against the live analytical cluster — the credential must never be printed, logged, or committed |
| Live backfill trigger | A real Airflow CLI invocation against the live scheduler — bounded to the two named DAGs, never a cluster-wide operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-31 | Information Disclosure | `migrate-analytics`'s discovered superuser credential | mitigate | Read live via `kubectl get secret -o jsonpath=...` into a shell variable, used only to build the port-forwarded `ALEMBIC_DSN`, never echoed to the terminal or written to a file — mirrors `_kubectl_get_secret_field`'s own established no-print discipline |
| T-08.1-32 | Elevation of Privilege | The live `dbt` Vault identity, now genuinely provisioned | mitigate | Proven, not merely coded: `test_dbt_reads_its_own_vault_path` + the two new negative tests confirm the exact least-privilege boundary this whole phase designed, against the real cluster |
| T-08.1-SC | Tampering | Live `make image-dbt` pulling `dbt-core`/`dbt-postgres` from PyPI at build time | accept | Same disposition as plan 08.1-02 — both packages already verified `[OK]`/Approved in RESEARCH.md's Package Legitimacy Audit; no new risk introduced by running the build live |
</threat_model>

<verification>
`pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -m cluster -q`
passes live. Both checkpoint tasks receive explicit human approval before this plan is considered
complete.
</verification>

<success_criteria>
The live cluster runs the complete phase end-to-end: a fresh file processes correctly through all
four tasks, historical files backfill correctly through the new pipeline with `replay_of_run_id`
lineage intact, and the `dbt` Vault identity is proven least-privilege on the real cluster.
</success_criteria>

<output>
Create `.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-13-SUMMARY.md` when done
</output>
