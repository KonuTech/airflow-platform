---
phase: 08.1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0010-dbt-silver-layer-boundary.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04, INCR-03, INCR-04, QUAL-10]
must_haves:
  truths:
    - "A reader of docs/adr/0010-dbt-silver-layer-boundary.md understands why dbt is back in scope narrowly (bronze->silver only) and why gold/SCD2 stay Python-owned, without needing to read the original exploration note."
    - "PROJECT.md's Key Decisions table and REQUIREMENTS.md's Out of Scope table no longer contradict the fact that dbt is now part of the shipped architecture."
  artifacts:
    - path: "docs/adr/0010-dbt-silver-layer-boundary.md"
      provides: "The formal, permanent architecture decision record for the dbt boundary"
      contains: "## Migration trigger"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row narrowed from blanket 'dbt excluded' to the real, narrow boundary"
      contains: "ADR-0010"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Out of Scope row narrowed to match"
      contains: "ADR-0010"
---

<objective>
Formalize the architecture decision this whole phase implements: dbt is back in scope, narrowly
(bronze→silver only), superseding — not deleting — `PROJECT.md`'s original blanket "dbt excluded"
Key Decision and `REQUIREMENTS.md`'s "dbt or an external transformation framework" Out-of-Scope
entry. This closes the folded todo from `08.1-CONTEXT.md`
(`.planning/todos/pending/draft-adr-dbt-silver-layer-boundary.md`).

Purpose: every later plan in this phase implements a decision that, absent this ADR, contradicts
two already-committed planning documents. Writing it now (wave 1, zero code dependency) means the
rest of the phase can cite `ADR-0010` from day one instead of only at the end.
Output: `docs/adr/0010-dbt-silver-layer-boundary.md`, and narrowed (not deleted) entries in
`PROJECT.md`/`REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/notes/dbt-silver-layer-architecture-decision.md
@.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-CONTEXT.md

<interfaces>
<!-- docs/adr/0000-template.md, full file — the exact skeleton every ADR in this repo follows -->
---
status: {proposed | accepted | rejected | deprecated | superseded by ADR-00NN}
date: YYYY-MM-DD
---
# ADR-00NN: {short title, a decision phrased as a claim}
## Context and Problem Statement
## Considered Options
## Decision Outcome
### Consequences
## Migration trigger
## References

<!-- .planning/PROJECT.md line 198 (the row to narrow, not delete) -->
| dbt excluded | README models transformation and SCD in Python (§36, §54–61). Introducing dbt would split the transformation story across two paradigms | — Pending |

<!-- .planning/PROJECT.md line 151 (the Out of Scope bullet to narrow) -->
- **dbt or an external transformation framework** — README §36/§54 model transformation and SCD logic in the Python layer; adding dbt would fork the transformation story. Revisit if the warehouse layer outgrows Python

<!-- .planning/REQUIREMENTS.md Out of Scope table (same narrowing) -->
| dbt or an external transformation framework | README §36/§54–61 model transformation and SCD in Python; adding dbt forks the transformation story across two paradigms |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-0010</name>
  <files>docs/adr/0010-dbt-silver-layer-boundary.md</files>
  <read_first>
    docs/adr/0000-template.md
    docs/adr/0008-pipeline-composition-seam.md
    .planning/notes/dbt-silver-layer-architecture-decision.md
  </read_first>
  <action>
    Create `docs/adr/0010-dbt-silver-layer-boundary.md` following `0000-template.md`'s exact
    section skeleton and `0008`'s own tone/depth (frontmatter `status: accepted`,
    `date: 2026-08-18`; title: "ADR-0010: dbt owns bronze-to-silver transformation only — gold
    publish and SCD2 stay Python-owned"). Context and Problem Statement: summarize
    `.planning/notes/dbt-silver-layer-architecture-decision.md`'s "What triggered this" and
    numbered reasoning (9 points) in your own words — do not re-derive the reasoning, cite and
    condense it. Considered Options: (A) keep dbt fully excluded (status quo), (B) dbt owns
    bronze→silver→gold end-to-end, (C) dbt owns bronze→silver only, gold stays Python
    `MergePublisher` (chosen). Decision Outcome: **C**, because dbt's `merge` incremental
    strategy compiles to literal PostgreSQL `MERGE` (PG BUG #18279, the same concurrency bug
    `MergePublisher` was already built to avoid) and because dbt's own per-model transactions
    cannot participate in META-03's single-transaction guarantee. Consequences: Good (dedup/
    late-arrival logic moves into a purpose-built tool instead of a hand-rolled Python stage;
    least-privilege role separation slots into the existing `etl_app`/`analytics_owner`/
    `grafana_reader` pattern); Bad (a new image, a new role, a new credential-delivery path, and
    lineage now has an explicit hop to bridge, per D-12 — name this honestly, mirroring `0008`'s
    own "Bad, because" convention rather than presenting the decision as costless); Neutral
    (Phase 10's SCD2 rejects `dbt snapshot` for the identical structural reason gold does, so
    this decision's boundary logic already generalizes). Migration trigger: name a concrete,
    observable trigger for revisiting (e.g. "if dbt-postgres ever ships a `merge` strategy proven
    safe under PostgreSQL's snapshot semantics" or "if a future phase needs dbt to own gold too")
    — never leave this section blank. References: README §4.2, §25-27, §32-36, §60-61, §68, §75,
    §81.6, §83; `.planning/research/SUMMARY.md`; `08.1-RESEARCH.md`;
    `.planning/notes/dbt-silver-layer-architecture-decision.md`.
  </action>
  <acceptance_criteria>
    - `docs/adr/0010-dbt-silver-layer-boundary.md` exists with frontmatter `status: accepted`
    - File contains `## Context and Problem Statement`, `## Considered Options`, `## Decision Outcome`, `### Consequences`, `## Migration trigger`, `## References`
    - File's `## Migration trigger` section contains at least one sentence naming a concrete, observable revisit condition (never the literal placeholder text from the template)
    - File contains `BUG #18279` and `META-03`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Narrow PROJECT.md and REQUIREMENTS.md's dbt-excluded entries</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    In `.planning/PROJECT.md`: (1) Replace the Out of Scope bullet at line 151 ("dbt or an
    external transformation framework...") with a narrowed version stating dbt now owns
    bronze-to-silver transformation as of Phase 08.1 (ADR-0010), while gold publish and SCD2
    remain explicitly Python-owned and out of dbt's scope. (2) Replace the Key Decisions table
    row "dbt excluded | ... | — Pending" (line 198) with a narrowed Decision text (e.g. "dbt
    scoped to bronze→silver only (superseded 08.1)") and Rationale referencing the MERGE-bug/
    META-03-transaction reasoning, Outcome changed to "✓ Good" with a trailing "(ADR-0010)"
    citation — do not delete the row (this project's own convention is additive/superseding, not
    erasing history, mirroring `discovery.py`'s idempotency-key-formula evolution discipline). In
    `.planning/REQUIREMENTS.md`: replace the Out of Scope table's "dbt or an external
    transformation framework" row with the same narrowed wording, citing ADR-0010, and add a
    one-line note directly beneath the Out of Scope table (or in the table's own reasoning
    column) that DEDUP-01..04/INCR-03/INCR-04/QUAL-10 are the specific requirement IDs this
    narrowing affects — cross-referencing the Traceability table's own existing "Phase 08.1"
    rows for those IDs (already correct, added when the roadmap was updated) rather than
    duplicating them.
  </action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `ADR-0010` at least twice (Out of Scope bullet + Key Decisions row)
    - `.planning/PROJECT.md`'s Key Decisions table no longer contains the literal string "dbt excluded" as an unqualified/un-superseded decision label
    - `.planning/REQUIREMENTS.md` contains `ADR-0010`
    - `.planning/REQUIREMENTS.md`'s Out of Scope table's dbt row explicitly states gold publish and SCD2 remain out of scope for dbt (the narrowing is real, not a blanket removal)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan touches only Markdown planning/documentation artifacts, no runtime code, no
credentials, no infrastructure.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-09 | Repudiation | ADR-0010's own decision record | accept | Documentation-only artifact; no runtime trust boundary exists to mitigate. The ADR's own value IS the audit trail (its "Migration trigger" section makes future reversal an explicit, observable decision rather than a silent drift) |
</threat_model>

<verification>
`grep -c "ADR-0010" .planning/PROJECT.md .planning/REQUIREMENTS.md` both return a positive count.
`ls docs/adr/0010-dbt-silver-layer-boundary.md` succeeds.
</verification>

<success_criteria>
ADR-0010 exists, follows this repo's established ADR shape, and `PROJECT.md`/`REQUIREMENTS.md`
no longer contradict the fact that dbt is now part of the shipped architecture.
</success_criteria>

<output>
Create `.planning/phases/08.1-dbt-silver-transformation-layer-dbt-postgres-adapter-owns-br/08.1-04-SUMMARY.md` when done
</output>
