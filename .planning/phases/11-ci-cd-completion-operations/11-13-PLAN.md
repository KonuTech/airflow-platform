---
phase: 11-ci-cd-completion-operations
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/airflow-unavailable.md
  - docs/runbooks/vault-unavailable.md
  - docs/runbooks/kubernetes-pod-stuck.md
  - docs/runbooks/failed-backfill.md
  - docs/runbooks/task-repeatedly-failing.md
  - docs/runbooks/csv-malformed.md
  - docs/runbooks/schema-changed.md
  - docs/runbooks/duplicate-batch.md
  - docs/runbooks/late-arriving-data.md
  - docs/runbooks/cdc-failure.md
  - docs/runbooks/scd-correction.md
  - docs/runbooks/corrupted-file.md
  - docs/runbooks/partial-database-load.md
  - docs/runbooks/secret-rotation.md
  - docs/runbooks/unauthorized-access.md
  - tests/policy/test_runbooks_structure.py
autonomous: true
requirements: [OBS-06]

must_haves:
  truths:
    - "15 of README §89's 18 operational scenarios have a runbook, each with symptoms/diagnosis/recovery/reprocessing/verification sections"
    - "Every runbook sourced from a real incident cites its exact .planning/debug/resolved/*.md source and uses that incident's actual diagnosis and fix, not a hypothetical one"
    - "The CDC-failure scenario is documented as an honest out-of-v1-scope stub pointing at the Source/Publisher seam, never silently omitted"
  artifacts:
    - path: "docs/runbooks/airflow-unavailable.md"
      provides: "Operator-facing distillation of the dagrun-scheduler-stall.md incident"
    - path: "tests/policy/test_runbooks_structure.py"
      provides: "Structural proof every runbook file has the 5 required sections"
  key_links:
    - from: "docs/runbooks/airflow-unavailable.md"
      to: ".planning/debug/resolved/dagrun-scheduler-stall.md"
      via: "cited by path, content matches the incident's real root_cause/fix/verification"
      pattern: "dagrun-scheduler-stall"
---

<objective>
Write 15 of the 18 verified README §89 operational runbooks: the 5 sourced from this project's own
real, already-diagnosed incidents, the 9 documenting already-built features as operator how-tos,
and 1 honest out-of-scope stub (CDC failure). The remaining 3 (MinIO unavailable, PostgreSQL
unavailable, secret unavailable) deliberately trail plan 11-09's chaos tests and are written in
plan 11-14, per D-41's own explicit "runbooks trail the chaos tests" ordering.

Purpose: OBS-06 requires a runbook per §89 scenario, documenting symptoms/diagnosis/recovery/
reprocessing/verification, so someone who did not build the platform can operate it.
Output: 15 runbook files + a structural policy test proving they exist and are correctly shaped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/debug/resolved/dagrun-scheduler-stall.md
@.planning/debug/resolved/wait-for-files-stuck-task.md
@.planning/debug/resolved/airflow-scheduler-stuck-tasks.md
@.planning/debug/resolved/backfill-does-not-redrive-rejected-row.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.

This plan is the literal fulfillment of "operate it without tribal knowledge."
</context>

<interfaces>
Verified README §89 list (18 items, NOT 17 — 11-RESEARCH.md Pitfall 11 corrects CONTEXT.md's own
miscount): Airflow unavailable, MinIO unavailable, PostgreSQL unavailable, Vault unavailable,
Kubernetes pod stuck, CSV malformed, Schema changed, Duplicate batch, Failed backfill,
Late-arriving data, CDC failure, SCD correction, Corrupted file, Task repeatedly failing, Partial
database load, Secret unavailable, Secret rotation, Unauthorized access. This plan writes all
EXCEPT MinIO unavailable / PostgreSQL unavailable / Secret unavailable (plan 11-14).

Per-scenario source mapping for THIS plan (from 11-RESEARCH.md's own verified table — cite the
exact source file/requirement in each doc, do not write from memory or invent detail):
- Airflow unavailable -> `.planning/debug/resolved/dagrun-scheduler-stall.md` (Docker Desktop/WSL2
  restart breaking the DAGs hostPath mount, cluster-wide scheduler freeze)
- Vault unavailable -> `.planning/debug/resolved/wait-for-files-stuck-task.md` (Vault reseal after
  host restart)
- Kubernetes pod stuck -> `.planning/debug/resolved/airflow-scheduler-stuck-tasks.md` (node CPU
  exhaustion + xcom-sidecar leak)
- Failed backfill -> `.planning/debug/resolved/backfill-does-not-redrive-rejected-row.md` +
  INCR-06's historical-schema-resolution mechanism
- Task repeatedly failing -> `.planning/debug/resolved/airflow-scheduler-stuck-tasks.md` again, or
  ORCH-04's retry/failure semantics if a distinct angle is clearer
- CSV malformed -> existing feature, VALID-01 (Phase 8) — document current behavior, not an
  incident
- Schema changed -> existing feature, SCHEMA-04/05 (Phase 6)
- Duplicate batch -> existing feature, LOAD-08 batch ledger (Phase 4)
- Late-arriving data -> existing feature, INCR-03/04 (Phase 08.1)
- CDC failure -> NO real subject exists (CDC dropped from v1, REQUIREMENTS.md Out of Scope,
  DoD 44/45/46/87) — write a short, honest stub: not applicable in v1, here is the Source/Publisher
  seam (Phase 3 ADR-0008) that would carry it later. Do not omit this file.
- SCD correction -> existing feature, SCD-07 late-arriving correction (Phase 10) — an operational
  how-to, not a failure mode
- Corrupted file -> existing feature, LOAD-10 integrity verification (Phase 8)
- Partial database load -> existing feature, `meta.v_run_recovery` (Phase 9, plan 09-06,
  `tests/integration/test_run_recovery_view.py`) — REQUIREMENTS.md's traceability table still says
  "Pending" for LOAD-06, which is STALE documentation (last updated before Phase 9 executed); this
  runbook documents the real, already-built view, and update REQUIREMENTS.md's status is NOT this
  plan's job (out of scope — do not edit REQUIREMENTS.md here).
- Secret rotation -> existing feature, SEC-09 (Phase 5, documented rotation procedure) +
  `tests/e2e/vault/test_rotation.py`
- Unauthorized access -> existing feature, SEC-12 positive/negative test pattern (Phase 5),
  `tests/e2e/vault/test_negative_auth.py`

Content discipline (from `.planning/debug/resolved/dagrun-scheduler-stall.md`'s own `##
Resolution` section, already read): a runbook's Diagnosis/Recovery/Verification sections should
read as the operator-facing distillation of exactly that incident log's `root_cause`/`fix`/
`verification` fields — the load-bearing evidence chain only, not every dead end investigated.

Structural form (no existing analog — `docs/runbooks/` does not exist yet): use
`docs/ci-branch-protection.md`'s prose/heading TONE (procedural how-to, not an incident-log
transcript) with exactly these 5 `##` headings per file, in this order: `## Symptoms`, `##
Diagnosis`, `## Recovery`, `## Reprocessing`, `## Verification`. Naming convention: kebab-case from
the scenario name, one file per scenario, under `docs/runbooks/`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 5 real-incident runbooks</name>
  <files>docs/runbooks/airflow-unavailable.md, docs/runbooks/vault-unavailable.md, docs/runbooks/kubernetes-pod-stuck.md, docs/runbooks/failed-backfill.md, docs/runbooks/task-repeatedly-failing.md</files>
  <read_first>
    .planning/debug/resolved/dagrun-scheduler-stall.md
    .planning/debug/resolved/wait-for-files-stuck-task.md
    .planning/debug/resolved/airflow-scheduler-stuck-tasks.md
    .planning/debug/resolved/backfill-does-not-redrive-rejected-row.md
  </read_first>
  <action>
    Write all five files, each with exactly the 5 required `##` headings. Each `## Diagnosis` and
    `## Recovery` section must be traceable to the cited incident log's own `root_cause`/`fix`
    content (paraphrase for an operator audience, do not invent detail the incident log doesn't
    support). Each file's top line (or a frontmatter-style note) must cite the exact source path,
    e.g. "Sourced from `.planning/debug/resolved/dagrun-scheduler-stall.md`." `task-repeatedly-
    failing.md` may reuse `airflow-scheduler-stuck-tasks.md` again OR pivot to ORCH-04's own
    documented retry/failure-callback semantics — state which you chose in the doc itself.
    `failed-backfill.md` must also reference INCR-06's historical-schema-resolution guarantee in
    its `## Reprocessing` section (a failed backfill's correct re-drive path depends on it).
  </action>
  <acceptance_criteria>
    - All 5 files exist with exactly 5 `##` headings each, in the specified order.
    - Each file cites its real incident source by exact path.
    - No file states a root cause or fix that contradicts its cited incident log.
  </acceptance_criteria>
  <verify>
    <automated>ls docs/runbooks/airflow-unavailable.md docs/runbooks/vault-unavailable.md docs/runbooks/kubernetes-pod-stuck.md docs/runbooks/failed-backfill.md docs/runbooks/task-repeatedly-failing.md</automated>
  </verify>
  <done>5 runbooks exist, each a faithful operator-facing distillation of a real, previously-diagnosed incident.</done>
</task>

<task type="auto">
  <name>Task 2: 10 existing-feature + stub runbooks</name>
  <files>docs/runbooks/csv-malformed.md, docs/runbooks/schema-changed.md, docs/runbooks/duplicate-batch.md, docs/runbooks/late-arriving-data.md, docs/runbooks/cdc-failure.md, docs/runbooks/scd-correction.md, docs/runbooks/corrupted-file.md, docs/runbooks/partial-database-load.md, docs/runbooks/secret-rotation.md, docs/runbooks/unauthorized-access.md</files>
  <read_first>
    REQUIREMENTS.md (VALID-01, SCHEMA-04/05, LOAD-08, INCR-03/04, SCD-07, LOAD-10, SEC-09, SEC-12 — exact requirement text for each)
    tests/e2e/vault/test_rotation.py, tests/e2e/vault/test_negative_auth.py (secret-rotation.md / unauthorized-access.md cite these directly)
    docs/secrets-architecture.md (SEC-14's existing secrets documentation — cross-reference rather than duplicate for secret-rotation.md/unauthorized-access.md)
  </read_first>
  <action>
    (Scope note, revision 1: kept as ONE 10-file task rather than split into two 5-file
    tasks. Each file is a short, template-driven document sharing the same 5-heading shape
    and the same REQUIREMENTS.md/test-file lookup pattern, with zero cross-file coupling --
    splitting would duplicate this framing paragraph and the read_first list across two tasks
    without reducing executor effort or risk. This was a live checker question, answered here
    deliberately rather than left unaddressed.)

    Write all ten files as "how to operate an already-built feature," not incident reconstructions
    — `## Symptoms` describes what an operator observes (e.g. a validation-report FAIL/QUARANTINE
    status, a schema-drift alert, a rejected-record row), `## Diagnosis` points at the exact
    queryable source (`meta.validation_results`, `meta.rejected_records`, `meta.schema_versions`,
    `meta.v_run_recovery`, Vault's audit log, etc.), `## Recovery`/`## Reprocessing` describe the
    documented re-drive/correction path (VALID-08 for csv-malformed/corrupted-file, INCR-06 for
    late-arriving-data, the SCD Publisher's recompute-from-ordered-history mechanism for
    scd-correction). `cdc-failure.md` is the one deliberate exception: write it as a short, honest
    stub stating CDC is out of v1 scope (cite REQUIREMENTS.md's Out of Scope section and the exact
    dropped DoD items), and name the `Source`/`Publisher` seam (ADR-0008) that would carry a CDC
    feed later without redesign — still include all 5 headings, with `## Symptoms` through `##
    Verification` each stating plainly "not applicable in v1" plus the pointer, rather than
    fabricating content for a feature that does not exist.
  </action>
  <acceptance_criteria>
    - All 10 files exist with exactly 5 `##` headings each, in the specified order.
    - `cdc-failure.md` explicitly states CDC is out of v1 scope and cites the Source/Publisher seam
      — it does not describe a CDC failure mode as though CDC exists.
    - `partial-database-load.md` names `meta.v_run_recovery` and cites
      `tests/integration/test_run_recovery_view.py` as its proof.
    - `secret-rotation.md` and `unauthorized-access.md` each cite their exact Phase-5 test file by
      path.
  </acceptance_criteria>
  <verify>
    <automated>ls docs/runbooks/csv-malformed.md docs/runbooks/schema-changed.md docs/runbooks/duplicate-batch.md docs/runbooks/late-arriving-data.md docs/runbooks/cdc-failure.md docs/runbooks/scd-correction.md docs/runbooks/corrupted-file.md docs/runbooks/partial-database-load.md docs/runbooks/secret-rotation.md docs/runbooks/unauthorized-access.md</automated>
  </verify>
  <done>10 more runbooks exist, all correctly framed as operating-an-existing-feature docs, with the CDC stub honestly scoped rather than omitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Structural policy test for this plan's 15 files</name>
  <files>tests/policy/test_runbooks_structure.py</files>
  <read_first>
    tests/policy/test_supply_chain_guards.py (the reusable-checker-function style to mirror, even though this test walks markdown files rather than YAML)
  </read_first>
  <behavior>
    - `test_every_runbook_created_so_far_has_the_five_required_headings`: for each of this plan's
      15 files, all 5 `##` headings (`Symptoms`, `Diagnosis`, `Recovery`, `Reprocessing`,
      `Verification`) are present, in order.
    - `test_a_runbook_missing_a_heading_is_reported`: non-vacuity — a deep-copy-and-mutate-style
      check against an in-memory string missing one heading is reported by the same checker
      function used above.
  </behavior>
  <action>
    Write `tests/policy/test_runbooks_structure.py` with a reusable `missing_headings(text: str)
    -> list[str]` function and thin test functions calling it against each of this plan's 15 files
    plus a synthetic mutated string for the non-vacuity test. Do NOT assert `len(list(docs/
    runbooks/*.md)) == 18` yet — that assertion belongs to plan 11-14, which completes the set; a
    premature count assertion here would be a false negative between this plan and that one. Name
    this file's own module docstring explicitly stating 3 files are added later by plan 11-14 and
    why (D-41's chaos-suite-trailing rule), so a future reader isn't confused by an apparently
    incomplete count.
  </action>
  <verify>
    <automated>uv run pytest tests/policy/test_runbooks_structure.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass.
    - The test module does not assert a total file count of 18 (that assertion is added by plan
      11-14).
  </acceptance_criteria>
  <done>Every runbook this plan created is structurally proven correct; the test module explicitly documents why the count assertion is deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook content -> an operator acting under pressure | Inaccurate diagnosis/recovery steps in a runbook are actively worse than no runbook — they cost time and can cause a wrong, damaging action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-35 | Repudiation | A runbook describing a fix that doesn't match the real incident | mitigate | every real-incident runbook is required to cite its exact source path, and its content is checked against that source's own `root_cause`/`fix`/`verification` fields, not written from memory |
| T-11-36 | Information Disclosure | Secrets architecture detail duplicated/drifted across two docs | mitigate | `secret-rotation.md`/`unauthorized-access.md` cross-reference `docs/secrets-architecture.md` and the Phase 5 test files rather than re-describing the mechanism, avoiding a second, driftable copy |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | no new third-party dependency is introduced |
</threat_model>

<verification>
`uv run pytest tests/policy/test_runbooks_structure.py -q` passes.
</verification>

<success_criteria>
15 of 18 §89 runbooks exist, structurally verified, each traceable to a real source; the CDC stub
is honest rather than omitted; the remaining 3 are explicitly and correctly deferred to plan 11-14.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-13-SUMMARY.md` when done.
</output>
