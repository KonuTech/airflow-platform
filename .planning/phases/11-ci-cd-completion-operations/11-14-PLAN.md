---
phase: 11-ci-cd-completion-operations
plan: 14
type: execute
wave: 2
depends_on: ["11-09", "11-13"]
files_modified:
  - docs/runbooks/minio-unavailable.md
  - docs/runbooks/postgresql-unavailable.md
  - docs/runbooks/secret-unavailable.md
  - tests/policy/test_runbooks_structure.py
autonomous: true
requirements: [OBS-06]

must_haves:
  truths:
    - "The 3 remaining §89 scenarios are documented from real, observed chaos-test behavior, not hypothetically ahead of that evidence existing"
    - "docs/runbooks/ contains exactly 18 files, matching the verified README §89 list exactly"
  artifacts:
    - path: "docs/runbooks/minio-unavailable.md"
      provides: "Documented from tests/e2e/chaos/test_minio_unavailable.py's actual observed behavior"
    - path: "docs/runbooks/postgresql-unavailable.md"
      provides: "Documented from tests/e2e/chaos/test_database_unavailable.py's actual observed behavior"
    - path: "docs/runbooks/secret-unavailable.md"
      provides: "Documented from the vault-unavailable chaos test's observed behavior, with the distinct-from-Vault-outage diagnosis this scenario needs"
  key_links:
    - from: "docs/runbooks/minio-unavailable.md"
      to: "tests/e2e/chaos/test_minio_unavailable.py"
      via: "the exact symptom/recovery this test already proved live"
      pattern: "test_minio_unavailable"
---

<objective>
Complete the 18-file §89 runbook set with the 3 scenarios D-41 deliberately defers until real
observed evidence exists: MinIO unavailable, PostgreSQL unavailable, and secret unavailable. Run
plan 11-09's chaos tests live, observe their real output, and write from that — not from
hypothesis.

Purpose: OBS-06's remaining coverage, completed honestly rather than speculatively ahead of the
evidence D-41 requires.
Output: 3 more runbook files; `docs/runbooks/` now contains exactly 18 files; the deferred
count-assertion lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/phases/11-ci-cd-completion-operations/11-09-SUMMARY.md
@.planning/phases/11-ci-cd-completion-operations/11-13-SUMMARY.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.
</context>

<interfaces>
From plan 11-09's `tests/e2e/chaos/test_minio_unavailable.py`, `test_database_unavailable.py` and
`test_vault_unavailable.py` (read all three in full — the actual committed test code, not this
description): each test already contains the exact fault-injection mechanism, the exact symptom
signature it asserts on, and the exact recovery command. This plan's three runbooks are the
operator-facing prose distillation of exactly those tests' own bodies, the same way plan 11-13's
real-incident runbooks distilled `.planning/debug/resolved/*.md` files.

`secret-unavailable.md` is distinct from `vault-unavailable.md` (11-RESEARCH.md's own finding): a
missing/never-created secret PATH is a different failure surface than Vault itself being
sealed/unreachable. There is no dedicated chaos test for this exact scenario (QUAL-15's 11 named
scenarios do not include it as its own item) — write this runbook from (a) the `vault-unavailable`
chaos test's observed behavior for the SHARED symptom class (`resolve_secret()` failing) and (b)
an explicit `## Diagnosis` section distinguishing the two: `vault status` sealed/unreachable means
Vault-unavailable; `vault kv get <path>` succeeding for OTHER paths but 404-ing for the ETL
workload's specific expected key means secret-unavailable. Cite Phase 5's audit-log mechanism
(SEC-08) as the diagnostic tool that tells them apart in practice.

From `tests/policy/test_runbooks_structure.py` (plan 11-13's file — read it, extend it, do not
replace it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the 3 relevant chaos tests live, write from their observed behavior</name>
  <files>docs/runbooks/minio-unavailable.md, docs/runbooks/postgresql-unavailable.md, docs/runbooks/secret-unavailable.md</files>
  <read_first>
    tests/e2e/chaos/test_minio_unavailable.py
    tests/e2e/chaos/test_database_unavailable.py
    tests/e2e/chaos/test_vault_unavailable.py
    docs/secrets-architecture.md (SEC-08 audit-log mechanism, for secret-unavailable.md's diagnosis section)
  </read_first>
  <action>
    Run `uv run --group cluster pytest tests/e2e/chaos/test_minio_unavailable.py
    tests/e2e/chaos/test_database_unavailable.py tests/e2e/chaos/test_vault_unavailable.py -q -m
    cluster -v` and capture the actual symptom text/log output each produces. Write
    `minio-unavailable.md` and `postgresql-unavailable.md` with the same 5-heading structure as
    plan 11-13's files, sourcing `## Symptoms`/`## Diagnosis`/`## Recovery`/`## Verification`
    directly from what the corresponding test actually asserted and observed (cite the test file
    by path, the same discipline plan 11-13 applied to `.planning/debug/resolved/*.md`). Write
    `secret-unavailable.md` per the interfaces block's distinguishing-diagnosis instruction, citing
    both the vault-unavailable chaos test (shared symptom class) and SEC-08's audit log (the
    diagnostic that tells the two apart).
  </action>
  <acceptance_criteria>
    - All 3 files exist with exactly 5 `##` headings each, in the specified order.
    - `minio-unavailable.md` and `postgresql-unavailable.md` each cite their source chaos test file
      by path and describe the ACTUAL symptom that test asserts, not a generic/hypothetical one.
    - `secret-unavailable.md`'s `## Diagnosis` section explicitly distinguishes this scenario from
      Vault-unavailable using the `vault status` vs. `vault kv get <path>` distinction.
  </acceptance_criteria>
  <verify>
    <automated>ls docs/runbooks/minio-unavailable.md docs/runbooks/postgresql-unavailable.md docs/runbooks/secret-unavailable.md</automated>
  </verify>
  <done>3 more runbooks exist, each traceable to real, just-observed chaos-test behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the structural policy test to the full 18-file set</name>
  <files>tests/policy/test_runbooks_structure.py</files>
  <read_first>
    tests/policy/test_runbooks_structure.py (plan 11-13's file)
  </read_first>
  <behavior>
    - `test_docs_runbooks_contains_exactly_18_files`: `len(sorted(Path("docs/runbooks").glob("*.md")))
      == 18`.
    - `test_the_full_verified_scenario_set_is_covered_by_filename`: the 18 expected kebab-case
      filenames (the exact verified README §89 list) are all present, no more, no fewer — a
      renamed or missing scenario file is reported by name.
    - (existing, unchanged from plan 11-13) every file still has all 5 required headings.
  </behavior>
  <action>
    Extend `tests/policy/test_runbooks_structure.py` with the two new behaviors above, using an
    explicit, hard-coded 18-item expected filename list (transcribed directly from
    11-RESEARCH.md's own verified §89 list) so a future accidental rename or deletion fails loudly
    by name, not just by count. Remove this plan's module-docstring note about the count assertion
    being deferred (plan 11-13's own note) since it is no longer true.
  </action>
  <verify>
    <automated>uv run pytest tests/policy/test_runbooks_structure.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass, including the new count and named-set assertions.
    - `docs/runbooks/` contains exactly 18 `.md` files.
  </acceptance_criteria>
  <done>OBS-06 is structurally complete and permanently regression-tested: 18 files, the exact verified §89 set, each with all 5 required sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runbook content sourced from a chaos test's assertions | If the test's own assertions are shallow, the runbook inherits that shallowness — this is why Task 1 runs the tests live and reads real output rather than reading only the test source code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-37 | Repudiation | A runbook written from the test's SOURCE CODE rather than its OBSERVED live output | mitigate | Task 1 explicitly runs the three chaos tests live with `-v` and captures real output before writing, matching D-41's own "written from... observed behavior" wording literally |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | no new third-party dependency is introduced |
</threat_model>

<verification>
`uv run pytest tests/policy/test_runbooks_structure.py -q` passes with the full 18-file assertion.
</verification>

<success_criteria>
OBS-06 is fully and correctly delivered: 18 runbooks, structurally verified, each sourced honestly
(real incident, existing feature, or observed chaos-test behavior) per D-41's own rules.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-14-SUMMARY.md` when done.
</output>
