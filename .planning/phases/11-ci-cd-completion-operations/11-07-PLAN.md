---
phase: 11-ci-cd-completion-operations
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dataplat/src/dataplat/config/model.py
  - configs/datasets/customers.yaml
  - configs/datasets/orders.yaml
  - packages/dataplat/src/dataplat/retention/__init__.py
  - packages/dataplat/src/dataplat/retention/policy.py
  - tests/unit/test_retention_policy.py
  - helm/values/local/minio.yaml
  - helm/values/ci/minio.yaml
  - docs/adr/0011-raw-immutability-iam-not-worm.md
autonomous: true
requirements: [INFRA-11]

must_haves:
  truths:
    - "A dataset config with no retention: block behaves identically to today — no accidental retention enforcement anywhere"
    - "The retention policy evaluator never raises and never deletes on its own — it only ever produces a structured dry-run report unless enforce is explicitly true"
    - "Raw's default retention window is unset (indefinite) even though the config surface supports a window, structurally"
    - "The MinIO raw deny-delete IAM policy Phase 2 already built is verified live, not silently re-implemented, and its comment no longer misdescribes Phase 11 as unresolved"
    - "D-36: RetentionConfig.raw_days defaults to None and neither dataset YAML sets it to a non-null value, so raw's retention window is indefinite by default while remaining structurally configurable"
    - "D-37: customers.yaml and orders.yaml set concrete tiered retention day values within D-37's locked ranges, specifically processed 60, quarantine 180, validation reports and ingestion metadata 730, and logs 30"
    - "D-38: RetentionConfig.enforce defaults to False at the Pydantic model level and the evaluator only ever reports would-delete candidates unless enforce is explicitly set true, matching the dry-run-by-default safety guarantee"
    - "D-39: Retention configuration is declared per-dataset as a retention block inside each dataset's existing YAML, validated by the same Pydantic RetentionConfig model, not a separate platform-wide retention.yaml"
  artifacts:
    - path: "packages/dataplat/src/dataplat/config/model.py"
      provides: "RetentionConfig, registered as DatasetConfig.retention: RetentionConfig | None = None"
      contains: "class RetentionConfig(BaseModel)"
    - path: "packages/dataplat/src/dataplat/retention/policy.py"
      provides: "A constructor-parameterized, never-raising retention dry-run/enforce evaluator"
    - path: "docs/adr/0011-raw-immutability-iam-not-worm.md"
      provides: "The closed-out record of the D-08/D-40 decision thread"
  key_links:
    - from: "configs/datasets/customers.yaml"
      to: "packages/dataplat/src/dataplat/config/model.py::RetentionConfig"
      via: "retention: block validated by DatasetConfig"
      pattern: "retention:"
    - from: "packages/dataplat/src/dataplat/retention/policy.py"
      to: "RetentionConfig.enforce"
      via: "conditional delete gate, default false"
      pattern: "enforce"
---

<objective>
Build the retention CONTRACT and EVALUATOR half of INFRA-11 as one TDD-eligible vertical slice:
the Pydantic config shape, its wiring into two real dataset YAMLs with D-37's tiered defaults, and
a pure, constructor-parameterized policy evaluator that reports what a run WOULD delete and only
conditionally acts. Also closes out D-40 (Phase 2's forward-referenced MinIO IAM-policy revisit
point) as a verify-and-document task, since it is already fully implemented.

Purpose: INFRA-11 requires "configurable retention policies... enforced separately from processing
logic." This plan delivers the config surface and the decision logic; the DAG that actually calls
it is plan 11-08. D-40 explicitly names Phase 11 as its revisit point — verifying and closing it
here, rather than quietly rebuilding it, is a scope-reduction-prohibition-adjacent correctness
requirement of this phase (see the quality gate: "D-40 is NOT re-implemented as new work").
Output: `RetentionConfig`, `retention/policy.py`, two extended dataset YAMLs, and ADR-0011.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/phases/11-ci-cd-completion-operations/11-PATTERNS.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.

Retention that silently deletes data on first run would violate the Core Value outright — dry-run
by default is what makes this trustworthy.
</context>

<interfaces>
From `packages/dataplat/src/dataplat/config/model.py` (read `ScdConfig` at lines 513-541 and
`DatasetConfig` at lines 544-620 in full — these are the two closest analogs and the exact
registration point):
- Every opt-in config class uses `model_config = ConfigDict(extra="forbid", frozen=True)` —
  never omit this on `RetentionConfig`.
- `DatasetConfig` registers each opt-in block as `<name>: <Config> | None = None`, with a
  matching Attributes docstring entry. `ConfigRegistry.sync()` persists `config.model_dump(mode=
  "json")` as an opaque JSONB blob — no change needed there; a new field is captured automatically.

From `configs/datasets/customers.yaml` (read in full — the `scd:`/`freshness:` blocks at lines
37-45 and 103-111 are the exact opt-in-block-with-a-decision-ID-comment shape to copy for
`retention:`).

From `packages/dataplat/src/dataplat/validate/circuit_breaker.py::RejectionRateCircuitBreaker` and
`packages/dataplat/src/dataplat/scd/delete_detection.py::MassDeleteCircuitBreaker` (read both in
full): the shared shape — constructor takes already-known totals, `apply()`/`evaluate()` never
re-queries, a `total == 0` trivial-pass guard is always the first branch, findings are structured
`ValidationResult`-shaped objects with `threshold`/`observed` dicts. The retention evaluator copies
this shape but diverges in ONE load-bearing way: the circuit breakers RAISE on breach; retention
must REPORT and only conditionally act, and must NEVER raise merely because something is old
enough to delete.

From `helm/values/local/minio.yaml` lines 51-66 and `helm/values/ci/minio.yaml` (read both — the
`etl-app` policy's `Deny` statement on `s3:DeleteObject`/`s3:DeleteObjectVersion` against
`arn:aws:s3:::raw/*` already exists, committed in Phase 2, with a live positive+negative test at
`tests/e2e/cluster/test_minio_buckets.py::test_raw_delete_is_denied_for_app_credential` /
`::test_raw_delete_is_permitted_for_admin_credential`). D-40's ENTIRE resolution is already built.
This plan's job is to verify it still holds and stop the comment from misdescribing Phase 11 as an
open question.

From `docs/adr/0000-template.md` and `docs/adr/0010-dbt-silver-layer-boundary.md` (read both in
full): the exact ADR structure and the next available number (`0011` — confirmed no ADR 0011
exists yet).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RetentionConfig contract + two dataset YAMLs</name>
  <files>packages/dataplat/src/dataplat/config/model.py, configs/datasets/customers.yaml, configs/datasets/orders.yaml</files>
  <read_first>
    packages/dataplat/src/dataplat/config/model.py (ScdConfig and DatasetConfig, lines 513-620)
    configs/datasets/customers.yaml
    configs/datasets/orders.yaml
  </read_first>
  <action>
    Add `class RetentionConfig(BaseModel)` to `config/model.py`, placed alongside the other opt-in
    config classes (near `ScdConfig`), with `model_config = ConfigDict(extra="forbid",
    frozen=True)` and these fields, matching D-37's tiered semantics and D-38's fail-closed default
    exactly: `raw_days: int | None = None` (D-36 — structurally supported, `None` by default,
    meaning indefinite), `processed_days: int | None = None`, `quarantine_days: int | None = None`,
    `validation_reports_days: int | None = None`, `ingestion_metadata_days: int | None = None`,
    `logs_days: int | None = None`, `enforce: bool = False` (D-38 — the field DEFAULT is the
    fail-closed guarantee, not a YAML convention). Write a full docstring naming D-36/D-37/D-38/D-39
    the way `ScdConfig`'s docstring names D-05/D-06. Register `retention: RetentionConfig | None =
    None` on `DatasetConfig`, with a matching Attributes entry following the existing per-field
    docstring convention (see the `scd:`/`freshness:` entries at lines 586-589).

    Extend `configs/datasets/customers.yaml` and `configs/datasets/orders.yaml` with a `retention:`
    block, each preceded by a comment citing D-37/D-38/D-39 the way the existing `scd:`/
    `freshness:` blocks cite their own decision IDs. Use concrete numeric defaults within D-37's
    locked ranges (Claude's discretion): `processed_days: 60`, `quarantine_days: 180`,
    `validation_reports_days: 730`, `ingestion_metadata_days: 730`, `logs_days: 30`, `raw_days:
    null` (omit the key entirely or set it explicitly to show the indefinite default is a deliberate
    choice, not an oversight — prefer omitting it, since `RetentionConfig.raw_days` already
    defaults to `None`), `enforce: false`.
  </action>
  <acceptance_criteria>
    - `class RetentionConfig(BaseModel)` exists in `config/model.py` with `model_config =
      ConfigDict(extra="forbid", frozen=True)` and `enforce: bool = False` as a field default (not
      merely a YAML convention — verify by instantiating `RetentionConfig()` with zero arguments in
      a Python REPL/test and confirming `.enforce is False`).
    - `DatasetConfig.retention: RetentionConfig | None = None` is present.
    - Both `configs/datasets/customers.yaml` and `configs/datasets/orders.yaml` contain a
      `retention:` block; neither sets `raw_days` to a non-null value; neither sets `enforce:
      true`.
    - `uv run python -c "from dataplat.config.loader import load_config; load_config('customers')"`
      (or the project's actual loader invocation shape — confirm via
      `packages/dataplat/src/dataplat/config/loader.py`) succeeds with zero validation errors for
      both datasets.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit -k config -q</automated>
  </verify>
  <done>Both dataset configs validate with a populated, discretionary-but-in-range retention block; RetentionConfig's own default is fail-closed at the type level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retention policy evaluator (TDD)</name>
  <files>packages/dataplat/src/dataplat/retention/__init__.py, packages/dataplat/src/dataplat/retention/policy.py, tests/unit/test_retention_policy.py</files>
  <read_first>
    packages/dataplat/src/dataplat/validate/circuit_breaker.py (RejectionRateCircuitBreaker, whole file)
    packages/dataplat/src/dataplat/scd/delete_detection.py (MassDeleteCircuitBreaker, lines 167-286)
    tests/unit/validate/test_circuit_breaker.py (whole file — the exact test-naming and fixture style to mirror)
    packages/dataplat/src/dataplat/config/model.py (RetentionConfig, from Task 1)
  </read_first>
  <behavior>
    - `test_a_dry_run_never_deletes_anything`: constructing the evaluator with `enforce=False` and
      calling its plan/evaluate method on a non-empty set of candidates returns a report listing
      every over-window candidate as "would delete," with an explicit `deleted_count == 0` (or
      equivalent) — no side effect performed.
    - `test_zero_candidates_never_raises`: an empty candidate set produces a structural PASS-shaped
      report (`observed={"candidate_count": 0}`), mirroring `RejectionRateCircuitBreaker`'s
      zero-rows guard — never a `ZeroDivisionError` or any other exception.
    - `test_a_window_of_none_never_selects_any_candidate`: a layer configured with `days=None`
      (D-36's raw default) reports zero candidates regardless of how old the input records are —
      "indefinite" is enforced structurally, not by an accidentally-large number.
    - `test_a_candidate_older_than_the_window_is_selected_and_one_exactly_at_the_boundary_is_not`:
      boundary-value test — an item aged exactly `days` old is NOT selected (retention windows are
      exclusive of the boundary; state this explicitly in the evaluator's docstring), one day older
      IS selected.
    - `test_enforce_true_marks_candidates_for_deletion_but_the_evaluator_itself_still_never_performs_io`:
      with `enforce=True`, the report distinguishes "would delete" from "will delete," but the
      evaluator function/class itself takes no storage/DB handle and performs no I/O — it is a
      pure decision function; the CALLER (plan 11-08's DAG) is responsible for acting on the
      report. This is the load-bearing architectural boundary: keep deletion mechanics entirely out
      of this module.
  </behavior>
  <action>
    Create `packages/dataplat/src/dataplat/retention/__init__.py` (empty/trivial, matching the
    convention of other `dataplat` subpackages). Create `retention/policy.py`: a
    `RetentionCandidate` frozen dataclass (`layer: str`, `identifier: str`, `age_days: int`, `size_bytes:
    int | None = None`) and a `RetentionEvaluator` (or `evaluate_retention(...)` free function —
    choose whichever fits this module's own eventual DAG-calling shape better; document the choice)
    that takes a `RetentionConfig` and a `Sequence[RetentionCandidate]` (already queried by the
    caller — mirroring `RejectionRateCircuitBreaker`'s "totals come from the constructor" rule,
    this function/class NEVER queries MinIO or PostgreSQL itself) and returns a structured
    `RetentionReport` (per-layer: candidate count, would-delete count, total size, oldest/newest
    age — D-38's exact wording — plus `enforce: bool` echoed back). Write the RED tests first
    (`tests/unit/test_retention_policy.py`, mirroring `test_circuit_breaker.py`'s naming/fixture
    style), watch them fail for the right reason (module/class doesn't exist yet), then implement
    until GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_retention_policy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5+ tests pass.
    - `retention/policy.py` imports no `boto3`, `psycopg`, or any I/O-performing module — grep
      confirms this (`grep -E "^import (boto3|psycopg)|^from (boto3|psycopg)" packages/dataplat/src/dataplat/retention/policy.py` returns nothing).
    - The evaluator's own docstring states the boundary-exclusive convention explicitly.
  </acceptance_criteria>
  <done>A pure, fully-tested retention decision function exists; plan 11-08's DAG is the only place that will ever call boto3/psycopg to act on its report.</done>
</task>

<task type="auto">
  <name>Task 3: Verify D-40 (do not rebuild) + close the loop with ADR-0011</name>
  <files>helm/values/local/minio.yaml, helm/values/ci/minio.yaml, docs/adr/0011-raw-immutability-iam-not-worm.md</files>
  <read_first>
    helm/values/local/minio.yaml (lines 51-66, the existing objectlocking/D-08 comment and the etl-app Deny statement)
    helm/values/ci/minio.yaml
    tests/e2e/cluster/test_minio_buckets.py (the already-existing live proof — read in full, do not write a new test)
    docs/adr/0000-template.md
    docs/adr/0010-dbt-silver-layer-boundary.md
  </read_first>
  <action>
    Run the EXISTING live proof to confirm D-40 still holds on the current cluster:
    `uv run --group cluster pytest tests/e2e/cluster/test_minio_buckets.py -q -m cluster -k
    "delete"`. Do NOT write a new test and do NOT add a new IAM statement — the `Deny` statement on
    `arn:aws:s3:::raw/*` for `s3:DeleteObject`/`s3:DeleteObjectVersion` already exists in both
    `helm/values/local/minio.yaml` and `helm/values/ci/minio.yaml` (committed Phase 2, `c600905`).

    Edit the `objectlocking: false` comment block in BOTH `minio.yaml` files (currently reads
    "...Phase 11 (rebuild-from-raw, ephemeral environment) is the named place to revisit it.") to
    state the decision is RESOLVED: raw immutability is enforced via the IAM deny-delete policy
    below (already present), not object-lock/WORM, per Phase 11 ADR-0011 — replace "is the named
    place to revisit it" with something like "was revisited in Phase 11: ADR-0011 confirms the IAM
    deny-delete policy below as the resolution, not WORM."

    Write `docs/adr/0011-raw-immutability-iam-not-worm.md` following `0000-template.md`'s exact
    structure: Context (README §63 raw immutability + Phase 2's D-08 objection to WORM — retained
    test objects becoming impossible to clean up on a persistent local cluster), Considered Options
    (IAM deny-delete policy vs. object-lock/WORM), Decision Outcome (IAM deny-delete, already
    implemented since Phase 2, chosen because it gives every workload identity the same practical
    inability to delete raw objects while remaining revocable for a genuine break-glass admin
    scenario — WORM's retention lock is permanent), Consequences (good: cleanable in dev; bad: a
    compromised admin credential COULD still delete raw, unlike true WORM; neutral: n/a), Migration
    trigger (a genuine compliance/regulatory requirement for tamper-proof retention would revisit
    this toward WORM — state this explicitly, do not leave it blank), References (README §63,
    §90/§91, `.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md` D-40,
    `helm/values/local/minio.yaml`).
  </action>
  <acceptance_criteria>
    - `uv run --group cluster pytest tests/e2e/cluster/test_minio_buckets.py -q -m cluster -k
      delete` passes against the live cluster (2 tests: denied-for-app, permitted-for-admin).
    - Neither `helm/values/local/minio.yaml` nor `helm/values/ci/minio.yaml` gained a NEW `Deny`
      statement (diff shows comment-only changes to these two files).
    - `docs/adr/0011-raw-immutability-iam-not-worm.md` exists, follows the template's section
      headings exactly, and its "Migration trigger" section is non-empty.
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/cluster/test_minio_buckets.py -q -m cluster -k delete</automated>
  </verify>
  <done>D-40 is confirmed live and unchanged; the stale forward-reference comment is corrected; the decision is closed out in writing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RetentionConfig (per-dataset YAML) -> the DAG that will act on it (plan 11-08) | A misconfigured window is a data-loss risk if the enforce/dry-run boundary is not type-level fail-closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Tampering (data loss) | `RetentionConfig.enforce` | mitigate | field DEFAULT is `False` at the Pydantic model level, not merely a YAML convention — a dataset config that omits `retention:` entirely, or omits `enforce:` inside it, can never accidentally hard-delete; proven by Task 2's own test suite |
| T-11-17 | Repudiation | A retention report with no durable record | accept | this plan builds only the decision function; plan 11-08's DAG is responsible for logging/persisting the report before any deletion — out of this plan's own scope by design (interface-first separation) |
| T-11-18 | Elevation of Privilege | Break-glass admin credential retains raw delete | accept | this is D-40's own explicit, documented trade-off versus WORM (ADR-0011's Consequences section states it plainly); mitigated operationally by the admin credential being used by no live workload, only manual/`cluster-rebuild` tooling |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | no new third-party dependency is introduced by this plan |
</threat_model>

<verification>
`uv run pytest tests/unit/test_retention_policy.py tests/unit -k config -q` and `uv run --group
cluster pytest tests/e2e/cluster/test_minio_buckets.py -q -m cluster -k delete` both pass.
</verification>

<success_criteria>
RetentionConfig exists, fail-closed by construction; a pure, fully-tested evaluator exists ready
for plan 11-08's DAG to call; D-40 is verified (not rebuilt) and formally closed out.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-07-SUMMARY.md` when done.
</output>
