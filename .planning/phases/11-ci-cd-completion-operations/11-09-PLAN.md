---
phase: 11-ci-cd-completion-operations
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/e2e/chaos/__init__.py
  - tests/e2e/chaos/conftest.py
  - tests/e2e/chaos/test_pod_crash.py
  - tests/e2e/chaos/test_database_unavailable.py
  - tests/e2e/chaos/test_minio_unavailable.py
  - tests/e2e/chaos/test_vault_unavailable.py
autonomous: true
requirements: [QUAL-15]

must_haves:
  truths:
    - "Killing a task pod mid-run is proven to recover cleanly (extending, not duplicating, Phase 4's existing idempotency proof)"
    - "Blocking network access to the analytical PostgreSQL mid-run produces a graceful task failure and a clean recovery once access is restored, never a silent hang or corrupted state"
    - "Scaling MinIO to zero replicas mid-run produces the same graceful failure/recovery shape"
    - "Deliberately sealing Vault reproduces the exact real incident already diagnosed in this project's own debug history, and make vault-unseal is confirmed as the recovery"
  artifacts:
    - path: "tests/e2e/chaos/conftest.py"
      provides: "Shared chaos-suite fixtures: fault injection + guaranteed restoration"
    - path: "tests/e2e/chaos/test_vault_unavailable.py"
      provides: "Live reproduction of the wait-for-files-stuck-task.md incident"
  key_links:
    - from: "tests/e2e/chaos/conftest.py"
      to: "pyproject.toml markers"
      via: "pytest.mark.chaos registered alongside pytest.mark.cluster"
      pattern: "chaos:"
    - from: "tests/e2e/chaos/test_vault_unavailable.py"
      to: ".planning/debug/resolved/wait-for-files-stuck-task.md"
      via: "reproduces the same sealed-Vault symptom this incident already diagnosed"
      pattern: "vault operator seal"
---

<objective>
Build the first four of QUAL-15's eleven catalogued failure scenarios — the infrastructure-
unavailability group (pod crash, database unavailable, MinIO unavailable, Vault unavailable) —
plus the shared chaos-suite scaffolding (new pytest marker, conftest with guaranteed-restoration
fixtures) every later chaos plan and workflow (11-10, 11-05) builds on.

Purpose: QUAL-15 requires a passing test for each of README §84's 11 named scenarios. This project
already has real, diagnosed incidents for two of these four (pod crash — Phase 4's idempotency
proof; Vault unavailable — the wait-for-files-stuck-task.md incident); this plan makes each one a
permanent, repeatable, automated proof rather than a one-time diagnosis.
Output: `tests/e2e/chaos/` exists with a shared conftest and 4 of 11 scenario files, run live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/debug/resolved/wait-for-files-stuck-task.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.
</context>

<interfaces>
From `tests/e2e/slice/test_pod_kill_retry.py` (read in full before writing `test_pod_crash.py` —
this file ALREADY implements `test_pod_kill_mid_load_produces_no_duplicates` and
`test_pod_kill_mid_dbt_build_produces_no_duplicates`, a real `kubectl delete pod` mid-load,
idempotency-proven since Phase 4/08.1). Do not duplicate this work. `test_pod_crash.py` must do ONE
of: (a) extend coverage to a task this file does NOT already kill (it kills `stage` and
`dbt_build`; it does not kill `wait_for_files`/`discover`/`publish` — killing one of those and
confirming Airflow's own retry mechanics recover it, with no manual read_first assumption about
which is untested until you actually check), or (b) if every meaningfully distinct task-kill angle
truly is already covered, write `test_pod_crash.py` as a thin, explicitly-documented pointer test
(e.g. a test that asserts `test_pod_kill_retry.py`'s tests exist and pass, or a docstring-only
module explaining the reuse) — never silently write a second, near-identical "kill the stage pod"
test just to have a file present.

From `tests/e2e/cluster/conftest.py` (read in full, already read this session): `kubectl_context`,
`kubectl`, `kubectl_json` fixtures already exist at session scope and are directly reusable — do
NOT reinvent a second kubectl-context resolution mechanism in the new chaos conftest.

From `.planning/debug/resolved/wait-for-files-stuck-task.md` (read in full): the exact root cause
(Vault reseals on pod/host restart, no auto-unseal by design per D-02) and exact symptom (the
`wait_for_files` sensor's connection-not-found 404 exhausting its retry budget) this plan's
`test_vault_unavailable.py` reproduces on purpose, then recovers from via `scripts/vault-unseal.py`
(the same script `make vault-unseal` already wraps).

From `pyproject.toml`'s `[tool.pytest.ini_options]` `markers` list (already read): register a new
`"chaos: deliberately breaks a live component to prove recovery; excluded from the offline gate
and from make cluster-verify, run via a dedicated CI job"` entry, following the exact string-shape
convention of the existing 7 entries.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Chaos-suite scaffolding + pod_crash + database_unavailable</name>
  <files>pyproject.toml, tests/e2e/chaos/__init__.py, tests/e2e/chaos/conftest.py, tests/e2e/chaos/test_pod_crash.py, tests/e2e/chaos/test_database_unavailable.py</files>
  <read_first>
    pyproject.toml ([tool.pytest.ini_options] markers list)
    tests/e2e/slice/test_pod_kill_retry.py (whole file — decide the pod_crash approach per the interfaces block above)
    tests/e2e/cluster/conftest.py (kubectl_context, kubectl, kubectl_json fixtures)
    tests/e2e/vault/__init__.py, tests/e2e/cluster/__init__.py (the tiny docstring-only __init__.py convention)
    kubernetes/rbac-etl.yaml (etl namespace + analytics-db service name/labels, for the NetworkPolicy target)
  </read_first>
  <action>
    Register the `chaos` marker in `pyproject.toml`. Create `tests/e2e/chaos/__init__.py`
    (docstring-only, naming QUAL-15's 11 scenarios, mirroring `tests/e2e/vault/__init__.py`'s
    tiny-file convention). Create `tests/e2e/chaos/conftest.py`: reuse `tests/e2e/cluster/
    conftest.py`'s fixtures by relying on pytest's own conftest inheritance (do not copy-paste
    them) plus ONE new fixture this suite specifically needs — a `network_fault` context-manager
    fixture that applies a NetworkPolicy manifest (built from a small in-repo template, NOT via
    `kubernetes/`-committed YAML since this is a TRANSIENT test fixture, not a permanent platform
    manifest — apply it via the Kubernetes Python client or `kubectl apply -f -` with a heredoc,
    which is the stdin form `test_no_manual_kubectl_surgery.py` already permits) denying egress
    from a given namespace/label-selector to a given target, and GUARANTEES deletion of that
    NetworkPolicy in a `finally`/fixture-teardown even if the test body raises — a chaos test that
    leaves a cluster genuinely broken on failure is worse than the outage it was testing (D-25's
    own reasoning, taken to its necessary conclusion for fixture design).

    Write `test_pod_crash.py` per the interfaces block's decision framework: read
    `test_pod_kill_retry.py` first, identify whichever real task-pod-kill angle it does NOT already
    cover, and either extend coverage there or write the documented-reuse pointer test — record
    which choice you made and why in the test module's own docstring.

    Write `test_database_unavailable.py`: apply a `network_fault` NetworkPolicy denying egress
    from the `etl` namespace to the analytics CNPG Cluster's pods (label-selector-based, read
    `kubernetes/rbac-etl.yaml`/`helm/values/local/cnpg-analytics.yaml` for the exact
    namespace/label to target), trigger a real pipeline run (or use an already-in-flight one),
    confirm the task fails with a clear, attributable error (a connection-refused/timeout signature
    in the task log, not a silent hang past a bounded wait), then remove the NetworkPolicy and
    confirm a subsequent run/retry succeeds cleanly with no duplicate or corrupted rows (reuse this
    project's own idempotency-proof pattern from `test_pod_kill_retry.py`/`test_smoke_and_
    idempotency.py` for the "no corruption" assertion).
  </action>
  <acceptance_criteria>
    - `pyproject.toml`'s markers list contains a `chaos:` entry.
    - `tests/e2e/chaos/conftest.py`'s fault-injection fixture removes its NetworkPolicy even when
      the wrapped test raises (verify by writing one throwaway failing assertion temporarily inside
      a test using the fixture, confirming the NetworkPolicy is gone afterward via `kubectl get
      networkpolicy -n <ns>`, then removing that throwaway assertion).
    - `test_pod_crash.py`'s module docstring states explicitly which task-kill angle it covers and
      why, with a citation to `test_pod_kill_retry.py`.
    - `test_database_unavailable.py` passes live: the run fails clearly during the fault window and
      recovers cleanly afterward with zero duplicate/corrupted rows.
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/chaos/test_pod_crash.py tests/e2e/chaos/test_database_unavailable.py -q -m cluster</automated>
  </verify>
  <done>Two of the four scenarios pass live, and the shared fault-injection fixture is proven to clean up after itself even on failure.</done>
</task>

<task type="auto">
  <name>Task 2: minio_unavailable + vault_unavailable</name>
  <files>tests/e2e/chaos/test_minio_unavailable.py, tests/e2e/chaos/test_vault_unavailable.py</files>
  <read_first>
    .planning/debug/resolved/wait-for-files-stuck-task.md (whole file — the exact symptom and fix to reproduce)
    scripts/vault-unseal.py (the exact recovery mechanism this test calls to restore service)
    helm/values/local/minio.yaml (StatefulSet naming/namespace — mode: standalone, replicas: 1)
  </read_first>
  <action>
    Write `test_minio_unavailable.py`: `kubectl -n data scale statefulset/minio --replicas=0`,
    trigger/observe a run that needs MinIO (discovery or staging), confirm the task fails clearly
    (not a silent hang) within a bounded wait, then `kubectl -n data scale statefulset/minio
    --replicas=1`, wait for the pod to become Ready again (reuse `scripts/wait-for.sh`'s
    `wait_for_statefulset_ready` shape via a subprocess call or the Kubernetes Python client
    equivalent), and confirm a subsequent run succeeds with no corruption. Guarantee the
    replica count is restored to 1 in a `finally` block even if an assertion fails partway through.

    Write `test_vault_unavailable.py`: deliberately reproduce the diagnosed incident —
    `kubectl exec vault-0 -n vault -- vault operator seal` (sealing, not deleting — this survives
    restart per D-02's single-key Shamir design and is exactly what a host restart already does in
    practice), trigger/observe the `wait_for_files` sensor or a fresh DAG run, confirm the SAME
    class of symptom the incident log documents (a connection/secret-resolution failure exhausting
    retry budget, not a crash), then run `scripts/vault-unseal.py` directly (the same script `make
    vault-unseal` wraps) and confirm service resumes — a subsequent run reaches its normal
    terminal state. Guarantee Vault is left unsealed afterward even on assertion failure (a
    `finally` block re-running the unseal script unconditionally).
  </action>
  <acceptance_criteria>
    - Both tests pass live against the local cluster.
    - Both restore full service (MinIO at replicas=1 and Ready; Vault unsealed) even when run with
      a deliberately-injected failing assertion partway through (spot-check one of the two by hand).
    - `test_vault_unavailable.py`'s docstring cites `.planning/debug/resolved/
      wait-for-files-stuck-task.md` by path.
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/chaos -q -m cluster</automated>
  </verify>
  <done>All four Task-1+Task-2 scenarios in tests/e2e/chaos/ pass live, and this suite's own conftest fixtures are proven to always restore full cluster health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chaos test -> live cluster state | These tests deliberately break real components; a bug in the test's OWN teardown is itself an outage risk to whatever else is running on the same local cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-22 | Denial of Service | Chaos test's own fault injection left in place after a failed assertion | mitigate | every fault (NetworkPolicy, replica scale-down, Vault seal) is restored in a `finally`/fixture-teardown block, proven by Task 1's own spot-check with a deliberately-failing assertion |
| T-11-23 | Tampering | A stale NetworkPolicy silently narrowing production-like traffic after a test run | mitigate | the NetworkPolicy is applied via a transient, test-owned object (never committed to `kubernetes/`), and its name is scoped/labeled uniquely per test run so a leaked one is trivially identifiable and removable |
| T-11-24 | Repudiation | No record of which fault was injected when | accept | this plan relies on pytest's own test output/log as the record; a persistent chaos-run audit trail is out of scope for this phase |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | no new third-party dependency is introduced |
</threat_model>

<verification>
`uv run --group cluster pytest tests/e2e/chaos -q -m cluster` passes for all four scenarios in this
plan.
</verification>

<success_criteria>
4 of QUAL-15's 11 scenarios pass live with guaranteed-restoring fault injection; the shared
scaffolding is ready for plan 11-10 to add the remaining 7.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-09-SUMMARY.md` when done.
</output>
