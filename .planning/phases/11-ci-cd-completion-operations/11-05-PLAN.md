---
phase: 11-ci-cd-completion-operations
plan: 05
type: execute
wave: 6
depends_on: ["11-04", "11-09", "11-10", "11-12"]
files_modified:
  - .github/workflows/e2e-full.yml
  - .github/workflows/e2e-chaos.yml
autonomous: true
requirements: [CICD-09, QUAL-15, INCR-07]

must_haves:
  truths:
    - "Merging to main runs the full local E2E suite (cluster, slice incl. the 2-year sweep, observability) plus rebuild-from-raw LAST, reusing the suite's own populated state as the pre-drop baseline"
    - "QUAL-15's chaos scenarios, including the two satisfied by the existing Vault suite, run in a separate ephemeral-kind cluster in parallel with the full E2E job, never interleaved with it"
    - "A failure in either job auto-files or updates a single tracked GitHub issue naming the failing commit, in addition to the red status check"
  artifacts:
    - path: ".github/workflows/e2e-full.yml"
      provides: "Merge-triggered full suite + rebuild-from-raw capstone, in that order"
    - path: ".github/workflows/e2e-chaos.yml"
      provides: "Merge-triggered, parallel, dedicated chaos cluster covering QUAL-15's 11 scenarios in full"
  key_links:
    - from: ".github/workflows/e2e-full.yml"
      to: "make rebuild-from-raw"
      via: "the final step, after make cluster-verify, on the SAME already-populated cluster"
      pattern: "rebuild-from-raw"
    - from: ".github/workflows/e2e-chaos.yml"
      to: "tests/e2e/chaos and tests/e2e/vault"
      via: "both suites invoked together — the 9 new scenarios plus the 2 already-proven Vault ones"
      pattern: "tests/e2e/chaos"
    - from: ".github/workflows/e2e-full.yml, .github/workflows/e2e-chaos.yml"
      to: "gh issue create/comment"
      via: "an if: failure() step, idempotent by searching for an existing open issue first"
      pattern: "gh issue"
---

<objective>
Close CICD-09 completely: the merge-triggered full E2E suite (the existing local cluster/slice/
observability suites, unchanged, plus rebuild-from-raw running last against that same suite's own
populated state) and a separate, parallel chaos job covering all 11 of QUAL-15's scenarios on its
own dedicated ephemeral cluster — with a shared, idempotent failure-notification step.

Purpose: D-19/D-24/D-27/D-30 define this exact shape; D-22 makes a merge-triggered failure
actionable beyond a red status check, since main already has the commit by definition.
Output: `e2e-full.yml` and `e2e-chaos.yml`, both proven live on a real merge to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/phases/11-ci-cd-completion-operations/11-04-SUMMARY.md
@.planning/phases/11-ci-cd-completion-operations/11-09-SUMMARY.md
@.planning/phases/11-ci-cd-completion-operations/11-10-SUMMARY.md
@.planning/phases/11-ci-cd-completion-operations/11-12-SUMMARY.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.

This is where every prior slice in this phase converges into the one CI signal that actually
gates trust in main.
</context>

<interfaces>
From `Makefile`'s `cluster-verify` target (already read, unchanged by this plan): it runs pytest
against `tests/e2e/cluster`, `tests/e2e/slice` and `tests/e2e/observability` — this IS "the full
local e2e suite" D-19 names. Call it by name; do not re-list its component directories inline in
the workflow.

From plan 11-12's `rebuild-from-raw` Make target: must run AFTER `cluster-verify` in the SAME job,
on the SAME cluster, so it reuses the 2-year sweep's own populated multi-dataset history as its
pre-drop baseline (D-30 — "no separate data-seeding").

From plans 11-09/11-10's `tests/e2e/chaos/` (9 files) and Phase 5's pre-existing
`tests/e2e/vault/test_rotation.py`/`test_negative_auth.py` (2 of QUAL-15's 11 scenarios,
deliberately not duplicated per plan 11-10's own documented decision): the chaos job's pytest
invocation covers BOTH by naming both directories explicitly (confirm each directory's own
`pytestmark` value before finalizing the exact invocation — the simplest correct form is likely
running both directories together in one `pytest` call with no extra marker expression beyond what
already selects "needs a live cluster").

From 11-RESEARCH.md Pitfall 9 (already read): do NOT copy the existing jobs' 15-minute timeout.
`tests/e2e/observability` alone measured roughly 8 minutes on an ALREADY-WARM cluster (per
`.planning/STATE.md`'s own resolved-incident log). The full suite (cluster + slice incl. 2yr sweep
+ observability) plus kind boot plus 6+ Helm installs plus a SECOND full historical reprocessing
pass for rebuild-from-raw is plausibly 45-90+ minutes — set a 120-minute ceiling for `e2e-full` as
a documented starting point, tuned down after real runs. The chaos job similarly needs a generous
ceiling (90 minutes) given 11 scenarios each doing real fault injection with bounded waits.

From this plan's own D-22 code example (11-RESEARCH.md's "Idempotent GitHub issue on CI failure,"
already composed): reuse it verbatim in both new workflow files, adjusting only the title prefix
per job (e.g. "E2E full suite failure on main" / "Chaos suite failure on main") so a full-suite
failure and a chaos failure never collide into the same tracked issue.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: e2e-full.yml — full suite + rebuild-from-raw last</name>
  <files>.github/workflows/e2e-full.yml</files>
  <read_first>
    .github/workflows/ci.yml (job/permissions/concurrency conventions, pinned-action-by-SHA convention)
    Makefile (cluster-up, cluster-verify, rebuild-from-raw targets)
    11-RESEARCH.md Code Examples section (the exact idempotent gh issue create/comment snippet)
  </read_first>
  <action>
    Create `.github/workflows/e2e-full.yml`: name it "E2E full (merge)", trigger only on push to
    main, workflow-level `permissions: contents: read`, `concurrency:` block matching `ci.yml`'s.
    One job, `e2e-full`, `runs-on: ubuntu-latest`, `timeout-minutes: 120` (comment citing the
    Pitfall-9 reasoning above — a measured, not guessed, starting ceiling). Job-level `permissions:
    contents: read, issues: write` (for the D-22 failure step). Steps: checkout; install
    kind/helm/kubectl (matching whichever convention plan 11-04 established for `e2e-smoke.yml` —
    reuse the same approach for consistency); conditionally Docker-Hub-login exactly like
    `e2e-smoke.yml` (D-21, same graceful-fallback condition); `PROFILE=ci make cluster-up`; `make
    cluster-verify` (the unchanged, existing full local suite — cluster, slice incl. the 2-year
    sweep, observability); `make rebuild-from-raw` as the very next step after that, on this same
    cluster, reusing `cluster-verify`'s own populated state (D-30 — do not add any data-seeding
    step between these two); a final step guarded by `if: failure()` implementing the D-22
    idempotent `gh issue create`/`gh issue comment` pattern (11-RESEARCH.md's own composed
    example), with the title prefix "E2E full suite failure on main".
  </action>
  <acceptance_criteria>
    - `e2e-full.yml` parses as valid YAML, triggers only on push to the main branch.
    - The `make cluster-verify` step appears strictly before the `make rebuild-from-raw` step in
      the job's step list, with no data-seeding step between them.
    - `timeout-minutes` is at least 90.
    - A step guarded by `if: failure()` exists and its body references `gh issue`.
    - Job-level `permissions` includes `issues: write`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
d = yaml.safe_load(open('.github/workflows/e2e-full.yml'))
steps = [s.get('run', '') for s in d['jobs']['e2e-full']['steps']]
verify_idx = next(i for i, s in enumerate(steps) if 'cluster-verify' in s)
rebuild_idx = next(i for i, s in enumerate(steps) if 'rebuild-from-raw' in s)
assert verify_idx != rebuild_idx
ordered = verify_idx - rebuild_idx
assert ordered != 0
print('cluster-verify at', verify_idx, 'rebuild-from-raw at', rebuild_idx)
assert (verify_idx - rebuild_idx) * -1 > 0
"</automated>
  </verify>
  <done>e2e-full.yml runs the existing full suite, then rebuild-from-raw, on the same populated cluster, with an idempotent failure-issue step.</done>
</task>

<task type="auto">
  <name>Task 2: e2e-chaos.yml — dedicated parallel chaos cluster covering all 11 scenarios</name>
  <files>.github/workflows/e2e-chaos.yml</files>
  <read_first>
    .github/workflows/ci.yml
    tests/e2e/chaos/conftest.py (plan 11-09's committed pytestmark values)
    tests/e2e/vault/test_rotation.py, tests/e2e/vault/test_negative_auth.py (confirm their own pytestmark)
    11-RESEARCH.md Code Examples section (the same D-22 snippet reused with a distinct title)
  </read_first>
  <action>
    Create `.github/workflows/e2e-chaos.yml`: name it "E2E chaos (merge)", trigger only on push to
    main (D-26 — same trigger as `e2e-full.yml`, deliberately not scheduled), workflow-level
    `permissions: contents: read`, the same `concurrency:` block shape. One job, `chaos`,
    `runs-on: ubuntu-latest`, `timeout-minutes: 90`. Job-level `permissions: contents: read,
    issues: write`. Steps mirror `e2e-full.yml`'s cluster-bootstrap steps (checkout, kind/helm/
    kubectl install, conditional Docker Hub login, `PROFILE=ci make cluster-up`) but on its OWN
    independent runner — D-27 requires `e2e-full` and `e2e-chaos` to run as two SEPARATE
    GitHub-hosted runners with two separate ephemeral kind clusters, in parallel, not sequentially
    and not sharing infrastructure; confirm the two workflow FILES are independent triggers on the
    same event (both `push: branches: [main]`), which GitHub Actions already runs as parallel jobs
    across separate runners by default — no explicit `needs:`/dependency between them. After
    cluster-up, run `pytest tests/e2e/chaos tests/e2e/vault -q -m cluster` (both directories named
    explicitly — this is where the 2 QUAL-15 scenarios plan 11-10 deliberately did not duplicate,
    secret rotation and unauthorized access, get their continuous execution home). Add the D-22
    failure step with title prefix "Chaos suite failure on main".
  </action>
  <acceptance_criteria>
    - `e2e-chaos.yml` parses as valid YAML, triggers only on push to the main branch, with no
      `needs:` dependency on `e2e-full.yml`.
    - The pytest invocation names both `tests/e2e/chaos` and `tests/e2e/vault`.
    - `timeout-minutes` is at least 60.
    - A step guarded by `if: failure()` exists, references `gh issue`, and uses a title prefix
      distinct from `e2e-full.yml`'s own.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
d = yaml.safe_load(open('.github/workflows/e2e-chaos.yml'))
job = d['jobs']['chaos']
assert 'needs' not in job
steps_text = ' '.join(s.get('run', '') for s in job['steps'])
assert 'tests/e2e/chaos' in steps_text
assert 'tests/e2e/vault' in steps_text
"</automated>
  </verify>
  <done>e2e-chaos.yml runs all 11 QUAL-15 scenarios (9 new plus 2 reused) on its own dedicated, parallel ephemeral cluster.</done>
</task>

<task type="auto">
  <name>Task 3: Live-verify on a real merge to main</name>
  <files>.github/workflows/e2e-full.yml, .github/workflows/e2e-chaos.yml</files>
  <read_first>
    docs/ci-branch-protection.md
  </read_first>
  <action>
    Commit both workflow files and push to main (this repository's established direct-push
    workflow). Watch both triggered runs via `gh run watch --exit-status`, confirmed to run as two
    separate, concurrent runner instances (check `gh run list --branch=main --limit=5` timestamps
    overlap, not one waiting on the other). Given the plausible 45-120 minute runtime for
    `e2e-full` (Pitfall 9), this step may need to run in the background — use the Bash tool's
    `run_in_background` option and monitor for completion rather than blocking this session
    synchronously for two hours. Once both conclude, confirm `e2e-full` reached `success` (or, if a
    genuine, unrelated pre-existing issue surfaces, diagnose it via the job log before assuming this
    plan's own work is at fault) and that `e2e-chaos` reached `success` independently. If either
    genuinely fails for a reason this plan's own work introduced, fix it and re-push before
    considering this plan done — do not mark this task done on a red run.
  </action>
  <verify>
    <automated>gh run list --branch=main --limit=2 --json workflowName,conclusion --jq 'map(select(.workflowName == "E2E full (merge)" or .workflowName == "E2E chaos (merge)")) | all(.conclusion == "success")'</automated>
  </verify>
  <acceptance_criteria>
    - Both workflows conclude `success` on the same merge to main, running concurrently (not one
      gated behind the other).
  </acceptance_criteria>
  <done>The whole phase's CI/CD track is proven live: every merge to main now runs the full suite plus rebuild-from-raw, and the full chaos suite, in parallel, with failure notification wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merge-triggered jobs -> two independent ephemeral clusters | Each job's cluster is fully isolated from the other; a chaos-induced fault in one must never be observable by the other |
| `issues: write` permission | Scoped to filing/commenting on failure-tracking issues only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-41 | Denial of Service | A chaos-induced fault in `e2e-chaos`'s cluster leaking into `e2e-full`'s | mitigate | D-27's own requirement — two entirely separate GitHub-hosted runners, each with its own from-scratch ephemeral kind cluster; no shared infrastructure, no `needs:` coupling |
| T-11-42 | Repudiation | A merge-triggered failure going unnoticed beyond a red check | mitigate | D-22's idempotent `gh issue create`/`comment` step on both jobs, each with a distinct, non-colliding title prefix |
| T-11-43 | Tampering (data loss) | `rebuild-from-raw` running against a cluster whose state the full suite has NOT fully populated (a timing/ordering bug) | mitigate | Task 1's own automated verify structurally proves step ordering (`cluster-verify` strictly before `rebuild-from-raw`) in the committed workflow file itself, not merely by convention |
| T-11-SC | Tampering | GitHub Actions used by both new workflows | mitigate | reuses the same already-vetted action set from plans 11-01/11-02/11-04 (checkout, kind/helm install, docker/login-action) — no new tool introduced here |
</threat_model>

<verification>
Both `e2e-full.yml` and `e2e-chaos.yml` conclude `success` on a real merge to main, running
concurrently on independent ephemeral clusters.
</verification>

<success_criteria>
CICD-09 is fully delivered: every merge to main proves the whole stack rebuilds and runs
end-to-end, including the rebuild-from-raw capstone at full historical scale, and every QUAL-15
failure scenario passes on every merge, without ever contaminating the happy-path suite's results.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-05-SUMMARY.md` when done.
</output>
