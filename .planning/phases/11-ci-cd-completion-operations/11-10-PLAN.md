---
phase: 11-ci-cd-completion-operations
plan: 10
type: execute
wave: 2
depends_on: ["11-09"]
files_modified:
  - tests/e2e/chaos/test_malformed_csv.py
  - tests/e2e/chaos/test_invalid_encoding.py
  - tests/e2e/chaos/test_oom.py
  - tests/e2e/chaos/test_task_timeout.py
  - tests/e2e/chaos/test_duplicate_batch.py
autonomous: true
requirements: [QUAL-15]

must_haves:
  truths:
    - "A malformed CSV dropped into a live pipeline quarantines the bad rows and loads the good ones, never silently dropping or crashing the run"
    - "An unexpected-encoding file is detected, reported with a confidence score, and never silently mis-decoded"
    - "A pod that exceeds its memory limit is OOMKilled cleanly and Airflow's own retry/failure semantics take over, with no corrupted partial state"
    - "A task that exceeds its configured timeout fails cleanly and is retried or marked failed per its own DAG-declared policy"
    - "A genuinely duplicate batch (not just a duplicate file) is detected and does not produce duplicate or corrupted target rows"
  artifacts:
    - path: "tests/e2e/chaos/test_oom.py"
      provides: "Live reproduction of the known publish-task OOM class (10-07-PLAN.md's own observed OOMKilled finding) as a permanent regression proof"
  key_links:
    - from: "tests/e2e/chaos/test_malformed_csv.py"
      to: "tests/fixtures/csv corpus"
      via: "an existing malformed fixture from the QUAL-08 edge-case corpus, not a bespoke one-off file"
      pattern: "tests/fixtures/csv"
    - from: "tests/e2e/chaos/test_oom.py"
      to: "container_resources memory limit"
      via: "a deliberately-undersized KPO resource override for one task run"
      pattern: "memory"
---

<objective>
Complete QUAL-15's chaos suite with the remaining data/resource-shaped scenarios: malformed CSV,
invalid encoding, OOM, task timeout, and duplicate batch. Two of README §84's 11 named scenarios —
secret rotation and unauthorized secret access — are DELIBERATELY NOT duplicated here: Phase 5
already built `tests/e2e/vault/test_rotation.py` and `test_negative_auth.py` as live,
passing proofs of exactly these two scenarios, and those tests are not currently wired into any
continuously-run CI suite (only a dedicated, separately-invoked `make vault-verify`). Plan 11-05's
chaos workflow includes `tests/e2e/vault` alongside `tests/e2e/chaos` for this reason — see that
plan's Task 1. This is a documented reuse decision, not a dropped requirement.

Purpose: closes QUAL-15's remaining data/resource scenarios with genuinely new coverage, while
explicitly avoiding rebuilding what Phase 5 already proved.
Output: 5 new chaos test files, run live; a documented, correct non-duplication decision for the
remaining 2 scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ci-cd-completion-operations/11-CONTEXT.md
@.planning/phases/11-ci-cd-completion-operations/11-RESEARCH.md
@.planning/phases/11-ci-cd-completion-operations/11-09-SUMMARY.md

## Phase Goal

**As a** platform operator who did not build this system, **I want to** trust that the whole
environment provably rebuilds from the repository, every catalogued failure mode has a passing
test, and CI gates every change end-to-end, **so that** I can operate, recover and extend the
platform without relying on tribal knowledge.
</context>

<interfaces>
From `tests/e2e/chaos/conftest.py` (plan 11-09's output — read the actual file): reuse its
fault-injection and cluster fixtures; do not duplicate `kubectl_context`/`kubectl`/`s3_client`.

From `tests/fixtures/corpus.yaml` and `tests/fixtures/csv/` (the QUAL-08 edge-case corpus, already
generated from a seed — read `tests/fixtures/corpus.yaml` to find an already-cataloged malformed
and an already-cataloged wrong-encoding fixture): reuse an EXISTING corpus fixture for
`test_malformed_csv.py`/`test_invalid_encoding.py` rather than authoring a new one-off broken file
— the corpus IS the specification (QUAL-08), and duplicating its content defeats that purpose.

From `airflow/dags/csv_ingest_customers.py`'s own comment on the `publish` task (already read):
"Live-observed OOMKilled (exit_code 137) at 256Mi during this plan's own live sweep" — this is a
REAL, already-diagnosed OOM incident (10-07-PLAN.md). `test_oom.py` should deliberately reproduce
this class of failure (override a task's `container_resources` memory limit down to a size known
to be insufficient for its real workload) rather than inventing an artificial trigger, then confirm
Airflow's own failure/retry semantics take over cleanly with no partial/corrupted state — this
directly matches ORCH-04's own "explicit retry and failure behaviour" requirement.

From `airflow/dags/_common/kpo.py`'s own comment on `startup_timeout_seconds=120` (already read):
the project's own established understanding of one class of timeout. `test_task_timeout.py` should
target `execution_timeout` (the KPO/task-level ceiling on how long a task may run) — set a
deliberately-tiny value for one throwaway task invocation and confirm Airflow marks it failed (or
retries, per that task's own declared `retries`) rather than hanging indefinitely.

From `tests/e2e/slice/test_smoke_and_idempotency.py` and `tests/e2e/slice/test_backfill_reentry.py`
(read both, or at least their module docstrings and test names) and `.planning/STATE.md`'s
`260817-umv` Quick Task entry (an `AlreadyRunningBackfill` collision race, already fixed): before
writing `test_duplicate_batch.py`, confirm whether these already prove "duplicate batch" fully.
If they do, `test_duplicate_batch.py` should target the angle they do NOT cover — most likely two
CONCURRENT (not sequential) triggers racing to claim the same batch — rather than re-proving
sequential re-upload idempotency a second time.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: malformed_csv + invalid_encoding + duplicate_batch</name>
  <files>tests/e2e/chaos/test_malformed_csv.py, tests/e2e/chaos/test_invalid_encoding.py, tests/e2e/chaos/test_duplicate_batch.py</files>
  <read_first>
    tests/fixtures/corpus.yaml
    tests/e2e/slice/test_smoke_and_idempotency.py
    tests/e2e/slice/test_backfill_reentry.py
    .planning/STATE.md (260817-umv Quick Task entry)
    tests/e2e/chaos/conftest.py
  </read_first>
  <action>
    Write `test_malformed_csv.py`: upload an existing malformed fixture from
    `tests/fixtures/csv/` (identify one via `corpus.yaml`'s own manifest — a structurally broken
    row shape, e.g. wrong column count or unclosed quote) into a live dataset's raw prefix, drive
    (or wait for) a real ingestion run, and assert: the run completes (not FAIL_FILE for this
    fixture's configured strategy — confirm the target dataset's `quality:`/structural-validation
    strategy first), the bad row(s) land in `meta.rejected_records` with a row number and error
    type, and the good rows in the same file still load. Reuse `VALID-01`'s existing structural
    validation machinery — write no new parsing/validation code, only the live-drive+assert
    harness.

    Write `test_invalid_encoding.py`: upload an existing wrong-encoding fixture (e.g. a
    Windows-1250/1252 file from the corpus) and assert the run's recorded encoding-detection
    confidence and chosen encoding (queryable via `meta.files`/the batch's own diagnostic output)
    match the fixture's known-correct encoding — proving detection, not a silent mis-decode.

    Write `test_duplicate_batch.py` per the interfaces block's decision framework: if genuine new
    coverage is needed (most likely: two concurrent triggers of the identical batch), drive that
    scenario live (e.g. two near-simultaneous `airflow dags trigger`/backfill invocations for the
    same file/window) and assert exactly one logical batch is ever claimed/loaded — reuse the
    `UNIQUE (dataset, batch_key)` ledger constraint (LOAD-08) as the mechanism under test, not
    something to reimplement.
  </action>
  <acceptance_criteria>
    - All three tests pass live.
    - `test_malformed_csv.py` and `test_invalid_encoding.py` each cite the specific corpus fixture
      filename they reuse in a comment, and neither adds a new fixture file.
    - `test_duplicate_batch.py`'s docstring states which existing test(s) it deliberately does not
      duplicate and what angle is genuinely new here.
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/chaos/test_malformed_csv.py tests/e2e/chaos/test_invalid_encoding.py tests/e2e/chaos/test_duplicate_batch.py -q -m cluster</automated>
  </verify>
  <done>Three data-integrity chaos scenarios pass live, all reusing existing corpus fixtures and existing correctness mechanisms rather than inventing parallel ones.</done>
</task>

<task type="auto">
  <name>Task 2: oom + task_timeout, live-verify all seven of this plan's scenarios</name>
  <files>tests/e2e/chaos/test_oom.py, tests/e2e/chaos/test_task_timeout.py</files>
  <read_first>
    airflow/dags/csv_ingest_customers.py (the publish task's own OOM comment and _STAGE_RESOURCES/_DISCOVER_RESOURCES profiles)
    airflow/dags/_common/kpo.py (common_kpo_kwargs, on_finish_action: delete_pod comment)
  </read_first>
  <action>
    Write `test_oom.py`: construct a throwaway KubernetesPodOperator task invocation (or a small,
    standalone Pod manifest running the same `dataplat` entrypoint against a workload known to need
    more memory than a deliberately-undersized limit) with `container_resources` memory limit set
    below the already-observed OOM threshold (256Mi, per the real 10-07 finding), run it, and
    assert: the pod is OOMKilled (exit code 137, `kubectl get pod ... -o
    jsonpath='{.status.containerStatuses[0].lastState.terminated.reason}'` == `OOMKilled`), the
    Airflow task instance reaches a clean failed/up_for_retry state (not stuck), and — critically —
    no partial/corrupted rows are visible in the target table after the OOM (the publication
    transaction's own all-or-nothing guarantee, META-03, holds under this failure mode too).

    Write `test_task_timeout.py`: run a throwaway task with a deliberately tiny
    `execution_timeout` against a workload that will not finish in time, confirm Airflow marks it
    failed with an `AirflowTaskTimeout`-shaped signal (not a hang), and confirm it either retries
    per its own `retries` config or reaches a clean terminal failed state.

    Run the full plan live: `uv run --group cluster pytest tests/e2e/chaos -q -m cluster` (all nine
    files created across plans 11-09 and 11-10) and confirm every scenario passes together in one
    pass, not just individually — a fault from one test leaking into the next is exactly the
    contamination risk D-25 exists to prevent.
  </action>
  <acceptance_criteria>
    - Both new tests pass live.
    - `uv run --group cluster pytest tests/e2e/chaos -q -m cluster` passes end to end with all 9
      files from plans 11-09+11-10 collected and green in a single run.
    - `test_oom.py` explicitly asserts zero partial/corrupted rows post-OOM, not merely that the
      task failed.
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/chaos -q -m cluster</automated>
  </verify>
  <done>All 9 chaos test files (this plan's 5 plus plan 11-09's 4) pass together in one live run, with the 2 remaining QUAL-15 scenarios (secret rotation, unauthorized access) explicitly and correctly deferred to plan 11-05's inclusion of the existing tests/e2e/vault suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deliberately-undersized resource limits (OOM test) -> shared cluster capacity | A test that forces an OOM must not starve unrelated concurrently-running workloads on the same constrained kind nodes (see project memory on CPU/memory starvation history) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-25 | Denial of Service | `test_oom.py`'s undersized pod | mitigate | the throwaway pod's CPU request stays at a normal profile — only memory LIMIT is undersized — so it cannot itself starve other pods' CPU scheduling the way this project's own documented historical incidents did |
| T-11-26 | Tampering (data loss) | Duplicate-batch race condition | mitigate | the test proves the EXISTING `UNIQUE (dataset, batch_key)` constraint (LOAD-08) rejects the loser of a genuine race, rather than introducing a new, untested concurrency mechanism |
| T-11-27 | Repudiation | Malformed/invalid-encoding fixtures reused across environments | accept | fixtures are the committed, seed-generated QUAL-08 corpus — deterministic and already reviewed; no new untrusted input is introduced |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | no new third-party dependency is introduced |
</threat_model>

<verification>
`uv run --group cluster pytest tests/e2e/chaos -q -m cluster` passes for all 9 files across plans
11-09 and 11-10.
</verification>

<success_criteria>
9 of QUAL-15's 11 scenarios have new, live, passing tests; the remaining 2 are correctly satisfied
by an already-existing, now-continuously-wired Phase 5 suite (plan 11-05), documented explicitly
rather than silently assumed.
</success_criteria>

<output>
Create `.planning/phases/11-ci-cd-completion-operations/11-10-SUMMARY.md` when done.
</output>
