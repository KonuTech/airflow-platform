---
phase: 05-vault-secrets-workload-identity
verified: 2026-08-14T21:24:03Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/13
  gaps_closed:
    - "Development secrets are reproducible when rebuilding the local environment from scratch (ROADMAP SC5 / SEC-13's literal wording) — CR-01 (scripts/vault-bootstrap.py sourcing credential VALUES from three already-deleted Kubernetes Secrets) and CR-02 (_ensure_policy never re-applying a drifted policy body) both fixed by gap-closure plan 05-06, independently re-verified live in this session (not merely re-reading 05-06-SUMMARY.md's narrative)."
  gaps_remaining: []
  regressions: []
---

# Phase 5: Vault Secrets & Workload Identity Verification Report

**Phase Goal:** Vault is the only source of runtime credentials, and workload identity is real enough that an unauthorized service account is provably denied
**Verified:** 2026-08-14T21:24:03Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 05-06, plus a separate resolved debug session for an unrelated scheduler fault)

## Verification Approach Note

ROADMAP.md sets `**Mode:** mvp` on Phase 5 (confirmed via `gsd-sdk query roadmap.get-phase 5`), but the phase goal does not match the User Story format (`As a ..., I want to ..., so that ....`) — this is a uniform, repository-wide default rather than a deliberate per-phase choice for an infra/security phase. Consistent with the prior verification pass's own reasoning (re-confirmed here, not merely carried forward), this report proceeds with **standard goal-backward verification** against ROADMAP's five numbered Success Criteria and the PLAN.md frontmatter `must_haves` blocks (05-01 through 05-06), not the MVP User Flow Coverage format.

## Re-Verification Context

The prior pass (`05-VERIFICATION.md`, 2026-08-14T17:13:25Z) found **12/13 must-haves verified**, with one FAILED truth: SEC-13's "reproducible when rebuilding the local environment from scratch" clause, root-caused to `scripts/vault-bootstrap.py`'s `_ensure_etl_secrets`/`_ensure_airflow_secrets` sourcing Vault KV credential VALUES from three Kubernetes Secrets that this same phase's own plans (05-02, 05-03) had already deleted (CR-01), plus a related dormant defect where `_ensure_policy` never re-applied a drifted policy body (CR-02).

Gap-closure plan `05-06` fixed both defects, added an offline regression test (`tests/unit/test_vault_bootstrap.py`), and ran a live-cluster proof (scoped Vault-release-and-PVC reinstall). That live proof surfaced a **separate, unrelated infrastructure fault** — a Docker Desktop/WSL2-level restart had broken the DAGs `hostPath` mount on all 3 kind nodes, freezing Airflow's scheduler cluster-wide via `DagModel.is_stale`. This was diagnosed and fixed via a dedicated `/gsd:debug` session (`.planning/debug/resolved/dagrun-scheduler-stall.md`), independently confirmed resolved.

Per the user's explicit instruction, this verification does **not** take 05-06-SUMMARY.md's "Final Status" narrative at face value. Every claim below was independently re-derived from the live cluster, the current code, and direct database queries, in this session — not by re-reading prior reports.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | [SC1/SEC-05] With the Airflow metadata-DB connection's env var unset and no `minio_default` DB row, DAGs still resolve connections through Vault's `VaultBackend` and run to a genuine terminal status | ✓ VERIFIED | Ran live, fresh, this session: `uv run --frozen --group cluster pytest tests/e2e/vault/test_airflow_backend.py -v -m cluster` — **4/4 passed**, including `test_dag_still_resolves_its_connection_and_runs` (the one test that was still failing when 05-06-SUMMARY.md was written, for a self-resolving backlog-depth reason it explicitly disclosed — re-run here shows it now passes cleanly). Independently corroborated by a direct `psql` query against the live analytical DB (not trusting the SUMMARY's cited numbers): `meta.ingestion_runs` shows **27 SUCCEEDED, 0 FAILED, 1 PENDING** as of this session, with `run_id=5127` confirmed `SUCCEEDED` at `2026-08-14 20:09:50.202112+00` — matching 05-06-SUMMARY.md's claim exactly, independently reproduced by my own query, not copied from the report. |
| 2 | [SC2/SEC-06/SEC-07] `csv-processor` (namespace `etl`) authenticates via Kubernetes auth and reads exactly its own two Vault KV paths | ✓ VERIFIED | Ran live, fresh: `pytest tests/e2e/vault/test_positive_auth.py -m cluster` — 1 passed (part of a full 16/16 fresh run, see below). Live `vault read auth/kubernetes/role/csv-processor` (via `kubectl exec -n vault vault-0`, this session): `bound_service_account_names=[csv-processor]`, `bound_service_account_namespaces=[etl]`, `policies=[csv-processor]`; live `vault policy read csv-processor`: exactly two `read`-only paths (`etl/data/analytics-db`, `etl/data/minio`) — byte-identical to the prior pass, no regression. Live audit-tail (`python scripts/vault-audit-tail.py --lines 15`) shows a real, current `etl:csv-processor` login and both KV reads succeeding seconds before this check. |
| 3 | [SC2/SEC-12] The `default` ServiceAccount (and, as a stretch case, a different-namespace `airflow-scheduler` SA) is denied login against the `csv-processor` Vault role, by an automated test | ✓ VERIFIED | Ran live, fresh: `pytest tests/e2e/vault/test_negative_auth.py -m cluster` — 2 passed (part of the fresh 16/16 run). |
| 4 | [SC3/INFRA-06] Vault runs as a persistent, non-dev StatefulSet; a `vault-0` restart reseals it without data loss; `make vault-unseal` alone restores service | ✓ VERIFIED | Live `vault status` (this session): `Storage Type: file`, `Sealed: false`, `Total Shares/Threshold: 1/1` (Shamir, not dev). **Stronger evidence than the prior pass**: `vault-0`'s pod AGE/restart count (`1 (37m ago)`, 130m total age at check time) confirms a *real* restart occurred since the 05-06 gap-closure session (the debug session's own worker2/control-plane node restarts, which incidentally bounced co-located Vault) — and Vault's live `Cluster ID` (`243eafb8-3395-e561-671c-1164e094518e`) matches the ID the debug session's own resolution log cites as the *same, recovered* instance, not a fresh re-init — direct, independent confirmation that data survived a real restart, not merely a staged one. `tests/e2e/vault/test_unseal_survives_restart.py` (this truth's dedicated proof) was **actually executed** in this pass's fresh 16/16 run — the prior pass only read it in full and declined to re-run it as "redundant." |
| 5 | [SC4/SEC-08] Vault's audit log shows which workload read which path, when, and whether it succeeded, with no secret values present in the log; `make vault-audit-tail` renders it human-readably | ✓ VERIFIED | Ran live, fresh: `pytest tests/e2e/vault/test_audit_log.py -m cluster` — 3 passed (part of the fresh 16/16 run). Also ran `python scripts/vault-audit-tail.py --lines 15` directly this session: output shows real, current login/read entries (`auth/kubernetes/login`, `etl:csv-processor`, `airflow:airflow-worker`, `airflow:airflow-api-server`) with identity and outcome, zero raw JSON, zero secret values. |
| 6 | [SC5/SEC-01/SEC-03/SEC-04] No credential literal exists in any script or Helm values file for this phase's migrated credentials; the three legacy Kubernetes Secrets no longer exist anywhere in the live cluster | ✓ VERIFIED (asterisk from the prior pass now lifted) | Live `kubectl get secret -n etl csv-processor-db csv-processor-s3` and `-n airflow airflow-minio-connection`: all three `NotFound`, re-confirmed this session. Repo-wide grep for the three legacy Secret names finds them only in test files asserting absence, the regression guard itself, and historical/past-tense comments (`scripts/stages/75-etl.sh`, `helm/values/{local,ci}/airflow.yaml` — see Anti-Patterns for one now-stale comment). `pytest tests/policy/test_workflow_secrets.py tests/policy/test_no_stale_secrets.py` — **21/21 passed**, run fresh this session. The prior pass's asterisk on SEC-01 ("the underlying bootstrap tooling cannot itself be reconstituted from empty state") is now lifted: that was CR-01, fixed by plan 05-06 and independently re-verified below (truth 7). |
| 7 | [SC5/SEC-13] Development secrets (`.secrets/vault-init.json`) are marked, never committed, isolated from production, **and reproducible when rebuilding the local environment from scratch** | ✓ VERIFIED (with one disclosed scope caveat) | **This is the previously-FAILED truth. Independently re-verified from first principles, not by trusting 05-06-SUMMARY.md's narrative:** (1) Direct code read of the current `scripts/vault-bootstrap.py` confirms `_ensure_etl_secrets`/`_ensure_airflow_secrets` no longer reference `csv-processor-db`, `csv-processor-s3`, or `airflow-minio-connection` anywhere — `grep -n "csv-processor-db\|csv-processor-s3\|airflow-minio-connection\|_DB_SECRET_NAME\|_S3_SECRET_NAME\|_AIRFLOW_MINIO_SECRET_NAME" scripts/vault-bootstrap.py` returns zero matches; the only remaining references to `etl-secrets.sh` are explicitly historical (`now-deleted scripts/etl-secrets.sh`, `git show 6d86cb8:...`). `etl/analytics-db`'s DSN is now generated via a fresh `kubectl exec`-driven `ALTER ROLE etl_app WITH PASSWORD ...` against the CNPG primary (via new `_kubectl_cluster_primary_pod`/`_kubectl_exec_psql` helpers, password passed via stdin never argv); `etl/minio` and `airflow/connections/minio_default` are sourced from the live `data/minio-app` Secret (confirmed live: this Secret exists with exactly one key, `secretKey`). (2) Ran `uv run pytest tests/unit/test_vault_bootstrap.py -v` myself, fresh: **7/7 passed**, fully offline/mocked. (3) Ran `uv run ruff check` + `uv run mypy scripts/vault-bootstrap.py`: clean. (4) Ran the full `make check` gate myself, twice, fresh: 124 policy tests + 150 unit/regression tests + 70 fixture-digest checks, all passed, exit 0. (5) Ran the **full live e2e Vault suite** myself, fresh, this session: `pytest tests/e2e/vault -q -m cluster` — **16/16 passed** (stronger than 05-06-SUMMARY.md's own cited 15/16 — the one test that was failing when that report was written now passes, consistent with its own "self-resolving backlog depth" explanation). (6) Independently read Vault's live KV metadata (`vault kv metadata get etl/analytics-db` / `etl/minio` / `-mount=airflow connections/minio_default` via `kubectl exec -n vault vault-0`): all three show `created_time` timestamps of `2026-08-14T18:55:0Xz` — consistent with a genuinely fresh bootstrap (not stale, pre-05-06 data), and `airflow/connections/minio_default` shows 3 versions (consistent with the live rotation test's own rotate-then-restore cycle having run against this same fresh instance). (7) Independently queried `meta.ingestion_runs` directly via `psql`: confirmed `run_id=5127, status=SUCCEEDED, started_at=2026-08-14 20:09:50` exists — proving a real pipeline run completed end-to-end on the freshly-generated `etl_app`/MinIO credentials, exactly as claimed. **One disclosed scope caveat, carried forward honestly rather than silently dropped:** the proof was a *scoped* Vault-release-and-PVC reinstall (delete `data-vault-0`/`audit-vault-0` + pod `vault-0` in namespace `vault` only), not a literal `make cluster-down && make cluster-up` full teardown — `docs/secrets-architecture.md` §6 (read in full this session) explicitly and accurately discloses this, does not overclaim, and argues the code-path equivalence (vault-bootstrap.py cannot distinguish "whole cluster rebuilt" from "only Vault's storage wiped" — both present an identical empty-KV-store precondition, the only thing its logic branches on). I independently assessed this argument and find it sound: `_kubectl_cluster_primary_pod`/`_kubectl_get_secret_field` read *live* cluster state at bootstrap time regardless of that state's age, and fail closed (raise `RuntimeError`) rather than silently succeed if a dependency (CNPG primary, `minio-app` Secret) isn't ready yet — the same fail-closed behavior a full rebuild would also exercise. Given the offline mocked unit test (deterministic, cluster-state-independent) plus this live, real proof plus the sound equivalence argument, the literal wording is satisfied in substance; the untested slice is the specific *mechanism* used to reach an empty Vault, not the credential-population logic itself. |
| 8 | [05-01] `make vault-bootstrap` is idempotent against an already-configured Vault: re-running performs zero additional writes | ✓ VERIFIED | `tests/e2e/vault/test_dev_secrets_reproducible.py::test_rerunning_vault_bootstrap_against_a_live_vault_changes_nothing` passed in the fresh 16/16 run this session. |
| 9 | [05-02] `resolve_secret()` interprets `vault://mount/path#field`, authenticates once per process, fails closed on malformed refs / hvac errors / missing fields | ✓ VERIFIED | `tests/unit/test_secrets_resolver.py` included in the fresh `make check` run (150 unit/regression tests passed, zero failures). Live-exercised for real by truths 1–2 above (same code path a real KPO pod and `test_positive_auth.py` both use, re-run fresh this session). No code changes to `resolver.py` since the prior pass — regression-checked, unchanged. |
| 10 | [05-03] The Vault `airflow` role's `bound_service_account_names` reflects empirically-justified identities, not an uncorrected guess | ✓ VERIFIED | Live `vault read auth/kubernetes/role/airflow` (this session): `bound_service_account_names=[airflow-api-server airflow-triggerer airflow-worker airflow-scheduler]` — unchanged from the prior pass, no regression. |
| 11 | [05-04/SEC-09] Rotating `minio_default`'s value in Vault is reflected on Airflow's very next read, with no pod restart | ✓ VERIFIED | `tests/e2e/vault/test_rotation.py::test_rotating_minio_default_is_observed_with_no_restart` passed in the fresh 16/16 run this session. |
| 12 | [05-05] A permanent regression guard prevents the three migrated Secret names from ever again being a creation target | ✓ VERIFIED | `pytest tests/policy/test_no_stale_secrets.py -v` run directly, fresh, this session — 4/4 passed. Wired into `make check`/`make policy` (confirmed present in the fresh full `make check` run's 124-test policy total). |
| 13 | [05-05/SEC-14] The secrets architecture is documented end-to-end in one place (injection, trust boundaries, production substitution) | ✓ VERIFIED (caveat from the prior pass now resolved) | `docs/secrets-architecture.md` exists (318 lines, grew from 263), 10 numbered/named sections confirmed via `grep -n "^## \|^### "`. Section 6 (SEC-13/production substitution) was read in full this session: it now accurately, honestly cites exactly what plan 05-06 proved (scoped reinstall, 15-then-16/16 e2e result, the separate scheduler fault and its fix, the residual "full cluster-down/up not independently re-run" limit) — no overclaiming found. The prior pass's caveat ("§6's clean-checkout reproducibility claim is currently inaccurate") is resolved: the document was rewritten, not merely re-asserted. |

**Score:** 13/13 truths verified (prior FAILED truth 7 is now VERIFIED with one disclosed, non-blocking scope caveat)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/vault-bootstrap.py` | Idempotent hvac bootstrap, credential sourcing that survives an empty-Vault rebuild | ✓ VERIFIED | 824 lines (was 628 before 05-06). Read in full this session. `_ensure_etl_secrets`/`_ensure_airflow_secrets` rewritten (live `kubectl exec`+`ALTER ROLE` for the analytics DSN, live `data/minio-app` Secret for MinIO creds); `_ensure_policy` rewritten to detect and re-apply policy-body drift (CR-02). No dependency on any deleted Secret — grep-confirmed zero matches. |
| `tests/unit/test_vault_bootstrap.py` | Offline, mocked regression guard for the empty-Vault bootstrap path and CR-02 | ✓ VERIFIED | 320 lines (plan required ≥120). 7 test functions, all match the plan's `<behavior>` spec (password-generation+DSN-write, skip-when-present, minio-source correctness, conn_uri assembly, policy-drift reapply, policy-no-drift skip, dead-constant non-vacuity). Ran fresh this session: 7/7 passed. |
| `docs/secrets-architecture.md` | Corrected, accurately-cited SEC-13 reproducibility claim | ✓ VERIFIED | 318 lines. §6 rewritten with the accurate, appropriately-scoped account (see truth 13). |
| `.planning/phases/05-vault-secrets-workload-identity/05-VALIDATION.md` | Updated SEC-13 verification mapping | ✓ VERIFIED | Read in full. "Per-Task Verification Map" has new T1/T2 rows for plan 05-06 with concrete evidence citations; "Manual-Only Verifications" SEC-13 row rewritten to reflect the partial-but-substantive live proof, honestly scoped. |
| All 17 artifacts from plans 05-01–05-05 (Vault Helm values, unseal/bootstrap scripts, resolver.py, KPO wiring, e2e test suite, audit-tail, policy guard, ADR-0009) | Unchanged since the prior pass's full verification | ✓ VERIFIED (regression check) | No code changes to these files since the prior pass except `scripts/vault-bootstrap.py` (covered above) and `docs/secrets-architecture.md` (covered above). Full `make check` + full live e2e suite re-run fresh, zero regressions found. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scripts/vault-bootstrap.py::_ensure_etl_secrets` | Kubernetes Secret `data/minio-app` | `_kubectl_get_secret_field(..., name="minio-app", key="secretKey")` | ✓ WIRED | Confirmed by direct code read; live `kubectl get secret -n data minio-app` confirms the Secret exists with exactly key `secretKey`. |
| `scripts/vault-bootstrap.py::_ensure_etl_secrets` | CNPG `Cluster` `data/analytics-db`'s primary pod | `_kubectl_exec_psql` running `ALTER ROLE etl_app WITH PASSWORD ...` | ✓ WIRED | Confirmed by direct code read (SQL text, stdin-only, `_ANALYTICS_APP_ROLE="etl_app"`). Live-exercised: `etl/analytics-db`'s KV metadata shows a fresh `created_time` consistent with this path having executed. |
| `tests/unit/test_vault_bootstrap.py` | `scripts/vault-bootstrap.py` | `importlib.util.spec_from_file_location`/`module_from_spec`/`exec_module` | ✓ WIRED | Confirmed present (`grep -n "spec_from_file_location\|module_from_spec\|exec_module"`) and functionally proven — the test suite runs and passes against the real file. |
| `airflow/dags/_common/kpo.py` | `packages/dataplat/.../resolver.py` | `value="vault://etl/...#..."`, resolved inside the pod | ✓ WIRED (regression check) | Unchanged since prior pass; corroborated by fresh `meta.ingestion_runs` SUCCEEDED rows (27 now, up from 23 at the prior pass). |
| `scripts/vault-bootstrap.py` | Vault's `csv-processor`/`airflow` Kubernetes-auth roles and policies | `create_role`/`create_or_update_policy` | ✓ WIRED (regression check) | Live-read this session: role/policy bindings byte-identical to the prior pass's citations. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `csv_processor.cli._build_common()` | `dsn`, `access_key`, `secret_key` | `resolve_secret(resolve_secret("env://DATAPLAT_DB_DSN"))` → `vault://etl/analytics-db#dsn` | Yes — independently re-confirmed this session via a direct `psql` query (`meta.ingestion_runs`: 27 SUCCEEDED, 0 FAILED, 1 PENDING, most recent success seconds before this check; a live `discover`/`build-ingest-args` task pod was observed actually running during this verification session) | ✓ FLOWING |
| Airflow `minio_default` Connection | resolved by `BaseHook.get_connection` | `VaultBackend` → `airflow/connections/minio_default#conn_uri` | Yes — live audit-tail this session shows `airflow:airflow-worker`/`airflow:airflow-api-server` reads of `airflow/data/connections/*` seconds before the check, and the fresh e2e `test_airflow_backend.py` run (4/4) confirms the DAG's sensor and full run actually complete using it | ✓ FLOWING |
| `scripts/vault-bootstrap.py::_ensure_etl_secrets`/`_ensure_airflow_secrets` (the previously-broken path) | `etl/analytics-db`, `etl/minio`, `airflow/connections/minio_default` | Live `kubectl exec`+`ALTER ROLE` / live `data/minio-app` Secret | Yes — this session's own live `vault kv metadata get` reads show genuinely fresh `created_time` values for all three paths, and the unit test suite proves the generation logic deterministically, offline | ✓ FLOWING |

### Behavioral Spot-Checks

All checks below were run live against the actual cluster during **this** verification session (not inherited from any SUMMARY.md or prior VERIFICATION.md).

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Vault is unsealed, non-dev, file-backed, same instance across a real restart | `kubectl exec -n vault vault-0 -- vault status` | `Sealed: false`, `Storage Type: file`, `Cluster ID: 243eafb8-...` (matches the debug session's own cited recovered-instance ID) | ✓ PASS |
| Three legacy Secrets are gone | `kubectl get secret -n etl csv-processor-db csv-processor-s3`; `-n airflow airflow-minio-connection` | All three `NotFound` | ✓ PASS |
| `scripts/vault-bootstrap.py` has no remaining reference to deleted Secrets/constants | `grep -n "csv-processor-db\|csv-processor-s3\|airflow-minio-connection\|_DB_SECRET_NAME\|_S3_SECRET_NAME\|_AIRFLOW_MINIO_SECRET_NAME" scripts/vault-bootstrap.py` | Zero matches | ✓ PASS |
| New offline unit-test regression guard passes | `uv run pytest tests/unit/test_vault_bootstrap.py -v` | 7/7 passed | ✓ PASS |
| Lint/type-check clean on the fixed file | `uv run ruff check scripts/vault-bootstrap.py tests/unit/test_vault_bootstrap.py && uv run mypy scripts/vault-bootstrap.py` | Both clean | ✓ PASS |
| Full offline gate | `make check` (run twice, independently) | 124 policy + 150 unit/regression + 70 fixture-digest checks, exit 0, both times | ✓ PASS |
| Phase-5-specific policy tests | `pytest tests/policy/test_no_stale_secrets.py tests/policy/test_workflow_secrets.py tests/policy/test_values_profiles.py tests/policy/test_gates_actually_fail.py tests/policy/test_dag_thinness.py tests/policy/test_no_manual_kubectl_surgery.py tests/policy/test_offline_gate_stays_offline.py -v` | 56 passed | ✓ PASS |
| Full live e2e Vault suite | `uv run --frozen --group cluster pytest tests/e2e/vault -v -m cluster` | **16/16 passed** (stronger than 05-06-SUMMARY.md's own cited 15/16) | ✓ PASS |
| Live Vault KV metadata shows genuinely fresh secret creation | `vault kv metadata get etl/analytics-db` / `etl/minio` / `-mount=airflow connections/minio_default` (via `kubectl exec vault-0`) | All three `created_time ≈ 2026-08-14T18:55:0Xz`; `airflow/connections/minio_default` at version 3 (consistent with a rotate-then-restore having run) | ✓ PASS |
| Live role/policy bindings unchanged, least-privilege | `vault policy read csv-processor` / `airflow`; `vault read auth/kubernetes/role/csv-processor` / `airflow` | Exact paths only, no wildcards; SA bindings match documented justification | ✓ PASS |
| A real pipeline run succeeded on freshly-generated credentials | `psql ... SELECT run_id, status, started_at FROM meta.ingestion_runs WHERE run_id = 5127` | `SUCCEEDED`, `2026-08-14 20:09:50.202112+00` — independently reproduced, matches 05-06-SUMMARY.md's claim | ✓ PASS |
| Pipeline continues succeeding, live, during this verification | `psql ... SELECT count(*) FILTER (WHERE status='SUCCEEDED'), ... FROM meta.ingestion_runs` | `27 SUCCEEDED, 0 FAILED, 1 PENDING` (up from 23 at the prior verification pass, 25 mid-session) | ✓ PASS |
| `make vault-audit-tail` produces human-readable output, no secret values | `python scripts/vault-audit-tail.py --lines 15` | Clean, compact lines; zero raw JSON; zero secret values | ✓ PASS |
| Zero debt markers across all 39 files this phase touched (05-01 through 05-06) | `grep -n -E "\bTBD\b\|\bFIXME\b\|\bXXX\b"` across the full key-files list from every plan's SUMMARY | Zero matches | ✓ PASS |

### Probe Execution

No probe scripts declared or found (`find scripts -path '*/tests/probe-*.sh'` — none; no PLAN/SUMMARY references). SKIPPED — not applicable to this phase.

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| INFRA-06 | 05-01 | Vault deployed in-cluster, survives cluster restart without manual data loss | ✓ SATISFIED | Live restart-survival re-confirmed this session via a real, independently-observed restart event (Cluster ID match) plus the dedicated e2e test actually executed |
| SEC-01 | 05-02, 05-03, 05-05, 05-06 | Dedicated secrets-management solution is the only source of runtime credentials | ✓ SATISFIED (asterisk lifted) | True for live runtime reads AND for bootstrap-from-empty (CR-01 fixed, independently re-verified) |
| SEC-03 | 05-02 | No credential hard-coded in Python source | ✓ SATISFIED | Grep-clean; `test_workflow_secrets.py` 17/17, run fresh |
| SEC-04 | 05-02 | No secret baked into any container image | ✓ SATISFIED | No image/Dockerfile changes this phase or in 05-06 |
| SEC-05 | 05-03 | Airflow resolves connections through Vault backend, verified with DB conn deleted + env unset | ✓ SATISFIED | Live, fresh test run (4/4, including the previously-flaky one) + fresh DB corroboration |
| SEC-06 | 05-02, 05-03 | Task pods obtain only their own required credentials via explicit namespace/SA matched to Vault role | ✓ SATISFIED | Live role/policy inspection + live test, fresh |
| SEC-07 | 05-02, 05-03 | Workloads authenticate with least-privilege K8s SA identity, not shared root token | ✓ SATISFIED | Root token confined to host-side bootstrap tooling only (code-confirmed, unchanged) |
| SEC-08 | 05-04 | Secret access auditable (who/when/success) without logging values | ✓ SATISFIED | Live, fresh test run + live audit-tail run |
| SEC-09 | 05-04 | Rotation documented, including restart-vs-dynamic-refresh distinction | ✓ SATISFIED | `docs/secrets-architecture.md` §4 + live, fresh rotation test |
| SEC-12 | 05-02 | Negative test proves unauthorized SA denied | ✓ SATISFIED | Live, fresh test run (2/2) |
| SEC-13 | 05-01, 05-04, 05-05, 05-06 | Dev secrets marked/isolated/never committed/**reproducible on fresh rebuild** | ✓ SATISFIED (previously BLOCKED, now closed) | CR-01 fixed and independently re-verified: code, offline unit tests, live proof (16/16 e2e, real pipeline SUCCEEDED row), all reproduced fresh this session. One disclosed, non-blocking scope caveat (see truth 7). |
| SEC-14 | 05-05 | Secrets architecture documented end-to-end | ✓ SATISFIED (caveat resolved) | Document exists, 318 lines, §6 rewritten with an accurate account of what was and wasn't proven |

No orphaned requirements: every ID ROADMAP.md maps to "Phase 5" (`INFRA-06, SEC-01, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, SEC-08, SEC-09, SEC-12, SEC-13, SEC-14`) appears in at least one plan's frontmatter `requirements:` list (cross-checked against all six plans, 05-01 through 05-06), and no plan claims a requirement ROADMAP.md does not also map to this phase.

### Anti-Patterns Found

All confirmed by direct code reading during **this** verification pass (not inherited from 05-REVIEW.md without re-checking). CR-01 and CR-02 (the two Critical-tier findings from the pre-05-06 review) are **RESOLVED** — confirmed fixed by direct code read and passing tests, not merely re-asserted.

| File | Line(s) | Pattern | Severity | Impact |
|------|---------|---------|----------|--------|
| `scripts/vault-bootstrap.py` | (was 416-504, now rewritten) | ~~CR-01: credential sourcing depended on deleted Secrets~~ | ✓ RESOLVED | Fixed by plan 05-06, independently re-verified this session (code read, 7/7 unit tests, 16/16 live e2e, live DB row) |
| `scripts/vault-bootstrap.py` | (was 279-286, now 288-324) | ~~CR-02: `_ensure_policy` never re-applied a drifted policy body~~ | ✓ RESOLVED | Fixed by plan 05-06; `read_policy()` now compared against target HCL before skipping; unit-tested (both drift and no-drift cases) and live-sanity-checked |
| `packages/dataplat/src/dataplat/secrets/resolver.py` | 91-109 | `vault://` branch's `try/except` doesn't catch every exception `_vault_client()` can raise (`OSError`, `requests.exceptions.ConnectionError`) | ⚠️ WARNING (unfixed, out of 05-06's declared scope) | Same as the prior pass — re-confirmed still present via 05-REVIEW.md's fresh re-review (dated after 05-06's commits) and not contradicted by anything found this session |
| `packages/csv-processor/src/csv_processor/cli.py` | 197 | `discover()` has no `except Exception:` fallback (unlike `ingest()`) | ⚠️ WARNING (unfixed, out of scope) | Unchanged since the prior pass |
| `scripts/vault-bootstrap.py` | 373-382 (`_ensure_kubernetes_role`) | Drift check only compares `bound_service_account_names`, never `bound_service_account_namespaces`/`policies`/TTLs | ⚠️ WARNING (unfixed, out of scope — confirmed still present by direct read this session) | Same defect class CR-02 fixed for policies, not yet applied to roles |
| `scripts/vault-audit-tail.py` | 174-177 | Unguarded `None`-vs-absent-key crash risk on an explicit JSON `null` field | ⚠️ WARNING (unfixed, out of scope) | Unchanged since the prior pass |
| `helm/versions.env` | 27 | Vault server/image version has no companion pinned key | ℹ️ INFO (unfixed, out of scope) | Unchanged since the prior pass |
| `packages/dataplat/src/dataplat/secrets/resolver.py` | 85-90 | `file://` branch silently drops a path segment on a malformed two-slash reference instead of failing closed | ⚠️ WARNING (newly flagged by 05-REVIEW.md's post-05-06 pass; currently unreachable — no live call site uses `file://`) | Dormant, zero current blast radius |
| `scripts/vault-bootstrap.py`, `scripts/vault-unseal.py`, `tests/e2e/vault/conftest.py`, `tests/e2e/vault/test_unseal_survives_restart.py` | various | Duplicated `_port_forwarded_vault` helper has already drifted (script copies don't escalate to `SIGKILL` on a stuck `terminate()`) | ℹ️ INFO (unfixed, out of scope) | Low impact; `kubectl port-forward` reliably honors `SIGTERM` |
| `scripts/stages/75-etl.sh` | 15-21 | **New finding, not previously flagged by any prior review or verification pass.** Header comment states all three migrated credentials "are now Vault-served, sourced once (by scripts/vault-bootstrap.py) FROM those Secrets at migration time" — this description is now stale/inaccurate post-05-06: `vault-bootstrap.py` no longer sources any of the three from a one-time migration read of the deleted Secrets at all; it regenerates `etl_app`'s password fresh via `ALTER ROLE` and reads `data/minio-app` live, on every empty-Vault bootstrap | ℹ️ INFO | Documentation drift only — this file was outside plan 05-06's declared scope (`scripts/vault-bootstrap.py`, `tests/unit/test_vault_bootstrap.py`, `docs/secrets-architecture.md`, `05-VALIDATION.md`), so its omission is understandable, not a violation of that plan's own acceptance criteria. No functional impact — worth a one-line comment fix as a cheap follow-up, does not block this phase's goal. |

**Debt markers:** Zero `TBD`/`FIXME`/`XXX` markers found across all 39 files this phase created or modified (05-01 through 05-06), re-scanned fresh this session — no regression from the prior pass's finding.

### Human Verification Required

None. This phase is entirely backend/infrastructure; every claim was directly, programmatically testable and was independently re-tested live during this session (not inherited from any prior report).

### Gaps Summary

**No gaps remain. The prior pass's sole blocking gap (SEC-13's "reproducible when rebuilding the local environment from scratch" clause) is closed.**

Gap-closure plan 05-06 fixed the root cause (`scripts/vault-bootstrap.py`'s credential-sourcing functions depending on three already-deleted Kubernetes Secrets, plus the related CR-02 policy-drift defect) and proved the fix live. This verification pass independently re-derived every load-bearing claim from first principles rather than trusting 05-06-SUMMARY.md's narrative: re-read the fixed code directly, re-ran the new offline unit-test suite (7/7), re-ran the full offline gate (`make check`, twice, clean), re-ran the entire live e2e Vault suite fresh (16/16 — an improvement over 05-06-SUMMARY.md's own cited 15/16, consistent with that report's "self-resolving backlog depth" explanation for the one prior failure), independently queried the live analytical database for the specific `run_id=5127` `SUCCEEDED` row the SUMMARY cited (confirmed byte-for-byte), and found the pipeline continuing to succeed in real time during this verification session (27 SUCCEEDED / 0 FAILED / 1 PENDING, up from 23 at the prior verification pass).

The one honestly-disclosed, non-blocking residual: the literal `make cluster-down && make cluster-up && make vault-unseal && make vault-bootstrap && make vault-verify` sequence from an actual clean checkout was not independently re-run (neither by plan 05-06 nor by this verification pass) — the live proof used a scoped Vault-only reinstall instead. `docs/secrets-architecture.md` §6 discloses this explicitly rather than overclaiming, and the code-path equivalence argument (the bootstrap script's own logic cannot distinguish the two scenarios — both produce the identical empty-KV-store precondition its branches key off) was independently assessed and found sound. This is documented here as a caveat within a VERIFIED truth, consistent with how truths 4 and 8 in this same report already accept an equivalent, non-literal proof method for their respective claims.

One new, minor, non-blocking finding from this pass's own adversarial review: `scripts/stages/75-etl.sh`'s header comment (unchanged since plan 05-03, outside 05-06's declared file scope) now inaccurately describes the credential-sourcing mechanism plan 05-06 replaced. This is a one-line documentation-drift issue with no functional impact — noted as an INFO-level anti-pattern, not a phase-blocking gap.

A separate, unrelated infrastructure fault (a Docker Desktop/WSL2-level mount failure that froze Airflow's scheduler cluster-wide) was found during plan 05-06's live-proof task, diagnosed and fixed via `.planning/debug/resolved/dagrun-scheduler-stall.md`, and independently re-confirmed resolved in this session (the cluster is healthy, the DAG backlog is actively draining, and new task pods were observed running live during this verification).

---

_Verified: 2026-08-14T21:24:03Z_
_Verifier: Claude (gsd-verifier)_
