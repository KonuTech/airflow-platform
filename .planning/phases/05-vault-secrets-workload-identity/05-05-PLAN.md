---
phase: 05-vault-secrets-workload-identity
plan: 05
type: execute
wave: 5
depends_on: ["05-03", "05-04"]
files_modified:
  - tests/policy/test_no_stale_secrets.py
  - docs/adr/0009-openbao-licence-escape-hatch.md
  - docs/adr/README.md
  - docs/secrets-architecture.md
autonomous: true
requirements: [SEC-01, SEC-14]
must_haves:
  truths:
    - "D-01: csv-processor-db, csv-processor-s3 and airflow-minio-connection never appear as a Secret-creation target anywhere in the committed tree again -- enforced as a permanent regression guard"
    - "The secrets architecture -- injection mechanism, trust boundaries, and the production-substitution path -- is documented in one place a future operator can read end to end"
  artifacts:
    - path: "tests/policy/test_no_stale_secrets.py"
      provides: "SEC-01's final structural proof, permanent regression guard"
    - path: "docs/secrets-architecture.md"
      provides: "SEC-14's end-to-end documentation"
    - path: "docs/adr/0009-openbao-licence-escape-hatch.md"
      provides: "The Vault-is-BUSL-1.1 licence risk acceptance and its named migration trigger"
  key_links:
    - from: "docs/adr/README.md"
      to: "docs/adr/0009-openbao-licence-escape-hatch.md"
      via: "table row + removal from the deliberately-deferred-records table"
      pattern: "0009-openbao-licence-escape-hatch"
---

<objective>
Lock SEC-01's structural claim in permanently -- the three D-01 migration-target Secret names never reappear as a creation target -- and write SEC-14's end-to-end secrets architecture documentation, including the pre-announced ADR-0009 on Vault's BUSL-1.1 licence and the OpenBao escape hatch.

Purpose: this is the phase's wrap-up. Every other plan in this phase PROVED a specific behavior live; this plan is where those individually-proven facts become a permanent guard (so a future phase cannot silently reintroduce a stale Secret) and a coherent, readable document (so SEC-14's "documented end-to-end" claim is actually true, not scattered across five PLAN.md files).
Output: `tests/policy/test_no_stale_secrets.py` passes and joins `make policy`; `docs/secrets-architecture.md` and `docs/adr/0009-openbao-licence-escape-hatch.md` exist; `docs/adr/README.md`'s pre-announced Phase 5 entry is fulfilled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vault-secrets-workload-identity/05-CONTEXT.md
@.planning/phases/05-vault-secrets-workload-identity/05-RESEARCH.md
@.planning/phases/05-vault-secrets-workload-identity/05-01-SUMMARY.md
@.planning/phases/05-vault-secrets-workload-identity/05-02-SUMMARY.md
@.planning/phases/05-vault-secrets-workload-identity/05-03-SUMMARY.md
@.planning/phases/05-vault-secrets-workload-identity/05-04-SUMMARY.md
</context>

<interfaces>
<!-- Verified this planning session. -->

- `tests/policy/test_workflow_secrets.py`'s D-14 section (its own module, not this new file) already establishes the exact scanning shape to extend: `INFRA_YAML_DIRS = (REPO_ROOT / "helm", REPO_ROOT / "kubernetes", REPO_ROOT / "kind")`, `INFRA_SCRIPT_DIR = REPO_ROOT / "scripts"`, a `FORBIDDEN_LITERAL_KEYS`-shaped frozenset, a `_flatten_keys`-shaped YAML walk for the infra dirs, and a plain-text regex walk over `scripts/**/*.sh`. This new file is a SEPARATE module (`tests/policy/test_no_stale_secrets.py`), not an edit to `test_workflow_secrets.py` -- its claim ("these three names never appear as a creation target") is a different predicate over an overlapping but not identical surface (it also needs to check `helm/values/*/airflow.yaml`'s `secretKeyRef.name` fields, which `test_workflow_secrets.py`'s existing checks do not target).
- `docs/adr/README.md`'s "Deliberately deferred records" table currently has exactly one row: "Vault is BUSL-1.1 and IBM-owned; OpenBao is the API-compatible escape hatch | **Phase 5** | Nothing is deployed against Vault until Phase 5...". This plan both writes `0009-openbao-licence-escape-hatch.md` AND removes this row (the record is no longer deferred) AND adds the new row to the numbered "Records" table above it, per that file's own "Adding a record" instructions (steps 1-3).
- `docs/adr/0006-unmaintained-upstream-artifacts.md` is the closest analog for ADR-0009's shape: `## Context and Problem Statement` / `## Considered Options` / `## Decision Outcome` with `### Consequences` / `## Migration trigger` (a bulleted list of concrete, observable events -- never "none" left unstated) / `## References`.
- `docs/README.md`'s directory table does not enumerate individual files under `docs/` (only top-level directories) -- `docs/ci-branch-protection.md` is not listed there either. `docs/secrets-architecture.md` is a peer of that file; do not add a row to `docs/README.md`'s table for it.
- SEC-14's exact wording: "The secrets architecture is documented end-to-end -- injection mechanism, trust boundaries, and how a production secrets manager would substitute without application code changes." The production-substitution content must specifically address: auto-unseal (cloud KMS/transit) vs. this phase's D-02 scripted single-key local convenience; a genuine multi-key-holder Shamir ceremony vs. the local shortcut; OpenBao as the OSI-licensed escape hatch (cross-reference ADR-0009); VSO as the documented next step if a long-lived-Deployment ever needs rotation-without-restart beyond what direct SA-token login already provides (STACK.md's two-tier pattern).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The permanent SEC-01 structural guard</name>
  <files>tests/policy/test_no_stale_secrets.py</files>
  <read_first>
    tests/policy/test_workflow_secrets.py
    scripts/etl-secrets.sh
    helm/values/local/airflow.yaml
    helm/values/ci/airflow.yaml
  </read_first>
  <behavior>
    - The current committed tree (post plans 05-02/05-03) has no reference to `csv-processor-db`, `csv-processor-s3`, or `airflow-minio-connection` as a Secret-creation target anywhere under `scripts/**/*.sh` or `helm/values/**/*.yaml`'s `secretKeyRef.name`/`existingSecret` fields -> the test passes with zero problems reported.
    - Injecting one of the three names back into an in-memory copy of `scripts/etl-secrets.sh`'s text (as a scratch mutation, mirroring `test_workflow_secrets.py`'s own `test_a_literal_credential_key_is_reported` non-vacuity shape) -> reported.
    - Injecting one of the three names back into an in-memory copy of a parsed `helm/values/local/airflow.yaml` as a `secretKeyRef.name` value -> reported.
  </behavior>
  <action>
    Create `tests/policy/test_no_stale_secrets.py`. Define `STALE_SECRET_NAMES: frozenset[str] = frozenset({"csv-processor-db", "csv-processor-s3", "airflow-minio-connection"})`. Write `stale_secret_problems() -> list[str]` that: (a) scans every `scripts/**/*.sh` file's raw text for each stale name appearing as a `kubectl apply`/Secret-manifest-construction target (a simple substring search restricted to non-comment lines is sufficient -- mirror `test_workflow_secrets.py`'s `script_literal_key_problems`'s comment-stripping discipline) and reports the file/line if found; (b) walks every parsed YAML document under `helm/values/local/` and `helm/values/ci/` (using the same `_flatten_keys`-shaped dotted-path walk `test_workflow_secrets.py` already establishes, or importing its `_flatten_keys` function directly if it is exported at module scope -- otherwise duplicate it with a comment naming the source) and reports any leaf whose key is `secretKeyRef.name` (or `.name` under a `secretKeyRef` dict) or `existingSecret` whose VALUE is one of `STALE_SECRET_NAMES`. Write `test_no_stale_secret_name_appears_as_a_creation_target()` asserting `stale_secret_problems()` is empty, with an assertion message citing D-01 and this plan. Write the two non-vacuity tests from the Behavior section, each mutating an in-memory copy (never touching disk) and asserting the mutation IS reported -- mirroring `test_workflow_secrets.py`'s own `test_a_literal_credential_key_is_reported`/`test_a_generated_credential_in_a_script_is_not_reported` pairing shape (also add a false-positive control: `csv-processor` alone, or `minio-app`, must NOT be reported -- only the three FULL stale names).
  </action>
  <acceptance_criteria>
    - `pytest tests/policy/test_no_stale_secrets.py -q` passes against the current tree
    - `pytest tests/policy -q -m "not manifests"` (the full `make policy` selection) still passes with this new module included
    - Each of the three non-vacuity tests fails if its own injected mutation is removed (verify by temporarily commenting out the mutation line and re-running -- then restore it)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/policy/test_no_stale_secrets.py -q</automated>
  </verify>
  <done>SEC-01's claim -- Vault is the only source of runtime credentials for these three migrated paths -- is now a permanent, automatically-enforced regression guard, not a one-time fact.</done>
</task>

<task type="auto">
  <name>Task 2: SEC-14 documentation and ADR-0009</name>
  <files>docs/secrets-architecture.md, docs/adr/0009-openbao-licence-escape-hatch.md, docs/adr/README.md</files>
  <read_first>
    docs/adr/0006-unmaintained-upstream-artifacts.md
    docs/adr/0000-template.md
    docs/adr/README.md
    docs/ci-branch-protection.md
  </read_first>
  <action>
    Create `docs/secrets-architecture.md` covering, in order: (1) **Injection mechanism** -- the two-tier direct-SA-token-login pattern (never Agent Injector/CSI/VSO/ESO), the exact `vault://mount/path#field` URI shape, `resolve_secret()`'s scheme dispatch, and `VaultBackend`'s `backend_kwargs`; (2) **Trust boundaries** -- a table matching this phase's five PLAN.md `<threat_model>` blocks' boundaries, consolidated (host-developer-to-Vault for bootstrap/unseal; ServiceAccount-JWT-to-Vault for both workload identities; Vault's internal plaintext listener, citing T-05-05 explicitly); (3) **What is where** -- a table of every credential this phase migrated (dataset: `etl/analytics-db#dsn`, `etl/minio#access_key`, `etl/minio#secret_key`, `airflow/connections/minio_default#conn_uri`) and which identity reads each; (4) **Rotation** (SEC-09) -- which credentials are read once per process (KPO/ETL pods -- "rotation" for this tier means the next pod) vs. read live per lookup (Airflow's `VaultBackend`, `AIRFLOW__SECRETS__USE_CACHE=False`), backed by plan 05-04's `test_rotation.py`; (5) **Audit** (SEC-08) -- how to read it (`make vault-audit-tail`), what it proves, backed by `test_audit_log.py`; (6) **Production substitution** (SEC-14's explicit ask) -- auto-unseal (cloud KMS/transit) replacing D-02's scripted single-key local unseal; a genuine multi-key-holder Shamir ceremony replacing the local shortcut; OpenBao as the OSI-licensed escape hatch (cross-reference ADR-0009 by relative link); VSO as the documented next step if a long-lived Deployment ever needs rotation-without-restart; TLS as a hard requirement (not the accepted local-dev T-05-05 plaintext) in any non-local deployment. Cite every source PLAN.md/SUMMARY.md this content is drawn from by relative path, matching this repository's existing documentation citation discipline (see `docs/ci-branch-protection.md`'s own footnote/reference style).

    Create `docs/adr/0009-openbao-licence-escape-hatch.md`, numbered per `docs/adr/README.md`'s own numbering rule (next free number after `0008`), using `docs/adr/0000-template.md`'s section skeleton and `docs/adr/0006-unmaintained-upstream-artifacts.md`'s level of concreteness. Context: Vault's server (chart default `2.0.3`) went major-2.x on 2026-04-14 and is BUSL-1.1-licensed, IBM-owned (per `.claude/CLAUDE.md` §E). Considered Options: (A) accept Vault as-is for local dev, since BUSL-1.1 permits non-competing internal use and this project is not a competing secrets-management product; (B) adopt OpenBao now instead; (C) adopt a cloud-native secrets manager instead (rejected -- defeats the local, production-like kind-cluster mandate, README §3.1). Decision Outcome: accept Vault for this milestone, name OpenBao as the documented, API-compatible migration target. Migration trigger (a real bulleted list, never "none"): a BUSL-1.1 licence-term change adverse to this project's use; a security-relevant CVE against the pinned Vault version left unpatched past a stated window; a decision to deploy this pattern outside local dev, where BUSL-1.1's field-of-use restriction becomes operative in a way it is not for a laptop-only kind cluster. References: `.claude/CLAUDE.md` §E, `.planning/research/STACK.md` §E, this phase's `05-RESEARCH.md`.

    Edit `docs/adr/README.md`: add a row to the numbered "Records" table for `0009` (status `accepted`, today's date), and remove the now-fulfilled row from the "Deliberately deferred records" table (that table's whole point is records NOT yet written -- leaving a written record's row there would be stale, matching this file's own "an in-place edit... destroys exactly the evidence" discipline applied in reverse: a table meant to track absence must not claim something is still absent once it is not).
  </action>
  <acceptance_criteria>
    - `docs/secrets-architecture.md` exists and contains all six named sections, each citing at least one concrete file (PLAN.md, SUMMARY.md, or source file) by relative path
    - `docs/adr/0009-openbao-licence-escape-hatch.md` exists with a non-empty `## Migration trigger` section containing at least three concrete, observable events
    - `docs/adr/README.md`'s Records table contains a row for `0009`, and its Deliberately-deferred-records table no longer contains the Phase 5 / Vault-BUSL row
    - `grep -c '^## ' docs/adr/0009-openbao-licence-escape-hatch.md` reports at least `5` (Context and Problem Statement, Considered Options, Decision Outcome, Migration trigger, References)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/secrets-architecture.md && test -f docs/adr/0009-openbao-licence-escape-hatch.md && grep -q "0009-openbao-licence-escape-hatch" docs/adr/README.md</automated>
  </verify>
  <done>SEC-14 is satisfied by one readable document covering injection, trust boundaries and production substitution; the pre-announced ADR-0009 exists and docs/adr/README.md's own bookkeeping reflects it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy -> future operator trust | A `docs/secrets-architecture.md` that overstates what was actually proven (vs. what was merely configured) would itself be a threat to this project's Core Value (trusted, verifiable) -- every claim in it must cite the PLAN.md/SUMMARY.md/test that actually proved it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 (final) | Information Disclosure | The full committed tree, post-migration | mitigate | `tests/policy/test_no_stale_secrets.py` makes SEC-01's claim a permanent, automatically-enforced fact rather than a point-in-time observation that could silently regress in a later phase |
| T-05-12 | Repudiation | Undocumented production-substitution path | mitigate | `docs/secrets-architecture.md`'s explicit substitution section means a future migration off this phase's local-dev conveniences (single-key unseal, plaintext listener) is a documented decision, not a rediscovery |

</threat_model>

<verification>
`pytest tests/policy -q -m "not manifests"` passes with the new policy module included. `docs/secrets-architecture.md` and `docs/adr/0009-openbao-licence-escape-hatch.md` exist and are cross-referenced from `docs/adr/README.md`.
</verification>

<success_criteria>
SEC-01 is a permanent regression guard, not a one-time proof. SEC-14 is satisfied by a single, accurate, well-cited document. The phase's twelve requirements are all closed.
</success_criteria>

<output>
Create `.planning/phases/05-vault-secrets-workload-identity/05-05-SUMMARY.md` when done
</output>
