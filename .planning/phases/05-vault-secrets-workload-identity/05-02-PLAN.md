---
phase: 05-vault-secrets-workload-identity
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/dataplat/src/dataplat/secrets/resolver.py
  - packages/dataplat/src/dataplat/errors.py
  - packages/dataplat/pyproject.toml
  - uv.lock
  - tests/unit/test_secrets_resolver.py
  - scripts/vault-bootstrap.py
  - airflow/dags/_common/kpo.py
  - tests/e2e/vault/test_positive_auth.py
  - tests/e2e/vault/test_negative_auth.py
  - scripts/etl-secrets.sh
autonomous: true
requirements: [SEC-03, SEC-04, SEC-06, SEC-07, SEC-12, SEC-01]
must_haves:
  truths:
    - "resolve_secret() interprets a vault:// reference by authenticating once per process via Kubernetes auth and reading a KV v2 secret, never learning or logging the value's origin beyond that one scheme dispatch"
    - "A KubernetesPodOperator pod running as ServiceAccount csv-processor in namespace etl can read its own two Vault-stored credentials (analytics-db dsn, minio access/secret key) and resolve them through DATAPLAT_DB_DSN/DATAPLAT_S3_ACCESS_KEY/DATAPLAT_S3_SECRET_KEY exactly as before"
    - "The default ServiceAccount in namespace etl is denied a Kubernetes-auth login against the csv-processor Vault role -- asserted by an automated test, not inferred from policy text"
    - "The csv-processor-db and csv-processor-s3 Kubernetes Secrets no longer exist, and scripts/etl-secrets.sh no longer creates them"
  artifacts:
    - path: "packages/dataplat/src/dataplat/secrets/resolver.py"
      provides: "vault:// scheme dispatch: scheme://mount/path#field, module-level cached authenticated hvac.Client"
    - path: "tests/e2e/vault/test_positive_auth.py"
      provides: "SEC-06/SEC-07 live proof: csv-processor SA reads its own two Vault paths"
    - path: "tests/e2e/vault/test_negative_auth.py"
      provides: "SEC-12 live proof: default SA's login against role csv-processor fails closed"
  key_links:
    - from: "airflow/dags/_common/kpo.py"
      to: "packages/dataplat/src/dataplat/secrets/resolver.py"
      via: "env var literal value=\"vault://etl/...#...\", resolved inside the pod by resolve_secret()"
      pattern: "vault://etl/"
    - from: "packages/dataplat/src/dataplat/secrets/resolver.py"
      to: "Vault's csv-processor Kubernetes-auth role"
      via: "hvac client.auth.kubernetes.login(role=\"csv-processor\", jwt=<projected SA token>)"
      pattern: "auth.kubernetes.login"
---

<objective>
Add a `vault://` scheme to `dataplat.secrets.resolver.resolve_secret()`, wire the `csv-processor` KubernetesPodOperator pod's three database/object-store credentials through it instead of `secretKeyRef`, populate Vault's `etl` KV mount from the existing dev credentials, and prove -- live, on the cluster -- both that the `csv-processor` ServiceAccount reads its own two paths and that the `default` ServiceAccount in the same namespace is denied. Retire `csv-processor-db` and `csv-processor-s3` once that proof is green.

Purpose: SEC-15 (Phase 3) built `SecretsResolver` specifically so this swap would be a scheme addition, not a redesign (deviation D3). This plan is that swap for the ETL pod's own credentials, and it is the phase's first concrete SEC-06/SEC-07/SEC-12 proof.
Output: `resolve_secret("vault://etl/analytics-db#dsn")` and friends work inside a real KPO pod; `tests/e2e/vault/test_positive_auth.py` and `test_negative_auth.py` pass against the live cluster; two of the three D-01 credential migrations are complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-vault-secrets-workload-identity/05-CONTEXT.md
@.planning/phases/05-vault-secrets-workload-identity/05-RESEARCH.md
@.planning/phases/05-vault-secrets-workload-identity/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact current source, read directly this planning session. The executor should use
these as the literal starting point, not re-derive them. -->

From `packages/dataplat/src/dataplat/secrets/resolver.py` (current, in full):
```
    parsed = urlsplit(ref)
    if parsed.scheme == "env":
        value = os.environ.get(parsed.netloc or parsed.path.lstrip("/"))
        if value is None:
            msg = f"environment variable not set for ref {ref!r}"
            raise SecretResolutionError(msg, context={"ref": ref})
        return value
    if parsed.scheme == "file":
        try:
            return Path(parsed.path).read_text(encoding="utf-8").strip()
        except OSError as exc:
            msg = f"cannot read secret file for ref {ref!r}: {exc}"
            raise SecretResolutionError(msg, context={"ref": ref}) from exc
    msg = f"unsupported secret ref scheme {parsed.scheme!r} in {ref!r}"
    raise SecretResolutionError(msg, context={"ref": ref})
```
The new `if parsed.scheme == "vault":` branch goes between the `file` branch and the final fail-closed `raise`, following the identical try/except-wrap-as-`SecretResolutionError` shape.

From `packages/dataplat/src/dataplat/errors.py`'s `SecretResolutionError` docstring (needs correcting -- it currently reads "an unrecognized scheme such as `vault://` before Phase 5 implements it"; `vault://` is now a recognized, real scheme that can also fail, not an example of an unrecognized one).

From `airflow/dags/_common/kpo.py` (current `env_vars` list, to replace the two `secret_key_ref`-based entries):
```
        "env_vars": [
            k8s.V1EnvVar(
                name="DATAPLAT_DB_DSN",
                value_from=k8s.V1EnvVarSource(
                    secret_key_ref=k8s.V1SecretKeySelector(name=_DB_DSN_SECRET_NAME, key="dsn"),
                ),
            ),
            k8s.V1EnvVar(
                name="DATAPLAT_S3_ACCESS_KEY",
                value_from=k8s.V1EnvVarSource(
                    secret_key_ref=k8s.V1SecretKeySelector(name=_S3_SECRET_NAME, key="access_key"),
                ),
            ),
            k8s.V1EnvVar(
                name="DATAPLAT_S3_SECRET_KEY",
                value_from=k8s.V1EnvVarSource(
                    secret_key_ref=k8s.V1SecretKeySelector(name=_S3_SECRET_NAME, key="secret_key"),
                ),
            ),
            k8s.V1EnvVar(name="DATAPLAT_S3_ENDPOINT_URL", value=_S3_ENDPOINT_URL),
            *(extra_env_vars or []),
        ],
```
`_DB_DSN_SECRET_NAME`/`_S3_SECRET_NAME` module constants become unused once these three become plain `value="vault://..."` literals -- remove them. `DATAPLAT_S3_ENDPOINT_URL` is unchanged (it was never a Secret).

From `packages/csv_processor/src/csv_processor/cli.py`'s `_build_common()`: the exact three references `resolve_secret()` is called with today are `"env://DATAPLAT_DB_DSN"`, `"env://DATAPLAT_S3_ACCESS_KEY"`, `"env://DATAPLAT_S3_SECRET_KEY"` (plus `"env://DATAPLAT_S3_ENDPOINT_URL"`) -- these constant strings in `cli.py` do NOT change; only what `kpo.py` puts INTO those env vars changes, from a Secret-sourced value to the literal string `"vault://..."`, which `resolve_secret()` then interprets. `_build_common()` is called exactly once per pod invocation (`discover`/`ingest` each call it once) -- this is why a module-level cached `hvac.Client` (authenticate once per process) is correct and sufficient; no token-renewal logic is needed for a short-lived KPO pod.

From `scripts/etl-secrets.sh`'s `_ensure_csv_processor_db_secret` and `_ensure_csv_processor_s3_secret` (both to be deleted from `cmd_ensure` and as standalone functions once the live proof passes) -- `_ensure_airflow_minio_connection_secret` and its `cmd_ensure` call stay untouched; that credential is plan 05-03's, not this plan's (D-01: one credential's removal per its own confirmed plan, never a batch).

From `05-01-PLAN.md`'s `scripts/vault-bootstrap.py`: the `etl` KV v2 mount, the `csv-processor` policy/role, and the audit device already exist after plan 05-01. This plan's own bootstrap extension only needs to WRITE the two KV secret VALUES (`etl/analytics-db#dsn`, `etl/minio#access_key`/`#secret_key`) -- the mount/policy/role scaffolding is already there.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The vault:// contract in resolve_secret()</name>
  <files>packages/dataplat/src/dataplat/secrets/resolver.py, packages/dataplat/src/dataplat/errors.py, packages/dataplat/pyproject.toml, uv.lock, tests/unit/test_secrets_resolver.py</files>
  <read_first>
    packages/dataplat/src/dataplat/secrets/resolver.py
    packages/dataplat/src/dataplat/errors.py
    packages/dataplat/pyproject.toml
    tests/unit/test_secrets_resolver.py
    tests/unit/test_csv_processor_cli.py
  </read_first>
  <behavior>
    - `resolve_secret("vault://etl/analytics-db#dsn")` with a mocked `_vault_client()` returning a fake client whose `secrets.kv.v2.read_secret_version(mount_point="etl", path="analytics-db")` returns `{"data": {"data": {"dsn": "postgresql://..."}}}` -> returns `"postgresql://..."`.
    - `resolve_secret("vault://etl/minio#access_key")` against the same mocked client with a different `path`/`field` combination -> returns the value at that field.
    - `resolve_secret("vault://etl/analytics-db")` (no `#field` fragment) -> raises `SecretResolutionError` naming the malformed-reference shape (`scheme://mount/path#field`), without ever calling the mocked client.
    - A mocked client whose `read_secret_version` raises `hvac.exceptions.VaultError` -> `resolve_secret(...)` raises `SecretResolutionError`, never lets the `hvac` exception escape unwrapped.
    - A mocked client returning data with no matching field key -> `resolve_secret(...)` raises `SecretResolutionError` (a `KeyError` wrapped, not propagated raw).
    - The existing `test_vault_scheme_fails_closed_rather_than_passing_through` test (asserting `vault://` is REJECTED) is replaced -- `vault://` is now a real, working scheme; its old assertion is false the moment this task lands.
  </behavior>
  <action>
    Add `"hvac>=2.4,<3"` to `packages/dataplat/pyproject.toml`'s `[project.dependencies]` list, in the same `>=X,<X+1major` pinned-range style as the other five entries. Run `uv lock` to regenerate `uv.lock` (this is the SECOND `hvac` addition in this phase -- plan 05-01 already added it to the root `pyproject.toml`'s `cluster` group for the e2e test tier's independence; this one makes `hvac` a real runtime dependency of `dataplat` itself, which is what makes it install into the `csv-processor` Docker image with zero Dockerfile changes, per `docker/csv-processor/Dockerfile`'s existing `uv sync --locked --all-packages --no-dev` build).

    Edit `resolver.py`: add `import hvac` and `import os` (already imported) near the top. Add a module-level `_client: hvac.Client | None = None` and a `_vault_client() -> hvac.Client` function that: returns the cached client if already set; otherwise builds `hvac.Client(url=os.environ["VAULT_ADDR"])`, reads the projected ServiceAccount token from `/var/run/secrets/kubernetes.io/serviceaccount/token` (the default path every pod already has -- no explicit volume mount needed), calls `client.auth.kubernetes.login(role=os.environ["VAULT_K8S_ROLE"], jwt=<token text>)`, caches and returns it. Both `VAULT_ADDR` and `VAULT_K8S_ROLE` are read via plain `os.environ[...]` (not through `resolve_secret()` itself -- they are non-secret configuration, not credentials, matching how `_S3_ENDPOINT_URL` in `kpo.py` is already a plain value, not a `resolve_secret()`-mediated reference).

    Add the `if parsed.scheme == "vault":` branch (see Interfaces for the exact surrounding shape to extend): parse `mount_point = parsed.netloc`, `path = parsed.path.lstrip("/")`, `field = parsed.fragment`; if any of the three is empty, raise `SecretResolutionError(f"malformed vault:// ref (need scheme://mount/path#field): {ref!r}", context={"ref": ref})` WITHOUT calling `_vault_client()`. Otherwise, in a `try`/`except`, call `_vault_client().secrets.kv.v2.read_secret_version(mount_point=mount_point, path=path)`, index `["data"]["data"][field]`, and return it; catch `hvac.exceptions.VaultError` and re-raise as `SecretResolutionError(f"vault read failed for ref {ref!r}: {exc}", context={"ref": ref})` (`from exc`); catch `KeyError` and re-raise as `SecretResolutionError(f"vault secret at {mount_point}/{path} has no field {field!r}", context={"ref": ref})`.

    Correct the module docstring: replace the `file:///vault/secrets/analytical-db` illustrative example (which describes the Agent Injector sidecar pattern this project does NOT use) with `vault://etl/analytics-db#dsn`, and remove the "`vault://` is real -- it is Phase 5's, not this phase's" sentence, which is now false.

    Edit `errors.py`'s `SecretResolutionError` docstring: change "an unrecognized scheme such as `vault://` before Phase 5 implements it" to describe `vault://` as a recognized scheme that can now also fail to resolve, alongside `env://`/`file://` -- not an example of an unrecognized one.

    Edit `tests/unit/test_secrets_resolver.py`: replace `test_vault_scheme_fails_closed_rather_than_passing_through` with the mocked-`hvac`-client tests from the Behavior section above, using `monkeypatch.setattr(resolver, "_vault_client", lambda: fake_client)` (the module-level-callable-patching convention `tests/unit/test_csv_processor_cli.py` already establishes for this repository), with `fake_client` a small stub or `unittest.mock.MagicMock` whose `.secrets.kv.v2.read_secret_version(...)` is configured per test case. Keep `test_resolver_never_returns_the_raw_unresolved_reference_string` and the `env://`/`file://` tests exactly as they are.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/test_secrets_resolver.py -q` passes, with at least the six behaviors above each covered by a named test
    - `resolve_secret("vault://etl/analytics-db")` (no fragment) raises `SecretResolutionError` and the assertion message names `scheme://mount/path#field`
    - `mypy packages/dataplat/src` passes (the existing `hvac.*` `ignore_missing_imports` override in root `pyproject.toml` already covers the new import)
    - `uv lock --check` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/unit/test_secrets_resolver.py -q && mypy packages/dataplat/src</automated>
  </verify>
  <done>resolve_secret() has a working, fail-closed vault:// scheme with a module-level cached authenticated client, covered by unit tests using a mocked hvac.Client -- no live cluster needed for this task.</done>
</task>

<task type="auto">
  <name>Task 2: Populate Vault's etl KV secrets and wire the KPO pod's env vars</name>
  <files>scripts/vault-bootstrap.py, airflow/dags/_common/kpo.py</files>
  <read_first>
    scripts/vault-bootstrap.py
    airflow/dags/_common/kpo.py
    scripts/etl-secrets.sh
  </read_first>
  <action>
    Extend `scripts/vault-bootstrap.py` with an idempotent `_ensure_etl_secrets(client)` step, called after the existing mount/auth-method/role/policy steps (05-01-PLAN.md Task 2). Guard: read `client.secrets.kv.v2.read_secret_version(mount_point="etl", path="analytics-db")`; if it succeeds (secret already present), skip and print "already present"; on a 404-shaped `hvac.exceptions.InvalidPath`, populate it. Source the VALUES from the live `csv-processor-db`/`csv-processor-s3` Kubernetes Secrets that `scripts/etl-secrets.sh` already created (Phase 4) -- read them the same way `scripts/etl-secrets.sh`'s own `_read_minio_app_secret_key` does (`kubectl get secret ... -o jsonpath=... | base64 -d`, via a `subprocess.run` call, not a new `kubectl` wrapper library), decode, and write via `client.secrets.kv.v2.create_or_update_secret(mount_point="etl", path="analytics-db", secret={"dsn": <value>})` and `create_or_update_secret(mount_point="etl", path="minio", secret={"access_key": "etl-app", "secret_key": <value>})`. Never print either value.

    Add `VAULT_ADDR` and `VAULT_K8S_ROLE` env vars to `common_kpo_kwargs()`'s returned `env_vars` list in `kpo.py`: `k8s.V1EnvVar(name="VAULT_ADDR", value="http://vault.vault.svc.cluster.local:8200")` and `k8s.V1EnvVar(name="VAULT_K8S_ROLE", value="csv-processor")` -- both plain literal values (non-secret configuration, matching `DATAPLAT_S3_ENDPOINT_URL`'s existing shape, never a `resolve_secret()` reference). Replace the three `secret_key_ref`-based `V1EnvVar` entries for `DATAPLAT_DB_DSN`, `DATAPLAT_S3_ACCESS_KEY`, `DATAPLAT_S3_SECRET_KEY` with plain-value entries: `value="vault://etl/analytics-db#dsn"`, `value="vault://etl/minio#access_key"`, `value="vault://etl/minio#secret_key"` respectively. Remove the now-unused `_DB_DSN_SECRET_NAME`/`_S3_SECRET_NAME` module constants. Update the module docstring's sentence describing these four env vars as "resolved via SecretsResolver's env:// scheme" and "NAMES them as Kubernetes secretKeyRef/plain-value sources" -- three of the four now hold a `vault://` literal, not a Kubernetes Secret reference; state this precisely rather than leaving the old, now-inaccurate description in place.

    No Helm redeploy is needed for the `kpo.py` change -- it is plain Python read by the DAG processor at parse time, not baked into a Helm values file; the next task-pod launch picks it up automatically.
  </action>
  <acceptance_criteria>
    - `python scripts/vault-bootstrap.py` run against a live, unsealed, bootstrapped Vault populates `etl/analytics-db` and `etl/minio`, and a second run prints "already present" for both without changing their `read_secret_version(...)["data"]["metadata"]["version"]`
    - `airflow/dags/_common/kpo.py`'s `common_kpo_kwargs()["env_vars"]` contains exactly two entries with `.value_from` set (none, after this task -- confirm zero) and the DSN/access-key/secret-key entries carry `.value` starting with `"vault://etl/"`
    - `ruff check airflow/dags/_common/kpo.py` and `mypy` (via `TYPECHECK_PATHS`, which does not include `airflow/dags` -- confirm via `pytest tests/policy/test_dag_thinness.py -q` that this file's exemption-by-name still holds) both pass
  </acceptance_criteria>
  <verify>
    <automated>ruff check airflow/dags/_common/kpo.py && pytest tests/policy/test_dag_thinness.py -q</automated>
  </verify>
  <done>Vault holds the same DSN and MinIO application credential the KPO pod already used, under etl/analytics-db and etl/minio; the KPO pod spec asks for them via vault:// references, not secretKeyRef.</done>
</task>

<task type="auto">
  <name>Task 3: Prove SEC-06/SEC-07/SEC-12 live, then retire the two old Secrets</name>
  <files>tests/e2e/vault/test_positive_auth.py, tests/e2e/vault/test_negative_auth.py, scripts/etl-secrets.sh</files>
  <read_first>
    tests/e2e/vault/conftest.py
    tests/e2e/cluster/test_minio_buckets.py
    scripts/etl-secrets.sh
  </read_first>
  <action>
    Create `tests/e2e/vault/test_positive_auth.py` with `pytestmark = pytest.mark.cluster`. Obtain a projected token for ServiceAccount `csv-processor` in namespace `etl` via `kubectl create token csv-processor -n etl` (a live, host-side `kubectl` call the test performs directly -- `tests/` is outside the manual-kubectl-surgery scan). Build an `hvac.Client` against `vault_addr`, call `client.auth.kubernetes.login(role="csv-processor", jwt=<that token>)`, then read `etl/data/analytics-db` and `etl/data/minio` via the now-authenticated client and assert both `dsn` and `access_key`/`secret_key` are non-empty strings matching what `vault-bootstrap.py` wrote. Additionally, actually run `dataplat`'s `_build_common()` path end-to-end: invoke `python -m dataplat ...` (or the equivalent `dataplat` console entry point) inside a throwaway pod or via `kubectl run --rm -i` is NOT permitted by the manual-surgery policy for `scripts/`, but IS fine here since it is test code -- OR, simpler and sufficient, assert the KV read above succeeds through the SAME `role="csv-processor"` login the pod itself performs, which is what `resolve_secret("vault://...")` does internally; do not require a full live pod run if the KV-read-through-the-role assertion already exercises the identical Vault-side authorization boundary SEC-06/SEC-07 concern.

    Create `tests/e2e/vault/test_negative_auth.py` with `pytestmark = pytest.mark.cluster`. Obtain a projected token for the `default` ServiceAccount in namespace `etl` (`kubectl create token default -n etl`). Assert `client.auth.kubernetes.login(role="csv-processor", jwt=<default's token>)` raises `hvac.exceptions.VaultError` -- the login itself must fail; no token is ever issued, so there is nothing to attempt a KV read with afterward. Add a second assertion using the actual csv-processor KV paths: even if (hypothetically, to catch a future regression) a caller obtained SOME valid client token through an unrelated path, confirm `client.auth.kubernetes.login` for `default` never succeeds by asserting the raised exception occurs before any `read_secret_version` call is reachable in this test's own code path. ROADMAP SC2 names only the `default` ServiceAccount, and this test satisfies that literal wording; do NOT block this task on it, but if it is cheap once the `default`-SA case is written, add an optional third case reusing the SAME shape: a projected token for `airflow-scheduler` (namespace `airflow`) also fails `client.auth.kubernetes.login(role="csv-processor", jwt=...)` -- CONTEXT.md's Claude's-Discretion note flags this broader cross-namespace case as a stretch, not a requirement.

    Once both pass against the live cluster, edit `scripts/etl-secrets.sh`: delete the `_ensure_csv_processor_db_secret` and `_ensure_csv_processor_s3_secret` function bodies and their two calls inside `cmd_ensure` (leaving `_ensure_airflow_minio_connection_secret` and its call untouched -- that is plan 05-03's own credential). Update the file's header comment to note these two are now Vault-served (plan 05-02) and the third remains Kubernetes-Secret-served pending plan 05-03. Then, against the live cluster, delete the two now-stale Secrets directly: `kubectl --context kind-<cluster> delete secret csv-processor-db csv-processor-s3 -n etl` (an operator action performed directly, not embedded in any committed script -- consistent with this repository's kubectl-surgery policy, which scans committed `.sh` files, not ad hoc operator commands).
  </action>
  <acceptance_criteria>
    - `pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -q -m cluster` passes against the live cluster
    - After the Secret removal, `kubectl get secret -n etl csv-processor-db csv-processor-s3` reports `NotFound` for both
    - (Full live-pipeline proof -- a KPO pod launched from `csv_ingest_customers` resolving all three `vault://` references and `meta.ingestion_runs` reaching a terminal status -- is deliberately NOT re-verified here; it is covered transitively by 05-03-PLAN.md Task 2's own live DAG trigger, which exercises this exact `kpo.py` wiring while proving SEC-05. This plan's own gate is the Vault-side authorization boundary itself, SEC-06/SEC-07/SEC-12, proven by `test_positive_auth.py`/`test_negative_auth.py`.)
    - `grep -c csv-processor-db scripts/etl-secrets.sh` and `grep -c csv-processor-s3 scripts/etl-secrets.sh`, filtered to exclude comment lines (`grep -v '^\s*#'`), both report `0`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -q -m cluster</automated>
  </verify>
  <done>csv-processor reads exactly its own two Vault paths; default is denied, proven by an automated negative test; both old Kubernetes Secrets are gone from the live cluster and from the script that used to create them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KPO pod's projected ServiceAccount token -> Vault `auth/kubernetes/login` | Untrusted input crosses here in the sense that Vault, not this codebase, is what validates the token's identity via TokenReview |
| `resolve_secret()`'s vault:// branch -> the pod's own process memory | The resolved value lives only in process memory from this point forward, never written to disk, never logged (OBS-05's redaction processor covers `context={"dsn": ...}`-shaped values if one is ever passed, per `errors.py`'s reserved-keys design) |
| `csv-processor` Vault policy -> `etl/data/*` | Scoped to exactly `analytics-db` and `minio` under mount `etl`; cannot read `airflow/data/connections/*` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | `scripts/vault-bootstrap.py`'s `csv-processor` role | mitigate | `bound_service_account_names=["csv-processor"]`, `bound_service_account_namespaces=["etl"]` -- an exact, non-wildcard, single-identity match; a future permission-denied must never be "fixed" by widening this list (PITFALLS #13/B5) |
| T-05-03 | Elevation of Privilege | Vault `auth/kubernetes/login` for role `csv-processor` | mitigate | `test_negative_auth.py` proves the `default` ServiceAccount's login itself fails -- no token is ever issued to a mismatched identity |
| T-05-06 | Information Disclosure | `csv-processor-db`, `csv-processor-s3` Kubernetes Secrets | mitigate | Deleted from the live cluster and from `scripts/etl-secrets.sh`'s creation logic only AFTER `test_positive_auth.py`/`test_negative_auth.py` pass -- D-01's per-credential, prove-then-remove sequencing, not a batch cleanup |
| T-05-09 | Tampering | `resolve_secret()`'s `vault://` malformed-reference path | mitigate | A missing mount/path/field raises `SecretResolutionError` before any network call -- a malformed reference can never silently resolve to an empty string or fall through to a different scheme |

</threat_model>

<verification>
`pytest tests/unit/test_secrets_resolver.py -q` passes offline. `pytest tests/e2e/vault/test_positive_auth.py tests/e2e/vault/test_negative_auth.py -q -m cluster` passes against the live cluster. `kubectl get secret -n etl csv-processor-db csv-processor-s3` both report NotFound.
</verification>

<success_criteria>
The vault:// scheme works end to end from KPO pod spec through `resolve_secret()` to a live PostgreSQL/MinIO connection. The csv-processor identity is proven both positively and negatively. Two of the three D-01 Secret migrations are complete.
</success_criteria>

<output>
Create `.planning/phases/05-vault-secrets-workload-identity/05-02-SUMMARY.md` when done
</output>
