---
phase: 06-universal-csv-engine-schema-contracts-normalization
plan: 17
type: execute
wave: 5
depends_on: ["06-15", "06-16"]
files_modified:
  - tests/property/test_determinism.py
autonomous: true
requirements: [QUAL-16]
must_haves:
  truths:
    - "Staging the identical source bytes under the identical config and processor version twice produces an identical set of _record_hash values, every time, across a range of generated inputs"
    - "Staging the identical source bytes under a DIFFERENT config (e.g. a different decimal_separator) produces at least one different _record_hash, proving the hash is actually sensitive to configuration, not vacuously constant"
  artifacts:
    - path: "tests/property/test_determinism.py"
      provides: "QUAL-16's property test — identical input+config+version yields an identical output hash, and a changed config yields a different one, over a range of Hypothesis-generated inputs"
      contains: "def test_identical_input_yields_identical_output_hash"
---

<objective>
Close Phase 6 with QUAL-16's determinism property: run the now-fully-wired pipeline (detection →
normalization → hashing, plans 06-14/06-15/06-16 all landed) twice over identical source bytes, config
and processor version, and prove the resulting `_record_hash` set is byte-identical every time, across
a range of Hypothesis-generated CSV inputs — not just one hand-picked example. Separately prove the
hash is NOT vacuously constant: changing one configuration value (e.g. `decimal_separator`) that
genuinely changes a normalized value must change that row's hash too.

Purpose: this is deliberately the LAST plan in the phase. 06-RESEARCH.md's own Code Examples section
flagged this test as "not yet wired to a real pipeline entry point (that entry point does not exist
until later this phase's tasks land)" — that entry point (`StagingLoader.load()`, now normalizing for
real, computing `_record_hash` over normalized values) only exists once plans 06-15 and 06-16 have both
landed. This plan is the phase's own closing proof that everything wired together behaves
deterministically, the platform's stated Core Value made literally testable.
Output: `tests/property/test_determinism.py`, a Hypothesis property test driving real `StagingLoader.load()`
calls against a throwaway database and object store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-RESEARCH.md
@packages/dataplat/src/dataplat/load/staging.py
@tests/integration/conftest.py
</context>

<interfaces>
```python
# 06-RESEARCH.md's own pattern for this test's shape (Code Examples section) -- adapt to the now-real
# StagingLoader.load() entry point rather than an abstract run_pipeline_and_hash placeholder:
from hypothesis import given, settings

@given(source_bytes=..., config=..., processor_version=...)
@settings(max_examples=50)  # normalization + hashing is not free; keep this bounded
def test_identical_input_yields_identical_output_hash(source_bytes, config, processor_version):
    hash_1 = run_pipeline_and_hash(source_bytes, config, processor_version)
    hash_2 = run_pipeline_and_hash(source_bytes, config, processor_version)
    assert hash_1 == hash_2
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Determinism property — identical input+config+version yields an identical hash set</name>
  <files>tests/property/test_determinism.py</files>
  <read_first>
    packages/dataplat/src/dataplat/load/staging.py
    tests/integration/conftest.py
    tests/property/test_chunking_properties.py
  </read_first>
  <behavior>
    - For a generated small table of `customers`-shaped rows (2-10 rows, valid `birth_date`/
      `event_ts` values drawn from a bounded, always-parseable range so the property is about
      determinism, not about exercising every rejection path plan 06-09 already covers), staging the
      identical bytes twice via two independent `StagingLoader.load()` calls (two different
      `run_id`/staging tables, same source content, same `DatasetConfig`) produces the SAME ordered
      set of `_record_hash` values both times.
    - Changing exactly one `NormalizationConfig` field that affects a normalized value used in the
      hash (e.g. constructing a second `DatasetConfig` with a different but still-valid
      `decimal_separator`, run only if the generated table includes a numeric column exercising it —
      otherwise use a `columns:` contract difference that is guaranteed to change at least one
      row's normalized value) produces a DIFFERENT `_record_hash` set than the original config.
  </behavior>
  <action>
    Create `tests/property/test_determinism.py` mirroring `tests/property/test_chunking_properties.py`'s
    exact structure (module docstring, `@settings(max_examples=...)` budget comment — bound this test
    lower than the chunking property's 200, e.g. 20-50, since each example drives real
    testcontainers-backed I/O and Decimal/date parsing, not free in-memory string generation; state
    the chosen bound's rationale in a comment, citing this project's ~90-second feedback-latency
    convention adjusted for an integration-tier test). Build a small Hypothesis strategy generating
    valid `customers`-shaped row tuples (bounded `customer_id`, a name from a short fixed list, a
    country code, a valid `birth_date` in `%Y-%m-%d` form, a valid `event_ts` in
    `%Y-%m-%dT%H:%M:%S%z` form — reuse `tests/fixtures/slice-corpus.yaml`'s own value pools as the
    generation source rather than generating arbitrary strings, since this property is about
    determinism given VALID input, not about re-testing every rejection path already covered
    elsewhere in this phase). For each generated table: serialize it to CSV bytes, upload to the
    throwaway MinIO (via `s3_client`/`minio_config` from `tests/integration/conftest.py`), build a
    `PipelineContext` from `customers.yaml`'s real config and `migrated_dsn`, call
    `StagingLoader(target_columns=(...)).load(ctx, conn)` TWICE against two fresh
    `run_id`s/staging tables, query both staging tables' `_record_hash` columns ordered by
    `_source_row_number`, and assert the two ordered lists are identical. Then repeat with a second
    `DatasetConfig` differing in exactly one normalization-relevant field and assert at least one hash
    differs.

    This test is integration-tier (needs Docker via testcontainers) — mark it with the project's
    existing `@pytest.mark.integration` convention (matching every other Docker-dependent test in this
    repository) so it runs under `make test-integration`, not the fast unit-tier default.
  </action>
  <acceptance_criteria>
    - `pytest tests/property/test_determinism.py -m integration -x -q` exits 0
    - The test module contains both the "same config → same hashes" and "different config → different hashes" assertions
    - `pytest tests/property/test_determinism.py -m integration --durations=5` shows the whole file completing well within this project's documented feedback-latency expectations for the integration tier
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundary — this plan tests the already-threat-modeled pipeline end-to-end | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-23 | Repudiation | A non-deterministic hash undermining every downstream claim of reproducibility (README §67, this platform's Core Value) | mitigate | This plan's own existence is the mitigation — a failing property test here is the earliest, cheapest possible signal that some upstream stage (encoding, normalization, hashing) introduced hidden non-determinism (wall-clock, randomness, iteration-order dependence) |
</threat_model>

<verification>
- `pytest tests/property/test_determinism.py -m integration -x -q`
- `pytest tests/unit tests/property tests/regression -q` (final full offline sweep for the phase)
- `pytest tests/integration -m integration -q` (final full integration sweep for the phase)
</verification>

<success_criteria>
- Identical input+config+version produces an identical `_record_hash` set, proven over a range of generated valid inputs, not one example
- A genuine configuration change is proven to change at least one hash — the property is non-vacuous
</success_criteria>

<output>
Create `.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-17-SUMMARY.md` when done
</output>
