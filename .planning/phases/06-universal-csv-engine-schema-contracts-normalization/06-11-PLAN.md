---
phase: 06-universal-csv-engine-schema-contracts-normalization
plan: 11
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - packages/dataplat/src/dataplat/normalize/boolean_null.py
  - packages/dataplat/src/dataplat/normalize/unicode.py
  - tests/unit/normalize/test_boolean_null.py
  - tests/unit/normalize/test_unicode.py
autonomous: true
requirements: [CSV-10, CSV-12, QUAL-04]
must_haves:
  truths:
    - "1 and 0 are never silently interpreted as boolean absent contract evidence that they are boolean tokens at all"
    - "An unmapped boolean token (e.g. Maybe) is rejected, never defaulted to false"
    - "NULL Industries (a company name containing the token NULL) survives as data, never destroyed by substring-matching against the null-token list"
    - "Every string value is NFC-normalized before it could ever reach a hash computation, unconditionally, for every dataset"
    - "A None field produced by NullTokenNormalizer passes through every downstream normalizer that can see it (BooleanNormalizer for its own column, UnicodeNormalizer for every column) unchanged — never crashing, never coerced back into a string"
  artifacts:
    - path: "packages/dataplat/src/dataplat/normalize/boolean_null.py"
      provides: "NullTokenNormalizer + BooleanNormalizer — exact-match token normalization, never substring, never a silent default"
      contains: "class BooleanNormalizer"
    - path: "packages/dataplat/src/dataplat/normalize/unicode.py"
      provides: "UnicodeNormalizer — unconditional NFC normalization (D-15), the hard ordering edge every hash computation this project ever adds must sit downstream of"
      contains: "class UnicodeNormalizer"
  key_links:
    - from: "packages/dataplat/src/dataplat/normalize/unicode.py"
      to: "packages/dataplat/src/dataplat/pipeline/engine.py::RaggedRowGuard"
      via: "implements the same StreamingStage protocol"
      pattern: "class UnicodeNormalizer\\(StreamingStage\\)"
---

<objective>
Build CSV-10's boolean/NULL half and CSV-12's Unicode NFC normalizer, both as `StreamingStage`s: exact
(never substring) matching against contract-declared NULL tokens and locale-specific boolean token
maps, rejecting any unmapped boolean value rather than defaulting it; and an unconditional,
non-configurable NFC normalization pass that is D-15's fixed platform rule — every string value, for
every dataset, before any hash computation ever runs.

Purpose: two of the independently-parallel normalizer plans, grouped into one plan because both are
individually small relative to dates/numeric (a token lookup and a single `unicodedata.normalize`
call). Corpus fixtures 24 (null tokens, with the deliberate `"NULL Industries"` trap), 60 (boolean
tokens, with the `O`-means-French-`Oui` trap), 42 (invisible zero-width/bidi characters) and 44 (NFC
vs. NFD) each pin exactly the failure mode this plan must not reproduce.
Output: `NullTokenNormalizer`, `BooleanNormalizer`, `UnicodeNormalizer` — all `StreamingStage`s, unit
tested against every CSV-10-boolean/null and CSV-12-tagged corpus fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-RESEARCH.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-PATTERNS.md
</context>

<interfaces>
```python
# packages/dataplat/src/dataplat/pipeline/engine.py::RaggedRowGuard -- the shape both classes mirror
# (see plan 06-09's <interfaces> block for the full quoted class)

# packages/dataplat/src/dataplat/config/model.py
class NormalizationConfig(BaseModel):
    null_tokens: list[str] = [""]           # D-14 default: empty string only
    boolean_true_tokens: list[str] = []
    boolean_false_tokens: list[str] = []
    ...

# packages/dataplat/src/dataplat/diagnostics.py
DIAGNOSTIC_CODES: Final[frozenset[str]]  # contains "unmapped-boolean-token"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NullTokenNormalizer + BooleanNormalizer — exact-match tokens, never substring, never a default</name>
  <files>packages/dataplat/src/dataplat/normalize/boolean_null.py, tests/unit/normalize/test_boolean_null.py</files>
  <read_first>
    packages/dataplat/src/dataplat/pipeline/engine.py
    packages/dataplat/src/dataplat/models/record.py
  </read_first>
  <behavior>
    - Fixture `24_null_values.csv`, `null_tokens=["", "NULL", "null", "N/A", "NA", "-", "None"]`: rows
      1-7 (empty string, `"NULL"`, `"N/A"`, `"NA"`, `"null"`, `"-"`, `"None"`) all normalize to absent.
      Row 8 (`"NULL Industries"`) is NOT absent — it survives as the literal string `"NULL Industries"`
      (proving the match is exact-equality against the whole field, never `"NULL" in value`).
    - Fixture `60_boolean_localized.csv`, `boolean_true_tokens=["Tak", "Ja", "O", "Y"]`,
      `boolean_false_tokens=["Nie", "Nein", "N"]`: `"Tak"`/`"Ja"`/`"O"`/`"Y"` all normalize to `True`;
      `"Nie"`/`"Nein"`/`"N"` all normalize to `False`. `"Maybe"` (row 8, not in either list) produces
      a `RejectedRecord` with `error_type == "unmapped-boolean-token"` — never silently defaulted to
      `False`.
    - A bare `"0"` or `"1"` value, with NO boolean tokens declared including them, is left untouched
      by `BooleanNormalizer` — it must never independently decide `"1"`/`"0"` mean
      `True`/`False` absent an explicit contract declaration naming them (CSV-10's own named risk).
    - A field value of `None` at `BooleanNormalizer`'s `column_index` (already normalized to absent by
      an upstream `NullTokenNormalizer` for a nullable boolean column — this plan's own platform-wide
      `None` convention, below) passes through unchanged — never rejected as
      `"unmapped-boolean-token"`, never coerced to `False`, never re-processed as if it were a string.
  </behavior>
  <action>
    Create `dataplat/normalize/boolean_null.py` with the module-conventions shape (module docstring
    citing CSV-10 and fixtures 24/60 by number, and the "matching is exact, never substring" rule
    stated as prominently as the corpus fixture's own comment states it).

    Platform-wide null representation (this is the load-bearing decision every other normalizer in
    this phase — and plan 06-09/06-10's `DateNormalizer`/`NumericNormalizer` — depends on, so it is
    NOT a per-class stylistic choice): an absent field is represented as Python `None` placed directly
    into the row tuple, widening `RecordChunk.rows`'s element type from `tuple[str, ...]` to
    `tuple[str | None, ...]`. Update `RecordChunk`'s docstring/type reference with a code comment here,
    since this is the first stage in the codebase to widen a row's field type beyond `str`. Every
    normalizer staged AFTER a `NullTokenNormalizer` for the same column (or, for `UnicodeNormalizer`
    in Task 2 below, ANY column, since it runs unconditionally last over the whole row) MUST treat an
    already-`None` field as "already normalized, pass through unchanged" rather than attempting to
    parse/transform/reject it. Check `RaggedRowGuard`'s and `StagingLoader`'s existing `tuple[str,
    ...]` assumptions elsewhere in this codebase and confirm nothing downstream of these normalizers
    assumes every field is a `str` without checking.

    Implement two separate `StreamingStage` classes in this one file:
    - `class NullTokenNormalizer(StreamingStage)`, `name = "null_token_normalizer"`, constructor
      `column_index: int`, `column_name: str`, `null_tokens: tuple[str, ...]`. `apply()`: for each
      row, if the field's raw value is an EXACT match (`value in self._null_tokens`, never
      `token in value`) against any declared token, replace the field with `None` per the
      platform-wide representation above.
    - `class BooleanNormalizer(StreamingStage)`, `name = "boolean_normalizer"`, constructor
      `column_index: int`, `column_name: str`, `true_tokens: tuple[str, ...]`, `false_tokens: tuple[str,
      ...]`. `apply()`: FIRST, if the row's value at `column_index` is already `None` (normalized to
      absent upstream by a `NullTokenNormalizer` for this same nullable column, per plan 06-16's stage
      ordering that guarantees `NullTokenNormalizer` runs before `BooleanNormalizer` for any nullable
      column), pass the field through unchanged and do not attempt any token lookup or rejection for
      it. Otherwise, exact-match the field's raw value against `true_tokens` (→ the Python value
      `True`) or `false_tokens` (→ `False`); a value in neither set is rejected with `error_type ==
      "unmapped-boolean-token"`. `"0"`/`"1"` receive NO special-cased handling anywhere in this class —
      they participate in exact-match lookup exactly like any other string and are rejected as
      unmapped unless a caller explicitly included them in one of the two token lists.

    Write `tests/unit/normalize/test_boolean_null.py` covering every case in this task's `<behavior>`
    block for both classes, using the same placeholder-context pattern as plan 06-09/06-10.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/normalize/test_boolean_null.py -x -q` exits 0
    - The test module contains an explicit test asserting `"NULL Industries"` survives `NullTokenNormalizer.apply()` unchanged
    - The test module contains an explicit test asserting a bare `"0"`/`"1"` with no declared boolean tokens is NOT interpreted as boolean by `BooleanNormalizer` — it is rejected as unmapped when boolean typing is being enforced, or left untouched when this stage is simply not applied to that column (state clearly in the test which behavior you are proving)
    - A synthetic row whose value at `BooleanNormalizer`'s `column_index` is `None` produces a surviving row with that field still `None`, not a `RejectedRecord`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UnicodeNormalizer — unconditional NFC, the hard ordering edge</name>
  <files>packages/dataplat/src/dataplat/normalize/unicode.py, tests/unit/normalize/test_unicode.py</files>
  <read_first>
    packages/dataplat/src/dataplat/pipeline/engine.py
  </read_first>
  <behavior>
    - Fixture `44_unicode_nfc_vs_nfd.csv`: the NFC-form `"Wiśniewski"` and the NFD-form
      `"Wiśniewski"` (visually identical, byte-distinct) both normalize to the SAME string after
      `UnicodeNormalizer.apply()` — assert `unicodedata.is_normalized("NFC", result)` is `True` for
      both, and that the two rows' normalized values are byte-identical to each other.
    - Fixture `42_zero_width_and_bidi.csv`: values containing U+200B (zero-width space) and U+200E
      (left-to-right mark) are NOT stripped by NFC normalization alone (NFC does not remove these code
      points — verify this against the actual fixture behavior rather than assuming; NFC only
      canonicalizes composed/decomposed forms). Document explicitly in this stage's docstring, citing
      the fixture, that invisible-character STRIPPING is out of this stage's scope (NFC ≠ invisible-
      character removal) — this plan builds exactly D-15's locked rule (NFC only), nothing broader.
    - `UnicodeNormalizer.apply()` NEVER rejects a row — `StageResult.rejected` is always empty for
      this stage, since D-15 states NFC is a pure, always-succeeding transform.
    - A field value of `None` anywhere in a row (produced by an upstream `NullTokenNormalizer` for any
      nullable column — Task 1's platform-wide `None` convention) passes through
      `UnicodeNormalizer.apply()` unchanged as `None` — never passed to `unicodedata.normalize()`
      (which raises `TypeError` on a non-`str` argument), never coerced to the literal string
      `"None"`. This matters more for `UnicodeNormalizer` than for any other stage in this phase:
      unlike the per-column normalizers, `UnicodeNormalizer` runs unconditionally, LAST, over EVERY
      column of EVERY row (per plan 06-16's wiring) — including nullable columns it did not itself
      null out.
  </behavior>
  <action>
    Create `dataplat/normalize/unicode.py` with the module-conventions shape (module docstring citing
    CSV-12, D-15 verbatim — "every string value is NFC-normalized before hashing/comparison, for
    every dataset, unconditionally. No per-dataset NFC/NFD/none choice" — and the hard ordering edge
    from ROADMAP.md's plan guidance: "normalization MUST precede hashing... both deduplication and
    SCD change detection hash normalized content"). Implement `class UnicodeNormalizer(StreamingStage)`,
    `name = "unicode_normalizer"`, with NO configuration parameters at all beyond
    `field_delimiter`-style bookkeeping if genuinely needed (there is no per-dataset knob for this
    stage — its entire point is having none). `apply(ctx, chunk)`: for every field in every row, if the
    field's value is `None` (already normalized to absent by an upstream `NullTokenNormalizer` for a
    nullable column — this stage runs unconditionally LAST over every column, including ones it did
    not itself null out), leave it as `None` unchanged; otherwise replace it with
    `unicodedata.normalize("NFC", value)`. Always returns
    `StageResult(chunk=chunk.replace(rows=normalized_rows), rejected=[], findings=[])` — document in
    the docstring why `rejected` is always empty here (this is the one exception to
    every other normalizer's row-rejecting behavior in this phase), and document the `None`-passthrough
    guard immediately next to it, since a `TypeError` from calling `unicodedata.normalize()` on a
    non-`str` field is the single most likely regression this always-on, every-column stage could
    introduce.

    Write `tests/unit/normalize/test_unicode.py` covering every case in this task's `<behavior>` block.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/normalize/test_unicode.py -x -q` exits 0
    - `python -c "
import unicodedata
from dataplat.normalize.unicode import UnicodeNormalizer
n = UnicodeNormalizer()
print('ok')
"` exits 0
    - The test module contains an explicit assertion that the NFC and NFD forms of the same name (fixture 44) become byte-identical strings after normalization
    - A synthetic row containing `None` in one field and a normal string in another produces a surviving row where the `None` field is still `None` and the string field is NFC-normalized — `UnicodeNormalizer.apply()` never raises `TypeError`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw field values → `NullTokenNormalizer`/`BooleanNormalizer`/`UnicodeNormalizer` | Untrusted content; every match is exact-equality, never a pattern/substring match that could be exploited to misclassify adjacent data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Tampering | Substring null-token matching silently destroying real data containing a token as a substring | mitigate | Fixture 24's explicit `"NULL Industries"` test proves exact-equality matching only |
| T-06-01 | Tampering (data integrity) | Encoding-confusion / homograph strings producing different hash keys for visually-identical values | mitigate | `UnicodeNormalizer`'s unconditional NFC pass is D-15's actual mitigation for this project-wide threat, proven against fixture 44 here — the wiring plan (06-16) is responsible for ensuring this stage runs before any hash computation in the real pipeline |
| T-06-18 | Elevation of Privilege | An unmapped boolean value silently defaulting to `False`, producing an incorrect access/eligibility decision downstream | mitigate | `BooleanNormalizer` rejects unmapped values outright rather than defaulting — fixture 60's own framing: "a boolean has two values so a default is always half wrong" |
| T-06-24 | Denial of Service (data-quality) | An unguarded `None` field (from an upstream `NullTokenNormalizer`) reaching `unicodedata.normalize()` and raising an unhandled `TypeError`, crashing the entire chunk's processing rather than failing one row | mitigate | `UnicodeNormalizer`'s `None`-passthrough guard (Task 2) is checked first, before any transform is attempted, for every field of every row |
</threat_model>

<verification>
- `pytest tests/unit/normalize/test_boolean_null.py tests/unit/normalize/test_unicode.py -x -q`
- `pytest tests/unit -x -q`
</verification>

<success_criteria>
- Fixtures 24, 42, 44, 60 all pass against their own `expect:` blocks
- `"NULL Industries"` and a bare `"0"`/`"1"` both survive their respective normalizers exactly as the corpus demands
- NFC normalization is unconditional, always-succeeding, and proven to collapse fixture 44's NFC/NFD pair
- `None` fields pass through `BooleanNormalizer` and `UnicodeNormalizer` unchanged, never raising and never being coerced back into a string
</success_criteria>

<output>
Create `.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-11-SUMMARY.md` when done
</output>
