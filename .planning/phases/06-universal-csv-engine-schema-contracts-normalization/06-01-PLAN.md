---
phase: 06-universal-csv-engine-schema-contracts-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/csv-processor/pyproject.toml
  - packages/csv-processor/src/csv_processor/detect/__init__.py
  - packages/dataplat/src/dataplat/normalize/__init__.py
  - packages/dataplat/src/dataplat/schema/__init__.py
  - tests/unit/detect/__init__.py
  - tests/unit/normalize/__init__.py
  - tests/unit/schema/__init__.py
  - tools/corpus/manifest.py
  - tools/corpus/generators.py
  - tests/fixtures/corpus.yaml
  - tests/unit/test_corpus_semantic_fixtures.py
  - migrations/versions/0009_meta_schema_versions.py
  - tests/integration/test_migrations.py
autonomous: true
requirements: [CSV-11, SCHEMA-03]
must_haves:
  truths:
    - "A developer running `uv sync` inside packages/csv-processor gets charset-normalizer, chardet and clevercsv installed, ready for every downstream detector plan to import"
    - "make fixtures regenerates a corpus that includes a real .zip fixture, byte-identically, from the seed"
    - "alembic upgrade head against a throwaway PostgreSQL creates meta.schema_versions with the exact ARCHITECTURE.md §2.1 column shape and closes migration 0004's deferred FK"
    - "Every new Python package directory this phase needs (detect/, normalize/, schema/, and their test mirrors) exists so Wave 2's eleven parallel plans never race to create the same __init__.py"
  artifacts:
    - path: "packages/csv-processor/pyproject.toml"
      provides: "charset-normalizer, chardet, clevercsv as direct dependencies"
    - path: "migrations/versions/0009_meta_schema_versions.py"
      provides: "meta.schema_versions table + the FK closing migration 0004's deferred column"
      contains: "schema_versions"
    - path: "tests/fixtures/corpus.yaml"
      provides: "a 70th fixture: a .zip-compressed wrapper around 01_simple.csv"
  key_links:
    - from: "migrations/versions/0009_meta_schema_versions.py"
      to: "meta.ingestion_runs.schema_version_id"
      via: "op.create_foreign_key"
      pattern: "create_foreign_key.*schema_version_id"
---

<objective>
Lay the Wave-0 groundwork every other Phase 6 plan depends on: declare the three new CSV-detection
libraries as real dependencies, teach the corpus generator to produce `.zip` fixtures (today it only
knows `gzip`), add the `.zip` fixture itself and fix the hardcoded fixture-count assertion that would
otherwise break the moment it lands, create the `meta.schema_versions` migration that closes
migration 0004's deliberately-deferred foreign key, and pre-create every new Python package directory
this phase's eleven parallel Wave-2 plans will write into (so no two concurrently-running plans ever
try to create the same `__init__.py`).

Purpose: this is pure "make the next eleven plans possible" work — no detection logic, no
normalization logic, nothing dataset-facing. Everything here is either a one-line dependency
declaration, a mechanical extension of an already-proven generator pattern (`.gz` → `.zip`), or a
migration that mirrors two already-committed migrations almost verbatim.
Output: `packages/csv-processor` can `import charset_normalizer`/`chardet`/`clevercsv`; `make fixtures`
produces a 70-fixture corpus including one `.zip`; `alembic upgrade head` creates
`meta.schema_versions`; six new empty package directories exist for Wave 2 to write into.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the three detection libraries as dependencies; create every new package directory</name>
  <files>packages/csv-processor/pyproject.toml, packages/csv-processor/src/csv_processor/detect/__init__.py, packages/dataplat/src/dataplat/normalize/__init__.py, packages/dataplat/src/dataplat/schema/__init__.py, tests/unit/detect/__init__.py, tests/unit/normalize/__init__.py, tests/unit/schema/__init__.py</files>
  <read_first>
    packages/csv-processor/pyproject.toml
    packages/dataplat/src/dataplat/config/__init__.py
    packages/dataplat/src/dataplat/sources/__init__.py
    tests/unit/__init__.py
  </read_first>
  <action>
    In `packages/csv-processor/pyproject.toml`'s `[project.dependencies]` list, extend the existing
    `dependencies = ["dataplat", "click>=8.4,<9"]` line to
    `["dataplat", "click>=8.4,<9", "charset-normalizer>=3.4.9,<4", "chardet>=7.5.1,<8", "clevercsv>=0.8.5,<1"]`
    — the exact pins 06-RESEARCH.md's Standard Stack table verified live this session. Add a one-line
    comment above the new entries explaining why they are direct (not transitive) dependencies of
    `csv_processor` specifically, not `dataplat` (detection is exclusively `csv_processor`'s territory
    per `setup.cfg`'s import-linter contract 1). Run `uv lock` from the repo root afterward so
    `uv.lock` picks up the three new resolved versions, then `uv sync` so they are actually importable
    in the local venv.

    Create six new empty Python package directories, each holding only a docstring-plus-future-import
    `__init__.py` matching the exact convention documented in `packages/dataplat/src/dataplat/config/__init__.py`
    and `packages/dataplat/src/dataplat/sources/__init__.py` (a module docstring stating "this package
    marker re-exports nothing — callers import from the submodule directly", then
    `from __future__ import annotations`, nothing else):
    - `packages/csv-processor/src/csv_processor/detect/__init__.py` — docstring names this as home to
      the five CSV-01/02/03/04/05/06/07/08 detectors landing in Wave 2 of this phase.
    - `packages/dataplat/src/dataplat/normalize/__init__.py` — docstring names this as home to the
      CSV-09/10/12 `StreamingStage` normalizers landing in Wave 2.
    - `packages/dataplat/src/dataplat/schema/__init__.py` — docstring names this as home to the
      SCHEMA-03/04/05/06 versioning/evolution/repository modules landing in Wave 2.
    - `tests/unit/detect/__init__.py`, `tests/unit/normalize/__init__.py`, `tests/unit/schema/__init__.py`
      — mirror `tests/unit/__init__.py`'s existing (trivial) shape exactly.

    These six directories exist ONLY so that Wave 2's eleven concurrently-executing plans never need
    to create the same file as a sibling plan — every Wave 2 task that adds a new module into one of
    these packages MUST NOT recreate or modify its `__init__.py`; it already exists after this task.
  </action>
  <acceptance_criteria>
    - `packages/csv-processor/pyproject.toml` contains `charset-normalizer>=3.4.9,<4`, `chardet>=7.5.1,<8`, `clevercsv>=0.8.5,<1`
    - `uv run python -c "import charset_normalizer, chardet, clevercsv"` from `packages/csv-processor/` exits 0
    - All six `__init__.py` files exist and each is non-empty (a real docstring, not a placeholder)
    - `uv run python -c "import csv_processor.detect, dataplat.normalize, dataplat.schema"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Teach the corpus generator to build .zip fixtures; add the .zip fixture; fix the fixture-count assertion</name>
  <files>tools/corpus/manifest.py, tools/corpus/generators.py, tests/fixtures/corpus.yaml, tests/unit/test_corpus_semantic_fixtures.py</files>
  <read_first>
    tools/corpus/manifest.py
    tools/corpus/generators.py
    tests/fixtures/corpus.yaml
    tests/unit/test_corpus_semantic_fixtures.py
  </read_first>
  <action>
    In `tools/corpus/manifest.py`, change the module-level constant
    `_COMPRESSIONS: Final[tuple[str, ...]] = ("gzip",)` to `("gzip", "zip")`. No other change is
    needed in this file — `_validate_wrapper` already only checks `fixture.compression is not None`
    generically.

    In `tools/corpus/generators.py`'s `_write_wrapper` function, replace the single unconditional
    `if fixture.compression != "gzip":` guard-and-raise with a dispatch: keep the existing gzip branch
    exactly as-is (verified working), and add a new `elif fixture.compression == "zip":` branch that
    opens `zipfile.ZipFile(raw, mode="w")` and writes the target's bytes via `zf.writestr(...)`.
    Pin `zipfile.ZipInfo.date_time` to `(1980, 1, 1, 0, 0, 0)` (ZIP's minimum representable date)
    explicitly when constructing the `ZipInfo` for the written member — `ZipInfo` defaults its
    `date_time` field to the current wall-clock time, which would make this fixture exactly as
    non-reproducible across runs as an unpinned gzip fixture (mirrors this same function's existing
    R5 comment for gzip's `mtime`/`filename`). Only raise `GeneratorError` for a compression value
    outside `{"gzip", "zip"}` (currently unreachable, guarded at load time — keep the `# pragma: no cover`
    marker on that final else branch). Import `zipfile` at module scope alongside the existing `gzip`
    import.

    In `tests/fixtures/corpus.yaml`, add one new fixture entry immediately after `61_gzipped.csv.gz`
    (before `62_multipart_split`, matching its `wrapper`-generator neighbor), named
    `"71_zipped.csv.zip"` — the next number after the highest currently declared (70) — with
    `covers: [CSV-11]`, `generator: wrapper`, `wraps: "01_simple.csv"`, `compression: zip`, and an
    `expect:` block shaped exactly like `61_gzipped.csv.gz`'s (`decompresses_to: "01_simple.csv"`,
    `data_rows: 20`). Do not add `gzip_mtime`/`gzip_filename` fields — those are gzip-specific header
    fields with no zip equivalent once `date_time` is pinned per Task 2's generator change above.
    Add a one-sentence comment above the new fixture explaining it is `.zip`'s counterpart to
    `61_gzipped.csv.gz`, added in Phase 6 to give CSV-11's `.zip` half real corpus coverage (Pitfall 8:
    no `.zip` fixture existed before this phase).

    In `tests/unit/test_corpus_semantic_fixtures.py`, find the module's hardcoded fixture-count
    assertion and the `README_SEVENTY_THREE`/`FEATURES_THREE_FOUR` name tuples (read the full file
    around lines 60-160 to find every place "69" or the two tuples are referenced, including the
    module docstring's own "THE CORPUS IS COMPLETE AS OF PHASE 1... sixty-nine" framing in
    `corpus.yaml`'s header comment, which this task does NOT touch — that comment documents Phase 1's
    original completeness claim and stays historically accurate as written). Add `"71_zipped.csv.zip"`
    to whichever tuple/list this test module uses to assert the corpus's declared name set, and update
    every place a literal `69` describes the total fixture count to `70`. Update `corpus.yaml`'s own
    header comment block (the one stating "THE CORPUS IS COMPLETE AS OF PHASE 1... sixty-nine
    fixtures... no gaps, no duplicates, no inventions") with one additional sentence noting Phase 6
    added a 70th (`71_zipped.csv.zip`) per README §73's own "grow the corpus as edge cases are
    discovered" policy — do not rewrite the rest of that comment block.
  </action>
  <acceptance_criteria>
    - `grep -n '"zip"' tools/corpus/manifest.py` shows `_COMPRESSIONS` containing both `"gzip"` and `"zip"`
    - `grep -n "elif fixture.compression == .zip." tools/corpus/generators.py` matches
    - `make fixtures` (or `uv run python -m tools.corpus generate --manifest tests/fixtures/corpus.yaml --out <tmp>`) exits 0 and produces `71_zipped.csv.zip` whose contents decompress via `python -m zipfile -e` to the same bytes as `01_simple.csv`
    - `pytest tests/unit/test_corpus_semantic_fixtures.py -x -q` exits 0
    - `make fixtures-verify` exits 0 (digest oracle regenerated for the new fixture and matches)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: meta.schema_versions migration; flip the pre-0009 FK-absence test to FK-presence</name>
  <files>migrations/versions/0009_meta_schema_versions.py, tests/integration/test_migrations.py</files>
  <read_first>
    migrations/versions/0001_meta_datasets_config_versions.py
    migrations/versions/0004_meta_ingestion_runs.py
    tests/integration/test_migrations.py
    tests/integration/conftest.py
  </read_first>
  <action>
    Create `migrations/versions/0009_meta_schema_versions.py` with `revision = "0009"`,
    `down_revision = "0008"` (confirm `0008` is still the current head by checking
    `migrations/versions/` for any file with `down_revision = "0008"` before writing this value — if a
    later migration already exists, chain onto the actual current head instead). Use
    06-RESEARCH.md's Code Examples section's already-verified draft migration as the concrete starting
    point (it was built by reading migrations 0001 and 0004 directly, not independently invented):
    `op.create_table("schema_versions", ...)` in schema `meta`, with columns `schema_version_id`
    (BigInteger Identity PK), `dataset_id` (BigInteger FK to `meta.datasets.dataset_id`, not null),
    `version` (Integer, not null), `schema_hash` (Text, not null), `hash_version` (SmallInteger, not
    null, `server_default=sa.text("1")` — META-02), `columns` (JSONB, not null — ordered
    name/type/nullable/position/format entries), `derived_from` (Text, not null — app-validated
    `"CONTRACT"`/`"INFERRED"`, never a native Postgres enum, matching every other migration's
    convention), `compatibility` (Text, not null — app-validated `"COMPATIBLE"`/`"BREAKING"`),
    `breaking_changes` (JSONB, nullable), `valid_from`/`valid_to` (DateTime with timezone,
    `valid_from` not null, `valid_to` nullable). Add `UniqueConstraint("dataset_id", "version",
    name="uq_schema_versions_dataset_version")` and a partial unique index
    `uq_schema_versions_current_per_dataset` on `dataset_id` with
    `postgresql_where=sa.text("valid_to IS NULL")` — the identical "at most one CURRENT row" pattern
    `meta.config_versions` already uses (migration 0001). `GRANT SELECT, INSERT, UPDATE ON
    meta.schema_versions TO etl_app`. Then close migration 0004's deliberately-deferred FK via
    `op.create_foreign_key("fk_ingestion_runs_schema_version_id", "ingestion_runs", "schema_versions",
    ["schema_version_id"], ["schema_version_id"], source_schema="meta", referent_schema="meta")` —
    the exact call migration 0004's own comment names as the closing action. Write a `downgrade()`
    that drops the FK first, then drops the table (reverse order of `upgrade()`).

    In `tests/integration/test_migrations.py`, rename
    `test_ingestion_runs_schema_version_id_has_no_fk` to
    `test_ingestion_runs_schema_version_id_has_an_fk_after_0009` and invert its assertion: instead of
    `assert rows == []`, assert exactly one row is returned and that its `constraint_name` equals
    `"fk_ingestion_runs_schema_version_id"`. This test currently passes BECAUSE the FK does not exist
    yet — once migration 0009 lands it would otherwise start failing by design; this task's edit is
    the fix, not an incidental side effect. Also add `("meta", "schema_versions")` to this test
    module's `EXPECTED_TABLES` set and to `GRANTED_TABLES`, and add
    `("meta", "schema_versions", "hash_version")` to `HASH_VERSION_COLUMNS` — mirroring the existing
    entries for `meta.files`/`meta.config_versions`/`normalized.customers` exactly, so
    `test_upgrade_head_creates_the_slice_schema`, `test_etl_app_grants` and
    `test_hash_version_columns` all cover the new table without a separate new test.
  </action>
  <acceptance_criteria>
    - `alembic -c migrations/alembic.ini upgrade head` against a throwaway PostgreSQL exits 0 and creates `meta.schema_versions`
    - `pytest tests/integration/test_migrations.py -m integration -x -q` exits 0 (all tests, including the renamed one and the three extended-set tests)
    - `grep -n "test_ingestion_runs_schema_version_id_has_no_fk" tests/integration/test_migrations.py` returns no match (old name gone)
    - `grep -n "fk_ingestion_runs_schema_version_id" migrations/versions/0009_meta_schema_versions.py` matches inside `op.create_foreign_key(`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml dependency declaration | Adds three third-party packages to the supply chain of every future `csv-processor` image build |
| Alembic migration | DDL executed with elevated (schema-owner) privileges against the analytical database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering (supply chain) | `charset-normalizer`, `chardet`, `clevercsv` npm/pip installs | accept | 06-RESEARCH.md's Package Legitimacy Audit already ran `slopcheck install charset-normalizer chardet clevercsv parse` this research session — all three came back `[OK]`, none `[ASSUMED]`/`[SUS]`/`[SLOP]`, cross-verified via PyPI JSON API and live installation. No blocking human-verify checkpoint is required per the package-legitimacy gate rule (only `[ASSUMED]`/`[SUS]` packages need one). |
| T-06-06 | Elevation of Privilege | migration 0009's `GRANT SELECT, INSERT, UPDATE ON meta.schema_versions TO etl_app` | mitigate | Grants exactly the same three privileges every other `meta.*` table already grants `etl_app` (no `DELETE`/`TRUNCATE`/`DROP`), verified by `test_etl_app_grants`'s extended assertion set in Task 3 |
</threat_model>

<verification>
- `pytest tests/unit/test_corpus_semantic_fixtures.py -x -q` — corpus count/name assertions pass
- `make fixtures && make fixtures-verify` — generator produces the new `.zip` fixture byte-identically
- `pytest tests/integration/test_migrations.py -m integration -x -q` — migration 0009 creates the right shape and closes the FK
- `uv run python -c "import charset_normalizer, chardet, clevercsv, csv_processor.detect, dataplat.normalize, dataplat.schema"` — every new dependency and package directory is importable
</verification>

<success_criteria>
- `packages/csv-processor/pyproject.toml` declares the three detection libraries at their pinned versions and `uv sync` installs them
- The corpus is 70 fixtures, including a real `.zip` wrapper, generated from the seed and verified against a regenerated digest oracle
- `meta.schema_versions` exists after `alembic upgrade head`, with the exact ARCHITECTURE.md §2.1 column shape, and `meta.ingestion_runs.schema_version_id` now carries a real foreign key
- Six new empty package directories exist, ready for Wave 2's eleven plans to populate without file-creation races
</success_criteria>

<output>
Create `.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-01-SUMMARY.md` when done
</output>
