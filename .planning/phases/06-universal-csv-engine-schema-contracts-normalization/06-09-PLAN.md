---
phase: 06-universal-csv-engine-schema-contracts-normalization
plan: 09
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - packages/dataplat/src/dataplat/normalize/dates.py
  - tests/unit/normalize/test_dates.py
  - tests/property/test_dst_correctness.py
autonomous: true
requirements: [CSV-09, QUAL-17, QUAL-04]
must_haves:
  truths:
    - "2026-02-30, 31/02/2026 and not-a-date all produce explicit rejected records naming the row, never a silently coerced or dropped date"
    - "A nonexistent local time (DST spring-forward gap) is rejected with a named diagnostic; an ambiguous local time (DST autumn overlap) requires a declared fold policy, never silently takes the first fold"
    - "A two-digit year is only ever resolved via a contract-declared pivot, never CPython's inherited POSIX default"
    - "An Excel spreadsheet serial date that denotes a day that never existed (the phantom 1900 leap day) is rejected, never silently converted via naive offset arithmetic"
    - "A None field value (already normalized to absent by an upstream NullTokenNormalizer for a nullable date/timestamp column) passes through DateNormalizer unchanged — never parsed, never rejected as an invalid date"
  artifacts:
    - path: "packages/dataplat/src/dataplat/normalize/dates.py"
      provides: "DateNormalizer — a StreamingStage parsing declared-format dates/timestamps, classifying DST gap/overlap, rejecting invalid values as RejectedRecords"
      contains: "class DateNormalizer"
    - path: "tests/property/test_dst_correctness.py"
      provides: "QUAL-17's property test: DST gap/overlap classification holds for arbitrary generated local times, not just the corpus's three fixed examples"
  key_links:
    - from: "packages/dataplat/src/dataplat/normalize/dates.py"
      to: "packages/dataplat/src/dataplat/pipeline/engine.py::RaggedRowGuard"
      via: "implements the same StreamingStage protocol, same constructor-injection convention"
      pattern: "class DateNormalizer\\(StreamingStage\\)"
---

<objective>
Build CSV-09's explicit-format date/timestamp normalizer as a `StreamingStage` (mirroring
`RaggedRowGuard` exactly — a `name` class attribute, an `apply(ctx, chunk) -> StageResult` that never
raises for a row-level problem), and QUAL-17's DST-correctness property test using the verified
`zoneinfo` + PEP 495 `fold` classification function that exactly reproduces every value in corpus
fixture `55_dst_gap_and_overlap.csv`. Every parse uses `datetime.strptime` against a contract-declared
format — never `dateutil.parser.parse`, never a guessed format.

Purpose: one of the independently-parallel normalizer plans. This is the single most fixture-dense
piece of Phase 6 — nine corpus fixtures (22, 23, 52, 53, 54, 55, 56, 59, plus `50`'s scientific-notation
sibling) each pin a specific, named failure mode a naive date parser gets wrong. `customers` needs this
for real (`birth_date`/`event_ts`, per its new `columns:` contract from plan 06-02), but wiring it into
the live pipeline is a later plan's job (06-16) — this plan proves the normalizer correct in isolation.
Output: `DateNormalizer`, unit-tested against every date/timestamp-tagged corpus fixture, plus a
Hypothesis property test proving DST classification holds generally, not just for the corpus's three
fixed rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-RESEARCH.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-PATTERNS.md
</context>

<interfaces>
```python
# packages/dataplat/src/dataplat/pipeline/protocol.py -- the protocol this stage implements
class StreamingStage(Protocol):
    name: str
    def apply(self, ctx: PipelineContext, chunk: RecordChunk) -> StageResult: ...

# packages/dataplat/src/dataplat/pipeline/engine.py::RaggedRowGuard -- the EXACT shape to mirror,
# including constructor-injection of runtime parameters (never read from a global):
class RaggedRowGuard(StreamingStage):
    name = "ragged_row_guard"
    def __init__(self, *, field_delimiter: str = ",") -> None: ...
    def apply(self, ctx: PipelineContext, chunk: RecordChunk) -> StageResult:
        # kept: list[tuple[str, ...]]; rejected: list[RejectedRecord]
        # never raise for a row-level problem; return StageResult(chunk=chunk.replace(rows=...), rejected=rejected, findings=[])

# packages/dataplat/src/dataplat/models/record.py
@dataclass(frozen=True, slots=True)
class RejectedRecord:
    source_row_number: int
    error_type: str       # a diagnostics.py code, e.g. "invalid-calendar-date"
    error_message: str
    raw_line: str
    error_column: str | None = None

# packages/dataplat/src/dataplat/diagnostics.py
DIAGNOSTIC_CODES: Final[frozenset[str]]
# contains: "invalid-calendar-date", "nonexistent-local-time",
#           "naive-timestamp-without-a-declared-zone", "spreadsheet-serial-date-does-not-exist"
```

The verified DST classification function (06-RESEARCH.md Code Examples, exactly reproduces every
value in corpus fixture 55) — copy directly:

```python
import datetime as dt
from zoneinfo import ZoneInfo

def classify_naive_local(naive: dt.datetime, zone: ZoneInfo) -> str:
    aware_fold0 = naive.replace(tzinfo=zone, fold=0)
    utc0 = aware_fold0.astimezone(dt.timezone.utc)
    roundtrip0 = utc0.astimezone(zone).replace(tzinfo=None)
    if roundtrip0 != naive:
        return "nonexistent"
    utc1 = naive.replace(tzinfo=zone, fold=1).astimezone(dt.timezone.utc)
    return "ambiguous" if utc0 != utc1 else "unambiguous"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DateNormalizer — explicit strptime parsing, invalid-date rejection, two-digit-year pivot, spreadsheet epoch</name>
  <files>packages/dataplat/src/dataplat/normalize/dates.py, tests/unit/normalize/test_dates.py</files>
  <read_first>
    packages/dataplat/src/dataplat/pipeline/engine.py
    packages/dataplat/src/dataplat/pipeline/protocol.py
    packages/dataplat/src/dataplat/models/record.py
  </read_first>
  <behavior>
    - Fixture `22_eu_dates.csv` with format `"%d/%m/%Y"` parses `"31/12/2026"` → `date(2026, 12, 31)`.
    - Fixture `23_us_dates.csv` with format `"%m/%d/%Y"` parses `"12/31/2026"` → `date(2026, 12, 31)`
      — same underlying day, different declared format, proving the format is what decides, not the
      rendering.
    - `"2026-02-30"` (calendar-invalid: February has no 30th) under format `"%Y-%m-%d"` produces a
      `RejectedRecord` with `error_type == "invalid-calendar-date"`, never a coerced nearby date.
    - `"not-a-date"` under any format produces the same rejection shape, never an exception escaping
      `apply()`.
    - Fixture `53_two_digit_year.csv`: with `two_digit_year_pivot` unset, parsing a two-digit-year
      value raises a configuration-level error BEFORE any row is processed (a missing pivot is a
      contract-authoring mistake, not a row-level problem) — with the pivot declared (e.g. pivot=68,
      matching CPython's own inherited default so the test has a concrete, checkable value), every
      row in the fixture parses to the fixture's own declared `iso_values_under_the_inherited_pivot`.
    - Fixture `54_excel_serial_dates.csv`: serial `60` under `spreadsheet_epoch="1900"` produces a
      `RejectedRecord` with `error_type == "spreadsheet-serial-date-does-not-exist"` (the phantom 1900
      leap day) — serial `45880` produces the fixture's own declared
      `serial_45880_under_the_1900_system` value; the same serial under `spreadsheet_epoch="1904"`
      produces the fixture's own declared `serial_45880_under_the_1904_system` value (proving the
      epoch, not the serial number, decides the date).
    - A field value of `None` at `column_index` (already normalized to absent by an upstream
      `NullTokenNormalizer` for a nullable date/timestamp column — see plan 06-11 Task 1's
      platform-wide `None` convention, and plan 06-16's stage ordering that guarantees
      `NullTokenNormalizer` runs BEFORE `DateNormalizer` for any nullable column) passes through
      `DateNormalizer.apply()` unchanged — never attempted against `strptime` or spreadsheet-epoch
      arithmetic, never rejected as `"invalid-calendar-date"`.
  </behavior>
  <action>
    Create `dataplat/normalize/dates.py` with the module-conventions shape (module docstring citing
    CSV-09 and the corpus fixtures this stage is built against by number). Implement
    `class DateNormalizer(StreamingStage)` with `name = "date_normalizer"`, constructor parameters
    threaded in exactly like `RaggedRowGuard.__init__`'s `field_delimiter` (never read from a global
    or re-derived internally): `column_index: int`, `column_name: str`, `format: str | None` (the
    declared strptime format for ordinary date/timestamp columns), `two_digit_year_pivot: int | None`,
    `spreadsheet_epoch: str | None` (mutually exclusive with `format` — a column is either a rendered
    date under a strptime format OR a spreadsheet serial integer, never both; raise a plain
    `ValueError` from `__init__` if both or neither are supplied). `apply(ctx, chunk)` reads each
    row's value at `column_index`. If that value is already `None` (normalized to absent by an
    upstream `NullTokenNormalizer` for this same nullable column — this is the common case for any
    nullable date/timestamp column once plan 06-16 wires `NullTokenNormalizer` to run first), leave
    the field as `None` unchanged and move on to the next field — do not call `strptime`/epoch
    arithmetic on it, and do not reject the row for it. Otherwise, parse it (via
    `datetime.strptime(value, self.format)` for the format case, wrapping `ValueError` into a
    `RejectedRecord` with `error_type == "invalid-calendar-date"`; via epoch-offset arithmetic for the
    spreadsheet-serial case, rejecting non-existent-date serials like the 1900-system's phantom leap
    day with `error_type == "spreadsheet-serial-date-does-not-exist"`), REPLACES that field's string
    value in the row tuple with a canonical ISO-format string (`date.isoformat()`/
    `datetime.isoformat()`) on success, and accumulates any rejections. Two-digit-year handling: when
    `self.format` contains `%y`, require `two_digit_year_pivot` to be set (raise `ValueError` from
    `__init__` otherwise — a missing pivot is a contract-authoring mistake per fixture 53's own
    framing: "the pivot is a contract decision rather than something a reader may invent"); apply the
    pivot by re-deriving the four-digit year from `strptime`'s own two-digit-year result via the
    declared pivot boundary (do not simply trust CPython's inherited default — compute it explicitly
    and test it against fixture 53's declared `python_strptime_inherited_pivot` table to prove your
    pivot logic is deliberate, not accidental).

    Write `tests/unit/normalize/test_dates.py` covering every case in `<behavior>` above, using
    `_make_context()`'s placeholder-`PipelineContext` pattern from `tests/unit/test_pipeline_errors.py`
    (a minimal `RunContext`, everything else unused by the code under test) and building `RecordChunk`s
    directly from each corpus fixture's declared `raw_values`/`iso_values` pairs (no need to run the
    full corpus generator for this normalizer's own unit tests — the fixture's `expect:` block already
    IS the input/output pair).
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/normalize/test_dates.py -x -q` exits 0
    - `python -c "
from dataplat.normalize.dates import DateNormalizer
n = DateNormalizer(column_index=0, column_name='d', format='%Y-%m-%d', two_digit_year_pivot=None, spreadsheet_epoch=None)
print('ok')
"` exits 0
    - A synthetic chunk containing `'2026-02-30'` produces exactly one `RejectedRecord` with `error_type == 'invalid-calendar-date'` and zero surviving rows for that record
    - A synthetic chunk containing `None` (not the string `""`) at `column_index` produces a surviving row with that field still `None`, and zero `RejectedRecord`s for that row
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Naive-timestamp DST classification (fixtures 55/56) — wired into DateNormalizer</name>
  <files>packages/dataplat/src/dataplat/normalize/dates.py, tests/unit/normalize/test_dates.py</files>
  <read_first>
    packages/dataplat/src/dataplat/errors.py
  </read_first>
  <behavior>
    - Fixture `55_dst_gap_and_overlap.csv` row 1 (`"2026-03-29 02:30:00"`, `Europe/Warsaw`): rejected
      with `error_type == "nonexistent-local-time"`.
    - Fixture `55` row 2 (`"2026-10-25 02:30:00"`): with `ambiguous_time_policy` unset, rejected with
      `error_type == "naive-timestamp-without-a-declared-zone"` is WRONG for this case — it must
      instead surface as a distinct rejection requiring the fold policy (use a new, precise
      `error_type` you choose and add to `dataplat/diagnostics.py`'s catalog if the ten pre-seeded
      codes from plan 06-02 do not already cover it exactly — check the catalog first; if genuinely
      missing, add ONE new kebab-case entry there in this same task, since no other Wave 2 plan is
      concurrently editing `diagnostics.py`'s exact same lines for this specific case). With
      `ambiguous_time_policy="earliest"` declared, row 2 resolves to `row_2_utc_under_fold_0`; with
      `"latest"`, it resolves to `row_2_utc_under_fold_1`.
    - Fixture `55` row 3 (unambiguous case) resolves to the fixture's own declared `row_3_utc`.
    - Fixture `56_mixed_timezone_offsets.csv` row 3 (naive, no offset, no declared zone) rejected with
      `error_type == "naive-timestamp-without-a-declared-zone"` — must NOT inherit the offset of the
      row above it, must NOT default to UTC, must NOT default to the server's zone.
    - The `None`-passthrough guard added in Task 1 applies identically to this task's timezone-aware
      branch: a field value of `None` never reaches `classify_naive_local` or any fold-policy logic —
      it is handled once, at the top of `apply()`, before any format/timezone branching happens.
  </behavior>
  <action>
    Add `classify_naive_local` to `dataplat/normalize/dates.py` exactly as verified in this plan's
    `<interfaces>` block (copy directly — it is proven against every value in fixture 55 already).
    Extend `DateNormalizer` with two more mutually-exclusive-with-`format` constructor parameters:
    `timezone: str | None` (an IANA zone name — when set, values are parsed as naive local times and
    classified via `classify_naive_local`) and `ambiguous_time_policy: str = "reject"`
    (`"reject"`/`"earliest"`/`"latest"`). When `timezone` is set and a parsed value classifies as
    `"nonexistent"`, reject with `error_type == "nonexistent-local-time"`. When it classifies as
    `"ambiguous"`: `"reject"` policy rejects the row (choose and document the exact `error_type` you
    use, updating `dataplat/diagnostics.py` if genuinely needed — see this task's `<behavior>` note);
    `"earliest"`/`"latest"` resolve via `fold=0`/`fold=1` respectively and the row survives, with its
    field replaced by the resolved UTC ISO-8601 string. When no `timezone`/`format` is declared at all
    for a column whose values are naive (fixture 56 row 3's exact case), reject with `error_type ==
    "naive-timestamp-without-a-declared-zone"`. Do not duplicate Task 1's `None`-passthrough guard here
    — it already lives at the top of `apply()` and covers this branch too; only add the
    timezone-specific logic below that existing guard.

    Extend `tests/unit/normalize/test_dates.py` with every case in this task's `<behavior>` block.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/normalize/test_dates.py -x -q` exits 0
    - Fixture 55's three rows produce exactly the classifications the fixture declares (`nonexistent`, `ambiguous`-requiring-policy, `unambiguous`)
    - Fixture 56 row 3 produces `error_type == "naive-timestamp-without-a-declared-zone"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: QUAL-17's DST-correctness property test</name>
  <files>tests/property/test_dst_correctness.py</files>
  <read_first>
    tests/property/test_chunking_properties.py
  </read_first>
  <action>
    Create `tests/property/test_dst_correctness.py`, mirroring `tests/property/test_chunking_properties.py`'s
    exact structure (module docstring, `@settings(max_examples=...)` budget comment citing this
    project's ~90-second feedback-latency convention). Use the verified Hypothesis strategy from
    06-RESEARCH.md's Code Examples section (`st.datetimes(min_value=..., max_value=..., timezones=
    st.just(ZoneInfo("Europe/Warsaw")), allow_imaginary=True)` bounded to the spring-forward window
    around `2026-03-29`) to generate candidate local times, and assert `classify_naive_local`'s
    classification matches what `DateNormalizer` (from plan 06-09's Task 2) actually does when fed
    that value: every `"nonexistent"`-classified time produces a `RejectedRecord` with
    `error_type == "nonexistent-local-time"`; every value that round-trips cleanly and is unambiguous
    is accepted and its resolved UTC value equals `astimezone(UTC)`'s own direct computation. Add a
    second bounded strategy around the autumn-overlap window (`2026-10-25`) and assert every
    `"ambiguous"`-classified time, under `ambiguous_time_policy="reject"`, is rejected — and under
    `"earliest"`/`"latest"`, resolves to the `fold=0`/`fold=1` UTC value respectively, for every
    generated example, not just the corpus's one fixed row.
  </action>
  <acceptance_criteria>
    - `pytest tests/property/test_dst_correctness.py -x -q` exits 0
    - The test file contains at least two `@given(...)` property tests (gap window, overlap window)
    - `pytest tests/property/test_dst_correctness.py -x -q --durations=5` completes in well under 90 seconds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw date/timestamp field values → `DateNormalizer.apply()` | Untrusted content; every parse path is a bounded `strptime` call against an explicit format, never a guessing parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | A crafted date value engineered to produce a plausible-but-wrong date under an ambiguous format | mitigate | CSV-09's core guarantee: `datetime.strptime` against an explicit, contract-declared format either parses correctly or raises — never `dateutil`-style guessing. Fixture 52's genuinely-undecidable case is proven to decline, not guess |
| T-06-29 | Denial of Service | Hypothesis-generated adversarial datetime ranges in the property test causing runaway example generation | accept | Both strategies are explicitly bounded to a fixed calendar window (one day around each transition), never an open-ended range |
</threat_model>

<verification>
- `pytest tests/unit/normalize/test_dates.py tests/property/test_dst_correctness.py -x -q`
- `pytest tests/unit tests/property -x -q`
</verification>

<success_criteria>
- Every date/timestamp-tagged corpus fixture (22, 23, 52, 53, 54, 55, 56, 59) passes against its own `expect:` block
- `2026-02-30`/`31/02/2026`/`not-a-date` all produce explicit `RejectedRecord`s, never a coerced value
- The DST gap/overlap classification is proven as a Hypothesis property, not just three fixed corpus rows
- A `None` field (already-normalized absent) passes through `DateNormalizer` unchanged in both the plain-format and timezone-aware branches
</success_criteria>

<output>
Create `.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-09-SUMMARY.md` when done
</output>
