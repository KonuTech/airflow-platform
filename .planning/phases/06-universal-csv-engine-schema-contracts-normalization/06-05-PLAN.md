---
phase: 06-universal-csv-engine-schema-contracts-normalization
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - packages/csv-processor/src/csv_processor/detect/dialect.py
  - tests/unit/detect/test_dialect.py
autonomous: true
requirements: [CSV-04, CSV-05, CSV-06, QUAL-04]
must_haves:
  truths:
    - "Comma, semicolon, pipe, tab and colon dialects all detect correctly against the corpus"
    - "A single-column file with no delimiter anywhere is detected as such, never crashes and never guesses a delimiter"
    - "Dialect detection runs on a sample where a column's decimal separator cannot be confused with the field delimiter, proven against 68_utf8_bom_semicolon_pl_excel.csv"
    - "A contract-declared delimiter always overrides detection"
  artifacts:
    - path: "packages/csv-processor/src/csv_processor/detect/dialect.py"
      provides: "detect_dialect — clevercsv wrapper with the single-column guard and contract override"
      contains: "def detect_dialect"
  key_links:
    - from: "tests/unit/detect/test_dialect.py"
      to: "csv_processor/detect/dialect.py::detect_dialect"
      via: "corpus-fixture-parametrized calls"
      pattern: "detect_dialect\\("
---

<objective>
Build CSV-04/05/06's dialect detector: `clevercsv.Detector().detect(sample)` wrapped with the
single-column crash guard 06-RESEARCH.md verified live (`SimpleDialect(delimiter='', ...)` raises on
`.to_csv_dialect()` unless special-cased first), a contract-declared override path (CSV-05), and a
result type ready for `csv.reader` to consume directly for the real streaming parse.

Purpose: this is one of the five independently-parallel detector plans. `clevercsv` is the verified
choice over a hand-rolled heuristic (it correctly separates a `;` delimiter from a `,` decimal
separator on exactly the shape that would corrupt every amount column under a naive approach) and
over stdlib `csv.Sniffer` (documented to raise on single-column files and misidentify delimiters
inside quoted fields).
Output: `detect_dialect`, corpus-fixture-parametrized against every dialect/quoting-tagged fixture,
producing a `csv.Dialect`-compatible result the streaming reader can be constructed from.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-CONTEXT.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-RESEARCH.md
@.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-PATTERNS.md
</context>

<interfaces>
```python
# packages/dataplat/src/dataplat/errors.py
class CsvDialectDetectionError(SourceError): ...
class CsvParsingError(SourceError): ...

# packages/dataplat/src/dataplat/diagnostics.py
DIAGNOSTIC_CODES: Final[frozenset[str]]  # contains "dialect-detection-declined"

# packages/dataplat/src/dataplat/config/model.py
class CsvParsingConfig(BaseModel):
    delimiter: str | None = None   # contract override; None = detect
    quotechar: str = '"'
    ...
```

Pitfall 1's verified guard (06-RESEARCH.md Common Pitfalls #1, exact reproduction of fixture
`38_single_column_no_delimiter.csv`'s crash): check `dialect.delimiter == ""` immediately after
`clevercsv.Detector().detect(sample)` returns, BEFORE calling `.to_csv_dialect()` — that call raises
`_csv.Error: "delimiter" must be a 1-character string` otherwise, always on genuinely single-column
input.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_dialect — clevercsv wrapper with the single-column guard</name>
  <files>packages/csv-processor/src/csv_processor/detect/dialect.py, tests/unit/detect/test_dialect.py</files>
  <read_first>
    packages/csv-processor/src/csv_processor/source.py
  </read_first>
  <behavior>
    - Fixtures `02_semicolon.csv`, `03_pipe.csv`, `04_tab.csv`, `01_simple.csv` (comma): each detects
      its own declared delimiter exactly.
    - Fixture `38_single_column_no_delimiter.csv`: does not raise; returns a result whose delimiter is
      explicitly "declined"/`None`, matching the fixture's own `detection_declines: true`,
      `detected_delimiter: null` expectation — never a guessed comma/semicolon/pipe/tab.
    - Fixture `68_utf8_bom_semicolon_pl_excel.csv`: detects `;` as the delimiter even though the file
      also contains comma-shaped decimal values (`1234,56`) — proving dialect detection runs
      independently of, and before, any numeric-content inspection.
    - Fixture `37_delimiter_frequency_differs_header_vs_body.csv`: detects correctly despite the header
      row's delimiter-character frequency differing from the body rows' (read this fixture's own
      `expect:` block for the exact assertion — do not assume; the fixture exists specifically because
      a frequency-only heuristic gets this wrong).
    - A `contract_delimiter` argument, when provided, short-circuits detection entirely and is used
      as-is, regardless of what the sample would otherwise detect.
  </behavior>
  <action>
    Create `csv_processor/detect/dialect.py` with the module-conventions shape (module docstring
    citing CSV-04/05/06 and Pitfall 1). Implement `detect_dialect(sample: str, *, contract_delimiter:
    str | None = None) -> DialectDetection` — `sample` is already-decoded text (dialect detection runs
    on decoded text, after the encoding detector's output, per the architecture diagram in
    06-RESEARCH.md). `DialectDetection` is a frozen dataclass carrying at minimum `delimiter: str |
    None` (`None` means "declined — the caller must fall back to a contract value or treat this as
    single-column"), `quotechar: str`, and a `declined: bool` flag. When `contract_delimiter` is
    given, skip `clevercsv` entirely and return it directly. Otherwise call
    `clevercsv.Detector().detect(sample)`; if the result's `delimiter == ""` (Pitfall 1's exact guard,
    check BEFORE calling `.to_csv_dialect()`), return the declined result. Otherwise convert via
    `.to_csv_dialect()` and populate the result from it.

    Add `to_stdlib_dialect(detection: DialectDetection) -> type[csv.Dialect]` (or an equivalent
    factory producing whatever shape `csv.reader(..., dialect=...)` needs) — this is the function a
    later wiring plan (06-14) calls to actually construct the streaming reader; build it now so this
    plan is self-contained and its own tests can prove round-trip correctness (parse a corpus fixture
    with `csv.reader` constructed from this function's output and assert the parsed rows match the
    fixture's own row content).

    Write `tests/unit/detect/test_dialect.py` as a corpus-parametrized suite (same
    `load_manifest`/`generate_corpus` pattern as the encoding detector plan) over every fixture whose
    `covers` includes `"CSV-04"` or `"CSV-05"` (02, 03, 04, 08, 09, 10, 35, 36, 37, 38, 40, 66, 68,
    plus `20_decimal_comma.csv`/`21_decimal_point.csv`/`52_date_ambiguous_dm_vs_md.csv` which also
    cite CSV-05). For each, assert the detected delimiter matches the fixture's own `detected_delimiter`
    expectation exactly, including the `null` (declined) case for fixture 38.
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/detect/test_dialect.py -x -q` exits 0
    - `python -c "from csv_processor.detect.dialect import detect_dialect; r = detect_dialect('customer_reference\nCUST-000001\n'); assert r.declined and r.delimiter is None; print('ok')"` prints `ok` without raising
    - No `_csv.Error` is ever raised by `detect_dialect` itself for any corpus fixture's content
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire CSV-06's real-parser proof (quoted delimiters, escaped quotes, multiline) and CsvParsingError</name>
  <files>packages/csv-processor/src/csv_processor/detect/dialect.py, tests/unit/detect/test_dialect.py</files>
  <read_first>
    packages/dataplat/src/dataplat/errors.py
  </read_first>
  <action>
    Add coverage proving CSV-06's requirement text literally — "quoted delimiters, escaped quotes,
    multiline fields and inconsistent quoting are handled by a real CSV parser — never string
    splitting" — via `to_stdlib_dialect`'s round-trip: for fixtures `08_quoted_fields.csv`,
    `09_embedded_commas.csv`, `10_embedded_newlines.csv`, `35_quote_in_unquoted_field.csv`,
    `36_doubled_vs_backslash_escape.csv`, and `66_triple_nasty.csv` (all three hazards in one field),
    construct a real `csv.reader` from `to_stdlib_dialect`'s output over the fixture's actual decoded
    content and assert the parsed field values match the fixture's own declared `value_row_N`/similar
    expectations exactly (fixture 66 declares `value_row_1_payload: "a,b\nc\"d"` — assert your parser
    reconstructs exactly that string, not an approximation).

    `detect_dialect`/`to_stdlib_dialect` themselves do not need to raise `CsvDialectDetectionError` for
    any case this plan covers (every dialect-tagged corpus fixture either detects cleanly or declines
    cleanly — declining is a valid, non-exceptional outcome per fixture 38's own framing). Add exactly
    one raise site: when `to_stdlib_dialect` is called on a `DialectDetection` where `declined=True`
    and no `contract_delimiter` was ever supplied (i.e., there is genuinely no usable delimiter),
    raise `CsvDialectDetectionError` (from `dataplat.errors`) with
    `context={"diagnostic_code": "dialect-detection-declined"}` — this is the boundary between "declined,
    but the caller has a contract fallback" (not an error) and "declined, with no fallback at all"
    (a real file/run-fatal condition).
  </action>
  <acceptance_criteria>
    - `pytest tests/unit/detect/test_dialect.py -x -q` exits 0
    - Fixture 66's round-trip test asserts the exact string `'a,b\nc"d'` for the payload field
    - `python -c "from csv_processor.detect.dialect import to_stdlib_dialect, DialectDetection
from dataplat.errors import CsvDialectDetectionError
try:
    to_stdlib_dialect(DialectDetection(delimiter=None, quotechar='\"', declined=True))
except CsvDialectDetectionError as e:
    assert e.context['diagnostic_code'] == 'dialect-detection-declined'
    print('ok')
"` prints `ok`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decoded file text (post-encoding-detection) → `clevercsv.Detector().detect()` | Untrusted content; clevercsv runs its consistency measure over a bounded sample |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-26 | Denial of Service | `clevercsv.Detector().detect()` over a large/adversarial sample | mitigate | Detection runs over a bounded sample (first ~64 KiB plus a mid-file sample, per STACK.md §F's documented convention), never the whole file — 06-RESEARCH.md explicitly warns "never run CleverCSV over the whole file — its consistency measure is superlinear-ish and it is a detector, not a parser" |
| T-06-10 | Tampering | A crafted file whose delimiter equals a numeric column's decimal separator, engineered to corrupt amount fields | mitigate | Dialect detection runs strictly before numeric normalization (architecture-wide hard ordering, proven against fixture 68) and the config-level collision validator (06-02) rejects a contract that would make this ambiguous by construction |
</threat_model>

<verification>
- `pytest tests/unit/detect/test_dialect.py -x -q`
- `pytest tests/unit -x -q`
</verification>

<success_criteria>
- Every CSV-04/05/06-tagged corpus fixture detects (or correctly declines) exactly per its `expect:` block
- The single-column crash (Pitfall 1) never reproduces
- `to_stdlib_dialect` round-trips real quoted/escaped/multiline content correctly via a genuine `csv.reader`
</success_criteria>

<output>
Create `.planning/phases/06-universal-csv-engine-schema-contracts-normalization/06-05-SUMMARY.md` when done
</output>
