---
phase: 03-dataplat-core-library-metadata-control-plane
plan: 07
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/dataplat/pyproject.toml
  - uv.lock
  - packages/dataplat/src/dataplat/cli.py
  - docker/csv-processor/Dockerfile
  - Makefile
  - docs/README.md
  - tests/unit/test_cli_error_handling.py
  - tests/policy/test_no_latest_image_tag.py
  - tests/integration/test_docker_image.py
autonomous: true
requirements: [INFRA-08, QUAL-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`dataplat --version` (the installed console script) prints the version resolved from installed distribution metadata, never a hardcoded literal (reusing `dataplat.version.resolve_version()` from Phase 1)"
    - "A `DataPlatformError` raised anywhere inside a CLI command is caught exactly once, logged with structured context, and produces a non-zero exit code — never a raw Python traceback (D-06, QUAL-03)"
    - "`docker build` produces an image whose runtime user is a numeric UID (1000), never root, and whose ENTRYPOINT is `dataplat`, matching ADR-0004's `--no-install-workspace --frozen` then `--locked` layer ordering"
    - "`docker run <image>:<git-sha> dataplat --version` succeeds and prints a version string — success criterion 3, proven as an automated integration test, not a manual step"
    - "No Makefile recipe or CI workflow ever tags the csv-processor image `:latest` — every build tags it by `git rev-parse --short HEAD` (INFRA-08), and a policy test fails the build if that regresses"
  artifacts:
    - path: "packages/dataplat/src/dataplat/cli.py"
      provides: "The pod entrypoint: --version, and the D-06 catch-exactly-once error boundary"
      contains: "def main"
    - path: "docker/csv-processor/Dockerfile"
      provides: "The multi-stage, non-root, git-SHA-tagged image build"
      contains: "ENTRYPOINT"
    - path: "tests/policy/test_no_latest_image_tag.py"
      provides: "INFRA-08 enforced as a fast, offline static check"
    - path: "tests/integration/test_docker_image.py"
      provides: "Success criterion 3 proven by an actual docker build + run"
  key_links:
    - from: "packages/dataplat/src/dataplat/cli.py"
      to: "packages/dataplat/src/dataplat/observability/logging.py"
      via: "structlog configured once at CLI startup"
      pattern: "configure"
    - from: "docker/csv-processor/Dockerfile"
      to: "packages/dataplat/pyproject.toml"
      via: "[project.scripts] dataplat = \"dataplat.cli:main\" — the console script the image's ENTRYPOINT invokes"
      pattern: "project.scripts"
  prohibitions:
    - "The Dockerfile must never install apache-airflow or anything from the Airflow image's dependency set (ADR-0004)."
    - "No credential, .env file or fixture secret may be baked into the image — trivy's secret scanner is the eventual CI gate (Phase 11); this plan's own acceptance criteria check for the absence directly."
---

<objective>
Give the phase its pod entrypoint and its first buildable, runnable container image. `cli.py`
wires `--version` and the D-06 catch-exactly-once error boundary; the Dockerfile follows
ADR-0004's exact layer ordering and tags by git SHA, never `:latest`, closing out INFRA-08.

Purpose: success criterion 3 is literal — `docker run csv-processor:<git-sha> dataplat --version`
must print the version. This plan is where that becomes true and automated, not asserted.

Output: `dataplat/cli.py`, `docker/csv-processor/Dockerfile`, a Makefile image-build target, and
three tests: CLI error-handling (unit), no-`:latest` tag policy (static), and an actual
build-and-run smoke test (integration).
</objective>

<user_story>
**As a** developer who just cloned this repository,
**I want to** run one Make target and get a working, git-SHA-tagged `csv-processor` image that
prints its own version when run,
**so that** I can prove the image builds correctly before any real DAG ever tries to schedule a
pod from it.
</user_story>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dataplat-core-library-metadata-control-plane/03-CONTEXT.md
@.planning/phases/03-dataplat-core-library-metadata-control-plane/03-RESEARCH.md
@.planning/phases/03-dataplat-core-library-metadata-control-plane/03-PATTERNS.md
@packages/dataplat/src/dataplat/errors.py
@packages/dataplat/src/dataplat/observability/logging.py
@packages/dataplat/src/dataplat/version.py
@docs/adr/0004-two-images-two-dependency-sets.md
@Makefile
@tests/policy/test_supply_chain_guards.py
@docs/README.md
</context>

<interfaces>
From `03-RESEARCH.md` lines 738-770 (verified, ADR-0004-compliant) — the exact Dockerfile shape:
dependency-only layer with `uv sync --frozen --no-install-workspace --no-dev`, then
`COPY packages/` and `uv sync --locked --no-dev`, then a `runtime` stage with `USER 1000` and
`ENTRYPOINT ["dataplat"]`. `[project.scripts] dataplat = "dataplat.cli:main"` in
`packages/dataplat/pyproject.toml` is the console script this ENTRYPOINT invokes.

From `03-PATTERNS.md` Cluster O — `main(argv) -> int` shape from `tools/corpus/__main__.py` lines
151-167 and the catch-once shape from `tools/security/gitleaks_selftest.py` lines 380-399, both
already quoted in this repo's own `03-PATTERNS.md`.

From `tests/policy/test_supply_chain_guards.py` line 323 — `MUTABLE_TAG_VALUES = frozenset({"",
"latest", "main", "master", "edge", "nightly"})`, the literal set to reuse when checking the
image-build recipe never selects one of these as the csv-processor tag.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cli.py — --version and the catch-exactly-once error boundary</name>
  <files>packages/dataplat/pyproject.toml, uv.lock, packages/dataplat/src/dataplat/cli.py, tests/unit/test_cli_error_handling.py</files>
  <read_first>
    - `03-RESEARCH.md` lines 734-766 (Cluster O reasoning, Standard Stack's `click` rationale)
    - `packages/dataplat/src/dataplat/version.py` — `resolve_version()`'s exact signature, already built in Phase 1
    - `packages/dataplat/src/dataplat/errors.py` (plan 03-01) — the four exception classes to catch
    - `packages/dataplat/src/dataplat/observability/logging.py` (plan 03-03) — `configure()`'s exact signature
    - `tools/security/gitleaks_selftest.py` lines 380-399 — the catch-domain-error-log-return-1 shape to mirror
  </read_first>
  <behavior>
    - Test 1: `CliRunner().invoke(cli, ["--version"])` exits 0 and stdout contains the string returned by `resolve_version()`
    - Test 2: invoking a command that raises `ConfigurationError("bad config")` internally exits 1, prints no raw Python traceback to stdout/stderr, and the structured log line (captured via `capsys`/a test-only logging hook) contains `"bad config"`
    - Test 3: invoking a command that raises a plain, undeclared `Exception` (not a `DataPlatformError`) is NOT caught by the D-06 boundary — it propagates (proving the catch is scoped to `DataPlatformError` only, never a blanket `except Exception`, per ARCHITECTURE.md §4.5's "No `except Exception` anywhere else")
    - Test 4: the click group is configured with `no_args_is_help=True` or equivalent, so invoking `dataplat` with zero arguments does not crash
  </behavior>
  <action>
    In `packages/dataplat/pyproject.toml`, add `[project.scripts]` with exactly
    `dataplat = "dataplat.cli:main"`. Run `uv lock` and commit the regenerated lockfile. This is
    the third, and last, `uv.lock` regeneration this phase makes: plan 03-01 first added
    `dataplat`'s runtime dependencies, plan 03-02 added the root `dev`/`cluster` tooling
    dependencies, and this is the console-script entry point — no further dependency or lockfile
    edit is planned after this task.

    `dataplat/cli.py`: a `click.group()` named `cli` (or equivalent), with one subcommand
    `--version` implemented as click's own `@click.version_option(version=resolve_version(),
    prog_name="dataplat")` decorator on the group — this is the idiomatic click shape for a
    version flag and avoids hand-rolling a `version` subcommand. Add a `main(argv: list[str] |
    None = None) -> int` function (the `[project.scripts]` entry point) that invokes the click
    group inside a `try/except DataPlatformError` block: on catch, call
    `dataplat.observability.logging.configure(in_cluster=<read from an env var such as
    DATAPLAT_LOG_JSON, defaulting appropriately>)` if not already configured, log the error via a
    bound structlog logger at `error` level with `error_type=type(exc).__name__,
    error_message=str(exc), **exc.context`, and return `1`; on success, return `0`. Configure
    logging once, near the top of `main()`, before dispatching to the click group — every future
    subcommand inherits this without reconfiguring. Do not catch bare `Exception` anywhere in this
    module. `if __name__ == "__main__": sys.exit(main())` at module end, matching
    `tools/corpus/__main__.py`'s convention.

    `tests/unit/test_cli_error_handling.py`: implement the four behaviors above using
    `click.testing.CliRunner`. For Tests 2-3, add a small test-only click command registered only
    within the test module (not in `cli.py` itself) that deliberately raises, so `cli.py`'s
    production surface stays limited to `--version` in this phase.
  </action>
  <acceptance_criteria>
    - `uv run --frozen pytest tests/unit/test_cli_error_handling.py -q` exits 0, 4+ tests collected
    - `uv run --frozen dataplat --version` (after `uv sync`) prints a version string and exits 0
    - `grep -c "except Exception" packages/dataplat/src/dataplat/cli.py` is 0
    - `uv run --frozen mypy packages/dataplat/src` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv lock --check && uv run --frozen pytest tests/unit/test_cli_error_handling.py -q && uv run --frozen dataplat --version && ! grep -q "except Exception" packages/dataplat/src/dataplat/cli.py && uv run --frozen mypy packages/dataplat/src</automated>
  </verify>
  <done>dataplat --version works end to end via the installed console script; DataPlatformError is caught exactly once with structured logging; an undeclared exception still propagates.</done>
</task>

<task type="auto">
  <name>Task 2: The csv-processor Dockerfile, its Make target, and the no-:latest policy test</name>
  <files>docker/csv-processor/Dockerfile, Makefile, docs/README.md, tests/policy/test_no_latest_image_tag.py</files>
  <read_first>
    - `03-RESEARCH.md` lines 736-770 (the complete, verbatim Dockerfile) and `docs/adr/0004-two-images-two-dependency-sets.md` in full — the `--no-install-workspace --frozen` then `--locked` ordering is load-bearing, not stylistic
    - `tests/policy/test_supply_chain_guards.py` lines 314-472 — the `MUTABLE_TAG_VALUES` frozenset and the image-pin-scanning idiom to adapt
    - `tests/policy/test_pinned_tool_versions_agree.py` lines 233-250 — the Makefile-recipe-body regex-scanning idiom (closer in shape to what this task's policy test needs than the YAML tree-walk)
    - `Makefile` lines 45-53 (`.PHONY`, `##` help convention) and lines 150-153 (a Docker/cluster-gated target's comment-block convention)
    - `docs/README.md` line 21 — currently claims `docker/csv-processor/` is "Phase 4"; this phase's own success criterion 3 contradicts that
  </read_first>
  <action>
    `docker/csv-processor/Dockerfile`: copy the verbatim structure from 03-RESEARCH.md's Code
    Examples — `FROM ghcr.io/astral-sh/uv:0.12.3 AS uv`, `FROM python:3.12-slim-bookworm AS
    builder`, copy the `uv` binary, `WORKDIR /app`, `ENV UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy`;
    copy `pyproject.toml`/`uv.lock` plus both members' `pyproject.toml` files, run `uv sync
    --frozen --no-install-workspace --no-dev` (dependency-only layer, cacheable across source
    changes); then `COPY packages/ packages/` and `uv sync --locked --no-dev` (full lockfile
    re-verified now that member sources exist); `FROM python:3.12-slim-bookworm AS runtime`,
    create a numeric non-root user (`groupadd -r app -g 1000 && useradd -r -g app -u 1000 -m
    app`), copy `/app/.venv` from the builder stage with `--chown=app:app`, set `PATH`/
    `PYTHONUNBUFFERED=1`/`PYTHONDONTWRITEBYTECODE=1`, `USER 1000`, `ENTRYPOINT ["dataplat"]`. Add
    OCI labels `org.opencontainers.image.revision`, `.source`, `.version`, each populated from a
    Docker build ARG (e.g. `ARG GIT_SHA` passed at build time), so the running container's own
    image metadata answers "which commit built this" — this is what makes §62 replayability
    concrete, and the label value should be the same string that becomes
    `meta.ingestion_runs.processor_image_digest` once a real run exists (Phase 4's job to wire,
    not this plan's).

    `Makefile`: add a target `image-csv-processor` to `.PHONY` with a `##` comment. Recipe: `docker
    build --build-arg GIT_SHA=$$(git rev-parse --short HEAD) -t
    csv-processor:$$(git rev-parse --short HEAD) -f docker/csv-processor/Dockerfile .` — the tag is
    ALWAYS the git short SHA, computed inline, never a literal and never `:latest`. This target
    joins neither `check` nor `ci` (it is exercised by the integration test in Task 3, and by a
    future CI image-publish job in Phase 11).

    `docs/README.md`: update the `docker/csv-processor/` row's "Filled by" column from "Phase 4"
    to "Phase 3", matching the correction this plan's own success criterion 3 requires.

    `tests/policy/test_no_latest_image_tag.py`: mirror `tests/policy/test_pinned_tool_versions_
    agree.py`'s Makefile-recipe-body regex-scanning idiom. Read the `image-csv-processor` target's
    recipe body from `Makefile` and assert: (a) it contains `git rev-parse --short HEAD` (or an
    equivalent git-SHA-producing expression) at least twice — once for the build arg, once for the
    tag; (b) it does not contain the literal substring `:latest` anywhere; (c) reusing
    `test_supply_chain_guards.py`'s `MUTABLE_TAG_VALUES` frozenset, none of those literal values
    appear as a hardcoded `-t csv-processor:<value>` substring in the recipe.
  </action>
  <acceptance_criteria>
    - `docker build -f docker/csv-processor/Dockerfile .` succeeds (build only, no run yet — Task 3 proves run)
    - `make image-csv-processor` builds and tags the image as `csv-processor:<current short SHA>`
    - `docs/README.md`'s `docker/csv-processor/` row says "Phase 3", not "Phase 4"
    - `uv run --frozen pytest tests/policy/test_no_latest_image_tag.py -q` exits 0
    - `uv run --frozen pytest tests/policy/test_no_latest_image_tag.py -q` FAILS if `:latest` is temporarily added to the Makefile recipe (verify manually during development, then revert — do not commit the temporary change)
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/policy/test_no_latest_image_tag.py -q && grep -q "git rev-parse --short HEAD" Makefile && ! grep -A3 "^image-csv-processor:" Makefile | grep -q ":latest"</automated>
  </verify>
  <done>The Dockerfile builds a non-root, git-SHA-labeled image via ADR-0004's exact layer ordering; make image-csv-processor never selects :latest; the policy test catches a regression.</done>
</task>

<task type="auto">
  <name>Task 3: Prove success criterion 3 — build, run, read the version back</name>
  <files>tests/integration/test_docker_image.py</files>
  <read_first>
    - `docker/csv-processor/Dockerfile` and the `Makefile` `image-csv-processor` target from Task 2 of this plan
    - `.planning/phases/03-dataplat-core-library-metadata-control-plane/03-VALIDATION.md` — the Manual-Only Verifications section, which states success criterion 3 must be an automated integration/build test, not a manual step
    - `03-RESEARCH.md` § Environment Availability — Docker daemon confirmed live this session (`docker ps` succeeded), 29.6.2
  </read_first>
  <action>
    `tests/integration/test_docker_image.py`: `test_image_builds_and_prints_its_version` — shells
    out to `docker build --build-arg GIT_SHA=<a fixed test value> -t
    csv-processor:test-<short random suffix or the actual current git SHA> -f
    docker/csv-processor/Dockerfile <repo root>` (use `subprocess.run` with a generous timeout —
    building `python:3.12-slim-bookworm`-based layers can take real wall-clock time on a cold
    cache; document in a comment that Docker layer caching makes repeat runs fast), asserts the
    build exits 0, then runs `docker run --rm csv-processor:<that tag> dataplat --version`,
    asserts it exits 0, and asserts stdout contains a non-empty version string (not the Phase-1
    `UNKNOWN_VERSION` sentinel `"0.0.0+unknown"` — the image must have `dataplat` genuinely
    installed, not merely importable from an uninstalled source tree). Clean up the built image
    (`docker rmi`) in a `finally`/fixture-teardown so repeated local runs do not accumulate
    dangling tags. Mark the test with a reasonably generous `pytest.mark.timeout` or rely on the
    `cluster`-group pytest invocation's own default timeout handling — a cold Docker build is the
    slowest single operation in this phase's test suite, and this test's placement in
    `tests/integration/` (not `tests/unit/`) reflects that: it never runs on every task commit,
    only per-wave and at the phase gate (per this phase's own Sampling Rate).
  </action>
  <acceptance_criteria>
    - `uv run --frozen --group cluster pytest tests/integration/test_docker_image.py -q` exits 0
    - The test's captured stdout from `docker run ... dataplat --version` is non-empty and is not the literal string `"0.0.0+unknown"`
    - No image tag used by this test is `:latest`
    - The built test image is removed after the test completes (verify no `csv-processor:test-*` or leftover tag remains via `docker images` after a local run)
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen --group cluster pytest tests/integration/test_docker_image.py -q</automated>
  </verify>
  <done>An automated test builds the real Dockerfile, runs it, and asserts dataplat --version prints a genuine installed version — success criterion 3 is mechanically proven, not manually eyeballed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdin/argv → click command dispatch | this phase's only command is `--version`; no untrusted input parsing risk yet, but the catch-once boundary is the pattern every later, riskier command (Phase 4's `ingest`) inherits |
| built image → registry (future) | this plan only builds locally; no push happens here, but the tag discipline (git-SHA-only) is the control that matters once a push exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Elevation of Privilege | container runtime user | mitigate | `USER 1000` (numeric, so Kubernetes `runAsNonRoot` can verify it), never root; asserted by the Dockerfile's own content, checked in Task 2's acceptance criteria |
| T-03-16 | Tampering | mutable `:latest` tag reintroduced later | mitigate | `tests/policy/test_no_latest_image_tag.py` fails the build on any recipe that selects a value from `MUTABLE_TAG_VALUES`; runs in `make check`'s `policy` target from this plan onward |
| T-03-SC | Tampering | base images (`python:3.12-slim-bookworm`, `ghcr.io/astral-sh/uv:0.12.3`) | accept | Both are official, widely-scanned publisher images already used elsewhere in this project's toolchain conventions; `trivy image` scanning is Phase 11's CI gate, not this phase's — recorded here so the gap is visible rather than silent |
</threat_model>

## Verification

- `uv run --frozen pytest tests/unit/test_cli_error_handling.py tests/policy/test_no_latest_image_tag.py -q` and `uv run --frozen --group cluster pytest tests/integration/test_docker_image.py -q` all green
- `make check` green with the new unit/policy tests collected

## Success criteria

- ROADMAP Phase 3 success criterion 3 is mechanically proven: `docker run csv-processor:<git-sha>
  dataplat --version` prints the version, from an image tagged by git SHA, never `:latest`
- QUAL-03's CLI-side catch-exactly-once boundary is proven, completing the requirement's
  errors-as-values (plan 03-06) and exception-hierarchy (plan 03-01) halves

<output>
Create `.planning/phases/03-dataplat-core-library-metadata-control-plane/03-07-SUMMARY.md` when done
</output>
