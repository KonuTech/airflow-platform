---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - packages/dataplat/src/dataplat/models/assignment.py
  - packages/dataplat/src/dataplat/models/receipt.py
  - packages/dataplat/src/dataplat/discovery.py
  - packages/dataplat/src/dataplat/config/model.py
  - configs/datasets/customers.yaml
autonomous: true
requirements: [ORCH-08, ORCH-03, LOAD-03, INCR-08]
must_haves:
  truths:
    - "Discovery lists s3://raw/customers/, hashes every new object, and freezes exactly one assignment document per unit before any KubernetesPodOperator task reads it"
    - "Re-running discovery over an unchanged set of objects produces byte-identical assignment documents and no new meta.files/meta.ingestion_runs rows"
    - "A re-uploaded file whose content hash already exists for this dataset is recorded with duplicate_of_file_id set and never assigned a run"
    - "The number of units discovery hands to Dynamic Task Mapping in one run never exceeds configs/datasets/customers.yaml's batching.max_units_per_run"
    - "meta.files.business_date is never populated from datetime.now() or any wall-clock read"
  artifacts:
    - path: "packages/dataplat/src/dataplat/models/assignment.py"
      provides: "AssignmentDocument pydantic model, extra=forbid"
    - path: "packages/dataplat/src/dataplat/discovery.py"
      provides: "discover_files() -- the frozen-manifest-authoring function"
    - path: "configs/datasets/customers.yaml"
      provides: "D-13's duplicate_policy: skip and batching.max_units_per_run"
  key_links:
    - from: "packages/dataplat/src/dataplat/discovery.py"
      to: "s3://metadata/assignments/<dataset>/<run>/unit-*.json"
      via: "ObjectStore.put_object"
      pattern: "assignments/"
    - from: "packages/dataplat/src/dataplat/discovery.py"
      to: "meta.ingestion_runs"
      via: "MetadataRepository.get_or_create_ingestion_run"
      pattern: "get_or_create_ingestion_run"
---

<objective>
Build the frozen-manifest authoring function every discovery run executes, and the two data contracts (`AssignmentDocument`, `Receipt`) that cross the Airflow-DAG-to-pod and pod-to-Airflow boundaries. This is the ORCH-08 mechanism made concrete: list once, hash once, freeze once, never re-derive from a live listing mid-run.

Purpose: `discover_files` is the one place file identity, content-hash-based deduplication (LOAD-03/D-13), the idempotency-key formula (ARCHITECTURE.md Q7), and the Dynamic-Task-Mapping fan-out cap (ORCH-03) all meet.
Output: `dataplat.discovery.discover_files()`, `dataplat.models.assignment.AssignmentDocument`, `dataplat.models.receipt.Receipt`, and the two new `configs/datasets/customers.yaml` fields these depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
<!-- The idempotency_key formula, verbatim from .planning/research/ARCHITECTURE.md Question 7
(lines 790-798), read directly this session -- do not invent a different formula. -->
```
idempotency_key = sha256(
    dataset_name  ‖ file.content_sha256 ‖ config_hash ‖
    schema_version ‖ processor_image_digest ‖ target_partition ‖ policy_digest
)
```
`try_number` and `dag_run_id` are DELIBERATELY ABSENT — an Airflow retry must produce the identical key.

**This phase's adaptation** (schema_version/target_partition/policy_digest have no populated value yet -- `schema_version_id` stays NULL until Phase 6, no partitioning or quarantine-policy concept exists until Phase 6/8): compute `idempotency_key = sha256(f"{dataset_name}|{content_sha256_hex}|{config_hash}|{processor_image}").hexdigest()`. A later phase EXTENDS this formula by appending the missing terms once they have real values; it does not replace it. State this explicitly in the module docstring, citing ARCHITECTURE.md Q7 by name, so a future reader does not mistake the omission for an oversight.

The assignment-document JSON shape, adapted from ARCHITECTURE.md §6.2 (lines 706-731) to this phase's populated fields only (drop `schema_version_id`, `partition`, `policy` -- none exist yet):
```json
{
  "assignment_version": 1,
  "run_id": 8123,
  "idempotency_key": "a3f1...",
  "dataset": "customers",
  "config_version_id": 7,
  "config_hash": "9c2e...",
  "file": {"file_id": 9911, "object_uri": "s3://raw/customers/....csv",
            "content_sha256": "7d1a...", "size_bytes": 18234421},
  "batch": {"batch_key": "customers:...", "batch_id": 442}
}
```
Note: singular `"file"`, not the array `"files"` ARCHITECTURE.md shows — this phase is one-file-one-batch (03-RESEARCH.md's own documented simplification), so pluralizing now would be premature generality with no consumer.

The receipt shape, verbatim from ARCHITECTURE.md §6.3 (line 743-745), adapted the same way (drop `quarantined` -- no quarantine concept exists yet; keep everything else):
```json
{"run_id": 8123, "status": "SUCCEEDED", "rows_read": 182734, "rows_loaded": 182722,
 "rows_invalid": 0, "rows_deduplicated": 12, "duration_ms": 41022,
 "report_uri": null}
```

From `packages/dataplat/src/dataplat/config/model.py` (current) -- `SourceConfig`, the model to extend:
```python
class SourceConfig(BaseModel):
    model_config = ConfigDict(extra="forbid", frozen=True)
    type: str
    bucket: str
    path: str
    change_semantics: str
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AssignmentDocument, Receipt, and the two new config fields</name>
  <files>packages/dataplat/src/dataplat/models/assignment.py, packages/dataplat/src/dataplat/models/receipt.py, packages/dataplat/src/dataplat/config/model.py, configs/datasets/customers.yaml</files>
  <read_first>
    packages/dataplat/src/dataplat/config/model.py
    packages/dataplat/src/dataplat/config/loader.py
    configs/datasets/customers.yaml
    packages/dataplat/src/dataplat/models/record.py
  </read_first>
  <behavior>
    - `AssignmentDocument.model_validate_json(...)` on the exact JSON shape in Interfaces succeeds and round-trips every field
    - `AssignmentDocument.model_validate_json(...)` on the same JSON with one extra, unrecognized top-level key raises `pydantic.ValidationError` (proves `extra="forbid"` -- this is the untrusted-input validation boundary T-04-02 names: the assignment document is technically attacker-influenceable, since a future second writer could exist)
    - `Receipt(...).model_dump_json()` produces valid JSON under 4096 bytes for a representative row-count/duration payload
    - `DatasetConfig.model_validate(...)` against a document containing `source.duplicate_policy: "skip"` and `batching.max_units_per_run: 100` succeeds; a document omitting `batching` entirely fails validation (the field is required, not defaulted -- a missing cap must fail loudly, never silently default to unbounded)
  </behavior>
  <action>
    Create `packages/dataplat/src/dataplat/models/assignment.py`: `AssignmentDocument` (`ConfigDict(extra="forbid", frozen=True)`, matching `config/model.py`'s established convention exactly) with fields `assignment_version: int`, `run_id: int`, `idempotency_key: str`, `dataset: str`, `config_version_id: int`, `config_hash: str`, `file: FileAssignment`, `batch: BatchAssignment`. Nested `FileAssignment` (`file_id: int, object_uri: str, content_sha256: str, size_bytes: int`) and `BatchAssignment` (`batch_key: str, batch_id: int`), same `ConfigDict`. Module docstring cites this session's derivation from ARCHITECTURE.md §6.2, and states plainly this model is validated on READ (by a later plan's `ingest` command) as well as constructed on WRITE (by `discover_files` in this plan) -- the SAME model serves both directions, so a schema drift between writer and reader is structurally impossible.

    Create `packages/dataplat/src/dataplat/models/receipt.py`: `Receipt` (same `ConfigDict`) with fields `run_id: int`, `status: str`, `rows_read: int`, `rows_loaded: int`, `rows_invalid: int`, `rows_deduplicated: int`, `duration_ms: int`, `report_uri: str | None = None`. Docstring states the ≤4KB XCom budget (ARCHITECTURE.md §6.3) and that this model is written to `/airflow/xcom/return.json` by a later plan's CLI, never returned as a bare dict.

    Extend `SourceConfig` in `config/model.py` with `duplicate_policy: str` (plain `str`, matching this file's own stated convention that strategy/policy fields are never a Python `Enum` -- see the module docstring's existing reasoning) -- placed as the last field, after `change_semantics`. Add a NEW top-level model `BatchingConfig` (`ConfigDict(extra="forbid", frozen=True)`, `max_units_per_run: int`) and a required field `batching: BatchingConfig` on `DatasetConfig`, placed after `load`. Update `configs/datasets/customers.yaml`: add `duplicate_policy: skip` inside the existing `source:` block (D-13, locked decision), and a new top-level `batching: {max_units_per_run: 100}` block (100 is comfortably below Airflow's platform-level `max_map_length` default of 1024, per ORCH-03, and far above this phase's actual test file counts) -- match the file's own header-comment convention naming which CONTEXT.md/ARCHITECTURE.md decision drove the addition.
  </action>
  <acceptance_criteria>
    - Every `<behavior>` bullet passes as a `pytest` test in `tests/unit/test_assignment_document.py` and `tests/unit/test_batching_config.py` (new files)
    - `uv run --frozen mypy packages/dataplat/src` passes with the two new model modules
    - `configs/datasets/customers.yaml` validates end to end through `dataplat.config.loader.load_config` with zero errors
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/unit/test_assignment_document.py tests/unit/test_batching_config.py -x -q</automated>
  </verify>
  <done>Both cross-boundary data contracts exist, are extra=forbid validated, and configs/datasets/customers.yaml carries the two new fields this plan's discover_files depends on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: discover_files — list, hash, dedup-check, freeze, cap</name>
  <files>packages/dataplat/src/dataplat/discovery.py</files>
  <read_first>
    packages/dataplat/src/dataplat/discovery.py
    packages/dataplat/src/dataplat/metadata/repository.py
    packages/dataplat/src/dataplat/storage/objectstore.py
    packages/dataplat/src/dataplat/models/assignment.py
    packages/dataplat/src/dataplat/observability/logging.py
  </read_first>
  <behavior>
    - Given a bucket/prefix with 3 new CSV objects and an empty `meta.files`, `discover_files(...)` returns 3 `DiscoveredUnit`s, writes exactly 3 assignment JSON objects to `s3://metadata/assignments/...`, and leaves exactly 3 rows in `meta.files` and 3 in `meta.ingestion_runs` (all `status='PENDING'`)
    - Calling `discover_files(...)` a SECOND time over the SAME unchanged 3 objects returns 0 `DiscoveredUnit`s (all three runs are already `PENDING` or beyond -- wait: re-discovery of a still-`PENDING` run SHOULD be re-offered, since it was never attempted; only `SUCCEEDED` runs are excluded) -- concretely: a run whose `get_or_create_ingestion_run` call returns `status='SUCCEEDED'` is excluded from the returned units; `status` of anything else (`PENDING`, `FAILED`, `RUNNING`) is included. Verify by manually setting one of the three rows to `SUCCEEDED` between calls and confirming the second `discover_files` call returns exactly 2 units, not 3, not 0
    - A file whose content_sha256 already exists for the dataset (uploaded under a different `object_uri`) gets a `meta.files` row with `duplicate_of_file_id` set to the prior file's id, and is NOT included in the returned units (D-13's `skip` policy, read from `ctx.config.source.duplicate_policy`)
    - `meta.files.business_date` is `NULL` on every row this function creates -- assert this directly; the function must contain no `datetime.now()`, `date.today()`, or read of any `logical_date`-shaped parameter anywhere (grep the file for these patterns as an explicit assertion, not just a spot check)
    - When more than `batching.max_units_per_run` files are newly `PENDING` in one call, the returned list is capped at exactly `max_units_per_run`, deterministically sorted by `object_uri` (never by listing order, which S3-compatible stores do not guarantee stable across pages) -- the excluded remainder's `meta.files`/`meta.ingestion_runs` rows still exist (already upserted) and are picked up by literally the next `discover_files` call, not lost
  </behavior>
  <action>
    Create `packages/dataplat/src/dataplat/discovery.py`. Define `DiscoveredUnit` (frozen, slotted dataclass: `assignment_uri: str, idempotency_key: str, run_id: int`) -- this is the ~200-byte-per-entry return shape a later plan's DAG task turns into `KubernetesPodOperator.expand(arguments=...)`. Define `discover_files(*, metadata: MetadataRepository, objects: ObjectStore, dataset_id: int, dataset_name: str, config: DatasetConfig, config_version_id: int, config_hash: str, processor_image: str, processor_version: str) -> list[DiscoveredUnit]`.

    Sequence: (1) `objects.list_objects(config.source.bucket, config.source.path)`, sorted by `.key` (deterministic order, ORCH-08's frozen-manifest requirement extends to "same inputs produce the same manifest," not just "frozen once written"). (2) For each object: stream+hash via `objects.get_object(...)` read in chunks through `hashlib.sha256()` (never load the whole file into memory at once -- reuse the SAME streaming discipline `csv_processor.source`'s chunked reader already established, though this function reads raw bytes, not CSV records) to get `content_sha256`. (3) `existing_file_id = metadata.find_file_by_content_hash(dataset_id=dataset_id, content_sha256=content_sha256)`; `duplicate_of_file_id = existing_file_id if existing_file_id is not None and config.source.duplicate_policy == "skip" else None`. (4) `file_id = metadata.create_file(dataset_id=dataset_id, object_uri=..., content_sha256=content_sha256, hash_version=1, size_bytes=..., filename=..., status="DISCOVERED", duplicate_of_file_id=duplicate_of_file_id)`. (5) If `duplicate_of_file_id is not None`: `continue` -- no batch, no run, no assignment document for this object (D-13's early-exit). (6) Else: `batch_id = metadata.create_batch(dataset_id=dataset_id, batch_key=f"{dataset_name}:{content_sha256[:16]}", status="OPEN")`, `metadata.link_batch_file(batch_id=batch_id, file_id=file_id, sequence_no=1)` (one-file-one-batch, per 03-RESEARCH.md's documented simplification -- state this in a comment). (7) Compute `idempotency_key` per the Interfaces formula above. (8) `run_id, status = metadata.get_or_create_ingestion_run(idempotency_key=..., dataset_id=dataset_id, file_id=file_id, batch_id=batch_id, config_version_id=config_version_id, processor_version=processor_version, processor_image_digest=processor_image)`. (9) If `status == "SUCCEEDED"`: skip (already done). (10) Else: build the `AssignmentDocument`, `objects.put_object(bucket="metadata", key=f"assignments/{dataset_name}/{run_id}.json", body=doc.model_dump_json().encode("utf-8"))`, append a `DiscoveredUnit(assignment_uri=f"s3://metadata/assignments/{dataset_name}/{run_id}.json", idempotency_key=..., run_id=run_id)` to a candidates list. (11) After the loop: if `len(candidates) > config.batching.max_units_per_run`, log a structured warning (via `dataplat.observability.logging.get_logger()`, never `print`) naming the excess count, truncate to the cap. Return the (possibly truncated) list.

    Per-object progress: one structured log line per discovered object (dataset, object_uri, content_sha256 prefix, decision: NEW/DUPLICATE/ALREADY_SUCCEEDED) -- silence during a listing of many objects is the same "indistinguishable from a hang" concern PITFALLS B4 already names for chunked COPY, applied here to discovery.
  </action>
  <acceptance_criteria>
    - Every `<behavior>` bullet passes as a `pytest` test (new `tests/integration/test_discover_files.py`, written in the NEXT plan that owns integration tests -- this task's own acceptance is a unit-level proof against a fake `MetadataRepository`/`ObjectStore` double, or a lightweight direct call if testcontainers fixtures are trivially reachable from this plan; do not skip verifying the behavior just because the dedicated integration-test plan lands later)
    - `grep -n "datetime.now\|date.today\|logical_date" packages/dataplat/src/dataplat/discovery.py` returns zero matches
    - `mypy packages/dataplat/src` passes
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/unit -k discovery -x -q; grep -c "datetime.now\|date.today" packages/dataplat/src/dataplat/discovery.py | grep -qx 0</automated>
  </verify>
  <done>discover_files lists, hashes, dedup-checks, freezes and caps -- the complete ORCH-08/LOAD-03/ORCH-03/INCR-08 mechanism, ready for a later plan's CLI to invoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| discover_files → object store listing | Object keys/sizes under `s3://raw/customers/` are attacker-influenceable if raw ever accepts untrusted uploads (out of scope this phase, but the code must not assume trust) |
| Assignment document (write side) | Written here, read by a later plan's `ingest` command running in a different pod -- the boundary crossing is what makes T-04-02 real even though this phase's own writer is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-02 | Tampering, Elevation of Privilege | `AssignmentDocument` | mitigate | `extra="forbid"` + `frozen=True` pydantic model is the validation boundary; this plan's Task 1 behavior spec explicitly tests that an extra key is rejected. The READ side (a later plan) must call `AssignmentDocument.model_validate_json` before using any field to build a SQL identifier or object-store path -- never trust the raw JSON directly |
| T-04-05 | Denial of Service | `discover_files`'s cap enforcement | mitigate | `batching.max_units_per_run` truncates the returned unit list deterministically; excluded units are never lost (still `PENDING` in `meta.ingestion_runs`, picked up next run) — proven by the behavior spec's cap test |
| T-04-10 (new) | Tampering | Content hash computed from CSV field values reaching a query | mitigate | `content_sha256` and every `object_uri`/`filename` value used in this plan's SQL calls cross exclusively through `MetadataRepository`'s already-parameterized methods (Task 2 calls no raw SQL directly) |

</threat_model>

<verification>
`uv run --frozen pytest tests/unit -k "assignment or receipt or batching or discovery" -x -q` passes. `mypy packages/dataplat/src` passes. `configs/datasets/customers.yaml` loads cleanly through `dataplat.config.loader.load_config`.
</verification>

<success_criteria>
AssignmentDocument and Receipt exist as the two data contracts crossing the Airflow/pod boundary. discover_files implements the complete frozen-manifest, content-hash-dedup, and fan-out-cap mechanism, leaving nothing for a later plan to add beyond calling it.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-03-SUMMARY.md` when done
</output>
