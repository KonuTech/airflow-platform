---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 07
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-05"]
files_modified:
  - airflow/dags/smoke_kubernetes_pod.py
  - airflow/dags/csv_ingest_customers.py
  - airflow/dags/_common/__init__.py
  - airflow/dags/_common/kpo.py
  - tests/unit/test_dag_structure.py
  - tests/unit/test_resolve_window.py
  - tests/policy/test_dag_thinness.py
  - tests/policy/test_dag_line_budget.py
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, ORCH-05, ORCH-06, ORCH-07, ORCH-08, ORCH-09]
must_haves:
  truths:
    - "Both DAGs parse with zero import errors under DagBag(include_examples=False)"
    - "Neither DAG file contains parsing, validation, typing (in the CSV-detection sense) or database-write code -- both call only dataplat/csv_processor functions, via KPO's cmds/arguments"
    - "csv_ingest_customers.py stays under 150 lines; smoke_kubernetes_pod.py stays under 30"
    - "The ingest task is bounded: it never expands past configs/datasets/customers.yaml's batching.max_units_per_run, and every KPO task declares CPU/memory requests and limits"
    - "A DAG run triggered with logical_date=None (asset/API-triggered) does not raise KeyError anywhere in this DAG's task code"
  artifacts:
    - path: "airflow/dags/csv_ingest_customers.py"
      provides: "S3KeySensor -> discover (KPO) -> build_ingest_args -> ingest (KPO, expanded) -> aggregate_receipts"
    - path: "airflow/dags/smoke_kubernetes_pod.py"
      provides: "the permanent U1 platform smoke test"
  key_links:
    - from: "airflow/dags/csv_ingest_customers.py"
      to: "localhost:5001/csv-processor:<sha>"
      via: "KubernetesPodOperator(image=Variable.get('csv_processor_image'))"
      pattern: "csv_processor_image"
    - from: "airflow/dags/csv_ingest_customers.py"
      to: "kubernetes/rbac-etl.yaml's granted identity"
      via: "namespace=\"etl\", service_account_name=\"csv-processor\""
      pattern: "namespace=\"etl\""
---

<objective>
Author the two DAG files this whole phase exists to prove: the permanent, deliberately trivial platform smoke test (U1), and the real vertical-slice pipeline — both as thin orchestration shells containing zero parsing/validation/DB-write logic, calling only the `dataplat discover`/`dataplat ingest` CLI subcommands a prior plan built.

Purpose: ORCH-01 through ORCH-09 in one place — TaskFlow API, KPO-only execution, bounded fan-out, explicit retries, `logical_date=None` tolerance, the 150-line budget, sensor-based dataset dependency, frozen-manifest expansion, and resourced pods.
Output: two DAG files, a tiny `_common/kpo.py` shared-kwargs helper, and the structural/policy/unit tests that make every one of the above a checked, automated fact rather than a claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
<!-- Every value below is either already verified this planning session against the live
cluster/chart, or derived directly from a prior plan in this phase. Do not re-derive. -->

**Task graph for `csv_ingest_customers`** (a correction to 04-RESEARCH.md's own diagram, which showed `discover_files`/`resolve_config` as a plain in-process `@task` — impossible, since `dataplat` is never installed in the Airflow image per STACK.md/ADR-0004; both are folded into ONE `discover` KubernetesPodOperator task using the SAME `csv-processor` image `ingest` uses):
```
S3KeySensor(deferrable) "wait_for_files"
        |
resolve_window() -- @task, independent, logs the window, ORCH-05's testable proof
        |
discover -- KubernetesPodOperator, runs ONCE (not expanded), cmds=["dataplat"],
        |    arguments=["discover", "--dataset", "customers"], do_xcom_push=True
        v
build_ingest_args(discover.output) -- @task, pure Python, reshapes discover's
        |    XCom {"units": [...]} into [["ingest", "--assignment", uri], ...]
        v
ingest -- KubernetesPodOperator.partial(...).expand(arguments=build_ingest_args(...))
        |    one pod per unit, bounded by configs/datasets/customers.yaml's
        |    batching.max_units_per_run (enforced by discover_files, a prior plan)
        v
aggregate_receipts(ingest.output) -- @task, pure Python, logs a run summary
```
`import from airflow.sdk` exclusively (`dag, task` at minimum) — never `airflow.decorators`/`airflow.models` (STACK.md gotcha 1, still current for 3.3.0).

**`S3KeySensor`** (04-RESEARCH.md Pattern 4, verified against `apache-airflow-providers-amazon` docs this session):
```python
from airflow.providers.amazon.aws.sensors.s3 import S3KeySensor
wait_for_files = S3KeySensor(
    task_id="wait_for_files", bucket_name="raw", bucket_key="customers/*.csv",
    wildcard_match=True, aws_conn_id="minio_default", deferrable=True, poke_interval=30,
)
```
`aws_conn_id="minio_default"` is the Connection a prior plan (04-02) created as `AIRFLOW_CONN_MINIO_DEFAULT` via a Secret — do not invent a second Connection ID.

**`resolve_window`** (ORCH-05, verified code, use verbatim):
```python
@task
def resolve_window(dag_run=None) -> dict[str, str | None]:
    if dag_run is None or dag_run.logical_date is None:
        return {"logical_date": None, "data_interval_start": None, "data_interval_end": None}
    return {"logical_date": dag_run.logical_date.isoformat(),
            "data_interval_start": dag_run.data_interval_start.isoformat(),
            "data_interval_end": dag_run.data_interval_end.isoformat()}
```

**KPO settings** (04-RESEARCH.md Pattern 5, verified against `apache-airflow-providers-cncf-kubernetes` docs this session — `on_finish_action` default is `delete_pod`, not `delete_succeeded_pod`; must be set explicitly): `namespace="etl"` (NOT `data-etl`), `service_account_name="csv-processor"`, `do_xcom_push=True`, `on_finish_action="delete_succeeded_pod"`, `get_logs=True`, `image=Variable.get("csv_processor_image")` (an Airflow Variable a prior plan's `make image-csv-processor` sets after every push — read it at DAG-parse time is acceptable here since it is a lightweight, non-DB-write, Airflow-native operation, not "business logic"). `container_resources` via `from kubernetes.client import models as k8s`, `k8s.V1ResourceRequirements(requests={"cpu": "250m", "memory": "512Mi"}, limits={"cpu": "1", "memory": "1Gi"})` for `ingest` (matches `helm/values/local/airflow.yaml`'s own `workers.kubernetes.resources` precedent) and a smaller `{"cpu": "100m", "memory": "128Mi"}`/`{"cpu": "500m", "memory": "256Mi"}` for `discover` (a lighter, non-CSV-streaming workload). Environment: four env vars every KPO task needs (`DATAPLAT_DB_DSN` from `secretKeyRef: {name: csv-processor-db, key: dsn}`; `DATAPLAT_S3_ACCESS_KEY`/`DATAPLAT_S3_SECRET_KEY` from `secretKeyRef: {name: csv-processor-s3, key: access_key/secret_key}`; `DATAPLAT_S3_ENDPOINT_URL` as a plain value `http://minio.data.svc.cluster.local:9000`) — these four Secrets/values were created by a prior plan (04-02); reference them by the EXACT names given there, do not invent new ones.

`ingest`'s task additionally sets `retries=3, retry_exponential_backoff=True` (ORCH-04) and `max_active_tis_per_dag=5` (bounds concurrent ingest pods within one DagRun's expand — the local kind cluster has finite CPU; cite ROADMAP's own "pod amplification is real... a 32-CPU host falls over" concern). `discover`'s task sets `retries=2, retry_exponential_backoff=True`.

**Smoke DAG mechanism** — `dataplat --version` does NOT write to the XCom sidecar path (it prints to stdout, which the sidecar never reads) and the platform's own package version ("0.1.0") is not the git SHA anyway. Use a shell one-liner that overrides the image's `ENTRYPOINT ["dataplat"]` entirely via KPO's `cmds=`, reading the `GIT_SHA` environment variable the Dockerfile now bakes in (a prior plan added `ENV GIT_SHA=${GIT_SHA}` to the runtime stage — confirm this landed before writing this DAG, or add it here if it did not):
```python
cmds=["sh", "-c"],
arguments=['printf \'{"git_sha": "%s"}\' "$GIT_SHA" > /airflow/xcom/return.json'],
```
`do_xcom_push=True` still applies — the sidecar watches the file regardless of what wrote it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The two DAG files and the shared KPO-kwargs helper</name>
  <files>airflow/dags/smoke_kubernetes_pod.py, airflow/dags/csv_ingest_customers.py, airflow/dags/_common/__init__.py, airflow/dags/_common/kpo.py</files>
  <read_first>
    packages/dataplat/src/dataplat/cli.py
    packages/csv-processor/src/csv_processor/cli.py
    docker/csv-processor/Dockerfile
  </read_first>
  <action>
    Confirm `docker/csv-processor/Dockerfile`'s runtime stage sets `ENV GIT_SHA=${GIT_SHA}` (added by a prior plan for the CLI wiring's own image work); if it is missing, add it now as a small, justified addition citing this DAG's smoke test as the consumer.

    Create `airflow/dags/_common/__init__.py` (empty marker). Create `airflow/dags/_common/kpo.py`: one function, `common_kpo_kwargs(*, resources: k8s.V1ResourceRequirements) -> dict[str, object]`, returning a dict with `namespace="etl"`, `service_account_name="csv-processor"`, `image=Variable.get("csv_processor_image")`, `do_xcom_push=True`, `on_finish_action="delete_succeeded_pod"`, `get_logs=True`, `container_resources=resources`, `env_vars=[k8s.V1EnvVar(name="DATAPLAT_DB_DSN", value_from=k8s.V1EnvVarSource(secret_key_ref=k8s.V1SecretKeySelector(name="csv-processor-db", key="dsn"))), k8s.V1EnvVar(name="DATAPLAT_S3_ACCESS_KEY", value_from=k8s.V1EnvVarSource(secret_key_ref=k8s.V1SecretKeySelector(name="csv-processor-s3", key="access_key"))), k8s.V1EnvVar(name="DATAPLAT_S3_SECRET_KEY", value_from=k8s.V1EnvVarSource(secret_key_ref=k8s.V1SecretKeySelector(name="csv-processor-s3", key="secret_key"))), k8s.V1EnvVar(name="DATAPLAT_S3_ENDPOINT_URL", value="http://minio.data.svc.cluster.local:9000")]`. Module docstring states this file exists ONLY to remove boilerplate duplication between `discover` and `ingest`'s KPO construction (per 04-RESEARCH.md's own "`_common/` ONLY if the line budget gets tight, NEVER business logic" guidance) — it constructs Kubernetes API objects, nothing CSV- or business-logic-shaped, so it does not violate ORCH-02/06's DAG-thinness rule; `tests/policy/test_dag_thinness.py` (this plan) must confirm this file is exempt from the "no DB writes / no CSV parsing" scan for the RIGHT reason (it genuinely contains none), not because it was overlooked.

    Create `airflow/dags/smoke_kubernetes_pod.py` (<30 lines): module docstring naming U1, ORCH-01, ORCH-06 explicitly and stating this file is a PERMANENT regression fixture, never a throwaway. `from airflow.sdk import dag`; `from airflow.providers.cncf.kubernetes.operators.pod import KubernetesPodOperator`; `from airflow.models import Variable` (or `airflow.sdk`'s own re-export if it provides one — check which the pinned provider version expects); `from kubernetes.client import models as k8s`; `from airflow_dags._common.kpo import common_kpo_kwargs` (or however this repo's DAG-folder import resolution works — confirm `airflow/dags/` is on `sys.path`/importable as expected, matching how the chart's `dags_folder` and Airflow's own DAG-file-as-top-level-module convention works; a relative import `from _common.kpo import common_kpo_kwargs` may be required instead — verify against how Airflow's `DagFileProcessor` actually imports files in this folder, since `airflow/dags/*.py` files are typically imported as standalone modules, not as a package, making a plain `from _common.kpo import ...` the more likely correct form). `@dag(dag_id="smoke_kubernetes_pod", schedule="@daily", start_date=<a fixed past date>, catchup=False, tags=["smoke", "platform"])` wrapping a single `KubernetesPodOperator(task_id="print_version_to_xcom", name="smoke-kubernetes-pod", cmds=["sh","-c"], arguments=['printf \'{"git_sha": "%s"}\' "$GIT_SHA" > /airflow/xcom/return.json'], retries=1, **common_kpo_kwargs(resources=k8s.V1ResourceRequirements(requests={"cpu":"100m","memory":"128Mi"}, limits={"cpu":"500m","memory":"256Mi"})))`.

    Create `airflow/dags/csv_ingest_customers.py` (<150 lines): module docstring naming every requirement this file satisfies (ORCH-01 through ORCH-09) and the D-01 through D-04 decisions it implements, citing 04-CONTEXT.md by section. Imports: `from airflow.sdk import dag, task`; `from airflow.providers.amazon.aws.sensors.s3 import S3KeySensor`; `from airflow.providers.cncf.kubernetes.operators.pod import KubernetesPodOperator`; `from airflow.models import Variable`; `from kubernetes.client import models as k8s`; `from _common.kpo import common_kpo_kwargs`. Define `resolve_window` exactly per Interfaces. `@dag(dag_id="csv_ingest_customers", schedule="*/1 * * * *", start_date=<fixed past date>, catchup=False, max_active_runs=1, tags=["vertical-slice", "customers"])` — the `schedule` value's rationale (a short, real interval so a new opportunity to sense exists almost immediately after the previous run completes, since `max_active_runs=1` otherwise leaves dead time where no run is active/deferred) must be a one-line comment, not silent. Inside the dag function: instantiate `wait_for_files` (`S3KeySensor`, per Interfaces); call `window = resolve_window()`; instantiate `discover` (`KubernetesPodOperator`, `task_id="discover"`, `cmds=["dataplat"]`, `arguments=["discover", "--dataset", "customers"]`, `retries=2, retry_exponential_backoff=True`, `**common_kpo_kwargs(resources=<the lighter discover sizing from Interfaces>)`); define `@task def build_ingest_args(discovered: dict) -> list[list[str]]: return [["ingest", "--assignment", unit["assignment_uri"]] for unit in discovered["units"]]`; instantiate `ingest = KubernetesPodOperator.partial(task_id="ingest", cmds=["dataplat"], retries=3, retry_exponential_backoff=True, max_active_tis_per_dag=5, **common_kpo_kwargs(resources=<the ingest sizing from Interfaces>)).expand(arguments=build_ingest_args(discover.output))`; define `@task def aggregate_receipts(receipts: list[dict]) -> None: <log a one-line summary via the Airflow task logger, e.g. total rows_loaded across receipts — this is orchestration glue over an already-small XCom value, not business logic>`; wire `wait_for_files >> discover` and `aggregate_receipts(ingest.output)` as the final call. Add a one-line comment stating `ingest`'s fan-out is bounded upstream by `discover_files`'s own `batching.max_units_per_run` enforcement (a prior plan), not by anything in this file, and that the platform-level `[core] max_map_length` default (1024) is untouched and comfortably above the configured cap.
  </action>
  <acceptance_criteria>
    - `wc -l airflow/dags/csv_ingest_customers.py` is under 150; `wc -l airflow/dags/smoke_kubernetes_pod.py` is under 30
    - `DagBag(dag_folder="airflow/dags", include_examples=False).import_errors == {}`
    - `airflow dags list` (against a live or locally-configured Airflow) shows both `dag_id`s
    - Neither DAG file imports `csv`, `psycopg`, `boto3`, or `pydantic` directly — both delegate exclusively through `KubernetesPodOperator`'s `cmds`/`arguments`
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen python -c "from airflow.models import DagBag; b = DagBag(dag_folder='airflow/dags', include_examples=False); assert not b.import_errors, b.import_errors; print('OK', len(b.dags), 'dags')"</automated>
  </verify>
  <done>Both DAG files exist, parse cleanly, stay within their line budgets, and contain no business logic — every requirement ORCH-01 through ORCH-09 is structurally present.</done>
</task>

<task type="auto">
  <name>Task 2: Structural, policy and unit tests proving every ORCH requirement</name>
  <files>tests/unit/test_dag_structure.py, tests/unit/test_resolve_window.py, tests/policy/test_dag_thinness.py, tests/policy/test_dag_line_budget.py</files>
  <read_first>
    tests/policy/test_no_postgres_csv_parsing.py
    tests/unit/test_config_hashing.py
    airflow/dags/csv_ingest_customers.py
    airflow/dags/smoke_kubernetes_pod.py
  </read_first>
  <action>
    Create `tests/unit/test_dag_structure.py`: `DagBag(dag_folder="airflow/dags", include_examples=False)` fixture (module- or session-scoped); `test_no_import_errors` (`import_errors == {}`); `test_both_dags_present` (`{"smoke_kubernetes_pod", "csv_ingest_customers"} <= set(dagbag.dags)`); `test_retries_set` (every `KubernetesPodOperator`/`S3KeySensor` task instance in `csv_ingest_customers` has `retries` set to a positive int — walk `dag.tasks`, `isinstance` check); `test_kpo_resources` (every `KubernetesPodOperator` task has a non-`None` `container_resources` with both `requests` and `limits` populated — mock/introspect `.container_resources`, do not actually `.execute()` a pod); `test_uses_s3_key_sensor` (exactly one `S3KeySensor` instance exists in `csv_ingest_customers`, with `deferrable=True` and `poke_interval=30`); `test_max_active_runs_is_one` (`dag.max_active_runs == 1`); `test_namespace_and_service_account` (every `KubernetesPodOperator` in both DAGs has `namespace="etl"` and `service_account_name="csv-processor"`).

    Create `tests/unit/test_resolve_window.py`: import `resolve_window`'s underlying Python callable from the DAG module (`DagBag`-loaded modules are importable via `dagbag.dags["csv_ingest_customers"]`, or import the file directly via `importlib` given `airflow/dags/` is not a normal package — read `test_dag_structure.py`'s own fixture approach once written and reuse the SAME loading mechanism, do not invent a second one). `test_resolve_window_with_no_dag_run` (calling the underlying function with `dag_run=None` returns all-`None` dict, no `KeyError`); `test_resolve_window_with_asset_triggered_dag_run` (a stand-in/mock object with `logical_date=None` returns the same all-`None` shape); `test_resolve_window_with_scheduled_dag_run` (a mock with real `logical_date`/`data_interval_start`/`data_interval_end` values returns their `.isoformat()` strings).

    Create `tests/policy/test_dag_thinness.py`, copying `tests/policy/test_no_postgres_csv_parsing.py`'s full skeleton (`REPO_ROOT`, `_candidate_files()` scoped to `airflow/dags/*.py` only, one `violations: list[str]` accumulator, the mandatory `test_the_scan_actually_reaches_files` anti-vacuity test). Named `FORBIDDEN_*` patterns (plural, since this is markers not one regex): direct `import csv` / `import psycopg` / `import boto3` / `import pydantic` at the top of a DAG file (a business-logic marker — KPO's `cmds`/`arguments` are the only sanctioned way this DAG touches those concerns), and any raw SQL-shaped string literal (a heuristic regex for `SELECT `/`INSERT INTO`/`UPDATE ` appearing inside `airflow/dags/*.py`, case-insensitive). Exempt `airflow/dags/_common/kpo.py` explicitly by name from the import-based checks (it legitimately imports `kubernetes.client.models`, which is Kubernetes-API-object construction, not business logic) but NOT from the SQL-string check (it should never contain one either).

    Create `tests/policy/test_dag_line_budget.py`: `len(Path("airflow/dags/csv_ingest_customers.py").read_text().splitlines()) < 150` and `len(Path("airflow/dags/smoke_kubernetes_pod.py").read_text().splitlines()) < 30`, each a named test with a failure message stating ORCH-06 and the measured count.
  </action>
  <acceptance_criteria>
    - All four new test files pass under `uv run --frozen pytest tests/unit/test_dag_structure.py tests/unit/test_resolve_window.py tests/policy/test_dag_thinness.py tests/policy/test_dag_line_budget.py -x -q`
    - `test_dag_thinness.py`'s anti-vacuity test (`test_the_scan_actually_reaches_files`) passes, proving the scan is not silently walking zero files
    - Every test in this task runs WITHOUT a live cluster or Docker daemon (pure `DagBag` parsing + static analysis) — confirm by running with no `cluster`/testcontainers dependency group installed
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/unit/test_dag_structure.py tests/unit/test_resolve_window.py tests/policy/test_dag_thinness.py tests/policy/test_dag_line_budget.py -x -q</automated>
  </verify>
  <done>Every ORCH requirement this phase names has a fast, offline, automated proof — no live cluster needed to catch a DAG-structure regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DAG-parse time → Airflow Variable read | `Variable.get("csv_processor_image")` executes on every DAG-file parse (the DAG processor's periodic re-stat loop) — a missing Variable must fail loudly at parse time, not launch an empty/garbage image reference |
| `resolve_window`'s `dag_run.conf` | Not read by this phase's DAG at all (no asset-triggered downstream consumer yet) — flagged here only because a future DAG built on this pattern will need to validate `dag_run.conf` before trusting it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-03 | Elevation of Privilege | `namespace="etl", service_account_name="csv-processor"` on every KPO task | mitigate | Declared explicitly on every task (never left to a chart default), matched to the least-privilege RBAC a prior plan granted — `test_namespace_and_service_account` makes a regression to a wider/omitted namespace an automated failure |
| T-04-05 | Denial of Service | `ingest.expand(arguments=...)` | mitigate | Bounded by `discover`'s own `batching.max_units_per_run` cap (a prior plan) plus this task's `max_active_tis_per_dag=5` throttle on concurrent execution within one run |
| T-04-04 | Information Disclosure | `Variable.get("csv_processor_image")` | accept | An image reference string is not secret; no credential is ever read via `Variable.get` in this DAG |

</threat_model>

<verification>
`uv run --frozen python -c "from airflow.models import DagBag; b = DagBag(dag_folder='airflow/dags', include_examples=False); assert not b.import_errors"` passes. `uv run --frozen pytest tests/unit/test_dag_structure.py tests/unit/test_resolve_window.py tests/policy/test_dag_thinness.py tests/policy/test_dag_line_budget.py -x -q` passes.
</verification>

<success_criteria>
Both DAG files exist, parse cleanly, stay within budget, contain zero business logic, and every ORCH requirement is proven by a fast offline test — ready for a later plan's live-cluster E2E suite to exercise them for real.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-07-SUMMARY.md` when done
</output>
