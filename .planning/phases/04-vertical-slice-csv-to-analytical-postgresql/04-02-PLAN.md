---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kubernetes/rbac-etl.yaml
  - scripts/etl-secrets.sh
  - scripts/stages/75-etl.sh
  - helm/values/local/airflow.yaml
  - helm/values/ci/airflow.yaml
  - Makefile
autonomous: true
requirements: [ORCH-09]
user_setup: []
must_haves:
  truths:
    - "A ServiceAccount csv-processor exists in namespace etl, and the identity that actually calls the Kubernetes API to create KPO pods (airflow-worker under KubernetesExecutor locally, airflow-scheduler under LocalExecutor in CI) is permitted to create/get/list/watch/delete pods and get pods/log in namespace etl -- and nothing broader"
    - "A dev-only DSN Secret for the etl_app Postgres role exists in namespace etl, and etl_app can actually authenticate with it"
    - "MinIO application credentials are reachable from a pod in namespace etl and from Airflow's own S3KeySensor, without any credential literal in a committed file"
    - "airflow/dags/*.py is visible inside the scheduler, dag-processor and KubernetesExecutor-launched worker pod filesystems at /opt/airflow/dags"
    - "make image-csv-processor pushes a git-SHA-tagged image to the local registry and records that tag as the image the DAG should launch"
  artifacts:
    - path: "kubernetes/rbac-etl.yaml"
      provides: "ServiceAccount csv-processor + Role/RoleBinding granting airflow-worker and airflow-scheduler pod lifecycle rights in namespace etl"
    - path: "scripts/etl-secrets.sh"
      provides: "idempotent ensure of csv-processor-db, csv-processor-s3 (namespace etl) and airflow-minio-connection (namespace airflow) Secrets"
    - path: "helm/values/local/airflow.yaml"
      provides: "extraVolumes/extraVolumeMounts DAG hostPath mount on scheduler, dagProcessor, apiServer, workers.kubernetes; AIRFLOW_CONN_MINIO_DEFAULT env wiring"
  key_links:
    - from: "helm/values/{local,ci}/airflow.yaml"
      to: "kind/cluster.yaml's /mnt/dags hostPath"
      via: "extraVolumes hostPath.path: /mnt/dags"
      pattern: "path: /mnt/dags"
    - from: "kubernetes/rbac-etl.yaml"
      to: "the airflow-worker ServiceAccount (namespace airflow)"
      via: "RoleBinding subject"
      pattern: "name: airflow-worker"
---

<objective>
Wire every piece of cluster-level infrastructure this phase's DAG and pods need to exist and be reachable at all, before either DAG file is authored: the `etl` namespace's identity and RBAC, the dev-only credentials the `csv-processor` pod and Airflow's sensor need, and the DAG-source hostPath mount that makes `airflow/dags/*.py` visible to Airflow at all.

Purpose: Pitfall 4 in 04-RESEARCH.md is explicit that building the DAG first and expecting it to "just appear" fails silently (no error, just absence from `airflow dags list`) because the Helm-values wiring was never done. This plan removes that failure mode before any DAG exists to fail it.
Output: a live `etl` namespace with a ServiceAccount and least-privilege RBAC; three Kubernetes Secrets (`csv-processor-db`, `csv-processor-s3` in `etl`; `airflow-minio-connection` in `airflow`); both Helm values profiles mounting `airflow/dags/`; a `make image-csv-processor` that pushes to the local registry and records the tag for the DAG to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
<!-- Live-cluster facts verified this planning session -- do not re-derive these,
they are load-bearing and were confirmed directly against the running cluster. -->

- Namespace is `etl`, NOT `data-etl` (04-CONTEXT.md's own canonical_refs text is wrong; `kubectl get ns` confirms `cnpg-system, data, airflow, etl, ingress-nginx`, and `etl` is currently empty).
- `kubectl get sa -n airflow` currently lists: `airflow-api-server, airflow-create-user-job, airflow-dag-processor, airflow-migrate-database-job, airflow-scheduler, airflow-triggerer, airflow-worker, default`.
- The chart's OWN generated `airflow-pod-launcher-rolebinding` (namespace `airflow`, already live) binds Role `airflow-pod-launcher-role` to THREE subjects: `ServiceAccount airflow-scheduler`, `ServiceAccount airflow-worker`, `ServiceAccount airflow-triggerer` — granting `pods: create,list,get,patch,watch,delete`, `pods/log: get`, `pods/exec: create,get`, `events: list,watch`, scoped to namespace `airflow` only. `rbac-etl.yaml` in this plan grants the SAME verb set, scoped to namespace `etl`, to `airflow-worker` AND `airflow-scheduler` (not `airflow-triggerer` — it never executes a `KubernetesPodOperator.execute()` call). Two subjects are needed because: locally (`executor: KubernetesExecutor`), the KPO-launching Python code runs inside the ephemeral per-task-instance worker pod, whose live `pod_template_file.yaml` (read directly from the `airflow-config` ConfigMap this session) sets `serviceAccountName: "airflow-worker"`; in CI (`executor: LocalExecutor`), task code runs in-process inside the scheduler pod itself, under `airflow-scheduler`. This is a CORRECTION to 04-RESEARCH.md's Assumption A3 ("the scheduler ServiceAccount... since pod-creation calls originate from the scheduler process itself") — that reasoning is right for CI's LocalExecutor and wrong for local's KubernetesExecutor; bind both.
- `helm show values` on the pinned chart 1.22.0 (pulled and read this session) confirms `extraVolumes`/`extraVolumeMounts` exist as TOP-LEVEL keys under `scheduler:`, `dagProcessor:`, `apiServer:`, and nested under `workers.kubernetes:` — confirmed by line number in the pulled chart, not inferred. This corrects 04-RESEARCH.md's Assumption A1 from MEDIUM to HIGH confidence: the exact keys are `scheduler.extraVolumes`/`scheduler.extraVolumeMounts`, `dagProcessor.extraVolumes`/`dagProcessor.extraVolumeMounts`, `apiServer.extraVolumes`/`apiServer.extraVolumeMounts`, `workers.kubernetes.extraVolumes`/`workers.kubernetes.extraVolumeMounts` — four separate insertion points, NOT one global key.
- `kind/cluster.yaml` already bind-mounts `airflow/dags/` (WSL ext4 host path) to `/mnt/dags` on every node, read-only. This is the exact `hostPath.path` every `extraVolumes` entry below must reference.
- `analytics-db-app` Secret (namespace `data`, CNPG-generated) carries `user=analytics_owner` (the schema OWNER role) — NOT `etl_app`. `etl_app` is created via `postInitApplicationSQL: CREATE ROLE etl_app LOGIN;` (`helm/values/{local,ci}/cnpg-analytics.yaml`) with every migration's `GRANT SELECT, INSERT, UPDATE ... TO etl_app` already applied — but **no password has ever been set for `etl_app`**. This plan sets one and packages it into a dev-only DSN Secret; least-privilege demands the pod connect as `etl_app`, not as the schema owner.
- `minio-app` Secret (namespace `data`) carries key `secretKey`; the MinIO application access key is the fixed, non-secret literal string `"etl-app"` (see `scripts/minio-credentials.sh`'s own `cmd_show`, which hardcodes it identically) — reuse this exact value, do not invent a new one.
- `scripts/airflow-metadata-secret.sh` is the EXACT template for cross-namespace Secret derivation: read source keys via `kubectl get secret ... -o jsonpath`, base64-decode, URL-encode where the value crosses into a URI, build the target Secret's manifest as `stringData` piped to `kubectl apply -f -` on stdin — values NEVER touch argv. Copy this shape, not `minio-credentials.sh`'s (which generates fresh randoms rather than deriving from an existing source).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: etl namespace RBAC and the three dev-only credential Secrets</name>
  <files>kubernetes/rbac-etl.yaml, scripts/etl-secrets.sh, scripts/stages/75-etl.sh</files>
  <read_first>
    kubernetes/namespaces.yaml
    scripts/airflow-metadata-secret.sh
    scripts/minio-credentials.sh
    scripts/stages/70-airflow.sh
    helm/values/local/cnpg-analytics.yaml
  </read_first>
  <action>
    Create `kubernetes/rbac-etl.yaml`: a header comment stating the file's invariant (exactly `get/list/watch/create/patch/delete` on `pods`, `get` on `pods/log`, `create/get` on `pods/exec`, `list/watch` on `events`, in namespace `etl`, to exactly two named subjects — nothing broader, no `cluster-admin`, no wildcard verb/resource — matching the chart's own `airflow-pod-launcher-role` verb set exactly, scoped to a different namespace). Four `---`-separated documents, flat style matching `namespaces.yaml`: (1) `ServiceAccount` named `csv-processor` in namespace `etl` (the identity the KPO pod itself runs as); (2) `Role` named `etl-pod-launcher-role` in namespace `etl` with the verb set above; (3) `RoleBinding` named `etl-pod-launcher-rolebinding` binding that Role to `ServiceAccount airflow-worker` (namespace `airflow`) AND `ServiceAccount airflow-scheduler` (namespace `airflow`) — two subjects, per the Interfaces section's reasoning; (4) do NOT create any additional Role for the `csv-processor` SA itself — this phase's pod never creates further pods, only reads its own Secrets/env, which needs no RBAC grant at all (the default `automountServiceAccountToken` + Secret-via-env wiring needs no Role).

    Create `scripts/etl-secrets.sh`, modeled directly on `scripts/airflow-metadata-secret.sh`'s structure (`_kubectl`, `_apply_secret` helpers copied verbatim in shape; one `ensure` subcommand; every value piped, never argv): three sub-derivations inside `cmd_ensure`, each independently idempotent (skip if the target Secret already exists, mirroring `minio-credentials.sh`'s per-secret "already exists — leaving it unchanged" pattern):
    (a) **`etl-app` password + `csv-processor-db` Secret** (namespace `etl`, key `dsn`): if `etl`/`csv-processor-db` does not exist, generate `openssl rand -hex 32`, resolve the analytical cluster's current primary pod name via `kubectl get cluster -n data analytics-db -o jsonpath='{.status.currentPrimary}'` (never hardcode `analytics-db-1` — a failover would change it), then `kubectl exec -i -n data <primary-pod> -- psql -U postgres -d analytics` with `ALTER ROLE etl_app WITH PASSWORD '<value>';` piped via STDIN (never as an exec argument — mirrors this script family's "never in argv" discipline), then build `postgresql://etl_app:<urlencoded-password>@analytics-db-rw.data:5432/analytics` (host qualified as `<svc>.<namespace>` exactly like `airflow-metadata-secret.sh`'s own connection string, since a pod in `etl` resolves `analytics-db-rw.data` via cluster DNS but not the bare `analytics-db-rw`) and apply it as `stringData.dsn` in namespace `etl`.
    (b) **`csv-processor-s3` Secret** (namespace `etl`, keys `access_key`, `secret_key`): if absent, read `minio-app`'s `secretKey` (namespace `data`) via `_read_source_key`-style jsonpath+base64-decode, apply `stringData: {access_key: "etl-app", secret_key: "<value>"}` in namespace `etl`.
    (c) **`airflow-minio-connection` Secret** (namespace `airflow`, key `AIRFLOW_CONN_MINIO_DEFAULT`): if absent, read the same `minio-app` `secretKey`, URL-encode it (reuse `_urlencode` from `airflow-metadata-secret.sh`'s exact shape), build `aws://etl-app:<urlencoded-secret>@/?endpoint_url=http%3A%2F%2Fminio.data.svc.cluster.local%3A9000&region_name=us-east-1` (Airflow's URI-form connection encoding: empty host between `@` and `/`, extras as query params), apply as `stringData` in namespace `airflow`.
    Docstring header must state the TRUST MODEL identically to `airflow-metadata-secret.sh`'s own (host-side kubeconfig principal, no in-cluster RBAC needed for THIS script to run, superseded by Vault in Phase 5) and must state explicitly that `(a)`'s `ALTER ROLE` runs via `kubectl exec` against the CNPG primary pod using peer/local trust as `postgres`, not via a network connection, so no new inbound-connection RBAC is needed either.

    Create `scripts/stages/75-etl.sh` (numbered after `70-airflow.sh` — RBAC subjects `airflow-worker`/`airflow-scheduler` must already exist; before nothing later depends on it): applies `kubernetes/rbac-etl.yaml` via `kubectl apply -f -n etl -f kubernetes/rbac-etl.yaml` (or without `-n` since the manifest sets namespaces itself — confirm which is correct against `namespaces.yaml`'s own apply convention) then calls `scripts/etl-secrets.sh ensure`. Match `70-airflow.sh`'s exact shebang/`set -euo pipefail`/`_kubectl` wrapper shape (`read_first` it to copy conventions, not invent new ones).
  </action>
  <acceptance_criteria>
    - `kubectl apply -f kubernetes/rbac-etl.yaml` succeeds and `kubectl auth can-i create pods -n etl --as=system:serviceaccount:airflow:airflow-worker` returns `yes`; the same check `--as=system:serviceaccount:etl:default` returns `no`
    - `kubectl auth can-i create pods -n airflow --as=system:serviceaccount:airflow:airflow-worker` behavior is UNCHANGED by this plan (already `yes` via the chart's own pre-existing RBAC) — confirms no accidental widening
    - `scripts/etl-secrets.sh ensure` run twice in a row is a no-op the second time (each of the three `_secret_exists` checks short-circuits; `kubectl get secret -n etl csv-processor-db -o jsonpath='{.metadata.resourceVersion}'` is unchanged across the two runs)
    - `psql "postgresql://etl_app:<password>@localhost:<port>/analytics"` (via a port-forward to `analytics-db-rw`) using the password just set authenticates successfully and `SELECT current_user` returns `etl_app`
    - `kubectl get secret -n etl csv-processor-s3 -o jsonpath='{.data.access_key}' | base64 -d` equals `etl-app`
  </acceptance_criteria>
  <verify>
    <automated>kubectl apply -f kubernetes/rbac-etl.yaml --dry-run=server && scripts/etl-secrets.sh ensure && kubectl auth can-i create pods -n etl --as=system:serviceaccount:airflow:airflow-worker</automated>
  </verify>
  <done>The etl namespace has exactly the RBAC it needs and no more; three dev-only credential Secrets exist and are individually idempotent to re-ensure.</done>
</task>

<task type="auto">
  <name>Task 2: Helm DAG-mount and MinIO-connection wiring, both profiles</name>
  <files>helm/values/local/airflow.yaml, helm/values/ci/airflow.yaml</files>
  <read_first>
    helm/values/local/airflow.yaml
    helm/values/ci/airflow.yaml
    kind/cluster.yaml
  </read_first>
  <action>
    In BOTH `helm/values/local/airflow.yaml` and `helm/values/ci/airflow.yaml`, add, identically (this is a FOURTH divergence axis and must be argued per D-06's own header comment convention — add one sentence to each file's existing header block stating: "DAG-mount wiring and the Airflow-side MinIO connection are identical in both profiles because the underlying hostPath and Secret name are identical in both — this is infrastructure wiring, not sizing, and belongs with the other non-diverging keys"):
    - `dags: {persistence: {enabled: false}, gitSync: {enabled: false}}` at top level (explicitly turns off the chart's own PVC/git-sync mechanism — the chart's own default is `persistence.enabled: true`, a 100Gi PVC, which is the wrong mechanism for a node-local hostPath).
    - `scheduler.extraVolumes`, `scheduler.extraVolumeMounts` — add alongside the EXISTING `scheduler.resources`/`scheduler.logGroomerSidecar` keys (do not create a second `scheduler:` top-level stanza).
    - `dagProcessor.extraVolumes`, `dagProcessor.extraVolumeMounts` — alongside existing `dagProcessor.resources`.
    - `apiServer.extraVolumes`, `apiServer.extraVolumeMounts` — alongside existing `apiServer.resources` (low-cost inclusion per 04-RESEARCH.md Assumption A4: the API server likely does not strictly need it since Airflow 3's UI reads serialized DAG structure from the metadata DB, not files directly, but including it removes the ambiguity for zero cost).
    - `workers.kubernetes.extraVolumes`, `workers.kubernetes.extraVolumeMounts` — alongside the EXISTING `workers.kubernetes.resources` block (this is the one that matters most: it is this key that determines the `serviceAccountName: airflow-worker` pod template's volumes, confirmed this session by reading the live `airflow-config` ConfigMap's rendered `pod_template_file.yaml`).
    Every one of the four `extraVolumes` entries is IDENTICAL: `[{name: dags, hostPath: {path: /mnt/dags, type: Directory}}]`. Every one of the four `extraVolumeMounts` entries is IDENTICAL: `[{name: dags, mountPath: /opt/airflow/dags, readOnly: true}]`. `/mnt/dags` must match `kind/cluster.yaml`'s `containerPath` EXACTLY (verify by reading it, do not assume) — do not invent a different path. `/opt/airflow/dags` is the chart's own default `dags_folder`, do not override `dags.mountPath`.

    ALSO in both files: wire `AIRFLOW_CONN_MINIO_DEFAULT` (from Task 1's `airflow-minio-connection` Secret) into `scheduler`, `dagProcessor` is not needed (it never executes sensor pokes), `triggerer` (the deferred `S3KeySensor`'s poke runs HERE, per D-01's deferrable design), and `workers.kubernetes` (task-instance pods need it too, for consistency, even though only the initial poke before deferral runs there). Use each component's existing `env`/`extraEnvFrom`-style key if the chart exposes one at that level — grep the pulled chart's `values.yaml` for `env:`/`extraEnv:` under `scheduler:`, `triggerer:`, `workers:` (the same `helm pull apache-airflow/airflow --version 1.22.0 --untar` command used to verify `extraVolumes` this session works identically for this) and use whichever key the chart actually supports at each of those three levels; each entry references `secretKeyRef: {name: airflow-minio-connection, key: AIRFLOW_CONN_MINIO_DEFAULT}`.
  </action>
  <acceptance_criteria>
    - `helm template apache-airflow/airflow --version 1.22.0 -f helm/values/local/airflow.yaml` (and same for `ci/airflow.yaml`) renders without error and the rendered scheduler/dagProcessor/apiServer Deployments and the rendered `workers.kubernetes` pod-template-file ConfigMap all contain a volume named `dags` with `hostPath.path: /mnt/dags` and a matching volumeMount at `/opt/airflow/dags`
    - The rendered scheduler/triggerer/`workers.kubernetes` pod specs contain an env var `AIRFLOW_CONN_MINIO_DEFAULT` sourced from `secretKeyRef.name: airflow-minio-connection`
    - `helm/values/local/airflow.yaml` and `helm/values/ci/airflow.yaml` remain identical on every key except the pre-existing D-06 divergence axes (replicas/resources/monitoring/executor) plus this task's additions, which are byte-identical between the two files (diff the two files' new blocks and confirm zero difference)
    - `tests/policy/test_values_profiles.py` (existing divergence-axis test) still passes, or is extended if it enumerates an explicit allowlist of keys that may differ
  </acceptance_criteria>
  <verify>
    <automated>./tools/bin/helm template test apache-airflow/airflow --version 1.22.0 -f helm/values/local/airflow.yaml | grep -A2 "name: dags" | grep -q "/mnt/dags" && uv run --frozen pytest tests/policy/test_values_profiles.py -x -q</automated>
  </verify>
  <done>Both Helm profiles mount airflow/dags/ into every Airflow component that needs it and wire the MinIO connection, with zero unargued divergence between the two files.</done>
</task>

<task type="auto">
  <name>Task 3: Image build/push to the local registry, and recording the tag for the DAG</name>
  <files>Makefile</files>
  <read_first>
    Makefile
    docker/csv-processor/Dockerfile
    helm/versions.env
  </read_first>
  <action>
    Extend the existing `image-csv-processor` target: after the current `docker build ... -t csv-processor:$$(git rev-parse --short HEAD)` line, add a second tag `docker tag csv-processor:$$(git rev-parse --short HEAD) localhost:5001/csv-processor:$$(git rev-parse --short HEAD)`, then `docker push localhost:5001/csv-processor:$$(git rev-parse --short HEAD)` (matching U1's explicit "push to the local registry" requirement and this project's own established local-registry-over-`kind-load` convention from Phase 2 research). Compute `GIT_SHA` once via a `make` variable (`GIT_SHA := $(shell git rev-parse --short HEAD)`) near the top of the target rather than invoking `git rev-parse` three separate times inline, reducing risk of a mid-build commit changing the SHA between calls. After a successful push, set the Airflow Variable the DAG reads: `kubectl exec -n airflow deploy/airflow-api-server -- airflow variables set csv_processor_image "localhost:5001/csv-processor:$(GIT_SHA)"` — guard this behind a check that a live cluster is reachable (reuse the SAME `kubectl get nodes` reachability check pattern `scripts/doctor.sh`/`tests/e2e/cluster/conftest.py`'s `_require_cluster` already use, so a developer building the image with no cluster running gets a clear message, not a raw `kubectl` connection-refused error) — if unreachable, print a one-line warning ("no live cluster — image pushed but csv_processor_image Variable NOT set; run this target again once the cluster is up, or set it manually") and continue rather than failing the whole target (an image build/push must not hard-fail just because nobody has a cluster up yet). Update the target's `##` help comment and its docstring-style comment block to describe the new push+Variable-set behavior, keeping the existing `T-03-15/INFRA-08` replayability comment intact.
  </action>
  <acceptance_criteria>
    - `make image-csv-processor` against a live cluster: `docker images localhost:5001/csv-processor` shows the current git-SHA tag; `curl -s http://localhost:5001/v2/csv-processor/tags/list` (or `docker manifest inspect`) confirms the tag is present in the registry; `kubectl exec -n airflow deploy/airflow-api-server -- airflow variables get csv_processor_image` returns `localhost:5001/csv-processor:<same-sha>`
    - `make image-csv-processor` with no live cluster reachable still builds and pushes the image successfully, prints the warning, and exits 0 (not a failure)
    - Re-running `make image-csv-processor` twice back-to-back on an unchanged working tree produces the identical tag both times (idempotent by construction — the tag IS the content-addressed git SHA)
  </acceptance_criteria>
  <verify>
    <automated>make image-csv-processor && docker images localhost:5001/csv-processor --format '{{.Tag}}' | grep -q "$(git rev-parse --short HEAD)"</automated>
  </verify>
  <done>make image-csv-processor is the one command that builds, tags, pushes and registers the image the DAG will launch — reusable across this phase's every later plan that needs a fresh build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Airflow worker/scheduler pod → Kubernetes API (namespace etl) | The identity that creates KPO pods crosses from the `airflow` namespace's control plane into `etl`'s data plane |
| Host developer kubeconfig → live cluster Secrets | `scripts/etl-secrets.sh` runs with the human operator's own cluster-admin kubeconfig, not an in-cluster identity |
| KPO pod (csv-processor SA) → analytical PostgreSQL / MinIO | The pod authenticates as `etl_app` (narrowly granted) and MinIO's `etl-app` application credential, never as the schema owner or MinIO root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-03 | Elevation of Privilege | `kubernetes/rbac-etl.yaml` | mitigate | Named Role scoped to exactly `pods`/`pods/log`/`pods/exec`/`events` verbs in namespace `etl` only, bound to exactly two named ServiceAccounts (`airflow-worker`, `airflow-scheduler`) — no `cluster-admin`, no wildcard resource/verb, verified by the negative `kubectl auth can-i ... --as=system:serviceaccount:etl:default` check returning `no` |
| T-04-08 (new) | Elevation of Privilege | `etl_app`'s Postgres grants | accept | `etl_app` only ever receives `SELECT, INSERT, UPDATE` per-table grants from each migration (already Phase-3-established); this plan sets its password but grants nothing new — the pod cannot `DROP`/`ALTER`/`CREATE` |
| T-04-09 (new) | Information Disclosure | `scripts/etl-secrets.sh` | mitigate | Every generated/derived value moves only through `stringData` piped to `kubectl apply -f -` on stdin or through `kubectl exec -i` stdin — never a CLI argument, never echoed, matching T-02-16/T-02-23's established precedent in this repo |
| T-04-SC | Tampering | none — no new package installs this plan | mitigate | N/A this plan; `apache-airflow-providers-amazon` was already cleared `[OK]` in 04-RESEARCH.md's Package Legitimacy Audit and is bundled in the stock image, not a new install |

</threat_model>

<verification>
`kubectl auth can-i create pods -n etl --as=system:serviceaccount:airflow:airflow-worker` returns `yes`; the same for `system:serviceaccount:etl:default` returns `no`. `helm template` on both profiles renders the `dags` volume into scheduler/dagProcessor/apiServer/workers.kubernetes. `scripts/etl-secrets.sh ensure` is idempotent. `make image-csv-processor` builds, tags, pushes, and (with a live cluster) sets `csv_processor_image`.
</verification>

<success_criteria>
Every piece of cluster-side infrastructure the DAG and its pods need — identity, RBAC, credentials, DAG-file visibility, a pushed image — exists and is independently verifiable, before either DAG file is written.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-02-SUMMARY.md` when done
</output>
