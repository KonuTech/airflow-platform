---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0006_normalized_customers_business_key_unique.py
  - packages/dataplat/src/dataplat/pipeline/protocol.py
  - packages/dataplat/src/dataplat/metadata/repository.py
  - packages/dataplat/src/dataplat/metadata/postgres.py
  - packages/dataplat/src/dataplat/storage/objectstore.py
  - packages/dataplat/src/dataplat/config/registry.py
autonomous: true
requirements: [LOAD-09, LOAD-08, LOAD-04, META-03]
must_haves:
  truths:
    - "normalized.customers.customer_id carries a real UNIQUE constraint so ON CONFLICT (customer_id) can arbitrate on it"
    - "A caller can pre-allocate an ingestion_runs row (idempotent, discovery-time) and separately claim it for exclusive execution (pod-startup-time) via two distinct, non-conflatable operations"
    - "A caller can list objects under a bucket/prefix and write a new object, through the same ObjectStore seam that already reads objects"
    - "A caller can re-resolve a dataset's historical config document by config_version_id, without reading configs/*.yaml from disk"
  artifacts:
    - path: "migrations/versions/0006_normalized_customers_business_key_unique.py"
      provides: "UNIQUE(customer_id) constraint replacing the plain index"
      contains: "op.create_unique_constraint"
    - path: "packages/dataplat/src/dataplat/metadata/repository.py"
      provides: "get_or_create_ingestion_run, claim_ingestion_run, finalize_publication Protocol methods; create_file gains duplicate_of_file_id"
    - path: "packages/dataplat/src/dataplat/storage/objectstore.py"
      provides: "ObjectStore.list_objects, ObjectStore.put_object"
    - path: "packages/dataplat/src/dataplat/config/registry.py"
      provides: "ConfigRegistry.get_by_id"
  key_links:
    - from: "packages/dataplat/src/dataplat/metadata/postgres.py"
      to: "meta.ingestion_runs"
      via: "INSERT ... ON CONFLICT (idempotency_key) / UPDATE ... WHERE status IN (...)"
      pattern: "ON CONFLICT \\(idempotency_key\\)"
    - from: "migrations/versions/0006_normalized_customers_business_key_unique.py"
      to: "normalized.customers"
      via: "op.create_unique_constraint"
      pattern: "uq_customers_customer_id"
---

<objective>
Land every data-access-layer contract the rest of Phase 4 builds against, before any of it is written: the migration that makes `ON CONFLICT (customer_id)` legal, the two distinct ingestion-run upsert operations (discovery-time pre-allocation vs. pod-startup-time claim), a status-finalizing method that shares the publication transaction, object listing/writing, and historical config resolution by ID.

Purpose: every later Phase 4 plan (staging, publish, discovery, CLI orchestration, DAGs) calls these exact signatures. Getting them right once, before three other plans start consuming them, is what Interface-First ordering buys.
Output: `migrations/versions/0006_*.py` applied cleanly against a fresh `alembic upgrade head`; `MetadataRepository`/`PostgresMetadataRepository`, `ObjectStore`/`S3ObjectStore`, `ConfigRegistry` all carry the new methods, each covered by an integration test against testcontainers Postgres/MinIO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
<!-- Exact current shapes this plan extends. Read the full files before editing -->
<!-- them; these excerpts are the load-bearing parts only. -->

From `packages/dataplat/src/dataplat/metadata/repository.py` (Protocol, current):
```
class MetadataRepository(Protocol):
    def get_or_create_dataset(self, dataset_name: str) -> int: ...
    def create_file(self, *, dataset_id: int, object_uri: str, content_sha256: bytes,
        hash_version: int, size_bytes: int, filename: str, status: str) -> int: ...
    def find_file_by_content_hash(self, *, dataset_id: int, content_sha256: bytes) -> int | None: ...
    def create_batch(self, *, dataset_id: int, batch_key: str, status: str) -> int: ...
    def link_batch_file(self, *, batch_id: int, file_id: int, sequence_no: int) -> None: ...
    def create_ingestion_run(self, *, idempotency_key: str, dataset_id: int, config_version_id: int,
        processor_version: str, processor_image_digest: str, status: str,
        file_id: int | None = None, batch_id: int | None = None) -> int: ...
    def update_ingestion_run_status(self, *, run_id: int, status: str, **fields: object) -> None: ...
```
`create_ingestion_run` already exists as a plain `INSERT ... RETURNING` (raises on a duplicate `idempotency_key`) — this plan does NOT remove it; `get_or_create_ingestion_run` is a NEW, separate, idempotent-upsert method for discovery-time pre-allocation, per Pitfall 5 below.

From `packages/dataplat/src/dataplat/metadata/postgres.py` — the exact no-op-upsert idiom to copy (`get_or_create_dataset`, lines 96-109):
```
row = conn.execute(
    """
    INSERT INTO meta.datasets (dataset_name) VALUES (%s)
    ON CONFLICT (dataset_name) DO UPDATE
        SET dataset_name = EXCLUDED.dataset_name
    RETURNING dataset_id
    """,
    (dataset_name,),
).fetchone()
```
`_INGESTION_RUN_UPDATABLE_FIELDS` frozenset (lines 30-62) already exists on this module — do not duplicate it.

From `packages/dataplat/src/dataplat/storage/objectstore.py` (current):
```
class ObjectStore(Protocol):
    def get_object(self, bucket: str, key: str) -> io.TextIOWrapper: ...

class S3ObjectStore(ObjectStore):
    def __init__(self, *, endpoint_url: str, access_key: str, secret_key: str) -> None: ...
    def get_object(self, bucket: str, key: str) -> io.TextIOWrapper: ...
```
`self._client: S3Client = boto3.client("s3", endpoint_url=..., aws_access_key_id=..., aws_secret_access_key=..., region_name="us-east-1", config=Config(signature_version="s3v4", s3={"addressing_style": "path"}))` — reuse this exact client, do not construct a second one.

From `packages/dataplat/src/dataplat/config/registry.py` (current, `ConfigRegistry.sync` and `_require_row` helper already exist) — `get_by_id` follows the same `self._pool.connection()` / `_require_row` pattern.

From `migrations/versions/0005_normalized_customers.py` (the table this migration alters):
```
op.create_index("ix_customers_customer_id", "customers", ["customer_id"], unique=False, schema="normalized")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0006 (UNIQUE constraint) and PipelineContext.source field</name>
  <files>migrations/versions/0006_normalized_customers_business_key_unique.py, packages/dataplat/src/dataplat/pipeline/protocol.py</files>
  <read_first>
    migrations/versions/0005_normalized_customers.py
    migrations/versions/0004_meta_ingestion_runs.py
    packages/dataplat/src/dataplat/pipeline/protocol.py
    packages/dataplat/src/dataplat/sources/protocol.py
  </read_first>
  <action>
    Create `migrations/versions/0006_normalized_customers_business_key_unique.py`: `revision = "0006"`, `down_revision = "0005"`, `branch_labels = None`, `depends_on = None`. `upgrade()`: `op.drop_index("ix_customers_customer_id", table_name="customers", schema="normalized")` then `op.create_unique_constraint("uq_customers_customer_id", "customers", ["customer_id"], schema="normalized")`. `downgrade()`: reverse (drop the constraint via `type_="unique"`, recreate the plain index). Module docstring must state explicitly why migration 0005's "plain index, since a uniqueness constraint would fight MERGE" reasoning no longer applies: LOAD-09/PITFALLS #14 reject `MERGE` for this table in favor of `pg_advisory_xact_lock` + `INSERT ... ON CONFLICT (customer_id)`, which requires a real unique constraint or exclusion constraint as its conflict target — this migration is that precondition. Note in the docstring that a `UNIQUE` constraint creates its own backing B-tree index, so no index coverage is lost. No `GRANT` statement needed (altering an existing table's indexing does not change table-level grants); add a one-line docstring note confirming this rather than a silent omission.

    In `packages/dataplat/src/dataplat/pipeline/protocol.py`, add a new field `source: Source | None = None` to the `PipelineContext` frozen dataclass, appended AFTER the existing `log: FilteringBoundLogger` field (dataclass field-ordering rule: a field with a default must not precede a field without one). Under the `if TYPE_CHECKING:` block, add `from dataplat.sources.protocol import Source`. Note in a comment that this creates a TYPE_CHECKING-only mutual reference between `pipeline/protocol.py` and `sources/protocol.py` (the latter already TYPE_CHECKING-imports `PipelineContext` from this same module) — this is safe: neither import executes at runtime, both are guarded by `from __future__ import annotations` + `if TYPE_CHECKING:`, so there is no real circular import. Default `None` (not a required field) specifically so this does not break any existing Phase 3 test that constructs `PipelineContext(...)` without a `source=` argument — grep `tests/unit/` for `PipelineContext(` construction sites and confirm none break; if any exist and pass all 6 original fields positionally, they still work unchanged since `source` is appended last with a default. Update the class docstring's Attributes list to document `source`.
  </action>
  <acceptance_criteria>
    - `migrations/versions/0006_normalized_customers_business_key_unique.py` exists with `revision = "0006"`, `down_revision = "0005"`
    - Running `alembic upgrade head` against a fresh testcontainers Postgres (via the existing `run_migrations`/`migrated_dsn` fixtures) succeeds with no error
    - `\d normalized.customers` (or `information_schema.table_constraints`) shows `uq_customers_customer_id` as a `UNIQUE` constraint type and `ix_customers_customer_id` no longer exists
    - `alembic downgrade -1` from head succeeds and restores the plain index
    - `PipelineContext` has a `source` field typed `Source | None`, defaulting to `None`
    - `grep -rn "PipelineContext(" tests/unit/ packages/` shows no call site broken by the new field (all either omit it, relying on the default, or the grep confirms zero direct constructions exist yet)
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/integration/test_migrations.py -k "0006 or upgrade_head" -x 2>/dev/null || uv run --group cluster pytest tests/integration -k migration -x</automated>
  </verify>
  <done>Migration 0006 applies and downgrades cleanly against a real Postgres; PipelineContext carries an optional source field with zero breakage to existing constructions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MetadataRepository extension — the two ingestion-run upserts, finalize_publication, duplicate-aware create_file</name>
  <files>packages/dataplat/src/dataplat/metadata/repository.py, packages/dataplat/src/dataplat/metadata/postgres.py</files>
  <read_first>
    packages/dataplat/src/dataplat/metadata/repository.py
    packages/dataplat/src/dataplat/metadata/postgres.py
    packages/dataplat/src/dataplat/load/publish/protocol.py
    tests/integration/test_metadata_repository.py
  </read_first>
  <behavior>
    - `get_or_create_ingestion_run(idempotency_key=..., dataset_id=..., file_id=..., batch_id=..., config_version_id=..., processor_version=..., processor_image_digest=...) -> tuple[int, str]`: called twice with the SAME `idempotency_key` returns the SAME `run_id` both times, and the second call performs no INSERT (row count in `meta.ingestion_runs` stays 1). Returns `(run_id, status)` where `status` is the row's CURRENT status after the call (e.g. `"PENDING"` on first call, whatever it already was on a repeat call) — the caller uses this to decide whether to include the unit in a Dynamic-Task-Mapping expand list.
    - `claim_ingestion_run(idempotency_key=..., try_number=..., pod_name=...) -> tuple[int, str] | None`: when the row's status is `PENDING` or `FAILED`, or `RUNNING` with an EXPIRED `lease_expires_at`, returns `(run_id, "RUNNING")` and the row's status/lease/try_number/pod_name are updated. When the row's status is `SUCCEEDED`, or `RUNNING` with a LIVE (unexpired) lease, returns `None` and the row is UNCHANGED (no UPDATE happens — verify via a second read that `k8s_pod_name`/`lease_expires_at` did not change). Calling `claim_ingestion_run` before `get_or_create_ingestion_run` has ever created the row (no matching `idempotency_key`) returns `None` (no row to claim), not an exception.
    - `finalize_publication(conn=..., run_id=..., file_id=..., batch_id=..., rows_loaded=..., finished_at=..., report_uri=...) -> None`: given an ALREADY-OPEN, caller-supplied `conn` (never opens its own connection, never commits/rolls back — same contract as `Publisher.publish`), updates `meta.files.status='PROCESSED'` for `file_id`, `meta.batches.status='PUBLISHED'` for `batch_id`, and `meta.ingestion_runs` (`status='SUCCEEDED'`, `finished_at`, `rows_loaded`, `report_uri`) for `run_id` — verify all three via a SEPARATE connection AFTER the caller commits `conn`'s transaction (not before — the whole point is these three updates are invisible until the caller's COMMIT).
    - `create_file(..., duplicate_of_file_id: int | None = None) -> int`: change from a plain `INSERT ... RETURNING` (raises `UniqueViolation` on a repeat `(dataset_id, object_uri, content_sha256)`) to an idempotent `INSERT ... ON CONFLICT (dataset_id, object_uri, content_sha256) DO UPDATE ... RETURNING file_id` — calling it twice with the identical five business-identity arguments returns the SAME `file_id` both times and the row count in `meta.files` stays 1. `duplicate_of_file_id`, when provided, is stored on the row (verify via a direct read).
  </behavior>
  <action>
    Add the four Protocol method signatures above to `MetadataRepository` in `repository.py`, each with a docstring stating "Maps to ``&lt;exact SQL shape&gt;``" per this file's own established convention (see `update_ingestion_run_status`'s docstring for the shape to copy), and explicitly documenting the Pitfall-5 distinction between `get_or_create_ingestion_run` (discovery-time, tolerates re-runs, no-op upsert) and `claim_ingestion_run` (pod-startup-time, enforces exclusivity, conditional `UPDATE ... WHERE`) — state in both docstrings that these are two different SQL statements with two different jobs and must never be conflated or implemented as the same query.

    Implement all four in `PostgresMetadataRepository` (`postgres.py`). `get_or_create_ingestion_run`: `INSERT INTO meta.ingestion_runs (idempotency_key, dataset_id, file_id, batch_id, config_version_id, processor_version, processor_image_digest, status) VALUES (%s,...,'PENDING') ON CONFLICT (idempotency_key) DO UPDATE SET idempotency_key = EXCLUDED.idempotency_key RETURNING run_id, status`. `claim_ingestion_run`: `UPDATE meta.ingestion_runs SET status='RUNNING', try_number=%(try_number)s, k8s_pod_name=%(pod_name)s, started_at=COALESCE(started_at, now()), lease_expires_at=now() + interval '5 minutes' WHERE idempotency_key=%(key)s AND (status IN ('PENDING','FAILED') OR (status='RUNNING' AND lease_expires_at < now())) RETURNING run_id, status` — when `cur.fetchone()` returns `None`, return `None` (this is an EXPECTED outcome, not an invariant violation — do not raise or use the `if row is None: raise RuntimeError(...)` pattern this file uses elsewhere for "RETURNING always yields a row" cases; annotate why with a comment referencing the two legitimate `None` causes: already-succeeded, or a live concurrent claim). `finalize_publication`: three sequential `conn.execute(...)` calls (files, batches, ingestion_runs UPDATEs) using the SAME `conn` parameter passed in, no `with self._pool.connection()` — this is the one method on this class that does NOT open its own connection, because it must land inside the SAME transaction as `Publisher.publish`'s `INSERT ... ON CONFLICT` (META-03's atomicity requirement). Document this exception prominently in both the Protocol docstring and the implementation, citing META-03. `create_file`: change the SQL to `INSERT INTO meta.files (dataset_id, object_uri, content_sha256, hash_version, size_bytes, filename, status, duplicate_of_file_id) VALUES (%s,...) ON CONFLICT (dataset_id, object_uri, content_sha256) DO UPDATE SET filename = EXCLUDED.filename, duplicate_of_file_id = EXCLUDED.duplicate_of_file_id RETURNING file_id` (a `UNIQUE` constraint `uq_files_dataset_uri_content` already exists per migration 0002 — confirm this is a real conflict target, not merely an index, before relying on `ON CONFLICT` against it).

    Add `Connection`/`Any` to `postgres.py`'s `TYPE_CHECKING` imports for `finalize_publication`'s `conn: Connection[Any]` parameter type (`from psycopg import Connection`).
  </action>
  <acceptance_criteria>
    - Every `<behavior>` bullet above passes as a distinct `pytest` test in `tests/integration/test_metadata_repository.py` (extended, not replaced)
    - `create_file` called twice with identical arguments leaves exactly 1 row in `meta.files` (`SELECT COUNT(*) FROM meta.files WHERE dataset_id=... AND object_uri=...` returns 1)
    - `finalize_publication`'s three UPDATEs are invisible to a second connection until the first connection's transaction commits (proves it never commits itself)
    - `mypy packages/dataplat/src` passes with the new `Connection[Any]` parameter typed correctly
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/integration/test_metadata_repository.py -x -q</automated>
  </verify>
  <done>All four new/changed MetadataRepository operations exist, are idempotent exactly where the behavior spec says, and finalize_publication provably shares the caller's transaction.</done>
</task>

<task type="auto">
  <name>Task 3: ObjectStore listing/writing and ConfigRegistry.get_by_id</name>
  <files>packages/dataplat/src/dataplat/storage/objectstore.py, packages/dataplat/src/dataplat/config/registry.py</files>
  <read_first>
    packages/dataplat/src/dataplat/storage/objectstore.py
    packages/dataplat/src/dataplat/config/registry.py
    packages/dataplat/src/dataplat/config/model.py
    tests/integration/conftest.py
  </read_first>
  <action>
    In `objectstore.py`, add to the `ObjectStore` Protocol: `list_objects(self, bucket: str, prefix: str) -> Iterator[ObjectSummary]` and `put_object(self, bucket: str, key: str, body: bytes) -> None`. Define `ObjectSummary` as a new frozen, slotted dataclass in this same module (`key: str, etag: str, size_bytes: int, last_modified: datetime`) — this is the minimal shape `dataplat.discovery` needs (key to construct `object_uri`, size for `meta.files.size_bytes`; content hashing is done separately via `get_object` + streaming sha256, never trusted from `etag`, since MinIO's ETag is an MD5 of implementation-dependent multipart chunking, not the platform's `content_sha256`). Implement both on `S3ObjectStore`: `list_objects` uses `self._client.get_paginator("list_objects_v2").paginate(Bucket=bucket, Prefix=prefix)`, yielding one `ObjectSummary` per `Contents` entry across pages (never `list_objects` singular, which truncates at 1000 keys silently); `put_object` calls `self._client.put_object(Bucket=bucket, Key=key, Body=body)`. Both wrap `(ClientError, BotoCoreError)` into `StorageError` exactly like `get_object` already does, with `context={"bucket": bucket, "prefix": prefix}` / `context={"bucket": bucket, "key": key}` respectively — copy the exact try/except shape from `get_object`, do not invent a new error-handling pattern.

    In `registry.py`, add `ConfigRegistry.get_by_id(self, config_version_id: int) -> DatasetConfig`: `SELECT config_document FROM meta.config_versions WHERE config_version_id = %s`, raise `StorageError` via the existing `_require_row` helper if no row, then `DatasetConfig.model_validate(row[0])` (the JSONB column already round-trips through psycopg as a Python dict — no `json.loads` needed) and return it. Import `DatasetConfig` outside `TYPE_CHECKING` now (this method uses it at runtime, unlike `sync`'s existing TYPE_CHECKING-only reference). Docstring must state this is the mechanism that lets historical reprocessing resolve a file's config EXACTLY as it was at ingestion time, without ever reading `configs/*.yaml` from disk — the pod that will call this (a later plan) does not have `configs/` mounted or baked in for `ingest`, only for `discover`.
  </action>
  <acceptance_criteria>
    - `list_objects("raw", "customers/")` against a testcontainers MinIO with 3 uploaded objects under that prefix yields exactly 3 `ObjectSummary` entries, and yields 0 for an empty/absent prefix (not an exception)
    - `put_object` followed by `get_object` on the same bucket/key round-trips the exact bytes
    - `get_by_id` called with a `config_version_id` returned by an earlier `ConfigRegistry.sync()` call returns a `DatasetConfig` that `==` the config object originally synced
    - `get_by_id` called with a nonexistent `config_version_id` raises `StorageError`, never `IndexError`/`TypeError`
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/integration -k "objectstore or config_registry" -x -q</automated>
  </verify>
  <done>ObjectStore supports listing and writing through the same seam it already reads through; ConfigRegistry can re-resolve a config by its stored ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DB-write API → analytical PostgreSQL | Every new/changed method here issues SQL against operator-controlled but data-bearing tables; values include file sizes, hashes, statuses derived from untrusted upstream input (S3 object metadata, CSV content) |
| discover/ingest process → object store | `list_objects`/`put_object` cross into MinIO with a bucket/prefix/key built partly from config, partly (later, in a downstream plan) from discovered filenames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-01 | Tampering | `postgres.py`'s four new/changed methods | mitigate | Every value crosses via `%s`/named placeholders exactly like the existing methods in this file; no f-string/`.format()` SQL assembly anywhere in this plan's diff — verified by `ruff`'s `S608` rule staying silent (no `# noqa: S608` added here, unlike `update_ingestion_run_status`'s pre-existing, already-justified suppression) |
| T-04-06 (new) | Tampering | `finalize_publication`'s caller-supplied `conn` | accept | `conn` is only ever supplied by this phase's own trusted orchestration code (a later plan), never by an external caller; the method's docstring states it must never be exposed to a call site outside the publication transaction |
| T-04-07 (new) | Information Disclosure | `ObjectSummary`/list_objects results | accept | Object keys/sizes are not secret; no credential or file content is returned by `list_objects` |

</threat_model>

<verification>
`uv run --group cluster pytest tests/integration -x -q` passes in full (extends, does not break, the existing 03-02 integration suite). `uv run --frozen mypy packages/dataplat/src` passes. `alembic upgrade head` then `alembic downgrade -1` then `alembic upgrade head` all succeed against a fresh testcontainers Postgres.
</verification>

<success_criteria>
Migration 0006 is live and reversible. `MetadataRepository`/`PostgresMetadataRepository` expose exactly the four new/changed operations with the idempotency semantics specified. `ObjectStore`/`S3ObjectStore` can list and write, not just read. `ConfigRegistry` can resolve a config by its stored surrogate ID. No existing Phase 3 test regresses.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-01-SUMMARY.md` when done
</output>
