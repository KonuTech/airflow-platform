---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 08
type: execute
wave: 5
depends_on: ["04-02", "04-07"]
files_modified:
  - tests/fixtures/slice-corpus.yaml
  - tests/e2e/slice/__init__.py
  - tests/e2e/slice/conftest.py
  - tests/e2e/slice/test_smoke_and_idempotency.py
  - tests/e2e/slice/test_pod_kill_retry.py
  - tests/e2e/slice/test_concurrent_select.py
  - docs/spikes/U1-smoke-xcom.md
  - docs/spikes/U3-throughput-baseline.md
autonomous: true
requirements: [QUAL-06, QUAL-09, LOAD-01, LOAD-02, LOAD-03, LOAD-05]
must_haves:
  truths:
    - "Dropping a UTF-8 comma CSV into s3://raw/customers/ results in rows appearing in normalized.customers within the sensor's poke window, unattended"
    - "Re-uploading identical file content under a new name produces zero additional rows"
    - "Killing the ingest pod mid-load with a real kubectl delete pod, and letting Airflow retry, leaves no duplicate rows"
    - "A concurrent SELECT running throughout an in-flight publish observes only the pre-publish or fully-published row count, never a partial one"
    - "The smoke DAG's XCom contains the git SHA that was actually built and pushed"
  artifacts:
    - path: "tests/fixtures/slice-corpus.yaml"
      provides: "a ~50-200 row fixture (D-07) and a ~1M row fixture (D-06), both Faker-style synthetic data via existing tools/corpus ColumnSpec kinds"
    - path: "tests/e2e/slice/test_pod_kill_retry.py"
      provides: "the permanent D-09/D-10/D-11 regression test"
    - path: "docs/spikes/U3-throughput-baseline.md"
      provides: "the committed streaming-throughput + peak-RSS baseline ROADMAP success criterion 5 requires"
  key_links:
    - from: "tests/e2e/slice/conftest.py"
      to: "tests/fixtures/slice-corpus.yaml"
      via: "tools.corpus.generators.generate_corpus, called on demand"
      pattern: "generate_corpus"
    - from: "tests/e2e/slice/test_pod_kill_retry.py"
      to: "meta.ingestion_runs.rows_read / lease_expires_at"
      via: "a deadline-loop poll, never sleep"
      pattern: "time.monotonic"
---

<objective>
Prove, against the live kind cluster with the real DAGs running, every claim this phase's ROADMAP success criteria make: unattended end-to-end delivery, zero-additional-rows on rerun and re-upload, no duplicates after a genuine pod kill, no partial visibility during publish, and the smoke DAG's XCom carrying the real built SHA. Record the U1 and U3 spike results as committed artifacts.

Purpose: this is QUAL-06/QUAL-09's actual proof, and the permanent regression fixtures for the platform's single largest correctness risk (silent duplication) per D-09/D-10.
Output: the `tests/e2e/slice/` suite, runnable via `make cluster-verify` (extended by a later plan), plus two committed spike-result documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
From `tests/fixtures/corpus.yaml` (the EDGE-CASE corpus — do not add slice fixtures here; this is a SEPARATE manifest, same shape):
```yaml
version: 1
master_seed: "airflow-platform/corpus/v1"
fixtures:
  - name: "01_simple.csv"
    covers: [CSV-04, CSV-07]
    generator: tabular
    encoding: utf-8
    bom: false
    delimiter: ","
    header: [id, name, amount]
    rows: 20
    row_spec:
      id: { kind: zero_padded_int, width: 6, start: 1 }
      name: { kind: pick, values: ["Kowalski", "Nowak"] }
      amount: { kind: decimal, min: "100.00", max: "99999.99", scale: 2 }
```
No new `ColumnSpec` kind is needed for `customer_id`/`name`/`country`/`birth_date`/`event_ts` — all four are expressible with the EXISTING `zero_padded_int`/`pick` kinds (a `pick` list of ~30-50 pre-written realistic full names/ISO country codes/birth-date strings/ISO timestamp strings is deterministic, Faker-*style*, and requires zero changes to `tools/corpus/manifest.py`/`generators.py`). Generate via the ALREADY-GENERIC CLI: `python -m tools.corpus generate --manifest tests/fixtures/slice-corpus.yaml --out <dir>` — no code change to `tools/corpus/__main__.py` needed (its `--manifest`/`--out` flags already accept any manifest path).

From `tests/e2e/cluster/conftest.py` (the `_require_cluster` skip-with-reason pattern to copy — see this plan's Task 1) and `tests/e2e/cluster/test_airflow_workloads.py`'s `_port_forwarded_postgres`/`metadata_connection` fixture shape (the poll-until-connected, deadline-loop, never-`sleep`-blindly pattern — copy this shape for polling `meta.ingestion_runs` too, substituting the poll TARGET).

`analytics-db-app`/`analytics_connection`-equivalent: this suite needs a live connection to the ANALYTICAL cluster specifically (not the Airflow metadata cluster) — `tests/e2e/cluster/test_postgres_topology.py`'s `analytics_connection` fixture (`_cluster_connection(kubectl_context, kubectl_json, "analytics-db")`) is the exact, already-proven pattern; port-forward `svc/analytics-db-rw` in namespace `data`, credentials from the `analytics-db-app` Secret. This suite's OWN connections should authenticate as `etl_app` where the test specifically wants to observe what the PIPELINE sees (matching production access), or as the owner where broader test-harness access is needed (e.g. cleanup) — decide per test and document which.

D-11's polling target: `meta.ingestion_runs.rows_read` -- populated live, mid-run, by `04-05`'s `run_ingest` heartbeat (`pipeline/run.py`), which updates `rows_read`/`rows_parsed` alongside `lease_expires_at` on every heartbeat tick (every ~60s), sourced from a progress holder `04-04`'s `StagingLoader.load()` updates via its `on_progress` callback after each chunk (see 04-05 Task 1 and 04-04 Task 1). This plan's `test_pod_kill_retry.py` polls `rows_read > 0 AND status = 'RUNNING'` as the mid-load signal -- D-11's named field, genuinely populated, no fallback needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures, conftest, and the smoke-XCom + idempotent-reupload tests</name>
  <files>tests/fixtures/slice-corpus.yaml, tests/e2e/slice/__init__.py, tests/e2e/slice/conftest.py, tests/e2e/slice/test_smoke_and_idempotency.py</files>
  <read_first>
    tests/fixtures/corpus.yaml
    tools/corpus/manifest.py
    tools/corpus/generators.py
    tests/e2e/cluster/conftest.py
    tests/e2e/cluster/test_postgres_topology.py
    tests/e2e/cluster/test_minio_buckets.py
  </read_first>
  <action>
    Create `tests/fixtures/slice-corpus.yaml` with a header comment stating (per `corpus.yaml`'s own convention) why this is a SEPARATE manifest: realistic, uniform E2E data, not edge-case pathologies. `master_seed: "airflow-platform/slice-corpus/v1"` (distinct from the edge-case corpus's seed — a shared seed with a shared name would couple the two corpora's random streams together, R1's concern generalized). Two fixtures: `customers_small.csv` (`rows: 120`, `profile: default`, header `[customer_id, name, country, birth_date, event_ts]`, `row_spec`: `customer_id: {kind: zero_padded_int, width: 6, start: 1}`, `name: {kind: pick, values: [<30-50 realistic "First Last" strings>]}`, `country: {kind: pick, values: [<15-20 ISO 3166-1 alpha-2 codes>]}`, `birth_date: {kind: pick, values: [<30-50 pre-written YYYY-MM-DD strings spanning ~1950-2005>]}`, `event_ts: {kind: pick, values: [<30-50 pre-written ISO-8601 UTC timestamp strings spanning a plausible recent range>]}`); `customers_large.csv` (`rows: 1000000`, `profile: large`, SAME header/row_spec shape, `expect: {approx_bytes: <measured after first generation>, rlimit_as_bytes: 536870912}` — the `_validate_large_profile` rule in `tools/corpus/manifest.py` requires `approx_bytes > 2 * rlimit_as_bytes`; pick `rlimit_as_bytes` to represent the KPO ingest pod's configured memory LIMIT from `common_kpo_kwargs` (1Gi, i.e. 1073741824) — `approx_bytes` must then exceed 2147483648, which a 1,000,000-row 5-column fixture with realistic string values comfortably does; measure the actual generated size once and fill in the real number, do not guess).

    Create `tests/e2e/slice/__init__.py` (empty, matching `tests/e2e/cluster/__init__.py`). Create `tests/e2e/slice/conftest.py`: an autouse, session-scoped `_require_cluster` fixture copied in shape from `tests/e2e/cluster/conftest.py`'s own (same skip message convention, same `kubectl_context` reachability check — either import it directly if `tests/e2e/slice/` inherits `tests/e2e/cluster/conftest.py`'s fixtures via pytest's parent-conftest mechanism, or re-derive identically; verify which by checking whether `tests/e2e/slice` is independently collected before deciding, and do NOT invent a second, differently-worded skip message either way). A session-scoped `slice_fixtures_dir` fixture: generates BOTH fixtures from `tests/fixtures/slice-corpus.yaml` into a `tmp_path_factory`-provided session-scoped directory on first use (via `tools.corpus.generators.generate_corpus` + `tools.corpus.manifest.load_manifest`, called directly in Python — no `make` step, no pre-generated files committed to git, matching QUAL-08's "generated from a seed, not committed en masse" policy extended to this corpus too), returning the directory `Path`. An `analytics_connection`/`analytics_owner_connection` fixture pair, copied in shape from `tests/e2e/cluster/test_postgres_topology.py`'s `_cluster_connection("analytics-db")` (import and reuse that module's helper if it is exported/importable, or copy the ~15-line helper — prefer import if `test_postgres_topology.py` exposes it at module level without a leading underscore; otherwise copy, do not create a THIRD divergent implementation). A `poll_ingestion_run(conn, idempotency_key, *, timeout=120) -> dict` helper: a `time.monotonic()` deadline loop (never `sleep(N)` for the whole wait, only short `sleep(0.5)` between polls), querying `SELECT status, rows_loaded, lease_expires_at FROM meta.ingestion_runs WHERE idempotency_key = %s`, returning the row as a dict once `status` reaches a terminal value (`SUCCEEDED`/`FAILED`/`SKIPPED_DUPLICATE`/`SKIPPED_CONCURRENT`) or raising `AssertionError` naming the timeout and the last-observed status if the deadline passes — this is the ONE polling helper every test in this directory reuses, matching PITFALLS' "sleep in E2E tests is a permanent-flakiness trap" (line ~2168) and this codebase's OWN established deadline-loop idiom (`_port_forwarded_postgres`).

    Create `tests/e2e/slice/test_smoke_and_idempotency.py`. `test_smoke_dag_xcom_contains_built_sha` (U1): triggers or waits for the NEXT `smoke_kubernetes_pod` DagRun (poll `airflow dags list-runs`/the REST API for a `success` state on a `@daily`-scheduled DAG — since the schedule is daily, TRIGGER it explicitly via `kubectl exec ... airflow dags trigger smoke_kubernetes_pod`, then poll for completion via a deadline loop, never sleep), reads the task's XCom via `kubectl exec ... airflow tasks state`/the Airflow REST API's XCom endpoint, asserts the `git_sha` value equals `git rev-parse --short HEAD` run against THIS checkout — this is the U1 pass criterion verbatim. Writes/updates `docs/spikes/U1-smoke-xcom.md` (a short, committed Markdown note: date, git SHA proven, the exact assertion) — automate this write from the test itself (or from a small script the test invokes) so the artifact is regenerated, not hand-maintained.

    `test_idempotent_reupload` (D-07, LOAD-03/QUAL-09): uploads `customers_small.csv`'s bytes to `raw/customers/upload-1.csv`, waits (via `poll_ingestion_run`, keyed by a computable idempotency_key OR by polling `meta.files`/`meta.ingestion_runs` for a NEW row matching that content hash if the key cannot be predicted client-side — document which) for the run to reach `SUCCEEDED`, records `normalized.customers`'s row count; uploads the SAME bytes to `raw/customers/upload-2.csv` (different key, identical content); waits for discovery to register the second file; asserts `meta.files` shows the second file's row with `duplicate_of_file_id` set to the first's, asserts NO new `meta.ingestion_runs` row was created for the second file, and asserts `normalized.customers`'s row count is UNCHANGED. Cleans up both uploaded objects in a `finally` (admin credential, mirroring `test_minio_buckets.py`'s own cleanup convention).
  </action>
  <acceptance_criteria>
    - `tests/fixtures/slice-corpus.yaml` validates via `tools.corpus.manifest.load_manifest` with zero errors
    - `python -m tools.corpus generate --manifest tests/fixtures/slice-corpus.yaml --out <tmp> --fast` generates `customers_small.csv` only (large-profile skipped) in under 5 seconds
    - `test_smoke_dag_xcom_contains_built_sha` and `test_idempotent_reupload` both pass against the live cluster
    - `docs/spikes/U1-smoke-xcom.md` exists and names the exact SHA the test proved
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/slice/test_smoke_and_idempotency.py -x -q</automated>
  </verify>
  <done>The fixture corpus, shared E2E harness, U1's own proof, and the LOAD-03/QUAL-09 reupload proof all exist and pass against the live cluster.</done>
</task>

<task type="auto">
  <name>Task 2: The permanent pod-kill/retry test (D-09..D-11) and the U3 throughput/RSS baseline</name>
  <files>tests/e2e/slice/test_pod_kill_retry.py, docs/spikes/U3-throughput-baseline.md</files>
  <read_first>
    tests/e2e/cluster/test_airflow_workloads.py
    tests/e2e/slice/conftest.py
    04-05-PLAN.md
    04-04-PLAN.md
  </read_first>
  <action>
    Create `tests/e2e/slice/test_pod_kill_retry.py`. `test_pod_kill_mid_load_produces_no_duplicates` (D-09/D-10/D-11 — a REAL `kubectl delete pod`, never a self-kill env var): upload `customers_large.csv` (the 1M-row fixture) to a fresh `raw/customers/` key; poll `kubectl get pods -n etl -l <a label the KPO pod carries, e.g. the Airflow-injected `dag_id`/`task_id` labels>` in a deadline loop until a pod is `Running`; once running, poll `meta.ingestion_runs` for THIS run's row until `rows_read > 0 AND status = 'RUNNING'` (D-11's named signal, populated live by 04-05's heartbeat -- see Interfaces) is true; THEN `kubectl delete pod <name> -n etl --wait=false`; poll `meta.ingestion_runs` again until status reaches a terminal value (Airflow's own `retries=3` on the `ingest` task means a NEW pod launches automatically — this may take several minutes for a 1M-row reload; size the deadline generously, e.g. 600s, and justify the number in a comment); assert final status is `SUCCEEDED`; assert `SELECT COUNT(*) FROM normalized.customers WHERE customer_id = ANY(...)` (or `SELECT COUNT(DISTINCT customer_id)` compared against `SELECT COUNT(*)`) shows NO duplicate `customer_id` values — the `UNIQUE(customer_id)` constraint from migration 0006 makes a literal SQL-level duplicate impossible, so the meaningful assertion is that total row count matches expected (1,000,000, or the fixture's actual row count) exactly, not more.

    While this test's pod is loading (between "confirmed Running" and the `kubectl delete pod` call), measure and record: (a) throughput — `rows_loaded` (once available) divided by observed wall-clock duration, or a proxy computed from `meta.ingestion_runs.started_at`/heartbeat timestamps if the run does not complete during THIS particular kill-and-retry test (use a SEPARATE, non-killed full run of the same 1M-row fixture for a clean throughput measurement — killing mid-load is not the right run to measure steady-state throughput from; add a second test, `test_u3_throughput_and_peak_rss_baseline`, that uploads the SAME 1M-row fixture WITHOUT killing anything, polls to completion, computes `rows_loaded / (duration_ms / 1000)` rows/sec from the final `meta.ingestion_runs` row, and samples peak memory via `kubectl top pod -n etl <name> --no-headers` polled every few seconds during the run, OR reads `/sys/fs/cgroup/memory.peak` via `kubectl exec` into the pod if it is still running at sample time, taking the MAX observed value — do not attempt an exact peak if the pod completes and is deleted before a sample lands; document the measurement's honest limitation in `docs/spikes/U3-throughput-baseline.md` rather than overclaiming precision). Write the measured throughput (rows/sec) and peak RSS (MB) to `docs/spikes/U3-throughput-baseline.md`, committed, with the date, the fixture used, the pod's configured memory LIMIT (1Gi, from `common_kpo_kwargs`), and an explicit statement that a future >5x regression against this baseline should be treated as a bug (ROADMAP's own words), not a mystery.
  </action>
  <acceptance_criteria>
    - `test_pod_kill_mid_load_produces_no_duplicates` passes against the live cluster with a REAL `kubectl delete pod` (not a simulated/self-triggered failure) and asserts final row count equals the fixture's row count exactly, with zero duplicate `customer_id`s
    - `test_u3_throughput_and_peak_rss_baseline` passes and produces a nonzero rows/sec figure and a nonzero peak-RSS figure
    - `docs/spikes/U3-throughput-baseline.md` exists, is committed, and states the measured numbers plus the "5x regression = bug" policy
    - Neither test uses `time.sleep(N)` as its wait mechanism anywhere except the short inter-poll delay inside the shared `poll_ingestion_run`/deadline-loop helper
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/slice/test_pod_kill_retry.py -x -q --timeout=900</automated>
  </verify>
  <done>The platform's single largest correctness risk -- silent duplication after a genuine crash -- has a permanent, real, automated proof, and the U3 spike baseline is committed for future regression comparison.</done>
</task>

<task type="auto">
  <name>Task 3: Concurrent-SELECT-during-publish atomicity test (D-12)</name>
  <files>tests/e2e/slice/test_concurrent_select.py</files>
  <read_first>
    tests/e2e/slice/conftest.py
    04-04-PLAN.md
  </read_first>
  <action>
    Create `tests/e2e/slice/test_concurrent_select.py`. `test_concurrent_select_never_observes_partial_publish`: opens a SEPARATE, long-lived connection to the analytical cluster BEFORE triggering ingestion (the "observer" connection); records the pre-upload `normalized.customers` row count; uploads a fresh CSV to `raw/customers/`; spawns a background thread that repeatedly (every ~0.2s, via the observer connection, in AUTOCOMMIT so each query sees the latest committed snapshot) runs `SELECT COUNT(*) FROM normalized.customers` and appends each observed count to a list, running continuously until the main thread signals the run has reached a terminal status (via `poll_ingestion_run`); once terminal, stops the background thread and asserts every recorded count in the list is EITHER the pre-upload count OR the final post-upload count — never anything strictly between the two (a genuinely partial state). This is a live-cluster, real-network-latency version of 04-06's `test_atomic_commit`, and is the ONLY test in this phase that proves the property under REAL Kubernetes pod execution rather than a same-process two-connection simulation — state this distinction in the test's own docstring, since 04-06 already covers the mechanism in isolation and this test's job is proving it survives the real deployment topology.
  </action>
  <acceptance_criteria>
    - The test passes against the live cluster
    - The recorded observation list contains at least 3 samples strictly during the run's `RUNNING` window (proves the background poller actually caught the in-flight state, not just before/after — a test with zero mid-run samples would pass vacuously)
    - No `time.sleep()` gates the WAIT for run completion (only the background poller's own short, bounded interval)
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/e2e/slice/test_concurrent_select.py -x -q --timeout=300</automated>
  </verify>
  <done>Success criterion 3's second half -- "a concurrent SELECT never observes a half-loaded table" -- has its own dedicated, real-cluster proof, not an inference from the retry test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| E2E test process → live cluster kubectl/psql/S3 | This suite has cluster-admin-equivalent access via the developer's kubeconfig; it is a test harness, not production code, but still must not leak credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-04 | Information Disclosure | Credentials read via `analytics_connection`/MinIO fixtures | mitigate | Every credential is read fresh from live Secrets at fixture-call time, never written to disk or logged, matching `tests/e2e/cluster/conftest.py`'s own established `_read_minio_credentials`/`_read_app_secret` discipline |
| T-04-13 (new) | Denial of Service | `kubectl delete pod` against a live-cluster pod | accept | This is a deliberate, test-scoped destructive action against the developer's OWN local kind cluster, gated behind `_require_cluster`'s skip-with-reason and never runnable against a production target — accepted as the whole point of D-09 |

</threat_model>

<verification>
`uv run --group cluster pytest tests/e2e/slice -x -q --timeout=900` passes in full against the live kind cluster. Both `docs/spikes/*.md` files exist and are committed.
</verification>

<success_criteria>
Every QUAL-06/QUAL-09/LOAD-01/02/03/05 claim this phase makes is proven end to end against the real deployed platform, and both spikes' results are permanently recorded in the repository.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-08-SUMMARY.md` when done
</output>
