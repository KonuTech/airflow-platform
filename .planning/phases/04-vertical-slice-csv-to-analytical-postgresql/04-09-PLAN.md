---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 09
type: execute
wave: 5
depends_on: ["04-02", "04-07"]
files_modified:
  - scripts/ingest-demo.py
  - Makefile
autonomous: true
requirements: [QUAL-06]
must_haves:
  truths:
    - "make ingest-demo FILE=<path> uploads the file, never triggers the DAG directly, and prints a receipt once the sensor-driven run reaches a terminal status"
    - "make cluster-verify now also runs tests/e2e/slice alongside tests/e2e/cluster"
  artifacts:
    - path: "scripts/ingest-demo.py"
      provides: "D-14/D-15/D-16 developer demo workflow"
  key_links:
    - from: "Makefile's ingest-demo target"
      to: "scripts/ingest-demo.py"
      via: "$(RUN_CLUSTER) python scripts/ingest-demo.py --file $(FILE)"
      pattern: "ingest-demo"
---

<objective>
Give a developer a single command that exercises the real, unattended vertical slice — upload, wait for the sensor to notice, poll for completion, print a receipt — with no shortcut that bypasses the sensor.

Purpose: D-14/D-15/D-16, and this phase's own definition of "done" (ROADMAP: "a re-run produces zero additional rows" is not proven to a developer's satisfaction by test output alone — a working demo command is part of the deliverable).
Output: `make ingest-demo FILE=<path>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
From `Makefile` (current) — the two closest analog targets to copy conventions from:
```makefile
minio-creds:                   ## D-14: print live MinIO credentials, shell-sourceable [plan 02-04]
	@set -a; . helm/versions.env; set +a; \
	KUBECTL_CONTEXT="kind-$$CLUSTER_NAME" scripts/minio-credentials.sh show

cluster-verify:                 ## D-16: run tests/e2e/cluster against the live cluster [plan 02-02]
	$(RUN_CLUSTER) pytest tests/e2e/cluster -q
```
`FAST ?=` variable-argument pattern (Makefile, `fixtures` target) is the convention to copy for `FILE ?=` — declare, then fail loudly with a clear message if unset, matching `stage-%`'s own explicit-failure style (`if [ -z "$$script" ]; then echo "ERROR: ..." >&2; exit 1; fi`).

From `scripts/minio-credentials.sh`'s header/usage convention (shebang, decision-naming comment block, `usage()` printing to stderr and exiting `2` on bad invocation) — apply the SAME discipline to `scripts/ingest-demo.py`'s argument handling (via `argparse`, Python's equivalent).

Reuse, never reimplement: `S3ObjectStore`-adjacent `boto3` construction (`packages/dataplat/src/dataplat/storage/objectstore.py` lines 96-112) or the `s3_client` fixture's plain `boto3.client(...)` call shape (`tests/e2e/cluster/conftest.py` lines 220-227) for the upload step — never `mc` (not installed, not vendored, explicitly rejected in 04-RESEARCH.md's Don't-Hand-Roll table).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/ingest-demo.py — upload, poll, print the receipt</name>
  <files>scripts/ingest-demo.py</files>
  <read_first>
    scripts/minio-credentials.sh
    tests/e2e/cluster/conftest.py
    tests/e2e/slice/conftest.py
    packages/dataplat/src/dataplat/storage/objectstore.py
  </read_first>
  <action>
    Create `scripts/ingest-demo.py` (Python, chosen per 04-CONTEXT.md's "Claude's Discretion" — needs both `boto3` PutObject and a `psycopg` poll, both already pinned in the `cluster` dependency group, matching `tests/e2e/cluster/conftest.py`'s own import discipline). `argparse` with `--file` (required, a local path to upload) and `--key` (optional, defaults to `customers/{basename}-{unix-timestamp}.csv` so repeated demo runs never collide on object key). Resolves MinIO credentials via the SAME `scripts/minio-credentials.sh show` subprocess-and-parse pattern `tests/e2e/cluster/conftest.py`'s `_read_minio_credentials` already establishes (reuse the exact regex/shlex parsing shape, do not diverge), builds a `boto3` client identically to that fixture's own `s3_client("app")` construction. Uploads the file to `raw/<key>` via `put_object`. Prints "Uploaded to s3://raw/<key> — waiting for the sensor to notice (poke interval 30s, deferred)..." Then polls `meta.ingestion_runs` (via the analytical cluster, reusing `tests/e2e/cluster/test_postgres_topology.py`'s `_cluster_connection`-style port-forward-and-connect logic, OR a simpler direct DSN if this script is meant to run from the SAME host as `kubectl port-forward` — decide and document) — joins through `meta.files` on the uploaded object's content hash (compute the SHA-256 of the local file being uploaded, client-side, to match against `meta.files.content_sha256` once discovery registers it) with a `time.monotonic()` deadline loop (default 300s, overridable via `--timeout`), never a blind `sleep`. Once a matching `meta.ingestion_runs` row reaches a terminal status, prints the receipt: `run_id`, `status`, `rows_loaded`, `duration_ms`, `report_uri` — formatted as readable key-value lines, not raw JSON, since this is a human-facing demo output. On timeout, prints the last-observed state (or "not yet discovered" if `meta.files` never got a matching row) and exits nonzero with a message suggesting `kubectl logs`/the Airflow UI for further diagnosis — never silently hangs or exits 0 on a timeout.

    Explicitly do NOT add any `airflow dags trigger`/CLI-trigger call anywhere in this script (D-15, explicit user instruction) — the module docstring must state this prohibition by name, citing D-15, so a future edit does not "helpfully" add a shortcut.
  </action>
  <acceptance_criteria>
    - `python scripts/ingest-demo.py --file tests/fixtures/csv/01_simple.csv` against a live cluster with the DAG deployed: uploads the file, prints the waiting message, and (once the sensor+discover+ingest chain completes) prints a receipt with a real `run_id`/`status=SUCCEEDED`/`rows_loaded`
    - `grep -n "dags trigger\|dags_trigger\|trigger_dag" scripts/ingest-demo.py` returns zero matches
    - Running with `--file` pointing at a nonexistent path fails immediately with a clear message, before any upload attempt
    - No credential value appears in the script's own stdout/stderr output at any point
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster python scripts/ingest-demo.py --help</automated>
  </verify>
  <done>scripts/ingest-demo.py uploads, waits on the real sensor path, and prints a receipt -- with no CLI-trigger shortcut anywhere in it.</done>
</task>

<task type="auto">
  <name>Task 2: make ingest-demo and the cluster-verify extension</name>
  <files>Makefile</files>
  <read_first>
    Makefile
  </read_first>
  <action>
    Add `FILE ?=` near the existing `FAST ?=`/`PROFILE ?=` variable declarations. Add a new `ingest-demo` target (`## D-14: upload FILE and wait for the real sensor-driven pipeline [plan 04-09]` as its help comment) whose recipe first guards on `FILE` being non-empty — matching `stage-%`'s own explicit-failure style verbatim: an `if [ -z "$(FILE)" ]; then echo "ERROR: FILE is required, e.g. make ingest-demo FILE=tests/fixtures/csv/01_simple.csv" >&2; exit 1; fi` line, prefixed with `@` so the guard itself is silent — then, on success, runs `$(RUN_CLUSTER) python scripts/ingest-demo.py --file $(FILE)`. Add `ingest-demo` to the existing `.PHONY:` block. Extend `cluster-verify`'s existing recipe line from `pytest tests/e2e/cluster -q` to `pytest tests/e2e/cluster tests/e2e/slice -q` (a prior plan, 04-08, created `tests/e2e/slice/` — this is the "extending the existing cluster-verify pattern" 04-VALIDATION.md names), updating the target's `##` help comment to mention both directories.
  </action>
  <acceptance_criteria>
    - `make ingest-demo` with no `FILE` set fails immediately with the documented error message and nonzero exit, before attempting `$(RUN_CLUSTER)`
    - `make ingest-demo FILE=tests/fixtures/csv/01_simple.csv` against a live cluster runs `scripts/ingest-demo.py` and exits 0 on success
    - `make cluster-verify` (against a live cluster) collects tests from BOTH `tests/e2e/cluster` and `tests/e2e/slice`
    - `ingest-demo` appears in `make help`'s output
  </acceptance_criteria>
  <verify>
    <automated>make help | grep -q "ingest-demo" && grep -n "tests/e2e/cluster tests/e2e/slice" Makefile</automated>
  </verify>
  <done>make ingest-demo is a real, documented, working developer command, and make cluster-verify collects this phase's whole E2E suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer workstation → live cluster credentials | `scripts/ingest-demo.py` reads live MinIO/Postgres credentials the same way the E2E test harness does |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-09 | Information Disclosure | `scripts/ingest-demo.py`'s credential handling | mitigate | Credentials are read via the existing `scripts/minio-credentials.sh show` mechanism and a live Secret read, never hardcoded, never printed, never passed as a `--` CLI argument to any subprocess this script spawns |
| T-04-14 (new) | Elevation of Privilege | A future edit adding a CLI-trigger shortcut | mitigate | The module docstring explicitly names D-15 and prohibits `airflow dags trigger`; `test_no_manual_kubectl_surgery.py`-style repo policy conventions could be extended in a later phase to enforce this mechanically, noted as a follow-up, not built here (out of this plan's scope) |

</threat_model>

<verification>
`make help` lists `ingest-demo`. `make ingest-demo FILE=tests/fixtures/csv/01_simple.csv` succeeds against a live cluster. `make cluster-verify` collects both E2E directories.
</verification>

<success_criteria>
A developer has one command that demonstrates the real, unattended vertical slice end to end, with a human-readable receipt and zero shortcuts around the sensor.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-09-SUMMARY.md` when done
</output>
