---
phase: 04-vertical-slice-csv-to-analytical-postgresql
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-03", "04-04"]
files_modified:
  - tests/integration/test_discover_files.py
  - tests/integration/test_publish_merge.py
autonomous: true
requirements: [ORCH-08, META-03, LOAD-04, LOAD-08, LOAD-09, QUAL-05]
must_haves:
  truths:
    - "Discovery rerun over an unchanged object set produces the identical assignment manifest and creates zero additional meta.files/meta.ingestion_runs rows"
    - "Two concurrent publish transactions against overlapping customer_id sets serialize through pg_advisory_xact_lock and neither raises a cardinality or unique-constraint violation"
    - "Every loaded row's _run_id/_file_id/_batch_id/_source_row_number is independently verifiable by SQL"
    - "A duplicate meta.batches (dataset_id, batch_key) is rejected by the database, not merely by application discipline"
  artifacts:
    - path: "tests/integration/test_discover_files.py"
      provides: "ORCH-08 rerun-produces-identical-manifest proof"
    - path: "tests/integration/test_publish_merge.py"
      provides: "META-03 atomic-commit proof, LOAD-09 concurrency proof, LOAD-04/LOAD-08 lineage/uniqueness proofs"
  key_links:
    - from: "tests/integration/test_publish_merge.py"
      to: "packages/dataplat/src/dataplat/load/publish/merge.py + metadata/postgres.py's finalize_publication"
      via: "a hand-assembled publication transaction mirroring run_ingest's own shape"
      pattern: "pg_advisory_xact_lock"
---

<objective>
Prove, against a real Postgres and a real MinIO (testcontainers), the four correctness properties this phase's whole idempotency/atomicity story depends on: discovery is genuinely idempotent and frozen, publication is genuinely single-writer-safe under real concurrency, lineage is genuinely queryable, and the batch ledger's uniqueness constraint genuinely rejects a duplicate.

Purpose: QUAL-05's integration-test requirement, and the concrete evidence that 04-03's `discover_files` and 04-04's `MergePublisher` do what their own plans claimed without a live pod or DAG.
Output: two extended/new integration test files, runnable via `make test-integration`, that a later plan's E2E suite does not need to re-prove at the unit/integration level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-CONTEXT.md
@.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-RESEARCH.md
</context>

<interfaces>
From `tests/integration/conftest.py` (current, reuse directly — do not build a third MinIO/Postgres-spinning fixture):
```python
@pytest.fixture(scope="session")
def migrated_dsn(run_migrations, postgres_dsn) -> str: ...
@pytest.fixture(scope="session")
def minio_config(...) -> Iterator[dict[str, str]]: ...
@pytest.fixture(scope="session")
def s3_client(minio_config) -> Any: ...
```

From `tests/integration/test_metadata_repository.py` (existing style to copy — full-round-trip-in-one-test shape, `_insert_config_version`-style direct-SQL seeding helper for rows no repository method covers yet):
```python
@pytest.fixture
def repository(migrated_dsn: str) -> Iterator[PostgresMetadataRepository]:
    pool = create_pool(migrated_dsn)
    pool.open(wait=True)
    try:
        yield PostgresMetadataRepository(pool)
    finally:
        pool.close()
```

For the concurrency test (PITFALLS C1's named case): open TWO separate `psycopg.Connection`s against `migrated_dsn`, each with `autocommit=False`. Seed two staging tables with overlapping `customer_id`s (direct `psycopg.connect(dsn)` + `CREATE UNLOGGED TABLE` + `COPY`, since no repository method exists for seeding staging content at the integration-test level — this mirrors `test_metadata_repository.py`'s own precedent of seeding via direct SQL where no repository method exists). Run `MergePublisher().publish(...)` on connection A inside a transaction holding `pg_advisory_xact_lock`, then attempt the SAME on connection B from a background thread BEFORE committing A — assert B blocks (does not return) until A commits, then both eventually succeed with no error.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: test_discover_files.py — frozen-manifest rerun proof</name>
  <files>tests/integration/test_discover_files.py</files>
  <read_first>
    packages/dataplat/src/dataplat/discovery.py
    tests/integration/conftest.py
    tests/integration/test_metadata_repository.py
    packages/dataplat/src/dataplat/config/model.py
  </read_first>
  <action>
    Create `tests/integration/test_discover_files.py`. Fixture composition depends on `migrated_dsn`, `s3_client`, builds a `PostgresMetadataRepository`/`S3ObjectStore` pair against them (mirroring `test_metadata_repository.py`'s `repository` fixture shape). `test_rerun_produces_identical_manifest`: upload 2 CSV objects to `s3://raw/customers/`, call `discover_files(...)` once, capture the returned units' `idempotency_key`s and the assignment JSON bytes written to `s3://metadata/assignments/...`; call `discover_files(...)` again with no change to the source objects; assert the second call's returned units are EMPTY (both prior runs are still `PENDING`, so — per 04-03's own behavior spec — they ARE re-offered; re-read that spec precisely: `PENDING` runs ARE included on rerun, only `SUCCEEDED` ones are excluded) — adjust the assertion to match 04-03's actual documented semantics: mark both runs `SUCCEEDED` via `update_ingestion_run_status` between the two calls, THEN assert the second call returns zero units and `SELECT COUNT(*) FROM meta.files` is unchanged (still 2, not 4). `test_duplicate_content_is_skipped`: upload the SAME bytes under two different keys, call `discover_files` once (covering both in one call), assert exactly ONE unit is returned and the second file's `meta.files` row has `duplicate_of_file_id` set to the first's `file_id`. `test_business_date_stays_null`: after any discovery call, `SELECT business_date FROM meta.files` is `NULL` for every row. `test_batching_cap_defers_excess`: set `config.batching.max_units_per_run` to 1 via a locally-constructed `DatasetConfig`, upload 3 new objects, call `discover_files` once, assert exactly 1 unit returned and `meta.ingestion_runs` has 3 `PENDING` rows (not 1) — the excess is deferred, not dropped.
  </action>
  <acceptance_criteria>
    - All four tests pass against real testcontainers Postgres + MinIO
    - `test_rerun_produces_identical_manifest`'s second-call assertion is against `meta.files`/`meta.ingestion_runs` row counts, not merely against the returned Python list (proves no hidden duplicate row, not just no hidden duplicate return value)
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/integration/test_discover_files.py -x -q</automated>
  </verify>
  <done>ORCH-08's frozen-manifest guarantee and LOAD-03/INCR-08/ORCH-03's discovery-time behaviors are proven against a real database and object store.</done>
</task>

<task type="auto">
  <name>Task 2: test_publish_merge.py — atomicity, concurrency, lineage, batch uniqueness</name>
  <files>tests/integration/test_publish_merge.py</files>
  <read_first>
    tests/integration/test_publish_merge.py
    packages/dataplat/src/dataplat/load/publish/merge.py
    packages/dataplat/src/dataplat/metadata/postgres.py
    migrations/versions/0003_meta_batches_batch_files.py
  </read_first>
  <action>
    Extend `tests/integration/test_publish_merge.py` (created by 04-04's own Task 2; do not duplicate its existing tests, add these four alongside them). `test_atomic_commit`: seed a staging table, open one connection/transaction, call `MergePublisher().publish(...)` then `metadata.finalize_publication(conn=conn, ...)` WITHOUT committing yet; from a SECOND connection, assert `normalized.customers` row count and `meta.files`/`meta.batches`/`meta.ingestion_runs` statuses are all UNCHANGED (still pre-publish state); commit the first connection's transaction; assert the second connection NOW sees all four effects (rows + three status updates) simultaneously — this is META-03's literal claim, proven as one indivisible visibility transition, not four separate ones. `test_advisory_lock_serializes_concurrent_publishers`: the two-connection/background-thread shape from Interfaces — assert connection B's `publish()` call does not return until connection A commits (measure via a timestamp/event, not a fixed sleep), and assert the final `normalized.customers` state reflects BOTH publishes' rows with no `UniqueViolation`/`cardinality_violation` raised on either side. `test_lineage_columns_populated`: after one publish, `SELECT _run_id, _file_id, _batch_id, _source_row_number, _record_hash, _record_hash_version FROM normalized.customers WHERE customer_id = %s` returns non-NULL values matching exactly what was staged — answerable "which file, which batch, which run" by SQL alone, per the phase's own success criterion 4. `test_duplicate_batch_key_rejected`: `metadata.create_batch(dataset_id=X, batch_key="same-key", status="OPEN")` followed by a SECOND `create_batch` call with the identical `(dataset_id, batch_key)` raises (catch the underlying `psycopg.errors.UniqueViolation` or whatever `PostgresMetadataRepository` currently lets propagate — confirm which, since `create_batch`'s current implementation does a plain `INSERT ... RETURNING` per 04-01's untouched Protocol, not an upsert) — proves `uq_batches_dataset_batch_key` (migration 0003) is a real, enforced constraint, not decorative.
  </action>
  <acceptance_criteria>
    - All four tests pass against real testcontainers Postgres
    - `test_advisory_lock_serializes_concurrent_publishers` fails (not passes) if `pg_advisory_xact_lock` is removed from `merge.py`'s call path — confirm this negative case once during development (temporarily comment out the lock call, observe the test either flakes or raises a constraint violation, then restore it), so the test is proven to actually test the property it claims to
  </acceptance_criteria>
  <verify>
    <automated>uv run --group cluster pytest tests/integration/test_publish_merge.py -x -q</automated>
  </verify>
  <done>META-03's one-transaction claim, LOAD-09's single-writer claim, LOAD-04's lineage-queryability claim, and LOAD-08's batch-uniqueness claim are all proven against a real Postgres, not merely asserted in a docstring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only direct SQL seeding | These tests seed staging tables via direct `psycopg` SQL (no repository method exists for it), which is a test-harness-only pattern already established by `test_metadata_repository.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-04-01 | Tampering | Test-seeding SQL in both new/extended test files | mitigate | Every seeded value crosses via `%s` placeholders, matching the production code's own discipline — a test that string-interpolates SQL would set a bad precedent for anyone copying the test later |
| T-04-11 | Tampering | Concurrent publish races (verification, not new production code) | mitigate | This plan's `test_advisory_lock_serializes_concurrent_publishers` is the actual evidence that 04-04's mitigation for T-04-11 holds under real concurrency, not merely under a single-threaded test |

</threat_model>

<verification>
`uv run --group cluster pytest tests/integration/test_discover_files.py tests/integration/test_publish_merge.py -x -q` passes in full.
</verification>

<success_criteria>
Every correctness claim this phase's discovery and publication layers make is independently verified against a real database and object store, with a negative-case check proving the concurrency test is not vacuous.
</success_criteria>

<output>
Create `.planning/phases/04-vertical-slice-csv-to-analytical-postgresql/04-06-SUMMARY.md` when done
</output>
