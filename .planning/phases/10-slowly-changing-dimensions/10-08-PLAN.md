---
phase: 10-slowly-changing-dimensions
plan: 08
type: execute
wave: 5
depends_on: ["10-07"]
files_modified:
  - tests/e2e/slice/test_backfill_2year_sweep.py
autonomous: true
requirements: [SCD-09, SCD-10]

must_haves:
  truths:
    - "A live attribute change and a live backfill/correction targeting the SAME customer_id, running concurrently against the real cluster, never corrupt normalized.customers -- no overlapping validity ranges, no more than one is_current row per key"
    - "The exclusion constraint never actually fires during this test (a backstop, not the primary mechanism) -- if it fires, that is treated as a bug, not proof the constraint 'worked'"
    - "The final state is deterministic regardless of which of the two concurrent writers commits first"
  artifacts: []
  key_links: []
---

<objective>
Implement D-10's own explicitly-required, dedicated concurrency test: two genuinely concurrent
writers to `normalized.customers` -- a live attribute change and a backfill/correction -- racing
for the SAME `customer_id`. D-10 explicitly rejected relying only on Phase 9's inherited
`pg_advisory_xact_lock` concurrency proof, since the SCD Publisher's exclusion-constraint-plus-
recompute logic is genuinely new code, not a reuse of `MergePublisher`'s upsert path.

Purpose: prove live, under real concurrent Kubernetes pods and real PostgreSQL locking, that
Pattern 4's serialization discipline (the SAME advisory lock LOAD-09 already established) holds
for the SCD Publisher's DELETE+INSERT-per-key write path, not just for MergePublisher's
INSERT...ON CONFLICT path Phase 9 already proved.
Output: a new, standalone test function in test_backfill_2year_sweep.py, proven live against the
real kind cluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md
@.planning/phases/10-slowly-changing-dimensions/10-07-PLAN.md

<interfaces>
From tests/e2e/slice/test_backfill_2year_sweep.py's `test_live_run_concurrent_with_backfill_
same_dataset` (D-12/D-13, Phase 9's own closest precedent, read in full during planning): the
orchestration shape to reuse -- trigger a fresh backfill window via `airflow backfill create`,
while the DAG's own live `*/1 * * * *` schedule keeps producing ordinary `backfill_id IS NULL`
DagRuns, then poll `dag_run` for a genuinely CONCURRENT pair (one with `backfill_id IS NOT NULL`,
one with `backfill_id IS NULL`, both in a `running` state at the same observed moment) before
asserting no corruption once both finish. `_wait_for_new_dag_run_terminal`,
`_latest_dag_run_pk`, `_invoke_backfill_create`, `_window` are all existing helpers this plan
reuses unchanged.

From plan 10-07's Task 2: the corrected corruption-check queries (no duplicate is_current row
per key, no overlapping validity ranges) -- this plan's own assertions reuse the SAME query
shapes, applied specifically to the ONE customer_id this test deliberately targets with both
writers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the dedicated same-key concurrency test</name>
  <files>tests/e2e/slice/test_backfill_2year_sweep.py</files>
  <read_first>
    tests/e2e/slice/test_backfill_2year_sweep.py (test_live_run_concurrent_with_backfill_same_
    dataset in full, and its module-level helper functions -- already read during planning)
  </read_first>
  <action>
    Add a new test function, `test_scd_concurrent_attribute_change_and_correction_same_key`,
    reusing `test_live_run_concurrent_with_backfill_same_dataset`'s orchestration shape but
    targeting exactly ONE specific `customer_id` (pick one already present in the sweep corpus's
    roster from plan 10-06/10-07, index `>= 30` to avoid the orders fixture pool) with TWO
    deliberately conflicting deliveries: (a) a small, freshly-uploaded "live" CSV file containing
    a NEW attribute value for that customer_id, dated with `event_ts` AFTER every version already
    published for that key (a genuine forward attribute change); (b) a small, freshly-uploaded
    "backfill/correction" CSV file for the SAME customer_id, dated with an `event_ts` BETWEEN two
    of that key's already-existing version boundaries (a genuine late correction, same shape as
    plan 10-07's late-correction fixture but delivered via a SEPARATE file so its ingestion is a
    genuinely independent DagRun/pod, not folded into the same daily file). Upload both, trigger
    one via the DAG's live schedule and the other via a triggered backfill window (mirroring the
    precedent test's own trigger-shape split), poll until BOTH DagRuns are observed
    simultaneously `running` at least once (reusing the precedent test's own polling helpers), then
    wait for both to reach a terminal state. Assert: (1) neither DagRun failed; (2) exactly ONE
    `is_current = true` row exists for the targeted customer_id; (3) no two version rows for that
    customer_id have overlapping `validity` ranges (direct self-join query, same shape as plan
    10-07's Task 2 rewrite); (4) the FULL version chain for that key, read back and independently
    fed through `recompute_version_chain` (plan 10-02) against the SAME key's
    `staging.customers` history, matches exactly what is actually stored -- proving the live
    result is not just "non-corrupted" but the SAME deterministic answer the pure recompute
    function would produce, regardless of which writer's transaction committed first; (5) a
    direct query for `psycopg.errors.ExclusionViolation`-class failures anywhere in
    `meta.ingestion_runs`/task logs for either DagRun -- assert NEITHER DagRun's failure reason
    (if any -- there should be none) is an exclusion-constraint violation, since a live-observed
    violation would mean Pattern 2's recompute math or the advisory-lock discipline has a real
    bug, not that "the constraint worked as a backstop."
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k scd_concurrent</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k scd_concurrent`
      passes against the real kind cluster
    - Both DagRuns are independently confirmed to have overlapped in a `running` state at least
      once during the test (not merely sequential)
    - The final live state for the targeted customer_id exactly matches
      `recompute_version_chain`'s own pure-function output for the same bronze history
    - No exclusion-constraint violation is observed in either DagRun's outcome
  </acceptance_criteria>
  <done>D-10's dedicated concurrency test is implemented and passes live: a genuine attribute change and a genuine late correction for the SAME customer_id, running concurrently, produce a correct, deterministic, non-corrupted SCD2 version chain.</done>
</task>

<task type="auto">
  <name>Task 2: Full-module regression pass</name>
  <files>tests/e2e/slice/test_backfill_2year_sweep.py</files>
  <read_first>
    tests/e2e/slice/test_backfill_2year_sweep.py (no new reading needed beyond Task 1 -- this
    task runs the whole module as a final regression gate)
  </read_first>
  <action>
    Run the entire test_backfill_2year_sweep.py module against the live cluster (every test, not
    a `-k` filter) to confirm Task 1's new test coexists correctly with every earlier Phase 9 and
    Phase 10 test in the same module -- in particular that the new test's own file uploads and
    DagRun triggers do not collide with `_SweepState`'s module-scoped mutable state or with the
    customer_id ranges any other test in this file already claims. Fix any collision found (e.g.
    by adjusting Task 1's chosen customer_id index or day-index choices) rather than suppressing
    or skipping the conflicting test.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster` passes in full, every
      test in the module, with no skips introduced by this plan
  </acceptance_criteria>
  <done>The complete phase-spanning 2-year sweep test module passes end to end against the live cluster, including Phase 9's original 7-assertion proof, Phase 10's version-boundary/late-correction/DELETE-semantics/idempotent-rerun assertions, and this plan's dedicated same-key concurrency test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Two concurrent Kubernetes pods writing to normalized.customers | The exact boundary D-10 requires a dedicated live proof for -- new code path (SCDPublisher), previously only proven for MergePublisher's different write shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-03 | Tampering / DoS | Race between concurrent writers producing a corrupted/partial version chain | mitigate | This plan IS the live proof: pg_advisory_xact_lock('publish:normalized.customers') (unchanged LOAD-09 key) serializes both writers; the exclusion constraint (plan 10-01) is the backstop, confirmed here to never actually need to fire |
</threat_model>

<verification>
- pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster passes in full (whole module)
</verification>

<success_criteria>
D-10's dedicated concurrency test proves live that the SCD Publisher's DELETE+INSERT-per-key write path serializes correctly under real concurrent writers targeting the same business key, producing a deterministic, non-corrupted result regardless of commit order -- closing the phase's final open requirement.
</success_criteria>

<output>
Create .planning/phases/10-slowly-changing-dimensions/10-08-SUMMARY.md when done
</output>
