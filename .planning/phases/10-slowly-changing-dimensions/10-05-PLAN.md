---
phase: 10-slowly-changing-dimensions
plan: 05
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - migrations/versions/0036_v_customers_lineage_is_current.py
  - packages/dataplat/src/dataplat/pipeline/run.py
  - packages/dataplat/src/dataplat/metadata/repository.py
  - tests/integration/test_reconciliation.py
  - tests/integration/test_run_ingest.py
autonomous: true
requirements: [SCD-03]

must_haves:
  truths:
    - "meta.v_customers_lineage returns at most one row per customer_id (the current version), not one row per SCD2 version"
    - "The silver-gold reconciliation figures for customers correctly account for multiple gold rows existing per customer_id, without silently redefining an existing named field's meaning"
  artifacts:
    - path: "migrations/versions/0036_v_customers_lineage_is_current.py"
      provides: "meta.v_customers_lineage redefined with an is_current filter"
      contains: "is_current"
  key_links:
    - from: "packages/dataplat/src/dataplat/pipeline/run.py"
      to: "packages/dataplat/src/dataplat/metadata/repository.py"
      via: "_compute_silver_gold_reconciliation's new key_count-based customer accounting flows into record_reconciliation"
      pattern: "key_count_output"
---

<objective>
Fix D-08's two named production consumers of `normalized.customers`'s formerly-true "one row per
customer_id" assumption: `meta.v_customers_lineage` (migrations 0012/0026/0030's join chain) and
`_compute_silver_gold_reconciliation`'s (`pipeline/run.py`) row-count-based accounting. Both are
developed and tested independently of the SCD Publisher's own existence (different files, no
overlap with plan 10-04) but only become behaviorally meaningful once SCDPublisher actually
creates multi-row-per-key gold state.

Purpose: close D-08's two named consumer gaps before any live e2e proof runs against real
multi-version data, so `meta.v_customers_lineage` and reconciliation reporting are correct from
the first real SCD2 publish onward, not discovered broken during the Wave-4/5 live proof.
Output: migration 0036 (view fix); pipeline/run.py's reconciliation logic distinguishes total
row count from distinct-current-customer count without silently redefining `output_count`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md
@.planning/phases/10-slowly-changing-dimensions/10-01-PLAN.md

<interfaces>
From migrations/versions/0030_fix_v_customers_lineage_dedup_audit_model_name.py (full file, read
during planning) -- the exact "drop + recreate the FULL view text verbatim except one changed
clause" convention, with `_FIXED_CREATE_VIEW`/`_BUGGY_CREATE_VIEW` module constants and
`downgrade()` restoring the prior migration's exact view text.

From packages/dataplat/src/dataplat/pipeline/run.py's `_compute_silver_gold_reconciliation`
(lines ~293-397, read in full during planning): `_ReconciliationAggregates` is a frozen dataclass
with `output_count: int` (currently `count(*) FROM target_table`, unfiltered) and
`key_count_output: int | None` (already `count(DISTINCT customer_id) FROM target_table` --
already correct as-is for multi-row-per-key data, needs no change). `record_reconciliation`
(metadata/repository.py, ~line 990) writes both fields verbatim into
meta.reconciliation_results; its own docstring documents the exact INSERT shape including the
D-22 discrepancy formula computed AS A SQL EXPRESSION at write time.

From tests/integration/test_reconciliation.py (lines ~420-441, quoted verbatim in PATTERNS.md,
read during planning):
```python
assert row["input_count"] == _table_row_count(env.migrated_dsn, "silver.customers")
assert row["output_count"] == _table_row_count(env.migrated_dsn, "normalized.customers")
assert row["discrepancy"] == row["input_count"] - (row["output_count"] + row["dedup_count"])
```
This is the exact assertion this plan's Task 2 must rework.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 0036 -- meta.v_customers_lineage gains an is_current filter</name>
  <files>migrations/versions/0036_v_customers_lineage_is_current.py</files>
  <read_first>
    migrations/versions/0030_fix_v_customers_lineage_dedup_audit_model_name.py (full file --
    direct structural template, most recent link in the view's edit chain)
  </read_first>
  <action>
    Create migration 0036, `down_revision = "0035"` (chains directly onto plan 10-01's migration --
    verify 0035 is genuinely the head before writing this). Copy migration 0030's
    `_FIXED_CREATE_VIEW` text verbatim into a new `_PRIOR_CREATE_VIEW` constant (this migration's
    own "buggy," i.e. pre-this-change, view text, for a genuine `downgrade()`), then produce a new
    `_CURRENT_CREATE_VIEW` constant identical to it except: add `AND c.is_current` to the base
    `FROM normalized.customers c` clause's effective filter (either as a `WHERE c.is_current`
    clause or folded into an existing join condition -- whichever reads more naturally against
    the view's current structure) so the view returns at most one row per `customer_id` (the
    current SCD2 version) instead of one row per version. Every other column and join stays
    copied verbatim from migration 0030's text -- do not restructure anything else.
    `upgrade()`/`downgrade()` follow migration 0030's exact `DROP VIEW` + `CREATE VIEW` +
    re-`GRANT SELECT ... TO etl_app` + re-`GRANT SELECT ... TO grafana_reader` shape;
    `downgrade()` restores `_PRIOR_CREATE_VIEW` verbatim, a genuine working reversal (matching
    0030's own convention of restoring 0026's view bug-for-bug on downgrade).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && make migrate-analytics 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` applies migration 0036 with no error
    - A live query `SELECT customer_id, count(*) FROM meta.v_customers_lineage GROUP BY
      customer_id HAVING count(*) > 1` returns zero rows even after a customer_id has multiple
      SCD2 versions in normalized.customers
    - `alembic downgrade -1` then `alembic upgrade head` both succeed
    - `\dp meta.v_customers_lineage` still shows exactly `etl_app`/`grafana_reader` with SELECT,
      nothing broader
  </acceptance_criteria>
  <done>meta.v_customers_lineage returns exactly the current SCD2 version per customer_id, migration round-trips cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: silver-gold reconciliation -- multi-row-per-key-aware customer accounting</name>
  <files>
    packages/dataplat/src/dataplat/pipeline/run.py,
    packages/dataplat/src/dataplat/metadata/repository.py,
    tests/integration/test_reconciliation.py,
    tests/integration/test_run_ingest.py
  </files>
  <read_first>
    packages/dataplat/src/dataplat/pipeline/run.py (lines 215-397, `_ReconciliationAggregates`
    and `_compute_silver_gold_reconciliation` -- already read in full during planning),
    packages/dataplat/src/dataplat/metadata/repository.py (`record_reconciliation`,
    lines ~990-1090 -- already read in full during planning),
    tests/integration/test_reconciliation.py (lines ~400-445, the exact assertions this task
    reworks),
    tests/integration/test_run_ingest.py (lines ~40-50, the "customer_id is unique across runs"
    comment this task must correct)
  </read_first>
  <action>
    In `pipeline/run.py`'s `_compute_silver_gold_reconciliation`: `output_count` (currently plain
    `count(*) FROM target_table`) keeps its literal meaning ("total rows in the target table") --
    do NOT silently redefine what an existing named field means (this task's own guiding rule,
    matching PATTERNS.md's explicit warning). Instead, make the reconciliation math that COMPARES
    `input_count` to the target side use `key_count_output` (already computed, already
    `count(DISTINCT customer_id)`, already correct for multi-row-per-key gold) for any comparison
    whose intent is "does the target hold the same set of customers as the source," while
    `output_count` stays available for its own literal, now-larger-for-customers meaning ("total
    physical rows, including historical SCD2 versions"). Update `_ReconciliationAggregates`'s and
    `record_reconciliation`'s docstrings to state this distinction explicitly for
    `normalized.customers` (a dataset whose `output_count` can legitimately exceed
    `key_count_output`) so a future reader does not reintroduce the same assumption this plan is
    fixing. In `test_reconciliation.py`: rework the 3-line assertion block quoted in this plan's
    context section -- change `row["output_count"] == _table_row_count(env.migrated_dsn,
    "normalized.customers")` to explicitly state and assert BOTH: (a) `output_count` is still the
    literal total row count (unchanged formula, still passes for `orders`, and still passes for
    `customers` in a fixture with no SCD2 versioning yet), and (b) `key_count_output` equals
    `count(DISTINCT customer_id)` and reflects the distinct-customer view. Add a NEW,
    customers-specific test seeding at least 2 SCD2 versions for one customer_id and asserting
    `output_count > key_count_output` for that dataset (the genuinely new behavior this task
    introduces), while `discrepancy`'s own formula (`input_count - (output_count + rejected_count
    + dedup_count)`) is documented as no longer meaningfully "zero" for a multi-versioned
    customers dataset -- assert the new customers-specific test does NOT assert `discrepancy ==
    0` the way the orders-oriented precedent does, and instead asserts against
    `key_count_output` for the "does source match target's customer set" question. In
    `test_run_ingest.py`: update the literal comment/assertion (lines ~40-50) that states
    `normalized.customers.customer_id` is unique across runs -- correct it to describe the real,
    current invariant (`customer_id` identifies a version CHAIN, not a single row; uniqueness now
    holds for `(customer_id) WHERE is_current`, matching migration 0035's own partial index) --
    preserve the file's documented customer_id-range partitioning discipline
    (`test_publish_merge.py`'s old 2000/3001/4001/5001 ranges are now free since that file is
    deleted by plan 10-04 -- note this in the comment but do not reuse those exact ranges without
    checking plan 10-04's SUMMARY for what it actually used).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/integration/test_reconciliation.py tests/integration/test_run_ingest.py -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_reconciliation.py tests/integration/test_run_ingest.py -q -m
      integration` passes in full
    - `output_count`'s docstring and behavior are UNCHANGED in meaning (still a literal
      `count(*)`) -- no silent redefinition of an existing named field
    - A new test proves `output_count > key_count_output` is the CORRECT, expected shape for a
      customers dataset with more than one SCD2 version published
    - `test_run_ingest.py`'s comment no longer claims `customer_id` is unique across runs without
      qualification
  </acceptance_criteria>
  <done>Reconciliation reporting correctly distinguishes total-row-count from distinct-customer-count for a multi-row-per-key SCD2 dimension, without redefining any existing field's meaning; test_run_ingest.py's documented invariant matches the real, current one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| meta.v_customers_lineage / meta.reconciliation_results -> grafana_reader, etl_app | Read-only reporting surfaces; this plan changes their SELECT logic, not their grant scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-10 | Information Disclosure | meta.v_customers_lineage exposing stale/historical SCD2 versions to grafana_reader | mitigate | The is_current filter this migration adds ensures the view only ever surfaces the current version per customer_id, matching the view's own original single-row-per-customer intent -- historical versions remain queryable directly against normalized.customers by etl_app, never through this reporting view |
| T-10-01 | Tampering | migration 0036's view-redefinition SQL text | mitigate | Every identifier is literal and hand-written, copied verbatim from migration 0030 except the one documented change (is_current filter) -- no dynamic interpolation |
</threat_model>

<verification>
- pytest tests/integration/test_reconciliation.py tests/integration/test_run_ingest.py -q -m integration passes in full
- alembic upgrade head applies both 0035 (dependency) and 0036 cleanly, in order
- pytest tests/unit -q passes (no regression)
</verification>

<success_criteria>
meta.v_customers_lineage and silver-gold reconciliation reporting are both correct under multi-row-per-key SCD2 gold state, with output_count's existing meaning preserved (never silently redefined) and a new, explicit distinction drawn between total-row-count and distinct-current-customer-count.
</success_criteria>

<output>
Create .planning/phases/10-slowly-changing-dimensions/10-05-SUMMARY.md when done
</output>
