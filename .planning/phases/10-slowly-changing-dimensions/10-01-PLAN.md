---
phase: 10-slowly-changing-dimensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/0035_normalized_customers_scd2.py
  - packages/dataplat/src/dataplat/config/model.py
  - configs/datasets/customers.yaml
  - packages/dataplat/src/dataplat/load/publish/protocol.py
  - packages/dataplat/src/dataplat/load/publish/merge.py
  - packages/dataplat/src/dataplat/load/publish/merge_orders.py
  - packages/dataplat/src/dataplat/pipeline/run.py
  - tests/integration/test_migrations.py
autonomous: true
requirements: [SCD-03, SCD-12]

must_haves:
  truths:
    - "normalized.customers can hold more than one row per customer_id, and PostgreSQL itself rejects two rows for the same customer_id whose validity ranges overlap"
    - "Every SCD2 dimension column (valid_to/is_current/validity/signup_country) exists on normalized.customers with correct types and defaults, and existing rows backfill as valid first versions"
    - "etl_app can DELETE from normalized.customers (scoped to exactly this table) and every Publisher.publish() call site can pass staged_run_ids through to the Publisher"
  artifacts:
    - path: "migrations/versions/0035_normalized_customers_scd2.py"
      provides: "btree_gist extension, dropped UNIQUE(customer_id), added valid_to/is_current/validity/signup_country columns, EXCLUDE USING gist constraint, GRANT DELETE"
    - path: "packages/dataplat/src/dataplat/config/model.py"
      provides: "ColumnContract.scd_type (Type 0/1/2 dispatch) and ScdConfig (delete_semantics/mass_delete_threshold)"
      contains: "scd_type"
    - path: "configs/datasets/customers.yaml"
      provides: "signup_country column (D-13), scd_type tags on every business column, scd: block"
      contains: "signup_country"
  key_links:
    - from: "packages/dataplat/src/dataplat/load/publish/protocol.py"
      to: "packages/dataplat/src/dataplat/pipeline/run.py"
      via: "Publisher.publish() gains staged_run_ids, publish_ingest computes it before calling publisher.publish()"
      pattern: "staged_run_ids"
---

<objective>
Lay the DDL, config-schema and protocol foundation every later Phase 10 plan builds on: migrate
`normalized.customers` in place to an SCD2 shape (D-07), extend `DatasetConfig`/`ColumnContract`
so Type-0/1/2 column treatment and DELETE semantics are config-not-code (D-02/D-05/D-06/D-13), and
extend the `Publisher` protocol so a Publisher can see which runs this publish pass is finalizing
(`staged_run_ids`) -- required by Finding F-2's run-scoped DELETE-detection in a later plan.

Purpose: every downstream Phase 10 plan (recompute logic, delete-detection, the SCD Publisher
itself, the corpus fixtures) needs this schema/config/protocol shape to already exist and be
stable -- this is the "define contracts first" wave.
Output: migration 0035 applied; ColumnContract.scd_type/ScdConfig in config/model.py;
customers.yaml carries signup_country + per-column scd_type + a scd: config block;
Publisher.publish() accepts staged_run_ids everywhere it is implemented or called.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md

<interfaces>
From migrations/versions/0005_normalized_customers.py and 0006 (read in full during planning):
customer_id is sa.Integer(); name/country/birth_date/event_ts are nullable=True at DB level (the
app contract requires them via customers.yaml, not DDL); the id surrogate key is
sa.BigInteger() + sa.Identity(always=True) (D-09, unchanged by this plan); embedded lineage
columns (_run_id, _file_id, _batch_id, _source_row_number, _record_hash, _record_hash_version,
_ingested_at) are unchanged.

From packages/dataplat/src/dataplat/load/publish/protocol.py (read in full during planning) --
the Publisher Protocol's publish() signature is
`publish(self, ctx: PipelineContext, source_table: str, conn: Connection[Any]) -> PublishResult`.

From packages/dataplat/src/dataplat/pipeline/run.py -- publish_ingest is the ONLY call site.
`staged = ctx.metadata.list_staged_run_ids(dataset_id=dataset_id)` runs near the top of the
function (before the transaction even opens); `staged_run_ids = [run_id for run_id, _, _, _ in
staged]` is currently computed AFTER `publisher.publish(ctx, source_table, conn)` runs -- this
plan moves that list-comprehension earlier so it can be passed INTO publish() and reused
unchanged at its current call site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0035 -- normalized.customers in-place SCD2 migration</name>
  <files>migrations/versions/0035_normalized_customers_scd2.py</files>
  <read_first>
    migrations/versions/0005_normalized_customers.py,
    migrations/versions/0006_normalized_customers_business_key_unique.py,
    migrations/versions/0022_staging_customers_orders_durable_tables.py,
    migrations/versions/0030_fix_v_customers_lineage_dedup_audit_model_name.py,
    .planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md (Pattern 1, lines 187-293)
  </read_first>
  <action>
    Create migration 0035, down_revision = "0034" (the current head -- verify no newer migration
    landed since planning by listing migrations/versions/ before writing down_revision). Follow
    RESEARCH.md Pattern 1's worked draft as the starting point but treat every SQL statement as
    needing live verification against Alembic 1.19.1 / PostgreSQL 18 (Assumption A1: verify
    op.create_exclude_constraint(..., using="gist")'s kwarg spelling actually works -- if it
    raises, drop the using= kwarg since PostgreSQL defaults EXCLUDE to GiST). In upgrade(): (a)
    op.execute("CREATE EXTENSION IF NOT EXISTS btree_gist") -- this repo's first CREATE EXTENSION
    (grep migrations/ confirms zero prior matches); (b) op.drop_constraint on
    "uq_customers_customer_id" (table "customers", schema "normalized", type_="unique") -- undoes
    migration 0006, since ON CONFLICT (customer_id) can no longer be this table's write path once
    more than one row per key is legal (D-07); (c) op.alter_column "event_ts" nullable=False --
    a NULL event_ts would produce an unbounded-lower tstzrange and silently defeat the exclusion
    constraint; (d) add column valid_to (sa.DateTime(timezone=True), nullable=False,
    server_default sa.text("'9999-12-31 00:00:00+00'::timestamptz") -- dbt's own documented
    dbt_valid_to_current example value) and is_current (sa.Boolean(), nullable=False,
    server_default sa.text("true")) -- the server_defaults backfill every EXISTING row as a valid
    first SCD2 version with zero extra UPDATE (D-07's "backfilled as each customer's first SCD2
    version"); (e) add column signup_country (sa.Text(), nullable=True) -- D-13's new Type-0
    column; (f) op.execute a raw ALTER TABLE adding a generated STORED column `validity tstzrange
    GENERATED ALWAYS AS (tstzrange(event_ts, valid_to, '[)')) STORED`; (g)
    op.create_exclude_constraint("excl_customers_business_key_validity", "customers",
    ("customer_id", "="), ("validity", "&&"), schema="normalized", using="gist") -- SCD-12's own
    constraint; (h) op.create_index "ix_customers_is_current" on ["customer_id"], unique=False,
    postgresql_where=sa.text("is_current"); (i) op.execute "GRANT DELETE ON normalized.customers
    TO etl_app" -- verify via a live `\dp normalized.customers` check (or the migration's own
    apply during this task's verify step) that etl_app does NOT already have DELETE before
    assuming this grant is new (Security Domain V4, T-10-02 in this plan's threat model). Write a
    genuine, verified downgrade(): drop the is_current index, drop the exclusion constraint, drop
    validity, drop is_current/valid_to/signup_country, restore event_ts nullable, restore
    uq_customers_customer_id. Docstring must cite D-07, SCD-12 and migration 0006 by number,
    matching this repo's own migration-docstring convention (see 0006/0030's own docstrings).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && make migrate-analytics 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` (via make migrate-analytics) applies migration 0035 with no error
    - A `pg_constraint` query against normalized.customers shows a constraint of type 'x'
      (EXCLUDE) named excl_customers_business_key_validity, and no constraint named
      uq_customers_customer_id
    - normalized.customers has columns valid_to, is_current, validity, signup_country
    - `\dp normalized.customers` (or an equivalent aclexplode query) shows etl_app with DELETE
      privilege
    - `alembic downgrade -1` followed by `alembic upgrade head` both succeed with no error
  </acceptance_criteria>
  <done>Migration 0035 applies cleanly forward and reverses cleanly; the exclusion constraint, new columns and DELETE grant all exist on the live analytical database.</done>
</task>

<task type="auto">
  <name>Task 2: Config schema -- ColumnContract.scd_type, ScdConfig, customers.yaml additions</name>
  <files>packages/dataplat/src/dataplat/config/model.py, configs/datasets/customers.yaml</files>
  <read_first>
    packages/dataplat/src/dataplat/config/model.py (ColumnContract, DatasetConfig,
    ReconciliationConfig sections -- already read in full during planning; match their exact
    ConfigDict/docstring style),
    configs/datasets/customers.yaml (full file)
  </read_first>
  <action>
    In config/model.py: add a module-level `_SCD_TYPES = Literal["type_0", "type_1", "type_2"]`
    next to `_COLUMN_TYPES` (same closed-Literal convention, same reasoning: no dispatch registry
    exists for this field). Add `scd_type: _SCD_TYPES | None = None` to ColumnContract (default
    None for a column that is a business key, the effective-date source, or otherwise not
    SCD-tracked -- customer_id and event_ts in customers.yaml both stay None). Document in the
    docstring: type_0 = "value from the earliest bronze row for this business key is kept
    forever; any later incoming value is silently ignored (SCD-01)"; type_1 = "value from the
    latest bronze row for this business key overwrites every version row, no history kept
    (SCD-02)"; type_2 = "a normalized-hash-detected change starts a new validity-scoped version
    (SCD-03/SCD-05)". Add a new ScdConfig(BaseModel) class (same
    ConfigDict(extra="forbid", frozen=True) shape as ReconciliationConfig) with
    `delete_semantics: Literal["ignore", "invalidate", "new_record"]` (SCD-08's closed
    vocabulary -- a real Literal, not the plain-str registry convention, since no Python dispatch
    registry exists for it, matching ColumnContract.type's own precedent) and
    `mass_delete_threshold: float` (D-06). Add `scd: ScdConfig | None = None` to DatasetConfig
    (opt-in, same precedent as reconciliation/quality). In customers.yaml: add a new columns:
    entry `signup_country` with `type: string`, `nullable: true`, `required: false`,
    `scd_type: type_0` (D-13's new column -- required: false since older files never carried it,
    and D-13's own semantics is "accept on first insert, ignore later," not "reject files missing
    it"); add `scd_type: type_2` to the existing `name` and `country` entries; add
    `scd_type: type_1` to the existing `birth_date` entry; leave `customer_id` and `event_ts`
    with no scd_type key (defaults to None). Add a new top-level `scd:` block with
    `delete_semantics: invalidate` (D-05) and `mass_delete_threshold: 0.10` (D-06, Phase 8's 10%
    starting reference value). Do NOT change `load.strategy` in this task -- it stays `merge`
    until plan 10-04 registers and wires the real SCDPublisher.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && python -c "from dataplat.config.loader import load_config; c = load_config('configs/datasets/customers.yaml'); print([(col.name, col.scd_type) for col in c.columns]); print(c.scd)"</automated>
  </verify>
  <acceptance_criteria>
    - `load_config` on customers.yaml succeeds and returns a DatasetConfig whose `.scd` is not
      None, with `delete_semantics == "invalidate"` and `mass_delete_threshold == 0.10`
    - The columns list contains a `signup_country` entry with `scd_type == "type_0"`
    - `name`/`country` columns report `scd_type == "type_2"`; `birth_date` reports
      `scd_type == "type_1"`; `customer_id`/`event_ts` report `scd_type is None`
    - A config document setting an unknown `scd_type` value (e.g. `"type_3"`) fails Pydantic
      validation with a clear error
  </acceptance_criteria>
  <done>customers.yaml validates cleanly against the extended DatasetConfig model, carrying signup_country, per-column scd_type tags, and the scd: block.</done>
</task>

<task type="auto">
  <name>Task 3: Publisher protocol -- staged_run_ids, plus test_migrations.py rewrite for the new constraint shape</name>
  <files>
    packages/dataplat/src/dataplat/load/publish/protocol.py,
    packages/dataplat/src/dataplat/load/publish/merge.py,
    packages/dataplat/src/dataplat/load/publish/merge_orders.py,
    packages/dataplat/src/dataplat/pipeline/run.py,
    tests/integration/test_migrations.py
  </files>
  <read_first>
    packages/dataplat/src/dataplat/load/publish/protocol.py,
    packages/dataplat/src/dataplat/load/publish/merge.py,
    packages/dataplat/src/dataplat/load/publish/merge_orders.py,
    packages/dataplat/src/dataplat/pipeline/run.py (lines 989-1210, publish_ingest),
    tests/integration/test_migrations.py (the tests near lines 273-325 and 720-735, plus the
    dbt_app-permission-denial test near line 631 as the "attempt the forbidden thing, assert the
    specific psycopg error" structural template)
  </read_first>
  <action>
    Extend `Publisher.publish()`'s signature in protocol.py to
    `publish(self, ctx, source_table, conn, *, staged_run_ids)` with
    `staged_run_ids: Sequence[int]` (import `Sequence` from `collections.abc` under
    `TYPE_CHECKING`), documenting in the docstring that this is the exact list of run_ids this
    publish pass is finalizing (computed by the caller BEFORE this call, never re-derived inside
    publish()) -- needed by any Publisher whose DELETE-detection or recompute logic must be
    scoped to "this pass's own files" (Finding F-2). Update MergePublisher.publish() and
    OrdersMergePublisher.publish() to accept `*, staged_run_ids: Sequence[int]` and ignore it
    (mirroring the existing `ctx: PipelineContext,  # noqa: ARG002` unused-parameter convention
    already on MergePublisher.publish()'s own `ctx` argument) -- their SQL text requires zero
    change. In pipeline/run.py's publish_ingest: move the line
    `staged_run_ids = [run_id for run_id, _, _, _ in staged]` to run immediately after `staged`
    is first assigned (near the top of the function, before the `if not staged:` early-return),
    and pass `staged_run_ids=staged_run_ids` into the existing `publisher.publish(ctx,
    source_table, conn, ...)` call; delete the now-duplicate later computation, keeping every
    other use of `staged_run_ids` (record_watermark, the finalize loop) unchanged. In
    test_migrations.py: rewrite `test_0006_customer_id_has_a_real_unique_constraint` and
    `test_0006_downgrade_restores_the_plain_index_and_reupgrade_restores_the_constraint`
    (lines 273-325) so their post-migration-0035 expectation is a constraint of type 'x'
    (EXCLUDE) rather than 'u' (UNIQUE) -- update whatever helper computes
    `_customers_customer_id_constraint_types` accordingly, and split
    `test_gold_indexes_exist_and_business_key_uniqueness_is_unchanged` (line ~720) so its
    `orders` half is unchanged and its `customers` half is replaced. Add a new test
    `test_exclusion_constraint_rejects_overlapping_validity` matching the file's own
    "attempt the forbidden thing, assert the specific psycopg error" style (see the
    dbt_app-permission-denial test near line 631): directly INSERT two rows for the SAME
    customer_id with overlapping validity ranges and assert `psycopg.errors.ExclusionViolation`
    is raised. Also add a small assertion that `signup_country` exists in
    `information_schema.columns` for normalized.customers, and that etl_app's
    `information_schema.role_table_grants` includes `DELETE` on normalized.customers.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/integration/test_migrations.py -q -m integration -k "exclusion or customer_id or gold_indexes"</automated>
  </verify>
  <acceptance_criteria>
    - `Publisher.publish()`'s Protocol signature and both concrete implementations
      (MergePublisher, OrdersMergePublisher) all accept `staged_run_ids` as a keyword-only
      argument
    - `publish_ingest` computes `staged_run_ids` once, before its first use, and passes it into
      `publisher.publish()`
    - `pytest tests/unit -q` still passes (no regression to existing Publisher-consuming unit
      tests)
    - `test_exclusion_constraint_rejects_overlapping_validity` passes against the live migrated
      schema, raising `psycopg.errors.ExclusionViolation` for an overlapping INSERT
    - The two rewritten 0006-era tests assert `("EXCLUDE",)` (or the real `contype` code) instead
      of `("UNIQUE",)`
  </acceptance_criteria>
  <done>Publisher.publish() carries staged_run_ids end-to-end from publish_ingest through every concrete Publisher; test_migrations.py reflects the new EXCLUDE-constraint shape and proves it rejects overlaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Migration DDL -> analytical PostgreSQL | Alembic runs as the CNPG superuser (make migrate-analytics); the GRANT statement this migration adds is the only new runtime-privilege surface |
| etl_app (runtime role) -> normalized.customers | New DELETE capability this migration grants; every future writer (the SCD Publisher, plan 10-04) uses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-01 | Tampering | migration 0035's DDL text | mitigate | Every identifier in this migration (table/column/constraint names) is a literal, hand-written string in the migration source -- never built from row content or external input; matches this repo's migration-authorship convention |
| T-10-02 | Elevation of Privilege | `GRANT DELETE ON normalized.customers TO etl_app` | mitigate | Scoped to exactly this one table (not schema-wide), matching migration 0024's dbt_app INSERT-only narrow-grant convention; verified via a live `\dp` check in Task 1's acceptance criteria, not assumed |
| T-10-03 | Tampering / DoS | Concurrent writers to normalized.customers during/after this migration | accept (mitigated by later plans) | The exclusion constraint this migration adds is the DB-level backstop; the advisory-lock discipline that makes it the backstop (not the primary mechanism) is unchanged LOAD-09 machinery, exercised live by plan 10-08's dedicated concurrency test -- this plan only lays the constraint, it does not yet have a live writer to race |
</threat_model>

<verification>
- `make migrate-analytics` applies cleanly; `alembic downgrade -1 && alembic upgrade head` round-trips
- `python -c "from dataplat.config.loader import load_config; load_config('configs/datasets/customers.yaml')"` succeeds
- `pytest tests/integration/test_migrations.py -q -m integration` passes in full (not just the new/changed tests)
- `pytest tests/unit -q` passes (Publisher protocol change does not break existing unit coverage)
</verification>

<success_criteria>
normalized.customers is migrated in place to an SCD2-capable shape with a live, tested exclusion constraint; customers.yaml and DatasetConfig carry every Type-0/1/2 and DELETE-semantics declaration later plans need; every Publisher implementation and call site agrees on the new staged_run_ids parameter.
</success_criteria>

<output>
Create `.planning/phases/10-slowly-changing-dimensions/10-01-SUMMARY.md` when done
</output>
