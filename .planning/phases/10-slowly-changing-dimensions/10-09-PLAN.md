---
phase: 10-slowly-changing-dimensions
plan: 09
type: execute
wave: 4
depends_on: ["10-04", "10-05"]
files_modified:
  - tests/e2e/slice/test_referential_orphan.py
  - tests/e2e/slice/test_concurrent_select.py
  - tests/e2e/slice/test_backfill_reentry.py
  - tests/e2e/slice/test_smoke_and_idempotency.py
  - tests/e2e/slice/test_pod_kill_retry.py
  - tests/e2e/slice/test_dbt_silver_pipeline.py
autonomous: true
requirements: [SCD-03]

must_haves:
  truths:
    - "Every e2e test file that queries or asserts against normalized.customers's row shape has been individually read and, where it assumed one-row-per-customer_id, fixed to be correct under SCD2 multi-row-per-key state"
    - "No e2e test in this list silently passes (or silently becomes vacuous) after the SCDPublisher goes live -- each was proven to still test something meaningful"
  artifacts: []
  key_links: []
---

<objective>
Close RESEARCH.md Finding F-4's remaining test blast radius: six e2e test files a grep for
`normalized.customers` surfaces beyond the D-08-named production consumers and the four files
already fixed in plans 10-04/10-05, each individually flagged by RESEARCH.md as "needs individual
review, not assumed clean by omission." This plan is a genuine investigation task, not a
known-fix mechanical patch -- RESEARCH.md did not determine which of these six actually break,
only that each must be checked.

Purpose: prevent the SCDPublisher going live (plan 10-04, already merged by this plan's wave)
from silently breaking or silently invalidating tests written under the old one-row-per-key
assumption, anywhere in the e2e suite -- not just the files already known to be affected.
Output: each of the six files reviewed; any cardinality assumption found is fixed; a short note
in this plan's own SUMMARY documenting what was found in each file (including "found clean, no
change needed" as a valid, expected outcome for some of them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md

<interfaces>
RESEARCH.md Finding F-4 (Pitfall 4, read during planning) names these six files as needing
individual review without a known fix: tests/e2e/slice/test_smoke_and_idempotency.py,
test_pod_kill_retry.py, test_referential_orphan.py, test_concurrent_select.py,
test_backfill_reentry.py, test_dbt_silver_pipeline.py. The investigation method RESEARCH.md used
to find the OTHER, already-fixed files (grep for `normalized.customers` and specifically for
`count(*)`/exact-row-count/uniqueness assertions against it) is the same method to apply here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and fix test_referential_orphan.py, test_concurrent_select.py, test_backfill_reentry.py</name>
  <files>tests/e2e/slice/test_referential_orphan.py, tests/e2e/slice/test_concurrent_select.py, tests/e2e/slice/test_backfill_reentry.py</files>
  <read_first>
    tests/e2e/slice/test_referential_orphan.py (full file),
    tests/e2e/slice/test_concurrent_select.py (full file),
    tests/e2e/slice/test_backfill_reentry.py (full file)
  </read_first>
  <action>
    For each of the three files: grep the file for `normalized.customers` and for any
    `count(*)`/`COUNT(*)`, exact-row-count, or business-key-uniqueness assertion touching it.
    Classify each finding as one of: (a) genuinely broken -- an assertion that will now fail
    or become spuriously vacuous once `normalized.customers` legitimately holds more than one row
    per `customer_id` (e.g. asserting `count(*) == N` where N was derived assuming one row per
    published customer); (b) already cardinality-agnostic -- e.g. an `EXISTS`-based check, a
    `count(DISTINCT customer_id)` check, or a check scoped to `is_current` -- needs no change;
    (c) irrelevant -- the file references `normalized.customers` only incidentally (e.g. as a
    referential-integrity TARGET table for `orders`, which stays single-row-per-key-agnostic
    since `ReferentialIntegrityBarrier` is `EXISTS`-based per its own module docstring, verified
    during Phase 10 planning). Fix every (a) finding using the same reasoning already applied in
    plan 10-04's `test_publish_ingest.py` fix and plan 10-05's `test_reconciliation.py` fix
    (distinguish "how many distinct customers" from "how many physical rows," never assume they
    are equal for `customers`). Leave (b)/(c) findings unmodified. `test_referential_orphan.py`
    in particular is likely to fall mostly into (b)/(c) given `ReferentialIntegrityBarrier`'s own
    verified-unaffected status (10-CONTEXT.md's "Verified non-issue" note) -- do not invent a fix
    where none is needed.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_referential_orphan.py tests/e2e/slice/test_concurrent_select.py tests/e2e/slice/test_backfill_reentry.py -q -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files has been read in full and every `normalized.customers`-touching
      assertion classified (a)/(b)/(c) in this plan's own SUMMARY
    - Every (a) finding is fixed and passes live against the real kind cluster
    - No (b)/(c) finding was modified without cause
  </acceptance_criteria>
  <done>All three files are individually reviewed; any genuine cardinality-assumption break is fixed and proven live.</done>
</task>

<task type="auto">
  <name>Task 2: Review and fix test_smoke_and_idempotency.py, test_pod_kill_retry.py, test_dbt_silver_pipeline.py</name>
  <files>tests/e2e/slice/test_smoke_and_idempotency.py, tests/e2e/slice/test_pod_kill_retry.py, tests/e2e/slice/test_dbt_silver_pipeline.py</files>
  <read_first>
    tests/e2e/slice/test_smoke_and_idempotency.py (full file),
    tests/e2e/slice/test_pod_kill_retry.py (full file),
    tests/e2e/slice/test_dbt_silver_pipeline.py (full file)
  </read_first>
  <action>
    Apply the identical investigation method from Task 1 to these three files.
    `test_dbt_silver_pipeline.py` deserves particular attention: it exercises the
    `discover -> stage -> dbt_build -> publish` chain end to end and may assert
    `silver.customers`/`normalized.customers` row counts at multiple hops -- `silver.customers`
    itself is UNCHANGED by this phase (dbt's own dedup/collapse behavior is untouched, per
    RESEARCH.md's explicit "Bronze deduplication... unchanged" architectural-responsibility-map
    entry), so any assertion scoped to `silver.customers` needs no change; only assertions
    reaching through to `normalized.customers`'s own row shape need review. `test_pod_kill_retry.py`
    and `test_smoke_and_idempotency.py` likely assert idempotency via exact row counts after a
    retry/re-run -- verify whether their fixture ever triggers a genuine SCD2 version boundary
    (most smoke/retry fixtures deliberately use UNCHANGING data to isolate the retry mechanism
    from data-content concerns) -- if so, `normalized.customers`'s row count for that fixture's
    customer_ids should still be exactly 1 per key after this phase (SCD-09's idempotent-replay
    guarantee), and the existing assertion may already be correct as written; only change it if a
    live run proves otherwise.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_smoke_and_idempotency.py tests/e2e/slice/test_pod_kill_retry.py tests/e2e/slice/test_dbt_silver_pipeline.py -q -m cluster</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three files has been read in full and every `normalized.customers`-touching
      assertion classified (a)/(b)/(c) in this plan's own SUMMARY
    - Every (a) finding is fixed and passes live against the real kind cluster
    - `test_dbt_silver_pipeline.py`'s `silver.customers`-scoped assertions are confirmed
      unmodified (dbt's own dedup behavior is out of this phase's scope)
  </acceptance_criteria>
  <done>All three remaining files are individually reviewed; any genuine cardinality-assumption break is fixed and proven live; the phase's full test blast radius (RESEARCH.md Finding F-4) is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test suite correctness -> confidence in the live SCDPublisher | This plan's boundary is entirely internal (test code review), no new runtime trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-11 | Repudiation | A silently-vacuous test (passes for the wrong reason after the SCD Publisher goes live) going undetected | mitigate | Every file in this plan is individually read and its normalized.customers-touching assertions classified and documented in the SUMMARY -- "found clean" is only accepted after the classification step, never assumed by omission (this is the direct process fix for RESEARCH.md Finding F-4's own root cause) |
</threat_model>

<verification>
- pytest tests/e2e/slice/test_referential_orphan.py tests/e2e/slice/test_concurrent_select.py tests/e2e/slice/test_backfill_reentry.py tests/e2e/slice/test_smoke_and_idempotency.py tests/e2e/slice/test_pod_kill_retry.py tests/e2e/slice/test_dbt_silver_pipeline.py -q -m cluster passes in full
</verification>

<success_criteria>
All six remaining Finding-F-4 test files are individually reviewed and, where a genuine cardinality assumption existed, fixed and proven live against the real cluster -- closing the full documented test blast radius of the normalized.customers SCD2 migration.
</success_criteria>

<output>
Create .planning/phases/10-slowly-changing-dimensions/10-09-SUMMARY.md when done
</output>
