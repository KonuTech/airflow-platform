---
phase: 10-slowly-changing-dimensions
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - packages/dataplat/src/dataplat/scd/delete_detection.py
  - tests/integration/test_scd_delete_detection.py
autonomous: true
requirements: [SCD-08]

must_haves:
  truths:
    - "A customer_id present in gold's currently-current set but absent from this pass's run-scoped silver snapshot is correctly identified as vanished, and a customer_id merely absent from silver's whole cumulative history (but present in this pass) is never misclassified"
    - "The mass-delete circuit breaker raises QualityThresholdExceeded when the vanished fraction exceeds the dataset's configured threshold, and passes cleanly below it"
    - "Switching customers.yaml's scd.delete_semantics between ignore/invalidate/new_record changes the snapshot-diff function's returned action for a vanished key without changing how vanished keys are detected"
  artifacts:
    - path: "packages/dataplat/src/dataplat/scd/delete_detection.py"
      provides: "run-scoped snapshot-diff query, MassDeleteCircuitBreaker (BarrierStage), delete-semantics dispatch"
      exports: ["MassDeleteCircuitBreaker", "find_vanished_customer_ids"]
  key_links:
    - from: "packages/dataplat/src/dataplat/scd/delete_detection.py"
      to: "packages/dataplat/src/dataplat/validate/circuit_breaker.py"
      via: "MassDeleteCircuitBreaker mirrors RejectionRateCircuitBreaker's constructor-parameterized, apply(ctx)-raises shape"
      pattern: "QualityThresholdExceeded"
---

<objective>
Implement Finding F-2's run-scoped DELETE-detection snapshot diff and D-06's mass-delete circuit
breaker as an independent, DB-testable unit -- the piece of the SCD Publisher that answers "which
currently-current customers are missing from THIS pass's file(s)" and "is that fraction plausible
or does it look like a truncated/bad file." Built and proven here, against real PostgreSQL
(testcontainers), before plan 10-04 assembles the full SCDPublisher around it.

Purpose: isolate the one query RESEARCH.md flagged as easy to get subtly wrong (an unscoped
`silver.customers` read makes DELETE-detection permanently vacuous, since silver never drops a
business key it stops seeing) into its own small, directly-testable module.
Output: dataplat.scd.delete_detection.find_vanished_customer_ids (the run-scoped anti-join) and
MassDeleteCircuitBreaker (a BarrierStage), both integration-tested against real PostgreSQL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md
@.planning/phases/10-slowly-changing-dimensions/10-01-PLAN.md

<interfaces>
From packages/dataplat/src/dataplat/validate/circuit_breaker.py (RejectionRateCircuitBreaker,
full file, read during planning) -- the exact shape to mirror: a BarrierStage whose constructor
takes already-known totals (never re-derives them from ctx), whose apply(ctx) returns a
StageResult with a placeholder RecordChunk plus a single ValidationResult finding on PASS, and
raises QualityThresholdExceeded (dataplat.errors) with a context dict on breach. `if
self._current_count == 0: return <trivial PASS>` is the existing empty-input guard to mirror for
"gold has no current customers yet."

From packages/dataplat/src/dataplat/validate/referential.py (ReferentialIntegrityBarrier, full
file, read during planning) -- the anti-join SQL shape and identifier-interpolation discipline
(T-04-01/T-08-11 convention: only config/run-derived identifiers are ever interpolated, never row
content).

From packages/dataplat/src/dataplat/metadata/repository.py record_watermark's own docstring
(lines ~908-928, read during planning) -- the precedent for run-scoping:
`WHERE _run_id = ANY(%(run_ids)s)` against silver.<dataset>, exactly the scoping this plan's
snapshot-diff query must use.

From plan 10-01: Publisher.publish() now receives `staged_run_ids: Sequence[int]` -- this plan's
functions accept `staged_run_ids` as a plain argument (not by re-deriving it), since the caller
(plan 10-04's SCDPublisher.publish()) already has it.

From plan 10-01: ctx.config.scd is `ScdConfig | None` with `.delete_semantics` (one of "ignore",
"invalidate", "new_record") and `.mass_delete_threshold` (float).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: find_vanished_customer_ids -- run-scoped snapshot diff (Finding F-2)</name>
  <files>packages/dataplat/src/dataplat/scd/delete_detection.py, tests/integration/test_scd_delete_detection.py</files>
  <read_first>
    packages/dataplat/src/dataplat/validate/referential.py (full file -- anti-join SQL shape),
    packages/dataplat/src/dataplat/metadata/repository.py (record_watermark docstring,
    lines ~895-935 -- the run-scoping precedent this function must match)
  </read_first>
  <behavior>
    - Test 1 (correctly detects a real vanish): seed normalized.customers with 3 is_current=true
      rows (customer_id 1, 2, 3) and silver.customers with only customer_id 1 and 2 tagged with
      `_run_id` values inside `staged_run_ids` -- calling `find_vanished_customer_ids` returns
      exactly `{3}`.
    - Test 2 (unscoped rows never count as present -- the F-2 regression guard): seed
      silver.customers with customer_id 3 ALSO present, but tagged with a `_run_id` NOT in
      `staged_run_ids` (an older, unrelated run) -- customer_id 3 is STILL reported vanished,
      proving the read is genuinely scoped to `staged_run_ids`, not the whole table.
    - Test 3 (nothing vanished): every currently-current customer_id also appears in the
      run-scoped silver snapshot -- returns an empty set.
    - Test 4 (empty gold): normalized.customers has zero is_current=true rows -- returns an empty
      set without error (nothing can vanish from nothing).
  </behavior>
  <action>
    Implement `find_vanished_customer_ids(conn: Connection[Any], *, staged_run_ids: Sequence[int])
    -> set[str]` in delete_detection.py: a single parameterized SQL statement matching Pattern
    3's shape -- a CTE selecting `DISTINCT customer_id FROM silver.customers WHERE _run_id =
    ANY(%(staged_run_ids)s)`, then `SELECT customer_id FROM normalized.customers WHERE is_current
    AND customer_id NOT IN (SELECT customer_id FROM <that CTE>)`. `staged_run_ids` is the ONLY
    parameter bound as a value (`%(staged_run_ids)s`); table/column names are literal,
    hand-written identifiers (T-10-01, matching merge.py/referential.py's own convention -- this
    Publisher is deliberately single-dataset, same precedent as MergePublisher's own module
    docstring). Cast the result rows to `str` (matching `PublishResult.published_business_keys`'s
    own string convention).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/integration/test_scd_delete_detection.py -q -m integration -k vanished</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_scd_delete_detection.py -q -m integration -k vanished`
      passes, all 4 behaviors covered against a real testcontainers PostgreSQL
    - The SQL text contains no f-string/`.format()` interpolation of anything other than literal,
      hand-written identifiers -- `staged_run_ids` crosses exclusively via a bound parameter
  </acceptance_criteria>
  <done>find_vanished_customer_ids correctly and exclusively detects vanish via THIS pass's run-scoped snapshot, proven against a live PostgreSQL including the exact F-2 regression case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MassDeleteCircuitBreaker and delete-semantics dispatch (D-05/D-06)</name>
  <files>packages/dataplat/src/dataplat/scd/delete_detection.py, tests/integration/test_scd_delete_detection.py</files>
  <read_first>
    packages/dataplat/src/dataplat/validate/circuit_breaker.py (full file -- the exact shape to
    mirror),
    packages/dataplat/src/dataplat/errors.py (QualityThresholdExceeded's constructor)
  </read_first>
  <behavior>
    - Test 1 (breach raises): `current_count=100`, `vanished_count=15`, `threshold=0.10` raises
      `QualityThresholdExceeded` whose `context` dict includes the observed ratio and configured
      threshold.
    - Test 2 (within threshold passes): `current_count=100`, `vanished_count=5`, `threshold=0.10`
      returns a PASS `StageResult`, no exception.
    - Test 3 (empty-gold trivial pass): `current_count=0` (mirrors
      RejectionRateCircuitBreaker's own `total_rows_read == 0` guard) returns a trivial PASS
      regardless of `vanished_count`.
    - Test 4 (exactly at threshold does not breach): `vanished_count / current_count ==
      threshold` exactly does not raise (mirrors the existing breaker's `if ratio > self._threshold`,
      not `>=`).
    - Test 5 (delete-semantics dispatch, ignore): calling the dispatch function with
      `delete_semantics="ignore"` and a non-empty vanished set returns an action indicating "no
      database write should happen for these keys."
    - Test 6 (delete-semantics dispatch, invalidate): `delete_semantics="invalidate"` returns an
      action naming the vanished keys AND the correct SQL to close them out --
      `UPDATE normalized.customers SET valid_to = %(snapshot_max_event_ts)s, is_current = false
      WHERE customer_id = ANY(%(vanished_ids)s) AND is_current` -- proven live: seed 2
      is_current rows, mark one vanished, apply, assert exactly that one row now has
      `is_current = false` and `valid_to` equal to the supplied snapshot max event_ts (never
      `now()` -- SCD-06's effective-dating requirement applied to the DELETE path).
    - Test 7 (delete-semantics dispatch, new_record): `delete_semantics="new_record"` inserts a
      new version row for each vanished key (copying forward that key's current Type-2 attribute
      values unchanged, `valid_from = snapshot_max_event_ts`, `is_current = true`) and closes the
      prior current row (`valid_to = snapshot_max_event_ts`, `is_current = false`) -- proven live:
      after applying, exactly 2 rows exist for that customer_id, exactly 1 is_current, and the
      new row's `id` (surrogate key) differs from the old row's.
  </behavior>
  <action>
    Implement `MassDeleteCircuitBreaker(BarrierStage)` in delete_detection.py: constructor takes
    `threshold: float`, `current_count: int`, `vanished_count: int` (mirrors
    RejectionRateCircuitBreaker's constructor-parameterized shape exactly); `apply(ctx)` returns
    the trivial-PASS `StageResult` when `current_count == 0`, else computes
    `ratio = vanished_count / current_count` and raises `QualityThresholdExceeded` when
    `ratio > threshold`, else returns a PASS `StageResult` -- copy
    RejectionRateCircuitBreaker's exact `StageResult`/`ValidationResult` construction pattern,
    substituting "vanished"/"current" for "rejected"/"read" in every field name and message.
    Implement `apply_delete_semantics(conn, *, delete_semantics: str, vanished_ids: set[str],
    snapshot_max_event_ts: datetime) -> tuple[str, ...]` (returns the vanished ids actually
    acted on, for `published_business_keys`): `"ignore"` is a no-op (returns an empty tuple);
    `"invalidate"` runs the `UPDATE ... SET valid_to = ..., is_current = false` statement from
    Test 6, `snapshot_max_event_ts` bound as a value (never row content, never `now()`);
    `"new_record"` runs an `INSERT INTO normalized.customers (...) SELECT ..., %(snapshot_max_event_ts)s
    AS valid_from, ... FROM normalized.customers WHERE customer_id = ANY(%(vanished_ids)s) AND
    is_current` immediately followed by the SAME `UPDATE` invalidate uses to close the old row,
    both in the caller's already-open transaction. Raise `ConfigurationError` (dataplat.errors)
    for any `delete_semantics` value outside `{"ignore", "invalidate", "new_record"}` (defensive
    -- Pydantic's `Literal` on `ScdConfig.delete_semantics` should already prevent this, but this
    function has no way to know its caller validated first).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/integration/test_scd_delete_detection.py -q -m integration</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_scd_delete_detection.py -q -m integration` passes in full
      (both tasks' behaviors, all 11 tests)
    - `MassDeleteCircuitBreaker.apply()` never re-derives `current_count`/`vanished_count` from
      `ctx` (constructor-parameterized only, matching RejectionRateCircuitBreaker's own
      documented reasoning)
    - The `invalidate`/`new_record` SQL statements bind `snapshot_max_event_ts` and
      `vanished_ids` as VALUES only -- no interpolated row content
  </acceptance_criteria>
  <done>MassDeleteCircuitBreaker and the three DELETE-semantics actions are implemented and proven live against real PostgreSQL, with invalidate's effective-dating using the snapshot's own max event_ts, never ingestion time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| silver.customers / normalized.customers (already-validated pipeline data) -> this module's SQL | Both tables only ever hold values that already passed Phase 6's validation/normalization pipeline -- no untrusted external input reaches this module directly |
| customers.yaml's scd.delete_semantics config value -> SQL branch dispatch | A config file, not row content, but still validated defensively (T-10-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-01 | Tampering | find_vanished_customer_ids / apply_delete_semantics SQL text | mitigate | Every interpolated fragment is a literal, hand-written identifier (table/column names); every actual value (staged_run_ids, vanished_ids, snapshot_max_event_ts) crosses exclusively via bound %(...)s parameters, matching merge.py/referential.py's T-04-01/T-08-11 precedent |
| T-10-07 | Tampering | apply_delete_semantics receiving an out-of-vocabulary delete_semantics string | mitigate | Defensive ConfigurationError raised for any value outside {ignore, invalidate, new_record}, even though ScdConfig's Pydantic Literal should already prevent this upstream -- defense in depth, matching this repo's general "never trust the caller validated" discipline |
| T-10-08 | Denial of Service | MassDeleteCircuitBreaker misconfigured with threshold=0.0 on a dataset with any natural churn | accept | Same accepted-risk framing as T-08-14 (RejectionRateCircuitBreaker's own precedent) -- a deliberately strict, developer-configured threshold is a strictness choice, not a platform defect |
</threat_model>

<verification>
- pytest tests/integration/test_scd_delete_detection.py -q -m integration passes in full
- pytest tests/unit -q passes (no regression)
</verification>

<success_criteria>
DELETE-detection is correctly scoped to this pass's own files (never vacuous, never falsely tripped by unrelated historical runs), the mass-delete circuit breaker raises exactly at threshold, and all three configured DELETE semantics (ignore/invalidate/new_record) are implemented and proven against real PostgreSQL, with invalidate's effective date sourced from the snapshot itself, never ingestion time.
</success_criteria>

<output>
Create .planning/phases/10-slowly-changing-dimensions/10-03-SUMMARY.md when done
</output>
