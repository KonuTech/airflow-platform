---
phase: 10-slowly-changing-dimensions
plan: 07
type: execute
wave: 4
depends_on: ["10-04", "10-05", "10-06"]
files_modified:
  - tests/e2e/slice/test_backfill_2year_sweep.py
autonomous: true
requirements: [SCD-03, SCD-06, SCD-07, SCD-08, SCD-09, SCD-10, SCD-11, QUAL-14]

must_haves:
  truths:
    - "The live 2-year sweep corpus now includes a genuine attribute-change event, a late-arriving correction, and a missing-customer day, and the sweep test asserts correct SCD2 output for all three against the real kind cluster"
    - "A vanished customer's current row is correctly invalidated (valid_to/is_current closed) with an event-time-derived valid_to, never now()"
    - "A separate, deliberately-truncated snapshot that omits an implausible fraction of the already-established roster causes the run to FAIL LOUDLY against the real kind cluster (D-06's circuit breaker trips), and gold state is provably unchanged by that failed run -- the D-06 live proof, symmetric with the invalidate path's live proof"
    - "Re-running the entire 2-year backfill a second time leaves the SCD2 version count for every customer_id unchanged (D-12)"
    - "The existing duplicate-customer_id corruption assertion in test_live_run_concurrent_with_backfill_same_dataset no longer treats a legitimate multi-version customer_id as corruption"
  artifacts: []
  key_links:
    - from: "tests/e2e/slice/test_backfill_2year_sweep.py"
      to: "tools/corpus/dated_series.py"
      via: "sweep_corpus fixture calls generate_dated_series with the new attribute_change/late_correction/missing_customer parameters"
      pattern: "attribute_change_day_index"
    - from: "tests/e2e/slice/test_backfill_2year_sweep.py"
      to: "tools/corpus/dated_series.py"
      via: "a separate, single-day generate_dated_series call (same master_seed/rows_per_day as sweep_corpus) supplies mass_delete_day_index/mass_delete_member_indices for the D-06 circuit-breaker-trip proof"
      pattern: "mass_delete_day_index"
---

<objective>
Wire plan 10-06's corpus anomalies into the live 2-year sweep test and prove SCD-03/06/07/08/09/
10/11 and QUAL-14 against the real kind cluster -- the phase's central live proof. Also prove
D-06's mass-delete circuit breaker trips against a real, deliberately-truncated snapshot (not
just the contrived integration-test counts plan 10-03 already proved in isolation), rewrite the
one existing assertion RESEARCH.md's Pattern 4 flagged as now-wrong (a "no duplicate
customer_id" check that would misclassify legitimate SCD2 versioning as corruption), and extend
the existing idempotent-rerun test per D-12 to assert SCD2 version-count stability, not just row
count.

Purpose: this is where "the SCD Publisher works in integration tests" becomes "the SCD Publisher
works against the real cluster, the real Airflow scheduler, and the real 14-day backfill this
project already trusts for every other correctness proof in Phase 9" -- and where D-06's safety
mechanism is proven to actually stop a bad file in production conditions, not merely in a
testcontainers arithmetic check.
Output: test_backfill_2year_sweep.py's sweep_corpus fixture generates the extended corpus; new
assertions prove version boundaries, late correction, DELETE invalidate, and D-12's idempotent
rerun; a new, separate test proves the D-06 mass-delete circuit breaker fails a real run loudly
without mutating gold state; the old corruption check is rewritten to check for overlapping
validity / duplicate is_current, not "any duplicate row."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-slowly-changing-dimensions/10-CONTEXT.md
@.planning/phases/10-slowly-changing-dimensions/10-RESEARCH.md
@.planning/phases/10-slowly-changing-dimensions/10-PATTERNS.md
@.planning/phases/10-slowly-changing-dimensions/10-06-PLAN.md

<interfaces>
From tests/e2e/slice/test_backfill_2year_sweep.py (read extensively during planning): module
constants near the top (num_days=14, gap_day_index=5, schema_change_day_index=7,
late_event_day_index=10, rows_per_day=50) feed BOTH `generate_dated_series` calls inside the
`sweep_corpus` fixture (module-scoped, generates once, uploads to MinIO). `_SweepState` is a
module-scoped mutable dataclass threading state between Task-ordered tests within one pytest
session. `test_full_2year_sweep_customers_and_orders` is the main 7-assertion live proof;
`test_idempotent_rerun_produces_zero_additional_rows` is D-12's base to extend;
`test_live_run_concurrent_with_backfill_same_dataset` contains Pattern 4's now-wrong corruption
assertion (a `SELECT customer_id, count(*) ... HAVING count(*) > 1` shape, or equivalent) that
must be rewritten to check overlapping validity/duplicate is_current instead.
`_TERMINAL_RUN_STATUSES = frozenset({"SUCCEEDED", "FAILED", "SKIPPED_DUPLICATE",
"SKIPPED_CONCURRENT"})` (module-level constant, already read) -- `"FAILED"` is this file's own
existing vocabulary for a hard task/run failure, the exact status Task 3 asserts on.
`_wait_for_dataset_files_terminal(conn, *, dataset, filenames, timeout)` (already read in full)
polls `meta.ingestion_runs` keyed by filename and returns each file's terminal `status` once
every requested filename reaches one of `_TERMINAL_RUN_STATUSES` -- this is the exact helper
Task 3 reuses to observe the bad file reaching `"FAILED"`, no new polling mechanism needed.
`test_live_run_concurrent_with_backfill_same_dataset` (already read) demonstrates this file's own
precedent for triggering a single FRESH DagRun outside the main backfill window and polling for
it -- Task 3's trigger mechanism should mirror that pattern, not the whole-window backfill CLI
`test_full_2year_sweep_customers_and_orders` uses.

From plan 10-06: generate_dated_series gains attribute_change_day_index/
attribute_change_member_index, late_correction_arrival_day_index/late_correction_member_index/
late_correction_offset_days, missing_customer_day_index/missing_customer_member_index, and
(Task 3) mass_delete_day_index/mass_delete_member_indices -- all meaningful only for
dataset="customers", all raising ValueError if set for "orders". mass_delete_day_index and
mass_delete_member_indices must be supplied together (ValueError if only one is set).
BackfillCorpusManifest records each as a field. Task 3's own docstring note: the mass-delete
fixture is designed to be generated/processed OUTSIDE the successful sweep window, since tripping
the breaker is expected to fail that day's run loudly.

customers.yaml (plan 10-01) configures `mass_delete_threshold: 0.10` -- the D-06 fixture's removed
fraction must comfortably exceed this value (plan 10-06 Task 3 already asserts > 0.10 at the unit
level; this plan's live fixture should use the same comfortably-exceeding proportion, e.g. roughly
30% of the roster, not a value near the boundary).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the extended corpus and prove version boundaries, late correction, DELETE invalidate</name>
  <files>tests/e2e/slice/test_backfill_2year_sweep.py</files>
  <read_first>
    tests/e2e/slice/test_backfill_2year_sweep.py (full file -- already read extensively during
    planning; re-read the module constants block, the sweep_corpus fixture, and
    test_full_2year_sweep_customers_and_orders in full before editing)
  </read_first>
  <action>
    Choose concrete, non-colliding day/member indices for the three new anomalies, avoiding the
    existing gap(5)/schema-change(7)/late-event(10) days and staying within a small `num_days`
    (extend `_NUM_DAYS` from 14 to whatever comfortably fits all six anomaly days without
    cramming -- e.g. 18-20 -- document the chosen value's reasoning in a comment, matching this
    file's own existing "comfortably under [budget]" comment style). Pick member indices `>= 30`
    for every new anomaly (never colliding with the orders referential-integrity pool). Add the
    new module constants (`_ATTRIBUTE_CHANGE_DAY_INDEX`, `_ATTRIBUTE_CHANGE_MEMBER_INDEX`,
    `_LATE_CORRECTION_ARRIVAL_DAY_INDEX`, `_LATE_CORRECTION_MEMBER_INDEX`,
    `_LATE_CORRECTION_OFFSET_DAYS`, `_MISSING_CUSTOMER_DAY_INDEX`,
    `_MISSING_CUSTOMER_MEMBER_INDEX`) and pass them into the `sweep_corpus` fixture's
    `generate_dated_series("customers", ...)` call only (never the orders call, which must keep
    raising ValueError if any of these were mistakenly passed -- do not pass them there). Add new
    assertions inside (or immediately after) `test_full_2year_sweep_customers_and_orders`,
    following its own numbered-comment-block convention: (8) query
    `normalized.customers WHERE customer_id = <the attribute-change member's id>
    ORDER BY valid_from` and assert exactly 2 version rows exist, with `valid_from`/`valid_to`
    correctly bracketing the change day's date, `is_current` true only on the later row, and
    `name`/`country` differing between them exactly as the corpus manifest recorded; (9) query
    the late-correction member's version rows and assert the backdated version's `valid_from`
    falls strictly between two OTHER version boundaries for that same key (proving SCD-07's
    "recompute repositions a late correction chronologically," not merely "adds a row at the
    end"); (10) query the missing-customer member's row and assert `is_current = false`,
    `valid_to` equal to the corpus's own last-observed max event_ts for that snapshot (never a
    value close to wall-clock "now" -- SCD-06 applied to the DELETE path), proving D-05's
    `invalidate` default fired correctly on a real cluster. Every new assertion must state, in a
    failure message, WHICH property it is proving (mirroring this file's own existing assertion
    style).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k full_2year_sweep</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k full_2year_sweep`
      passes against the real kind cluster
    - The attribute-change member shows exactly 2 correctly-bracketed version rows
    - The late-correction member's backdated version sits chronologically between two other
      versions of the same key, not merely appended
    - The missing-customer member is correctly invalidated with an event-time-derived valid_to
  </acceptance_criteria>
  <done>The live 2-year sweep proves SCD-03/06/07/08 against the real cluster using the extended corpus's three new anomalies.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 idempotent-rerun version-count assertion and the Pattern-4 corruption-check rewrite</name>
  <files>tests/e2e/slice/test_backfill_2year_sweep.py</files>
  <read_first>
    tests/e2e/slice/test_backfill_2year_sweep.py (the `test_idempotent_rerun_produces_zero_
    additional_rows` and `test_live_run_concurrent_with_backfill_same_dataset` functions in full
    -- already read/located during planning)
  </read_first>
  <action>
    In `test_idempotent_rerun_produces_zero_additional_rows` (or a new test placed immediately
    beside it if the existing one is structured around a single specific assertion that doesn't
    naturally extend): before the second full-window backfill re-run, capture
    `SELECT customer_id, count(*) FROM normalized.customers GROUP BY customer_id` as a baseline
    version-count-per-key snapshot; after the re-run, assert the SAME snapshot is byte-for-byte
    identical (D-12's "SCD2 version count is unchanged" requirement, extending Phase 4's original
    "re-run produces zero additional rows" pattern to version count instead of raw row count).
    Also assert `normalized.customers`'s TOTAL row count is unchanged (a stronger, redundant
    check catching any accidental extra INSERT the per-key snapshot might miss). In
    `test_live_run_concurrent_with_backfill_same_dataset`: locate the corruption assertion
    RESEARCH.md's Pattern 4 names (a `SELECT customer_id, count(*) ... HAVING count(*) > 1` shape
    or equivalent "no duplicate customer_id" check) and replace it with two assertions that
    reflect the CORRECT SCD2 invariant instead: (a) `SELECT customer_id, count(*) FROM
    normalized.customers WHERE is_current GROUP BY customer_id HAVING count(*) > 1` returns zero
    rows (at most one current version per key -- genuine corruption if violated); (b) a direct
    live query proving no overlapping validity ranges exist for any single customer_id (e.g.
    `SELECT a.customer_id FROM normalized.customers a JOIN normalized.customers b ON
    a.customer_id = b.customer_id AND a.id <> b.id AND a.validity && b.validity` returns zero
    rows -- the exclusion constraint should make this structurally impossible, but this test
    proves it live under real concurrency, not just via the DDL's own guarantee). Update this
    test's docstring/comments to state explicitly that MULTIPLE rows per customer_id are now the
    expected, correct shape (RESEARCH.md's own "should never actually fire" framing for the
    exclusion constraint applies here too).
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k "idempotent_rerun or concurrent_with_backfill"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k "idempotent_rerun or
      concurrent_with_backfill"` passes against the real kind cluster
    - The idempotent-rerun test asserts BOTH total row count AND per-key version count are
      unchanged after a full second backfill
    - The rewritten corruption check asserts "at most one is_current row per key" and "no
      overlapping validity ranges," never "no duplicate customer_id row at all"
  </acceptance_criteria>
  <done>D-12's idempotent-replay proof covers SCD2 version count explicitly, and the concurrency test's corruption assertion reflects the real, correct multi-version SCD2 invariant instead of the pre-Phase-10 single-row assumption.</done>
</task>

<task type="auto">
  <name>Task 3: D-06 mass-delete circuit-breaker live trip proof</name>
  <files>tests/e2e/slice/test_backfill_2year_sweep.py</files>
  <read_first>
    tests/e2e/slice/test_backfill_2year_sweep.py (this plan's own Task 1 output -- the extended
    `sweep_corpus` fixture and its established roster/current gold state -- plus
    `test_live_run_concurrent_with_backfill_same_dataset`'s "trigger one fresh DagRun outside the
    main window and poll `_wait_for_dataset_files_terminal`" pattern, already read/located during
    planning)
  </read_first>
  <action>
    Run this test AFTER `test_full_2year_sweep_customers_and_orders` has established real,
    current SCD2 gold state for the sweep's roster (this file's existing Task-ordering convention
    already sequences tests this way via `_SweepState`). Generate ONE additional, single-day
    customers file via a SEPARATE `generate_dated_series("customers", ...)` call: same
    `master_seed` and `rows_per_day` as `sweep_corpus`'s own call (so the roster's customer_ids
    and baseline attribute values are IDENTICAL to what the main sweep already published --
    roster baselines are customer-scoped, not day-scoped, per plan 10-06 Task 1, so this holds
    regardless of `start_date`), `start_date` set to the day immediately following the extended
    sweep's own last day (`_NUM_DAYS`), `num_days=1`, and `mass_delete_day_index=0` paired with a
    `mass_delete_member_indices` block chosen to remove roughly 30% of the roster (comfortably
    above customers.yaml's configured `mass_delete_threshold: 0.10`), using member indices `>=
    30` that do not collide with Task 1's attribute-change/late-correction/missing-customer
    indices (reuse plan 10-06 Task 3's own collision guard as the authority on this -- if the
    chosen block collides, `generate_dated_series` itself raises, catching the mistake
    immediately). Set `gap_day_index`/`schema_change_day_index`/`late_event_day_index` to values
    outside this single-day range (e.g. `1`) since they are required parameters but irrelevant
    here. Upload this one file directly via `sweep_corpus`'s own upload helper/bucket/prefix
    convention (reuse it, do not hand-roll a second upload path). Trigger a single fresh customers
    DagRun for this file (mirroring
    `test_live_run_concurrent_with_backfill_same_dataset`'s existing "trigger one fresh DagRun and
    poll" pattern -- NOT the whole-window backfill CLI Task 1 uses), then call
    `_wait_for_dataset_files_terminal` for this one filename. Assert: (a) the file's terminal
    `meta.ingestion_runs.status` is exactly `"FAILED"` (the mass-delete circuit breaker raised
    `QualityThresholdExceeded` inside the SCD publish stage, propagating to a hard run failure --
    not a silent partial success, and not merely a different terminal status like
    `SKIPPED_DUPLICATE`); (b) gold state is UNCHANGED by this failed run -- for every member index
    in the removed block, `normalized.customers` still shows `is_current = true` with the SAME
    `valid_from`/version count observed immediately before this file was processed (querying
    `normalized.customers` for those specific customer_ids before triggering the run and again
    after, and asserting the two snapshots are identical), proving the circuit breaker is a
    pre-mutation barrier stage (per plan 10-03's `BarrierStage` design), not a partial-apply-then-
    fail race. State in each assertion's failure message which property it is proving, matching
    this file's existing assertion style.
  </action>
  <verify>
    <automated>cd /home/konutec/projects/airflow-platform && pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k mass_delete</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster -k mass_delete` passes
      against the real kind cluster
    - The deliberately-truncated file's terminal `meta.ingestion_runs.status` is asserted to be
      exactly `"FAILED"`
    - Gold state for the removed members is asserted unchanged (same `is_current`/`valid_from`)
      before and after the failed run, proving the breaker prevented any mutation rather than
      merely erroring after one
    - The removed fraction used in the live fixture comfortably exceeds customers.yaml's
      configured `mass_delete_threshold` (0.10)
  </acceptance_criteria>
  <done>D-06's mass-delete circuit breaker is proven, against the real kind cluster with a real deliberately-truncated snapshot, to fail the run loudly and leave gold state untouched -- the live proof CONTEXT.md's "Real fixtures required" clause requires, symmetric with Task 1's invalidate-path live proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live kind cluster (real Airflow, real PostgreSQL) -> this test file's assertions | Same boundary every other test in this file already operates across; this plan adds assertions, not a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-03 | Tampering / DoS | Concurrent writers producing a corrupted/partial version chain, live-observed | mitigate | This plan's rewritten corruption check (Task 2) is the live proof that pg_advisory_xact_lock + the exclusion constraint hold under real concurrency, not just in a controlled integration test -- the SAME threat named in plans 10-01/10-04, closed here with a live assertion |
| T-10-06-LIVE | Tampering / DoS | A truncated/corrupted source snapshot masquerading as a legitimate mass customer deletion | mitigate | Task 3 is the live proof that D-06's `MassDeleteCircuitBreaker` (plan 10-03) actually stops a real, deliberately-bad file from silently invalidating a large slice of the customer dimension against the real cluster, not just in the contrived testcontainers counts plan 10-03's own integration test used |
</threat_model>

<verification>
- pytest tests/e2e/slice/test_backfill_2year_sweep.py -q -m cluster passes in full (the whole module, not just the new/changed tests -- confirms no regression to the existing 7-assertion proof or Task 1/2/3's own live orchestration tests)
</verification>

<success_criteria>
The live 2-year sweep proves SCD-03/06/07/08/09/10/11 and QUAL-14 against the real kind cluster,
using genuine attribute-change, late-correction and missing-customer fixtures; D-06's mass-delete
circuit breaker is proven, against a real deliberately-truncated snapshot on the real cluster, to
fail the run loudly without mutating gold state; the idempotent-rerun test is extended to SCD2
version count and the concurrency test's corruption check is corrected to the real multi-version
invariant.
</success_criteria>

<output>
Create .planning/phases/10-slowly-changing-dimensions/10-07-SUMMARY.md when done
</output>
</output>
