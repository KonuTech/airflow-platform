---
status: resolved
trigger: "Airflow KubernetesExecutor scheduler stall — csv_ingest_customers DAG stopped getting new DagRuns entirely. max_active_runs=1, the one active slot occupied by an old backlog DagRun whose resolve_window (failed) and wait_for_files (up_for_retry) tasks both froze ~2h before investigation, correlating with a scheduler pod restart. Scheduler logs show a watch-restart-from-resource_version-0 loop replaying stale terminal pod events. Blocks gap-closure plan 05-06's Task 2 (needs one real DAG SUCCEEDED run to fully close SEC-13 — the Vault/credential side is already independently proven, 15/16 e2e tests passing)."
created: 2026-08-14T19:32:51Z
updated: 2026-08-14T20:22:00Z
---

## Current Focus
<!-- OVERWRITE on each update - always reflects NOW -->

hypothesis: CONFIRMED (see Resolution.root_cause). Executing user-approved scoped fix: restart ONLY `airflow-platform-worker` (hosts dag-processor + api-server per user's checkpoint response), NOT worker2 or control-plane. This is a deliberately narrower blast radius than the original fix_rationale (which called for all 3 nodes) — user's explicit choice, documented in checkpoint response.
test: Pre-restart baseline diagnostics (node/pod placement, current /mnt/dags state on worker), then `docker restart airflow-platform-worker`, then verify mount reattachment + is_stale clears + scheduler resumption.
expecting: /mnt/dags on airflow-platform-worker shows real DAG files after restart; pods scheduled on that node come back Ready with /opt/airflow/dags populated; dag-processor re-parses and clears DagModel.is_stale; scheduler (wherever it runs) picks the stuck DagRun back up without further action.
next_action: RESOLVED. Orchestrator independently re-verified this session's fix (DagRun success, autonomous backlog progression, a fresh `meta.ingestion_runs` SUCCEEDED row post-Vault-reinstall) before presenting to the user. User confirmed and additionally approved closing the residual risk: `docker restart airflow-platform-worker2` and `docker restart airflow-platform-control-plane` were then also run (sequentially, each verified healthy before proceeding to the next). Both mounts reattached (real ext4, DAG files present); Vault (co-located on worker2) was bounced and re-unsealed via `make vault-unseal`, recovering its existing storage (same Cluster ID, not a fresh init); all pods across all 3 nodes confirmed healthy post-restart (coredns, ingress-nginx, CNPG, MinIO, Airflow components). All 3 nodes now have a working DAGs mount — the residual TaskFlow-task risk flagged during the scoped fix is fully closed, not merely accepted. Session complete.
reasoning_checkpoint:
  hypothesis: "The kind cluster's DAGs hostPath bind mount (WSL2 host /home/konutec/projects/airflow-platform/airflow/dags -> kind node /mnt/dags -> pod /opt/airflow/dags) failed to reattach after the ~16:58:55 UTC Docker Desktop/WSL2-level restart that killed all containers on all 3 kind nodes simultaneously. All 3 nodes now show /mnt/dags backed by an empty, read-only tmpfs instead of the real host directory. This causes the DAG processor to discover zero DAG files, so it never re-parses any DAG and never clears DagModel.is_stale. The scheduler's get_running_dag_runs_to_examine() query filters WHERE DagModel.is_stale == false(), so it silently and permanently excludes ALL running DagRuns for ALL dag_ids from ever being examined again — a cluster-wide, silent scheduling freeze with zero exceptions logged anywhere."
  confirming_evidence:
    - "docker exec into all 3 kind node containers directly (bypassing Kubernetes) shows /mnt/dags is empty on every node, with `mount` reporting `none on /mnt/dags type tmpfs (ro,relatime)` instead of the bind-mounted host path"
    - "docker inspect on the control-plane node shows the configured mount IS a bind from the real host path (/home/konutec/projects/airflow-platform/airflow/dags) — the discrepancy is a live-attachment failure, not a config change"
    - "A test write (touch /mnt/dags/.rw-test) inside the node fails with 'Read-only file system', confirming this is a genuine fallback tmpfs, not something reparable from inside the container"
    - "Real DAG source files confirmed intact on the WSL2 host filesystem (csv_ingest_customers.py, smoke_kubernetes_pod.py, _common/) — no data loss, purely a mount-attachment problem"
    - "dag-processor logs show a completely empty 'DAG File Processing Stats' table and an infinite 'Not time to refresh bundle dags-folder' loop, consistent with zero files discovered to parse"
    - "DagModel.is_stale = True for BOTH csv_ingest_customers AND the unrelated smoke_kubernetes_pod DAG (control) — proves this is cluster-wide, not specific to csv_ingest_customers"
    - "dag_run.last_scheduling_decision for the stuck DagRun (and the single most-recent value across the ENTIRE metastore, all dag_ids) is frozen at 16:40:00 UTC; source read (models/dagrun.py) confirms this field is set unconditionally as the first line of update_state() — it never being called for 3+ hours proves the DagRun never reaches that code path"
    - "Source read of get_running_dag_runs_to_examine() (models/dagrun.py): `.where(DagModel.is_paused == false(), DagModel.is_stale == false())` — the exact, sole mechanism causally connecting is_stale=True to zero scheduling activity, a silent SQL exclusion with no exception path"
    - "No locks, no prepared transactions, no long-running queries, no exceptions/tracebacks in scheduler logs since restart — rules out DB contention or an unhandled error as alternative explanations"
    - "KubernetesJobWatcher source (installed provider 10.21.0) read directly: on plain ReadTimeoutError, resource_version is preserved (not reset to '0') — the watch-loop noise is normal reconnect behavior, not a resource_version bug blocking progress (falsifies hypothesis A)"
    - "SchedulerJob id=48 has been running/heartbeating continuously since 17:00:39 (3+ hours) — a scheduler restart ALREADY happened involuntarily and did not clear the stall, falsifying hypothesis B (scoped scheduler restart as fix) since that exact action was already involuntarily performed"
  falsification_test: "If DagModel.is_stale for csv_ingest_customers/smoke_kubernetes_pod is currently False, or /mnt/dags on the kind nodes shows real DAG files, this hypothesis is wrong. Both directly checked just now: is_stale=True (both DAGs), /mnt/dags empty (all 3 nodes)."
  fix_rationale: "Docker/Linux cannot live-remount a running container's bind mount from inside or outside without a container restart. All 3 kind nodes show the identical broken tmpfs fallback, so all 3 need `docker restart <node>` to restore real DAG-file visibility platform-wide, letting the dag-processor re-parse, clear is_stale, and unblock the scheduler's query. This addresses the actual root cause (broken bind mount) rather than a symptom — clearing TI state and the scheduler restart were both already tried/already happened and did not work, exactly as this diagnosis predicts they wouldn't."
  blind_spots: "Have not verified WHY the bind mount failed to reattach (Docker Desktop/WSL2 internals are opaque from here) or confirmed it will reattach cleanly on a fresh docker restart without a deeper Docker Desktop/WSL2 restart — inferred from known WSL2/Docker Desktop behavior patterns and that the mount IS still correctly configured per docker inspect, not yet verified by an actual successful reattachment. Have not confirmed what other workloads across other namespaces are currently in-flight and would be disrupted by a node restart — the fix's blast radius exceeds what was pre-authorized for this session, which is why this is being escalated as a checkpoint rather than executed."
tdd_checkpoint: null

## Symptoms
<!-- Written during gathering, then immutable -->

expected: csv_ingest_customers continues getting new scheduled DagRuns on its normal schedule, and previously-queued backlog DagRuns drain through the max_active_runs=1 slot over time, eventually reaching SUCCEEDED and writing rows to meta.ingestion_runs.
actual: Zero new DagRuns created since logical_date 2026-08-14T02:50:00 (next_dagrun_logical_date stuck hours behind wall-clock at time of check). The one active-slot DagRun's tasks (resolve_window, wait_for_files) show zero progress for 2+ hours — no new pod-launch attempts, no retries — despite being in up_for_retry/failed (non-terminal-for-DAG-purposes) states. Dozens of DagRuns sit queued behind it.
errors: Scheduler log loop, repeating ~every 30s (before intervention 2 below): "Kubernetes watch timed out waiting for events. Restarting watch." → "Event: and now my watch begins starting at resource_version: 0" → replayed "Failed" events for 2 already-terminal pods (csv-ingest-customers-resolve-window-kxybzrgx, csv-ingest-customers-wait-for-files-bwq05832), each followed by "Changing state of KubernetesResults(...) to failed".
reproduction: Ongoing/persistent state on the live kind cluster (context kind-airflow-platform), not a one-shot repro. Observe via `kubectl exec -n airflow deploy/airflow-api-server -- airflow dags list-runs csv_ingest_customers --state queued -o table` (dozens queued) and `airflow tasks states-for-dag-run csv_ingest_customers <stuck logical_date>` (frozen states, timestamps not advancing across repeated polls).
started: Task progress froze ~2026-08-14T17:00 UTC, correlating with the airflow-scheduler pod's most recent restart (pod airflow-scheduler-6fd4d96b44-hs7v7, RESTARTS=2, most recent ~122min before a 19:00 UTC check — i.e. ~17:00 UTC). The underlying backlog (~680 DagRuns) has earlier origin: an over-broad `airflow tasks clear` during plan 05-03's diagnostic work (documented in .planning/STATE.md's Blockers/Concerns as expected to be "self-draining... no action needed beyond waiting"). Live observation during this debug trigger contradicts that: the backlog is not currently draining at all, not just slow.

## Eliminated
<!-- APPEND only - prevents re-investigating after /clear -->

- hypothesis: Kind cluster node health / infrastructure failure is the root cause
  evidence: All 3 kind nodes (airflow-platform-control-plane, -worker, -worker2) confirmed `Ready` via `kubectl get nodes -o wide`
  timestamp: 2026-08-14T19:23:00Z

- hypothesis: The 3 stale/orphaned terminal pod objects alone are what's blocking the scheduler from progressing
  evidence: Force-deleted all 3 (`kubectl delete pod ... --force --grace-period=0`: 1 Error-status pod age 6h32m, 2 Unknown-status pods age 158m). Scheduler log watch-loop noise stopped (0 mentions of the stale pod names in a 15s log sample afterward), but the scheduler still queued zero new task instances for the DAG over a further ~2.5 minutes of polling — ruling this out as the sole/complete cause
  timestamp: 2026-08-14T19:23:23Z

- hypothesis: The one stuck DagRun's task-instance DB state alone is what's blocking a retry
  evidence: Ran `airflow tasks clear csv_ingest_customers -s "2026-08-14T02:50:00+00:00" -e "2026-08-14T02:50:00+00:00" -t resolve_window -d -y`. `resolve_window`'s state cleanly reset to None (confirmed via `airflow tasks states-for-dag-run`), but the scheduler never queued a new attempt over ~2 minutes of polling before the pod-deletion intervention above — ruling out a simple task-instance-state fix as sufficient on its own
  timestamp: 2026-08-14T19:17:02Z

- hypothesis: (A) KubernetesJobWatcher does not persist/resume resource_version across timeout-triggered restarts, causing an indefinite replay of stale terminal pod events that itself blocks forward progress (provider-level bug)
  evidence: Read the actual installed source (provider 10.21.0, kubernetes_executor_utils.py `KubernetesJobWatcher.run()`/`_run()`) directly from the live scheduler pod. On a plain `ReadTimeoutError` (30s idle-socket timeout, the exact error in our logs), `self.resource_version` is explicitly NOT reset — it retains its pre-call value and `_run()` is re-invoked with that same value. Reset to "0" only happens via `except Exception` (fatal/unexpected error) or `_health_check_kube_watchers()` finding a dead watcher process. The repeated replay of Failed events for the 2 stale pods (kxybzrgx, bwq05832) happened because those pods still matched the watcher's label selector (`POD_EXECUTOR_DONE_KEY != True`) until force-deleted — this is expected K8s watch behavior for not-yet-acknowledged terminal pods, not a resource_version bug. Confirmed the replay WAS reaching the executor's result-processing path (kubernetes_executor.py:537, "Task ... failed in pod ...", repeating every ~30s from 19:13:49–19:19:43, stopping only after force-delete ~19:23) — but this only affects redundant re-processing of an already-terminal TI's outcome; it does not touch the DagRun-level scheduling loop at all (confirmed via later evidence: `_schedule_dag_run` and `get_running_dag_runs_to_examine` have no dependency on the KubernetesExecutor watcher).
  timestamp: 2026-08-14T19:50Z

- hypothesis: (B) The scheduler pod's restart at ~17:00 UTC corrupted or reset in-memory executor state (running-pods tracking, concurrency-slot accounting) in a way that doesn't self-heal, independent of the watch-loop noise — fix would be a scoped scheduler pod restart to force state reconciliation
  evidence: SchedulerJob table shows job id=48 (hostname airflow-scheduler-6fd4d96b44-hs7v7) has start_date=2026-08-14T17:00:39.302515 and a fresh heartbeat (10s old when checked at 19:42). This means a full scheduler process restart ALREADY happened involuntarily at 17:00:39 (part of the whole-cluster event, see Evidence below) — and 3+ hours into that NEW process's life, `dag_run.last_scheduling_decision` for the stuck run is STILL frozen at 16:40:00 (before even the restart). A scheduler restart already occurred and did not self-heal the stall, directly falsifying "a scoped scheduler restart is the fix." The real mechanism (below) is a DB-persisted DagModel flag plus a broken node-level hostPath mount, neither of which a scheduler process restart can affect.
  timestamp: 2026-08-14T19:50Z

## Evidence
<!-- APPEND only - facts discovered during investigation -->

- timestamp: 2026-08-14T19:00:18Z
  checked: kubectl get pods -n airflow -l 'dag_id=csv_ingest_customers' (before any cleanup)
  found: 3 pods present — csv-ingest-customers-discover-if4mljrq (Error, 6h32m old), csv-ingest-customers-resolve-window-kxybzrgx (Unknown, 158m old), csv-ingest-customers-wait-for-files-bwq05832 (Unknown, 158m old)
  implication: KubernetesExecutor's own terminal-pod garbage collection also appears stalled, not just DagRun scheduling — a second, possibly related symptom

- timestamp: 2026-08-14T19:00:18Z
  checked: airflow dags details csv_ingest_customers
  found: max_active_runs=1, max_active_tasks=16, next_dagrun_logical_date stuck at 2026-08-14 16:39:00+00:00 (hours behind wall-clock at check time), is_paused=False
  implication: The single active-run slot is the serialization bottleneck; a genuinely stuck run blocks ALL forward scheduling for this DAG, explaining why the test's freshly-uploaded file was never noticed

- timestamp: 2026-08-14T19:00:18Z
  checked: airflow tasks states-for-dag-run csv_ingest_customers 2026-08-14T02:50:00+00:00 (before any cleanup)
  found: resolve_window=failed (start/end 16:39:52/17:00:39), wait_for_files=up_for_retry (start/end 16:39:53/17:00:39) — both terminated at the SAME moment (17:00:39), suggesting a shared external cause rather than independent task-level failures
  implication: The simultaneous termination timestamp is a strong clue — something (scheduler restart? watcher glitch?) killed both in-flight task pods at once, ~21 minutes after they started

- timestamp: 2026-08-14T19:15-19:17Z (repeated samples)
  checked: kubectl logs -n airflow -l component=scheduler --tail=40 / --since=15s
  found: Every ~30s cycle: "Kubernetes watch timed out waiting for events. Restarting watch." → "Event: and now my watch begins starting at resource_version: 0" → re-emitted "Failed" events for the same 2 terminal pods (kxybzrgx, bwq05832), each re-processed as a fresh KubernetesResults state change to "failed" — resource_version values were DIFFERENT on each cycle before pod deletion (594735-ish), suggesting either genuine new watch events or an incrementing counter on replay
  implication: The watch is not resuming from a persisted resource_version; whether this is expected provider behavior (e.g. watch always relists from 0 by design when it times out, and the "replay" is really just it re-observing pods that still exist) or a bug is the key open question for research

- timestamp: 2026-08-14T19:20-19:22Z (8 polls, ~20s apart)
  checked: airflow tasks states-for-dag-run csv_ingest_customers 2026-08-14T02:50:00+00:00 + kubectl get pods -n airflow -l dag_id=csv_ingest_customers, after clearing resolve_window
  found: resolve_window stayed at empty/None state (never re-transitioned to scheduled/queued/running); no new pod ever appeared ("No resources found in airflow namespace" every poll)
  implication: Clearing task-instance state in the metadata DB is necessary but not sufficient — something else prevents the scheduler's own scheduling loop from picking up the now-eligible task and dispatching a new pod for it

- timestamp: 2026-08-14T19:24Z
  checked: kubectl get pod -n airflow -l component=scheduler
  found: airflow-scheduler-6fd4d96b44-hs7v7, READY 2/2, RESTARTS 2 (most recent restart ~122min before this check, i.e. ~17:22 UTC — close to, slightly after, the 17:00:39 simultaneous task-termination timestamp above)
  implication: A scheduler pod restart shortly after the tasks froze is a strong correlation, not yet proven causal. Worth checking scheduler pod restart reason (OOMKilled? liveness probe failure?) and whether KubernetesExecutor's in-memory state (e.g. its own tracking of "which pods am I watching") is expected to survive a scheduler restart or needs external reconciliation that isn't happening

- timestamp: 2026-08-14T19:41Z
  checked: kubectl get pod -A -o json, comparing lastState.terminated across every container in every namespace
  found: EVERY pod's container(s) across EVERY namespace — including kube-system static pods (kube-controller-manager, kube-scheduler, kube-proxy x3, kindnet x3, coredns x2), both CNPG Postgres instances, MinIO, ingress-nginx, and all Airflow components (scheduler, scheduler-log-groomer, api-server, dag-processor, dag-processor-log-groomer, triggerer, triggerer-log-groomer) — restarted within the same 1-second window (2026-08-14T16:58:55Z–16:58:56Z), all with exitCode=255, reason=Unknown
  implication: This is not an Airflow-specific or scheduler-specific event at all — it's a whole-cluster container-runtime disruption (every container on every node killed simultaneously), consistent with a Docker Desktop/WSL2-level restart or suspend/resume (matches user's own memory notes on WSL2/Docker Desktop cgroup+memory caveats for this host). Node `Ready` conditions never flapped (lastTransitionTime unchanged from Aug 12) because the outage was shorter than Kubernetes' node-health grace period (~40s+), while the restart-to-recovery gap here was ~70-90s.

- timestamp: 2026-08-14T19:43Z
  checked: airflow dags list-runs csv_ingest_customers (full), job table SchedulerJob rows, fresh states-for-dag-run
  found: The DagRun itself (scheduled__2026-08-14T02:50:00+00:00) is in state=running (not stuck at the DagRun level) with 545 other DagRuns queued behind it (max_active_runs=1) and 450 already-success from before this run took the slot. SchedulerJob id=48 start_date=17:00:39.302515 — sub-second identical to when resolve_window/wait_for_files were marked failed/up_for_retry (17:00:39.58-60) — confirming the "simultaneous termination" at 17:00:39 was the NEW scheduler's own startup orphan-adoption logic correctly detecting and failing/retrying tasks whose pods died in the whole-cluster event, not some separate corruption event. Fresh re-check ~25 min after clearing resolve_window: still None state, never rescheduled; wait_for_files still up_for_retry, unchanged since 17:00:39.
  implication: The 17:00:39 event is now fully explained (correct orphan-adoption behavior). The real mystery shifts to: why has nothing progressed in the ~3 hours since, despite the DagRun being active and the scheduler heartbeating fine.

- timestamp: 2026-08-14T19:44-19:47Z
  checked: DB query for any task_instance activity (any dag_id) in the last 30 minutes; SchedulerJob heartbeat; DagModel pause status for all DAGs; scheduler logs filtered to exclude watch-loop noise
  found: Zero task-instance activity across ALL dag_ids for 30+ minutes (not scoped to csv_ingest_customers). SchedulerJob id=48 heartbeat fresh (10s old). Scheduler logs show periodic "Adopting or resetting orphaned tasks for active dag runs" every ~5 min (healthy maintenance loop) but nothing else notable. No pool exhaustion (default_pool: 128 slots, plenty free).
  implication: This looked like it could be total scheduler-loop paralysis (not scoped to one DagRun) — needed to check the actual DagRun-level scheduling-decision timestamp, not just task-instance activity, to pin down exactly which loop is stalled.

- timestamp: 2026-08-14T19:48Z
  checked: dag_run.last_scheduling_decision for the stuck run, and the single most-recent value of that column across the entire metastore (all dag_ids)
  found: last_scheduling_decision = 2026-08-14T16:40:00.611357 — over 3 hours stale, and this IS the most recent value across the whole metastore (next-newest rows are also all from ~16:36-16:39). Source read of models/dagrun.py confirms `self.last_scheduling_decision = start_dttm` is the unconditional first line of `update_state()` — so `update_state()` has not been successfully called for ANY DagRun in 3+ hours, spanning both the pre-crash scheduler (job 45) and the entire post-crash lifetime of the current scheduler (job 48).
  implication: The DagRun-level scheduling loop (`_schedule_dag_run` → `update_state()`) is not running for this or any DagRun — decisively narrows the search to why `_schedule_dag_run`/its caller would silently skip every dag run, with no exception logged.

- timestamp: 2026-08-14T19:50-19:52Z
  checked: pg_stat_activity, pg_locks (dag_run/task_instance), pg_prepared_xacts on the Airflow metadata DB; scheduler logs grepped for error/critical/traceback/exception since restart
  found: No long-running queries, no held or blocked locks on dag_run/task_instance, no prepared transactions, only 7 idle connections. No exceptions/tracebacks logged anywhere in scheduler logs since the 17:00:39 restart.
  implication: Rules out DB lock contention and an unhandled/crashing exception as the mechanism — the DagRun is being silently excluded from consideration, not blocked or erroring.

- timestamp: 2026-08-14T19:53-19:55Z
  checked: Source read of _schedule_dag_run (scheduler_job_runner.py) and get_running_dag_runs_to_examine (models/dagrun.py)
  found: `get_running_dag_runs_to_examine()` builds its query with `.where(DagModel.is_paused == false(), DagModel.is_stale == false())` before row-locking and limiting to the "oldest examined first" N rows. `_schedule_dag_run` itself also has an early-return path when `_verify_integrity_if_dag_changed()` returns False (logs a WARNING, not ERROR — missed by the earlier error/critical/traceback/exception grep), but `dag_version`/`serialized_dag` table checks confirmed a valid, recent DagVersion exists for csv_ingest_customers, ruling that specific path out.
  implication: `DagModel.is_stale` became the prime suspect — a silent SQL-level WHERE-clause exclusion would produce exactly zero exceptions/warnings and permanently exclude affected DagRuns from ever being examined, matching every symptom observed.

- timestamp: 2026-08-14T19:55Z
  checked: DagModel.is_stale, is_paused, last_parsed_time for csv_ingest_customers AND smoke_kubernetes_pod (unrelated control DAG)
  found: is_stale=True for BOTH DAGs (is_paused=False for both, so that's not the excluding filter). last_parsed_time frozen at ~16:39:4x UTC for both — the DAG PROCESSOR stopped actually reparsing/updating any DAG's DagModel row at that moment, ~19 minutes before the 16:58:55 whole-cluster crash, and never resumed even after its own container restarted at 16:58:55/56.
  implication: Confirmed cluster-wide (not csv_ingest_customers-specific) and confirmed causally connected via the exact query filter read above. Root cause is now in the DAG PROCESSOR's parsing pipeline, not the scheduler or KubernetesExecutor at all.

- timestamp: 2026-08-14T19:56-19:58Z
  checked: dag-processor pod logs (recent, live tail); dag-processor pod spec (volumes/mounts); dags-folder contents from inside dag-processor, scheduler, and api-server pods
  found: dag-processor logs show an empty "DAG File Processing Stats" table (zero files/PIDs tracked) and a repeating "Not time to refresh bundle dags-folder" loop. `/opt/airflow/dags/` is completely empty (only `.`/`..`) in ALL THREE checked pods (dag-processor, scheduler, api-server), with directory mtime `Aug 14 16:58` — matching the whole-cluster restart moment exactly. Pod spec shows the `dags` volume is a `hostPath` mount (`/mnt/dags`, `type: Directory`) — no git-sync sidecar or init-container copy step exists; it's a direct node-level passthrough per this project's documented "extraMounts" DAG-bundle approach.
  implication: The empty dags folder — not any Airflow-level bug — is why the dag-processor finds zero files to parse. Needed to check whether the underlying kind-node-level hostPath source is itself broken (in which case pod restarts won't help) or just this pod's mount view is stale (in which case pod restarts would fix it).

- timestamp: 2026-08-14T19:59-20:01Z
  checked: docker exec (bypassing Kubernetes entirely) into all 3 kind node containers: `ls -la /mnt/dags`, `mount | grep dags`, `docker inspect` for the configured bind-mount source, a test write, and confirmation the real source directory on the WSL2 host still has the DAG files
  found: All 3 kind nodes (control-plane, worker, worker2) show `/mnt/dags` empty with `mount` reporting `none on /mnt/dags type tmpfs (ro,relatime)`. `docker inspect` confirms the node is CONFIGURED with a bind mount from the real host path (`/home/konutec/projects/airflow-platform/airflow/dags`) to `/mnt/dags` — but the LIVE mount is an anonymous empty read-only tmpfs, not that bind. `touch /mnt/dags/.rw-test` fails with "Read-only file system" (confirms it's a genuine fallback mount, not writable/reparable from inside the container). The real DAG files (csv_ingest_customers.py, smoke_kubernetes_pod.py, _common/) are confirmed present and intact on the WSL2 host filesystem — no data loss.
  implication: ROOT CAUSE CONFIRMED. The Docker Desktop/WSL2-level restart at 16:58:55 broke the bind-mount attachment for the DAGs hostPath volume on all 3 kind nodes simultaneously — Docker fell back to an empty, read-only tmpfs instead of re-establishing the real bind mount. This is a known Docker-Desktop-on-WSL2 failure mode, not an Airflow or Kubernetes application bug. Fixing it requires restarting the affected Docker containers (the kind nodes) so Docker re-attaches the configured bind mount — cannot be fixed by restarting any Airflow pod, and cannot be worked around from inside the container (mount is read-only, no alternate path to the host directory exists inside the node's own filesystem namespace).

- timestamp: 2026-08-14T20:03Z
  checked: Pod-to-node placement for the airflow namespace (kubectl get pods -o wide) and current /mnt/dags state on airflow-platform-worker, prior to executing the user-approved scoped fix
  found: airflow-dag-processor and airflow-api-server both run on airflow-platform-worker; airflow-scheduler and airflow-triggerer both run on airflow-platform-worker2. Worker pod template (airflow-config ConfigMap, pod_template_file.yaml) confirms the `dags` volume is `hostPath: /mnt/dags, type: Directory, readOnly: true` with NO nodeSelector/affinity restricting task-executor pods to any particular node (only soft pod-anti-affinity between replicas of the SAME component). /mnt/dags on worker reconfirmed still the broken empty tmpfs immediately before the restart.
  implication: The user's scoped fix (worker only) targets exactly the node needed to unblock the DAG-processor/is_stale mechanism. But because task-executor pods have no node pinning, new task pods COULD still land on worker2/control-plane post-fix — a live, not just theoretical, residual risk to watch during verification.

- timestamp: 2026-08-14T20:04Z
  checked: `docker restart airflow-platform-worker` execution and immediate post-restart state (docker ps, kubectl get nodes, docker exec mount check on worker)
  found: Container restarted cleanly, node returned to `Ready` within the check window. `/mnt/dags` on worker changed from `none on /mnt/dags type tmpfs (ro,relatime)` [empty] to `/dev/sde on /mnt/dags type ext4 (ro,relatime,discard,errors=remount-ro,data=ordered)` containing the real files (.gitkeep, __pycache__, _common/, csv_ingest_customers.py, smoke_kubernetes_pod.py).
  implication: The bind mount reattached successfully on a single `docker restart` — confirms the fix_rationale's mechanism and resolves the previously-open blind_spot about whether reattachment would work cleanly.

- timestamp: 2026-08-14T20:05Z
  checked: Airflow pod status post-restart (kubectl get pods -o wide) and dag-processor pod's own /opt/airflow/dags view
  found: airflow-api-server and airflow-dag-processor (both on worker) restarted as part of the node restart (RESTARTS incremented) and came back Running with /opt/airflow/dags now populated with the real DAG files, matching the node-level mount. Two new task-executor pods immediately appeared: csv-ingest-customers-resolve-window-eqhju2yo scheduled onto airflow-platform-worker (fixed node), csv-ingest-customers-wait-for-files-d9ixmtw4 scheduled onto airflow-platform-worker2 (untouched, still-broken node) — confirming the residual risk flagged above is not just theoretical.
  implication: DAG processor immediately began re-parsing (no manual trigger needed) and the scheduler immediately began dispatching new task attempts for the previously-stuck DagRun — strong early signal the fix worked. Needed to verify the worker2-scheduled pod didn't silently fail before concluding safety.

- timestamp: 2026-08-14T20:06-20:08Z
  checked: Outcome of both new task pods (states-for-dag-run, scheduler logs, kubelet logs on worker2 for the wait_for_files pod's volume mounts, direct read of installed LocalDagBundle source, cross-check of worker2's mount via the already-running scheduler pod's own /opt/airflow/dags view — no new resources created on worker2)
  found: resolve_window (operator=_PythonDecoratedOperator, a `@task`-decorated TaskFlow function defined inline in the DAG file) succeeded on the FIXED node (worker). wait_for_files reached up_for_reschedule (a legitimate sensor-poke outcome, not a crash/failure state) despite running on the STILL-BROKEN worker2 node; kubelet logs confirm its `dags` hostPath volume WAS mounted (no mount error), and cross-checking the scheduler pod's own long-running view of /opt/airflow/dags on worker2 confirms that mount is genuinely still empty. LocalDagBundle source (dag_processing/bundles/local.py) confirms it reads purely from the local configured path with no network fallback. Zero failed task instances anywhere in the following 12+ minutes.
  implication: wait_for_files apparently did not need the local DAG file to execute correctly — consistent with it being a predefined operator/sensor class reconstructable from serialized DAG metadata via the API server, unlike inline `@task` TaskFlow functions which must re-import the actual source file. This means the residual risk from worker2/control-plane's broken mount is real but PARTIAL: it endangers TaskFlow-decorated tasks specifically, not all task types. Correctly characterized as "nothing has actually failed" rather than "verification surfaced something needing worker2/control-plane action" — continued monitoring rather than escalating a checkpoint, per the constraint that only ACTUAL required action on those nodes should trigger a stop.

- timestamp: 2026-08-14T20:11-20:13Z
  checked: DagModel.is_stale for both DAGs, dag_run.last_scheduling_decision across the metastore (most recent non-null rows), full task-level states for the originally-stuck DagRun, scan for any failed task instances in a 12-minute post-fix window, SchedulerJob heartbeat, backlog size
  found: is_stale=False for csv_ingest_customers AND smoke_kubernetes_pod (last_parsed_time 20:11:41Z). The stuck DagRun (scheduled__2026-08-14T02:50:00+00:00) reached state=success with all 6 tasks (discover, resolve_window, wait_for_files, build_ingest_args, ingest, aggregate_receipts) individually success. Scheduler had already autonomously advanced to the next backlog DagRun (02:51:00, state=running, last_scheduling_decision 20:11:52 — seconds-fresh) with zero scheduler-side intervention from this session. Zero failed task instances in the 12-minute window. SchedulerJob id=48 heartbeat fresh (20:12:30). ~52 DagRuns still queued, draining normally (full drain not attempted — not required per plan).
  implication: Fix fully verified per every criterion in the user's checkpoint plan: mount reattached, is_stale cleared for both DAGs (cluster-wide symptom resolved, not just csv_ingest_customers-scoped), scheduler resumed with no manual scheduler-side action, and the originally-stuck DagRun reached SUCCEEDED well within a reasonable wait (~6 minutes post-restart). Ready for human end-to-end confirmation.

## Resolution
<!-- OVERWRITE as understanding evolves -->

root_cause: A Docker Desktop/WSL2-level infrastructure event at 2026-08-14T16:58:55Z restarted every container on every kind node simultaneously (confirmed cluster-wide, not Airflow-specific). The DAGs hostPath bind mount (WSL2 host `airflow/dags/` -> kind node `/mnt/dags` -> pod `/opt/airflow/dags`) failed to reattach on all 3 nodes after that restart; Docker silently fell back to an empty, read-only tmpfs at the mount point instead of re-establishing the real bind mount. With `/opt/airflow/dags/` empty, the DAG processor discovers zero files, never re-parses any DAG, and never clears `DagModel.is_stale` (confirmed True for csv_ingest_customers AND an unrelated control DAG — this is cluster-wide, not scoped to csv_ingest_customers). The scheduler's `DagRun.get_running_dag_runs_to_examine()` query filters `WHERE DagModel.is_stale == false()`, so it silently and permanently excludes ALL running DagRuns for ALL dag_ids from ever being scheduled again — with zero exceptions or errors logged anywhere, since it's a plain SQL WHERE-clause exclusion, not a crash. The earlier KubernetesJobWatcher watch-loop noise (hypothesis A) was confirmed to be normal reconnect behavior for not-yet-acknowledged terminal pods, unrelated to the actual blocker. The scheduler pod's involuntary restart at 17:00:39 (part of the same whole-cluster event) correctly ran orphan-adoption logic (explaining the simultaneous resolve_window/wait_for_files failure at 17:00:39) but could not and did not fix the underlying stall (hypothesis B), because the real defect is a Docker-level mount attachment issue external to any Airflow process's memory or state.
fix: APPLIED in two stages, both explicitly user-approved. Stage 1 (scoped): `docker restart airflow-platform-worker` ONLY — the node hosting `airflow-dag-processor` and `airflow-api-server`. This alone unblocked scheduling cluster-wide, because the scheduler's blocking mechanism (`DagModel.is_stale`) is cleared by the DAG PROCESSOR re-parsing successfully, which only required ITS OWN node's mount to be fixed — the scheduler consumes `is_stale` from the DB, not a local filesystem read, so it resumed regardless of which node it runs on. Stage 2 (full closure, follow-up user decision after independent orchestrator verification): `docker restart airflow-platform-worker2` then `docker restart airflow-platform-control-plane`, run sequentially with health verification between each, closing the residual TaskFlow-task risk the scoped fix had left open on those two nodes.
verification: (1) `docker exec airflow-platform-worker` confirmed `/mnt/dags` changed from `none on /mnt/dags type tmpfs (ro,relatime)` [empty] to `/dev/sde on /mnt/dags type ext4 (ro,relatime,...)` with real files (csv_ingest_customers.py, smoke_kubernetes_pod.py, _common/, .gitkeep) — genuine bind-mount reattachment, not another tmpfs fallback. (2) Confirmed identically from inside the dag-processor pod's own mounted view (`/opt/airflow/dags`). (3) `DagModel.is_stale` flipped to `False` for both `csv_ingest_customers` and the control DAG `smoke_kubernetes_pod`, with fresh `last_parsed_time` (20:11:41Z, ~6 min after restart). (4) The originally-stuck DagRun (`scheduled__2026-08-14T02:50:00+00:00`) reached `state=success` with ALL 6 tasks (discover, resolve_window, wait_for_files, build_ingest_args, ingest, aggregate_receipts) individually `success` — full DagRun completion, not partial. (5) Scheduler (still running on untouched worker2, confirmed via fresh heartbeat job id=48) autonomously advanced to and is actively processing the next backlog DagRun (`02:51:00`, running) with zero manual scheduler-side intervention — proves the fix is general, not a one-off. (6) Zero task-instance failures anywhere in a 12+ minute post-fix window, despite task pods landing on BOTH the fixed node (worker) and the still-broken node (worker2) — see residual-risk note below. (7) Backlog actively draining (~52 DagRuns still queued at last check — full drain not required for this session per user's plan; scheduling-resumption + one full SUCCEEDED run were the acceptance bar, both met). RESIDUAL RISK — SUBSEQUENTLY CLOSED (stage 2, orchestrator + user, ~20:15-20:22Z): Orchestrator independently re-verified stage-1's fix before recommending stage 2: the originally-stuck DagRun confirmed `state=success`; scheduler confirmed autonomously advancing the backlog (`02:50 -> 02:52 -> 02:53` with zero manual intervention); a fresh `meta.ingestion_runs` row (`run_id=5127, status=SUCCEEDED, started_at=2026-08-14 20:09:50`) confirmed timestamped after Task 2's Vault reinstall (~18:22Z) — directly satisfying plan 05-06 Task 2's own acceptance criterion using a real, credential-backed pipeline run. One residual nuance surfaced during re-verification: re-running `pytest tests/e2e/vault/test_airflow_backend.py -q -m cluster` still failed on `test_dag_still_resolves_its_connection_and_runs` — root-caused to backlog DEPTH (the currently-running DagRun is still hours of logical-date-minutes behind wall-clock, so it has no reason to notice a freshly-uploaded test marker file), NOT a recurrence of the mount/is_stale bug — explicitly distinguished from this debug session's actual root cause and left as an accepted, self-resolving condition (forcing it faster would mean bulk-clearing the backlog, the same class of action already denied by the permission classifier once this project's session, per STATE.md — not repeated without further explicit authorization).

With the mount/is_stale bug independently reconfirmed fixed, user approved closing the flagged residual risk fully: `docker restart airflow-platform-worker2` — mount confirmed reattached (`/dev/sde ext4`, real DAG files, was empty tmpfs); Airflow pods on that node (scheduler, triggerer) recovered to Ready; Vault (co-located, incidentally bounced) came back `Sealed: true` as expected per this project's no-auto-unseal D-02 design, restored via `make vault-unseal` to `Sealed: false` with the SAME Cluster ID as before (`243eafb8-...`) — confirms genuine storage recovery, not reinitialization/data loss. Then `docker restart airflow-platform-control-plane` — mount confirmed reattached identically; brief transient `Forbidden` error from `kubectl` while the API server itself was mid-restart (expected, self-resolved within ~15s); all control-plane and kube-system components (etcd, kube-apiserver, kube-controller-manager, kube-scheduler, coredns x2, kindnet, kube-proxy, ingress-nginx-controller, local-path-provisioner) confirmed back to `Running`/`Ready` afterward. Final cluster-wide pod sweep: only pre-existing, unrelated stale pods remained (8 old `Error`/`Unknown` pods in namespace `etl`, ages 7-8h — predate this debug session, out of scope, not touched). All 3 kind nodes now have a genuinely reattached DAGs mount; the TaskFlow-task residual risk is fully closed, not merely accepted.
files_changed: []
