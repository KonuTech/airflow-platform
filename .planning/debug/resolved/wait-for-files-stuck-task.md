---
status: resolved
trigger: "csv_ingest_customers's wait_for_files (and occasionally discover) tasks intermittently get stuck in up_for_retry with no live pod, even after the prior fix in .planning/debug/resolved/airflow-scheduler-stuck-tasks.md (commit 6ea4129: kpo.py on_finish_action=delete_pod + ingest max_active_tis_per_dag 5->1). Since max_active_runs=1, a single stuck task blocks all new file discovery until manually cleared."
created: 2026-08-16
updated: 2026-08-16T20:55:00Z
resolution:
  root_cause: "vault-0 restarted (~19:06-19:10 UTC, same window as the broader host-level/kind-node disruption already flagged in Symptoms) and came back Sealed. This project's Vault deployment is deliberately single-key Shamir + file storage with no auto-unseal (D-02, docs/adr, scripts/vault-unseal.py, tests/e2e/vault/test_unseal_survives_restart.py) -- a restart ALWAYS reseals it and requires a manual `make vault-unseal` afterward, and nobody ran it after this restart. While sealed, Airflow's VaultBackend secrets backend cannot resolve Connections (including `minio_default`, used by wait_for_files's S3KeySensor). Airflow's secrets-backend chain surfaces this failure as a plain 'connection not found' (HTTP 404), indistinguishable at the log level from a genuinely-missing connection. The failing task correctly reports its own failure via the API and exits 0, which is why the pod shows Succeeded/no-crash while the TI still lands in up_for_retry -- not an executor/K8s bug, and not literally an infinite hang: the TI is honoring its own exponential-backoff retry_delay, but every retry keeps failing for the same reason (Vault still sealed) until max_tries exhausts into FAILED. Combined with max_active_runs=1, each Vault-sealed incident blocks all new file discovery for its entire multi-run retry-exhaustion window, matching every symptom reported. Directly mirrors the same class of incident in .planning/debug/resolved/dagrun-scheduler-stall.md (2026-08-14), where the identical host-level restart also resealed Vault."
  fix: "Ran the project's own documented, tested recovery procedure: `python scripts/vault-unseal.py` (equivalently `make vault-unseal`) from the repo root, using the existing `.secrets/vault-init.json` key material. No code change -- this is an operational recovery action for a known, deliberately-accepted local-dev-only Vault limitation (no auto-unseal; see scripts/vault-unseal.py's own docstring and SEC-14 production-substitution documentation, plan 05-05)."
  verification: "(1) Script reported 'unsealed' (not 'initialized') -- confirms it used the existing key, not a fresh init. (2) `vault status` post-fix: Sealed=false, Cluster ID=243eafb8-3395-e561-671c-1164e094518e -- the SAME Cluster ID recorded in the prior 2026-08-14 incident's resolution notes, proving genuine storage recovery, not data loss/reinitialization. (3) `airflow connections get minio_default` now succeeds (previously 'Connection not found.' on 5/5 consecutive attempts). (4) The previously-stuck TaskInstance (csv_ingest_customers.wait_for_files, run scheduled__2026-08-16T19:42:00) retried automatically at its own already-computed next_retry_datetime (20:08:13, ~40s after the fix, with zero manual TI-state intervention) and succeeded within 13s. (5) The full DagRun then progressed autonomously through discover -> build_ingest_args -> 6x ingest (max_active_tis_per_dag=1, sequential) -> aggregate_receipts, reaching DagRun state=success by 20:10:58 -- full completion, not partial. (6) Confirmed NOT a one-off: the freed max_active_runs=1 slot let subsequent scheduled ticks (20:10, 20:11) each complete a full DagRun within their own 1-minute window, with 20:12 running normally at last check -- the DAG is cycling normally again. (7) A second, older, pre-existing up_for_retry TI (discover, run scheduled__2026-08-14T01:32:00) was found but is inert/cosmetic: its own DagRun is already DagRun.state=success, so it holds no scheduling slot and blocks nothing current -- left untouched (out of scope, matches the Symptoms note flagging it as 'likely inert/leftover from an earlier incident')."
  files_changed: []
  independent_confirmation: "Orchestrator-side confirmation, ~40min after the agent's self-verification: (1) DagRun state query showed 11 consecutive `csv_ingest_customers` successes since the unseal (20:10 through 20:32, plus one normally-running), zero new stuck TIs (the only up_for_retry/queued rows left are the currently-in-flight 20:14 discover and the pre-existing inert 2026-08-14 discover, both expected/benign). (2) Fresh full run of `tests/e2e/observability/` (uv run --frozen --group cluster pytest tests/e2e/observability/ -m cluster -q): **6 passed, 1 failed** in 1260s. All 3 of the previously-failing stuck-task-pattern tests now pass: test_forced_freshness_breach_delivers_a_real_webhook_post, test_ingest_pod_traceparent_matches_persisted_trace_id, test_ingest_pod_dag_context_matches_persisted_lineage_row. This confirms the Vault-reseal root cause and fix. The 4th originally-failing test, test_prometheus_scrapes_dataplat_metrics_via_the_otel_collector, still fails but now for a DIFFERENT, unrelated reason -- discovery/ingestion now completes (a SUCCEEDED run was produced), but Prometheus returned an empty result vector for the `runs_started` query within the test's 180s timeout. This is a distinct issue (OTel collector scrape / ServiceMonitor / possibly-stale csv_processor_image Variable per the test's own failure message) outside this session's scope -- logged as a new follow-up, not reopened here."
---

## Symptoms

**Expected behavior:** A scheduled `csv_ingest_customers` DagRun's `wait_for_files`/`resolve_window` sensor tasks poke/defer normally and eventually succeed once matching files are discoverable, letting the DagRun progress through `discover` → `build_ingest_args` → `ingest` → `aggregate_receipts` to a terminal state, freeing the `max_active_runs=1` slot for the next scheduled tick. `tests/e2e/observability/` should pass cleanly and repeatably.

**Actual behavior:** `wait_for_files` (and occasionally `discover`) tasks intermittently enter `up_for_retry` with no live pod backing them and stay stuck there — this is a *residual* recurrence of a class of issue the prior debug session (`.planning/debug/resolved/airflow-scheduler-stuck-tasks.md`) explicitly flagged as NOT fully eliminated by its fix (root cause there: node CPU budget exhaustion + `airflow-xcom-sidecar` never terminating, compounded by KPO `startup_timeout_seconds` racing tight CPU). Two consecutive full `tests/e2e/observability/` suite runs today (2026-08-16) hit the identical 4 test failures (`test_forced_freshness_breach_delivers_a_real_webhook_post`, `test_prometheus_scrapes_dataplat_metrics_via_the_otel_collector`, `test_ingest_pod_traceparent_matches_persisted_trace_id`, `test_ingest_pod_dag_context_matches_persisted_lineage_row`), all failing with "discovery never registered it" traced back to this same stuck-task pattern.

Freshly reproduced live at session start (2026-08-16T19:56 UTC): `DagRun scheduled__2026-08-16T19:42:00` has `wait_for_files` stuck `up_for_retry`, no matching pod in `kubectl -n airflow get pods` or `kubectl -n etl get pods`. A second, older stuck TI also present: `discover` on `scheduled__2026-08-14T01:32:00` (up_for_retry) — likely inert/leftover from an earlier incident, not yet confirmed related.

**New data point not present in the prior debug session:** the `airflow-scheduler` pod itself (container `scheduler`, and `scheduler-log-groomer`) restarted twice around 2026-08-16T19:06:29–19:08:33 UTC, `exitCode: 255, reason: Unknown` — roughly matching the window in which the currently-stuck `scheduled__2026-08-16T19:42:00` run's task got stuck. Not yet established whether this is causally connected (e.g. OOM-kill, crash mid-dispatch orphaning the TI in a transient state) or coincidental. This is a genuinely new signal — the prior session never observed the scheduler process itself dying.

**Error messages:** None surfaced yet for the current occurrence — TaskInstance state shows `up_for_retry` with no exception recorded in the (not yet pulled) task log. Scheduler container's own last termination reason is reported as generic `Unknown` by Kubernetes (exitCode 255) — needs `kubectl -n airflow logs --previous` to get the real reason (OOMKilled vs crash vs liveness-probe kill).

**Timeline:** First flagged as a known residual risk in the prior debug session (resolved 2026-08-16, ~17:15 UTC) — explicitly documented as NOT fully eliminated by that session's fix, pending confirmation. Reproduced twice more today after that fix was live: two full `tests/e2e/observability/` suite runs (each ~9m46s) both hit it, plus this fresh live occurrence at 19:56 UTC. Cluster also underwent a host-level freeze/restart earlier in the day (unrelated investigation, ruled out as memory-pressure-caused per HANDOFF.json) that required `docker restart` on kind nodes to reattach the DAGs hostPath mount — worth double-checking that recovery was actually complete and durable, not a contributing factor here.

**Reproduction:** Not reliably on-demand; occurs intermittently under `tests/e2e/observability/` full-suite load (2/2 recent full-suite runs hit it) and apparently also under lighter/idle scheduling (the 19:42 UTC occurrence happened outside an active test run). To observe:
```
kubectl -n airflow exec deploy/airflow-scheduler -c scheduler -- python3 -c "
from airflow.models import DagRun, TaskInstance
from airflow.utils.session import create_session
with create_session() as session:
    tis = session.query(TaskInstance).filter(TaskInstance.dag_id=='csv_ingest_customers', TaskInstance.state.in_(['up_for_retry','queued','running'])).all()
    for t in tis:
        print(t.run_id, t.task_id, t.state, t.try_number, t.max_tries, t.queued_dttm)
"
```
Cross-reference against `kubectl -n airflow get pods` / `kubectl -n etl get pods` for absence of a matching live pod.

## Evidence

- timestamp: 2026-08-16T20:00:00Z
  checked: scheduler pod --previous container logs (restart window 19:06-19:08 UTC) and current pod events
  found: Scheduler container exited via graceful SIGTERM (signal 15), not OOM. Pod events show "MountVolume.SetUp failed ... failed to fetch token: serviceaccounts \"airflow-scheduler\" is forbidden ... no relationship found between node and this object" and repeated "Pod sandbox changed, it will be killed and re-created." -- consistent with node/containerd-level flakiness during that window, not an application bug. On restart, the scheduler's orphan-adoption logic correctly failed/retried tasks whose pods died mid-event (e.g. csv_ingest_customers.wait_for_files run scheduled__2026-08-16T18:25:00 -> up_for_retry). This restart is a symptom of the same underlying disruption, not the cause of the CURRENTLY stuck 19:42 run (43+ min later, no new scheduler restart at that time).
  implication: Scheduler restart at 19:06-19:08 is a red herring for the *current* stuck TI; need to trace the 19:42 run's own pod lifecycle directly.

- timestamp: 2026-08-16T20:00:30Z
  checked: DB query for scheduled__2026-08-16T19:42:00's wait_for_files TI -- pod name csv-ingest-customers-wait-for-files-5k1tkaex, try_number=2/max_tries=2, queued 19:51:22, start 19:51:28, end 19:51:33
  found: Pod already deleted (delete_worker_pods=True, on_finish_action semantics apply to worker pods too). No persisted task logs anywhere (scheduler /opt/airflow/logs empty, no remote logging configured, only the triggerer has a logs PVC) -- task-level stdout is unrecoverable after pod deletion for this executor config.
  implication: Must reconstruct outcome from scheduler + api-server HTTP access logs instead of the task's own log.

- timestamp: 2026-08-16T20:01:00Z
  checked: current (non---previous) scheduler log for pod csv-ingest-customers-wait-for-files-5k1tkaex, 19:51:22-19:51:43
  found: Pod went Pending -> Running -> **Succeeded** (exit 0) at 19:51:43.087. KubernetesExecutor's KubernetesResults for this event carried state=None (ambiguous), so `_change_state` (kubernetes_executor.py) fell back to reading the TI's *actual current DB state*, which by then was already up_for_retry. The scheduler then logged "Received executor event with state up_for_retry" -- i.e. the pod process itself exited cleanly (0) after having already reported a task failure via the API, not a pod/executor-level crash.
  implication: The failure happened INSIDE the task process (it called the failure-report API successfully, then exited 0) -- confirms this is an application-level failure inside the sensor's execute(), not an infra/executor bug.

- timestamp: 2026-08-16T20:02:30Z
  checked: airflow-api-server HTTP access log for the same pod/TI window (client 10.244.1.17, ti_id=01a00c19-0860-7183-a0f8-8fad62e01b8a)
  found: "GET /execution/connections/minio_default ... status_code=404" at 19:51:33.254197Z, immediately followed (297ms later) at 19:51:33.551246Z by "Task instance state updated ... new_state=up_for_retry". S3KeySensor's `aws_conn_id="minio_default"` lookup returned Not Found.
  implication: wait_for_files fails because the `minio_default` Airflow connection cannot be resolved -- need to determine WHY the connection lookup 404s given it succeeds the overwhelming majority of the time (2540/2551 historical runs succeeded).

- timestamp: 2026-08-16T20:03:30Z
  checked: `airflow connections get minio_default` from the scheduler pod, retried 5x; secrets backend config (`airflow.providers.hashicorp.secrets.vault.VaultBackend`, url=http://vault.vault.svc.cluster.local:8200)
  found: "Connection not found." on every one of 5 consecutive attempts -- currently, persistently reproducible, not flaky right now.
  implication: The connection lookup is failing NOW too -- points at the secrets backend (Vault) itself being unavailable, not a one-off race.

- timestamp: 2026-08-16T20:03:50Z
  checked: `kubectl -n vault get pods` / `vault status` inside vault-0
  found: vault-0 RESTARTS=2 (54m ago). `vault status`: Initialized=true, **Sealed=true**, Seal Type=shamir, Total Shares=1, Threshold=1, Storage Type=file (no auto-unseal).
  implication: ROOT CAUSE CONFIRMED. Vault is sealed after a pod restart ~54 min ago (same window as the broader host-level/kind-node disruption already flagged in Symptoms). Per this project's deliberate D-02 design (scripts/vault-unseal.py, docs/adr and tests/e2e/vault/test_unseal_survives_restart.py), Vault always re-seals on restart (single-key Shamir, file storage, no auto-unseal) and requires a manual `make vault-unseal` afterward -- nobody ran it after this restart. While sealed, VaultBackend.get_connection() cannot reach the KV store; Airflow's secrets-backend chain surfaces this as a plain "connection not found" (404), which is indistinguishable from a genuinely-missing connection at the HTTP/log level. This exactly matches the historical incident in .planning/debug/resolved/dagrun-scheduler-stall.md (2026-08-14), where the SAME class of host-level restart also resealed Vault (fixed there via `make vault-unseal`) alongside a separate DAGs-mount issue.

- timestamp: 2026-08-16T20:05:19Z
  checked: TaskInstance.next_retry_datetime() computed properly (with the real serialized DAG's task attached: retries=2, retry_delay=5min, retry_exponential_backoff=2.0) for the currently up_for_retry wait_for_files TI
  found: next_retry_datetime = 2026-08-16 20:08:10 (still in the future relative to 20:05:19 "now") -- i.e. this TI is NOT permanently/pathologically stuck in the Airflow-scheduler sense; it is correctly waiting out its own exponential-backoff window and WILL be retried automatically. It will very likely fail again on retry (since Vault is still sealed), continuing the backoff cycle until max_tries is exhausted and the TI (and DagRun, and downstream discover) finally reaches a terminal FAILED state.
  implication: The "stuck with no live pod" appearance is real from a user's perspective (nothing visibly happening for many minutes) but is actually normal exponential-backoff retry behavior compounding with `max_active_runs=1`: every Vault-sealed incident now costs one DagRun's entire retry-exhaustion window (tens of minutes) of total discovery blockage, repeating on every subsequent scheduled tick until Vault is unsealed. This reframes the bug from "Airflow/K8s executor defect" (the prior debug session's framing) to "Vault has no auto-recovery after a restart, and there is no monitoring/alerting to catch it quickly."

## Eliminated

- hypothesis: The scheduler pod's own restart (19:06-19:08 UTC, exit 255/signal 15) directly orphaned or corrupted the CURRENT (19:42) DagRun's wait_for_files task, mirroring the executor-state-corruption angle explored in the prior debug session
  evidence: The 19:42 run's wait_for_files task was queued/started/ended entirely at 19:51:2x-19:51:33, over 43 minutes AFTER the last scheduler restart; no scheduler restart occurred in that window. Its pod ran to completion (Succeeded, exit 0) and the scheduler processed its terminal event normally within the same second -- no orphan-adoption code path involved for this specific TI.
  timestamp: 2026-08-16T20:02:00Z

- hypothesis: A genuine Kubernetes executor/watcher bug (duplicate/ambiguous state reporting, akin to the xcom-sidecar leak fixed in the prior session) is causing the pod to finish successfully but get misreported as a failure
  evidence: The `state=None` fallback in `_change_state` (kubernetes_executor.py) is a documented, intentional code path (query the TI's real DB state when the pod's own exit status doesn't map cleanly) -- and the DB state it found (up_for_retry) was ALREADY correct at that point, because the task process itself had already PATCHed its own failure via `/execution/task-instances/.../state` before exiting 0. This is expected behavior given an application-level failure inside the task, not an executor defect.
  timestamp: 2026-08-16T20:02:45Z

## Current Focus

hypothesis: "CONFIRMED and FIXED -- see Resolution. Fix applied (scripts/vault-unseal.py) and fully self-verified: the originally-stuck DagRun reached success, and two subsequent scheduled ticks also completed cleanly. Awaiting human end-to-end confirmation before archiving."
test: "n/a -- self-verification complete, see Resolution.verification."
expecting: "User confirms the DAG continues cycling normally in their own observation, and that re-running `tests/e2e/observability/` no longer hits the 4 previously-failing tests that traced back to this stuck-task pattern."
next_action: "AWAITING HUMAN VERIFICATION. If confirmed: archive this session (move to resolved/, commit, append knowledge-base entry -- note for that entry: Vault reseal-after-restart with no auto-unseal, surfacing as an Airflow connection 404, correlated with host-level/kind-node disruptions; recovery is `make vault-unseal`). If the DAG stalls again for a DIFFERENT reason, or Vault reseals again soon, resume investigation -- worth flagging to the user as a follow-up (not auto-implemented, since it's a scope/design decision, not a bug fix): consider a lightweight periodic vault-unseal healthcheck/alert given this is now a 2nd recurrence of the same operational gap (see .planning/debug/resolved/dagrun-scheduler-stall.md, 2026-08-14)."
