"""Shared, non-business-logic DAG-authoring helpers (plan 04-07).

Empty marker beyond this docstring — callers import from ``_common.kpo``
directly, e.g. ``from _common.kpo import common_kpo_kwargs``.
"""

from __future__ import annotations
